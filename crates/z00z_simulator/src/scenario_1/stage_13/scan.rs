pub(crate) fn source_shape_note() -> &'static str {
    "stage13 hjmt examples stay on storage-owned settlement APIs and reject physical-layout authority names"
}

pub(crate) const FORBIDDEN_SOURCE_TERMS: &[&str] =
    &["ForestTreeId", "TreeId", "namespace bytes", "RedB row keys"];
