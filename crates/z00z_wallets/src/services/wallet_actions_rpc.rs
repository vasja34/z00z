// Intentionally empty.
//
// Sensitive wallet RPC actions must route through the live session/runtime
// guards instead of dormant reachability placeholders. The file remains in the
// module graph only as an inactive extension seam.
