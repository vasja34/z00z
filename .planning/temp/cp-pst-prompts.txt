./scripts/remove_target.sh crates
./scripts/remove_target.sh .
./scripts/full_verify.sh --max-safe-run

cargo clean
time cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test -p z00z_simulator --test test_pipeline_genesis_tx --release --features test-fast --features wallet_debug_dump
cargo test -p z00z_simulator --test test_scenario1_unified_gate --release --features test-fast --features wallet_debug_dump


time cargo test -p z00z_simulator --tests --release --features test-fast --features wallet_debug_dump

@agentPlugins 
/home/vadim/.config/Code/agentPlugins/github.com/github/awesome-copilot/skills/doublecheck/SKILL.md

gem-team
project-planning
structured-autonomy


---------------------
0️⃣ 🟥
/Plan-Workflow 
current_spec = "specs/014-z00z-storage/snapshot-storage-spec.md"
workflow_goal = "prorabotat ewe raz snapshot storage spec"
output_workflow = specs/014-z00z-storage/snapshot-storage-workflow.md

1️⃣ 🟧
/Review-Plan-Workflow
current_workflow = specs/014-z00z-storage/snapshot-storage-workflow.md

---------------------
2️⃣ 🟩
/Plan-Tasks
current_spec = specs/014-z00z-storage/snapshot-storage-workflow.md
output_plan = specs/014-z00z-storage/snapshot-storage-plan.md
workflow_goal= ""

3️⃣ 🟦
/Review-Plan-Tasks 
output_plan = specs/014-z00z-storage/snapshot-storage-plan.md
 
---------------------
4️⃣ 🟪
/Implement-Tasks 
current_spec = specs/014-z00z-storage/snapshot-storage-plan.md
current_task="⚙️ Phase 1."

5️⃣ ⬛️
/Review-Implement-Tasks 
current_spec = specs/014-z00z-storage/snapshot-storage-plan.md
current_task="⚙️ Phase 1."

---------------------
6️⃣ 🔴 
Plan-E2E-Tests ==> /Plan-Workflow 
workflow_goal= """define smart, complete, end-to-end integration tests and realistic examples that prove what the workflow does, how it behaves, which invariants it must preserve, and which failures it must reject.
The result must be directly usable by another engineer or agent to implement E2E coverage without guessing scenario boundaries, success criteria, or test anchors. Derive the critical user journeys, state transitions, proof paths, and failure paths that must be verified end to end.

The specification must explicitly address all of the following when relevant:
- which end-to-end behaviors must be proven;
- which integration paths are critical;
- which examples are needed to demonstrate successful execution;
- which negative scenarios must prove rejection or failure handling;
- which cryptographic invariants, proofs, commitments, roots, or signatures must be observed;
- which assertions prove correctness;
- what each test or example is meant to demonstrate.
- what measurable success or failure conditions make the scenario pass."""


-------------------------
🔴 /specs/014-z00z-storage/E2E-EXAMPLES-plan.MD"
prover chto etot spec polnostju implementirovan.
Your role: crypto code analytic and code reviewer; verify the code generated - critical, constructive, and objective. Verify if there are any logical errors, pitfalls, unresolved issues, consistency, quality, integrity of structures, criptographic correctness,full complince with checklist. 
FIX all found issues and warnings in YOLO mode

-------------------------
scripts/check_no_legacy_deps.sh
scripts/check_tx_perf.py



 AGENTS:
gem-documentation-writer - readme 
gem-researcher
gem-planner


-------------------------
prover kogo iz scripts/runnable_targets.toml enabled mozhno sdelat true
zapusti ./scripts/full_verify.sh --max-safe-run c YOLO mode i fix all erros i warnings

/crates/z00z_simulator/bin/scenario_1.rs - eneble 

./crates/z00z_wallets/scripts/z00z_wallet_egui.sh
exec cargo run --features egui --bin z00z_wallet_egui

[[target]]
id = "wallet_egui"
package = "z00z_wallets"
kind = "bin"
name = "z00z_wallet_egui"
enabled = true
features = ["test-fast", "egui"]
timeout_sec = 300
stop_after_sec = 5
note = "Enabled for smoke verification; it launches a desktop GUI and is auto-stopped after a 5-second smoke run."

-------------------------


**Critical DA Flow (MUST):**

- created_delta contains (asset_id, leaf_hash, da_locator)

- ==Receiver fetches full leaf body from DA using leaf_hash/locator==

- ==Without DA retrieval: receiver sees asset exists but cannot decrypt (soft burn)==

- Mitigation: Validator sampling (random 10 leaves per checkpoint), multi-backend redundancy

  ==mne kazhetsja tyt oshibka mi govorili o tom chto DA soderzhit tolko podpisi agregatora i validatorov a chto JMT derevo s leafs dostupno kak vneshnij service==


rename owner to wallet
    #[error("owner handle changed")]
    Owner,

--------------

Импорты в Rust: импорты из одного крейта/модуля группировать в один "use { ... }", не разносить по отдельным строкам.
--------------

cargo test -p z00z_storage --lib && cargo test -p z00z_wallets --lib && cargo test -p z00z_storage --test assets_suite && cargo test -p z00z_wallets --tests && cargo test -p z00z_simulator --lib && cargo test -p z00z_simulator --test test_stage4_digest --release --features test-fast --features wallet_debug_dump && cargo test -p z00z_simulator --test test_stage4_gates --release --features test-fast --features wallet_debug_dump && cargo test -p z00z_simulator --test test_stage6_checkpoint --release --features test-fast --features wallet_debug_dump && cargo test -p z00z_simulator --test test_pipeline_genesis_tx --release --features test-fast --features wallet_debug_dump && cargo test -p z00z_simulator --test test_scenario1_unified_gate --release --features test-fast --features wallet_debug_dump


