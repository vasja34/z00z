---
phase: 026-crypto-audit-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_core/src/assets/definition.rs
  - crates/z00z_core/src/assets/assets_config.rs
  - crates/z00z_core/src/hashing.rs
autonomous: true
requirements:
  - PH26-ASSET-ID
must_haves:
  truths:
    - Asset definitions cannot silently accept externally supplied identifiers on production paths.
    - Config-driven and runtime-created definitions resolve through one canonical identity rule.
    - Test-only asset-id hashing never appears in a production code path.
  artifacts:
    - crates/z00z_core/src/assets/definition.rs exposes one canonical definition-id derivation or validation seam.
    - crates/z00z_core/src/assets/assets_config.rs routes loaded definitions through the canonical identity seam.
    - crates/z00z_core/src/hashing.rs keeps test-only asset-id aliases out of production call paths.
  key_links:
    - Asset-definition constructors and config loaders use the same framed payload rule.
    - Canonical definition identity is stable before registry insertion or wire rehydration.
    - Genesis runtime integration is completed in plan 03, and trusted wire rehydration is completed in plan 04, instead of being left implicit here.
---
<!-- markdownlint-disable MD031 MD033 MD041 -->

<objective>
  Establish one canonical asset-definition identity seam for `z00z_core` and route the immediate assets-layer construction paths through it so later genesis-runtime and wire tasks reuse the same rule instead of inventing parallel derivation logic.

  Purpose: Close the under-bound definition-identity blocker from `026-FUSION.md` and create a reusable foundation for later registry, genesis, and wire tasks.
  Output: A canonical definition-id derivation or validation helper plus migrated assets-layer construction paths, with follow-on genesis integration made explicit in plan 03 and trusted wire rehydration made explicit in plan 04.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/026-crypto-audit-core/026-CONTEXT.md
@.planning/phases/026-crypto-audit-core/026-FUSION.md

<interfaces>
From `crates/z00z_core/src/assets/definition.rs`:

```rust
pub struct AssetDefinition {
    pub id: [u8; 32],
    pub class: AssetClass,
    pub name: String,
    pub symbol: String,
    pub decimals: u8,
    pub serials: u32,
    pub nominal: u64,
    pub domain_name: String,
    pub version: u8,
    pub crypto_version: u8,
    pub policy_flags: u8,
    pub metadata: Option<BTreeMap<String, String>>,
}
```

From `crates/z00z_core/src/hashing.rs`:

```rust
pub type AssetIdHasher = DomainHasher<AssetIdHashDomain>;
pub type TestAssetIdHasher = DomainHasher<TestAssetIdDomain>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create one canonical asset-definition identity seam</name>
  <files>crates/z00z_core/src/assets/definition.rs
crates/z00z_core/src/hashing.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
.planning/phases/026-crypto-audit-core/026-FUSION.md
crates/z00z_core/src/assets/definition.rs
crates/z00z_core/src/hashing.rs</read_first>
  <behavior>
    - Test 1: Two definitions with identical framed fields derive the same `id`.
    - Test 2: Changing `class`, `symbol`, `policy_flags`, or ordered metadata changes the derived `id`.
    - Test 3: Production constructors reject or overwrite caller-supplied IDs that do not match the canonical framed payload.
  </behavior>
  <action>Create one canonical definition-identity helper in `definition.rs` that frames and hashes the full payload fields `class`, `name`, `symbol`, `decimals`, `serials`, `nominal`, `domain_name`, `version`, `crypto_version`, `policy_flags`, and deterministic metadata order. Use the production `AssetIdHasher` only. Keep `TestAssetIdHasher` available for test-only call sites but remove it from any production construction path. Ensure `AssetDefinition::new` and any equivalent public constructor validate `id` against the canonical helper or derive it internally instead of trusting the incoming `id` bytes.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/assets/definition.rs` contains one canonical helper used by public definition construction.
    - `crates/z00z_core/src/hashing.rs` still exposes `TestAssetIdHasher`, but no production file under `crates/z00z_core/src/**` uses it outside tests.
    - Definition-id tests fail when a payload field changes and pass when the full framed payload is unchanged.
  </acceptance_criteria>
  <done>Canonical definition identity is centralized and caller-controlled IDs no longer bypass it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route config and trusted construction paths through the canonical identity seam</name>
  <files>crates/z00z_core/src/assets/assets_config.rs
crates/z00z_core/src/assets/definition.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
crates/z00z_core/src/assets/assets_config.rs
crates/z00z_core/src/assets/definition.rs
</read_first>
  <behavior>
    - Test 1: Config-driven definitions always emerge with the canonical derived `id`.
    - Test 2: A mismatched incoming `id` from a trusted construction path is rejected with a typed error.
  </behavior>
  <action>Update `assets_config.rs` and any remaining trusted construction entrypoints in `definition.rs` to call the canonical identity seam from Task 1 before a definition can be returned or inserted into a registry. Keep the public API English-only and typed-error based. Do not leave a path that constructs `AssetDefinition` directly from caller-controlled `id` bytes without canonical validation.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/assets/assets_config.rs` routes loaded definitions through the canonical helper from Task 1.
    - A typed error or deterministic re-derivation prevents config-driven ID drift.
    - No trusted construction path in `definition.rs` returns a definition whose `id` was never canonicalized.
    - This plan does not implicitly treat `crates/z00z_core/src/genesis/genesis.rs` or `crates/z00z_core/src/assets/wire.rs` as already migrated; those integrations are explicit follow-on targets in plans 03 and 04.
  </acceptance_criteria>
  <done>All asset-definition construction paths inside the assets layer converge on one canonical ID rule.</done>
</task>

</tasks>

<verification>
Re-run the bootstrap gate before broader cargo validation for each task. Keep `PH26-ASSET-ID` coverage explicit in the plan summary and require two consecutive clean `GSD-Review-Tasks-Execution` runs before closure.
</verification>

<success_criteria>
`z00z_core` uses one canonical asset-definition identity seam, and no production path can create a definition with an unchecked or test-domain identifier.
</success_criteria>

<output>
After completion, create `.planning/phases/026-crypto-audit-core/026-01-SUMMARY.md`
</output>
