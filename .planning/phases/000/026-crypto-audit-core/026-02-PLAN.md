---
phase: 026-crypto-audit-core
plan: "02"
type: execute
wave: 2
depends_on:
  - 026-01
files_modified:
  - crates/z00z_core/src/assets/snapshot.rs
  - crates/z00z_core/src/assets/registry.rs
  - crates/z00z_core/src/assets/wire.rs
autonomous: true
requirements:
  - PH26-REGISTRY
must_haves:
  truths:
    - Registry integrity changes when any canonical definition payload field changes.
    - Registry snapshots cannot preserve the same hash when only definition IDs stay constant.
    - Snapshot creation and update use the same canonical payload hashing rule.
  artifacts:
    - crates/z00z_core/src/assets/snapshot.rs hashes canonical ordered definition payloads.
    - crates/z00z_core/src/assets/registry.rs verifies and emits snapshot hashes with the same payload rule.
    - crates/z00z_core/src/assets/wire.rs exposes any canonical definition-bytes helper needed by snapshot hashing.
  key_links:
    - DefinitionWire canonical framing matches registry snapshot hashing.
    - Snapshot emit and snapshot apply share one hash contract.
---
<!-- markdownlint-disable MD031 MD033 MD041 -->

<objective>
Move registry integrity from identifier-only hashing to full ordered canonical definition-payload hashing.

Purpose: Close the registry and snapshot integrity blocker from `026-FUSION.md` by making payload drift visible to validators and wallets.
Output: One full-payload registry hash contract used by snapshot creation, verification, and update flows.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/026-crypto-audit-core/026-CONTEXT.md
@.planning/phases/026-crypto-audit-core/026-FUSION.md
@.planning/phases/026-crypto-audit-core/026-01-PLAN.md

<interfaces>
From `crates/z00z_core/src/assets/snapshot.rs`:

```rust
pub struct RegistryVersion {
    pub version: u64,
    pub hash: [u8; 32],
    pub timestamp: u64,
}

pub struct RegistrySnapshot {
    pub version: RegistryVersion,
    pub definitions: Vec<DefinitionWire>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redefine the registry hash over canonical definition payloads</name>
  <files>crates/z00z_core/src/assets/snapshot.rs
crates/z00z_core/src/assets/wire.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
.planning/phases/026-crypto-audit-core/026-FUSION.md
crates/z00z_core/src/assets/snapshot.rs
crates/z00z_core/src/assets/wire.rs
crates/z00z_core/src/assets/definition.rs</read_first>
  <behavior>
    - Test 1: Changing `name`, `symbol`, `policy_flags`, or metadata changes the registry hash even when IDs are unchanged.
    - Test 2: Reordering the same canonical payload does not change the hash when the ordered definition list is unchanged.
  </behavior>
  <action>Replace the current `RegistryVersion::compute_hash` identifier-only algorithm with a full ordered payload hash. Use the registry domain and frame the canonical `DefinitionWire` fields in deterministic order: `id`, `class`, `name`, `symbol`, `decimals`, `serials`, `nominal`, `domain_name`, `version`, `crypto_version`, `policy_flags`, and ordered metadata key/value pairs. Add or reuse a helper in `wire.rs` only if needed so the exact byte framing stays shared and explicit.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/assets/snapshot.rs` no longer hashes only a `[[u8; 32]]` identifier list as the authoritative registry integrity input.
    - Snapshot hash tests prove a non-ID payload change mutates the hash.
    - Any canonical framing helper used by snapshot hashing is defined once and referenced explicitly.
  </acceptance_criteria>
  <done>The registry hash becomes sensitive to full definition-payload drift instead of only identifier drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire snapshot creation and update to the new hash contract</name>
  <files>crates/z00z_core/src/assets/registry.rs
crates/z00z_core/src/assets/snapshot.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
crates/z00z_core/src/assets/registry.rs
crates/z00z_core/src/assets/snapshot.rs
</read_first>
  <behavior>
    - Test 1: Registry snapshot creation emits the same hash that update-time verification recomputes.
    - Test 2: A tampered snapshot payload with stable definition IDs but changed payload fields is rejected.
  </behavior>
  <action>Update snapshot creation, snapshot apply, and any registry verification helper in `registry.rs` to use the full-payload hash from Task 1. Preserve deterministic definition ordering with the existing `BTreeMap` semantics. Add negative coverage for a tampered `DefinitionWire` payload that keeps `id` constant but changes another canonical field.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/assets/registry.rs` recomputes registry integrity from definition payloads, not only ID arrays.
    - Snapshot-apply tests reject payload tampering that preserves definition IDs.
    - Snapshot create/apply tests prove the same canonical payload produces the same registry hash in both directions.
  </acceptance_criteria>
  <done>Registry snapshot integrity is anchored to full canonical definition content at both emit and apply time.</done>
</task>

</tasks>

<verification>
Close this plan only after the new full-payload hash path is exercised by both creation and tamper-rejection tests.
</verification>

<success_criteria>
Registry snapshots become content-addressed by full ordered definition payloads, and ID-only drift is no longer invisible.
</success_criteria>

<output>
After completion, create `.planning/phases/026-crypto-audit-core/026-02-SUMMARY.md`
</output>
