---
phase: 026-crypto-audit-core
plan: "05"
type: execute
wave: 2
depends_on:
  - 026-01
files_modified:
  - crates/z00z_core/src/assets/assets.rs
  - crates/z00z_core/src/assets/gas.rs
  - crates/z00z_core/src/assets/nonce.rs
  - crates/z00z_core/src/assets/amount.rs
  - crates/z00z_core/src/assets/mod.rs
autonomous: true
requirements:
  - PH26-AUTH
  - PH26-NONCE-FEE
must_haves:
  truths:
    - Owner or stealth verification binds canonical state instead of relying on a bare signature.
    - Core-side fee validation requires the canonical native fee asset identity, not only `AssetClass::Coin`, while tx-layer fee-digest binding remains an explicit follow-up outside this plan.
    - Production nonce helpers and amount policy fail closed on time-provider errors and proof-width bounds.
  artifacts:
    - crates/z00z_core/src/assets/assets.rs binds or validates owner and stealth-critical fields through canonical state.
    - crates/z00z_core/src/assets/gas.rs enforces native fee asset identity.
    - crates/z00z_core/src/assets/nonce.rs and amount.rs expose fail-closed policy surfaces.
  key_links:
    - Owner-signature semantics depend on the canonical asset identity from plan 01.
    - Native fee-asset validation and amount policy share one canonical asset-policy vocabulary.
    - This plan closes core-side fee-asset identity checks only; binding fee amount and fee asset into the transaction digest remains a separate tx-layer follow-up from `026-FUSION.md`.
---
<!-- markdownlint-disable MD031 MD033 MD041 -->

<objective>
Bind ownership, stealth, fee, nonce, and amount policy to authoritative core rules instead of fallback or class-only checks.

Purpose: Close the remaining high-confidence `z00z_core` blocker cluster from `026-FUSION.md` without widening scope into other crates.
Output: Canonical owner/stealth binding rules, core-side native fee-asset enforcement, fail-closed nonce helpers, and proof-width-aware amount policy. This plan does not claim that tx-layer fee-digest binding is solved inside `z00z_core`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/026-crypto-audit-core/026-CONTEXT.md
@.planning/phases/026-crypto-audit-core/026-FUSION.md
@.planning/phases/026-crypto-audit-core/026-01-PLAN.md

<interfaces>
From `crates/z00z_core/src/assets/assets.rs`:

```rust
pub fn to_owner_message(&self) -> Vec<u8>
pub fn verify_owner_signature(&self) -> Result<(), AssetError>
pub fn validate_stealth_consistency(&self) -> Result<(), AssetError>
```

From `crates/z00z_core/src/assets/gas.rs`:

```rust
pub struct GasAsset {
    pub base_asset: AssetDefinition,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind owner and stealth verification to authoritative state and native fee identity</name>
  <files>crates/z00z_core/src/assets/assets.rs
crates/z00z_core/src/assets/gas.rs
crates/z00z_core/src/assets/mod.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
.planning/phases/026-crypto-audit-core/026-FUSION.md
crates/z00z_core/src/assets/assets.rs
crates/z00z_core/src/assets/gas.rs
crates/z00z_core/src/assets/definition.rs</read_first>
  <behavior>
    - Test 1: Tampering `r_pub`, `owner_tag`, `enc_pack`, `tag16`, or `leaf_ad_id` causes owner or stealth verification to fail.
    - Test 2: A bare valid signature over incomplete authority semantics is no longer accepted as sufficient ownership proof.
    - Test 3: `GasAsset` rejects non-native coin definitions even if they are `AssetClass::Coin` or set the gas flag.
    - Test 4: If no authoritative native-fee accessor existed before this task, the new helper or sealed constant is exercised by tests instead of being treated as a pre-existing fact.
  </behavior>
  <action>Expand the canonical owner-verification rule in `assets.rs` so owner or stealth-critical fields are verifier-bound canonical state rather than unsigned adjunct data. Use the existing `to_owner_message()` and `verify_owner_signature()` seams explicitly: either extend the signed message or add verifier-side canonical binding checks, but the final rule must cover `r_pub`, `owner_tag`, `enc_pack`, `tag16`, and `leaf_ad_id`, and it must not overclaim commitment-opening ownership. In `gas.rs`, tighten `GasAsset::check_is_native_coin_asset` so it requires canonical native fee asset identity in addition to `AssetClass::Coin`. If `z00z_core` does not already expose an authoritative native-fee accessor, add one minimal explicit helper or sealed constant in `assets/mod.rs` or another existing assets module and label it as new work rather than as an existing fact. Do not hardcode unverifiable IDs directly in `gas.rs`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/assets/assets.rs` rejects tampered stealth-critical fields during authoritative verification.
    - `crates/z00z_core/src/assets/gas.rs` enforces canonical native fee asset identity in addition to `AssetClass::Coin`.
    - If a new native-fee identity helper or sealed constant is introduced, `crates/z00z_core/src/assets/mod.rs` or the chosen existing assets module exposes it explicitly and tests use that seam.
    - Tests prove that class-only or flag-only fee-asset checks no longer pass.
    - This task does not claim to solve tx-layer fee amount or fee-digest binding beyond the core-side identity gate.
  </acceptance_criteria>
  <done>Ownership, stealth, and fee checks are bound to authoritative state instead of partial or class-only checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make nonce and amount policy fail closed</name>
  <files>crates/z00z_core/src/assets/nonce.rs
crates/z00z_core/src/assets/amount.rs
crates/z00z_core/src/assets/mod.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
crates/z00z_core/src/assets/nonce.rs
crates/z00z_core/src/assets/amount.rs
crates/z00z_core/src/assets/mod.rs</read_first>
  <behavior>
    - Test 1: Production timestamp and nonce helpers return a typed error instead of substituting timestamp `0`.
    - Test 2: `NonceCounter` production-facing contract documents and tests the persistence or uniqueness requirement explicitly.
    - Test 3: `MAX_AMOUNT` matches the supported proof-width boundary and is no longer `u64::MAX`.
  </behavior>
  <action>Replace production-facing `unwrap_or(0)` fallback behavior in `nonce.rs` with typed-error `try_*` APIs and route the production-facing exports in `mod.rs` through the fail-closed surfaces. Preserve clearly named test-only or lightweight helpers only if they are explicit about non-production scope. In `amount.rs`, derive `MAX_AMOUNT` from the supported range-proof bit width policy instead of `u64::MAX`, and add tests that bind the amount ceiling to `RANGE_PROOF_BITS_V1` semantics. Document or enforce the `NonceCounter` persistence contract so uniqueness is not left as an implicit warning only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/assets/nonce.rs` has no production-facing `unwrap_or(0)` time fallback.
    - `crates/z00z_core/src/assets/amount.rs` no longer defines `MAX_AMOUNT` as `u64::MAX`.
    - Exported nonce helpers in `crates/z00z_core/src/assets/mod.rs` make the fail-closed production path explicit.
  </acceptance_criteria>
  <done>Nonce and amount policy become fail closed and proof-width aware on the production path.</done>
</task>

</tasks>

<verification>
Close this plan only after tampered stealth tuples, non-native fee assets, and time-provider failures all have negative coverage.
</verification>

<success_criteria>
Owner, stealth, core-side fee identity, nonce, and amount checks in `z00z_core` enforce authoritative state and fail-closed policy instead of partial or fallback behavior, while tx-layer fee-digest binding remains an explicit follow-up.
</success_criteria>

<output>
After completion, create `.planning/phases/026-crypto-audit-core/026-05-SUMMARY.md`
</output>
