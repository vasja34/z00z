---
phase: 026-crypto-audit-core
plan: "03"
type: execute
wave: 2
depends_on:
  - 026-01
files_modified:
  - crates/z00z_core/src/genesis/validator.rs
  - crates/z00z_core/src/genesis/genesis.rs
  - crates/z00z_core/src/genesis/genesis_config.rs
autonomous: true
requirements:
  - PH26-GENESIS
must_haves:
  truths:
    - Mainnet and testnet reject missing or mismatched genesis anchors.
    - Protected-network parsing never falls back to devnet-like behavior.
    - Seed validation fails closed on explicit insecure patterns rather than impossible Shannon thresholds.
  artifacts:
    - crates/z00z_core/src/genesis/validator.rs enforces fail-closed protected-network consensus checks.
    - crates/z00z_core/src/genesis/genesis.rs calls the protected-network verification path directly.
    - crates/z00z_core/src/genesis/genesis_config.rs documents or enforces the protected-network config contract.
  key_links:
    - Parsed `ChainType` drives both config validation and consensus-hash verification.
    - Protected-network seed policy and consensus-anchor policy fail closed in the same flow.
    - Genesis asset-definition creation in `genesis.rs` reuses the canonical definition-identity seam from plan 01 instead of preserving a parallel rule.
---
<!-- markdownlint-disable MD031 MD033 MD041 -->

<objective>
Make protected-network genesis validation fail closed and remove unsafe network or seed-policy fallback behavior.

Purpose: Close the clearest multi-source blocker in `026-FUSION.md` and eliminate consensus drift from missing anchors or unsafe parsing.
Output: A protected-network genesis path that rejects unknown chain types, missing anchors, mismatched hashes, and insecure seed fallbacks.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/026-crypto-audit-core/026-CONTEXT.md
@.planning/phases/026-crypto-audit-core/026-FUSION.md

<interfaces>
From `crates/z00z_core/src/genesis/validator.rs`:

```rust
pub fn verify_genesis_consensus(
    network_type: ChainType,
    computed_hash: &[u8; 32],
) -> Result<(), GenesisError>
```

From `crates/z00z_core/src/genesis/genesis.rs`:

```rust
let network_type = ChainType::from_str(&config.chain.chain_type)?;
verify_genesis_consensus(network_type, &state_hash)?;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make protected-network genesis anchors fail closed</name>
  <files>crates/z00z_core/src/genesis/validator.rs
crates/z00z_core/src/genesis/genesis.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
.planning/phases/026-crypto-audit-core/026-FUSION.md
crates/z00z_core/src/genesis/validator.rs
crates/z00z_core/src/genesis/genesis.rs</read_first>
  <behavior>
    - Test 1: `Mainnet` or `Testnet` with no configured expected hash returns a typed error instead of `Ok(())`.
    - Test 2: `Mainnet` or `Testnet` hash mismatch returns `GenesisStateMismatch` or a stricter typed failure.
    - Test 3: `Devnet` remains explicitly optional and does not define the protected-network rule.
  </behavior>
  <action>Replace the `Option<[u8; 32]>` fail-open behavior in `verify_genesis_consensus` with a protected-network anchor contract: `Mainnet` and `Testnet` must each resolve to one expected genesis hash and must error when the anchor is absent or mismatched; `Devnet` may stay optional. Rewire `run_genesis` to call this fail-closed verification using the already parsed `ChainType` instead of any optional detection gate that can skip verification on protected networks.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/genesis/validator.rs` no longer treats missing mainnet or testnet expected hashes as success.
    - `crates/z00z_core/src/genesis/genesis.rs` no longer hides protected-network verification behind `detect_chain_type(&config)`.
    - Protected-network tests cover missing-anchor and mismatched-anchor failures.
  </acceptance_criteria>
  <done>Protected-network genesis verification fails closed and cannot be skipped by an optional anchor path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace weak seed heuristics and unsafe network fallback policy</name>
  <files>crates/z00z_core/src/genesis/validator.rs
crates/z00z_core/src/genesis/genesis_config.rs
crates/z00z_core/src/genesis/genesis.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
crates/z00z_core/src/genesis/validator.rs
crates/z00z_core/src/genesis/genesis_config.rs
crates/z00z_core/src/genesis/genesis.rs</read_first>
  <behavior>
    - Test 1: Known weak seeds such as all-zero, sequential, repeating, or test-only seeds are rejected on protected networks.
    - Test 2: Unknown protected-network strings fail with a typed parsing error instead of degrading into devnet semantics.
    - Test 3: The code no longer uses a 200-bit Shannon-threshold gate on 32-byte samples as the production approval criterion.
    - Test 4: Genesis-created asset definitions still resolve through the plan-01 canonical identity seam after the protected-network path is tightened.
  </behavior>
  <action>Remove the impossible `LowEntropySeed` approval rule based on Shannon estimation of a 32-byte sample and replace it with explicit fail-closed policy checks: reject all-zero, sequential, repeating, and known test seeds for protected networks; keep any devnet-only relaxation explicit. Update config parsing and docs so protected-network parsing uses `ChainType::from_str` semantics everywhere instead of any `Option`-returning fallback helper for runtime acceptance. Because this task already edits `genesis.rs`, route `create_asset_definition()` and adjacent genesis definition construction through the canonical identity seam introduced in plan 01 instead of preserving a separate genesis-only derivation rule.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/genesis/validator.rs` no longer uses the Shannon threshold as the production allow-list gate.
    - Protected-network tests reject unsafe seeds and unknown chain-type fallbacks.
    - `crates/z00z_core/src/genesis/genesis_config.rs` and `genesis.rs` document the fail-closed protected-network contract.
    - `crates/z00z_core/src/genesis/genesis.rs` no longer leaves genesis-created `AssetDefinition` identity on a parallel derivation path that can drift from plan 01.
  </acceptance_criteria>
  <done>Protected-network seed and network policy become explicit, typed, and fail closed.</done>
</task>

</tasks>

<verification>
Do not close this plan until both anchor failures and unsafe-seed failures are covered by negative tests.
</verification>

<review_status>

- [x] Protected-network parsing and weak-seed rejection are fail-closed on the reviewed runtime path.
- [x] Missing protected-network anchors return explicit typed failures instead of passing silently.
- [ ] Mainnet and testnet expected genesis hashes are populated and enforced as concrete anchors.

</review_status>

<success_criteria>
Mainnet and testnet genesis validation reject missing anchors, mismatched anchors, weak seed patterns, and unknown network parsing paths.
</success_criteria>

<output>
After completion, create `.planning/phases/026-crypto-audit-core/026-03-SUMMARY.md`
</output>
