---
phase: 026-crypto-audit-core
plan: "04"
type: execute
wave: 3
depends_on:
  - 026-01
  - 026-02
files_modified:
  - crates/z00z_core/src/assets/wire.rs
  - crates/z00z_core/src/assets/wire_pkg.rs
  - crates/z00z_core/src/assets/test_wire.rs
autonomous: true
requirements:
  - PH26-WIRE
must_haves:
  truths:
    - Untrusted asset payloads cannot inject trusted-only secret material.
    - Public DTO decode either preserves protocol-state flags or rejects payloads that cannot carry them.
    - Wire-to-domain conversion validates canonical definition identity before registry insertion.
  artifacts:
    - crates/z00z_core/src/assets/wire.rs distinguishes validated conversion from blind struct casting.
    - crates/z00z_core/src/assets/wire_pkg.rs rejects forbidden secret-bearing or confidentiality-breaking input.
    - crates/z00z_core/src/assets/test_wire.rs covers state-flag fidelity and forbidden-field rejection.
  key_links:
    - DefinitionWire conversion uses the canonical identity seam from plan 01.
    - DTO import policy matches registry and snapshot integrity rules from plan 02.
---
<!-- markdownlint-disable MD031 MD033 MD041 -->

<objective>
Harden untrusted asset wire and DTO boundaries so confidentiality-breaking or state-dropping payloads are rejected instead of silently accepted.

Purpose: Close the untrusted wire-boundary blocker from `026-FUSION.md` and make the domain conversion boundary authoritative.
Output: Validated wire conversions and DTO rejection paths for secret-bearing, plaintext-sensitive, and state-dropping payloads.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/026-crypto-audit-core/026-CONTEXT.md
@.planning/phases/026-crypto-audit-core/026-FUSION.md
@.planning/phases/026-crypto-audit-core/026-01-PLAN.md
@.planning/phases/026-crypto-audit-core/026-02-PLAN.md

<interfaces>
From `crates/z00z_core/src/assets/wire.rs`:

```rust
pub struct DefinitionWire { /* canonical definition payload */ }

pub struct AssetWire {
    pub definition: AssetDefinition,
    pub amount: u64,
    pub is_frozen: bool,
    pub is_slashed: bool,
    pub secret: Option<[u8; 32]>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject secret-bearing and confidentiality-breaking untrusted payloads</name>
  <files>crates/z00z_core/src/assets/wire.rs
crates/z00z_core/src/assets/wire_pkg.rs
crates/z00z_core/src/assets/test_wire.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
.planning/phases/026-crypto-audit-core/026-FUSION.md
crates/z00z_core/src/assets/wire.rs
crates/z00z_core/src/assets/wire_pkg.rs
crates/z00z_core/src/assets/test_wire.rs</read_first>
  <behavior>
    - Test 1: Untrusted DTO decode rejects `secret` and any explicitly forbidden confidential field injection path.
    - Test 2: Public or untrusted DTO paths do not accept plaintext-sensitive fields such as `amount` unless the path is explicitly relabeled as trusted-internal or non-confidential by policy.
    - Test 3: DefinitionWire-to-domain conversion returns a typed validation result instead of blind `From<DefinitionWire>` construction.
  </behavior>
  <action>Refactor `wire.rs` and `wire_pkg.rs` so untrusted decode paths reject `secret` and any other trusted-only confidential material instead of carrying it into `Asset::to_asset()`. Explicitly decide the policy for plaintext `amount` on public DTOs: either reject confidential imports that expose it, or relabel that DTO as trusted-internal or otherwise non-confidential rather than leaving the boundary ambiguous. Replace blind `impl From<DefinitionWire> for AssetDefinition` conversion with a validated conversion that uses the canonical definition-identity seam from plan 01 and returns a typed `Result`. Keep any trusted-internal package format explicit and separate from the public or untrusted decode path.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - `crates/z00z_core/src/assets/wire.rs` no longer relies on blind `From<DefinitionWire>` conversion for authoritative domain construction.
    - `crates/z00z_core/src/assets/wire_pkg.rs` rejects forbidden secret-bearing fields on untrusted decode.
    - Public DTO tests prove plaintext `amount` is either rejected on confidential import paths or confined to an explicitly trusted or non-confidential boundary.
    - `crates/z00z_core/src/assets/test_wire.rs` contains negative tests for forbidden-field rejection.
  </acceptance_criteria>
  <done>Untrusted decode paths can no longer inject trusted-only secret material or unchecked definition identities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve or reject protocol-state flags explicitly at the DTO boundary</name>
  <files>crates/z00z_core/src/assets/wire.rs
crates/z00z_core/src/assets/wire_pkg.rs
crates/z00z_core/src/assets/test_wire.rs</files>
  <read_first>.planning/phases/026-crypto-audit-core/026-CONTEXT.md
crates/z00z_core/src/assets/wire.rs
crates/z00z_core/src/assets/wire_pkg.rs
crates/z00z_core/src/assets/test_wire.rs</read_first>
  <behavior>
    - Test 1: `is_frozen` and `is_slashed` survive the supported trusted round-trip path unchanged.
    - Test 2: If a public or untrusted DTO shape cannot preserve a protocol-state flag, decode rejects the payload instead of silently clearing the flag.
  </behavior>
  <action>Audit the `AssetWire` and package DTO shapes for `is_frozen` and `is_slashed`, especially the `AssetPkgWire::to_wire()` rehydration seam and any adjacent decode helpers. Choose one explicit policy per boundary: either preserve those flags end to end on the supported path, or reject payloads that cannot carry them. Do not leave a path that silently clears these fields during rehydration. Extend `test_wire.rs` so the chosen policy is proven for both positive and negative paths.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs report no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <acceptance_criteria>
    - DTO tests prove `is_frozen` and `is_slashed` are either preserved or explicitly rejected.
    - No decode path silently turns a flagged asset into an unflagged asset.
    - `AssetPkgWire::to_wire()` or its replacement no longer zeroes protocol-state flags without an explicit typed rejection rule.
    - The chosen policy is enforced by typed validation in code, not only by comments.
  </acceptance_criteria>
  <done>The wire boundary can no longer drop protocol-state flags silently.</done>
</task>

</tasks>

<verification>
Treat any remaining silent field drop as a blocker even if the round-trip still compiles.
</verification>

<review_status>

- [x] `DefinitionWire` rehydrate remains typed and validated before authoritative use.
- [x] `AssetWire::to_asset()` rejects secret-bearing imports and now requires full `verify_complete()` rehydrate.
- [x] Supported DTO rehydrate preserves `is_frozen` and `is_slashed` instead of silently clearing them.

</review_status>

<success_criteria>
Every untrusted asset import path either preserves authoritative state and identity exactly or rejects the payload explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/026-crypto-audit-core/026-04-SUMMARY.md`
</output>
