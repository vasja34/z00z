# Deferred Items

## 2026-03-28

- No active deferred items remain for `026-04`. The prior `z00z_crypto` claim rustdoc blocker was resolved in-tree, and the full workspace `cargo test --release --features test-fast --features wallet_debug_dump -- --nocapture` gate now passes.
