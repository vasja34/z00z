---
phase: 016-jmt-search-and-redb
plan: 02
type: execute
wave: 2
depends_on:
  - 016-01
files_modified:
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend.rs
  - crates/z00z_storage/src/snapshot/mod.rs
  - crates/z00z_storage/src/snapshot/store.rs
  - crates/z00z_storage/tests/redb_rehydrate.rs
autonomous: true
requirements:
  - STREDB-01
  - STREDB-02
must_haves:
  truths:
    - RedB persistence stores canonical snapshot and checkpoint blobs without changing their derived ids.
    - Durable load rebuilds a usable `AssetStore` with the same canonical root and canonical `AssetPath` lookup results as committed state.
    - Rehydration does not depend on wallet-owned persistence code or lazy replay side effects.
  artifacts:
    - path: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      provides: Proposed new internal RedB backend module with durable blob persistence and full AssetStore rehydration helpers.
    - path: crates/z00z_storage/src/snapshot/store.rs
      provides: Storage-owned snapshot byte helpers reused by the RedB backend without duplicating codec logic.
    - path: crates/z00z_storage/tests/redb_rehydrate.rs
      provides: Durable roundtrip and rehydration regression coverage.
  key_links:
    - from: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      to: crates/z00z_storage/src/snapshot/store.rs
      via: canonical snapshot byte generation and validation reuse
      pattern: build_snapshot|PrepSnapshot|PrepSnapshotStore
    - from: crates/z00z_storage/src/assets/store.rs
      to: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      via: durable load or constructor path into the backend rehydration helper
      pattern: load|rehyd|redb_backend
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Persist canonical artifact blobs and implement full `AssetStore` rehydration from RedB.

Purpose: Make durable state usable across process restarts before the search surface is added.
Output: Canonical blob persistence, one proposed storage-owned load path, rehydration helpers, and durable roundtrip tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/016-jmt-search-and-redb/016-CONTEXT.md
@.planning/phases/016-jmt-search-and-redb/016-01-PLAN.md
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/snapshot/mod.rs
@crates/z00z_storage/src/snapshot/store.rs
@crates/z00z_storage/src/checkpoint/mod.rs
@crates/z00z_storage/src/checkpoint/store.rs

<interfaces>
From crates/z00z_storage/src/snapshot/mod.rs:
```rust
pub use self::{
    error::PrepSnapshotError,
    store::build_snapshot,
    store::{PrepFsStore, PrepReplayEntry, PrepSnapshotStore},
    types::{PrepSnapshot, PrepSnapshotId, PrepSnapshotVersion},
};
```

From crates/z00z_storage/src/checkpoint/mod.rs:
```rust
pub use self::{
    codec::{encode_art_bin, encode_draft_bin, encode_exec_bin, encode_link_bin},
    ids::{CheckpointDraftId, CheckpointExecInputId, CheckpointId},
    store::{CheckpointFsStore, CheckpointStore},
};
```

From crates/z00z_storage/src/assets/store.rs:
```rust
pub fn get_item(&self, path: &AssetPath) -> Result<Option<StoreItem>, AssetStoreError>;
pub fn root(&self) -> Result<AssetStateRoot, AssetStoreError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reuse canonical snapshot and checkpoint bytes for RedB blob persistence per D-11 and D-12</name>
  <files>crates/z00z_storage/src/snapshot/mod.rs, crates/z00z_storage/src/snapshot/store.rs, crates/z00z_storage/src/assets/store_internal/redb_backend.rs</files>
  <action>Add the smallest storage-owned helpers needed so the proposed new RedB backend file can persist snapshot and checkpoint blobs using the existing canonical codecs and ids, instead of inventing a second blob schema. Reuse the exported checkpoint codec functions directly, and expose only crate-private or storage-owned helpers for snapshot bytes if current visibility is too narrow.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib snapshot:: -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib snapshot:: -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-02-PLAN.md current_task="Task 1: Reuse canonical snapshot and checkpoint bytes for RedB blob persistence per D-11 and D-12"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>The RedB backend persists canonical artifact bytes whose ids match the existing snapshot and checkpoint identity rules.</done>
</task>

<task type="auto">
  <name>Task 2: Implement full durable AssetStore rehydration per D-13 and Gate V2</name>
  <files>crates/z00z_storage/src/assets/store.rs, crates/z00z_storage/src/assets/store_internal/redb_backend.rs</files>
  <action>Implement one proposed storage-owned load constructor or associated function in `crates/z00z_storage/src/assets/store.rs` that rebuilds a usable `AssetStore` from RedB-backed durable state, including live JMT rows, committed roots, canonical path bindings, and only the minimal persisted metadata already introduced by wave 1. Do not define public search or pagination APIs in this task. Do not route rehydration through wallet persistence code, filesystem snapshot replay, or ad hoc lazy initialization.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-02-PLAN.md current_task="Task 2: Implement full durable AssetStore rehydration per D-13 and Gate V2"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>A RedB-backed load returns an `AssetStore` whose `root()` and canonical `get_item()` results match the committed durable state.</done>
</task>

<task type="auto">
  <name>Task 3: Add durable roundtrip and rehydration tests</name>
  <files>crates/z00z_storage/tests/redb_rehydrate.rs</files>
  <action>Create integration coverage that writes committed state through the RedB mutation path, reloads a fresh `AssetStore` from durable state, and proves canonical root equivalence plus canonical-path lookup equivalence. Include at least one case where artifact blobs are validated by their existing ids after reload.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test redb_rehydrate -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test redb_rehydrate -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-02-PLAN.md current_task="Task 3: Add durable roundtrip and rehydration tests"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>There is automated proof that RedB roundtrip and rehydration preserve canonical storage semantics.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_storage --lib -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib -- --nocapture`, `cargo test -p z00z_storage --test redb_rehydrate -- --nocapture`, and `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test redb_rehydrate -- --nocapture`.
</verification>

<success_criteria>
The RedB durability path is no longer write-only: committed state can be reloaded into a fully usable `AssetStore` with canonical semantics intact.
</success_criteria>

<output>
After completion, create `.planning/phases/016-jmt-search-and-redb/016-02-SUMMARY.md`
</output>