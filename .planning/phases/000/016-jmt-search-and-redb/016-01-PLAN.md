---
phase: 016-jmt-search-and-redb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_storage/Cargo.toml
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend.rs
  - crates/z00z_storage/src/error.rs
  - crates/z00z_storage/tests/redb_mutation.rs
autonomous: true
requirements:
  - STREDB-01
  - STREDB-04
must_haves:
  truths:
    - Live `AssetStore` mutations persist through one storage-owned RedB commit boundary before returning success.
    - RedB backend details stay private to `z00z_storage` and do not widen the canonical public storage contract.
    - Secondary index persistence cannot change canonical roots or make a failed durable write look committed.
  artifacts:
    - path: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      provides: Proposed new internal RedB backend module with table layout, write transaction helpers, and durable mutation primitives.
    - path: crates/z00z_storage/src/assets/store.rs
      provides: Mutation-path wiring from `put_item`, `del_item`, and `apply_ops` into the durable backend seam.
    - path: crates/z00z_storage/tests/redb_mutation.rs
      provides: Atomic write and root-safety regression coverage for durable mutation calls.
  key_links:
    - from: crates/z00z_storage/Cargo.toml
      to: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      via: storage crate dependency declaration
      pattern: redb =
    - from: crates/z00z_storage/src/assets/store.rs
      to: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      via: mutation boundary calls into the backend transaction seam
      pattern: put_item|del_item|apply_ops|redb_backend
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Define the storage-owned RedB durability boundary and wire it to the existing `AssetStore` mutation seam.

Purpose: Establish one private durable backend path for live-state writes before any load or search work depends on persisted data.
Output: RedB dependency, one proposed new internal backend module, mutation-path integration, and atomic-write regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/016-jmt-search-and-redb/016-CONTEXT.md
@.planning/phases/015-jmt-serialization-visualization/015-03-SUMMARY.md
@crates/z00z_storage/Cargo.toml
@crates/z00z_storage/src/lib.rs
@crates/z00z_storage/src/assets/mod.rs
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/error.rs
@crates/z00z_storage/README.md

<interfaces>
From crates/z00z_storage/src/lib.rs:
```rust
pub mod assets;
pub mod checkpoint;
pub mod error;
pub mod serialization;
pub mod snapshot;

pub use self::error::{CheckResult, CheckpointError, SerResult, SerializationError};
```

From crates/z00z_storage/src/assets/mod.rs:
```rust
pub use self::{
    store::{AssetStore, StoreOp},
    types::{AssetId, AssetPath, AssetStateRoot, CheckRoot, DefinitionId, SerialId, StoreItem},
};
```

From crates/z00z_storage/src/assets/store.rs:
```rust
pub fn put_item(&mut self, item: StoreItem) -> Result<AssetStateRoot, AssetStoreError>;
pub fn del_item(&mut self, path: &AssetPath) -> Result<AssetStateRoot, AssetStoreError>;
pub fn apply_ops(&mut self, ops: Vec<StoreOp>) -> Result<AssetStateRoot, AssetStoreError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the private RedB backend contract per D-10 through D-17</name>
  <files>crates/z00z_storage/Cargo.toml, crates/z00z_storage/src/assets/store_internal/redb_backend.rs, crates/z00z_storage/src/error.rs</files>
  <action>Add the `redb` dependency to `z00z_storage` and create the proposed new private backend file `crates/z00z_storage/src/assets/store_internal/redb_backend.rs` to own table families, key encoding, and transaction helpers for live-state durability per D-10, D-12, and D-17. Keep all `redb` types private to the backend module. Extend the storage error surface only as needed to report backend-open, transaction, and commit failures without leaking backend-specific details into higher-level semantics.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-01-PLAN.md current_task="Task 1: Add the private RedB backend contract per D-10 through D-17"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>The storage crate compiles with a private RedB backend module and a typed durable-backend error path, while public exports remain storage-owned.</done>
</task>

<task type="auto">
  <name>Task 2: Wire synchronous durable commit into the existing mutation boundary per D-14 and D-15</name>
  <files>crates/z00z_storage/src/assets/store.rs, crates/z00z_storage/src/assets/store_internal/redb_backend.rs</files>
  <action>Attach the durable backend to `AssetStore::{put_item, del_item, apply_ops}` so successful mutation calls return only after one RedB transaction has persisted the live JMT updates and required artifact blobs. Do not add background flushing, deferred artifact generation, or a second write path. Preserve canonical root semantics and keep secondary indexes convenience-only per D-07 and STREDB-04.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib assets:: -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib assets:: -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-01-PLAN.md current_task="Task 2: Wire synchronous durable commit into the existing mutation boundary per D-14 and D-15"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>The live mutation path has one synchronous durable commit seam and cannot report a committed `AssetStateRoot` when the durable transaction fails.</done>
</task>

<task type="auto">
  <name>Task 3: Add atomic-write and root-safety regression coverage</name>
  <files>crates/z00z_storage/tests/redb_mutation.rs</files>
  <action>Create integration coverage that proves: successful mutation calls persist the committed root, failed durable transactions do not expose partial live rows or artifact blobs, and secondary-index persistence does not alter canonical roots. Use real store operations and temp directories rather than mocks, per repo testing conventions.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test redb_mutation -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test redb_mutation -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-01-PLAN.md current_task="Task 3: Add atomic-write and root-safety regression coverage"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>There is automated evidence that the RedB mutation seam is atomic and root-safe.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_storage --lib -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib -- --nocapture`, `cargo test -p z00z_storage --test redb_mutation -- --nocapture`, and `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test redb_mutation -- --nocapture`.
</verification>

<success_criteria>
Phase 016 has one concrete durable write boundary that later load and search plans can rely on without guessing where persistence ownership lives.
</success_criteria>

<output>
After completion, create `.planning/phases/016-jmt-search-and-redb/016-01-SUMMARY.md`
</output>