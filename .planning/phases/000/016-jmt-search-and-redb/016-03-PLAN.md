---
phase: 016-jmt-search-and-redb
plan: 03
type: execute
wave: 3
depends_on:
  - 016-02
files_modified:
  - crates/z00z_storage/src/assets/mod.rs
  - crates/z00z_storage/src/assets/types.rs
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend.rs
  - crates/z00z_storage/src/assets/README.MD
  - crates/z00z_storage/tests/search_api.rs
autonomous: true
requirements:
  - STREDB-03
  - STREDB-04
must_haves:
  truths:
    - Storage-owned search APIs support exact canonical path lookup, exact `asset_id` lookup, deterministic scoped listing, and ordered prefix or range pagination.
    - Search ordering and pagination tokens derive from canonical path order, not backend iteration order.
    - Search indexes remain convenience-only and cannot change canonical roots or path ownership.
  artifacts:
    - path: crates/z00z_storage/src/assets/types.rs
      provides: Proposed new public search query, result, and pagination token types.
    - path: crates/z00z_storage/src/assets/store.rs
      provides: Proposed new storage-owned search methods implemented over canonical ordering and durable convenience indexes.
    - path: crates/z00z_storage/tests/search_api.rs
      provides: Deterministic ordering and pagination regression coverage.
  key_links:
    - from: crates/z00z_storage/src/assets/mod.rs
      to: crates/z00z_storage/src/assets/types.rs
      via: public re-export of search-facing storage types
      pattern: pub use self::.*types
    - from: crates/z00z_storage/src/assets/store.rs
      to: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      via: query path into persisted indexes or canonical-order rows
      pattern: search|range|prefix|page|redb_backend
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Expose deterministic storage-owned search APIs and pagination over canonical ordering.

Purpose: Deliver the public search surface only after durable write and durable load semantics are fixed.
Output: Proposed new search types, proposed new search methods, deterministic pagination behavior, and boundary documentation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/016-jmt-search-and-redb/016-CONTEXT.md
@.planning/phases/016-jmt-search-and-redb/016-02-PLAN.md
@crates/z00z_storage/README.md
@crates/z00z_storage/src/assets/README.MD
@crates/z00z_storage/src/assets/mod.rs
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/assets/types.rs

<interfaces>
From crates/z00z_storage/src/assets/mod.rs:
```rust
pub use self::{
    store::{AssetStore, StoreOp},
    types::{AssetId, AssetPath, AssetStateRoot, DefinitionId, SerialId, StoreItem},
};
```

From crates/z00z_storage/README.md:
```text
The canonical consensus path is AssetPath { definition_id, serial_id, asset_id }.
Derived counts, cache summaries, labels, and audit metadata are side indexes only and MUST NOT change committed roots.
```

From crates/z00z_storage/src/assets/store.rs:
```rust
pub fn get_item(&self, path: &AssetPath) -> Result<Option<StoreItem>, AssetStoreError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the public search contract per D-01 through D-09</name>
  <files>crates/z00z_storage/src/assets/types.rs, crates/z00z_storage/src/assets/mod.rs</files>
  <action>Add storage-owned public types for exact lookup requests, list or range requests, search results, and pagination tokens. Keep the canonical `AssetPath` contract primary and expose `asset_id` and list queries as convenience APIs only per D-06 and D-07. Define the exact search type names and token shape in this task, re-export that naming set explicitly from `crates/z00z_storage/src/assets/mod.rs`, and reuse the same names unchanged in Tasks 2 and 3.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib assets:: -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib assets:: -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-03-PLAN.md current_task="Task 1: Define the public search contract per D-01 through D-09"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>The storage crate has an explicit public search contract with typed inputs and outputs that does not elevate secondary indexes into canonical semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic search and pagination over canonical ordering</name>
  <files>crates/z00z_storage/src/assets/store.rs, crates/z00z_storage/src/assets/store_internal/redb_backend.rs</files>
  <action>Implement exact canonical path lookup, exact `asset_id` lookup, deterministic `definition_id` and `definition_id + serial_id` listing, and ordered prefix or range pagination. Prefer extending the current canonical-path resolution flow in `crates/z00z_storage/src/assets/store.rs`, including the existing private `resolve_path` helper, rather than introducing a parallel lookup abstraction. Derive ordering and pagination from canonical `definition_id -> serial_id -> asset_id` semantics rather than backend iteration order. Keep root ownership on the canonical path and ensure indexes remain convenience-only per STREDB-04.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib assets:: -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib assets:: -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-03-PLAN.md current_task="Task 2: Implement deterministic search and pagination over canonical ordering"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>The search surface returns stable deterministic results that do not depend on backend iteration quirks.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic search tests and boundary documentation</name>
  <files>crates/z00z_storage/tests/search_api.rs, crates/z00z_storage/src/assets/README.MD</files>
  <action>Create regression tests that prove stable result ordering, stable pagination, exact `asset_id` lookup behavior, and root invariance when indexes are rebuilt or extended. Update asset-store documentation so downstream callers understand which search surfaces are canonical versus convenience-only.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test search_api -- --nocapture</automated>
      <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test search_api -- --nocapture</automated>
      <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/016-jmt-search-and-redb/016-03-PLAN.md current_task="Task 3: Add deterministic search tests and boundary documentation"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Automated coverage and docs make the search contract explicit, deterministic, and clearly subordinate to canonical path ownership.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_storage --lib -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --lib -- --nocapture`, `cargo test -p z00z_storage --test search_api -- --nocapture`, and `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test search_api -- --nocapture`.
</verification>

<success_criteria>
Phase 016 exposes a deterministic storage-owned search API that rides on top of canonical storage ordering without changing root semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/016-jmt-search-and-redb/016-03-SUMMARY.md`
</output>