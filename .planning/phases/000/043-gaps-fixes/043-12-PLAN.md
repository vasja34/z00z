---
phase: 043-gaps-fixes
plan: "12"
type: execute
wave: 12
depends_on:
  - "043-11"
files_modified:
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
  - crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
autonomous: true
requirements:
  - PH43-ARCHIVE
must_haves:
  truths:
    - "Wallet snapshot naming, canonical JSONL history naming, and live tx-history directory naming share one wallet-stem contract."
    - "`wallet_<wallet_stem_hex>_tx_history.jsonl` is the only canonical plaintext history filename order."
    - "Canonical `.wlt` semantics remain wallet-state-first."
    - "The JSONL history artifact is colocated with the snapshot output, not with the RPC export tree."
    - "`test_redb_wlt_open` coverage stays limited to snapshot restore/load truth: restore may remain valid without JSONL input, while export-time JSONL omission must still fail closed through the archive/export seam."
  artifacts:
    - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
    - crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
    - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  key_links:
    - "wallet id -> wallet_stem(...) -> snapshot/jsonl/live-dir naming"
    - "snapshot output folder -> canonical JSONL history file -> live tx-history directory"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Lock the wallet stem and canonical filename contract before archive replay is widened.

Purpose: land `043-02 Wallet Stem And Canonical Filename Lock-In` exactly as written in `043-TODO-2.md`.
Output: shared wallet-stem helpers, canonical snapshot/JSONL names, and naming-boundary regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-fixes-spec-2.md
@.planning/phases/043-gaps-fixes/043-TODO-2.md
@crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
</context>

<tasks>

<task type="auto">
  <name>043-02 Wallet Stem And Canonical Filename Lock-In</name>
  <files>
    crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
    crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
    crates/z00z_wallets/tests/test_redb_wlt_open.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO-2.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-02` backlog item exactly as written in `043-TODO-2.md`. Add a shared wallet-stem helper that preserves the live first-8-byte to 16-hex-character stem format derived from `compute_wallet_file_id(&wallet_id.0)`; add `wallet_stem(wallet_id: &PersistWalletId) -> String` as the shared helper; add `wallet_snapshot_name(wallet_stem: &str) -> String` and `wallet_history_jsonl_name(wallet_stem: &str) -> String`; keep `wallet_tx_history_dir(...)` as the live tx-history directory helper with the same stem; make `wallet_<wallet_stem_hex>_tx_history.jsonl` the canonical JSONL history filename and treat `tx_history_<wallet_stem_hex>.jsonl` as non-canonical legacy wording only; if versioned or timestamped history variants must coexist, append that suffix after the shared wallet stem and before the extension instead of mutating wallet identity semantics; preserve `.wlt` semantics; keep the snapshot file plus canonical JSONL history file in the same output folder so the artifact pair remains immediately discoverable together; use `crates/z00z_wallets/tests/test_redb_wlt_open.rs` only for the truthful snapshot restore/load boundary that remains valid when the canonical JSONL history file is absent from restore input; keep the export-time omission failure on the archive/export seam so required JSONL emission is not hidden inside `.wlt` restore semantics; and add a focused naming-helper test file only if `crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs` cannot host the stem-and-filename assertions truthfully.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the naming source-shape gate first: `rg -n "wallet_stem\(|wallet_snapshot_name\(|wallet_history_jsonl_name\(|wallet_tx_history_dir\(" crates/z00z_wallets/src/services/wallet/store crates/z00z_wallets/src/services/wallet/tests crates/z00z_wallets/tests` and confirm the canonical wallet-prefixed JSONL helper exists.</automated>
    <automated>Run the narrow naming gates in this order: `cargo test -p z00z_wallets --lib wallet_service_suite -- --nocapture` and `cargo test -p z00z_wallets --test test_redb_wlt_open -- --nocapture`; confirm the latter covers snapshot restore/load truth and does not become a parallel archive/export test surface.</automated>
    <automated>Because this task changes Rust behavior and tests, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow naming gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO-2.md current_task="043-02 Wallet Stem And Canonical Filename Lock-In"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues in the code.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>The wallet snapshot file, canonical JSONL history file, and live tx-history directory are stem-synced and colocated as specified, while `.wlt` semantics remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wallet id -> output artifact names | Naming drift could decouple snapshot, JSONL history, and live tx-history directory semantics |
| canonical names -> operator expectations | Legacy filename order must not silently regain canonical status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-12-01 | T | wallet stem helper | mitigate | Centralize one shared wallet-stem derivation helper on the live store seam |
| T-043-12-02 | I | canonical JSONL filename | mitigate | Make `wallet_<wallet_stem_hex>_tx_history.jsonl` the only canonical filename order |
| T-043-12-03 | R | snapshot/history colocation | mitigate | Add tests that prove snapshot name, JSONL name, and live-dir name share the same stem and output root |

</threat_model>

<verification>
Re-read the naming helper seam and the two focused test homes together and confirm the canonical JSONL filename contract is explicit and wallet-prefixed.
</verification>

<success_criteria>
One shared wallet-stem helper drives snapshot, JSONL history, and live directory naming, and the legacy `tx_history_*` order is non-canonical only.
</success_criteria>

<output>
After completion, record the naming-contract evidence in `.planning/phases/043-gaps-fixes/043-coverage.md` and carry only phase-level closure notes into `.planning/phases/043-gaps-fixes/043-SUMMARY.md`.
</output>
