---
phase: 043-gaps-fixes
plan: "03"
type: execute
wave: 3
depends_on:
  - "043-02"
files_modified:
  - crates/z00z_storage/src/assets/proof.rs
  - crates/z00z_storage/src/assets/store_internal/proof_help.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_wallets/src/tx/spend/spend_verification.rs
  - crates/z00z_wallets/src/tx/verify/tx_verifier.rs
  - crates/z00z_storage/tests/test_assets_suite.rs
  - crates/z00z_storage/tests/test_claim_source_proof.rs
  - crates/z00z_wallets/tests/test_tx_pedersen.rs
  - crates/z00z_wallets/tests/test_spend_proof_backend.rs
  - crates/z00z_wallets/tests/test_tx_wrong_root.rs
autonomous: true
requirements:
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
must_haves:
  truths:
    - "Storage witness verification remains the `ProofBlob`/`chk_item`/`chk_blob` boundary."
    - "Pedersen conservation and asset-class total recomputation are explicit wallet/tx-layer checks, not JMT claims."
    - "Membership, root-binding, leaf, spend-proof, commitment, missing-evidence, and asset-class mismatch failures stay distinct."
  artifacts:
    - crates/z00z_storage/src/assets/proof.rs
    - crates/z00z_storage/src/assets/store_internal/store_query.rs
    - crates/z00z_wallets/src/tx/spend/spend_verification.rs
    - crates/z00z_storage/tests/test_assets_suite.rs
    - crates/z00z_wallets/tests/test_tx_pedersen.rs
  key_links:
    - "ProofBlob decode/chk_blob -> typed proof scan result"
    - "typed proof scan result -> wallet/tx commitment audit"
    - "operator-invoked asset-class audit -> explicit checkpoint/audit target comparison"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Separate storage membership from transaction conservation and from the manual asset-class audit surface.

Purpose: land `PH43-CONSERVE` and `PH43-ASSETAUDIT` without letting JMT membership semantics drift into Pedersen-balance claims.
Output: a typed storage proof-scan result, a wallet/tx conservation audit path, and explicit operator-invoked asset-class total recomputation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
@crates/z00z_storage/src/assets/proof.rs
@crates/z00z_wallets/src/tx/verify/tx_verifier.rs
</context>

<tasks>

<task type="auto">
  <name>043-03 Storage Membership And Conservation Separation</name>
  <files>
    crates/z00z_storage/src/assets/proof.rs
    crates/z00z_storage/src/assets/store_internal/proof_help.rs
    crates/z00z_storage/src/assets/store_internal/store_query.rs
    crates/z00z_wallets/src/tx/spend/spend_verification.rs
    crates/z00z_wallets/src/tx/verify/tx_verifier.rs
    crates/z00z_storage/tests/test_assets_suite.rs
    crates/z00z_storage/tests/test_claim_source_proof.rs
    crates/z00z_wallets/tests/test_tx_pedersen.rs
    crates/z00z_wallets/tests/test_spend_proof_backend.rs
    crates/z00z_wallets/tests/test_tx_wrong_root.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-03` backlog item exactly as written in `043-TODO.md`. Keep `ProofBlob::decode`, `chk_item`, and `chk_blob` as the only proof-verification gateway for storage witness bytes, then add a typed proof-scan result that exposes semantic root, backend-root binding, path, leaf hash, and verified leaf without exporting raw JMT proof internals. Land that scan result and the wallet/tx-side conservation audit on the existing truthful seams first; create one narrow helper such as `proof_scan.rs` or `commit_audit.rs` only if the current proof or tx seams cannot carry the behavior honestly, and record that necessity in `043-coverage.md` plus the final `043-SUMMARY.md`. Expose the asset-class Pedersen total recomputation as an explicit operator diagnostic surface rather than an admission-time dependency. Report distinct membership/root/leaf/spend-proof/commitment/missing-evidence/asset-class mismatch failures, and remove any wording that says JMT membership proves Pedersen conservation.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the JMT/public-root drift gate first: `rg -n "JMT.*conservation|conservation.*JMT|backend_root.*public" crates docs .planning/phases/043-gaps-fixes` and confirm the planning packet never states that JMT membership proves Pedersen conservation.</automated>
    <automated>Run the canonical 043-03 storage gate from `043-TODO.md` alongside the narrow storage checks: `cargo test -p z00z_storage assets:: --lib -- --nocapture`.</automated>
    <automated>Run the narrow storage and conservation gates in this order: `cargo test -p z00z_storage --test test_assets_suite -- --nocapture`, `cargo test -p z00z_storage --test test_claim_source_proof -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_pedersen -- --nocapture`, `cargo test -p z00z_wallets --test test_spend_proof_backend -- --nocapture`, and `cargo test -p z00z_wallets --test test_tx_wrong_root -- --nocapture`.</automated>
    <automated>Because this task changes Rust and proof-verification behavior, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-03 Storage Membership And Conservation Separation"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>Storage membership stays a storage concern, conservation stays a wallet/tx concern, asset-class recomputation stays an explicit operator audit, and focused tests prove those layers cannot be conflated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage proof bytes -> typed membership result | JMT witness verification must not leak into wallet-facing public proof claims |
| membership result -> conservation audit | A verified leaf must still require independent commitment/proof evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-03-01 | I | storage proof API | mitigate | Keep `ProofBlob`/`chk_*` as the only witness verification entry and avoid exposing raw JMT proof types |
| T-043-03-02 | T | conservation audit | mitigate | Require explicit validated leaves plus commitment/proof evidence and return typed mismatch classes |
| T-043-03-03 | D | asset-class audit | mitigate | Keep asset-class recomputation operator-invoked and outside canonical tx admission |

</threat_model>

<verification>
Re-read the storage proof seam and the wallet/tx audit seam together and confirm they now describe two separate verification layers plus one explicit operator audit surface.
</verification>

<success_criteria>
The codebase no longer implies that JMT membership proves Pedersen conservation, and distinct failure classes are locked by focused storage and wallet tests.
</success_criteria>

<output>
After completion, record the task outcome in the coverage ledger and carry only phase-level closeout narrative into `043-SUMMARY.md`.
</output>
