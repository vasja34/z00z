---
phase: 043-gaps-fixes
plan: "14"
type: execute
wave: 14
depends_on:
  - "043-12"
  - "043-13"
files_modified:
  - crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
  - crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
autonomous: true
requirements:
  - PH43-ARCHIVE
must_haves:
  truths:
    - "`TxStorageImpl` remains the live directory-based tx-history store with one JSON file per transaction."
    - "The canonical JSONL history file remains distinct from the live tx-history directory."
    - "`outputs/tx_exports` remains a separate summary-oriented RPC export tree."
    - "No docs, logs, or closeout notes imply that plaintext replay data can leak outside the approved JSONL artifact."
  artifacts:
    - crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
    - crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
    - crates/z00z_wallets/tests/test_tx_store_integration.rs
    - crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
  key_links:
    - "wallet stem helpers -> live tx-history directory -> canonical JSONL file boundary"
    - "tx_rpc_storage::tx_export_dir() -> outputs/tx_exports -> non-forensic export tree"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Freeze the live tx-store and RPC export boundaries after the new JSONL contract lands.

Purpose: land `043-04 Live Tx-Store And RPC Boundary Lock-In` exactly as written in `043-TODO-2.md`.
Output: explicit live-store versus JSONL versus RPC-export boundaries plus focused regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-fixes-spec-2.md
@.planning/phases/043-gaps-fixes/043-TODO-2.md
@crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
@crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
</context>

<tasks>

<task type="auto">
  <name>043-04 Live Tx-Store And RPC Boundary Lock-In</name>
  <files>
    crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
    crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
    crates/z00z_wallets/tests/test_tx_store_integration.rs
    crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
    crates/z00z_wallets/tests/test_redb_wlt_open.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO-2.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-04` backlog item exactly as written in `043-TODO-2.md`. Keep `TxStorageImpl` as the live directory-based tx-history store with one JSON file per transaction keyed by `tx_hash`; keep `outputs/tx_exports` as the separate summary-oriented RPC export tree and do not let it become the forensic history location; ensure docs, logs, and closeout notes do not leak plaintext seed phrases, decrypted asset-pack fields, or decrypted wallet-local secrets, and do not imply that any artifact other than the canonical JSONL history file may carry plaintext replay data outside the encrypted archive; keep the live tx-history directory, canonical JSONL history file, and the RPC export tree clearly distinct in code and docs; do not expand canonical tx-history storage semantics in this slice without a separate storage-contract spec; use `crates/z00z_wallets/tests/test_tx_store_integration.rs` and `crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs` as the primary boundary homes for explicit artifact-path separation; and extend `crates/z00z_wallets/tests/test_redb_wlt_open.rs` only if the snapshot-restore boundary needs a distinct check for the absence of forensic history.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the boundary source-shape gate first: `rg -n "Stores one JSON file per transaction|wallet_.*_tx_history|tx_export_dir\(|tx_exports" crates/z00z_wallets/src crates/z00z_wallets/tests` and confirm the live tx-store, canonical JSONL artifact, and RPC export tree stay distinct.</automated>
    <automated>Run the mandatory narrow boundary gates in this order: `cargo test -p z00z_wallets --test test_tx_store_integration -- --nocapture` and `cargo test -p z00z_wallets --lib wallet_service_suite -- --nocapture`. Then run `cargo test -p z00z_wallets --test test_redb_wlt_open -- --nocapture` only if the snapshot-restore boundary still needs that distinct forensic-history-absence check; otherwise run the replacement boundary-focused suite introduced during implementation.</automated>
    <automated>Because this task changes Rust behavior and storage/export boundaries, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow boundary gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO-2.md current_task="043-04 Live Tx-Store And RPC Boundary Lock-In"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues in the code.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>Live tx-history storage, canonical JSONL history output, and `outputs/tx_exports` remain three distinct artifact paths with no semantic or leakage drift across the boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live tx store -> canonical JSONL file | Replay artifact and live store must not be conflated into one persistence contract |
| wallet history surfaces -> RPC export tree | Summary exports must not become the forensic history location |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-14-01 | T | live tx-store contract | mitigate | Keep `TxStorageImpl` one-json-per-tx semantics explicit and tested |
| T-043-14-02 | I | RPC export tree | mitigate | Preserve `tx_export_dir()` as a separate summary-oriented export path |
| T-043-14-03 | I | closeout/logging wording | mitigate | Keep docs and evidence explicit that only the canonical JSONL history file may carry plaintext replay data outside the encrypted archive |

</threat_model>

<verification>
Re-read the tx-storage and RPC export seams together and confirm all three artifact paths remain distinct after the JSONL contract lands.
</verification>

<success_criteria>
Live tx-storage, canonical JSONL history output, and `outputs/tx_exports` remain distinct in both code and test evidence.
</success_criteria>

<output>
After completion, record the boundary evidence in `.planning/phases/043-gaps-fixes/043-coverage.md` and carry only phase-level closure notes into `.planning/phases/043-gaps-fixes/043-SUMMARY.md`.
</output>