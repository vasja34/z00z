---
phase: 043-gaps-fixes
plan: "11"
type: execute
wave: 11
depends_on:
  - "043-10"
files_modified:
  - crates/z00z_wallets/src/tx/commit_audit.rs
  - crates/z00z_wallets/src/tx/mod.rs
  - crates/z00z_wallets/tests/test_tx_pedersen.rs
  - crates/z00z_wallets/tests/test_spend_proof_backend.rs
  - crates/z00z_wallets/tests/test_tx_wrong_root.rs
  - crates/z00z_wallets/tests/test_tx_balance.rs
autonomous: true
requirements:
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
must_haves:
  truths:
    - "The public verifier remains honest unless an explicit conservation witness exists."
    - "Manual asset-class auditing is typed around target, status, outcome, and mismatch classes."
    - "Each audit target resolves to a concrete expected total commitment, including checkpoint and issuance or burn delta contracts."
    - "Fail-closed audit paths preserve mismatch class and entry index instead of collapsing into one generic error."
    - "The manual audit stays out-of-band from canonical tx admission."
    - "AssetClassAuditReport preserves asset_class, semantic_root, backend_root, root_bind, leaf_count or a documented compatibility alias, total_commitment or a documented compatibility alias, target, and optional mismatch class."
  artifacts:
    - crates/z00z_wallets/src/tx/commit_audit.rs
    - crates/z00z_wallets/src/tx/mod.rs
    - crates/z00z_wallets/tests/test_tx_pedersen.rs
    - crates/z00z_wallets/tests/test_spend_proof_backend.rs
  key_links:
    - "public package verifier wording -> explicit witness boundary -> audit contract"
    - "audit target selection -> typed outcome -> mismatch-class evidence"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Execute the canonical spec-2 audit contract slice without widening the existing verifier claim.

Purpose: land `043-01 Canonical Audit Outcome And Witness Boundary` exactly as written in `043-TODO-2.md`.
Output: typed audit target or status or outcome contracts, honest verifier wording, and focused audit regressions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-SUMMARY.md
@.planning/phases/043-gaps-fixes/043-fixes-spec-2.md
@.planning/phases/043-gaps-fixes/043-TODO-2.md
@crates/z00z_wallets/src/tx/commit_audit.rs
</context>

<tasks>

<task type="auto">
  <name>043-01 Canonical Audit Outcome And Witness Boundary</name>
  <files>
    crates/z00z_wallets/src/tx/commit_audit.rs
    crates/z00z_wallets/src/tx/mod.rs
    crates/z00z_wallets/tests/test_tx_pedersen.rs
    crates/z00z_wallets/tests/test_spend_proof_backend.rs
    crates/z00z_wallets/tests/test_tx_wrong_root.rs
    crates/z00z_wallets/tests/test_tx_balance.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO-2.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-01` backlog item exactly as written in `043-TODO-2.md`. Add `AssetClassAuditTarget`, `AssetClassAuditStatus`, `AssetClassAuditMismatchClass`, and `AssetClassAuditOutcome` on the existing tx seam or one narrow helper file; ensure each audit target resolves to a concrete expected total commitment before recomputation is compared, even when the target originated as a checkpoint or issuance or burn delta contract; expand `AssetClassAuditReport` so it carries the target and optional mismatch class while preserving `asset_class`, `semantic_root`, `backend_root`, `root_bind`, `leaf_count` or an explicitly documented compatibility alias, and `total_commitment` or an explicitly documented compatibility alias; keep the manual audit out-of-band from canonical tx admission; keep leaf-order, duplicate-entry, root-binding, asset-class, hash, commitment, and target failures distinguishable in the audit seam and its test-visible diagnostics; use `crates/z00z_wallets/tests/test_tx_pedersen.rs` and `crates/z00z_wallets/tests/test_spend_proof_backend.rs` as the primary audit homes, and keep `test_tx_wrong_root.rs` and `test_tx_balance.rs` only as fallback homes if they remain the truthful homes for wrong-root and public-verifier-honesty assertions; and keep comments, docs, and tests honest about the public conservation boundary so no code path claims conservation proof from public package bytes alone.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the audit source-shape gate first: `rg -n "AssetClassAuditTarget|AssetClassAuditStatus|AssetClassAuditMismatchClass|AssetClassAuditOutcome" crates/z00z_wallets/src/tx crates/z00z_wallets/tests` and confirm the new typed audit contract exists only on the canonical tx seam.</automated>
    <automated>Run the mandatory focused audit gates in this order: `cargo test -p z00z_wallets --test test_tx_pedersen -- --nocapture` and `cargo test -p z00z_wallets --test test_spend_proof_backend -- --nocapture`. Then run the truthful homes for wrong-root and public-verifier-honesty coverage: if they remain `crates/z00z_wallets/tests/test_tx_wrong_root.rs` and `crates/z00z_wallets/tests/test_tx_balance.rs`, run those exact test targets; otherwise run the replacement focused suite or suites introduced during implementation.</automated>
    <automated>Because this task changes Rust behavior and test surfaces, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow audit gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO-2.md current_task="043-01 Canonical Audit Outcome And Witness Boundary"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues in the code.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>The audit API is typed around target, status, outcome, and mismatch classes; fail-closed diagnostics stay distinct; and the public verifier remains honest about its limits unless an explicit witness is introduced by a future protocol-change spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public verifier wording -> external trust claim | Public package verification must not be overstated as public conservation proof |
| audit failure path -> diagnostics | Fail-closed paths must preserve typed mismatch semantics instead of generic collapse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-11-01 | I | public verifier wording | mitigate | Keep comments, docs, and tests explicit that conservation proof requires an explicit witness |
| T-043-11-02 | T | audit outcome contract | mitigate | Add typed target, status, outcome, and mismatch classes on the canonical tx seam |
| T-043-11-03 | R | fail-closed diagnostics | mitigate | Preserve mismatch class and entry index in audit-visible failures |

</threat_model>

<verification>
Re-read the audit seam and its focused tests together and confirm the typed outcome contract did not silently widen canonical tx admission.
</verification>

<success_criteria>
The audit contract is explicit and typed, the verifier boundary stays honest, and focused tests lock the mismatch taxonomy.
</success_criteria>

<output>
After completion, record the landed audit-contract evidence in `.planning/phases/043-gaps-fixes/043-coverage.md` and carry only phase-level closure notes into `.planning/phases/043-gaps-fixes/043-SUMMARY.md`.
</output>