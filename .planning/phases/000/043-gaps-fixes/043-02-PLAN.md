---
phase: 043-gaps-fixes
plan: "02"
type: execute
wave: 2
depends_on:
  - "043-01"
files_modified:
  - crates/z00z_wallets/src/tx/tx_assembler.rs
  - crates/z00z_wallets/src/tx/verify/tx_wire_types.rs
  - crates/z00z_wallets/src/tx/verify/tx_verifier.rs
  - crates/z00z_wallets/src/tx/balance.rs
  - crates/z00z_wallets/src/tx/state/state_resolved_input.rs
  - crates/z00z_wallets/src/tx/mod.rs
  - crates/z00z_wallets/tests/test_tx_balance.rs
  - crates/z00z_wallets/tests/test_tx_tamper.rs
  - crates/z00z_wallets/tests/test_tx_fee.rs
autonomous: true
requirements:
  - PH43-TXASM
must_haves:
  truths:
    - "The active `TxAssemblerImpl` path no longer returns reachable Phase 1 stub errors."
    - "Resolved confidential input evidence is required for input summation and commitment balance checks."
    - "Public package checks stay honest about what `verify_full_tx_package(...)` can and cannot prove."
    - "Duplicate output state keys and duplicate nonces reject before package emission."
  artifacts:
    - crates/z00z_wallets/src/tx/tx_assembler.rs
    - crates/z00z_wallets/src/tx/balance.rs
    - crates/z00z_wallets/tests/test_tx_balance.rs
    - crates/z00z_wallets/tests/test_tx_tamper.rs
    - crates/z00z_wallets/tests/test_tx_fee.rs
  key_links:
    - "TxAssemblyParams.inputs_bytes -> resolved input wire contract -> sum_inputs"
    - "TxAssemblyParams.tx_outputs_bytes -> canonical TxOutputWire decode -> verify_full_tx_package(...)"
    - "resolved input commitments + produced outputs + fee outputs -> canonical zero-delta helper"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Close the active transaction assembler seam without inventing fake confidential input data or overstating public verification guarantees.

Purpose: land `PH43-TXASM` on the live wallet tx lane before storage-backed conservation and archive work build on top of it.
Output: a fail-closed assembler path, a resolved-input summation contract, and focused tx/fee/tamper regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
@crates/z00z_wallets/src/tx/tx_assembler.rs
@crates/z00z_wallets/src/tx/balance.rs
</context>

<tasks>

<task type="auto">
  <name>043-02 Transaction Assembler Closure</name>
  <files>
    crates/z00z_wallets/src/tx/tx_assembler.rs
    crates/z00z_wallets/src/tx/verify/tx_wire_types.rs
    crates/z00z_wallets/src/tx/verify/tx_verifier.rs
    crates/z00z_wallets/src/tx/balance.rs
    crates/z00z_wallets/src/tx/state/state_resolved_input.rs
    crates/z00z_wallets/src/tx/mod.rs
    crates/z00z_wallets/tests/test_tx_balance.rs
    crates/z00z_wallets/tests/test_tx_tamper.rs
    crates/z00z_wallets/tests/test_tx_fee.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-02` backlog item exactly as written in `043-TODO.md`. Add the wallet-local resolved-input wire contract needed by `sum_inputs`, decode `inputs_bytes` through that contract instead of public `TxInputWire`, decode `tx_outputs_bytes` through the canonical output-wire path, and fail closed on malformed bytes, empty inputs, empty outputs, empty lanes, duplicate refs, duplicate output state keys, duplicate nonces, invalid fee-output shape, and invalid chain metadata. Keep `sum_tx_outputs` on the existing typed conversion path, build canonical `TxWire`/`TxPackage` bytes through the existing digest helpers, route public package checks through `verify_full_tx_package(...)`, and tighten the resolved-input balance helper so it consumes resolved commitments explicitly with fee outputs on the output side. Remove or rewrite every reachable Phase 1 assembler stub and any wording that claims public `TxPackage` bytes alone prove hidden-input conservation.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the source-shape stub gate first: `rg -n "not implemented in Phase 1|# TODO|placeholder|best-effort only|does not assert that a downstream proof verifier ran here" crates/z00z_wallets/src crates/z00z_storage/src` and confirm the active assembler seam is no longer stubbed or ambiguous.</automated>
    <automated>Run the canonical 043-02 seam gates from `043-TODO.md` alongside the narrow tx checks: `cargo test -p z00z_wallets tx_assembler --lib -- --nocapture` and `cargo test -p z00z_wallets --test test_stealth_request -- --nocapture`.</automated>
    <automated>Run the narrow tx gates in this order: `cargo test -p z00z_wallets --test test_tx_balance -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_tamper -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_fee -- --nocapture`, and `cargo test -p z00z_wallets tx_verifier --lib -- --nocapture`.</automated>
    <automated>Because this task changes Rust and validation behavior, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-02 Transaction Assembler Closure"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>The live assembler path no longer ships reachable Phase 1 stubs, public refs cannot masquerade as resolved confidential inputs, duplicate output state keys and duplicate nonces reject, and focused tx tests prove fail-closed behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public tx refs -> resolved input summation | Untrusted public refs must not be treated as confidential value/opening evidence |
| package bytes -> public balance claims | Public package verification must not overstate hidden-input conservation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-02-01 | T | `sum_inputs` | mitigate | Require an explicit resolved-input contract and reject public `TxInputWire` bytes |
| T-043-02-02 | I | canonical balance helper | mitigate | Keep fee outputs on the output side and reject non-zero commitment delta |
| T-043-02-03 | R | public verifier surface | mitigate | Route public package checks through `verify_full_tx_package(...)` and document limits precisely |

</threat_model>

<verification>
Re-read `tx_assembler.rs`, `balance.rs`, and the targeted tx tests together and confirm the live assembler contract is fail-closed and honest about resolved-input requirements.
</verification>

<success_criteria>
The active assembler path is real, typed, and fail-closed, and its focused tx/fee/tamper tests prove the canonical zero-delta and verifier-routing rules.
</success_criteria>

<output>
After completion, record the task outcome in the coverage ledger and carry only phase-level closeout narrative into `043-SUMMARY.md`.
</output>
