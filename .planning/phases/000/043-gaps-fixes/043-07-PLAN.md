---
phase: 043-gaps-fixes
plan: "07"
type: execute
wave: 7
depends_on:
  - "043-06"
files_modified:
  - crates/z00z_wallets/src/stealth/output/output.rs
  - crates/z00z_wallets/src/stealth/output/output_build.rs
  - crates/z00z_wallets/src/stealth/output/output_validator.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
  - crates/z00z_wallets/tests/test_stealth_output.rs
  - crates/z00z_wallets/tests/test_live_path_enforcement.rs
  - crates/z00z_wallets/tests/test_e2e_send_scan.rs
autonomous: true
requirements:
  - PH43-OUTPUT
must_haves:
  truths:
    - "Accepted sender flows use validated stealth-output builders where receiver approval is required."
    - "Raw builders remain explicit raw seams and do not become partially validated by convention."
    - "The live RPC send path and routed simulator entrypoints are guarded against raw-builder and raw-serial-builder regression."
    - "Compatibility-helper Rustdoc names the validated replacement and the missing policy checks."
  artifacts:
    - crates/z00z_wallets/src/stealth/output/output.rs
    - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
    - crates/z00z_simulator/src/scenario_1/stage_3.rs
    - crates/z00z_wallets/tests/test_stealth_output.rs
    - crates/z00z_wallets/tests/test_live_path_enforcement.rs
  key_links:
    - "approved sender flow -> validated builder -> receiver approval policy"
    - "raw builder contract -> explicit test-only/internal seam docs"
    - "build_tx_stealth_output(...) / build_tx_stealth_output_serial(...) -> source-shape guard"
    - "live RPC send path + routed simulator entrypoints -> validated-builder enforcement"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Harden the stealth-output builder contract on the live approved-sender path.

Purpose: land `PH43-OUTPUT` before the receive/tag/output regression wave so later tests lock the already-hardened live send path rather than papering over raw-builder drift.
Output: validated-builder routing on accepted flows, explicit raw-builder contract docs, and source-shape plus behavior tests guarding the live RPC send path.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
@crates/z00z_wallets/src/stealth/output/output.rs
@crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
</context>

<tasks>

<task type="auto">
  <name>043-07 Stealth Output Builder Contract Hardening</name>
  <files>
    crates/z00z_wallets/src/stealth/output/output.rs
    crates/z00z_wallets/src/stealth/output/output_build.rs
    crates/z00z_wallets/src/stealth/output/output_validator.rs
    crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
    crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs
    crates/z00z_simulator/src/scenario_1/stage_3.rs
    crates/z00z_wallets/tests/test_stealth_output.rs
    crates/z00z_wallets/tests/test_live_path_enforcement.rs
    crates/z00z_wallets/tests/test_e2e_send_scan.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-07` backlog item exactly as written in `043-TODO.md`. Audit every active call site of `build_tx_stealth_output(...)` and `build_tx_stealth_output_serial(...)` on accepted sender flows, including the live RPC send path and any routed simulator scenario entrypoint that currently builds approved outputs, replace those approved-flow call sites with `build_card_stealth_output_validated(...)` and `build_tx_stealth_output_validated(...)` or an explicitly named stricter successor, and keep raw builders narrow and explicitly documented as test-only or already-validated seams. Add source-shape guards that fail if the live RPC send path or any routed simulator scenario entrypoint calls either raw builder directly where receiver approval is required, add behavior tests proving malformed or unapproved card/request paths fail in the validated builders, and update every compatibility helper Rustdoc to name the validated replacement plus the missing policy checks. Do not add `test_tx_stealth_flow.rs` unless sender-flow ownership moves; the current output and live-path suites already own the seam truthfully.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the source-shape guard first: `rg -n "build_tx_stealth_output\(|build_tx_stealth_output_serial\(" crates/z00z_wallets/src crates/z00z_wallets/tests crates/z00z_simulator/src/scenario_1` and confirm hits remain only in raw-builder definitions, tests, or explicitly documented prevalidated seams.</automated>
    <automated>Run the narrow output-builder gates in this order: `cargo test -p z00z_wallets --test test_stealth_output -- --nocapture`, `cargo test -p z00z_wallets --test test_live_path_enforcement --features test-fast -- --nocapture`, and `cargo test -p z00z_wallets --test test_e2e_send_scan -- --nocapture`.</automated>
    <automated>Because this task changes Rust send-path behavior, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-07 Stealth Output Builder Contract Hardening"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>Accepted sender flows are routed through validated builders, raw builders remain explicitly raw seams, and the live RPC send path plus routed simulator entrypoints are guarded by source-shape and behavior tests against raw-builder and raw-serial-builder regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved sender flow -> stealth output construction | Receiver approval policy must not be bypassed by raw builders |
| compatibility helpers -> live send path | Helper/public convenience APIs must not silently become canonical approved-flow seams |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-07-01 | E | approved sender flow | mitigate | Replace live accepted-flow raw-builder call sites with validated constructors |
| T-043-07-02 | T | raw-builder contract | mitigate | Keep raw builders explicitly raw and documented as requiring prior validation |
| T-043-07-03 | R | RPC send path | mitigate | Add source-shape and end-to-end tests that fail on raw-builder regression |

</threat_model>

<verification>
Re-read the output-builder docs and the live RPC send path together and confirm approved flows now route only through validated builders.
</verification>

<success_criteria>
The send path cannot silently bypass receiver approval via raw builders, and focused output-builder tests prove the validated-routing contract.
</success_criteria>

<output>
After completion, record the task outcome in the coverage ledger and carry only phase-level closeout narrative into `043-SUMMARY.md`.
</output>
