---
phase: 043-gaps-fixes
plan: "13"
type: execute
wave: 13
depends_on:
  - "043-11"
  - "043-12"
files_modified:
  - crates/z00z_wallets/src/backup/crypto/backup_wire.rs
  - crates/z00z_wallets/src/backup/export/backup_exporter_impl.rs
  - crates/z00z_wallets/src/backup/export/backup_exporter_verify.rs
  - crates/z00z_wallets/src/backup/import/backup_importer_impl.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
  - crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
  - crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
  - crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
  - crates/z00z_wallets/tests/test_wallet_json_export.rs
autonomous: true
requirements:
  - PH43-ARCHIVE
must_haves:
  truths:
    - "`WalletForensicPack` remains the encrypted archive transport seam inside `BackupPayload`."
    - "The canonical JSONL tx-history file is required, colocated, and derived from the same validated forensic record set as the encrypted archive."
    - "Import mode gating remains explicit and malformed forensic sections fail closed before wallet-state mutation."
    - "Canonical JSONL replay preserves the full `TxRecord` view while keeping deeper semantic decode role- and key-dependent."
    - "The replay-visible `TxRecord` view explicitly includes `tx_hash`, `tx_bytes`, `status`, `timestamp_ms`, and `block_height`."
    - "No second encrypted archive stack, plaintext replay stack, or live tx-history store is introduced."
  artifacts:
    - crates/z00z_wallets/src/backup/crypto/backup_wire.rs
    - crates/z00z_wallets/src/backup/export/backup_exporter_impl.rs
    - crates/z00z_wallets/src/backup/export/backup_exporter_verify.rs
    - crates/z00z_wallets/src/backup/import/backup_importer_impl.rs
    - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
    - crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
    - crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
    - crates/z00z_wallets/tests/test_tx_store_integration.rs
    - crates/z00z_wallets/tests/test_wallet_json_export.rs
  key_links:
    - "validated forensic record set -> encrypted archive -> required canonical JSONL history file"
    - "explicit import mode -> archive validation -> replay into live tx store without pre-validation mutation"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Extend the archive seam without inventing a second encrypted or plaintext history stack.

Purpose: land `043-03 Forensic Transport And Canonical JSONL Emission` exactly as written in `043-TODO-2.md`.
Output: canonical JSONL history contract, required JSONL emission, explicit replay/import path, and fail-closed archive validation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-SUMMARY.md
@.planning/phases/043-gaps-fixes/043-fixes-spec-2.md
@.planning/phases/043-gaps-fixes/043-TODO-2.md
@crates/z00z_wallets/src/backup/crypto/backup_wire.rs
@crates/z00z_wallets/src/backup/export/backup_exporter_impl.rs
@crates/z00z_wallets/src/backup/import/backup_importer_impl.rs
</context>

<tasks>

<task type="auto">
  <name>043-03 Forensic Transport And Canonical JSONL Emission</name>
  <files>
    crates/z00z_wallets/src/backup/crypto/backup_wire.rs
    crates/z00z_wallets/src/backup/export/backup_exporter_impl.rs
    crates/z00z_wallets/src/backup/export/backup_exporter_verify.rs
    crates/z00z_wallets/src/backup/import/backup_importer_impl.rs
    crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
    crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
    crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
    crates/z00z_wallets/tests/test_tx_store_integration.rs
    crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
    crates/z00z_wallets/tests/test_wallet_json_export.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO-2.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-03` backlog item exactly as written in `043-TODO-2.md`. Keep `WalletForensicPack` inside `BackupPayload` as the encrypted transport seam and do not invent a second encrypted archive container stack, plaintext replay stack, or live tx-history store; keep canonical `.wlt` semantics unchanged; add `WalletTxHistoryJsonlEntry` as the canonical JSONL history serialization contract carrying at minimum `schema_version`, `tx_hash`, `record_hash`, `tx_bytes_hash`, and `record: TxRecord` unless a future spec amendment explicitly approves an equivalent shape; make the canonical JSONL tx-history file a required export artifact derived from the same validated forensic record set as the encrypted archive; emit `wallet_<wallet_stem_hex>_tx_history.jsonl` beside the wallet snapshot using the shared wallet stem; keep the encrypted archive on the explicit backup export target path chosen by the caller; ensure the JSONL artifact is hash-bound and secret-redacted; add a canonical JSONL replay helper that reads `wallet_<wallet_stem_hex>_tx_history.jsonl`, deserializes each line into a replayable record set, preserves the full `TxRecord` view for inspection after replay, including `tx_hash`, `tx_bytes`, `status`, `timestamp_ms`, and `block_height`, and fails closed on malformed lines, missing fields, duplicate labels, or hash mismatches before any live-store write; keep replay semantics honest about role-dependent deeper decode; keep import mode gating explicit; prove forensic enablement is explicit and not routed through `PersistBackupSettings`; keep any diagnostic filename or helper output derived from the shared wallet stem; keep exporter-side and importer-side validation symmetric; use `crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs` and `crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs` as the primary export/import boundary homes for the forensic-enable regression; and extend `crates/z00z_wallets/tests/test_wallet_json_export.rs` only if the existing export tests already own the archive serialization seam more truthfully than a new dedicated test file.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the archive source-shape gate first: `rg -n "WalletTxHistoryJsonlEntry|ForensicImportMode|new_with_forensic_history|PersistBackupSettings|wallet_history_jsonl_name|wallet_.*_tx_history\.jsonl|tx_hash|tx_bytes|timestamp_ms|block_height" crates/z00z_wallets/src crates/z00z_wallets/tests` and confirm the encrypted archive plus canonical JSONL contract live on the approved backup seam, preserve the explicit full-field `TxRecord` replay view, and are not silently driven by a persisted forensic toggle.</automated>
    <automated>Run the narrow archive gates in this order: `cargo test -p z00z_wallets --test test_wallet_export_pack_boundary -- --nocapture`, `cargo test -p z00z_wallets --lib backup_importer_impl -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_store_integration -- --nocapture`, and `cargo test -p z00z_wallets --lib wallet_service_suite -- --nocapture`.</automated>
    <automated>Because this task changes Rust behavior, import/export logic, and tests, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow archive gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO-2.md current_task="043-03 Forensic Transport And Canonical JSONL Emission"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues in the code.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>The encrypted archive remains the transport seam, the canonical JSONL history file is required and colocated, and tampered or malformed archive/history input fails closed before wallet-state mutation or live-store replay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| encrypted backup transport -> plaintext JSONL artifact | The required JSONL artifact must remain derived, hash-bound, and secret-redacted |
| import mode -> replay mutation | Tx-history replay must not mutate live state before archive or JSONL validation passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-13-01 | T | archive validation | mitigate | Keep exporter/importer validation symmetric and fail closed on tampered manifest or record hashes |
| T-043-13-02 | I | plaintext JSONL history | mitigate | Treat JSONL as required but hash-bound and secret-redacted replay data only |
| T-043-13-03 | E | import mode gating | mitigate | Require explicit `WalletOnly`, `TxHistoryOnly`, or `WalletPlusHistory` mode before tx-history import or replay |

</threat_model>

<verification>
Re-read the backup transport seam and its importer/exporter tests together and confirm the JSONL history file is required without becoming a second transport stack.
</verification>

<success_criteria>
The encrypted archive and required wallet-prefixed JSONL history file are both live, explicit, distinct, and fail closed on tamper or malformed replay input.
</success_criteria>

<output>
After completion, record the archive-contract evidence in `.planning/phases/043-gaps-fixes/043-coverage.md` and carry only phase-level closure notes into `.planning/phases/043-gaps-fixes/043-SUMMARY.md`.
</output>
