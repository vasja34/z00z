---
phase: 043-gaps-fixes
plan: "16"
type: execute
wave: 16
depends_on:
  - "043-15"
files_modified:
  - .planning/phases/043-gaps-fixes/043-coverage.md
  - .planning/phases/043-gaps-fixes/043-SUMMARY.md
  - .planning/phases/043-gaps-fixes/043-TODO-2.md
autonomous: true
requirements:
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
  - PH43-ARCHIVE
must_haves:
  truths:
    - "The final summary reflects the additive spec-2 slice honestly without reopening unrelated Phase 043 claims."
    - "Coverage, summary, TODO-2, and validation commands agree on the same landed or explicitly deferred state."
    - "The canonical wallet snapshot file, canonical JSONL history file, live tx-history directory, and RPC export tree remain distinct artifacts in the closeout narrative."
    - "No new external dependency or `z00z_utils` bypass was introduced in the spec-2 slice."
    - "No `043-SUMMARY-2.md`, `043-coverage-2.md`, or equivalent parallel closeout artifact is introduced."
  artifacts:
    - .planning/phases/043-gaps-fixes/043-coverage.md
    - .planning/phases/043-gaps-fixes/043-SUMMARY.md
    - .planning/phases/043-gaps-fixes/043-TODO-2.md
  key_links:
    - "validation command outputs -> coverage ledger -> final summary claims"
    - "spec-backed deferrals -> TODO-2/update discipline -> closeout truth"
    - "043-16 closeout -> 043-17 E2E tests -> 043-18 final evidence sync"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Close the spec-2 planning slice with a truthful, evidence-backed summary and no hidden semantic drift.

Purpose: land `043-06 Closeout And Summary` exactly as written in `043-TODO-2.md`.
Output: final coverage and summary updates for the additive Phase 043 spec-2 slice.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-fixes-spec-2.md
@.planning/phases/043-gaps-fixes/043-TODO-2.md
@.planning/phases/043-gaps-fixes/043-coverage.md
@.planning/phases/043-gaps-fixes/043-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>043-06 Closeout And Summary</name>
  <files>
    .planning/phases/043-gaps-fixes/043-coverage.md
    .planning/phases/043-gaps-fixes/043-SUMMARY.md
    .planning/phases/043-gaps-fixes/043-TODO-2.md
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO-2.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-06` backlog item exactly as written in `043-TODO-2.md`. Finalize `043-coverage.md` so each spec-2 requirement row points to landed code and named evidence or to an explicit spec-backed deferral; finalize `043-SUMMARY.md` with the exact validation commands, changed files, evidence anchors, residual risks, and any spec-backed deferrals; do not create `043-SUMMARY-2.md`, `043-coverage-2.md`, or any equivalent parallel closeout artifact; confirm the summary reflects the canonical wallet snapshot file, canonical JSONL history file, live tx-history directory, and RPC export tree as distinct artifacts; confirm the summary captures canonical JSONL replay or import and the full-field `TxRecord` view after replay; confirm the summary does not claim the canonical JSONL history file is optional or that `tx_history_<wallet_stem_hex>.jsonl` is canonical; confirm no new external dependencies or `z00z_utils` bypasses were introduced for this slice; if future work would strengthen the public conservation claim, route it through a protocol-change spec first instead of widening this backlog or summary ad hoc; verify closeout against the spec acceptance-criteria groups explicitly; and, if a new design constraint is discovered during implementation, update `043-fixes-spec-2.md` first, then update `043-TODO-2.md`, then refresh the summary.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the closeout validation wave in this order: `cargo fmt --all`, `cargo test -p z00z_wallets --test test_wallet_export_pack_boundary -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_store_integration -- --nocapture`, `cargo test -p z00z_wallets --test test_redb_wlt_open -- --nocapture`, `cargo test -p z00z_wallets --lib backup_exporter -- --nocapture`, `cargo test -p z00z_wallets --lib backup_importer_impl -- --nocapture`, and `cargo test -p z00z_wallets --lib wallet_backup -- --nocapture`.</automated>
    <automated>Because this task closes Rust-visible validation state, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow closeout validation wave.</automated>
    <automated>Run the closeout redaction and hygiene gates exactly as written in `043-TODO-2.md`: `rg -n '"seed_phrase"|"wallet_identity"|"tx_bytes"|"enc_pack"|"asset_secret"|"blinding"' .planning/phases/043-gaps-fixes/043-SUMMARY.md .planning/phases/043-gaps-fixes/043-coverage.md` and `git diff --check`.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO-2.md current_task="043-06 Closeout And Summary"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues in the code.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>The additive spec-2 slice closes only when code, tests, spec, coverage, and summary agree on the canonical JSONL history contract, the typed audit outcome behavior, and the live tx-store/RPC boundary with no leaked plaintext evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closeout narrative -> landed evidence | Summary claims must stay subordinate to named validation outputs and coverage rows |
| spec updates -> backlog authority | Any new design constraint must update the spec first and the backlog second |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-16-01 | R | final summary claims | mitigate | Require exact validation commands, evidence anchors, and residual-risk notes in `043-SUMMARY.md` |
| T-043-16-02 | T | backlog/spec authority chain | mitigate | Update `043-fixes-spec-2.md` before `043-TODO-2.md` if a new design constraint is discovered |
| T-043-16-03 | I | closeout evidence | mitigate | Enforce the redaction grep and `git diff --check` before any completion claim |

</threat_model>

<verification>
Confirm the final summary, coverage ledger, and TODO-2 authority chain all agree on the same landed or explicitly deferred spec-2 state.
</verification>

<success_criteria>
The spec-2 slice closes with truthful evidence, synchronized planning artifacts, and no hidden widening of Phase 043 semantics.
</success_criteria>

<output>
After completion, keep the authoritative additive closeout only in `.planning/phases/043-gaps-fixes/043-SUMMARY.md` and `.planning/phases/043-gaps-fixes/043-coverage.md`; `043-18` is the final E2E evidence sync for the additive spec-2 test slice.
</output>
