---
phase: 043-gaps-fixes
plan: "08"
type: execute
wave: 8
depends_on:
  - "043-07"
files_modified:
  - .planning/phases/043-gaps-fixes/043-coverage.md
  - crates/z00z_storage/tests/test_assets_suite.rs
  - crates/z00z_storage/tests/test_claim_source_proof.rs
  - crates/z00z_wallets/tests/test_tx_balance.rs
  - crates/z00z_wallets/tests/test_tx_tamper.rs
  - crates/z00z_wallets/tests/test_tx_fee.rs
  - crates/z00z_wallets/tests/test_tx_pedersen.rs
  - crates/z00z_wallets/tests/test_spend_proof_backend.rs
  - crates/z00z_wallets/tests/test_tx_wrong_root.rs
autonomous: true
requirements:
  - PH43-TXASM
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
must_haves:
  truths:
    - "The tx/conservation/audit seams are locked by named regression homes rather than prose-only claims."
    - "Every tx/conservation requirement row in `043-coverage.md` points to a green or explained narrow gate."
    - "Distinct typed failure classes remain observable across storage and wallet regression suites."
  artifacts:
    - .planning/phases/043-gaps-fixes/043-coverage.md
    - crates/z00z_storage/tests/test_assets_suite.rs
    - crates/z00z_wallets/tests/test_tx_balance.rs
    - crates/z00z_wallets/tests/test_tx_pedersen.rs
  key_links:
    - "tx/conservation requirement rows -> named regression tests -> coverage ledger evidence"
    - "storage tamper suites -> typed membership/root/leaf failures"
    - "wallet tx suites -> typed fee/commitment/audit mismatch failures"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Freeze the tx and conservation closure claims into focused regression evidence.

Purpose: land the `043-08` regression wave only after the implementation tasks are in place, so Phase 043 can prove assembler/conservation closure with named test anchors instead of narrative confidence.
Output: green or explicitly classified tx/conservation/audit regression homes plus updated evidence slots in `043-coverage.md`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
@.planning/phases/043-gaps-fixes/043-coverage.md
</context>

<tasks>

<task type="auto">
  <name>043-08 Tx And Conservation Regression Wave</name>
  <files>
    .planning/phases/043-gaps-fixes/043-coverage.md
    crates/z00z_storage/tests/test_assets_suite.rs
    crates/z00z_storage/tests/test_claim_source_proof.rs
    crates/z00z_wallets/tests/test_tx_balance.rs
    crates/z00z_wallets/tests/test_tx_tamper.rs
    crates/z00z_wallets/tests/test_tx_fee.rs
    crates/z00z_wallets/tests/test_tx_pedersen.rs
    crates/z00z_wallets/tests/test_spend_proof_backend.rs
    crates/z00z_wallets/tests/test_tx_wrong_root.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-08` backlog item exactly as written in `043-TODO.md`. Run the tightened wallet tx suites for assembler decode, fee-output shape, resolved-input delta, and verifier routing; run the storage and wallet proof suites that tamper semantic root, backend root, leaf hash, branch proof, output commitment, fee output, and asset-class audit target independently; then write the resulting green or failing anchors back into `043-coverage.md` so each tx/conservation requirement row names one decisive verification surface. Keep distinct typed failure classes visible; do not collapse them into one generic pass/fail note in the ledger.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the narrow regression wave exactly in this order: `cargo test -p z00z_wallets --test test_tx_balance -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_tamper -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_fee -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_pedersen -- --nocapture`, `cargo test -p z00z_wallets --test test_spend_proof_backend -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_wrong_root -- --nocapture`, `cargo test -p z00z_storage --test test_assets_suite -- --nocapture`, and `cargo test -p z00z_storage --test test_claim_source_proof -- --nocapture`.</automated>
    <automated>Because this task validates landed Rust behavior, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow regression wave.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-08 Tx And Conservation Regression Wave"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>The tx/conservation/audit seams have named regression evidence, `043-coverage.md` points to those anchors explicitly, and typed failure classes remain distinguishable in the narrow suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| landed implementation -> regression evidence | Phase closure claims must be tied to replayable narrow gates rather than prose |
| regression evidence -> coverage ledger | Coverage rows must not drift away from the actual decisive test homes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-08-01 | R | `043-coverage.md` evidence slots | mitigate | Rewrite the ledger only from actual narrow-gate outcomes and named tests |
| T-043-08-02 | D | regression wave | mitigate | Run the narrow suites before the broad release gate and stop on phase-scoped failures |
| T-043-08-03 | I | typed failure classes | mitigate | Preserve separate storage and wallet mismatch categories in both tests and ledger notes |

</threat_model>

<verification>
Confirm every tx/conservation requirement row in `043-coverage.md` points to one named regression anchor after this wave completes.
</verification>

<success_criteria>
Assembler and conservation closure are backed by named narrow regressions and the coverage ledger records those anchors precisely.
</success_criteria>

<output>
After completion, record the task outcome in the coverage ledger and carry only phase-level closeout narrative into `043-SUMMARY.md`.
</output>
