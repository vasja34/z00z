---
phase: 043-gaps-fixes
plan: "10"
type: execute
wave: 10
depends_on:
  - "043-09"
files_modified:
  - .planning/phases/043-gaps-fixes/043-coverage.md
  - .planning/phases/043-gaps-fixes/043-SUMMARY.md
  - crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
  - crates/z00z_wallets/src/backup/crypto/test_wallet_backup_suite.rs
  - crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
autonomous: true
requirements:
  - PH43-TXASM
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
  - PH43-ARCHIVE
  - PH43-RECEIVE
  - PH43-TAG
  - PH43-OUTPUT
must_haves:
  truths:
    - "`043-SUMMARY.md` closes the phase with decisive validation evidence, residual-risk truth, and any spec-backed deferrals."
    - "`043-coverage.md` marks every EV, PH43, D-043, and AC-043 row as landed evidence or explicit spec-backed deferral."
    - "Archive closeout and final phase validation do not widen canonical `.wlt` semantics or hide skipped gates."
    - "Closeout evidence stays redacted or hash-bound and never copies plaintext seed phrases, decrypted tx bytes, or unredacted tx-history payloads."
  artifacts:
    - .planning/phases/043-gaps-fixes/043-coverage.md
    - .planning/phases/043-gaps-fixes/043-SUMMARY.md
    - crates/z00z_wallets/src/backup/export/test_backup_exporter_suite.rs
    - crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
    - crates/z00z_wallets/src/backup/crypto/test_wallet_backup_suite.rs
    - crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
    - crates/z00z_wallets/tests/test_tx_store_integration.rs
    - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  key_links:
    - "coverage ledger rows -> summary evidence map -> decisive command outputs"
    - "archive exporter/importer closeout tests -> final simulator gates -> residual-risk notes"
    - "spec/TODO updates -> only when new source truth is discovered during execution"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Close the optional forensic archive slice and then close the full phase honestly.

Purpose: execute the `043-10` backlog item exactly, produce the authoritative closeout artifacts, and prove the phase with decisive command outputs rather than implied success.
Output: `043-SUMMARY.md`, final `043-coverage.md` evidence state, archive closeout validation, and final simulator truth gates.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
@.planning/phases/043-gaps-fixes/043-coverage.md
</context>

<tasks>

<task type="auto">
  <name>043-10 Archive Closure And Phase Closeout</name>
  <files>
    .planning/phases/043-gaps-fixes/043-coverage.md
    .planning/phases/043-gaps-fixes/043-SUMMARY.md
    crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
    crates/z00z_wallets/src/backup/crypto/test_wallet_backup_suite.rs
    crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
    crates/z00z_wallets/tests/test_tx_store_integration.rs
    crates/z00z_wallets/tests/test_redb_wlt_open.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-10` backlog item exactly as written in `043-TODO.md`. Finalize archive/export/import docs and evidence without widening canonical `.wlt` semantics, rerun the wallet export, exporter verify, tx-store, wallet-open, backup-import, and wallet-backup-crypto test homes after the archive envelope lands, create `043-SUMMARY.md` with final coverage, changed files, decisive validation commands, residual risks, and any spec-backed deferrals, and update `043-coverage.md` so every EV, PH43, D-043, and AC-043 row points to landed code plus named evidence or to an explicit spec-backed deferral. Capture or link the exact validation outputs referenced by the summary, record any intentionally skipped tests with exact reason and next owner, run an explicit closeout redaction gate on `043-SUMMARY.md` and `043-coverage.md`, keep closeout evidence redacted or hash-bound instead of copying plaintext seed phrases, decrypted tx bytes, or unredacted tx-history payloads, and update `043-fixes-spec.md` before `043-TODO.md` only if execution discovers a new source-truth constraint.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the narrow archive closeout gates in this order: `cargo test -p z00z_wallets --test test_wallet_export_pack_boundary -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_store_integration -- --nocapture`, `cargo test -p z00z_wallets --test test_redb_wlt_open -- --nocapture`, `cargo test -p z00z_wallets backup_exporter --lib -- --nocapture`, `cargo test -p z00z_wallets backup_importer_impl --lib -- --nocapture`, and `cargo test -p z00z_wallets wallet_backup --lib -- --nocapture`.</automated>
    <automated>Because this task closes phase-visible Rust behavior and evidence, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow archive gates.</automated>
    <automated>After the broad release gate is green, run the final simulator truth gates in this order: `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump` and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`.</automated>
    <automated>Run the phase-close security and redaction gates before any completion claim: `rg -n "JMT.*conservation|conservation.*JMT|backend_root.*public" crates docs .planning/phases/043-gaps-fixes`, `rg -n "std::fs" crates/z00z_wallets/src/backup crates/z00z_wallets/src/persistence`, `rg -n "RECEIVE_INVALID_PROOF" crates/z00z_wallets/src crates/z00z_wallets/tests`, and `rg -n '"seed_phrase"|"wallet_identity"|"tx_bytes"|"enc_pack"|"asset_secret"|"blinding"' .planning/phases/043-gaps-fixes/043-SUMMARY.md .planning/phases/043-gaps-fixes/043-coverage.md`.</automated>
    <automated>Before phase completion, run `rg -n '"seed_phrase"|"wallet_identity"|"tx_bytes"|"enc_pack"|"asset_secret"|"blinding"' .planning/phases/043-gaps-fixes/043-SUMMARY.md .planning/phases/043-gaps-fixes/043-coverage.md` and fail closeout if any copied evidence block still embeds raw secret or tx-history field payloads.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-10 Archive Closure And Phase Closeout"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>`043-SUMMARY.md` and `043-coverage.md` close the phase with decisive evidence, archive closeout stays faithful to canonical `.wlt` semantics, exporter-side and importer-side archive verification both remain on the existing backup seams, closeout evidence remains redacted or hash-bound, and the final simulator truth gates are either green or explicitly justified in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final closeout narrative -> actual evidence | Summary language must stay subordinate to decisive command outputs and named tests |
| archive closeout -> phase completion claim | Archive closure must not silently widen `.wlt` semantics or hide skipped validations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-10-01 | R | `043-SUMMARY.md` | mitigate | Require copied or linked decisive command outputs plus residual-risk notes |
| T-043-10-02 | T | final coverage ledger | mitigate | Mark every EV, PH43, D-043, and AC-043 row with landed evidence or explicit spec-backed deferral |
| T-043-10-03 | I | phase-close claim | mitigate | Run the final simulator truth gates after narrow and broad archive validation and document any justified blocker explicitly |

</threat_model>

<verification>
Confirm the final summary, coverage ledger, and simulator-gate evidence all say the same thing before Phase 043 is marked complete.
</verification>

<success_criteria>
Phase 043 can be closed only from `043-SUMMARY.md` and `043-coverage.md` with decisive evidence, honest residual-risk notes, and no silent scope drift.
</success_criteria>

<output>
After completion, finalize the authoritative phase closeout only in `.planning/phases/043-gaps-fixes/043-SUMMARY.md`.
</output>
