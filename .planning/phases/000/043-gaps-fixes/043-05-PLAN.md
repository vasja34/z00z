---
phase: 043-gaps-fixes
plan: "05"
type: execute
wave: 5
depends_on:
  - "043-04"
files_modified:
  - crates/z00z_wallets/src/receiver/scan/types_receive.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scan_support.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scanner/test_stealth_scanner.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scan_support/test_stealth_scan_support_suite.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_flow.rs
  - crates/z00z_wallets/tests/test_import_error_taxonomy.rs
  - crates/z00z_wallets/tests/test_runtime_validation_result.rs
autonomous: true
requirements:
  - PH43-RECEIVE
must_haves:
  truths:
    - "Receive DTO fields carry honest decoded opening semantics or an explicit redacted/unavailable state."
    - "Internal detector/runtime reject classes stay more precise than the public compatibility label."
    - "Public `RECEIVE_INVALID_PROOF` compatibility stays outward-only and does not imply a downstream proof verifier ran."
  artifacts:
    - crates/z00z_wallets/src/receiver/scan/types_receive.rs
    - crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
    - crates/z00z_wallets/tests/test_stealth_scanner_flow.rs
    - crates/z00z_wallets/tests/test_runtime_validation_result.rs
  key_links:
    - "DetectedAssetPack opening bytes -> receive DTO contract"
    - "internal detector/runtime reject class -> RPC compatibility mapping"
    - "scanner/report/log wording -> no false proof-verifier claim"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Make the receive DTO and receive status seams honest without breaking public compatibility.

Purpose: land `PH43-RECEIVE` on the refactored receiver-native path so placeholder field semantics and ambiguous reject reporting do not survive into later tag/output work.
Output: typed receive/opening semantics, precise internal reject classes, and focused receive compatibility regressions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
@crates/z00z_wallets/src/receiver/scan/types_receive.rs
@crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
</context>

<tasks>

<task type="auto">
  <name>043-05 Receive DTO And Status Honesty</name>
  <files>
    crates/z00z_wallets/src/receiver/scan/types_receive.rs
    crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
    crates/z00z_wallets/src/receiver/scan/stealth_scan_support.rs
    crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
    crates/z00z_wallets/src/receiver/scan/stealth_scanner/test_stealth_scanner.rs
    crates/z00z_wallets/src/receiver/scan/stealth_scan_support/test_stealth_scan_support_suite.rs
    crates/z00z_wallets/tests/test_stealth_scanner_flow.rs
    crates/z00z_wallets/tests/test_import_error_taxonomy.rs
    crates/z00z_wallets/tests/test_runtime_validation_result.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-05` backlog item exactly as written in `043-TODO.md`. Replace placeholder `asset_secret`/`blinding` semantics with an explicit decoded opening contract or an explicit redacted/unavailable state without creating a second decode path for bytes already owned by `DetectedAssetPack`. Add a precise internal detector/runtime reject taxonomy such as candidate-invalid, decrypt-failed, or proof-check-failed, keep proof-verifier failure distinct where the live seam already has that boundary, and map to public `ReceiveStatus::InvalidProof` only at the compatibility boundary that still requires that outward code. Update receive-report conversion, logging, and docs so detector-side failures do not claim a downstream proof verifier ran, preserve outward compatibility on the live asset RPC boundary, and extend the existing local scanner suites instead of inventing a second receive test lane.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the receive source-shape gate first: `rg -n "Asset secret bytes placeholder|Blinding bytes placeholder" crates/z00z_wallets/src/receiver/scan/types_receive.rs` and `rg -n "RECEIVE_INVALID_PROOF" crates/z00z_wallets/src crates/z00z_wallets/tests` to confirm placeholder semantics are gone and compatibility remains outward-only.</automated>
    <automated>Run the narrow receive gates in this order: `cargo test -p z00z_wallets --test test_stealth_scanner_flow -- --nocapture`, `cargo test -p z00z_wallets --test test_import_error_taxonomy -- --nocapture`, `cargo test -p z00z_wallets --test test_runtime_validation_result -- --nocapture`, `cargo test -p z00z_wallets stealth_scanner --lib -- --nocapture`, and `cargo test -p z00z_wallets stealth_scan_support --lib -- --nocapture`.</automated>
    <automated>Because this task changes Rust receive/report behavior, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-05 Receive DTO And Status Honesty"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>Receive DTO fields are honest about opening availability, internal reject classes stay precise, local scanner tests and live asset RPC compatibility tests stay green, and public compatibility no longer implies proof-verifier success where none ran.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detected opening bytes -> public DTO | Internal opening data must not be mislabeled or duplicated through placeholder wrappers |
| internal reject class -> outward status code | Compatibility mapping must not erase internal failure precision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-05-01 | I | receive DTO | mitigate | Use explicit typed opening/redaction states instead of placeholder field semantics |
| T-043-05-02 | R | status/report mapping | mitigate | Preserve precise internal reject classes and map to `RECEIVE_INVALID_PROOF` only at the outward compatibility edge |
| T-043-05-03 | T | logs/docs | mitigate | Remove wording that implies a downstream proof verifier ran on detector-side failure |

</threat_model>

<verification>
Re-read the receive DTO, scanner, and RPC mapping seams together and confirm the public compatibility label remains stable while internal diagnostics become precise and honest.
</verification>

<success_criteria>
The live receive seam uses typed opening semantics, detector/runtime failures are distinguished internally, and focused receive tests lock the compatibility boundary without false proof claims.
</success_criteria>

<output>
After completion, record the task outcome in the coverage ledger and carry only phase-level closeout narrative into `043-SUMMARY.md`.
</output>
