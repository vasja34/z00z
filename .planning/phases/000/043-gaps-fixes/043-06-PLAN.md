---
phase: 043-gaps-fixes
plan: "06"
type: execute
wave: 6
depends_on:
  - "043-05"
files_modified:
  - crates/z00z_wallets/src/receiver/scan/types_tag_cache.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scan_support.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scanner/test_stealth_scanner.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scan_support/test_stealth_scan_support_suite.rs
autonomous: true
requirements:
  - PH43-TAG
must_haves:
  truths:
    - "Strict `TagFilterOnly` selection requires proven tag-context completeness rather than cache liveness or cache size."
    - "Best-effort scans still fall back to direct scan when strict completeness is not proven."
    - "Request-bound candidate ordering remains ahead of request-less fallback ordering."
  artifacts:
    - crates/z00z_wallets/src/receiver/scan/types_tag_cache.rs
    - crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
    - crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
    - crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
  key_links:
    - "concrete Tag16Context materialization -> completeness state -> background_scan_strategy(...)"
    - "best-effort tag cache path -> direct scan fallback"
    - "request-bound candidates -> request-less fallback ordering"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Close the Tag16 completeness seam without turning best-effort cache metadata into strict ownership evidence.

Purpose: land `PH43-TAG` on the live scanner path before the output-builder hardening and regression waves lock the receive/send surface.
Output: an explicit tag-context completeness model, fail-closed `TagFilterOnly` selection, and focused tag-cache/prefilter regressions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
@crates/z00z_wallets/src/receiver/scan/types_tag_cache.rs
@crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
</context>

<tasks>

<task type="auto">
  <name>043-06 Tag16 Completeness Gate</name>
  <files>
    crates/z00z_wallets/src/receiver/scan/types_tag_cache.rs
    crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
    crates/z00z_wallets/src/receiver/scan/stealth_scan_support.rs
    crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
    crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
    crates/z00z_wallets/src/receiver/scan/stealth_scanner/test_stealth_scanner.rs
    crates/z00z_wallets/src/receiver/scan/stealth_scan_support/test_stealth_scan_support_suite.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-06` backlog item exactly as written in `043-TODO.md`. Add an explicit completeness state for tag contexts, keep `add_request` as liveness metadata only, add or extend a context-materialization path that records concrete `Tag16Context` values, and allow strict `TagFilterOnly` only when completeness is actually proven. Land this on the existing scan-type seams first; create `tag_context.rs` only if the current modules cannot hold the completeness state honestly, and record that necessity in `043-coverage.md` plus the final `043-SUMMARY.md`. Preserve request-bound candidate ordering ahead of request-less fallback ordering, keep best-effort scans able to fall back to direct scan, and keep cache statistics separate from completeness status so cache size alone never authorizes strict ownership classification.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the tag-completeness source-shape gate first: `rg -n "TagFilterOnly" crates/z00z_wallets/src/receiver/scan` and confirm strict tag-only mode is only allowed where concrete completeness is proven.</automated>
    <automated>Run the narrow tag gates in this order: `cargo test -p z00z_wallets --test test_stealth_scanner_cache -- --nocapture`, `cargo test -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture`, `cargo test -p z00z_wallets stealth_scanner --lib -- --nocapture`, and `cargo test -p z00z_wallets stealth_scan_support --lib -- --nocapture`.</automated>
    <automated>Because this task changes Rust scanner behavior, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-06 Tag16 Completeness Gate"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>Strict `TagFilterOnly` cannot be selected from request liveness or cache size alone, best-effort scans still fall back to direct scan, and focused tag tests prove both rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tag-cache metadata -> ownership classification | Best-effort liveness/cache data must not be treated as proof of completeness |
| strict tag-only mode -> scanner coverage | Incomplete contexts must fail closed instead of silently missing owned outputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-06-01 | T | `background_scan_strategy(...)` | mitigate | Gate `TagFilterOnly` behind explicit completeness state, not cache size |
| T-043-06-02 | I | tag-context materialization | mitigate | Keep `add_request` as liveness-only metadata and require concrete `Tag16Context` coverage |
| T-043-06-03 | D | fallback coverage | mitigate | Preserve direct-scan fallback when strict completeness is absent |

</threat_model>

<verification>
Re-read the tag-cache state and scanner strategy seams together and confirm completeness, liveness, and fallback behavior are now distinct concepts.
</verification>

<success_criteria>
The live scanner cannot use cache-size or request-liveness shortcuts to justify strict ownership classification, and the focused tag tests lock that fail-closed behavior.
</success_criteria>

<output>
After completion, record the task outcome in the coverage ledger and carry only phase-level closeout narrative into `043-SUMMARY.md`.
</output>
