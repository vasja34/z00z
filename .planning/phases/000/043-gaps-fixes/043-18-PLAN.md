---
phase: 043-gaps-fixes
plan: "18"
type: execute
wave: 18
depends_on:
  - "043-17"
files_modified:
  - .planning/phases/043-gaps-fixes/043-coverage.md
  - .planning/phases/043-gaps-fixes/043-SUMMARY.md
  - .planning/phases/043-gaps-fixes/043-TEST-SPEC.md
  - .planning/phases/043-gaps-fixes/043-TESTS-TASKS.md
autonomous: true
requirements:
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
  - PH43-ARCHIVE
must_haves:
  truths:
    - "`043-18` is the final additive spec-2 E2E evidence sync after `043-17` test implementation."
    - "Every spec-2 E2E scenario points to landed tests or an explicit spec-backed deferral."
    - "`SPEC2-E2E-10` is satisfied only through existing `043-coverage.md` and `043-SUMMARY.md` closeout evidence."
    - "Closeout artifacts remain `043-coverage.md` and `043-SUMMARY.md`; no parallel closeout file is created."
    - "Redaction, hygiene, and broad Rust gates run after the E2E test wave."
  artifacts:
    - .planning/phases/043-gaps-fixes/043-coverage.md
    - .planning/phases/043-gaps-fixes/043-SUMMARY.md
    - .planning/phases/043-gaps-fixes/043-TEST-SPEC.md
    - .planning/phases/043-gaps-fixes/043-TESTS-TASKS.md
  key_links:
    - "SPEC2-E2E scenarios -> named tests -> coverage ledger -> final summary"
    - "redaction grep -> closeout narrative -> no plaintext evidence leakage"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Synchronize the additive spec-2 E2E evidence after the tests land.

Purpose: make the Phase 043 final evidence truthful after `043-17` by updating the existing coverage and summary artifacts only.
Output: final E2E evidence anchors, validation commands, residual risks, and redaction results for the spec-2 slice.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/043-gaps-fixes/043-fixes-spec-2.md
@.planning/phases/043-gaps-fixes/043-TODO-2.md
@.planning/phases/043-gaps-fixes/043-TEST-SPEC.md
@.planning/phases/043-gaps-fixes/043-TESTS-TASKS.md
@.planning/phases/043-gaps-fixes/043-coverage.md
@.planning/phases/043-gaps-fixes/043-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>043-18 Spec-2 E2E Evidence Sync</name>
  <files>
    .planning/phases/043-gaps-fixes/043-coverage.md
    .planning/phases/043-gaps-fixes/043-SUMMARY.md
    .planning/phases/043-gaps-fixes/043-TEST-SPEC.md
    .planning/phases/043-gaps-fixes/043-TESTS-TASKS.md
  </files>
  <guard>Read the final `043-17` diff and command outputs before editing evidence artifacts; do not record intended tests as landed evidence.</guard>
  <action>Update `043-coverage.md` so every `SPEC2-E2E-*` scenario and every spec-2 requirement row points to landed test names, exact command outputs, or an explicit spec-backed deferral. Satisfy `SPEC2-E2E-10` by proving that `043-coverage.md` and `043-SUMMARY.md` are the only closeout evidence carriers for the additive spec-2 E2E slice. Update `043-SUMMARY.md` with exact validation commands, changed files, residual risks, and redaction results. Refresh `043-TEST-SPEC.md` and `043-TESTS-TASKS.md` only if implementation changed scenario ownership. Do not create `043-SUMMARY-2.md`, `043-coverage-2.md`, or any parallel test artifact.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the spec-2 E2E command wave from `043-17` again after evidence edits: `cargo test -p z00z_wallets --test test_wallet_export_pack_boundary -- --nocapture`, `cargo test -p z00z_wallets --lib backup_importer_impl -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_store_integration -- --nocapture`, `cargo test -p z00z_wallets --lib wallet_service_suite -- --nocapture`, `cargo test -p z00z_wallets --test test_redb_wlt_open -- --nocapture`, `cargo test -p z00z_wallets --test test_rpc_types_serialization -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_pedersen -- --nocapture`, and `cargo test -p z00z_wallets --test test_spend_proof_backend -- --nocapture`.</automated>
    <automated>Run `cargo fmt --all` and `cargo test --release --features test-fast --features wallet_debug_dump` after the focused E2E wave.</automated>
    <automated>Run the closeout evidence checks: `rg -n "SPEC2-E2E-|wallet_<wallet_stem_hex>_tx_history\\.jsonl|AssetClassAuditOutcome|WalletTxHistoryJsonlEntry|ForensicImportMode|outputs/tx_exports" .planning/phases/043-gaps-fixes/043-coverage.md .planning/phases/043-gaps-fixes/043-SUMMARY.md .planning/phases/043-gaps-fixes/043-TEST-SPEC.md .planning/phases/043-gaps-fixes/043-TESTS-TASKS.md`.</automated>
    <automated>Run the redaction gate: `rg -n '"seed_phrase"|"wallet_identity"|"tx_bytes"|"enc_pack"|"asset_secret"|"blinding"' .planning/phases/043-gaps-fixes/043-SUMMARY.md .planning/phases/043-gaps-fixes/043-coverage.md`; inspect any matches and keep only explicitly approved hash-bound or redacted evidence.</automated>
    <automated>Run the no-parallel-artifact gate and require zero matches: `ls .planning/phases/043-gaps-fixes | rg '043-(SUMMARY|coverage|TEST-SPEC|TESTS-TASKS)-2|SUMMARY-2|coverage-2'`; an `rg` exit code of 1 is the expected clean result for this specific no-match gate.</automated>
    <automated>Run `git diff --check`.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TEST-SPEC.md current_task="043-18 Spec-2 E2E Evidence Sync"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>Coverage, summary, test spec, and tests-tasks agree on the landed spec-2 E2E evidence, residual risks, and deferrals with no parallel closeout surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| test results -> final evidence | Summary and coverage must quote actual validation results, not intended coverage |
| closeout artifacts -> future execution | Existing phase-local artifacts remain the only authority surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-043-18-01 | R | evidence ledger | mitigate | Map every claim to command output, test name, or explicit deferral |
| T-043-18-02 | I | redaction | mitigate | Re-run summary and coverage redaction grep after all edits |
| T-043-18-03 | T | planning authority | mitigate | Reject any `*-2` parallel evidence artifact |

</threat_model>

<verification>
Confirm that `043-18` is the last additive spec-2 E2E evidence gate and that future implementation does not rely on prose-only closure.
</verification>

<success_criteria>
The phase has a final, evidence-backed spec-2 E2E test closeout without concept drift or parallel artifacts.
</success_criteria>

<output>
After completion, keep the final spec-2 E2E evidence in `.planning/phases/043-gaps-fixes/043-coverage.md` and `.planning/phases/043-gaps-fixes/043-SUMMARY.md`.
</output>
