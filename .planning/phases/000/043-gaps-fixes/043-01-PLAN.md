---
phase: 043-gaps-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/043-gaps-fixes/043-coverage.md
  - crates/z00z_wallets/tests/test_tx_balance.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
  - crates/z00z_wallets/tests/test_stealth_output.rs
autonomous: true
requirements:
  - PH43-TXASM
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
  - PH43-ARCHIVE
  - PH43-RECEIVE
  - PH43-TAG
  - PH43-OUTPUT
must_haves:
  truths:
    - "043-coverage.md exists and maps every EV, PH43, D-043, and AC-043 item to one owner file, one task, one test home, and one evidence slot."
    - "The Phase 043 seam inventory is evidence-backed before behavior changes begin."
    - "Out-of-scope TODO sweeps and vendor edits remain explicitly excluded."
  artifacts:
    - .planning/phases/043-gaps-fixes/043-coverage.md
    - crates/z00z_wallets/tests/test_tx_balance.rs
    - crates/z00z_wallets/tests/test_tx_store_integration.rs
    - crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
    - crates/z00z_wallets/tests/test_stealth_output.rs
  key_links:
    - "043-fixes-spec.md -> 043-coverage.md -> named test homes"
    - "043-TODO.md inventory commands -> coverage ledger evidence slots"
    - "Phase 043 exclusions -> no edits under crates/z00z_crypto/tari/**"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Lock the Phase 043 execution ledger before any behavioral edits land.

Purpose: create one repository-backed coverage map and one failing-anchor map so every later task can close a bounded seam instead of expanding the phase.
Output: `043-coverage.md`, seam inventory evidence, and named failing-anchor homes for the later implementation and regression waves.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
</context>

<tasks>

<task type="auto">
  <name>043-01 Coverage Ledger And Failing-Test Lock-In</name>
  <files>
    .planning/phases/043-gaps-fixes/043-coverage.md
    crates/z00z_wallets/tests/test_tx_balance.rs
    crates/z00z_wallets/tests/test_tx_store_integration.rs
    crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
    crates/z00z_wallets/tests/test_stealth_output.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-01` backlog item exactly as written in `043-TODO.md`. Before any behavior edits, run the Phase 043 inventory commands from the TODO/spec and record the starting residue in `043-coverage.md`. Map every EV, PH43, D-043, and AC-043 identifier to exact owner files, exact test homes, one evidence slot, and one status cell. Identify or add truthful failing anchors on the listed tx/archive/tag/output seams without widening the task into a repository-wide cleanup pass. Keep the exclusions explicit: unrelated fee/prover/generic backup/persistence TODOs stay out of scope, and no plan step may touch `crates/z00z_crypto/tari/**`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the canonical baseline gates from `043-TODO.md` before narrower task work: `cargo fmt --all` and `cargo check -p z00z_wallets --all-targets`.</automated>
    <automated>Run the inventory anchors from `043-TODO.md` and confirm coverage completeness with `rg -n "EV-|PH43-|D-043-|AC-043-" .planning/phases/043-gaps-fixes/043-coverage.md` plus the three inventory commands listed under `043-01`.</automated>
    <automated>If this task adds or changes Rust tests, run `cargo test --release --features test-fast --features wallet_debug_dump` after the inventory and ledger checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-01 Coverage Ledger And Failing-Test Lock-In"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>`043-coverage.md` exists, every Phase 043 requirement row has an owner/test/evidence entry, and the phase inventory is frozen without scope drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec/backlog -> execution ledger | Missing or misclassified rows can silently drop required work |
| seam inventory -> later edits | Unbounded inventory can reintroduce out-of-scope cleanup drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-01-01 | T | `043-coverage.md` row set | mitigate | Require every EV, PH43, D-043, and AC-043 identifier to appear with one evidence slot |
| T-043-01-02 | E | inventory scope | mitigate | Keep unrelated TODO families and vendor paths explicitly excluded in the ledger |
| T-043-01-03 | R | failing-anchor ownership | mitigate | Bind every seam to one named existing or newly added test home before behavior work starts |

</threat_model>

<verification>
Confirm `043-coverage.md` is the single execution ledger for the phase and that no out-of-scope cleanup families were pulled into the row set.
</verification>

<success_criteria>
Phase 043 has a repository-backed execution ledger, named failing anchors, and an evidence-backed seam inventory before any behavioral fix lands.
</success_criteria>

<output>
After completion, record the task outcome in the coverage ledger and carry only phase-level closeout narrative into `043-SUMMARY.md`.
</output>
