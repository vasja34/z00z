---
phase: 043-gaps-fixes
plan: "15"
type: execute
wave: 15
depends_on:
  - "043-11"
  - "043-12"
  - "043-13"
  - "043-14"
files_modified:
  - .planning/phases/043-gaps-fixes/043-coverage.md
  - .planning/phases/043-gaps-fixes/043-SUMMARY.md
  - crates/z00z_wallets/tests/test_tx_pedersen.rs
  - crates/z00z_wallets/tests/test_spend_proof_backend.rs
  - crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
  - crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
  - crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
autonomous: true
requirements:
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
  - PH43-ARCHIVE
must_haves:
  truths:
    - "Every spec-2 slice has a named regression anchor in `043-coverage.md`."
    - "Regression evidence proves the canonical JSONL history file is required, wallet-prefixed, and distinct from live store and RPC exports."
    - "Tampered archive lines, wrong-root or missing-evidence audit failures, and leakage attempts fail closed."
    - "Closeout notes remain redacted or hash-bound outside the encrypted archive boundary."
    - "Regression evidence stays in the existing `043-coverage.md` and `043-SUMMARY.md` artifacts; this wave does not create parallel coverage or summary documents."
  artifacts:
    - .planning/phases/043-gaps-fixes/043-coverage.md
    - .planning/phases/043-gaps-fixes/043-SUMMARY.md
    - crates/z00z_wallets/tests/test_tx_pedersen.rs
    - crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
    - crates/z00z_wallets/tests/test_tx_store_integration.rs
  key_links:
    - "spec-2 requirements -> regression homes -> coverage ledger evidence slots"
    - "required JSONL/replay behavior -> archive and tx-store regressions -> summary evidence"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Turn the spec-2 slice into explicit regression evidence before phase closeout is attempted.

Purpose: land `043-05 Regression Wave And Redaction Checks` exactly as written in `043-TODO-2.md`.
Output: named regression anchors, updated coverage ledger rows, and captured redaction evidence for the spec-2 slice.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-fixes-spec-2.md
@.planning/phases/043-gaps-fixes/043-TODO-2.md
@.planning/phases/043-gaps-fixes/043-coverage.md
@.planning/phases/043-gaps-fixes/043-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>043-05 Regression Wave And Redaction Checks</name>
  <files>
    .planning/phases/043-gaps-fixes/043-coverage.md
    .planning/phases/043-gaps-fixes/043-SUMMARY.md
    crates/z00z_wallets/tests/test_tx_pedersen.rs
    crates/z00z_wallets/tests/test_spend_proof_backend.rs
    crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
    crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
    crates/z00z_wallets/tests/test_tx_store_integration.rs
    crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO-2.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-05` backlog item exactly as written in `043-TODO-2.md`. Run the unit and integration tests that prove the audit outcome split, wallet-stem naming contract, required JSONL history file, and live store or RPC boundary separation; run a direct regression that proves `.wlt` restore or load semantics remain wallet-state-first and do not silently absorb canonical tx-history storage responsibilities; run the unit and integration tests that prove the canonical JSONL replay or import path can surface the full `TxRecord` view after replay; add or tighten negative coverage for tampered forensic archive entries, wrong-root or missing-evidence audit failures, leaf mismatches, asset-class mismatches, duplicate-entry failures, commitment mismatches, target mismatches, and any path that would leak plaintext seed phrases, decrypted asset-pack fields, or decrypted wallet-local secrets outside the encrypted boundary; ensure the regression wave directly proves the canonical JSONL history file is not optional and the legacy `tx_history_<wallet_stem_hex>.jsonl` wording remains non-canonical; map the required test matrix explicitly in `043-coverage.md`, with one truthful test or spec-backed deferral for no config toggle for forensic enablement, encrypted forensic archive completeness, canonical JSONL naming and wallet stem sync, canonical JSONL import and full-field view, separate archive vs live store vs RPC exports, and no plaintext leakage; update `043-coverage.md` so each spec-2 requirement and named regression wave points to a truthful validation anchor or explicit spec-backed deferral; and record the validation outputs and any residual risks in `043-SUMMARY.md`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the regression source-shape and redaction gate first: `rg -n "wallet_.*_tx_history\.jsonl|tx_history_.*\.jsonl|AssetClassAuditTarget|AssetClassAuditOutcome" crates/z00z_wallets/src crates/z00z_wallets/tests .planning/phases/043-gaps-fixes` and `rg -n '"seed_phrase"|"wallet_identity"|"tx_bytes"|"enc_pack"|"asset_secret"|"blinding"' .planning/phases/043-gaps-fixes/043-SUMMARY.md .planning/phases/043-gaps-fixes/043-coverage.md`.</automated>
    <automated>Run the matrix anchor gate before the test wave: `rg -n "No config toggle for forensic enablement|Encrypted forensic archive completeness|Canonical JSONL naming and wallet stem sync|Canonical JSONL import and full-field view|Separate archive vs live store vs RPC exports|No plaintext leakage" .planning/phases/043-gaps-fixes/043-coverage.md`.</automated>
    <automated>Run the narrow regression wave in this order: `cargo test -p z00z_wallets --test test_tx_pedersen -- --nocapture`, `cargo test -p z00z_wallets --test test_spend_proof_backend -- --nocapture`, `cargo test -p z00z_wallets --test test_wallet_export_pack_boundary -- --nocapture`, `cargo test -p z00z_wallets --lib backup_importer_impl -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_store_integration -- --nocapture`, `cargo test -p z00z_wallets --lib wallet_service_suite -- --nocapture`, and `cargo test -p z00z_wallets --test test_redb_wlt_open -- --nocapture`.</automated>
    <automated>Because this task updates Rust validation surfaces and closeout evidence, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow regression wave.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO-2.md current_task="043-05 Regression Wave And Redaction Checks"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues in the code.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>Every spec-2 requirement row has a named regression anchor or explicit spec-backed deferral, the required JSONL contract is evidenced, and closeout notes remain redacted or hash-bound outside the encrypted archive boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implemented slice -> coverage ledger | Spec-2 closure claims must be tied to named regression evidence |
| regression evidence -> summary/redaction notes | Closeout notes must not leak plaintext secret-bearing fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-15-01 | R | coverage ledger mapping | mitigate | Update every spec-2 row only from actual regression outcomes or explicit deferrals |
| T-043-15-02 | T | negative test matrix | mitigate | Add or tighten wrong-root, missing-evidence, duplicate, mismatch, and tamper regressions |
| T-043-15-03 | I | summary/coverage evidence | mitigate | Enforce redaction/hash-bound checks on summary and coverage before any closeout claim |

</threat_model>

<verification>
Confirm the regression anchors, coverage ledger rows, and redaction checks all agree on the same implemented state before the closeout plan starts.
</verification>

<success_criteria>
Spec-2 regression coverage is explicit, complete, and redacted, with no silent gaps between code, tests, coverage, and summary artifacts.
</success_criteria>

<output>
After completion, keep `.planning/phases/043-gaps-fixes/043-coverage.md` and `.planning/phases/043-gaps-fixes/043-SUMMARY.md` synchronized as the only authoritative evidence carriers for the spec-2 slice.
</output>
