---
phase: 043-gaps-fixes
plan: "09"
type: execute
wave: 9
depends_on:
  - "043-08"
files_modified:
  - .planning/phases/043-gaps-fixes/043-coverage.md
  - crates/z00z_wallets/tests/test_stealth_scanner_flow.rs
  - crates/z00z_wallets/tests/test_import_error_taxonomy.rs
  - crates/z00z_wallets/tests/test_runtime_validation_result.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
  - crates/z00z_wallets/tests/test_stealth_output.rs
  - crates/z00z_wallets/tests/test_live_path_enforcement.rs
  - crates/z00z_wallets/tests/test_e2e_send_scan.rs
autonomous: true
requirements:
  - PH43-RECEIVE
  - PH43-TAG
  - PH43-OUTPUT
must_haves:
  truths:
    - "Receive, tag, and output closure claims are bound to named regression homes."
    - "Every EV-008 through EV-014 seam in `043-coverage.md` points to one decisive green or explained test anchor."
    - "The live receive/send surface cannot regress back to ambiguous status, incomplete tag-only logic, or raw-builder/raw-serial-builder approved flows without a failing test."
    - "Best-effort direct-scan fallback remains locked by named regression homes when strict completeness is absent."
  artifacts:
    - .planning/phases/043-gaps-fixes/043-coverage.md
    - crates/z00z_wallets/tests/test_stealth_scanner_flow.rs
    - crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
    - crates/z00z_wallets/tests/test_stealth_output.rs
    - crates/z00z_wallets/tests/test_live_path_enforcement.rs
  key_links:
    - "receive/tag/output requirement rows -> named regression tests -> coverage ledger evidence"
    - "public compatibility mapping -> precise internal reject-class tests"
    - "approved sender path -> validated-builder enforcement tests"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Freeze the receive, tag, and output closure claims into focused regression evidence.

Purpose: land the `043-09` regression wave after the scanner/output implementation tasks so the phase can prove status honesty, tag completeness, and validated-builder routing on the live seams.
Output: green or explicitly classified receive/tag/output regression homes plus updated evidence slots in `043-coverage.md`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/043-gaps-fixes/043-CONTEXT.md
@.planning/phases/043-gaps-fixes/043-TODO.md
@.planning/phases/043-gaps-fixes/043-fixes-spec.md
@.planning/phases/043-gaps-fixes/043-coverage.md
</context>

<tasks>

<task type="auto">
  <name>043-09 Receive, Tag, And Output Regression Wave</name>
  <files>
    .planning/phases/043-gaps-fixes/043-coverage.md
    crates/z00z_wallets/tests/test_stealth_scanner_flow.rs
    crates/z00z_wallets/tests/test_import_error_taxonomy.rs
    crates/z00z_wallets/tests/test_runtime_validation_result.rs
    crates/z00z_wallets/tests/test_stealth_scanner_cache.rs
    crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
    crates/z00z_wallets/tests/test_stealth_output.rs
    crates/z00z_wallets/tests/test_live_path_enforcement.rs
    crates/z00z_wallets/tests/test_e2e_send_scan.rs
  </files>
  <guard>Read the exact source-evidence files, live seams, and named test homes listed by the matching task block in `043-TODO.md` before editing; this numbered plan inherits that mandatory pre-read gate.</guard>
  <action>Implement the `043-09` backlog item exactly as written in `043-TODO.md`. Run the scanner-flow, tag-cache, and source-shape test homes after the receive/tag/output implementation tasks land, add or tighten negative coverage for detector failure mapping, incomplete tag context in strict mode, cache-size-only strategy selection, direct-scan fallback when strict completeness is absent, and raw-builder or raw-serial-builder usage on the live send path and routed simulator entrypoints, and update `043-coverage.md` so every EV-008 through EV-014 seam points to one final regression home. Keep the ledger authoritative for these seams; do not leave any receive/tag/output row on a vague “covered by tests” note.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the approved-send source-shape gate first: `rg -n "build_tx_stealth_output\(|build_tx_stealth_output_serial\(" crates/z00z_wallets/src crates/z00z_wallets/tests crates/z00z_simulator/src/scenario_1` and confirm raw builders remain only in raw definitions, tests, or explicitly documented prevalidated seams.</automated>
    <automated>Run the narrow regression wave exactly in this order: `cargo test -p z00z_wallets --test test_stealth_scanner_flow -- --nocapture`, `cargo test -p z00z_wallets --test test_import_error_taxonomy -- --nocapture`, `cargo test -p z00z_wallets --test test_runtime_validation_result -- --nocapture`, `cargo test -p z00z_wallets --test test_stealth_scanner_cache -- --nocapture`, `cargo test -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture`, `cargo test -p z00z_wallets --test test_stealth_output -- --nocapture`, `cargo test -p z00z_wallets --test test_live_path_enforcement --features test-fast -- --nocapture`, and `cargo test -p z00z_wallets --test test_e2e_send_scan -- --nocapture`.</automated>
    <automated>Because this task validates landed Rust behavior, run `cargo test --release --features test-fast --features wallet_debug_dump` after the narrow regression wave.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TODO.md current_task="043-09 Receive, Tag, And Output Regression Wave"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues.</automated>
    <automated>If a commit is needed, use `/z00z-git-versioning`; do not use raw `git commit`.</automated>
  </verify>
  <done>Receive/tag/output closure is backed by named regression homes, EV-008 through EV-014 rows in `043-coverage.md` point to those anchors explicitly, direct-scan fallback remains locked for best-effort paths, and live receive/send regressions cannot reopen silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| landed receive/tag/output behavior -> regression evidence | Closure claims must be tied to replayable narrow tests, not prose |
| regression evidence -> coverage ledger | EV-008 through EV-014 rows must stay synchronized with the decisive test homes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-09-01 | R | `043-coverage.md` evidence slots | mitigate | Update receive/tag/output rows only from actual narrow-gate outcomes |
| T-043-09-02 | T | strict tag/send-path regressions | mitigate | Keep negative coverage for incomplete tag contexts, best-effort fallback loss, and raw-builder/raw-serial-builder use on the live path plus routed simulator entrypoints |
| T-043-09-03 | I | compatibility/status claims | mitigate | Preserve precise internal reject tests while checking outward compatibility remains stable |

</threat_model>

<verification>
Confirm every receive/tag/output requirement row in `043-coverage.md` points to one named regression anchor after this wave completes.
</verification>

<success_criteria>
Receive, tag, and output closure are all backed by named narrow regressions, best-effort direct-scan fallback remains covered, and the coverage ledger records those anchors precisely.
</success_criteria>

<output>
After completion, record the task outcome in the coverage ledger and carry only phase-level closeout narrative into `043-SUMMARY.md`.
</output>
