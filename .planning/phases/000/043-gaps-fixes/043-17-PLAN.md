---
phase: 043-gaps-fixes
plan: "17"
type: execute
wave: 17
depends_on:
  - "043-16"
files_modified:
  - crates/z00z_wallets/tests/test_tx_pedersen.rs
  - crates/z00z_wallets/tests/test_spend_proof_backend.rs
  - crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
  - crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
  - crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  - crates/z00z_wallets/tests/test_rpc_types_serialization.rs
autonomous: true
requirements:
  - PH43-CONSERVE
  - PH43-ASSETAUDIT
  - PH43-ARCHIVE
must_haves:
  truths:
    - "Spec-2 E2E coverage is implemented in existing truthful test homes instead of a parallel test layer."
    - "The required canonical JSONL history file is emitted, wallet-prefixed, colocated, hash-bound, and replayable."
    - "Canonical JSONL replay preserves the full `TxRecord` view after import."
    - "Import mode gating and tamper rejection fail closed before wallet-state mutation or live-store writes."
    - "The encrypted archive path, wallet snapshot, canonical JSONL file, live tx-history directory, and `outputs/tx_exports` remain distinct."
  artifacts:
    - crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
    - crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
    - crates/z00z_wallets/tests/test_tx_store_integration.rs
    - crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
    - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  key_links:
    - "043-fixes-spec-2.md -> SPEC2-E2E-01..09 -> existing Rust test homes"
    - "validated forensic record set -> encrypted archive + required JSONL -> replay into TxStorageImpl"
---

<!-- markdownlint-disable MD022 MD031 MD033 MD040 MD041 MD047 MD055 MD058 MD060 -->

<objective>
Implement additive spec-2 E2E integration coverage without widening the codebase architecture.

Purpose: land the runtime tests derived from `043-fixes-spec-2.md`, `043-TODO-2.md`, and the `SPEC2-E2E-01` through `SPEC2-E2E-09` scenarios in `043-TEST-SPEC.md`.
Output: focused Rust tests that prove the typed audit, wallet-stem naming, required canonical JSONL history, replay/import, artifact separation, redaction, and no-config-toggle contracts.
</objective>

<scenario_catalogue>

`043-17` owns runtime tests for `SPEC2-E2E-01` through `SPEC2-E2E-09`. `SPEC2-E2E-10` is an evidence
and closeout contract owned by `043-18`.

| Scenario | Test homes | Planned test names | Pass/fail oracle |
| --- | --- | --- | --- |
| `SPEC2-E2E-01` | `test_tx_pedersen.rs`, `test_spend_proof_backend.rs` | `audit_outcome_passes_target`, `audit_outcome_fails_typed` | A valid audit returns `Pass`; missing evidence, wrong root, duplicate entry, asset-class mismatch, commitment mismatch, and target mismatch return `FailClosed` with the precise mismatch class and entry index when applicable. |
| `SPEC2-E2E-02` | `test_wallet_service_suite.rs` | `wallet_history_names_share_stem`, `legacy_history_name_rejected` | Snapshot, JSONL, and live directory names share one wallet stem; `tx_history_<stem>.jsonl` never appears as the canonical helper output. |
| `SPEC2-E2E-03` | `test_wallet_export_pack_boundary.rs`, `backup_exporter` suite | `forensic_export_emits_jsonl`, `forensic_export_requires_jsonl` | Export writes the encrypted archive plus colocated wallet-prefixed JSONL from the same validated record set; missing JSONL or wrong output root fails the export contract. |
| `SPEC2-E2E-04` | `test_wallet_export_pack_boundary.rs`, `test_redb_wlt_open.rs` | `wlt_restore_ignores_history`, `wlt_export_requires_history` | Restore/load remains wallet-state-first without requiring JSONL input; export-time omission of the required JSONL artifact fails on the archive/export seam. |
| `SPEC2-E2E-05` | `test_tx_store_integration.rs`, `test_wallet_service_suite.rs` | `jsonl_replay_preserves_record`, `jsonl_replay_rejects_tamper` | Replay hydrates `TxStorageImpl` with `tx_hash`, `tx_bytes`, `status`, `timestamp_ms`, and `block_height`; malformed, duplicate, or hash-mismatched lines leave the live store unchanged. |
| `SPEC2-E2E-06` | `test_backup_importer_suite.rs`, `test_tx_store_integration.rs` | `import_mode_gates_history`, `import_rejects_before_mutation` | `WalletOnly`, `TxHistoryOnly`, and `WalletPlusHistory` mutate only selected state; malformed forensic input fails before wallet or live-store mutation. |
| `SPEC2-E2E-07` | `test_tx_store_integration.rs`, `test_wallet_service_suite.rs`, `tx_rpc_storage.rs` | `artifact_paths_remain_distinct` | Encrypted archive path, `.wlt` snapshot, canonical JSONL file, live tx-history directory, and `outputs/tx_exports` remain distinct by path and semantic role. |
| `SPEC2-E2E-08` | `test_wallet_export_pack_boundary.rs`, `test_wallet_service_suite.rs` | `operator_jsonl_redacts_secrets` | JSONL remains hash-bound and may carry opaque `TxRecord.tx_bytes`, but sentinel seed phrases, wallet identity secrets, decrypted asset-pack fields, blinding secrets, and unapproved plaintext evidence are absent. |
| `SPEC2-E2E-09` | `test_rpc_types_serialization.rs`, `test_backup_importer_suite.rs` | `forensic_toggle_not_persisted` | Forensic export/import enablement is explicit through constructor or import mode; `PersistBackupSettings` gains no persisted forensic toggle. |

</scenario_catalogue>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/043-gaps-fixes/043-fixes-spec-2.md
@.planning/phases/043-gaps-fixes/043-TODO-2.md
@.planning/phases/043-gaps-fixes/043-TEST-SPEC.md
@.planning/phases/043-gaps-fixes/043-TESTS-TASKS.md
@.planning/phases/043-gaps-fixes/043-coverage.md
</context>

<tasks>

<task type="auto">
  <name>043-17 Spec-2 E2E Integration Tests</name>
  <files>
    crates/z00z_wallets/tests/test_tx_pedersen.rs
    crates/z00z_wallets/tests/test_spend_proof_backend.rs
    crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
    crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs
    crates/z00z_wallets/tests/test_tx_store_integration.rs
    crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs
    crates/z00z_wallets/tests/test_redb_wlt_open.rs
    crates/z00z_wallets/tests/test_rpc_types_serialization.rs
  </files>
  <guard>Read `043-fixes-spec-2.md`, `043-TODO-2.md`, `043-TEST-SPEC.md`, and the existing test homes before editing; reuse existing fixtures and seams before creating any new file.</guard>
  <action>Implement `SPEC2-E2E-01` through `SPEC2-E2E-09` from `043-TEST-SPEC.md`. Keep tests in existing truthful homes unless reuse would blur ownership. Prove typed audit outcome behavior, wallet-stem naming, required JSONL emission beside `.wlt`, JSONL replay into `TxStorageImpl` with full `TxRecord` visibility, import-mode gating, tamper rejection before mutation, artifact-path separation, redacted/hash-bound plaintext artifacts, and the absence of a persisted forensic toggle in `PersistBackupSettings`. Do not create a second archive stack, second tx-history store, second verifier, `043-TEST-SPEC-2.md`, `043-TESTS-TASKS-2.md`, `043-coverage-2.md`, or `043-SUMMARY-2.md`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>Run the spec-2 E2E source-shape gate: `rg -n "WalletTxHistoryJsonlEntry|wallet_history_jsonl_name|wallet_.*_tx_history\\.jsonl|AssetClassAuditOutcome|AssetClassAuditMismatchClass|ForensicImportMode|PersistBackupSettings|outputs/tx_exports" crates/z00z_wallets/src crates/z00z_wallets/tests`.</automated>
    <automated>Run the focused spec-2 E2E test wave: `cargo test -p z00z_wallets --test test_wallet_export_pack_boundary -- --nocapture`, `cargo test -p z00z_wallets --lib backup_importer_impl -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_store_integration -- --nocapture`, `cargo test -p z00z_wallets --lib wallet_service_suite -- --nocapture`, `cargo test -p z00z_wallets --test test_redb_wlt_open -- --nocapture`, and `cargo test -p z00z_wallets --test test_rpc_types_serialization -- --nocapture`.</automated>
    <automated>Run the audit-focused gates: `cargo test -p z00z_wallets --test test_tx_pedersen -- --nocapture` and `cargo test -p z00z_wallets --test test_spend_proof_backend -- --nocapture`.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/043-gaps-fixes/043-TEST-SPEC.md current_task="043-17 Spec-2 E2E Integration Tests"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs show no significant issues in the code.</automated>
  </verify>
  <done>Spec-2 E2E tests exist in truthful test homes, prove both positive and negative paths, and do not introduce parallel implementation or planning surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| test expansion -> code architecture | Tests must lock existing seams, not invent production or test-only parallel layers |
| plaintext JSONL -> operator evidence | JSONL may carry replay data only when hash-bound and secret-redacted |
| import validation -> mutation | No wallet-state or live-store mutation may occur before validation succeeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-043-17-01 | T | artifact paths | mitigate | Assert archive, snapshot, JSONL, live dir, and RPC export paths are distinct |
| T-043-17-02 | I | plaintext artifacts | mitigate | Add redaction and hash-binding assertions for JSONL and closeout evidence |
| T-043-17-03 | E | import mode | mitigate | Prove tx-history mutation requires explicit `ForensicImportMode` |

</threat_model>

<verification>
Re-read every new or changed test against `043-fixes-spec-2.md` and confirm each assertion maps to a `SPEC2-E2E-*` row.
</verification>

<success_criteria>
Spec-2 E2E runtime coverage is implementation-ready, focused, positive/negative, and anchored to existing seams.
</success_criteria>

<output>
After completion, record test names and command results in `.planning/phases/043-gaps-fixes/043-coverage.md`; defer final closeout wording to `043-18`.
</output>
