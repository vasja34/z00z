<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 12
type: execute
wave: 6
depends_on:
  - 030-03
  - 030-04
files_modified:
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/README.md
  - crates/z00z_utils/src/io/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/mod.rs
  - .planning/phases/030-refactor-long-files/030-todo.md
  - crates/z00z_crypto/tests/test_public_surface.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_simulator/tests/test_stage4_source_shape.rs
autonomous: true
requirements:
  - PH30-NORMALIZE
  - PH30-SYNC
  - PH30-VERIFY
must_haves:
  truths:
    - Crypto and support callers use the final shallow export surfaces only after structural splits are stable.
    - Crypto/support docs, rustdoc, and planning refs match the final path surface.
    - Crypto normalization preserves the already approved public namespaces `domains`, `kdf_domains`, `aead_transport`, `hash_policy`, `hash_types`, `kdf_consensus`, and `kdf_extended` instead of collapsing them into a single replacement surface.
  artifacts:
    - path: crates/z00z_crypto/src/lib.rs
      provides: Final shallow crypto export surface.
    - path: crates/z00z_utils/src/io/mod.rs
      provides: Final shallow io export surface.
  key_links:
    - from: crates/z00z_crypto/src/lib.rs
      to: crates/z00z_wallets/tests/test_kdf.rs
      via: final crypto facade remains aligned to wallet-facing derivation
      pattern: kdf|hash|aead
---

<objective>
Normalize crypto and support caller-visible paths and synchronize docs, rustdoc, and planning refs.

Purpose: finish the crypto and support-surface cleanup on one coherent export story instead of leaving mixed internal-path references after the structural split waves.
Output: normalized crypto/io/support facades, synchronized docs/planning refs, and no stale deep-path references in code or rustdoc.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_crypto/src/lib.rs
@crates/z00z_crypto/README.md
@crates/z00z_utils/src/io/mod.rs
@crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs
@crates/z00z_simulator/src/scenario_1/stage_6_utils/mod.rs
@crates/z00z_crypto/tests/test_hash_policy.rs
@crates/z00z_crypto/tests/test_domain_separation.rs
@crates/z00z_crypto/tests/test_public_surface.rs
@crates/z00z_wallets/tests/test_kdf.rs
@crates/z00z_simulator/tests/test_stage4_split.rs
@crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
@crates/z00z_simulator/tests/test_stage4_source_shape.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the normalization affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave and record it with the caller inventory.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the dedicated crypto and support normalization wave</name>
  <files>crates/z00z_crypto/src/lib.rs, crates/z00z_utils/src/io/mod.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/mod.rs</files>
  <behavior>
    - Crypto, io, and simulator helper callers depend on the final shallow export surfaces.
    - No stale deep support paths survive after normalization.
  </behavior>
  <action>Use grep-backed caller inventories to finish the crypto and support normalization wave only after the relevant structural splits are stable. Keep `z00z_crypto::lib.rs` and `z00z_utils::io::mod.rs` as the final shallow facades, preserve the approved public namespaces `domains`, `kdf_domains`, `aead_transport`, `hash_policy`, `hash_types`, `kdf_consensus`, and `kdf_extended`, and keep simulator helper modules referenced through the intended utility mod surfaces rather than stale file-specific imports.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --test test_hash_policy -- --nocapture
cargo test -p z00z_crypto --release --test test_domain_separation -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture
cargo test -p z00z_wallets --release --test test_kdf -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_split -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_source_shape -- --nocapture
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test -p z00z_utils --release --features test-fast --all-targets -- --nocapture
cargo test -p z00z_crypto --release --features test-fast -- --nocapture
rg -n "pub mod domains;|pub mod kdf_domains;|pub mod aead_transport|pub mod hash_policy|pub mod hash_types|pub mod kdf_consensus|pub mod kdf_extended" crates/z00z_crypto/src/lib.rs
test -z "$(rg -n '^hash_domain!\(|^pub const HKDF_INFO_' crates/z00z_crypto/src -g '*.rs' | grep -v 'crates/z00z_crypto/src/domains.rs' | grep -v 'crates/z00z_crypto/src/kdf_domains.rs' || true)"
rg -n "z00z_crypto::(hash::|kdf::|aead::)|z00z_utils::io::fs::|stage_4_utils::tx_lane_impl|stage_6_utils::bundle_lane_impl" crates -g '*.rs'
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Crypto and support callers use the final shallow facades, and stale deep support paths are removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize crypto and support docs, rustdoc, and planning refs with the final surface</name>
  <files>crates/z00z_crypto/README.md, .planning/phases/030-refactor-long-files/030-todo.md, crates/z00z_crypto/src/lib.rs, crates/z00z_utils/src/io/mod.rs</files>
  <behavior>
    - Docs and planning refs stop pointing at stale crypto or support paths.
    - Rustdoc examples align with the final shallow exports.
  </behavior>
  <action>Update the crypto README, rustdoc-facing export examples, and phase-local planning references so they describe the final shallow crypto and support surfaces exactly. Close the wave only when code, docs, and planning refs agree.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --test test_hash_policy -- --nocapture
cargo test -p z00z_wallets --release --test test_kdf -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture
cargo test -p z00z_crypto --release --features test-fast -- --nocapture
rg -n "hash::|kdf::|aead::|io::fs::|tx_lane_impl|bundle_lane_impl" crates/z00z_crypto crates/z00z_utils crates/z00z_simulator .planning/phases/030-refactor-long-files -g '*.rs' -g '*.md'
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Crypto/support code, docs, and planning refs all describe the same final shallow surface.</done>
</task>

</tasks>

<verification>
Close only after the crypto/support normalization wave clears stale deep paths and synchronizes docs, rustdoc, and planning refs.
</verification>

<success_criteria>
Crypto and support crates finish the refactor on one coherent shallow export story with no stale deep-path references left behind.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-12-SUMMARY.md`
</output>