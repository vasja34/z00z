<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_crypto/src/hash.rs
  - crates/z00z_crypto/src/kdf.rs
  - crates/z00z_crypto/src/aead.rs
  - crates/z00z_crypto/src/domains.rs
  - crates/z00z_crypto/src/kdf_domains.rs
  - crates/z00z_crypto/src/types.rs
  - crates/z00z_crypto/src/backend_tari.rs
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/tests/test_hash_policy.rs
  - crates/z00z_crypto/tests/test_domain_separation.rs
  - crates/z00z_crypto/tests/domain_separation_tests.rs
  - crates/z00z_crypto/tests/test_public_surface.rs
  - crates/z00z_wallets/tests/test_kdf.rs
autonomous: true
requirements:
  - PH30-PROTECTED
  - PH30-VERIFY
must_haves:
  truths:
    - Domain declarations in `domains.rs`, KDF info ownership in `kdf_domains.rs`, transport framing in `aead_transport`, and public hash or KDF policy namespaces remain singular owners.
    - `z00z_crypto::lib.rs` remains the canonical top-level crypto export surface, and no new public namespace appears beside the existing approved surfaces.
    - Wallet-facing derivation and envelope behavior remains green through existing anchors.
  artifacts:
    - path: crates/z00z_crypto/src/lib.rs
      provides: Stable crypto export surface.
    - path: crates/z00z_crypto/src/aead.rs
      provides: AEAD facade and rustdoc anchor.
    - path: crates/z00z_crypto/tests/test_hash_policy.rs
      provides: Hash-policy regression anchor.
  key_links:
    - from: crates/z00z_crypto/src/lib.rs
      to: crates/z00z_wallets/tests/test_kdf.rs
      via: wallet-facing derivation keeps using the canonical crypto facade
      pattern: kdf|aead|hash
---

<objective>
Split `z00z_crypto` protected owner surfaces behind the stable public crypto facade.

Purpose: reduce review cost in crypto-heavy files without fragmenting domain-separation ownership, public entrypoints, or wallet-facing derivation behavior.
Output: focused crypto internals with one stable public facade and aligned cross-crate anchors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_crypto/src/hash.rs
@crates/z00z_crypto/src/kdf.rs
@crates/z00z_crypto/src/aead.rs
@crates/z00z_crypto/src/domains.rs
@crates/z00z_crypto/src/kdf_domains.rs
@crates/z00z_crypto/src/types.rs
@crates/z00z_crypto/src/backend_tari.rs
@crates/z00z_crypto/src/lib.rs
@crates/z00z_crypto/tests/test_hash_policy.rs
@crates/z00z_crypto/tests/test_domain_separation.rs
@crates/z00z_crypto/tests/domain_separation_tests.rs
@crates/z00z_crypto/tests/test_public_surface.rs
@crates/z00z_wallets/tests/test_kdf.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split hash, KDF, and AEAD files while keeping one canonical owner surface</name>
  <files>crates/z00z_crypto/src/hash.rs, crates/z00z_crypto/src/domains.rs, crates/z00z_crypto/src/blake2_hash.rs, crates/z00z_crypto/src/sha256_hash.rs, crates/z00z_crypto/src/hmac_sha256.rs, crates/z00z_crypto/src/kdf.rs, crates/z00z_crypto/src/kdf_domains.rs, crates/z00z_crypto/src/secret_bytes.rs, crates/z00z_crypto/src/argon2_params.rs, crates/z00z_crypto/src/argon2_kdf.rs, crates/z00z_crypto/src/hkdf_kdf.rs, crates/z00z_crypto/src/aead.rs, crates/z00z_crypto/src/aead_error.rs, crates/z00z_crypto/src/aead_primitives.rs, crates/z00z_crypto/src/aead_envelope.rs, crates/z00z_crypto/src/aead_aad.rs, crates/z00z_crypto/src/lib.rs</files>
  <behavior>
    - Domain tags, transcript framing, AAD builders, and KDF info constants keep one canonical owner each.
    - Public crypto entrypoints stay stable through `z00z_crypto::lib.rs` and the existing approved namespaces `domains`, `kdf_domains`, `aead_transport`, `hash_policy`, `hash_types`, `kdf_consensus`, and `kdf_extended`.
    - Rustdoc-visible AEAD examples remain truthful.
  </behavior>
  <action>Per D-14 and the crypto ownership rules, separate `hash.rs`, `kdf.rs`, and `aead.rs` by real semantic boundaries only. Do not allow parallel public helpers, shadow owner constants, or duplicate framing APIs to appear during the split. Keep `lib.rs` as the canonical top-level export surface, keep the existing approved namespaces `domains`, `kdf_domains`, `aead_transport`, `hash_policy`, `hash_types`, `kdf_consensus`, and `kdf_extended` as the only secondary public owner surfaces, and preserve existing wallet-facing usage through those surfaces. Do not move domain declarations out of `crates/z00z_crypto/src/domains.rs` or KDF info ownership out of `crates/z00z_crypto/src/kdf_domains.rs` into a new parallel namespace.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --test test_hash_policy -- --nocapture
cargo test -p z00z_crypto --release --test test_domain_separation -- --nocapture
cargo test -p z00z_crypto --release --test domain_separation_tests -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture
cargo test -p z00z_crypto --release --doc --features test-fast -- --nocapture
cargo test -p z00z_wallets --release --test test_kdf -- --nocapture
cargo test -p z00z_crypto --release --features test-fast -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The hash, KDF, and AEAD internals are split into coherent files while one canonical owner surface remains visible to callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split supporting crypto types and Tari backend helpers without widening the public facade</name>
  <files>crates/z00z_crypto/src/types.rs, crates/z00z_crypto/src/protocol_constants.rs, crates/z00z_crypto/src/crypto_constants.rs, crates/z00z_crypto/src/scalar_type.rs, crates/z00z_crypto/src/backend_tari.rs, crates/z00z_crypto/src/backend_init.rs, crates/z00z_crypto/src/backend_commitment.rs, crates/z00z_crypto/src/backend_range_proofs.rs, crates/z00z_crypto/src/backend_batch.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/tests/test_hash_policy.rs, crates/z00z_wallets/tests/test_kdf.rs</files>
  <behavior>
    - Support types and Tari backend helpers split cleanly without leaking new dependency-facing surfaces.
    - The public crypto facade stays stable and minimal.
  </behavior>
  <action>Move constants, scalar wrappers, backend init, commitment, range-proof, and batch helpers into cohesive supporting modules. Keep the public facade minimal and do not invent new external exports just because internals moved.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --test test_hash_policy -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture
cargo test -p z00z_wallets --release --test test_kdf -- --nocapture
cargo test -p z00z_crypto --release --features test-fast -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Supporting crypto types and backend helpers are clearer internally while the caller-facing crypto surface remains unchanged.</done>
</task>

</tasks>

<verification>
Close only after crypto-local and wallet-facing anchors stay green and the public crypto facade remains singular.
</verification>

<success_criteria>
Crypto internals are easier to navigate, but ownership of domain separation, AEAD framing, and KDF policy stays singular and externally stable.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-03-SUMMARY.md`
</output>
