<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 06
type: execute
wave: 3
depends_on:
  - 030-05
files_modified:
  - crates/z00z_wallets/src/core/key/seed.rs
  - crates/z00z_wallets/src/core/key/bip32.rs
  - crates/z00z_wallets/src/core/key/key_manager.rs
  - crates/z00z_wallets/src/core/key/mod.rs
  - crates/z00z_wallets/src/core/wallet/wallet.rs
  - crates/z00z_wallets/src/core/wallet/mod.rs
  - crates/z00z_wallets/tests/test_key_manager.rs
  - crates/z00z_wallets/tests/test_bip44.rs
  - crates/z00z_wallets/tests/test_seed_salt_policy.rs
  - crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
  - crates/z00z_wallets/tests/test_key_manager_storage_unlock.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-FACADE
  - PH30-VERIFY
must_haves:
  truths:
    - Seed, derivation path, key cache/state, and wallet identity code no longer live in one oversized ownership cluster.
    - Key and wallet facades remain stable while internals split.
  artifacts:
    - path: crates/z00z_wallets/src/core/key/mod.rs
      provides: Stable key facade.
    - path: crates/z00z_wallets/src/core/wallet/mod.rs
      provides: Stable wallet facade.
  key_links:
    - from: crates/z00z_wallets/src/core/key/mod.rs
      to: crates/z00z_wallets/tests/test_key_manager.rs
      via: key facade remains aligned with derivation and cache invariants
      pattern: KeyManager|Bip44Path|init_from_seed
---

<objective>
Split wallet key and identity surfaces behind stable key and wallet facades.

Purpose: isolate key derivation, seed policy, and wallet-identity logic without destabilizing caller-facing key or wallet contracts.
Output: focused key and wallet modules behind the current facades plus preserved derivation anchors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_wallets/src/core/key/seed.rs
@crates/z00z_wallets/src/core/key/bip32.rs
@crates/z00z_wallets/src/core/key/key_manager.rs
@crates/z00z_wallets/src/core/key/mod.rs
@crates/z00z_wallets/src/core/wallet/wallet.rs
@crates/z00z_wallets/src/core/wallet/mod.rs
@crates/z00z_wallets/tests/test_key_manager.rs
@crates/z00z_wallets/tests/test_bip44.rs
@crates/z00z_wallets/tests/test_seed_salt_policy.rs
@crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
@crates/z00z_wallets/tests/test_key_manager_storage_unlock.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split seed, BIP32, and key-manager files by semantic ownership</name>
  <files>crates/z00z_wallets/src/core/key/seed.rs, crates/z00z_wallets/src/core/key/seed_entropy_validation.rs, crates/z00z_wallets/src/core/key/seed_entropy_source.rs, crates/z00z_wallets/src/core/key/seed_mnemonic.rs, crates/z00z_wallets/src/core/key/seed_cipher.rs, crates/z00z_wallets/src/core/key/seed_kdf_params.rs, crates/z00z_wallets/src/core/key/seed_backup_format.rs, crates/z00z_wallets/src/core/key/bip32.rs, crates/z00z_wallets/src/core/key/bip32_constants.rs, crates/z00z_wallets/src/core/key/bip32_path.rs, crates/z00z_wallets/src/core/key/bip32_path_validator.rs, crates/z00z_wallets/src/core/key/bip32_child_key.rs, crates/z00z_wallets/src/core/key/bip32_key_deriver.rs, crates/z00z_wallets/src/core/key/bip32_ristretto_bridge.rs, crates/z00z_wallets/src/core/key/key_manager.rs, crates/z00z_wallets/src/core/key/key_cache.rs, crates/z00z_wallets/src/core/key/key_derivation.rs, crates/z00z_wallets/src/core/key/key_state.rs, crates/z00z_wallets/src/core/key/key_persistence.rs, crates/z00z_wallets/src/core/key/key_manager_impl.rs, crates/z00z_wallets/src/core/key/mod.rs</files>
  <behavior>
    - Seed policy, derivation path parsing, derivation execution, cache/state, and persistence separate into coherent ownership seams.
    - The key facade remains stable.
  </behavior>
  <action>Split the key stack only where the boundary is semantically meaningful and preserves the existing derivation policy. Keep one stable `core::key` facade and do not introduce a second path-policy or key-cache abstraction by accident. Preserve the atomic contract `decrypted BIP-39 seed -> BIP-44 path validation -> ChainType-scoped Ristretto bridge -> encrypted-seed persistence -> unlock-from-storage`, and do not create new wallet-local KDF, AEAD, or domain constants that shadow `z00z_crypto` or `redb_wallet_crypto` owner surfaces.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_key_manager -- --nocapture
cargo test -p z00z_wallets --release --test test_bip44 -- --nocapture
cargo test -p z00z_wallets --release --test test_seed_salt_policy -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_kdf_migration -- --nocapture
cargo test -p z00z_wallets --release --test test_key_manager_storage_unlock -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The key stack no longer hides multiple concerns in three giant files, and the key facade still exposes one coherent contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split `wallet.rs` into coherent identity and record seams behind the wallet facade</name>
  <files>crates/z00z_wallets/src/core/wallet/wallet.rs, crates/z00z_wallets/src/core/wallet/chain_id.rs, crates/z00z_wallets/src/core/wallet/wallet_identity.rs, crates/z00z_wallets/src/core/wallet/wallet_kernel.rs, crates/z00z_wallets/src/core/wallet/wallet_record.rs, crates/z00z_wallets/src/core/wallet/wallet_entity.rs, crates/z00z_wallets/src/core/wallet/mod.rs</files>
  <behavior>
    - Wallet identity, chain typing, kernel, record, and entity ownership separate cleanly.
    - The wallet facade remains caller-visible and stable.
  </behavior>
  <action>Refactor `wallet.rs` into coherent wallet-identity and record seams while keeping `core::wallet::mod.rs` as the stable outer surface. Preserve the current chain-typing and identity behavior already relied on by key and service layers.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_key_manager -- --nocapture
cargo test -p z00z_wallets --release --test test_bip44 -- --nocapture
cargo test -p z00z_wallets --release --test test_seed_salt_policy -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_kdf_migration -- --nocapture
cargo test -p z00z_wallets --release --test test_key_manager_storage_unlock -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The wallet facade stays coherent while `wallet.rs` no longer mixes identity, chain, kernel, and record ownership in one file.</done>
</task>

</tasks>

<verification>
Close only after key and wallet derivation anchors remain green and no duplicate key or wallet abstraction appears.
</verification>

<success_criteria>
Wallet key and identity code becomes structurally homogeneous while key and wallet facades stay stable for downstream callers.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-06-SUMMARY.md`
</output>
