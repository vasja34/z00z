<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_wallets/src/db/redb_wallet_store.rs
  - crates/z00z_wallets/src/db/mod.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  - crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
  - crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
  - crates/z00z_wallets/tests/test_key_manager_storage_unlock.rs
  - crates/z00z_wallets/tests/test_wallet_persistence_no_std.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-PROTECTED
  - PH30-VERIFY
must_haves:
  truths:
    - The wallet store no longer keeps migrations, crypto, objects, sessions, backup, and query helpers in one monolith.
    - `crate::db::redb_wallet_store::*` remains the stable caller-visible boundary while the internal store layout changes.
    - Boundary-sensitive store symbols keep one coherent owner surface during the split.
    - `crates/z00z_wallets/src/db/redb_wallet_crypto.rs` remains the sole owner of the persisted-wallet KDF, AAD, and envelope contract.
  artifacts:
    - path: crates/z00z_wallets/src/db/redb_wallet_store.rs
      provides: Stable store facade and orchestration surface.
    - path: crates/z00z_wallets/src/db/mod.rs
      provides: Stable wallet-db re-export boundary.
    - path: crates/z00z_wallets/tests/test_redb_wlt_open.rs
      provides: Open and migration regression anchor.
  key_links:
    - from: crates/z00z_wallets/src/db/redb_wallet_store.rs
      to: crates/z00z_wallets/src/db/mod.rs
      via: db facade re-exports preserve the store boundary
      pattern: redb_wallet_store
    - from: crates/z00z_wallets/src/db/redb_wallet_store.rs
      to: crates/z00z_wallets/tests/test_redb_wlt_open.rs
      via: open and migration semantics remain stable
      pattern: create_wlt|open_wlt|migrate_
---

<objective>
Split the RedB wallet-store monolith behind a stable persistence facade and protected session boundary.

Purpose: remove the highest-risk mixed-concern wallet file without destabilizing caller-visible store contracts or key-bearing session ownership.
Output: extracted store modules behind the existing `redb_wallet_store` surface plus aligned persistence regression anchors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_wallets/src/db/redb_wallet_store.rs
@crates/z00z_wallets/src/db/mod.rs
@crates/z00z_wallets/src/db/redb_wallet_crypto.rs
@crates/z00z_wallets/tests/test_redb_wlt_open.rs
@crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs
@crates/z00z_wallets/tests/test_tx_store_integration.rs
@crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
@crates/z00z_wallets/tests/test_key_manager_storage_unlock.rs
@crates/z00z_wallets/tests/test_wallet_persistence_no_std.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract semantic store modules behind the stable `redb_wallet_store` facade</name>
  <files>crates/z00z_wallets/src/db/redb_wallet_store.rs, crates/z00z_wallets/src/db/redb_wallet_store_tables.rs, crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs, crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs, crates/z00z_wallets/src/db/redb_wallet_crypto.rs, crates/z00z_wallets/src/db/redb_wallet_store_session.rs, crates/z00z_wallets/src/db/redb_wallet_store_backup.rs, crates/z00z_wallets/src/db/redb_wallet_store_queries.rs</files>
  <behavior>
    - The store splits by responsibility instead of numeric chunks.
    - `WltSession`, `ScanStatePayload`, lock helpers, and session-backed store helpers stay behind one stable caller-visible facade.
    - No duplicate crypto or session helper surface appears during the split.
  </behavior>
  <action>Per D-01 through D-03b, D-06, D-14, and D-15, split `redb_wallet_store.rs` along semantic seams only. Keep the root file as the stable orchestration and compatibility surface, and move tables, codecs, migrations, session, backup, and query helpers into proposed sibling modules only where each extracted file owns one homogeneous responsibility. Do not shard a coherent subsystem merely to hit a line target. Preserve the current external `crate::db::redb_wallet_store::*` contract during this wave. The persisted-wallet KDF, AAD, and envelope contract must stay owned by the existing `crates/z00z_wallets/src/db/redb_wallet_crypto.rs`; do not create a second wallet-store crypto surface or duplicate its constants and helpers.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_redb_wlt_open -- --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_store_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_kdf_migration -- --nocapture
cargo test -p z00z_wallets --release --test test_key_manager_storage_unlock -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The wallet store is split into coherent internal modules while `redb_wallet_store.rs` remains the stable caller-visible boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align the db facade, caller boundary, and persistence anchors to the split</name>
  <files>crates/z00z_wallets/src/db/mod.rs, crates/z00z_wallets/tests/test_redb_wlt_open.rs, crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs, crates/z00z_wallets/tests/test_tx_store_integration.rs, crates/z00z_wallets/tests/test_wallet_persistence_no_std.rs</files>
  <behavior>
    - The db facade stays coherent and does not expose deep internal module paths.
    - Existing integration anchors still prove wallet-open, snapshot-discovery, tx-store, and persistence-boundary behavior.
    - No forbidden direct std I/O creeps back in while files move.
  </behavior>
  <action>Keep `crates/z00z_wallets/src/db/mod.rs` as the stable re-export surface for wallet persistence. Update tests only where the split changes internal ownership, not public behavior. Preserve the no-raw-std-I/O boundary and record the exact grep audit scope used for any moved store symbols.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_wallet_persistence_no_std -- --nocapture
cargo test -p z00z_wallets --release --test test_redb_wlt_open -- --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_store_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_kdf_migration -- --nocapture
cargo test -p z00z_wallets --release --test test_key_manager_storage_unlock -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
rg -n "crate::db::redb_wallet_store::|redb_wallet_store::" crates/z00z_wallets -g '*.rs'
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The db facade stays shallow, the store split remains behavior-preserving, and the persistence anchors remain green.</done>
</task>

</tasks>

<verification>
Run bootstrap first, then the named wallet-store anchors, then the release profile gate, and do not close the plan until repeated task-execution reviews are clean.
</verification>

<success_criteria>
`redb_wallet_store.rs` no longer acts as a mixed persistence monolith, but the wallet store boundary remains stable and test-proven.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-01-SUMMARY.md`
</output>
