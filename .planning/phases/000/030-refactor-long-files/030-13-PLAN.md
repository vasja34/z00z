<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 13
type: execute
wave: 7
depends_on:
  - 030-01
files_modified:
  - crates/z00z_wallets/src/db/redb_wallet_store.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto.rs
  - crates/z00z_wallets/src/db/storage_backend.rs
  - crates/z00z_wallets/src/db/index_codecs.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_objects.rs
  - crates/z00z_wallets/src/db/mod.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-PROTECTED
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet DB continuation removes the remaining mixed persistence seams still left above the 400-line residue band.
    - `crate::db` remains the only caller-facing wallet persistence surface during the continuation split.
    - Boundary-sensitive wallet DB symbols stay singular and caller-stable during the split: `WltSession`, `ScanStatePayload`, lock helpers, session readers, and session-backed store helpers.
    - Session, codec, crypto, migration, and object-storage ownership stay explicit and testable.
  artifacts:
    - path: crates/z00z_wallets/src/db/redb_wallet_store.rs
      provides: Thin persistence facade over focused wallet-store seams.
    - path: crates/z00z_wallets/src/db/mod.rs
      provides: Stable shallow db facade.
  key_links:
    - from: crates/z00z_wallets/src/db/mod.rs
      to: crates/z00z_wallets/tests/test_redb_wlt_open.rs
      via: shallow wallet db contract remains stable through the split
      pattern: open_wlt|create_wlt|WltSession
---

<objective>
Continue the wallet DB split until the remaining persistence files no longer sit above the Phase 030 residue band.

Purpose: finish the largest wallet persistence cleanup without breaking the caller-visible `crate::db` contract.
Output: thinner DB root files, smaller seam modules, and preserved RedB/session behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_wallets/src/db/redb_wallet_store.rs
@crates/z00z_wallets/src/db/redb_wallet_crypto.rs
@crates/z00z_wallets/src/db/storage_backend.rs
@crates/z00z_wallets/src/db/index_codecs.rs
@crates/z00z_wallets/src/db/mod.rs
@crates/z00z_wallets/tests/test_redb_wlt_open.rs
@crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs
@crates/z00z_wallets/tests/test_tx_store_integration.rs
@crates/z00z_wallets/tests/test_wlt_validator.rs
@crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish splitting the oversized wallet-store root and storage-owned helper seams</name>
  <files>crates/z00z_wallets/src/db/redb_wallet_store.rs, crates/z00z_wallets/src/db/storage_backend.rs, crates/z00z_wallets/src/db/redb_wallet_store_objects.rs, crates/z00z_wallets/src/db/index_codecs.rs</files>
  <behavior>
    - The wallet-store root becomes a thin facade plus orchestration layer rather than a mixed persistence monolith.
    - Storage backend, codecs, and object persistence seams become independently reviewable.
  </behavior>
  <action>Continue the split from plan 01 by extracting or consolidating homogeneous seams still trapped in `redb_wallet_store.rs` and adjacent large DB helpers. Preserve `crate::db`, `WltSession`, `ScanStatePayload`, lock helpers, session readers, and session-backed store helpers as the stable caller contract, and do not reintroduce direct deep-path imports in wallet callers while reducing every touched DB implementation file below the continuation target band.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_redb_wlt_open -- --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_store_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_wlt_validator -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>if rg -n 'crate::db::redb_wallet_store(::|_)|crate::db::redb_wallet_store_[a-z_]+' crates/z00z_wallets/src -g '*.rs' | grep -v '^crates/z00z_wallets/src/db/'; then
  echo 'Legacy wallet DB deep imports remain outside the shallow crate::db facade'
  exit 1
fi</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The main wallet-store root and adjacent DB seams stop behaving like one oversized persistence blob.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce RedB crypto, migration, and wallet-store support files without widening the public DB surface</name>
  <files>crates/z00z_wallets/src/db/redb_wallet_crypto.rs, crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs, crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs, crates/z00z_wallets/src/db/mod.rs</files>
  <behavior>
    - Crypto and migration helpers stop accumulating unrelated responsibilities.
    - The shallow DB facade remains stable while internal ownership becomes explicit.
  </behavior>
  <action>Further split or reorganize the remaining oversized DB support files so crypto envelopes, migration logic, and RedB-specific helper operations stay semantically isolated. Keep the public DB facade stable, preserve the wallet migration contract, and update any guard tests or docs needed to keep the shallow DB surface truthful.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_wallet_kdf_migration -- --nocapture
cargo test -p z00z_wallets --release --test test_redb_wlt_open -- --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_store_integration -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Wallet DB crypto and migration ownership are explicit, and the DB facade remains stable while the residue band drops.</done>
</task>

</tasks>

<verification>
Close only after the wallet DB continuation leaves no touched persistence file above the >400-line continuation band and the shallow DB contract remains stable.
</verification>

<success_criteria>
Wallet persistence, migration, and RedB support files are split into coherent seams without breaking `crate::db` callers or session-backed storage behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-13-SUMMARY.md`
</output>