<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 09
type: execute
wave: 5
depends_on:
  - 030-01
  - 030-03
  - 030-05
  - 030-06
  - 030-07
files_modified:
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs
  - crates/z00z_wallets/src/core/tx/state_update.rs
  - crates/z00z_wallets/src/core/tx/claim_tx.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/src/core/tx/spending.rs
  - crates/z00z_wallets/src/core/tx/mod.rs
  - crates/z00z_wallets/tests/test_asset_ownership_security.rs
  - crates/z00z_wallets/tests/test_tx_assetpack.rs
  - crates/z00z_wallets/tests/test_claim_state_core.rs
  - crates/z00z_wallets/src/core/tx/test_claim_tx.rs
  - crates/z00z_wallets/tests/test_tx_digest_framing.rs
  - crates/z00z_wallets/tests/test_tx_fee.rs
  - crates/z00z_wallets/tests/test_tx_pass.rs
  - crates/z00z_wallets/tests/test_tx_poison.rs
  - crates/z00z_wallets/tests/test_view_key_contract.rs
  - crates/z00z_wallets/tests/test_tx_spent_gate.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-FACADE
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet RPC and tx-domain surfaces no longer mix transport, rate limiting, persistence, validation, claim, and spent-gate logic in oversized files.
    - The tx facade remains stable while tx and RPC internals split.
    - Claim and tx verification order stay behavior-compatible with the current reject-class expectations: claim verification preserves the existing pre-digest precedence, and generic tx verification preserves its current structure-before-balance-before-digest-before-signature-before-range-proof flow.
  artifacts:
    - path: crates/z00z_wallets/src/core/tx/mod.rs
      provides: Stable tx-domain facade.
    - path: crates/z00z_wallets/tests/test_tx_spent_gate.rs
      provides: Spent-gate and verification anchor.
  key_links:
    - from: crates/z00z_wallets/src/core/tx/mod.rs
      to: crates/z00z_wallets/tests/test_claim_state_core.rs
      via: tx-domain facade remains aligned to claim-state behavior
      pattern: claim|state|spent|verify
---

<objective>
Split wallet tx and RPC surfaces behind stable transaction and RPC facades.

Purpose: separate transport, transaction state, claim verification, and spending invariants without changing wallet-visible tx behavior.
Output: focused tx and RPC modules behind the current facades plus preserved tx and claim regression anchors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs
@crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs
@crates/z00z_wallets/src/core/tx/state_update.rs
@crates/z00z_wallets/src/core/tx/claim_tx.rs
@crates/z00z_wallets/src/core/tx/tx_verifier.rs
@crates/z00z_wallets/src/core/tx/spending.rs
@crates/z00z_wallets/src/core/tx/mod.rs
@crates/z00z_wallets/tests/test_asset_ownership_security.rs
@crates/z00z_wallets/tests/test_tx_assetpack.rs
@crates/z00z_wallets/tests/test_claim_state_core.rs
@crates/z00z_wallets/src/core/tx/test_claim_tx.rs
@crates/z00z_wallets/tests/test_tx_digest_framing.rs
@crates/z00z_wallets/tests/test_tx_fee.rs
@crates/z00z_wallets/tests/test_tx_pass.rs
@crates/z00z_wallets/tests/test_tx_poison.rs
@crates/z00z_wallets/tests/test_view_key_contract.rs
@crates/z00z_wallets/tests/test_tx_spent_gate.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split RPC transport files without duplicating tx or asset ownership logic</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_rpc_caches.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_rpc_rate_limits.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_rpc_stakes.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_rpc_balance.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_rpc_registry.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_rpc_history.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_rate_limits.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_idempotency.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_broadcast.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_impl.rs</files>
  <behavior>
    - Asset and tx RPC files separate caches, rate limits, registry/history, persistence, idempotency, and broadcast concerns.
    - RPC split does not recreate service or tx-domain ownership inside transport files.
  </behavior>
  <action>Split the RPC layer by transport-specific concerns only. Keep wallet-service and tx-domain ownership out of the new transport modules, and preserve the existing caller-visible RPC contract while the internals move.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_asset_ownership_security -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_assetpack -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The RPC layer is structurally split by transport responsibility without duplicating tx or asset ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split tx-state, claim, verifier, and spending files behind the stable tx facade</name>
  <files>crates/z00z_wallets/src/core/tx/state_update.rs, crates/z00z_wallets/src/core/tx/state_witness.rs, crates/z00z_wallets/src/core/tx/state_resolved_input.rs, crates/z00z_wallets/src/core/tx/state_checkpoint.rs, crates/z00z_wallets/src/core/tx/state_errors.rs, crates/z00z_wallets/src/core/tx/state_traits.rs, crates/z00z_wallets/src/core/tx/claim_tx.rs, crates/z00z_wallets/src/core/tx/claim_wire_types.rs, crates/z00z_wallets/src/core/tx/claim_errors.rs, crates/z00z_wallets/src/core/tx/claim_verifier.rs, crates/z00z_wallets/src/core/tx/claim_helpers.rs, crates/z00z_wallets/src/core/tx/claim_auth.rs, crates/z00z_wallets/src/core/tx/tx_verifier.rs, crates/z00z_wallets/src/core/tx/tx_wire_types.rs, crates/z00z_wallets/src/core/tx/tx_digest.rs, crates/z00z_wallets/src/core/tx/tx_errors.rs, crates/z00z_wallets/src/core/tx/spending.rs, crates/z00z_wallets/src/core/tx/spend_rules.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/spend_errors.rs, crates/z00z_wallets/src/core/tx/spend_test_helpers.rs, crates/z00z_wallets/src/core/tx/mod.rs</files>
  <behavior>
    - Tx state, claim, verification, digest, and spending invariants split along real semantics.
    - The tx facade stays stable and singular.
  </behavior>
  <action>Split tx-core files by real transaction-domain seams while keeping one stable tx facade. Preserve claim-state, spent-gate, and rotated-view invariants from the current tests. Keep the existing verifier ordering and reject-class progression explicit: claim verification must continue to resolve structure, scope, tx fields, nullifier, recipient card, leaf, claim proof, authority, owner attest, and only then digest mismatch; generic tx verification must continue to resolve structure, balance, digest, signatures, and then range proofs. Do not create a second claim or verifier entrypoint during the refactor.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_claim_state_core -- --nocapture
cargo test -p z00z_wallets --release test_claim_tx -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_digest_framing -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_fee -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_pass -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_poison -- --nocapture
cargo test -p z00z_wallets --release --test test_view_key_contract -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_spent_gate -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The tx core is split into coherent modules while the tx facade and current validation invariants remain stable.</done>
</task>

</tasks>

<verification>
Close only after RPC and tx-domain anchors remain green and the tx facade still exposes one coherent surface.
</verification>

<success_criteria>
Wallet transaction and RPC code no longer acts as a set of oversized mixed-concern files, but wallet-visible tx behavior and verification invariants stay unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-09-SUMMARY.md`
</output>
