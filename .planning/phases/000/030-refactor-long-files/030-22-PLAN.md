<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 22
type: execute
wave: 7
depends_on:
  - 030-03
  - 030-12
files_modified:
  - crates/z00z_crypto/src/aead.rs
  - crates/z00z_crypto/src/kdf.rs
  - crates/z00z_crypto/src/hash.rs
  - crates/z00z_crypto/src/types.rs
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/src/backend.rs
  - crates/z00z_crypto/src/ecdh.rs
  - crates/z00z_crypto/src/ecdh_stealth.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-PROTECTED
  - PH30-VERIFY
must_haves:
  truths:
    - Crypto continuation removes the remaining oversized protected roots above the residue band.
    - `z00z_crypto::lib.rs` remains the single shallow caller-visible crypto facade.
    - Domain-separation helpers, transcript framing, AAD builders, and KDF info constants remain canonically owned instead of fragmenting across backend or ECDH support files.
    - Domain/KDF/AEAD/hash ownership stays explicit and does not fragment into competing public surfaces.
  artifacts:
    - path: crates/z00z_crypto/src/lib.rs
      provides: Stable shallow crypto facade.
    - path: crates/z00z_crypto/src/hash.rs
      provides: Smaller hash-domain root over focused seams.
  key_links:
    - from: crates/z00z_crypto/src/lib.rs
      to: crates/z00z_crypto/tests/test_public_surface.rs
      via: crypto facade remains aligned to public surface expectations
      pattern: domains|kdf|aead|hash|ecdh
---

<objective>
Continue the crypto split until the remaining oversized crypto roots are reduced below the continuation band.

Purpose: finish crypto structural cleanup without widening or fragmenting the top-level crypto public surface.
Output: smaller crypto implementation roots and preserved shallow crypto exports.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_crypto/src/lib.rs
@crates/z00z_crypto/src/aead.rs
@crates/z00z_crypto/src/kdf.rs
@crates/z00z_crypto/src/hash.rs
@crates/z00z_crypto/src/types.rs
@crates/z00z_crypto/tests/test_hash_policy.rs
@crates/z00z_crypto/tests/test_domain_separation.rs
@crates/z00z_crypto/tests/test_public_surface.rs
@crates/z00z_wallets/tests/test_kdf.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce AEAD, KDF, hash, and type roots below the continuation band</name>
  <files>crates/z00z_crypto/src/aead.rs, crates/z00z_crypto/src/kdf.rs, crates/z00z_crypto/src/hash.rs, crates/z00z_crypto/src/types.rs, crates/z00z_crypto/src/lib.rs</files>
  <behavior>
    - AEAD, KDF, hash, and type roots become independently reviewable.
    - The shallow crypto facade remains singular and truthful.
  </behavior>
  <action>Continue the crypto split by reducing the remaining oversized AEAD, KDF, hash, and type roots below the >400 continuation band. Preserve the protected ownership rules from earlier Phase 030 work, keep `z00z_crypto::lib.rs` as the single caller surface, and do not introduce competing public crypto facades while extracting real ownership seams.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --test test_hash_policy -- --nocapture
cargo test -p z00z_crypto --release --test test_domain_separation -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture
cargo test -p z00z_wallets --release --test test_kdf -- --nocapture
cargo test -p z00z_crypto --release --doc -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The major crypto roots are reduced below the continuation band while the crypto facade stays singular and stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce backend and ECDH support residue without changing the public crypto story</name>
  <files>crates/z00z_crypto/src/backend.rs, crates/z00z_crypto/src/ecdh.rs, crates/z00z_crypto/src/ecdh_stealth.rs, crates/z00z_crypto/src/lib.rs</files>
  <behavior>
    - Backend and ECDH support files stop remaining as oversized support roots.
    - Public crypto surface and docs stay coherent.
  </behavior>
  <action>Reduce the remaining oversized backend and ECDH support files below the continuation band while preserving the current public crypto story. Keep backend ownership hidden behind the facade, preserve current ECDH behavior, keep transcript framing and AAD-builder ownership canonical, and align any helper reorganizations back to the same shallow crypto export surface.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --test test_hash_policy -- --nocapture
cargo test -p z00z_crypto --release --test test_domain_separation -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture
cargo test -p z00z_wallets --release --test test_kdf -- --nocapture
cargo test -p z00z_crypto --release --doc -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>if rg -n 'build_aad|AAD_|transcript|Merlin' crates/z00z_crypto/src/backend.rs crates/z00z_crypto/src/ecdh.rs crates/z00z_crypto/src/ecdh_stealth.rs; then
  echo 'AAD or transcript ownership leaked outside the canonical crypto seams'
  exit 1
fi</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Backend and ECDH support roots are reduced below the continuation band without changing the shallow crypto caller contract.</done>
</task>

</tasks>

<verification>
Close only after touched crypto roots fall below the continuation band and the shallow crypto public surface remains singular and test-backed.
</verification>

<success_criteria>
The remaining oversized crypto implementation roots are removed while the protected public crypto facade remains coherent and stable.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-22-SUMMARY.md`
</output>