<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 11
type: execute
wave: 6
depends_on:
  - 030-02
  - 030-08
files_modified:
  - crates/z00z_core/src/assets/mod.rs
  - crates/z00z_core/src/genesis/mod.rs
  - crates/z00z_core/README.md
  - .planning/phases/030-refactor-long-files/030-todo.md
  - crates/z00z_core/tests/assets/test_assets.rs
  - crates/z00z_core/tests/genesis/test_genesis.rs
autonomous: true
requirements:
  - PH30-NORMALIZE
  - PH30-SYNC
  - PH30-VERIFY
must_haves:
  truths:
    - Core asset and genesis callers use final shallow paths only after the structural split waves complete.
    - Core docs, rustdoc, and planning refs align to the final core facade layout.
  artifacts:
    - path: crates/z00z_core/src/assets/mod.rs
      provides: Final shallow asset facade.
    - path: crates/z00z_core/src/genesis/mod.rs
      provides: Final shallow genesis facade.
  key_links:
    - from: crates/z00z_core/src/genesis/mod.rs
      to: crates/z00z_core/tests/genesis/test_genesis.rs
      via: final core facade and tests agree on the same path surface
      pattern: ChainType|Genesis
---

<objective>
Normalize core caller-visible paths and synchronize docs, rustdoc, and planning refs.

Purpose: close the `z00z_core` structural refactor on final shallow asset and genesis facades instead of leaving a mixed path story behind.
Output: normalized core facades, updated docs/rustdoc, and no surviving legacy deep core-path references.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_core/src/assets/mod.rs
@crates/z00z_core/src/genesis/mod.rs
@crates/z00z_core/README.md
@crates/z00z_core/tests/assets/test_assets.rs
@crates/z00z_core/tests/genesis/test_genesis.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the normalization affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave and record it with the caller inventory.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the dedicated core normalization wave for assets and genesis</name>
  <files>crates/z00z_core/src/assets/mod.rs, crates/z00z_core/src/genesis/mod.rs, crates/z00z_core/tests/assets/test_assets.rs, crates/z00z_core/tests/genesis/test_genesis.rs</files>
  <behavior>
    - Core callers depend on the final shallow asset and genesis facades.
    - No legacy deep `genesis::genesis::ChainType` path survives.
  </behavior>
  <action>Use a grep-backed inventory to finish the core normalization wave only after the asset and genesis structural splits are stable. Remove remaining legacy deep core paths, record the caller inventory, and keep the final path surface shallow and singular.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_core --release --test test_assets -- --nocapture
cargo test -p z00z_core --release --test test_genesis -- --nocapture
cargo test -p z00z_core --release --test test_reproducibility -- --nocapture
cargo test -p z00z_core --release --features test-fast -- --nocapture
rg -n "z00z_core::genesis::genesis::ChainType|genesis::genesis::ChainType" crates -g '*.rs'
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Core callers use the final shallow asset and genesis facades, and no legacy deep genesis path remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize core docs, rustdoc, and planning refs with the normalized surface</name>
  <files>crates/z00z_core/README.md, .planning/phases/030-refactor-long-files/030-todo.md, crates/z00z_core/src/assets/mod.rs, crates/z00z_core/src/genesis/mod.rs</files>
  <behavior>
    - Core docs and planning refs match the final shallow core surface.
    - Rustdoc examples stop pointing at stale deep paths.
  </behavior>
  <action>Update the core README, rustdoc-facing examples, and phase-local planning references so they match the final asset and genesis facades exactly. Do not close the wave while docs still describe the pre-normalization path layout.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_core --release --test test_assets -- --nocapture
cargo test -p z00z_core --release --test test_genesis -- --nocapture
cargo test -p z00z_core --release --features test-fast -- --nocapture
rg -n "genesis::genesis::|assets::assets::|registry::registry::" crates/z00z_core crates/z00z_core/README.md .planning/phases/030-refactor-long-files -g '*.rs' -g '*.md'
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Core code, docs, and planning refs all point at the same final shallow facade surface.</done>
</task>

</tasks>

<verification>
Close only after the core normalization wave clears legacy deep paths and synchronizes docs, rustdoc, and planning refs.
</verification>

<success_criteria>
`z00z_core` finishes the refactor on one consistent shallow path surface for assets and genesis, with no stale documentation or deep-import residue.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-11-SUMMARY.md`
</output>
