<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_core/src/assets/assets.rs
  - crates/z00z_core/src/assets/registry.rs
  - crates/z00z_core/src/assets/nonce.rs
  - crates/z00z_core/src/assets/definition.rs
  - crates/z00z_core/src/assets/mod.rs
  - crates/z00z_core/tests/assets/test_assets.rs
  - crates/z00z_core/tests/assets/test_wire_format_snapshots.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-VERIFY
must_haves:
  truths:
    - The asset domain no longer keeps errors, classes, metadata, validation, ids, registry behavior, and nonce policy in one mixed file set.
    - Asset facade paths remain coherent while internal modules split.
    - Asset wire and snapshot compatibility stay green.
  artifacts:
    - path: crates/z00z_core/src/assets/assets.rs
      provides: Stable asset-domain facade.
    - path: crates/z00z_core/src/assets/mod.rs
      provides: Stable asset re-export surface.
    - path: crates/z00z_core/tests/assets/test_wire_format_snapshots.rs
      provides: Wire-compatibility regression anchor.
  key_links:
    - from: crates/z00z_core/src/assets/assets.rs
      to: crates/z00z_core/tests/assets/test_assets.rs
      via: asset-domain behavior remains stable through the split
      pattern: AssetError|AssetClass|AssetDefinition
---

<objective>
Split the `z00z_core` asset-domain monoliths while preserving wire, nonce, and registry contracts.

Purpose: make the core asset domain reviewable without changing canonical asset semantics, wire compatibility, or validation behavior.
Output: focused asset-domain modules behind the current asset facade and aligned asset-wire regression anchors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_core/src/assets/assets.rs
@crates/z00z_core/src/assets/registry.rs
@crates/z00z_core/src/assets/nonce.rs
@crates/z00z_core/src/assets/definition.rs
@crates/z00z_core/src/assets/mod.rs
@crates/z00z_core/tests/assets/test_assets.rs
@crates/z00z_core/tests/assets/test_wire_format_snapshots.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split `assets.rs` and `registry.rs` by semantic ownership instead of file size</name>
  <files>crates/z00z_core/src/assets/assets.rs, crates/z00z_core/src/assets/asset_error.rs, crates/z00z_core/src/assets/asset_class.rs, crates/z00z_core/src/assets/asset_metadata.rs, crates/z00z_core/src/assets/asset_validation.rs, crates/z00z_core/src/assets/asset_ownership.rs, crates/z00z_core/src/assets/registry.rs, crates/z00z_core/src/assets/registry_core.rs, crates/z00z_core/src/assets/registry_snapshot.rs, crates/z00z_core/src/assets/registry_config.rs, crates/z00z_core/src/assets/mod.rs</files>
  <behavior>
    - Asset-domain errors, metadata, ownership, validation, and registry behavior land on coherent seams.
    - The asset facade stays shallow and caller-visible paths remain stable.
    - The split does not replace one monolith with shard-like files.
  </behavior>
  <action>Per D-01 through D-03b and the non-soup rules in `030-todo.md`, separate `assets.rs` and `registry.rs` into homogeneous modules only where the responsibility boundary is real. Preserve `crates/z00z_core/src/assets/mod.rs` as the public re-export surface. Do not allow registry snapshot hashing, config loading, or mutation rules to drift into parallel abstractions.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_core --release --test test_assets -- --nocapture
cargo test -p z00z_core --release --test test_wire_format_snapshots -- --nocapture
cargo test -p z00z_core --release --features test-fast -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The main asset and registry files are split along semantic seams while the asset facade and compatibility anchors remain stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split nonce and definition policy files without changing canonical asset identity behavior</name>
  <files>crates/z00z_core/src/assets/nonce.rs, crates/z00z_core/src/assets/nonce_type.rs, crates/z00z_core/src/assets/nonce_counter.rs, crates/z00z_core/src/assets/nonce_derivation.rs, crates/z00z_core/src/assets/definition.rs, crates/z00z_core/src/assets/definition_id.rs, crates/z00z_core/src/assets/definition_validate.rs, crates/z00z_core/src/assets/mod.rs, crates/z00z_core/tests/assets/test_assets.rs, crates/z00z_core/tests/assets/test_wire_format_snapshots.rs</files>
  <behavior>
    - Nonce policy, nonce derivation, definition identity, and definition validation separate cleanly.
    - Canonical asset identity and wire snapshots remain unchanged.
  </behavior>
  <action>Keep canonical `AssetDefinition` identity, nonce policy, and validation logic explicit while moving them into more focused modules. Preserve the existing asset facade and any identity-bearing framing rules already hardened in earlier crypto-audit phases.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_core --release --test test_assets -- --nocapture
cargo test -p z00z_core --release --test test_wire_format_snapshots -- --nocapture
cargo test -p z00z_core --release --features test-fast -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Nonce and definition logic move behind coherent internal seams without changing canonical asset identity or snapshot compatibility.</done>
</task>

</tasks>

<verification>
Close only after asset integration and wire-snapshot tests stay green under the split and repeated execution reviews show no significant issues.
</verification>

<success_criteria>
The core asset domain is structurally clearer, wire-compatible, and still owned by one coherent asset facade.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-02-SUMMARY.md`
</output>
