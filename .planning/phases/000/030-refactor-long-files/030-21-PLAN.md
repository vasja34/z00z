<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 21
type: execute
wave: 7
depends_on:
  - 030-02
  - 030-08
  - 030-11
files_modified:
  - crates/z00z_core/src/assets/assets.rs
  - crates/z00z_core/src/assets/registry.rs
  - crates/z00z_core/src/assets/definition.rs
  - crates/z00z_core/src/assets/nonce.rs
  - crates/z00z_core/src/assets/leaf.rs
  - crates/z00z_core/src/assets/wire_pkg.rs
  - crates/z00z_core/src/assets/assets_config.rs
  - crates/z00z_core/src/genesis/genesis_output.rs
  - crates/z00z_core/bin/assets/assets_generation_cli.rs
  - crates/z00z_core/bin/genesis/assets_extractor_cli.rs
  - crates/z00z_core/bin/genesis/assets_analyzer_cli.rs
  - crates/z00z_core/src/genesis/mod.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-FACADE
  - PH30-VERIFY
must_haves:
  truths:
    - Core continuation removes the remaining oversized asset-domain and core CLI roots.
    - The shallow core asset/genesis surfaces remain stable while implementation ownership becomes explicit.
    - Asset/registry/wire and CLI behavior remain aligned with current core tests.
  artifacts:
    - path: crates/z00z_core/src/genesis/mod.rs
      provides: Stable shallow genesis facade.
    - path: crates/z00z_core/src/assets/assets.rs
      provides: Smaller asset-domain root over focused seams.
  key_links:
    - from: crates/z00z_core/src/genesis/mod.rs
      to: crates/z00z_core/tests/genesis/test_reproducibility.rs
      via: shallow core surfaces remain aligned to deterministic genesis behavior
      pattern: genesis|asset|registry|wire
---

<objective>
Continue the `z00z_core` split until the remaining oversized asset-domain and supporting CLI files are reduced below the residue band.

Purpose: finish core structural cleanup without breaking the shallow asset/genesis caller story created earlier in Phase 030.
Output: smaller core roots, explicit asset ownership seams, and preserved core behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_core/src/assets/assets.rs
@crates/z00z_core/src/assets/registry.rs
@crates/z00z_core/src/assets/definition.rs
@crates/z00z_core/src/assets/nonce.rs
@crates/z00z_core/src/genesis/mod.rs
@crates/z00z_core/tests/assets/test_assets.rs
@crates/z00z_core/tests/assets/test_wire_format_snapshots.rs
@crates/z00z_core/tests/genesis/test_genesis.rs
@crates/z00z_core/tests/genesis/test_reproducibility.rs
@crates/z00z_simulator/tests/test_genesis_integration.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce the remaining oversized asset-domain roots below the continuation band</name>
  <files>crates/z00z_core/src/assets/assets.rs, crates/z00z_core/src/assets/registry.rs, crates/z00z_core/src/assets/definition.rs, crates/z00z_core/src/assets/nonce.rs, crates/z00z_core/src/assets/leaf.rs, crates/z00z_core/src/assets/wire_pkg.rs, crates/z00z_core/src/assets/assets_config.rs</files>
  <behavior>
    - Asset-domain roots stop remaining as oversized mixed-concern files.
    - Asset identity, wire, and registry behavior remain stable.
  </behavior>
  <action>Continue the core split by reducing the remaining oversized asset-domain roots below the >400 continuation band. Preserve existing asset identity, wire, and registry semantics from earlier Phase 026 and 030 work, keep the shallow asset/genesis caller story stable, and extract only real ownership seams rather than cosmetic line slices.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_core --release --test test_assets -- --nocapture
cargo test -p z00z_core --release --test test_wire_format_snapshots -- --nocapture
cargo test -p z00z_core --release --test test_genesis -- --nocapture
cargo test -p z00z_core --release --test test_reproducibility -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_genesis_integration -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The remaining oversized asset-domain roots are reduced below the continuation band while core behavior remains stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce core CLI and genesis-output residue while preserving shallow caller paths</name>
  <files>crates/z00z_core/src/genesis/genesis_output.rs, crates/z00z_core/bin/assets/assets_generation_cli.rs, crates/z00z_core/bin/genesis/assets_extractor_cli.rs, crates/z00z_core/bin/genesis/assets_analyzer_cli.rs, crates/z00z_core/src/genesis/mod.rs</files>
  <behavior>
    - Core CLI/support roots become smaller and semantically focused.
    - Shallow genesis caller paths remain unchanged.
  </behavior>
  <action>Reduce the remaining oversized core CLI and genesis-output support files below the continuation band. Keep `z00z_core::genesis::*` as the stable shallow caller surface, preserve CLI behavior, and extract cohesive helper seams so these files stop remaining as large mixed orchestration roots.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_core --release --test test_genesis -- --nocapture
cargo test -p z00z_core --release --test test_reproducibility -- --nocapture
cargo test -p z00z_core --release --test test_assets -- --nocapture
cargo test -p z00z_core --release --test test_wire_format_snapshots -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_genesis_integration -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Core CLI and genesis-output support roots fall below the continuation band and remain aligned to the shallow genesis surface.</done>
</task>

</tasks>

<verification>
Close only after touched core roots fall below the continuation band and the shallow asset/genesis caller surface remains stable and test-backed.
</verification>

<success_criteria>
The remaining oversized `z00z_core` asset and support roots are removed without changing runtime behavior or shallow caller-visible contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-21-SUMMARY.md`
</output>