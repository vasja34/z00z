<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 18
type: execute
wave: 7
depends_on:
  - 030-09
files_modified:
  - crates/z00z_wallets/src/core/tx/state_update.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/src/core/tx/claim_tx.rs
  - crates/z00z_wallets/src/core/tx/asset_selector.rs
  - crates/z00z_wallets/src/core/tx/fee_estimator.rs
  - crates/z00z_wallets/src/core/stealth/facade_zkpack.rs
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/core/security/password.rs
  - crates/z00z_wallets/src/core/hashing.rs
  - crates/z00z_wallets/src/core/domains.rs
  - crates/z00z_wallets/src/core/claim/nullifier_store.rs
  - crates/z00z_wallets/src/core/storage/asset_storage_impl.rs
  - crates/z00z_wallets/src/core/tx/mod.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-PROTECTED
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet tx continuation removes the remaining oversized tx/security/stealth support roots.
    - The tx facade remains singular and stable while deeper ownership seams become explicit.
    - Wallet tx support files must not create parallel owners for domain-separation helpers, AAD constants, or digest-framing helpers that belong to canonical domain or hashing seams.
    - Spend, claim, digest, and support invariants preserve current reject-class and verification behavior.
  artifacts:
    - path: crates/z00z_wallets/src/core/tx/mod.rs
      provides: Stable tx-domain facade.
    - path: crates/z00z_wallets/src/core/tx/tx_verifier.rs
      provides: Smaller verifier root over focused tx verification seams.
  key_links:
    - from: crates/z00z_wallets/src/core/tx/mod.rs
      to: crates/z00z_wallets/tests/test_tx_spent_gate.rs
      via: tx-domain facade remains aligned to spend/claim verification behavior
      pattern: claim|spent|verify|digest
---

<objective>
Continue the wallet tx and security-domain split until the remaining oversized tx-support files fall below the residue band.

Purpose: finish tx-domain cleanup without reopening Phase 029 semantics or creating new caller-visible tx surfaces.
Output: smaller tx/security/stealth support files and preserved tx behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_wallets/src/core/tx/mod.rs
@crates/z00z_wallets/src/core/tx/state_update.rs
@crates/z00z_wallets/src/core/tx/tx_verifier.rs
@crates/z00z_wallets/src/core/tx/claim_tx.rs
@crates/z00z_wallets/src/core/stealth/output.rs
@crates/z00z_wallets/tests/test_claim_state_core.rs
@crates/z00z_wallets/tests/test_tx_digest_framing.rs
@crates/z00z_wallets/tests/test_tx_fee.rs
@crates/z00z_wallets/tests/test_tx_pass.rs
@crates/z00z_wallets/tests/test_tx_poison.rs
@crates/z00z_wallets/tests/test_tx_spent_gate.rs
@crates/z00z_crypto/tests/test_domain_separation.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce the remaining oversized tx-state, verifier, claim, selector, and fee roots</name>
  <files>crates/z00z_wallets/src/core/tx/state_update.rs, crates/z00z_wallets/src/core/tx/tx_verifier.rs, crates/z00z_wallets/src/core/tx/claim_tx.rs, crates/z00z_wallets/src/core/tx/asset_selector.rs, crates/z00z_wallets/src/core/tx/fee_estimator.rs, crates/z00z_wallets/src/core/tx/mod.rs</files>
  <behavior>
    - Tx-core implementation roots shrink below the continuation band.
    - Claim, digest, and spend/fee semantics remain behavior-compatible with the current tests.
  </behavior>
  <action>Continue the tx-domain split from plan 09 by extracting or consolidating the remaining oversized tx-state, verifier, claim, selector, and fee seams below the >400 continuation band. Preserve the current tx facade, keep verifier ordering and reject-class semantics stable, and avoid introducing a second public tx or claim entrypoint during this continuation wave.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_claim_state_core -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_digest_framing -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_fee -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_pass -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_poison -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_spent_gate -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The tx-core continuation files are reduced below the residue band while tx-domain behavior remains stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce stealth, password, hashing, domain, and storage-support residue tied to tx ownership</name>
  <files>crates/z00z_wallets/src/core/stealth/facade_zkpack.rs, crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/core/security/password.rs, crates/z00z_wallets/src/core/hashing.rs, crates/z00z_wallets/src/core/domains.rs, crates/z00z_wallets/src/core/claim/nullifier_store.rs, crates/z00z_wallets/src/core/storage/asset_storage_impl.rs, crates/z00z_wallets/src/core/tx/mod.rs</files>
  <behavior>
    - Stealth/security/hash/domain support files become independently reviewable.
    - Support reductions do not change tx ownership, nullifier, or zkpack behavior.
  </behavior>
  <action>Reduce the remaining oversized stealth/security/hash/domain support files that still sit on the tx critical path. Treat `domains.rs` and `hashing.rs` as protected seams, preserve current stealth output and password/nullifier behavior, keep domain-separation, AAD, and digest-framing ownership explicit, and align any updated helper seams back to the singular tx facade and its current tests.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_claim_state_core -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_spent_gate -- --nocapture
cargo test -p z00z_wallets --release --test test_view_key_contract -- --nocapture
cargo test -p z00z_crypto --release --test test_domain_separation -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>if rg -n 'hash_domain!\(|HKDF_INFO_|AAD_' crates/z00z_wallets/src/core/tx crates/z00z_wallets/src/core/stealth crates/z00z_wallets/src/core/security crates/z00z_wallets/src/core/claim -g '*.rs'; then
  echo 'Tx-adjacent support files still own domain, AAD, or KDF markers that must stay canonical'
  exit 1
fi</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Tx-adjacent stealth/security support roots are reduced below the residue band without changing current wallet tx behavior.</done>
</task>

</tasks>

<verification>
Close only after all touched tx-domain roots fall below the continuation band and the current claim/spend verification anchors stay green.
</verification>

<success_criteria>
Wallet tx, stealth, and security support files no longer remain as oversized roots, and the stable tx facade still carries the same externally visible behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-18-SUMMARY.md`
</output>