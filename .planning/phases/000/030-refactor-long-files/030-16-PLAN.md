<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 16
type: execute
wave: 7
depends_on:
  - 030-05
  - 030-06
files_modified:
  - crates/z00z_wallets/src/core/address/address_manager/address_manager_impl.rs
  - crates/z00z_wallets/src/core/address/stealth_request.rs
  - crates/z00z_wallets/src/core/address/canonical_snapshot.rs
  - crates/z00z_wallets/src/core/address/stealth_card.rs
  - crates/z00z_wallets/src/core/address/stealth_scan_support.rs
  - crates/z00z_wallets/src/core/address/stealth_scanner/types.rs
  - crates/z00z_wallets/src/core/address/stealth_trust.rs
  - crates/z00z_wallets/src/core/address/z00z_address/z00z_dual_address.rs
  - crates/z00z_wallets/src/core/address/z00z_address/z00z_single_address.rs
  - crates/z00z_wallets/src/core/address/mod.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-FACADE
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet address and stealth-ownership continuation reduces the remaining oversized address roots below the continuation band.
    - The shallow address facade remains stable while scanner/request/card/trust seams become explicit.
    - Stealth tag and leaf-AD derivation domains remain canonically owned by `core::stealth` and `core::domains`; address modules may use those helpers but must not redefine those domains.
    - Address-type and stealth support modules are easier to review without changing ownership behavior.
  artifacts:
    - path: crates/z00z_wallets/src/core/address/mod.rs
      provides: Stable address facade.
    - path: crates/z00z_wallets/src/core/address/address_manager/address_manager_impl.rs
      provides: Smaller address-manager implementation seam.
  key_links:
    - from: crates/z00z_wallets/src/core/address/mod.rs
      to: crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
      via: shallow address facade remains aligned to rate-limit and derivation behavior
      pattern: AddressManager|Bip44Path|Stealth
---

<objective>
Continue the wallet address and stealth-ownership split until the remaining oversized address-domain files fall below the residue band.

Purpose: finish the address-domain cleanup while preserving ownership detection and shallow address imports.
Output: smaller address/stealth modules and unchanged caller-visible address behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_wallets/src/core/address/mod.rs
@crates/z00z_wallets/src/core/address/address_manager/address_manager_impl.rs
@crates/z00z_wallets/src/core/address/stealth_request.rs
@crates/z00z_wallets/src/core/address/stealth_scan_support.rs
@crates/z00z_wallets/src/core/address/z00z_address/z00z_dual_address.rs
@crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
@crates/z00z_wallets/tests/test_deterministic_derivation_across_restarts.rs
@crates/z00z_wallets/tests/test_view_key_contract.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce address-manager and stealth scanner roots behind the shallow address facade</name>
  <files>crates/z00z_wallets/src/core/address/address_manager/address_manager_impl.rs, crates/z00z_wallets/src/core/address/stealth_scan_support.rs, crates/z00z_wallets/src/core/address/stealth_scanner/types.rs, crates/z00z_wallets/src/core/address/mod.rs</files>
  <behavior>
    - Address-manager and scanner support code stop accumulating mixed ownership at oversized scale.
    - The shallow address facade remains stable and caller-visible.
  </behavior>
  <action>Continue the address-domain split by reducing the remaining oversized address-manager and scanner support roots below the >400 continuation band. Preserve ownership detection, cache/rate-limit behavior, shallow address imports, and the canonical `core::stealth` plus `core::domains` ownership of stealth tag and leaf-AD derivation, and avoid reintroducing deep caller paths while extracting cohesive scanner and support seams.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_addr_rate_limit_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_deterministic_derivation_across_restarts -- --nocapture
cargo test -p z00z_wallets --release --test test_view_key_contract -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>if rg -n 'WalletTag16Hash|WalletLeafAdHash|hash_zk::<|hash_domain!\(' crates/z00z_wallets/src/core/address -g '*.rs'; then
  echo 'Address modules reintroduced stealth domain ownership that must stay canonical in core::stealth or core::domains'
  exit 1
fi</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Address-manager and stealth-scanner continuation files are smaller and semantically focused while the shallow address facade stays intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce request, card, trust, snapshot, and address-type residue</name>
  <files>crates/z00z_wallets/src/core/address/stealth_request.rs, crates/z00z_wallets/src/core/address/canonical_snapshot.rs, crates/z00z_wallets/src/core/address/stealth_card.rs, crates/z00z_wallets/src/core/address/stealth_trust.rs, crates/z00z_wallets/src/core/address/z00z_address/z00z_dual_address.rs, crates/z00z_wallets/src/core/address/z00z_address/z00z_single_address.rs, crates/z00z_wallets/src/core/address/mod.rs</files>
  <behavior>
    - Address-request, trust, and address-type files become independently reviewable.
    - Address rustdoc and caller-facing paths stay shallow and coherent.
  </behavior>
  <action>Continue the address cleanup across the remaining oversized request/card/trust/snapshot and address-type files. Keep `core::address` as the stable caller contract, preserve current address encoding/validation behavior, and reduce each touched implementation file below the continuation band by extracting real ownership seams instead of mechanically slicing lines.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_addr_rate_limit_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_deterministic_derivation_across_restarts -- --nocapture
cargo test -p z00z_wallets --release --test test_view_key_contract -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The remaining oversized address-domain files are reduced below the residue band while the shallow facade and rustdoc surface remain consistent.</done>
</task>

</tasks>

<verification>
Close only after the touched address-domain roots fall below the continuation band and the shallow address contract stays stable.
</verification>

<success_criteria>
Wallet address and stealth-ownership code is easier to review and maintain, with no remaining oversized address-domain root left above the >400 continuation band.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-16-SUMMARY.md`
</output>