<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 15
type: execute
wave: 7
depends_on:
  - 030-06
files_modified:
  - crates/z00z_wallets/src/core/key/key_manager_impl.rs
  - crates/z00z_wallets/src/core/key/seed_cipher.rs
  - crates/z00z_wallets/src/core/key/bip32.rs
  - crates/z00z_wallets/src/core/key/seed_backup_format.rs
  - crates/z00z_wallets/src/core/key/key_manager_redb.rs
  - crates/z00z_wallets/src/core/key/seed.rs
  - crates/z00z_wallets/src/core/key/bip32_path.rs
  - crates/z00z_wallets/src/core/key/stealth_keys.rs
  - crates/z00z_wallets/src/core/key/mod.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-PROTECTED
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet key and seed continuation removes the remaining oversized key-management and seed-handling roots above the residue band.
    - `crate::core::key` remains the single shallow caller-visible key facade.
    - Wallet key files must keep KDF info constants and derivation-domain ownership singular instead of redefining them inside wallet key modules.
    - Seed, BIP32/BIP44, RedB-backed key persistence, and stealth-key helpers stay explicitly owned.
  artifacts:
    - path: crates/z00z_wallets/src/core/key/mod.rs
      provides: Stable shallow key facade.
    - path: crates/z00z_wallets/src/core/key/key_manager_impl.rs
      provides: Smaller implementation root over extracted key-management seams.
  key_links:
    - from: crates/z00z_wallets/src/core/key/mod.rs
      to: crates/z00z_wallets/tests/test_key_manager.rs
      via: key facade remains aligned to current key-manager behavior
      pattern: Bip44Path|KeyManager|CipherSeedContainer
---

<objective>
Continue the wallet key and seed split until the remaining oversized key-domain files are reduced below the continuation residue band.

Purpose: finish the key-management and seed-domain cleanup without fragmenting the shallow `core::key` caller contract.
Output: smaller key/seed implementation seams and preserved derivation, persistence, and secrecy invariants.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_wallets/src/core/key/mod.rs
@crates/z00z_wallets/src/core/key/key_manager_impl.rs
@crates/z00z_wallets/src/core/key/seed_cipher.rs
@crates/z00z_wallets/src/core/key/bip32.rs
@crates/z00z_wallets/src/core/key/seed.rs
@crates/z00z_wallets/tests/test_key_manager.rs
@crates/z00z_wallets/tests/test_bip44.rs
@crates/z00z_wallets/tests/test_kdf.rs
@crates/z00z_wallets/tests/test_key_manager_storage_unlock.rs
@crates/z00z_wallets/tests/test_runtime_validation_result.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce oversized key-manager and encrypted-seed implementation roots</name>
  <files>crates/z00z_wallets/src/core/key/key_manager_impl.rs, crates/z00z_wallets/src/core/key/key_manager_redb.rs, crates/z00z_wallets/src/core/key/seed_cipher.rs, crates/z00z_wallets/src/core/key/mod.rs</files>
  <behavior>
    - Key-manager implementation files stop mixing cache, persistence, signing, unlock, and recovery responsibilities at oversized scale.
    - Encrypted-seed handling remains explicit and testable.
  </behavior>
  <action>Continue the key split by extracting or consolidating cohesive seams still trapped in `key_manager_impl.rs`, `key_manager_redb.rs`, and `seed_cipher.rs`. Preserve all key-manager invariants from Phase 029 and Phase 030, keep the shallow `core::key` facade stable, keep KDF info constants and derivation-domain ownership singular, and avoid reintroducing deep caller paths while reducing these roots below the >400 continuation band.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_key_manager -- --nocapture
cargo test -p z00z_wallets --release --test test_key_manager_storage_unlock -- --nocapture
cargo test -p z00z_wallets --release --test test_runtime_validation_result -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>if rg -n '^\s*(pub\s+)?const\s+(HKDF_INFO_|KDF_INFO_)' crates/z00z_wallets/src/core/key -g '*.rs'; then
  echo 'Wallet key modules still define KDF info constants that must stay canonical elsewhere'
  exit 1
fi</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Key-manager implementation and seed-cipher roots are smaller and semantically focused while preserving the current key contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce BIP32, seed, backup-format, and stealth-key residue behind the same key facade</name>
  <files>crates/z00z_wallets/src/core/key/bip32.rs, crates/z00z_wallets/src/core/key/bip32_path.rs, crates/z00z_wallets/src/core/key/seed.rs, crates/z00z_wallets/src/core/key/seed_backup_format.rs, crates/z00z_wallets/src/core/key/stealth_keys.rs, crates/z00z_wallets/src/core/key/mod.rs</files>
  <behavior>
    - BIP32/BIP44, seed, backup-format, and stealth-key ownership become independently reviewable.
    - The shallow key facade remains singular and well-documented.
  </behavior>
  <action>Continue the key-domain cleanup across the remaining oversized BIP32/path, seed, backup-format, and stealth-key files. Preserve the caller-visible `core::key` surface, keep BIP44 path semantics and entropy validation stable, and split by real ownership seams rather than by numeric slicing alone.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_bip44 -- --nocapture
cargo test -p z00z_wallets --release --test test_kdf -- --nocapture
cargo test -p z00z_wallets --release --test test_key_manager -- --nocapture
cargo test -p z00z_wallets --release --test test_runtime_validation_result -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The remaining oversized key-domain roots are reduced below the continuation band without fragmenting the shallow caller surface.</done>
</task>

</tasks>

<verification>
Close only after the remaining oversized key/seed files are reduced and the shallow `core::key` facade still serves as the single public entrypoint.
</verification>

<success_criteria>
Wallet key and seed logic are split into explicit seams while derivation, persistence, and secrecy invariants remain stable under the shallow `core::key` contract.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-15-SUMMARY.md`
</output>