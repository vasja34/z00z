<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 07
type: execute
wave: 4
depends_on:
  - 030-01
  - 030-05
  - 030-06
files_modified:
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/src/services/mod.rs
  - crates/z00z_wallets/src/services/app_service.rs
  - crates/z00z_wallets/src/egui_views/app_main_view.rs
  - crates/z00z_wallets/tests/test_wallet_service_errors.rs
  - crates/z00z_wallets/tests/test_app_service_create_wallet.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
  - crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-FACADE
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet service orchestration, app lifecycle service, and UI shell no longer hide multiple unrelated concerns in single files.
    - Service and app-shell facades remain stable while internals split.
  artifacts:
    - path: crates/z00z_wallets/src/services/wallet_service.rs
      provides: Stable wallet-service facade.
    - path: crates/z00z_wallets/src/services/mod.rs
      provides: Stable services re-export surface.
    - path: crates/z00z_wallets/tests/test_wallet_service_errors.rs
      provides: Service error anchor.
  key_links:
    - from: crates/z00z_wallets/src/services/mod.rs
      to: crates/z00z_wallets/tests/test_app_service_create_wallet.rs
      via: service facade remains aligned to wallet creation and app flow
      pattern: AppService|WalletService
---

<objective>
Split wallet service orchestration and app-shell surfaces behind stable service facades.

Purpose: remove one of the largest wallet orchestration files while preserving wallet-service, app-service, and app-shell behavior.
Output: focused service and app-shell modules behind the current service facade plus aligned integration anchors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_wallets/src/services/mod.rs
@crates/z00z_wallets/src/services/app_service.rs
@crates/z00z_wallets/src/egui_views/app_main_view.rs
@crates/z00z_wallets/tests/test_wallet_service_errors.rs
@crates/z00z_wallets/tests/test_app_service_create_wallet.rs
@crates/z00z_wallets/tests/test_tx_store_integration.rs
@crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, runner references, or UI config inputs, update that YAML/config in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split `wallet_service.rs` into coherent orchestration seams behind the service facade</name>
  <files>crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/src/services/wallet_service_state_store.rs, crates/z00z_wallets/src/services/wallet_service_session.rs, crates/z00z_wallets/src/services/wallet_service_limits.rs, crates/z00z_wallets/src/services/wallet_service_receive_flow.rs, crates/z00z_wallets/src/services/wallet_service_send_flow.rs, crates/z00z_wallets/src/services/wallet_service_recovery.rs, crates/z00z_wallets/src/services/wallet_service_derivers.rs, crates/z00z_wallets/src/services/wallet_service_reachability.rs, crates/z00z_wallets/src/services/wallet_service_background.rs, crates/z00z_wallets/src/services/wallet_service_events.rs, crates/z00z_wallets/src/services/mod.rs</files>
  <behavior>
    - State maps, session lifecycle, limits, send/receive flows, recovery, reachability, and background/event logic separate cleanly.
    - `WalletService` remains the stable caller-visible service surface.
  </behavior>
  <action>Split `wallet_service.rs` by true orchestration seams while preserving `WalletService` as the stable public service contract. Keep service-layer reachability, store integration, and session ownership explicit, and do not create a second competing service abstraction.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_store_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>`wallet_service.rs` becomes a coherent facade and orchestration layer instead of a mixed service monolith.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split app lifecycle and UI shell files without duplicating service ownership</name>
  <files>crates/z00z_wallets/src/services/app_service.rs, crates/z00z_wallets/src/services/app_wallet_lifecycle.rs, crates/z00z_wallets/src/services/app_chain_network.rs, crates/z00z_wallets/src/services/app_seed_password.rs, crates/z00z_wallets/src/services/app_kernel.rs, crates/z00z_wallets/src/services/app_service_impl.rs, crates/z00z_wallets/src/egui_views/app_main_view.rs, crates/z00z_wallets/src/egui_views/tab_registry.rs, crates/z00z_wallets/src/egui_views/ui_config.rs, crates/z00z_wallets/src/egui_views/ui_state_machine.rs, crates/z00z_wallets/src/egui_views/ui_theme.rs, crates/z00z_wallets/src/egui_views/main_view.rs</files>
  <behavior>
    - App lifecycle helpers and UI shell concerns separate cleanly.
    - The split does not recreate service logic inside the UI shell.
  </behavior>
  <action>Keep service ownership in the service layer and treat app-service and UI-shell files as composition surfaces only. Split lifecycle, chain/network, seed/password, kernel, tab registry, config, state machine, and theme concerns by actual responsibility while avoiding duplicated orchestration in the UI.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_app_service_create_wallet -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>App lifecycle and UI shell code are clearer, and service ownership is not duplicated outside the service layer.</done>
</task>

</tasks>

<verification>
Close only after service and app-shell anchors remain green and the service facade still owns orchestration semantics.
</verification>

<success_criteria>
Wallet services and app shell become reviewable without duplicating service logic or changing wallet-visible behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-07-SUMMARY.md`
</output>
