<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 17
type: execute
wave: 7
depends_on:
  - 030-06
  - 030-07
files_modified:
  - crates/z00z_wallets/src/core/wallet/wallet_entity.rs
  - crates/z00z_wallets/src/core/wallet/stub_responses.rs
  - crates/z00z_wallets/src/core/wallet/snapshot.rs
  - crates/z00z_wallets/src/core/wallet/errors.rs
  - crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
  - crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
  - crates/z00z_wallets/src/core/backup/wallet_backup.rs
  - crates/z00z_wallets/src/core/wallet/mod.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-FACADE
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet-core and backup continuation removes the remaining oversized wallet entity, snapshot, and backup implementation roots.
    - Wallet and backup callers still use the same shallow facades after the continuation split.
    - Backup/export and wallet-core behavior remain aligned to current wallet tests.
  artifacts:
    - path: crates/z00z_wallets/src/core/wallet/mod.rs
      provides: Stable shallow wallet facade.
    - path: crates/z00z_wallets/src/core/backup/wallet_backup.rs
      provides: Smaller backup root over extracted seams.
  key_links:
    - from: crates/z00z_wallets/src/core/wallet/mod.rs
      to: crates/z00z_wallets/tests/test_backup_restore_identity.rs
      via: wallet-core and backup surfaces remain behavior-compatible
      pattern: WalletState|Backup|Snapshot
---

<objective>
Continue the wallet-core and backup split until the remaining oversized wallet/backup files are reduced below the residue band.

Purpose: finish the wallet entity/snapshot/backup cleanup without widening the public wallet contract.
Output: smaller wallet-core and backup implementation seams plus preserved restore/export behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_wallets/src/core/wallet/mod.rs
@crates/z00z_wallets/src/core/wallet/wallet_entity.rs
@crates/z00z_wallets/src/core/wallet/snapshot.rs
@crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
@crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
@crates/z00z_wallets/src/core/backup/wallet_backup.rs
@crates/z00z_wallets/tests/test_backup_restore_identity.rs
@crates/z00z_wallets/tests/test_app_service_create_wallet.rs
@crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce wallet entity, snapshot, and stub-root residue</name>
  <files>crates/z00z_wallets/src/core/wallet/wallet_entity.rs, crates/z00z_wallets/src/core/wallet/stub_responses.rs, crates/z00z_wallets/src/core/wallet/snapshot.rs, crates/z00z_wallets/src/core/wallet/errors.rs, crates/z00z_wallets/src/core/wallet/mod.rs</files>
  <behavior>
    - Wallet-core roots stop mixing identity, snapshot, error, and stub concerns at oversized scale.
    - The wallet facade remains stable and well-bounded.
  </behavior>
  <action>Continue the wallet-core split by reducing the remaining oversized wallet entity/snapshot/stub roots below the >400 continuation band. Preserve the shallow wallet facade, keep error and snapshot contracts explicit, and avoid widening scope into new behavior while extracting cohesive wallet-core seams.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_app_service_create_wallet -- --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Wallet-core residual monoliths are smaller and semantically focused behind the same wallet facade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce backup importer/exporter roots while preserving restore behavior</name>
  <files>crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs, crates/z00z_wallets/src/core/backup/backup_importer_impl.rs, crates/z00z_wallets/src/core/backup/wallet_backup.rs, crates/z00z_wallets/src/core/wallet/mod.rs</files>
  <behavior>
    - Backup importer/exporter files become independently reviewable.
    - Restore/export behavior remains compatible with current wallet tests and backup contracts.
  </behavior>
  <action>Continue the backup split by reducing the remaining oversized backup importer/exporter roots below the continuation band while keeping wallet backup behavior stable. Preserve the current backup contracts, keep the wallet facade and backup entry surface truthful, and avoid adding new backup schema behavior during the structural cleanup.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_backup_restore_identity -- --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Backup roots are reduced below the continuation band and still preserve export/import behavior through the same shallow wallet surfaces.</done>
</task>

</tasks>

<verification>
Close only after wallet-core and backup roots fall below the continuation band and current backup/restore tests stay green.
</verification>

<success_criteria>
Wallet-core and backup logic are no longer concentrated in oversized roots, and wallet-facing restore/export behavior remains stable.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-17-SUMMARY.md`
</output>