<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 05
type: execute
wave: 2
depends_on:
  - 030-01
files_modified:
  - crates/z00z_wallets/src/core/address/address_manager.rs
  - crates/z00z_wallets/src/core/address/z00z_address.rs
  - crates/z00z_wallets/src/core/address/mod.rs
  - crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-FACADE
  - PH30-VERIFY
must_haves:
  truths:
    - Address-domain caches, rate limiting, expiry, codecs, normalization, and validation no longer live in two oversized files.
    - `core::address::mod.rs` stays the stable public address facade.
  artifacts:
    - path: crates/z00z_wallets/src/core/address/address_manager.rs
      provides: Stable address-manager facade.
    - path: crates/z00z_wallets/src/core/address/z00z_address.rs
      provides: Stable address-format facade.
  key_links:
    - from: crates/z00z_wallets/src/core/address/mod.rs
      to: crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
      via: address facade remains aligned to rate-limit and derivation behavior
      pattern: AddressManager|AddressManagerImpl|Bip44Path
---

<objective>
Split wallet address-domain surfaces behind stable address facades.

Purpose: make address management and address-format code reviewable without destabilizing caller-visible address APIs.
Output: focused address modules behind the existing address facade and aligned address integration anchors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_wallets/src/core/address/address_manager.rs
@crates/z00z_wallets/src/core/address/z00z_address.rs
@crates/z00z_wallets/src/core/address/mod.rs
@crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split `address_manager.rs` into coherent cache, expiry, and rate-limit seams</name>
  <files>crates/z00z_wallets/src/core/address/address_manager.rs, crates/z00z_wallets/src/core/address/address_manager_trait.rs, crates/z00z_wallets/src/core/address/address_manager_cache.rs, crates/z00z_wallets/src/core/address/cache_metrics.rs, crates/z00z_wallets/src/core/address/eviction_listener.rs, crates/z00z_wallets/src/core/address/rate_limiter_bucket.rs, crates/z00z_wallets/src/core/address/address_manager_expiry.rs, crates/z00z_wallets/src/core/address/address_manager_config.rs, crates/z00z_wallets/src/core/address/address_manager_impl.rs, crates/z00z_wallets/src/core/address/mod.rs</files>
  <behavior>
    - Trait surface, cache, expiry, config, and rate limiting each gain one coherent owner.
    - The address facade remains stable for callers.
  </behavior>
  <action>Per D-01 through D-03b and the shard-avoidance rules, split `address_manager.rs` only where the seam is semantically strong. Keep `mod.rs` as the stable public surface and avoid fragmenting one homogeneous cache or derivation concept into many tiny files.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_addr_rate_limit_integration -- --nocapture
cargo test -p z00z_wallets --release test_address_manager_create_card -- --nocapture
cargo test -p z00z_wallets --release test_address_manager_scan_integration -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>`address_manager.rs` no longer mixes trait, cache, expiry, and rate-limit ownership in one file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split `z00z_address.rs` while keeping one shared address-format facade</name>
  <files>crates/z00z_wallets/src/core/address/z00z_address.rs, crates/z00z_wallets/src/core/address/z00z_address_codec.rs, crates/z00z_wallets/src/core/address/z00z_address_features.rs, crates/z00z_wallets/src/core/address/z00z_address_validation.rs, crates/z00z_wallets/src/core/address/z00z_address_normalize.rs, crates/z00z_wallets/src/core/address/z00z_address_parts.rs, crates/z00z_wallets/src/core/address/z00z_single_address.rs, crates/z00z_wallets/src/core/address/z00z_dual_address.rs, crates/z00z_wallets/src/core/address/mod.rs</files>
  <behavior>
    - Codec, normalization, validation, and structural address parts separate cleanly.
    - One stable address-format facade remains visible to callers.
  </behavior>
  <action>Split address codec, normalization, validation, and single/dual address representation by true responsibility. Keep one shared public facade and do not create parallel public address types just because internals move.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_addr_rate_limit_integration -- --nocapture
cargo test -p z00z_wallets --release dual_new_accepts_many_pairs -- --nocapture
cargo test -p z00z_wallets --release uri_rejects_non_ascii -- --nocapture
cargo test -p z00z_wallets --release bech32m_detects_data_corruption -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>`z00z_address.rs` is no longer a mixed address monolith, and the address facade remains stable and cohesive.</done>
</task>

</tasks>

<verification>
Close only after address integration and lib anchors remain green through the split and the public address facade stays singular.
</verification>

<success_criteria>
Wallet address management and address-format code become structurally homogeneous without caller-visible path churn.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-05-SUMMARY.md`
</output>
