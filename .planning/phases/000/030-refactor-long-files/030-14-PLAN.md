<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 14
type: execute
wave: 7
depends_on:
  - 030-07
files_modified:
  - crates/z00z_wallets/src/services/wallet_service_store.rs
  - crates/z00z_wallets/src/services/wallet_service_session.rs
  - crates/z00z_wallets/src/services/wallet_service_actions.rs
  - crates/z00z_wallets/src/services/wallet_service_types.rs
  - crates/z00z_wallets/src/services/app_service.rs
  - crates/z00z_wallets/src/services/wallet_paths.rs
  - crates/z00z_wallets/src/egui_views/main_view.rs
  - crates/z00z_wallets/examples/example_0_wallet_creation.rs
  - crates/z00z_wallets/examples/example_2_rpc_complete.rs
  - crates/z00z_wallets/src/services/mod.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-FACADE
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet service continuation removes the remaining oversized orchestration seams still above the residue band.
    - Service and app-shell callers still enter through shallow service/app facades.
    - Example and app-shell flows stay aligned with the current service contract.
  artifacts:
    - path: crates/z00z_wallets/src/services/mod.rs
      provides: Stable service facade.
    - path: crates/z00z_wallets/src/egui_views/main_view.rs
      provides: Smaller app-shell entry root.
  key_links:
    - from: crates/z00z_wallets/src/services/mod.rs
      to: crates/z00z_wallets/examples/example_2_rpc_complete.rs
      via: example callers remain aligned to the shallow services surface
      pattern: WalletService|AppService|RateLimitPrecheck
---

<objective>
Continue the wallet service and app-shell split across remaining oversized orchestration, type, and example files.

Purpose: finish the service/app-shell cleanup without turning the shallow services facade back into a mixed-concern monolith.
Output: smaller service/app-shell implementation seams and examples that stay aligned to the shallow wallet surface.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_wallets/src/services/mod.rs
@crates/z00z_wallets/src/services/wallet_service_store.rs
@crates/z00z_wallets/src/services/wallet_service_session.rs
@crates/z00z_wallets/src/services/wallet_service_actions.rs
@crates/z00z_wallets/src/services/wallet_service_types.rs
@crates/z00z_wallets/src/services/app_service.rs
@crates/z00z_wallets/src/egui_views/main_view.rs
@crates/z00z_wallets/examples/example_0_wallet_creation.rs
@crates/z00z_wallets/examples/example_2_rpc_complete.rs
@crates/z00z_wallets/tests/test_wallet_service_errors.rs
@crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
@crates/z00z_wallets/tests/test_backup_restore_identity.rs
@crates/z00z_wallets/tests/test_app_service_create_wallet.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish splitting service-store, session, action, and service-type seams</name>
  <files>crates/z00z_wallets/src/services/wallet_service_store.rs, crates/z00z_wallets/src/services/wallet_service_session.rs, crates/z00z_wallets/src/services/wallet_service_actions.rs, crates/z00z_wallets/src/services/wallet_service_types.rs, crates/z00z_wallets/src/services/mod.rs</files>
  <behavior>
    - Service orchestration files stop mixing persistence, session, action, and type ownership at oversized scale.
    - The shallow services facade remains the caller-facing contract.
  </behavior>
  <action>Continue plan 07 by reducing the remaining oversized wallet-service implementation files below the continuation residue band. Keep `services::WalletService`, `AppService`, and the documented service helper types as the stable surface, and isolate store/session/action/type responsibilities into explicit seams rather than redistributing mixed logic across similarly large follow-on files.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo test -p z00z_wallets --release --test test_addr_rate_limit_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_backup_restore_identity -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The remaining oversized wallet-service implementation files are split into coherent orchestration seams behind the shallow service facade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce app-shell and example residue without widening feature scope</name>
  <files>crates/z00z_wallets/src/services/app_service.rs, crates/z00z_wallets/src/services/wallet_paths.rs, crates/z00z_wallets/src/egui_views/main_view.rs, crates/z00z_wallets/examples/example_0_wallet_creation.rs, crates/z00z_wallets/examples/example_2_rpc_complete.rs</files>
  <behavior>
    - App-shell and example roots become smaller and easier to review.
    - No new wallet UI or RPC behavior is introduced during the structural cleanup.
  </behavior>
  <action>Reduce the remaining oversized app-shell and example files by extracting cohesive helper seams while preserving the current placeholder/UI scope from plan 07. Keep examples aligned with shallow services and db facades, and avoid introducing new product behavior while shrinking the current roots below the >400 continuation band.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_app_service_create_wallet -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo check -p z00z_wallets --release --all-targets
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The app-shell and wallet examples are structurally smaller, and they still describe or call the same shallow services surface.</done>
</task>

</tasks>

<verification>
Close only after service/app-shell roots stop sitting above the residue band and all examples still resolve through the shallow wallet facades.
</verification>

<success_criteria>
Remaining service, app-shell, and example monoliths are reduced into coherent seams while public wallet orchestration behavior and documented surfaces stay stable.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-14-SUMMARY.md`
</output>