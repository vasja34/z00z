<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 24
type: execute
wave: 8
depends_on:
  - 030-13
  - 030-14
  - 030-15
  - 030-16
  - 030-17
  - 030-18
  - 030-19
  - 030-20
  - 030-21
  - 030-22
  - 030-23
files_modified:
  - .planning/phases/030-refactor-long-files/030-length_stat.md
  - .planning/phases/030-refactor-long-files/030-todo.md
  - crates/z00z_wallets/src/lib.rs
  - crates/z00z_wallets/src/db/mod.rs
  - crates/z00z_wallets/src/services/mod.rs
  - crates/z00z_wallets/src/core/address/mod.rs
  - crates/z00z_wallets/src/core/key/mod.rs
  - crates/z00z_wallets/src/core/tx/mod.rs
  - crates/z00z_wallets/README.md
  - crates/z00z_core/src/genesis/mod.rs
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_utils/src/io/mod.rs
autonomous: true
requirements:
  - PH30-NORMALIZE
  - PH30-SYNC
  - PH30-VERIFY
must_haves:
  truths:
    - The second-pass continuation closes the live non-test Rust inventory above 400 lines to zero.
    - Public facades, docs, and planning refs all describe the same final surfaces after the continuation waves.
    - The continuation residue audit is based on a fresh live inventory, not on the old static snapshot alone.
  artifacts:
    - path: .planning/phases/030-refactor-long-files/030-length_stat.md
      provides: Refreshed live oversize inventory with zero >400 residue target.
    - path: crates/z00z_wallets/src/lib.rs
      provides: Updated shallow wallet facade references after continuation waves.
  key_links:
    - from: .planning/phases/030-refactor-long-files/030-length_stat.md
      to: .planning/phases/030-refactor-long-files/030-todo.md
      via: live inventory and backlog status remain synchronized
      pattern: TOTAL_GT400|Phase 030|residue
---

<objective>
Run the cross-crate continuation normalization and live residue burn-down so the >400 inventory reaches zero and every facade/doc/planning reference becomes truthful again.

Purpose: convert the subsystem continuation waves into one coherent, verified end state for Phase 030.
Output: refreshed residue inventory, synchronized facades/docs/backlog, and zero non-test Rust files above 400 lines.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@crates/z00z_wallets/src/lib.rs
@crates/z00z_wallets/src/db/mod.rs
@crates/z00z_wallets/src/services/mod.rs
@crates/z00z_wallets/src/core/address/mod.rs
@crates/z00z_wallets/src/core/key/mod.rs
@crates/z00z_wallets/src/core/tx/mod.rs
@crates/z00z_core/src/genesis/mod.rs
@crates/z00z_crypto/src/lib.rs
@crates/z00z_utils/src/io/mod.rs
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recompute the live >400 inventory and burn remaining residue down to zero</name>
  <files>.planning/phases/030-refactor-long-files/030-length_stat.md, .planning/phases/030-refactor-long-files/030-todo.md</files>
  <behavior>
    - The live inventory becomes the source of truth for continuation completion.
    - Phase 030 backlog status matches the real remaining oversized roots.
  </behavior>
  <action>Recompute the live non-test Rust inventory over 400 lines, update `030-length_stat.md`, and keep working until the continuation residue reaches `TOTAL_GT400=0`. Reflect the real post-continuation status in `030-todo.md` so the backlog no longer describes already-completed structural debt as pending, and make any last seam extractions needed to reach zero before this plan closes.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
python3 - <<'PY'
from pathlib import Path
root = Path('crates')
items = []
for p in root.rglob('*.rs'):
    s = p.as_posix()
    if '/tari/' in s or '/tests/' in s or '/test/' in s:
        continue
    n = p.name
    if n == 'tests.rs' or n.startswith('test_') or n.endswith('_test.rs') or n.endswith('_tests.rs'):
        continue
    lines = sum(1 for _ in p.open('r', encoding='utf-8'))
    if lines > 400:
        items.append((lines, s))
print(f'TOTAL_GT400={len(items)}')
if items:
    for lines, s in sorted(items, reverse=True):
        print(f'{lines}\t{s}')
raise SystemExit(0 if not items else 1)
PY
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The live non-test Rust inventory above 400 lines is zero, and the phase-local backlog reflects that truthful end state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize all shallow facades and docs with the final continuation end state</name>
  <files>crates/z00z_wallets/src/lib.rs, crates/z00z_wallets/src/db/mod.rs, crates/z00z_wallets/src/services/mod.rs, crates/z00z_wallets/src/core/address/mod.rs, crates/z00z_wallets/src/core/key/mod.rs, crates/z00z_wallets/src/core/tx/mod.rs, crates/z00z_wallets/README.md, crates/z00z_core/src/genesis/mod.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_utils/src/io/mod.rs</files>
  <behavior>
    - Shallow facades and docs all describe the same final continuation state.
    - No stale caller-path or facade references survive the second continuation wave.
  </behavior>
  <action>Perform one final inventory-backed synchronization pass over the shallow wallet/core/crypto/utils facades and their adjacent documentation. Remove stale path references left by the continuation splits, keep the public caller surface coherent, and align the refreshed phase docs with the real final module layout.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
if rg -n "wallet_service::|redb_wallet_store::|address_manager::|bip32::|key_manager::|tx_verifier::|stage_4_utils::tx_lane_impl|stage_6_utils::bundle_lane_impl|io::fs::" \
  crates/z00z_wallets/src/lib.rs \
  crates/z00z_wallets/src/db/mod.rs \
  crates/z00z_wallets/src/services/mod.rs \
  crates/z00z_wallets/src/core/address/mod.rs \
  crates/z00z_wallets/src/core/key/mod.rs \
  crates/z00z_wallets/src/core/tx/mod.rs \
  crates/z00z_wallets/README.md \
  crates/z00z_core/src/genesis/mod.rs \
  crates/z00z_crypto/src/lib.rs \
  crates/z00z_utils/src/io/mod.rs \
  .planning/phases/030-refactor-long-files/030-todo.md \
  .planning/phases/030-refactor-long-files/030-length_stat.md; then
  echo 'Stale caller-path or facade references remain after continuation normalization'
  exit 1
fi
cargo test -p z00z_core --release --test test_reproducibility -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_source_shape -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Shallow facades, docs, and phase refs all describe the same final continuation state with no stale caller-path residue.</done>
</task>

</tasks>

<verification>
Close only after the live >400 inventory reaches zero and the final shallow facades/docs are synchronized to that end state.
</verification>

<success_criteria>
Phase 030 continuation finishes with zero non-test Rust files above 400 lines, a refreshed live residue report, and truthful shallow facades/docs/planning refs.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-24-SUMMARY.md`
</output>