<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 10
type: execute
wave: 6
depends_on:
  - 030-01
  - 030-05
  - 030-06
  - 030-07
  - 030-08
  - 030-09
files_modified:
  - crates/z00z_wallets/src/lib.rs
  - crates/z00z_wallets/src/db/mod.rs
  - crates/z00z_wallets/src/services/mod.rs
  - crates/z00z_wallets/src/core/address/mod.rs
  - crates/z00z_wallets/src/core/key/mod.rs
  - crates/z00z_wallets/src/core/tx/mod.rs
  - crates/z00z_wallets/README.md
  - .planning/phases/030-refactor-long-files/030-todo.md
autonomous: true
requirements:
  - PH30-NORMALIZE
  - PH30-SYNC
  - PH30-VERIFY
must_haves:
  truths:
    - Wallet caller-visible paths normalize only after the structural split waves are complete.
    - Wallet docs, rustdoc, and planning refs move in lockstep with any path cleanup.
  artifacts:
    - path: crates/z00z_wallets/src/lib.rs
      provides: Final shallow wallet facade.
    - path: crates/z00z_wallets/README.md
      provides: Wallet documentation aligned to the final path surface.
  key_links:
    - from: crates/z00z_wallets/src/lib.rs
      to: .planning/phases/030-refactor-long-files/030-todo.md
      via: planning refs and public facade stay synchronized
      pattern: redb_wallet_store|wallet_service|address|key|tx
---

<objective>
Normalize wallet caller-visible paths and synchronize docs, YAML, and planning refs.

Purpose: finish the wallet crate cleanup only after internal seams are stable, using one grep-backed normalization wave instead of piecemeal path churn.
Output: shallow wallet facades, synchronized docs/planning refs, and recorded caller-inventory evidence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_wallets/src/lib.rs
@crates/z00z_wallets/src/db/mod.rs
@crates/z00z_wallets/src/services/mod.rs
@crates/z00z_wallets/src/core/address/mod.rs
@crates/z00z_wallets/src/core/key/mod.rs
@crates/z00z_wallets/src/core/tx/mod.rs
@crates/z00z_wallets/README.md
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the normalization affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave and record it with the caller inventory.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the grep-backed wallet normalization wave after structural splits stabilize</name>
  <files>crates/z00z_wallets/src/lib.rs, crates/z00z_wallets/src/db/mod.rs, crates/z00z_wallets/src/services/mod.rs, crates/z00z_wallets/src/core/address/mod.rs, crates/z00z_wallets/src/core/key/mod.rs, crates/z00z_wallets/src/core/tx/mod.rs</files>
  <behavior>
    - Wallet callers move to shallow facade paths only in this dedicated normalization wave.
    - No structural split and public normalization are mixed together here.
  </behavior>
  <action>Use a full grep-backed inventory to normalize wallet callers onto the final shallow facades for db, services, address, key, and tx. Record the caller inventory source, the exact caller updates made, and the audit used to prove that legacy deep wallet paths no longer remain. Do not widen the wave beyond wallet-owned surfaces.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_key_manager -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo test -p z00z_wallets --release --test test_e2e_public_path -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_spent_gate -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
rg -n "crate::db::redb_wallet_store::|crate::services::wallet_service::|crate::core::address::address_manager::|crate::core::key::(seed|bip32|key_manager)::|crate::core::tx::(state_update|claim_tx|tx_verifier|spending)::" crates/z00z_wallets -g '*.rs'
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Wallet callers use the final shallow facades and the normalization wave has one explicit caller-inventory record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize wallet docs, rustdoc, and planning refs with the normalized surface</name>
  <files>crates/z00z_wallets/README.md, .planning/phases/030-refactor-long-files/030-todo.md, crates/z00z_wallets/src/lib.rs</files>
  <behavior>
    - Wallet docs and planning refs stop pointing at stale module paths.
    - Rustdoc examples match the final wallet facade surface.
  </behavior>
  <action>Update wallet README, rustdoc-visible exports, and phase-local planning refs that mention module or facade paths. Close the wave only when docs, code, and planning references agree on the normalized surface.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_app_service_create_wallet -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo test -p z00z_wallets --release --test test_e2e_public_path -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
rg -n "redb_wallet_store::|wallet_service::|address_manager::|bip32::|key_manager::|tx_verifier::" crates/z00z_wallets crates/z00z_wallets/README.md .planning/phases/030-refactor-long-files -g '*.rs' -g '*.md'
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Wallet code, docs, and planning refs all describe the same final shallow module surface.</done>
</task>

</tasks>

<verification>
Close only after the wallet normalization wave records caller inventory, clears legacy deep paths, and synchronizes docs and planning references.
</verification>

<success_criteria>
Wallet structural cleanup finishes on one coherent shallow public surface with no stale deep-import or stale-doc residue.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-10-SUMMARY.md`
</output>
