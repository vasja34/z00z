<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 25
type: execute
wave: 9
depends_on:
  - 030-24
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/030-refactor-long-files/030-length_stat.md
autonomous: true
requirements:
  - PH30-SYNC
  - PH30-VERIFY
must_haves:
  truths:
    - Phase 030 closeout artifacts truthfully reflect the continuation extension and its zero-residue outcome.
    - Roadmap, state, requirements, and length-stat artifacts agree on the same final phase status.
    - Phase 030 only recloses after all continuation summaries and final workspace verification are present.
  artifacts:
    - path: .planning/ROADMAP.md
      provides: Truthful phase status and full plan inventory.
    - path: .planning/STATE.md
      provides: Truthful current position after re-closing Phase 030.
  key_links:
    - from: .planning/ROADMAP.md
      to: .planning/STATE.md
      via: final phase closure is synchronized across planning artifacts
      pattern: 030-13|030-25|plans executed
---

<objective>
Reclose the extended Phase 030 with truthful planning artifacts and one final verification-backed phase summary state.

Purpose: ensure the continuation wave ends as cleanly and truthfully as the original 12-plan closeout intended, but now with the full >400 residue elimination included.
Output: synchronized roadmap/state/requirements artifacts and a reclosed Phase 030.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-length_stat.md
@.planning/phases/030-refactor-long-files/030-todo.md
</context>

If execution needs to create a git commit for this plan, it must use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synchronize roadmap, state, and requirements with the continuation closeout</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/phases/030-refactor-long-files/030-length_stat.md</files>
  <behavior>
    - Planning artifacts truthfully reflect 25 plans, zero >400 residue, and summary-backed completion.
    - No stale “12/12 complete” or reopened-state drift remains.
  </behavior>
  <action>After plans 13 through 24 finish and their summaries exist, update roadmap, state, requirements, and the refreshed length-stat artifact so they truthfully reflect the extended Phase 030 closeout. Remove stale references to the old 12-plan completion state, record the final continuation outcome, and ensure every planning artifact agrees on the same end state.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
test -f .planning/phases/030-refactor-long-files/030-13-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-14-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-15-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-16-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-17-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-18-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-19-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-20-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-21-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-22-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-23-SUMMARY.md
test -f .planning/phases/030-refactor-long-files/030-24-SUMMARY.md
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Roadmap, state, requirements, and length-stat artifacts all describe the same truthful continuation closeout state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the final zero-residue proof and phase-level closeout audit</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/030-refactor-long-files/030-length_stat.md</files>
  <behavior>
    - Phase 030 only recloses after one final live inventory and workspace verification pass.
    - Final planning artifacts remain consistent after the audit.
  </behavior>
  <action>Run one final phase-level audit that proves the live non-test Rust inventory above 400 lines is zero, the workspace gate is green, and the planning artifacts stayed synchronized after the closeout edits. If the audit finds residue or artifact drift, fix it before considering the phase reclosed.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
python3 - <<'PY'
from pathlib import Path
root = Path('crates')
items = []
for p in root.rglob('*.rs'):
    s = p.as_posix()
    if '/tari/' in s or '/tests/' in s or '/test/' in s:
        continue
    n = p.name
    if n == 'tests.rs' or n.startswith('test_') or n.endswith('_test.rs') or n.endswith('_tests.rs'):
        continue
    lines = sum(1 for _ in p.open('r', encoding='utf-8'))
    if lines > 400:
        items.append((lines, s))
print(f'TOTAL_GT400={len(items)}')
if items:
    for lines, s in sorted(items, reverse=True):
        print(f'{lines}\t{s}')
raise SystemExit(0 if not items else 1)
PY
cargo test --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>Phase 030 continuation recloses only after the live zero-residue proof and the final workspace verification pass both succeed.</done>
</task>

</tasks>

<verification>
Close only after all continuation summaries exist, the live >400 inventory is zero, and the final workspace gate is green.
</verification>

<success_criteria>
Extended Phase 030 is summary-backed, truthful across planning artifacts, and closed on a verified zero-residue inventory for non-test Rust files above 400 lines.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-25-SUMMARY.md`
</output>