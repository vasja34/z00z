<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_utils/src/io/fs.rs
  - crates/z00z_utils/src/io/mod.rs
  - crates/z00z_simulator/tests/test_stage4_split.rs
  - crates/z00z_simulator/tests/test_genesis_integration.rs
autonomous: true
requirements:
  - PH30-SEAMS
  - PH30-VERIFY
must_haves:
  truths:
    - Simulator helper lanes and `z00z_utils::io::fs` are no longer oversized mixed helper files.
    - Public stage entrypoints and `z00z_utils::io` entrypoints stay stable while helpers move.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs
      provides: Stable tx-lane helper facade.
    - path: crates/z00z_utils/src/io/mod.rs
      provides: Stable `z00z_utils::io` facade.
  key_links:
    - from: crates/z00z_utils/src/io/mod.rs
      to: crates/z00z_utils/src/io/fs.rs
      via: io facade delegates to focused filesystem helpers
      pattern: atomic_write|read_file|json|yaml|bincode
---

<objective>
Split simulator lane helpers and `z00z_utils::io::fs` without changing public stage or I/O entrypoints.

Purpose: reduce maintenance cost in helper-heavy support code while keeping the established simulator stage surface and utility facade intact.
Output: focused helper modules behind stable simulator and io facades.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs
@crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
@crates/z00z_utils/src/io/fs.rs
@crates/z00z_utils/src/io/mod.rs
@crates/z00z_simulator/tests/test_stage4_split.rs
@crates/z00z_simulator/tests/test_genesis_integration.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split simulator tx and bundle lane helpers into coherent utility modules</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_preparation_core.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/input_selection_scan.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/output_construction.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/wallet_state_capture.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/stage4_reporting.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/prep_snapshot_loader.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/fragment_construction.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/exec_input_builder.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bridge_output_router.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/demo_checkpoint_agg.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/stage6_logging.rs</files>
  <behavior>
    - Stage helper logic splits by preparation, validation, reporting, fragment, and routing responsibilities.
    - Public stage semantics stay unchanged.
  </behavior>
  <action>Keep the current simulator stage roots and existing helper directories as the stable outer shape. Split tx-lane and bundle-lane helper internals only where the seam is semantically real, and avoid creating tiny fragments that still need to be understood together as one algorithm.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_split -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_genesis_integration -- --nocapture
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump -- --nocapture</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The simulator helper lanes are split into coherent modules while stage semantics and outputs remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split `z00z_utils::io::fs` behind the stable io facade</name>
  <files>crates/z00z_utils/src/io/fs.rs, crates/z00z_utils/src/io/atomic_write.rs, crates/z00z_utils/src/io/file_read.rs, crates/z00z_utils/src/io/json_io.rs, crates/z00z_utils/src/io/yaml_io.rs, crates/z00z_utils/src/io/bincode_io.rs, crates/z00z_utils/src/io/mod.rs</files>
  <behavior>
    - Atomic writes, reads, and codec-specific helpers no longer share one oversized helper file.
    - `z00z_utils::io` remains the single public entrypoint.
  </behavior>
  <action>Split `fs.rs` by actual I/O responsibility while preserving `z00z_utils::io::mod.rs` as the only public facade. Do not leak raw codec or filesystem implementation details into new external paths.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release --all-targets -- --nocapture
cargo test -p z00z_utils --release --features test-fast --all-targets -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>`z00z_utils::io` stays public and stable while filesystem helper internals become reviewable.</done>
</task>

</tasks>

<verification>
Keep simulator smoke anchors and the `z00z_utils::io` facade green while helper internals move.
</verification>

<success_criteria>
Support-heavy simulator and io files are smaller and clearer without changing public simulator stage or utility entrypoints.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-04-SUMMARY.md`
</output>