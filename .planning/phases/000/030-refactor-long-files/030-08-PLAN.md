<!-- markdownlint-disable MD007 MD032 MD033 MD041 MD047 -->
---
phase: 030-refactor-long-files
plan: 08
type: execute
wave: 2
depends_on:
  - 030-02
files_modified:
  - crates/z00z_core/src/genesis/genesis.rs
  - crates/z00z_core/src/genesis/validator.rs
  - crates/z00z_core/src/genesis/mod.rs
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/src/lib.rs
  - crates/z00z_wallets/tests/test_bip44.rs
  - crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs
  - crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
  - crates/z00z_wallets/src/core/key/key_manager.rs
  - crates/z00z_wallets/src/core/key/seed.rs
  - crates/z00z_wallets/src/core/key/bip32.rs
  - crates/z00z_wallets/tests/test_key_manager.rs
  - crates/z00z_wallets/src/core/wallet/wallet.rs
  - crates/z00z_wallets/src/core/address/address_manager.rs
  - crates/z00z_core/tests/genesis/test_genesis.rs
  - crates/z00z_core/tests/genesis/test_reproducibility.rs
autonomous: true
requirements:
  - PH30-PROTECTED
  - PH30-NORMALIZE
  - PH30-VERIFY
must_haves:
  truths:
    - Genesis generation and validation code no longer live in oversized mixed files.
    - Current `ChainType` deep-import callers are normalized to the shallow genesis alias before deep-path cleanup.
    - The genesis facade remains stable while the split is in flight.
  artifacts:
    - path: crates/z00z_core/src/genesis/mod.rs
      provides: Stable genesis facade and shallow alias surface.
    - path: crates/z00z_core/tests/genesis/test_genesis.rs
      provides: Genesis integration anchor.
  key_links:
    - from: crates/z00z_core/src/genesis/mod.rs
      to: crates/z00z_wallets/src/services/wallet_service.rs
      via: shallow `ChainType` alias replaces deep imports before cleanup
      pattern: ChainType
---

<objective>
Split genesis surfaces and move callers to shallow `ChainType` aliases before deep-path cleanup.

Purpose: make genesis generation and validation easier to maintain without breaking deterministic genesis behavior or cross-crate caller paths.
Output: a split genesis domain behind the stable `genesis::mod.rs` facade plus a completed alias-first prerequisite wave.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/030-refactor-long-files/030-CONTEXT.md
@.planning/phases/030-refactor-long-files/030-todo.md
@crates/z00z_core/src/genesis/genesis.rs
@crates/z00z_core/src/genesis/validator.rs
@crates/z00z_core/src/genesis/mod.rs
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_wallets/src/lib.rs
@crates/z00z_wallets/tests/test_bip44.rs
@crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs
@crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
@crates/z00z_wallets/src/core/key/key_manager.rs
@crates/z00z_wallets/src/core/key/seed.rs
@crates/z00z_wallets/src/core/key/bip32.rs
@crates/z00z_wallets/tests/test_key_manager.rs
@crates/z00z_wallets/src/core/wallet/wallet.rs
@crates/z00z_wallets/src/core/address/address_manager.rs
@crates/z00z_core/tests/genesis/test_genesis.rs
@crates/z00z_core/tests/genesis/test_reproducibility.rs
</context>

Any helper file name first introduced in `<files>` below is a proposed seam target unless that file already exists in the current codebase. If an existing module already satisfies the seam, extend that existing module instead of creating a parallel abstraction. If the split affects tool-consumed YAML, stage wiring, or runner references, update that YAML in the same wave.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the alias-only prerequisite wave for `ChainType` and rustdoc paths</name>
  <files>crates/z00z_core/src/genesis/genesis.rs, crates/z00z_core/src/genesis/mod.rs, crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/src/lib.rs, crates/z00z_wallets/tests/test_bip44.rs, crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs, crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs, crates/z00z_wallets/src/core/key/key_manager.rs, crates/z00z_wallets/src/core/key/seed.rs, crates/z00z_wallets/src/core/key/bip32.rs, crates/z00z_wallets/tests/test_key_manager.rs, crates/z00z_wallets/src/core/wallet/wallet.rs, crates/z00z_wallets/src/core/address/address_manager.rs</files>
  <behavior>
    - All current deep imports of `z00z_core::genesis::genesis::ChainType` move to `z00z_core::genesis::ChainType` before deep-path cleanup.
    - Rustdoc examples and tests move with the alias rewrite in the same wave.
  </behavior>
  <action>Use the current grep-backed caller inventory as the execution boundary for the alias rewrite prerequisite wave. Do not remove deep paths first. First make `genesis::mod.rs` the explicit shallow alias surface, then update current code, tests, and rustdoc callers to that shallow path, including any rustdoc or example imports still anchored in `genesis.rs` itself.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_core --release --test test_genesis -- --nocapture
cargo test -p z00z_core --release --test test_reproducibility -- --nocapture
cargo test -p z00z_wallets --release --test test_bip44 -- --nocapture
cargo test -p z00z_wallets --release --test test_key_manager -- --nocapture
cargo test -p z00z_wallets --release --test test_addr_rate_limit_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_rpc_key_derive_e2e -- --nocapture
cargo test -p z00z_core --release --features test-fast -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
rg -n "z00z_core::genesis::genesis::ChainType|genesis::genesis::ChainType" crates -g '*.rs'
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>All known `ChainType` callers use the shallow genesis alias before any deep-path cleanup begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split `genesis.rs` and `validator.rs` behind the stable genesis facade</name>
  <files>crates/z00z_core/src/genesis/genesis.rs, crates/z00z_core/src/genesis/chain_type.rs, crates/z00z_core/src/genesis/genesis_accumulator.rs, crates/z00z_core/src/genesis/genesis_seed.rs, crates/z00z_core/src/genesis/genesis_derivation.rs, crates/z00z_core/src/genesis/validator.rs, crates/z00z_core/src/genesis/genesis_error.rs, crates/z00z_core/src/genesis/genesis_verification.rs, crates/z00z_core/src/genesis/genesis_config_validate.rs, crates/z00z_core/src/genesis/mod.rs, crates/z00z_core/tests/genesis/test_genesis.rs, crates/z00z_core/tests/genesis/test_reproducibility.rs</files>
  <behavior>
    - Genesis accumulation, seed policy, derivation, verification, and config validation split by responsibility.
    - The stable genesis facade and shallow alias surface remain intact.
  </behavior>
  <action>Split the genesis domain by real deterministic-generation and validation seams only. Keep `mod.rs` as the stable entrypoint, preserve deterministic behavior, and avoid reintroducing deep caller paths after the alias rewrite is complete.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_core --release --test test_genesis -- --nocapture
cargo test -p z00z_core --release --test test_reproducibility -- --nocapture
cargo test -p z00z_core --release --features test-fast -- --nocapture
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive runs show no significant issues and fix every issue or warning first.</automated>
  </verify>
  <done>The genesis domain is split into coherent modules, and the shallow alias path remains the stable external contract.</done>
</task>

</tasks>

<verification>
Close only after the alias-first prerequisite wave and the split wave are both green and no legacy deep `ChainType` path remains in code or rustdoc.
</verification>

<success_criteria>
Genesis files are structurally clearer, deterministic behavior remains stable, and callers no longer depend on the legacy deep `ChainType` path.
</success_criteria>

<output>
After completion, create `.planning/phases/030-refactor-long-files/030-08-SUMMARY.md`
</output>
