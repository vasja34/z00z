---
phase: 055-HJMT-boundary
plan: 055-04
type: execute
wave: 4
depends_on:
  - 055-03
files_modified:
  - crates/z00z_storage/benches/settlement_hjmt.rs
  - crates/z00z_storage/benches/settlement_proofs.rs
  - crates/z00z_storage/benches/settlement_benches.md
  - crates/z00z_storage/scripts/run_storage_settlement_bench.py
  - crates/z00z_storage/tests/test_bench_lanes.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_utils/report.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_utils/hjmt_examples.rs
  - crates/z00z_simulator/src/scenario_1/runner_verify.rs
  - crates/z00z_simulator/tests/test_scenario_settlement.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_storage/src/settlement/README.md
requirements:
  - PH55-04
autonomous: true
must_haves:
  truths:
    - "Logical benchmark lanes `hjmt_batch_proof_bytes` and `hjmt_batch_verify` live inside the existing consolidated bench homes rather than in new standalone benches."
    - "Stage 13 remains the only simulator evidence authority and is extended in place rather than forked into a second batch-proof scenario."
    - "Scenario evidence must compare one `ProofBlob`, current `Vec<ProofBlob>`, and `BatchProofBlobV1` while keeping the existing E1-E8 example identities."
    - "Measured numbers remain evidence only and cannot become protocol constants or proof-visible authority."
  artifacts:
    - crates/z00z_storage/benches/settlement_proofs.rs
    - crates/z00z_storage/benches/settlement_hjmt.rs
    - crates/z00z_storage/tests/test_bench_lanes.rs
    - crates/z00z_simulator/src/scenario_1/stage_13_utils/report.rs
    - crates/z00z_simulator/src/scenario_1/runner_verify.rs
  key_links:
    - "Bench evidence and Stage 13 evidence must both compare the new shared batch path against the current independent baseline."
    - "The docs and runner checks close the loop so the repository cannot claim a live batch proof without durable evidence."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 055-04 Plan: Benchmarks, Scenario Evidence, And Closeout Guardrails

<objective>
Add the required benchmark evidence and extend `scenario_1` Stage 13 so Phase
055 closes with durable comparison and tamper evidence instead of with unit
tests alone.

Purpose: prove that `BatchProofBlobV1` is a live production proof surface with
measured repository evidence and runner-verified artifact output.
Output: batch benchmark lanes in the existing bench harness, Stage 13 batch
comparison and tamper fields, stronger runner verification, and the final docs
or guardrails that keep those evidence surfaces canonical.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/055-HJMT-boundary/055-TODO.md
@.planning/phases/055-HJMT-boundary/055-CONTEXT.md
@.planning/phases/055-HJMT-boundary/055-SOURCE-AUDIT.md
@.planning/phases/055-HJMT-boundary/055-TEST-SPEC.md
@docs/tech-papers/Z00Z-HJMT-Upgrade.md
@docs/tech-papers/Z00Z-HJMT-Fixture-Checklist.md
@crates/z00z_storage/benches/settlement_hjmt.rs
@crates/z00z_storage/benches/settlement_proofs.rs
@crates/z00z_storage/benches/settlement_benches.md
@crates/z00z_simulator/src/scenario_1/stage_13_utils/report.rs
@crates/z00z_simulator/src/scenario_1/stage_13_utils/hjmt_examples.rs
@crates/z00z_simulator/src/scenario_1/runner_verify.rs
</context>

<coverage_contract>
This plan owns the benchmark, Stage 13 evidence, and closeout-guardrail portion
of Phase 2 in `055-TODO.md`: baseline comparison against `Vec<ProofBlob>`,
required logical benchmarks `hjmt_batch_proof_bytes` and `hjmt_batch_verify`,
the scenario-evidence extensions implied by the comparison contract, and the
final repository surfaces that must not overclaim the feature without measured
proof. All matching benchmark-matrix, evidence-gap, and release-gate bullets in
the upgrade paper and fixture checklist remain mandatory. Respect
`055-CONTEXT.md` decisions D-11, D-13, D-14, D-15, D-19, D-20, and D-21. The exact
`Mandatory Global Cross-Read Before Implementation` and `Mandatory Phase 2
Cross-Read For Every Numbered Plan` lists in `055-CONTEXT.md` remain
normative for this plan; omitting a source section name from the local
`read_first` bullets does not remove it from scope.
</coverage_contract>

<tasks>
<task type="auto">
<name>Add logical batch-proof benchmark lanes to the live storage bench harness</name>
<read_first>
- `.planning/phases/055-HJMT-boundary/055-CONTEXT.md` sections `Mandatory Global Cross-Read Before Implementation` and `Mandatory Phase 2 Cross-Read For Every Numbered Plan`
- `.planning/phases/055-HJMT-boundary/055-TODO.md` benchmark bullets
- `.planning/phases/055-HJMT-boundary/055-TEST-SPEC.md` benchmark owner-home rules
- `docs/tech-papers/Z00Z-HJMT-Upgrade.md` sections 3.5, 3.8, 9.1, 9.2, 9.3, 12, 12.1, Appendix E.2, and Appendix E.3
- `crates/z00z_storage/benches/settlement_proofs.rs`
- `crates/z00z_storage/benches/settlement_hjmt.rs`
- `crates/z00z_storage/benches/settlement_benches.md`
</read_first>
<files>
- `crates/z00z_storage/benches/settlement_hjmt.rs`
- `crates/z00z_storage/benches/settlement_proofs.rs`
- `crates/z00z_storage/benches/settlement_benches.md`
- `crates/z00z_storage/scripts/run_storage_settlement_bench.py`
- `crates/z00z_storage/tests/test_bench_lanes.rs`
- `crates/z00z_storage/src/settlement/README.md`
</files>
<action>
Extend the existing storage bench harness with logical batch-proof lanes instead
of creating new standalone bench crates. Add comparison lanes for one
`ProofBlob`, current `Vec<ProofBlob>`, and `BatchProofBlobV1` across clustered
and scattered path sets, with representative path counts and proof families
that match the upgrade paper's acceptance story. Wire the logical lanes
`hjmt_batch_proof_bytes` and `hjmt_batch_verify` through the existing
`settlement_proofs.rs` and `settlement_hjmt.rs` homes, update `settlement_benches.md` and
the bench runner so the exact commands and output-doc ownership remain
canonical, and extend the bench-lane guardrails so repo drift is caught by
tests. Keep measured values as evidence only; do not turn them into protocol
constants or docs-only claims.
</action>
<acceptance_criteria>
- The required logical batch benchmark lanes compile inside the live bench
  homes.
- Bench docs and runner commands describe the batch-proof comparison surface
  truthfully and use the live feature names.
- The batch lanes compare one `ProofBlob`, current `Vec<ProofBlob>`, and
  `BatchProofBlobV1` instead of replacing the old baseline.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Then run `cargo test --release`. Also run `cargo test -p z00z_storage --release --features test-params-fast --test test_bench_lanes -- --nocapture`, `cargo bench -p z00z_storage --bench settlement_proofs --no-run`, and `cargo bench -p z00z_storage --bench settlement_hjmt --no-run`. If the measured-report plumbing changes, also run `./crates/z00z_storage/scripts/run_storage_settlement_bench.py --bench settlement_proofs -- --sample-size 10 --warm-up-time 0.01 --measurement-time 0.02`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until at least 2 consecutive runs show no significant issues in the code.
4. When a commit is needed, use `/z00z-git-versioning`.
</verify>
<done>
The live bench harness can measure and document the additive batch-proof
surface against the current independent baseline.
</done>
</task>

<task type="auto">
<name>Extend Stage 13 reports and runner verification with batch-proof evidence</name>
<read_first>
- `.planning/phases/055-HJMT-boundary/055-CONTEXT.md` sections `Mandatory Global Cross-Read Before Implementation` and `Mandatory Phase 2 Cross-Read For Every Numbered Plan`
- `.planning/phases/055-HJMT-boundary/055-TEST-SPEC.md` Scenario 1 additional checks
- `crates/z00z_simulator/src/scenario_1/stage_13_utils/report.rs`
- `crates/z00z_simulator/src/scenario_1/stage_13_utils/hjmt_examples.rs`
- `crates/z00z_simulator/src/scenario_1/runner_verify.rs`
- `crates/z00z_simulator/tests/test_scenario_settlement.rs`
- `crates/z00z_simulator/tests/test_scenario1_stage_surface.rs`
</read_first>
<files>
- `crates/z00z_simulator/src/scenario_1/stage_13_utils/report.rs`
- `crates/z00z_simulator/src/scenario_1/stage_13_utils/hjmt_examples.rs`
- `crates/z00z_simulator/src/scenario_1/runner_verify.rs`
- `crates/z00z_simulator/tests/test_scenario_settlement.rs`
- `crates/z00z_simulator/tests/test_scenario1_stage_surface.rs`
</files>
<action>
Extend Stage 13 in place so the existing artifact pack becomes batch-proof
aware. Keep `E1` through `E8` stable, but add batch-proof evidence fields or
rows that record `proof_surface`, `path_count`, `path_shape`, `canonical_order`,
`atomic_verdict`, and current `shard_context_mode`. Extend the proof-size
report with comparison rows for one `ProofBlob`, current `Vec<ProofBlob>`, and
`BatchProofBlobV1`. Extend the tamper report and runner verification with the
batch-specific reject cases: unsupported root generation, canonical path
reorder, duplicate path, mixed proof family, opening mismatch, leaf-family
mismatch, witness-reference out of range, and wrong default commitment.
Strengthen the runner so missing batch comparison evidence, missing clustered or
scattered representative cases, or loss of the atomic-verdict contract causes
Stage 13 verification to fail. Do not create a second simulator lane or a new
artifact family when the existing report schemas can be extended.
</action>
<acceptance_criteria>
- Stage 13 artifacts prove the additive batch surface without changing the
  existing example identities or introducing a second scenario lane.
- Runner verification rejects scenario output that lacks the new batch-proof
  comparison or tamper evidence.
- The Stage 13 reports compare one `ProofBlob`, current `Vec<ProofBlob>`, and
  `BatchProofBlobV1` explicitly.
- The current live generation is recorded, and shard context remains absent for
  accepted Phase 055 batch evidence.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Then run `cargo test --release`. Also run `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools --test test_scenario_settlement -- --nocapture`, `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools --test test_scenario1_stage_surface -- --nocapture`, and `cargo run --release -p z00z_simulator --bin scenario_1 --features test-params-fast --features wallet_debug_tools`. If the scenario workload evidence changes, also run `./crates/z00z_storage/scripts/run_storage_settlement_bench.py --bench scenario_1`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until at least 2 consecutive runs show no significant issues in the code.
4. When a commit is needed, use `/z00z-git-versioning`.
</verify>
<done>
Stage 13 is extended in place with batch-proof comparison and tamper evidence,
and the runner enforces that evidence as part of the repository truth surface.
</done>
</task>
</tasks>

<threat_model>
- Repudiation: a feature can be "done" without durable evidence; Stage 13 and
  bench docs become the closeout authority.
- Information disclosure: metrics or scenario artifacts can drift into semantic
  truth or raw private counters; evidence remains bounded and non-authoritative.
- Tampering: benchmark or scenario claims can bypass the live additive baseline;
  comparisons must keep `ProofBlob`, `Vec<ProofBlob>`, and `BatchProofBlobV1`
  visible together.
</threat_model>

<verification>
Plan execution must produce `055-04-SUMMARY.md` with explicit measured or
runner-verified evidence for the batch-proof comparison surface, the Stage 13
artifact extensions, and the final guardrails that prevent the repo from
claiming Phase 055 closure without those evidence paths.
</verification>

<success_criteria>
- Logical batch benchmark lanes compile and are documented in the live harness.
- Stage 13 artifacts and runner verification include batch-proof comparison and
  tamper evidence.
- The repository has a closeout evidence path for the additive batch-proof
  surface instead of only unit tests.
</success_criteria>

<output>
Create `.planning/phases/055-HJMT-boundary/055-04-SUMMARY.md` when done.
</output>
