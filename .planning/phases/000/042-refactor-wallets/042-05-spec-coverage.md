# 042-05 Spec Coverage Ledger

**Status:** Wave 1 active; the receiver-native RPC/session/public-facade cleanup, concrete and trait-level receiver-manager contract rename, receiver cache/rate-limit config seam rename, and stealth-native filename/bin cleanup are landed. The honest remaining blocker set is now Phase 042 closeout work: broader docs/planning sync, release-style validation, and any remaining acceptance decisions, not wallet-crate address-era source residue.

## Control Decisions

| ID | Decision | Wave | Status | Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| D-01 | Use an epoch bump and reject old wallet snapshots/backups instead of building a one-shot converter. | 1 | done | Phase 042 planning note + current live inventory | Matches the product decision that legacy address APIs do not keep backward compatibility. |

## Coverage Map

| ID | Source item | Wave | Status | Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| W1-01 | Step 0 inventory command set | 1 | done | Inventory searches run across `crates/z00z_wallets/src` and `crates/z00z_wallets/tests` | Commands covered the legacy address names and the legacy RPC names. |
| W1-02 | Step 0 bucket split | 1 | done | Inventory output summarized in the wave log | Buckets: legacy implementation, public facades/re-exports, wallet-session callers, RPC callers, tests, docs/planning. |
| W1-03 | Step 0 inventory summary entry | 1 | done | This ledger and `042-05-wave-log.md` | The implementation summary will reference this ledger as the source-of-truth index. |
| W1-04 | Step 1 receiver DTOs and API names | 2 | done | `crates/z00z_wallets/src/adapters/rpc/types/key.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/key.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs`, `crates/z00z_wallets/src/adapters/rpc/types/common.rs`, `crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs` | Receiver-native derive/list/validate/label DTOs and route names are landed, dead address-era DTO wrappers are removed, and the wallet crate scan no longer shows the old address response/type names. |
| W1-05 | Step 1 replacement map and deprecation cleanup | 2 | done | `crates/z00z_wallets/src/adapters/rpc/methods/key.rs`, `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs`, `crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs` | The public replacement map is now live for the active key routes: receiver aliases are present, `wallet.key.derive_dual_address` is removed, and `wallet.key.derive_receiver` replaces `wallet.key.derive_key`. |
| W2-01 | Step 2 state-holder renames | 2 | done | `crates/z00z_wallets/src/services/wallet/types/wallet_service_types_state.rs`, `crates/z00z_wallets/src/services/wallet/types/wallet_service_types_core.rs`, `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation.rs`, `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation_recovery.rs`, `crates/z00z_wallets/src/wallet/entity/wallet_entity_core.rs`, `crates/z00z_wallets/src/wallet/entity/wallet_entity_constructor.rs` | The active live session and entity lane is receiver-native by state-holder, helper, manager-field, and trait contract name. The wallet crate scan no longer shows `AddressManager` / `AsyncAddressManager`. |
| W2-02 | Step 2 derivation stack rewrite | 2 | done | `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation.rs`, `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation_recovery.rs`, `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_rotation.rs`, `crates/z00z_wallets/src/services/wallet/paths/wallet_paths.rs` | The active derivation stack now uses receiver-native helper, manager, cache, and rate-limit vocabulary, including `wallet.receiver.*` config keys and `Z00Z_WALLET_RECEIVER_*` env keys. |
| W2-03 | Step 2 caller and duplicate cleanup | 2 | in-progress | `crates/z00z_wallets/src/services/wallet/session/wallet_service_session.rs`, `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_rotation.rs`, deleted `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_seed_derivation.rs`, deleted `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_snapshot.rs` | The two unwired duplicate files were deleted after confirming they are not included by the active session root; `wallet_service_session_rotation.rs` now depends on receiver-native state and `list_receivers(...)`, and the remaining cleanup sits above it in trait/public naming rather than in duplicate runtime files. |
| W3-01 | Step 3 recovery and gap-scan rewrite | 3 | in-progress | `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation_recovery.rs` | The active live helper now exposes `list_cached_receivers(...)`, `receiver_cache_file_path(...)`, receiver-usage vocabulary, and a `list_receivers(...)` manager seam; the remaining gap is stronger receiver-identifier semantics plus removal of address-era trait/response wording from the backing subsystem. |
| W3-02 | Step 3 cache policy | 3 | done | `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation.rs`, `crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation_recovery.rs`, `crates/z00z_wallets/src/services/wallet/paths/wallet_paths.rs`, `crates/z00z_wallets/src/services/wallet/tests/test_wallet_paths_suite.rs` | The compiled wallet lane and its config/env seam now use `.receiver_cache`, `wallet.receiver.*`, and `Z00Z_WALLET_RECEIVER_*`; the address-era cache/rate-limit names are absent from the wallet crate. |
| W4-01 | Step 4 RPC derive rebinding | 3 | in-progress | `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_derive.rs`, `crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs` | Live derive now returns `RuntimeDeriveReceiverResponse { public_key, path }`, no longer synthesizes `Z00ZSingleAddress`, and is exposed as `wallet.key.derive_receiver`; the remaining gap is whether `{ public_key, path }` is the final receiver-material contract or a temporary receiver-native bridge. |
| W4-02 | Step 4 RPC wiring cleanup | 3 | done | `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs`, `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs`, `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs`, `crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs` | The public derive wiring is now receiver-native: `wallet.key.derive_receiver` is registered and `wallet.key.derive_key` / `wallet.key.derive_dual_address` are no longer part of the active RPC contract. |
| W5-01 | Step 5 validation and label rebinding | 3 | done | `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_admin.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_derive.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support_tail.rs`, `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs`, `crates/z00z_wallets/src/adapters/rpc/types/key.rs` | Receiver validation and labeling are now receiver-native across the live helper, request, and response vocabulary inside the wallet crate. |
| W6-01 | Step 6 persistence and backup policy | 4 | in-progress | `crates/z00z_wallets/src/wallet/snapshot/snapshot_types.rs`, `crates/z00z_wallets/src/wallet/snapshot/snapshot_impl.rs`, `crates/z00z_wallets/src/backup/import/**`, `crates/z00z_wallets/src/backup/export/**` | The persisted contract now uses `ReceiverDeriverState`, `receiver_deriver`, and snapshot version `5`, and the wallet crate compile gate is green after the backup/import/export fixture rewrites; explicit old-format rejection coverage and the remaining cache/file policy work are still open. |
| W6-02 | Step 6 label migration decision | 4 | pending | `crates/z00z_wallets/src/services/wallet/store/**`, `crates/z00z_wallets/src/wallet/snapshot/**` | Preserve counters; intentionally drop labels when deterministic mapping cannot be proven. |
| W7-01 | Step 7 public facade removal | 4 | done | `crates/z00z_wallets/src/address/mod.rs`, `crates/z00z_wallets/src/key/mod.rs`, `crates/z00z_wallets/src/lib.rs`, `crates/z00z_wallets/src/wallet/mod.rs` | The public facade now exports only receiver-native manager contracts and the `Stealth*` address family. The wallet crate scan no longer shows the old public address-manager contract names. |
| W7-02 | Step 7 implementation archive gate | 4 | done | `crates/z00z_wallets/src/address/stealth_address/**`, `crates/z00z_wallets/bin/z00z-wallet-stealth-convert.rs` | The live implementation subtree, helper include filenames, guide, and helper binary are stealth-native on disk. File searches no longer show `z00z*address*` or `addr-convert` names inside the wallet crate. |
| W8-01 | Step 8 test rewrites and source-shape guards | 4 | pending | `crates/z00z_wallets/tests/**` | Rewrite tests around receiver DTOs, recovery counters, and fail-closed validation. |
| W8-02 | Step 8 backup/import/export tests and simulator checks | 4 | pending | `crates/z00z_wallets/tests/**`, `crates/z00z_simulator/**` | Preserve release-style checks where wallet RPC flows are used. |
| W9-01 | Step 9 docs updates | 4 | in-progress | `docs/**`, `crates/z00z_wallets/src/key/bip/docs/**`, `crates/z00z_wallets/src/wallet/WALLET-GUIDE.md` | Local derive docs now reflect `wallet.key.derive_receiver`, `RuntimeDeriveReceiverResponse { public_key, path }`, and the removed dual-address RPC, but the broader wallet documentation rewrite is still pending. |
| W10-01 | Step 10 final deletion gate | 5 | pending | `crates/z00z_wallets/src/**`, `crates/z00z_wallets/tests/**`, `crates/z00z_wallets/src/address/z00z_address.tar.gz` | Final gate is allowed only when the production source is clean and the archive target is gone or moved. |
| V-01 | Focused validation suite | 2-5 | in-progress | `cargo test -p z00z_wallets --lib test_snapshot_version -- --nocapture`, `cargo test -p z00z_wallets --no-run`, `cargo test -p z00z_wallets --test test_backup_restore_identity -- --nocapture`, `cargo test -p z00z_wallets --test test_rpc_key_derive_e2e -- --test-threads=1 --nocapture` | Focused wave-local validation is now being recorded in the wave log; broader release-style validation is still pending. |
| V-02 | Release-style validation suite | 5 | pending | `cargo test -p z00z_wallets --release --features test-fast --all-targets`, simulator release commands | Required before closeout. |
| V-03 | Source-shape gates | 5 | in-progress | `rg` legacy-name guards, `file_search` filename guards, `cargo check -p z00z_wallets --all-targets`, and focused wallet-path tests | The wallet crate scan is now clean for the targeted address-era symbols, filenames, DTO names, and config/env keys, and all wallet targets compile. Formal regression guards for future drift are still manual rather than codified in dedicated tests. |
| R-01 | Derivation counters drift during rename from address to receiver. | 2-5 | pending | Phase 042 risk table | Keep deterministic counter semantics and prove recovery. |
| R-02 | Labels cannot be mapped from legacy address strings to receiver ids. | 4 | pending | Phase 042 risk table | Prefer epoch bump over inventing a fake mapping. |
| R-03 | RPC methods remain address-shaped after internals migrate. | 3-4 | pending | Phase 042 risk table | Delete or rename the public methods in the same migration chain. |
| R-04 | `AddressManagerImpl` remains as a hidden dependency. | 2-4 | done | `rg` legacy-name sweep across `crates/z00z_wallets/**`, `file_search` filename sweep, and `cargo check -p z00z_wallets --all-targets` | The targeted wallet-crate hidden dependency risk has been closed by the receiver-manager contract rename and the clean residue scan. |
| R-05 | BIP32/BIP44 gets removed accidentally. | 2-4 | pending | Phase 042 risk table | Treat BIP32/BIP44 as the recovery substrate, not legacy address code. |
| R-06 | Receiver card display field named `address` causes concept drift. | 3 | pending | Phase 042 risk table | Rename or remove the display-only field. |
| AC-001 | Wallet receiver material is derived from seed/key/path state without `z00z_address`. | 2 | pending | Acceptance criteria | |
| AC-002 | RPC derive returns receiver/card/request material and no legacy address string. | 3 | in-progress | `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_derive.rs`, `crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs` | The live route and DTO are now receiver-native and the legacy address string is gone, but the phase still needs a final decision on whether `{ public_key, path }` is sufficient receiver material or must be upgraded to a stronger receiver/card/request contract. |
| AC-003 | Legacy address strings are rejected as unsupported or unknown input after cutover. | 3 | pending | Acceptance criteria | |
| AC-004 | Receiver card and payment request validation uses fail-closed stealth paths. | 3 | pending | Acceptance criteria | |
| AC-005 | Labels bind to receiver-local identifiers instead of legacy address strings. | 3 | pending | Acceptance criteria | |
| AC-006 | Recovery reconciles derivation progress with deterministic counter safety. | 2-4 | pending | Acceptance criteria | |
| AC-007 | Production source contains no live legacy address imports or types. | 5 | done | `rg` legacy-name sweep across `crates/z00z_wallets/**`, `file_search` filename sweep, and `cargo check -p z00z_wallets --all-targets` | The targeted wallet-crate source, tests, benches, docs, helper binary, and filenames no longer carry the legacy address-era names from the Phase 042 removal inventory. |
| AC-008 | Public crate exports expose only stealth receiver/card/request surfaces. | 5 | done | `crates/z00z_wallets/src/key/mod.rs`, `crates/z00z_wallets/src/lib.rs`, `crates/z00z_wallets/src/address/mod.rs`, `crates/z00z_wallets/src/wallet/mod.rs` | Public exports are receiver-native and stealth-native across the wallet crate facade. The remaining phase work is no longer public legacy export removal. |
| AC-009 | Tests prove receiver derivation, validation, labels, and recovery counters without legacy address tests. | 4-5 | pending | Acceptance criteria | |
| AC-010 | Active wallet/RPC docs describe receiver cards, payment requests, and scan outputs. | 4-5 | in-progress | `crates/z00z_wallets/README.md`, `crates/z00z_wallets/src/key/bip/docs/KEYS_EXPALNATION.md`, `crates/z00z_wallets/src/key/manager/KEYS-DERIVATION.md` | The touched derive docs now use receiver-native route/DTO names, but the full wallet/RPC documentation set is not yet migrated. |
| PN-01 | The phase is structural-only in the source spec, but this execution plan intentionally treats it as a stealth-only public-API migration with no backward compatibility. | 1 | done | Phase 042 planning note | This is the governing assumption for the epoch-bump decision. |

## Live Inventory Buckets

| Bucket | Current examples | Status | Evidence |
| --- | --- | --- | --- |
| Legacy implementation to delete | `address/stealth_address/**`, `address/manager/*`, `server_admin.rs`, `support.rs`, `support_tail.rs` | cleared-at-wallet-crate-scope | The live implementation, helper structs, and on-disk filenames are receiver-native/stealth-native for the targeted residue inventory. |
| Public facade / re-export to remove | `address/mod.rs`, `adapters/rpc/types/key.rs`, `adapters/rpc/methods/key.rs` | cleared-at-wallet-crate-scope | Public facade and RPC type/route names are receiver-native at wallet-crate scope. |
| Wallet-session caller to migrate | `wallet_service_session_derivation.rs`, `wallet_service_session_derivation_recovery.rs`, `wallet_service_session_rotation.rs` | cleared-at-wallet-crate-scope | The active session lane uses receiver-native helper, config, cache, and manager-contract names. |
| RPC caller to migrate | `adapters/rpc/methods/key.rs`, `key_impl/server_admin.rs`, `wallet_dispatcher_wiring_register.rs` | cleared-at-wallet-crate-scope | The active RPC caller layer is receiver-native for the targeted address-era inventory. |
| Tests to delete or rewrite | `tests/**`, `address/stealth_address/test_stealth_address_suite.rs`, `key_impl/test_key_impl.rs`, `test_address_manager_suite.rs`, backup/import/export tests | narrowed | Targeted legacy name residue is gone from the wallet crate, but broader acceptance/release validation and any non-inventory documentation rewrites still remain. |
| Historical docs / planning references | `key/bip/docs/KEYS_EXPALNATION.md`, `wallet/WALLET-GUIDE.md`, `address/stealth_address/Z00Z-ADDRESS-GUIDE.md`, `.planning/**` | keep-or-update | Inventory found docs that still teach the legacy model and should be rewritten or archived. |

## Notes

- Bootstrap gate passed with warnings only; the warning set is current and does not block Wave 1.
- The inventory search returned broad live surface matches in the wallet crate; the wave log records the most relevant hits and counts.
- The wallet-crate source-shape inventory is now clean for the targeted Phase 042 address-era residue. The honest remaining blocker is phase-closeout work outside that narrow slice: broader docs/planning sync, release-style validation, and any unresolved acceptance semantics such as the final receiver-material contract.
