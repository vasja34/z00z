---
phase: 042-refactor-wallets
plan: 04
type: execute
wave: 3
depends_on:
  - 042-02-PLAN.md
  - 042-03-PLAN.md
files_modified:
  - crates/z00z_wallets/src/core/mod.rs
  - crates/z00z_wallets/src/db/mod.rs
  - crates/z00z_wallets/src/services/mod.rs
  - crates/z00z_wallets/src/lib.rs
  - .planning/phases/042-refactor-wallets/042-*-SUMMARY.md
autonomous: true
requirements:
  - 042-CROSS-FACADE-CLEANUP
  - 042-CONTEXT-D10
  - 042-CONTEXT-D13
  - 042-CONTEXT-D16
must_haves:
  truths:
    - All temporary compatibility aliases are removed only after full row completion.
    - Final facade exports match migrated target layouts across core/db/services.
    - Validation gates and review loop complete with no significant unresolved findings.
  artifacts:
    - path: crates/z00z_wallets/src/core/mod.rs
      provides: Finalized core facade after alias cleanup
    - path: crates/z00z_wallets/src/db/mod.rs
      provides: Finalized db facade after alias cleanup
    - path: crates/z00z_wallets/src/services/mod.rs
      provides: Finalized services facade after alias cleanup
    - path: .planning/phases/042-refactor-wallets/042-04-SUMMARY.md
      provides: End-to-end closeout and audit evidence
  key_links:
    - from: crates/z00z_wallets/src/lib.rs
      to: core/db/services facades
      via: final public export reconciliation
    - from: phase summaries
      to: row manifests
      via: traceability proof for no-omission execution
---

<!-- markdownlint-disable MD022 MD032 MD033 MD041 MD055 MD056 MD060 -->

<objective>
Finalize cross-facade cleanup and phase closeout after row-by-row migrations of core, db, and services.

Purpose: Enforce D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16 final invariants and produce final evidence.
Output: Clean facades, green gates, review-stable closeout artifacts.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/042-refactor-wallets/042-CONTEXT.md
@.planning/phases/042-refactor-wallets/042-01-PLAN.md
@.planning/phases/042-refactor-wallets/042-02-PLAN.md
@.planning/phases/042-refactor-wallets/042-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-facade reconciliation and alias removal gate</name>
  <files>crates/z00z_wallets/src/core/mod.rs, crates/z00z_wallets/src/db/mod.rs, crates/z00z_wallets/src/services/mod.rs, crates/z00z_wallets/src/lib.rs</files>
  <action>Verify that all row manifests from plans 01-03 are fully done. Then remove remaining temporary aliases in facades in a controlled order (core -> db -> services -> lib export reconciliation). Do not remove aliases before proving downstream imports are rewired.</action>
  <verify>
    <automated>
      0) MUST confirm copied DB source tables remain present in this PLAN, each ledger change was executed line-by-line (no batch moves), all row manifests (core/db/services) report full completion, unresolved targets are still labeled `proposed`, and no duplicate/parallel abstraction lane was introduced.
      1) Run fail-fast gate first: `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` (if fails: stop, fix, rerun until pass).
      2) Run release regression gate: `cargo test --release --features test-fast --features wallet_debug_dump`.
      3) Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times; continue until at least 2 consecutive runs report no significant issues; fix every issue/warning between runs.
    </automated>
  </verify>
  <done>Facade aliases are removed without breaking compile/test gates, and all exports resolve via final paths.</done>
</task>

<task type="auto">
  <name>Task 2: Security and cryptographic invariant regression pass</name>
  <files>crates/z00z_wallets/src/core/**, crates/z00z_wallets/src/services/**, crates/z00z_wallets/src/db/**</files>
  <action>Audit moved paths for invariant safety: domain separation continuity, secret lifetime handling, fail-closed errors, and no debug helper exposure through production facades. Patch only structural export/import mistakes if found; do not alter business semantics.</action>
  <verify>
    <automated>
      0) MUST confirm copied DB source tables remain present in this PLAN, each ledger change was executed line-by-line (no batch moves), and there are no omitted ledger rows or legacy path leaks in production facades.
      1) Run fail-fast gate first: `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` (if fails: stop, fix, rerun until pass).
      2) Run release regression gate: `cargo test --release --features test-fast --features wallet_debug_dump`.
      3) Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times; continue until at least 2 consecutive runs report no significant issues; fix every issue/warning between runs.
    </automated>
  </verify>
  <done>Invariant audit passes with no significant unresolved findings and no behavior-level regressions introduced by structure changes.</done>
</task>

<task type="auto">
  <name>Task 3: Final Phase 042 closeout package</name>
  <files>.planning/phases/042-refactor-wallets/042-04-SUMMARY.md, .planning/phases/042-refactor-wallets/042-closeout-audit.md</files>
  <action>Create final closeout docs consolidating evidence from plans 01-03 (row manifests, gate outputs, review-run logs, unresolved blockers). For any phase closeout commit, use `/z00z-git-versioning` instead of manual git commit.</action>
  <verify>
    <automated>
      0) MUST confirm copied DB source tables remain present in this PLAN, and closeout includes explicit proof that each ledger row from core/db/services was executed line-by-line (no batch moves).
      1) Run fail-fast gate first: `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` (if fails: stop, fix, rerun until pass).
      2) Run release regression gate: `cargo test --release --features test-fast --features wallet_debug_dump`.
      3) Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times; continue until at least 2 consecutive runs report no significant issues; fix every issue/warning between runs.
    </automated>
  </verify>
  <done>Closeout package is complete, evidence is traceable, and phase is ready for execution handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final facade cleanup -> public callers | Alias removal can break external import contracts |
| moved security/crypto paths -> runtime usage | Structural drift can weaken safety invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-042-10 | T | final facade cleanup | mitigate | Remove aliases only after manifest completion proof and green gates |
| T-042-11 | I | secret/session/key paths | mitigate | Invariant audit on fail-closed and zeroization paths after rewiring |
| T-042-12 | E | debug helper exposure | mitigate | Explicit export audit to ensure debug/test helpers are not publicly reachable |
</threat_model>

<verification>
- All manifests complete and cross-checked against ledgers.
- Final export surface audit across core/db/services/lib.
- Ordered gate sequence preserved for all auto tasks.
</verification>

<success_criteria>
- No omitted rows across core/db/services ledgers.
- Final facades are stable and alias-free.
- Verification and review loops end clean with consecutive no-significant-issue runs.
</success_criteria>

<output>
After completion, create `.planning/phases/042-refactor-wallets/042-04-SUMMARY.md`.
</output>


