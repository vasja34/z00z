---
phase: 054-Refactor-Crates
plan: 054-01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_storage/benches/assets_proofs.rs
  - crates/z00z_storage/tests/test_live_guardrails.rs
  - crates/z00z_storage/tests/test_downstream_guardrails.rs
  - crates/z00z_storage/src/lib.rs
  - crates/z00z_storage/src/error.rs
  - crates/z00z_storage/src/backend/mod.rs
  - crates/z00z_storage/src/backend/error.rs
requirements:
  - PH54-01
autonomous: true
must_haves:
  truths:
    - "`SettlementStore`, `SettlementTreeBackend`, and the public proof helpers remain the live semantic authority while backend contracts are introduced beneath them."
    - "`StoreBackendError` may move files, but its live symbol stays stable until the delayed rename wave."
    - "`assets_proofs.rs` and the existing storage suites are compatibility gates, not collateral damage during structural refactor."
    - "Migration wording must stay anchored to the live current symbols during the move: `AggregatorService`, `ValidatorService`, `WatcherService`, `WatcherBoundary`, `ValidatorBoundary`, `IngressBoundary`, `OrderingBoundary`, `RecoveryBoundary`, `SchedulerBoundary`, `SettlementTreeBackend`, and `StoreBackendError`."
  artifacts:
    - crates/z00z_storage/src/backend/mod.rs
    - crates/z00z_storage/src/backend/error.rs
    - crates/z00z_storage/src/error.rs
    - crates/z00z_storage/benches/assets_proofs.rs
  key_links:
    - "Phase 053 generalized settlement remains the live contract while Phase 054 adds a lower backend seam."
    - "Low-level `StorageBackend` and `JournalBackend` do not replace `SettlementTreeBackend`."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 054-01 Plan: Guardrails And Backend Contract Freeze

<objective>
Freeze the proof/public API guardrails and introduce the low-level storage
backend seam without changing the live `SettlementStore` authority surface.

Purpose: make later adapter moves safe instead of speculative.
Output: compatibility gates plus `backend/mod.rs` and `backend/error.rs`
anchored beneath the existing semantic facade.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/054-Refactor-Crates/054-TODO.md
@.planning/phases/054-Refactor-Crates/054-CONTEXT.md
@.planning/phases/054-Refactor-Crates/054-SOURCE-AUDIT.md
@.planning/phases/000/053-HJMT-Backend/053-CONTEXT.md
@docs/tech-papers/Z00Z-HJMT-Upgrade.md
@docs/tech-papers/Z00Z-HJMT-Key-Terms.md
@docs/tech-papers/Z00Z-HJMT-Design.md
@crates/z00z_storage/src/lib.rs
@crates/z00z_storage/src/error.rs
@crates/z00z_storage/src/settlement/store/mod.rs
@crates/z00z_storage/benches/assets_proofs.rs
</context>

<coverage_contract>
This plan implements the complete Phase 054 guardrail and backend-contract
portion of `054-TODO.md`, not only the summarized bullets in this file. Treat
the Architectural Verdict, Migration Safety Rules, hard execution steps 1-3,
the “do not mix in one wave” list, and the section-18 carry-over rules as
mandatory. If `legacy-refactor-spec.md` is absent in the current worktree, use
`054-TODO.md` section 18 as the extracted live authority and do not recreate
the missing file.
</coverage_contract>

<tasks>
<task type="auto">
<name>Freeze proof-surface guardrails and add low-level backend contracts</name>
<read_first>
- `.planning/phases/054-Refactor-Crates/054-TODO.md` sections Architectural Verdict, Migration Safety Rules, hard steps 1-3, and section 18
- `docs/tech-papers/Z00Z-HJMT-Upgrade.md` section 1.7 on whole-system layering and backend seams
- `docs/tech-papers/Z00Z-HJMT-Key-Terms.md` for cross-phase naming authority while paper target-state names and live repo exports coexist
- `crates/z00z_storage/src/settlement/README.md` and `root-types.md` on semantic root and proof ownership
- `.github/requirements/Z00Z_DESIGN_FOUNDATION.md` public API, error, and no-parallel-layer rules
</read_first>
<files>
- `crates/z00z_storage/benches/assets_proofs.rs`
- `crates/z00z_storage/tests/test_live_guardrails.rs`
- `crates/z00z_storage/tests/test_downstream_guardrails.rs`
- `crates/z00z_storage/src/lib.rs`
- `crates/z00z_storage/src/error.rs`
- `crates/z00z_storage/src/backend/mod.rs`
- `crates/z00z_storage/src/backend/error.rs`
</files>
<action>
Lock the Phase 053 proof/public API compatibility surface before structural
moves land. Extend or add storage guardrails that explicitly protect
`SettlementStore`, `SettlementTreeBackend`, `SettlementStateRoot`,
`ProofBlob`, and the proof bench surface while introducing
`z00z_storage::backend::{StorageBackend, JournalBackend, ReadTxn, WriteTxn}` as
low-level durable contracts below the semantic facade. Move backend-specific
error definitions behind `backend/error.rs`, but keep the live symbol
`StoreBackendError` stable and do not rebrand public settlement or runtime
contracts in this slice. While the seam is being introduced, keep migration
text and guardrails anchored to the current live symbol set instead of
presenting paper target-state names as already-landed exports.
</action>
<acceptance_criteria>
- The repository has an explicit low-level backend seam beneath
  `SettlementTreeBackend` without changing the live `SettlementStore` API.
- `StoreBackendError` remains the stable symbol even if its owning file moves.
- Storage guardrails and proof-surface compatibility checks fail closed if the
  semantic/public contract drifts toward backend-root or layout authority.
</acceptance_criteria>
<verify>
1. If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop, fix the issues, and rerun it before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.
</verify>
<done>
The backend seam exists as a lower storage contract, and the public
settlement/proof surface is frozen as the compatibility oracle for later waves.
</done>
</task>
</tasks>

<threat_model>
- Spoofing: a low-level backend type can be mistaken for the semantic storage
  authority; guardrails keep `SettlementTreeBackend` and settlement proofs
  authoritative.
- Tampering: structural refactor can silently widen backend-root or layout
  authority; proof/public API guards block that drift before adapter moves.
- Repudiation: if `StoreBackendError` and seam contracts move without stable
  names, downstream breakage becomes hard to attribute; keep the live symbol
  stable in this slice.
</threat_model>

<verification>
Plan execution must produce `054-01-SUMMARY.md` with explicit evidence that the
proof/public API guardrails were established before any adapter move or
`SettlementStore` seam rewiring.
</verification>

<success_criteria>
- Low-level backend contracts exist beneath the semantic settlement facade.
- `StoreBackendError` is moved safely without premature rename drift.
- The public proof surface is guarded before structural storage moves begin.
</success_criteria>

<output>
Create `.planning/phases/054-Refactor-Crates/054-01-SUMMARY.md` when done.
</output>
