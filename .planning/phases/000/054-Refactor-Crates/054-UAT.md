---
status: passed
phase: 054-Refactor-Crates
source:
  - 054-01-SUMMARY.md
  - 054-02-SUMMARY.md
  - 054-03-SUMMARY.md
  - 054-04-SUMMARY.md
  - 054-05-SUMMARY.md
  - 054-06-SUMMARY.md
  - 054-07-SUMMARY.md
  - 054-SUMMARY.md
started: 2026-06-08T19:09:36Z
updated: 2026-06-08T22:04:21Z
---

# Phase 054 UAT

## Current Test

number: 7
name: Final release truth and blocker reporting remain honest
expected: |
  Run `cargo test --all --release -q` and
  `cargo test --release --features test-fast --features wallet_debug_dump`.

  The release workspace suite should pass green. The stale feature-name
  command should still fail immediately because the live manifest does not
  expose `test-fast` or `wallet_debug_dump`, and planning docs must not
  describe that stale command as blocking the workspace release suite.
awaiting: none

## Tests

### 1. Backend seam stays below the semantic settlement facade

expected: Run `cargo test -p z00z_storage --release --features test-params-fast --test test_live_guardrails --test test_downstream_guardrails -q` or inspect the current storage crate surface directly. The check should pass, and the live crate should keep `ReadTxn`, `WriteTxn`, `StorageBackend`, `JournalBackend`, and `StoreBackendError` below the semantic settlement facade. Downstream crates must not need to import backend-seam internals as public authority.
result: [passed]

### 2. Storage backend paths remain canonical with no bridge or shim return path

expected: Run `cargo test -p z00z_storage --release --features test-params-fast --test test_live_guardrails -q` or inspect `crates/z00z_storage/src/settlement/store/*`. The live store implementation should use the canonical `backend/common/*` and `backend/redb/*` ownership story, and no `store_codec`, `store_roots`, `store_mem`, or deleted `store_*` bridge path should return.
result: [passed]

### 3. Runtime planner authority lives only in the aggregator planning surface

expected: Run `cargo test --release -p z00z_aggregators -q` or inspect the current runtime planner flow. Batch ordering, route-table digesting, and one-shard route admission should resolve through the aggregator planner surface, while storage remains the owner of settlement semantics and proof truth.
result: [passed]

### 4. Placement and execution metadata stay downstream-only for validators, watchers, and node runtime

expected: Run `cargo test --release -p z00z_aggregators -p z00z_validators -p z00z_watchers -q` together with `cargo test -p z00z_rollup_node --release --features test-params-fast -q`. The checks should pass only if validators and watchers consume placement or execution as operational metadata, and `z00z_rollup_node` remains the orchestration root rather than a second planner or truth owner.
result: [passed]

### 5. Delayed rename wave leaves one canonical public facade path

expected: Run `cargo test -p z00z_wallets --release --features test-params-fast --test test_rename_guards -q` and inspect the touched crate roots if needed. Runtime, storage, and rollup-node surfaces should resolve through one canonical public path only; renamed implementation modules must not remain as a second public dialect.
result: [passed]

### 6. Docs and planning truth match the landed canonical topology

expected: Inspect `crates/z00z_runtime/*/README.md`, `crates/z00z_rollup_node/README.md`, `crates/z00z_storage/README.md`, `crates/z00z_storage/src/settlement/README.md`, `054-SUMMARY.md`, `054-SECURITY.md`, `054-VALIDATION.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`. The docs and planning packet should describe only the shipped canonical module layout, private implementation modules, crate-root facades, and the no-alias or no-shim closeout truth.
result: [passed]

### 7. Broad blockers remain recorded honestly and not overclaimed as phase-green workspace closure

expected: Run `cargo test --all --release -q` together with `cargo test --release --features test-fast --features wallet_debug_dump`. The workspace release suite should pass green, while the stale feature-name command should still fail immediately because the selected packages do not expose `test-fast` or `wallet_debug_dump`. The only honest broad blocker note is that stale manifest command, not the workspace release suite itself.
result: [passed]

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
