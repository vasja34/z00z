---
phase: 054-Refactor-Crates
plan: 054-03
type: execute
wave: 3
depends_on:
  - 054-02
files_modified:
  - crates/z00z_runtime/aggregators/src/lib.rs
  - crates/z00z_runtime/aggregators/src/agg_ordering.rs
  - crates/z00z_runtime/aggregators/src/agg_types.rs
  - crates/z00z_runtime/aggregators/src/batch_planner.rs
  - crates/z00z_storage/src/settlement/tx_plan.rs
  - crates/z00z_storage/src/settlement/tx_plan_help.rs
  - crates/z00z_storage/src/settlement/tx_plan_types.rs
requirements:
  - PH54-03
autonomous: true
must_haves:
  truths:
    - "Runtime gains planner authority only for canonicalization, route targeting, admission, and canonical planning digests."
    - "Storage keeps `tx_plan` helpers that still depend on `SettlementModel`, store-local dry-run, precheck, or rollback semantics."
    - "Current public runtime re-exports remain stable until the rename wave."
  artifacts:
    - crates/z00z_runtime/aggregators/src/batch_planner.rs
    - crates/z00z_runtime/aggregators/src/lib.rs
    - crates/z00z_storage/src/settlement/tx_plan.rs
  key_links:
    - "Planner authority becomes runtime-owned without demoting storage from semantic truth owner."
    - "`tx_plan` is split semantic-first, never moved wholesale."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 054-03 Plan: Runtime Batch Planner Split

<objective>
Move runtime-owned planner authority out of storage into an explicit batch
planner surface while retaining storage-local semantic helpers in
`z00z_storage`.

Purpose: separate routing/admission control from store-owned truth and replay.
Output: `batch_planner.rs` and a narrowed storage `tx_plan` surface.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/054-Refactor-Crates/054-TODO.md
@.planning/phases/054-Refactor-Crates/054-CONTEXT.md
@.planning/phases/054-Refactor-Crates/054-SOURCE-AUDIT.md
@.planning/phases/054-Refactor-Crates/054-02-SUMMARY.md
@docs/tech-papers/Z00Z-HJMT-Upgrade.md
@docs/tech-papers/Z00Z-HJMT-Key-Terms.md
@crates/z00z_runtime/aggregators/src/lib.rs
@crates/z00z_runtime/aggregators/src/ordering.rs
@crates/z00z_runtime/aggregators/src/types.rs
@crates/z00z_storage/src/settlement/store/hjmt_plan.rs
@crates/z00z_storage/src/settlement/tx_plan_help.rs
@crates/z00z_storage/src/settlement/tx_plan_types.rs
</context>

<coverage_contract>
This plan owns `054-TODO.md` hard steps 8-9, the runtime-aggregator target
rows that add `batch_planner.rs`, and the carry-now rule that `tx_plan` must be
split semantic-first rather than moved wholesale. It may begin only after the
`054-02-SUMMARY.md` seam-stabilization evidence records the post-seam storage
test and proof gate required by hard step 7.
</coverage_contract>

<tasks>
<task type="auto">
<name>Split runtime planner authority into `batch_planner.rs` while retaining store-local helpers</name>
<read_first>
- `.planning/phases/054-Refactor-Crates/054-TODO.md` Architectural Verdict, runtime aggregators table, hard steps 8-9, and the “cannot move all of tx_plan in one wave” rule
- `.planning/phases/054-Refactor-Crates/054-02-SUMMARY.md` seam-stabilization evidence so planner work does not start before the post-seam guardrail gate is satisfied
- `docs/tech-papers/Z00Z-HJMT-Upgrade.md` sections on runtime placement, `BatchPlanned`, and shard routing semantics
- `docs/tech-papers/Z00Z-HJMT-Key-Terms.md` so `BatchPlanned` and related naming stays consistent with the cross-phase glossary
- `crates/z00z_storage/src/settlement/README.md` and `root-types.md` so store-owned truth is not diluted
</read_first>
<files>
- `crates/z00z_runtime/aggregators/src/lib.rs`
- `crates/z00z_runtime/aggregators/src/agg_ordering.rs`
- `crates/z00z_runtime/aggregators/src/agg_types.rs`
- `crates/z00z_runtime/aggregators/src/batch_planner.rs`
- `crates/z00z_storage/src/settlement/tx_plan.rs`
- `crates/z00z_storage/src/settlement/tx_plan_help.rs`
- `crates/z00z_storage/src/settlement/tx_plan_types.rs`
</files>
<action>
Create `batch_planner.rs` in the aggregators crate and move only the
runtime-owned planner logic there: canonicalization, route-table targeting,
single-shard admission, canonical op digest generation, and `BatchPlanned`
inputs. Keep storage-local `tx_plan` helpers in `z00z_storage` when they still
depend on `SettlementModel`, store snapshots, dry-run, duplicate-path
protection, or rollback semantics. Preserve current public runtime symbol names
in this wave and keep storage as the semantic truth owner.
</action>
<acceptance_criteria>
- Runtime planner authority is explicit in `batch_planner.rs`.
- Storage retains store-scoped planning helpers that are still semantic or
  replay-related.
- No public runtime or storage symbol rename is coupled to this semantic split.
</acceptance_criteria>
<verify>
1. If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop, fix the issues, and rerun it before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.
</verify>
<done>
Planner authority is runtime-owned in a dedicated module, and storage keeps the
semantic helper subset it still owns.
</done>
</task>
</tasks>

<threat_model>
- Spoofing: runtime planner metadata can be mistaken for settlement truth; the
  plan keeps semantic/replay helpers in storage and limits runtime ownership to
  admission logic.
- Tampering: a move-only `tx_plan` refactor can break dry-run or rollback
  semantics; the plan requires an explicit semantic split instead.
- Elevation of privilege: aggregators could accidentally become full proof/root
  owners; the acceptance contract blocks that authority drift.
</threat_model>

<verification>
Plan execution must produce `054-03-SUMMARY.md` with explicit evidence showing
what moved into runtime planner authority and what remained in storage semantic
helpers.
</verification>

<success_criteria>
- Runtime planner authority has a dedicated home.
- Storage-local planning helpers remain in storage where semantic truth demands it.
- Public re-exports stay stable until the rename wave.
</success_criteria>

<output>
Create `.planning/phases/054-Refactor-Crates/054-03-SUMMARY.md` when done.
</output>
