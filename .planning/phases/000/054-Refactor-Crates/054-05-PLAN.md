---
phase: 054-Refactor-Crates
plan: 054-05
type: execute
wave: 4
depends_on:
  - 054-02
  - 054-03
files_modified:
  - crates/z00z_storage/src/checkpoint/artifact.rs
  - crates/z00z_storage/src/checkpoint/build.rs
  - crates/z00z_storage/src/checkpoint/store.rs
  - crates/z00z_storage/src/checkpoint/build_prepare.rs
  - crates/z00z_storage/src/checkpoint/build_state.rs
  - crates/z00z_storage/src/checkpoint/store_fs.rs
  - crates/z00z_storage/src/settlement/store.rs
  - crates/z00z_storage/src/settlement/redb_backend.rs
  - crates/z00z_storage/src/settlement/tx_plan.rs
  - crates/z00z_storage/src/settlement/tx_plan_help.rs
  - crates/z00z_storage/src/settlement/tx_plan_types.rs
  - crates/z00z_storage/src/serialization/build.rs
  - crates/z00z_storage/src/serialization/build/temp_tree.rs
  - crates/z00z_storage/src/checkpoint/mod.rs
  - crates/z00z_storage/src/snapshot/mod.rs
  - crates/z00z_storage/proptest-regressions/settlement/whitebox/whitebox_state.txt
requirements:
  - PH54-05
autonomous: true
must_haves:
  truths:
    - "Active storage `#[path]` bridge debt is cleaned only after backend seam and planner split stabilization."
    - "Checkpoint, snapshot, and serialization remain separate surface areas during canonical-module cleanup."
    - "Checkpoint split rows, whitebox residue, and support-file rows from TODO sections 6-17 are explicit cleanup or validation anchors, not implied leftovers."
    - "The simulator generated-data exception stays explicit and untouched in this storage-focused slice."
    - "TODO sections 10-15 contain some older or target-shape examples; live Phase 054 work must bind to the current repository equivalents: `settlement/store/hjmt_*.rs`, `backend/redb/*.rs`, `backend/common/*.rs`, canonical nested checkpoint and snapshot support paths, and `serialization/build/temp_tree.rs`."
  artifacts:
    - crates/z00z_storage/src/settlement/store.rs
    - crates/z00z_storage/src/settlement/redb_backend.rs
    - crates/z00z_storage/src/settlement/tx_plan.rs
    - crates/z00z_storage/src/serialization/build.rs
  key_links:
    - "Canonical-module cleanup follows semantics; it does not define them."
    - "Localized duplicate-helper removal must not hide proof, root, or replay regressions."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 054-05 Plan: Storage Canonical-Module Cleanup

<objective>
Remove the active storage bridge debt and localized duplicate helpers after the
backend seam and planner split are stable.

Purpose: turn the landed semantic topology into canonical modules instead of
continuing to rely on bridge wiring.
Output: active storage hot spots without `#[path = ...]` bridge debt and one
canonical serialization temp-tree helper.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/054-Refactor-Crates/054-TODO.md
@.planning/phases/054-Refactor-Crates/054-CONTEXT.md
@.planning/phases/054-Refactor-Crates/054-SOURCE-AUDIT.md
@crates/z00z_storage/src/settlement/store/mod.rs
@crates/z00z_storage/src/backend/redb/mod.rs
@crates/z00z_storage/src/settlement/store/hjmt_plan.rs
@crates/z00z_storage/src/checkpoint/mod.rs
@crates/z00z_storage/src/serialization/build.rs
@crates/z00z_storage/src/serialization/build/temp_tree.rs
</context>

<coverage_contract>
This plan owns the carry-now storage canonicalization backlog in `054-TODO.md`:
replace active bridge wiring in `settlement/store.rs`,
`settlement/redb_backend.rs`, and `settlement/tx_plan.rs`; remove the duplicate
serialization temp-tree path; and keep checkpoint/snapshot/serialization as
separate surfaces. It also carries the legacy source-shape rules for touched
storage modules, including the whitebox-removal and checkpoint-support rows
that act as explicit cleanup anchors in `054-TODO.md`. When TODO sections
10-15 use nested or target-shape path examples, this plan must normalize them
to the live storage files and canonical nested modules already present in the
repository.
</coverage_contract>

<tasks>
<task type="auto">
<name>Canonicalize active storage bridge modules and remove localized duplicate helpers</name>
<read_first>
- `.planning/phases/054-Refactor-Crates/054-TODO.md` sections 6-17, hard step 12, and section 18 carry-now storage cleanup bullets
- `.planning/phases/054-Refactor-Crates/054-CONTEXT.md` decisions D-12 through D-15
- `crates/z00z_storage/src/settlement/README.md` and `root-types.md` to preserve surface boundaries while cleaning module shape
</read_first>
<files>
- `crates/z00z_storage/src/checkpoint/artifact.rs`
- `crates/z00z_storage/src/checkpoint/build.rs`
- `crates/z00z_storage/src/checkpoint/store.rs`
- `crates/z00z_storage/src/checkpoint/build_prepare.rs`
- `crates/z00z_storage/src/checkpoint/build_state.rs`
- `crates/z00z_storage/src/checkpoint/store_fs.rs`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_storage/src/settlement/redb_backend.rs`
- `crates/z00z_storage/src/settlement/tx_plan.rs`
- `crates/z00z_storage/src/settlement/tx_plan_help.rs`
- `crates/z00z_storage/src/settlement/tx_plan_types.rs`
- `crates/z00z_storage/src/serialization/build.rs`
- `crates/z00z_storage/src/serialization/build/temp_tree.rs`
- `crates/z00z_storage/src/checkpoint/mod.rs`
- `crates/z00z_storage/src/snapshot/mod.rs`
- `crates/z00z_storage/proptest-regressions/settlement/whitebox/whitebox_state.txt`
</files>
<action>
Replace the active `#[path = ...]` bridge wiring in the Phase 054 storage hot
spots with canonical module structure only after the backend seam and planner
split are already stable. Remove the duplicate `serialization/build_temp_tree.rs`
path in favor of one canonical temp-tree helper, and normalize any touched
checkpoint support rows or snapshot module boundaries without collapsing those
surfaces into one generic backup layer. If the TODO-owned whitebox residue is
still dead after the seam cleanup, remove it here rather than carrying an
obsolete parallel test artifact forward. Keep the simulator generated-data
exception explicit and untouched in this slice. Where TODO rows use nested
target-shape names such as `settlement/hjmt/hjmt_*`, `settlement/store/*`,
`settlement/redb_backend/*`, or nested checkpoint and snapshot test paths, bind
execution to the current live storage equivalents instead of creating shadow
folders to match stale text.
</action>
<acceptance_criteria>
- The active storage hot spots used by Phase 054 no longer depend on bridge
  wiring to express their landed topology.
- Only one canonical serialization temp-tree helper remains.
- Any touched checkpoint support rows are normalized without inventing a second
  checkpoint or snapshot layer, and obsolete whitebox residue is either removed
  here or explicitly deferred with justification.
- Checkpoint, snapshot, serialization, and settlement stay separate surface
  areas after cleanup.
</acceptance_criteria>
<verify>
1. If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop, fix the issues, and rerun it before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.
</verify>
<done>
Active storage bridge debt is replaced with canonical modules, and the
duplicate serialization helper path is gone without collapsing storage
boundaries.
</done>
</task>
</tasks>

<threat_model>
- Tampering: canonicalization can silently alter root, proof, or replay
  semantics while looking “mechanical”; the plan requires post-seam-only cleanup
  and preserved surface boundaries.
- Repudiation: if the duplicate temp-tree helper remains, future regressions
  cannot be localized; the plan enforces one canonical helper.
- Elevation of privilege: over-broad cleanup could reach simulator generated
  data or unrelated crate families; scope is restricted to active storage hot
  spots.
</threat_model>

<verification>
Plan execution must produce `054-05-SUMMARY.md` with explicit storage
source-shape evidence. Closeout for this slice also records the legacy
source-shape gates that apply to touched storage modules: zero active
`#[path = ...]` bridge debt in the slice and the broader `fmt` / `clippy` /
`test` / `doc` evidence or any documented external blocker.
</verification>

<success_criteria>
- Phase 054 storage hot spots use canonical modules instead of bridge wiring.
- The duplicate serialization helper path is removed safely.
- Storage surface boundaries remain explicit and separate.
</success_criteria>

<output>
Create `.planning/phases/054-Refactor-Crates/054-05-SUMMARY.md` when done.
</output>
