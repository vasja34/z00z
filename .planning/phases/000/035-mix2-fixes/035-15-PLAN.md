---
phase: 035-mix2-fixes
plan: 15
type: execute
wave: 15
depends_on:
  - 035-14
files_modified:
  - .planning/phases/035-mix2-fixes/035-5-fix-spec.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_wallets/tests/
  - crates/z00z_wallets/tests/test_spec_terms_guard.rs
  - crates/z00z_wallets/tests/test_serial_leaf_ad.rs
  - crates/z00z_wallets/tests/test_tx_drift.rs
  - crates/z00z_core/src/assets/leaf.rs
  - crates/z00z_core/src/assets/version.rs
  - crates/z00z_core/src/assets/mod.rs
autonomous: true
requirements:
  - PH35-STL
must_haves:
  truths:
    - Derivation vector rules are frozen before receive-path enablement.
    - The V2 memo contract is defined explicitly as wallet-private decrypted metadata.
  artifacts:
    - crates/z00z_wallets/tests/test_spec_terms_guard.rs
    - crates/z00z_wallets/tests/test_serial_leaf_ad.rs
    - crates/z00z_wallets/tests/test_tx_drift.rs
    - crates/z00z_core/src/assets/leaf.rs
    - crates/z00z_core/src/assets/version.rs
    - crates/z00z_core/src/assets/mod.rs
    - .planning/phases/035-mix2-fixes/035-5-fix-spec.md
  key_links:
    - Existing wallet test seams must freeze the canonical formula families before the V2 decoded contract and receive-path work proceed; dedicated vector files are justified only if the current files cannot truthfully carry the matrix.
---

# Phase 035 Plan 15

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the second stealth-additions slice for Phase 035 by landing canonical tasks `035-35` through `035-37` in order.

Purpose: freeze derivation vectors and define the V2 memo contract before the receive path is enabled.
Output: derivation-vector regression coverage plus an explicit wallet-private V2 memo contract.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-5-fix-spec.md
@crates/z00z_wallets/tests/test_spec_terms_guard.rs
@crates/z00z_wallets/tests/test_serial_leaf_ad.rs
@crates/z00z_wallets/tests/test_tx_drift.rs
@crates/z00z_core/src/assets/leaf.rs
@crates/z00z_core/src/assets/version.rs
@crates/z00z_core/src/assets/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>035-35 Stealth Derivation Vector Freeze</name>
  <files>crates/z00z_wallets/tests/, crates/z00z_wallets/tests/test_spec_terms_guard.rs, crates/z00z_wallets/tests/test_serial_leaf_ad.rs, crates/z00z_wallets/tests/test_tx_drift.rs, .planning/phases/035-mix2-fixes/035-5-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Stealth derivation vectors are explicit and frozen with fixture-backed coverage for the canonical formula families.
    - Later receive-path work cannot change derivation expectations silently.
  </behavior>
  <action>Use the exact task title above per D-21 and D-22. Reread the `MANDATORY pre-read` block for `035-35` in `035-TODO.md`. Freeze fixture-backed vectors for `derive_k_dh(...)`, `derive_k_dh_with_req(...)`, `derive_s_out(...)`, `compute_owner_tag(...)`, `compute_tag16(...)`, `compute_tag16_with_req(...)`, and `compute_leaf_ad(...)`, reuse the existing fixture style instead of inventing a parallel format, and create the dedicated vector test and fixture files only if the current test files cannot truthfully carry the new matrix.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-35` closes only when the missing derivation surfaces are frozen in a reusable fixture-backed form on the canonical wallet-owned test seam.</done>
</task>

<task type="auto" tdd="true">
  <name>035-36 Derivation Drift Regression Sweep</name>
  <files>crates/z00z_wallets/tests/, crates/z00z_wallets/tests/test_spec_terms_guard.rs, crates/z00z_wallets/tests/test_tx_drift.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Regression tests fail if derivation behavior drifts from the frozen vectors.
    - Receive-path compatibility remains explicit where V1 and V2 differ.
  </behavior>
  <action>Use the exact task title above per D-21 and D-22. Reread the `MANDATORY pre-read` block for `035-36` in `035-TODO.md`. Add negative drift checks where domain strings, argument order, or request-bound versus card-bound contexts are intentionally wrong, prove the different context families do not collapse into one result, and keep drift coverage near the existing parity and drift tests instead of creating a parallel verification harness. If task `035-35` truthfully requires a dedicated vector test file, keep these negative checks adjacent to that file rather than inventing a second verification seam.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-36` closes only when derivation drift is frozen by regression tests on the live wallet receive-path surface.</done>
</task>

<task type="auto" tdd="true">
  <name>035-37 V2 Memo Contract Definition</name>
  <files>crates/z00z_core/src/assets/leaf.rs, crates/z00z_core/src/assets/version.rs, crates/z00z_core/src/assets/mod.rs, .planning/phases/035-mix2-fixes/035-5-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - V2 memo semantics are defined explicitly as wallet-private decrypted metadata.
    - The core asset decoded boundary carries the V2 contract before wallet receive-path enablement proceeds.
  </behavior>
  <action>Use the exact task title above per D-21 and D-22. Reread the `MANDATORY pre-read` block for `035-37` in `035-TODO.md`. Keep V1 stable, add a side-by-side V2 memo-capable decoded payload contract in the core asset types, add strict version-aware decode entrypoints instead of mutating the live V1 path in place, and keep memo encrypted inside `enc_pack`, bounded, optional, and wallet-facing rather than public consensus metadata.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-37` closes only when the V2 memo lane has a concrete decoded contract in the core asset types without destabilizing V1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| asset versioning -> stealth scan path | Version drift can misclassify payloads or widen memo exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-35 | T | derivation vectors | mitigate | Freeze vectors on the live scan path before receive-path enablement |
| T-035-36 | D | derivation regression coverage | mitigate | Add live-surface regression tests that fail on drift immediately |
| T-035-37 | I | V2 memo contract | mitigate | Keep memo semantics wallet-private and explicit in version detection plus tests |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
Stealth derivation is frozen, regression-tested, and paired with an explicit wallet-private V2 memo contract before receive-path enablement begins.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-15-SUMMARY.md`.
</output>