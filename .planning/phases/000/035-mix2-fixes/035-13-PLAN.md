---
phase: 035-mix2-fixes
plan: 13
type: execute
wave: 13
depends_on:
  - 035-12
files_modified:
  - .planning/phases/035-mix2-fixes/035-4-fix-spec.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_wallets/tests/test_s5_misuse_gate.rs
  - crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
  - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
autonomous: true
requirements:
  - PH35-SND
must_haves:
  truths:
    - The sender lane has a completed validation wave before acceptance is claimed.
    - Sender acceptance rests on repository-backed tests, not on documentation or unchecked adapter assumptions.
  artifacts:
    - crates/z00z_wallets/tests/test_s5_misuse_gate.rs
    - crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
    - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
    - .planning/phases/035-mix2-fixes/035-4-fix-spec.md
  key_links:
    - Sender validation and acceptance gates must observe the same canonical helper and validated entrypoint.
---

# Phase 035 Plan 13

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the sender validation slice for Phase 035 by landing canonical tasks `035-30` and `035-31` in order.

Purpose: prove the sender migration is complete before the phase moves into stealth additions.
Output: one explicit sender validation wave and one explicit sender acceptance gate.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-4-fix-spec.md
</context>

<tasks>

<task type="auto">
  <name>035-30 Sender Workflow Validation Wave</name>
  <files>.planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md, crates/z00z_wallets/tests/test_s5_misuse_gate.rs, crates/z00z_simulator/tests/test_stage3_nullifier_store.rs, crates/z00z_simulator/tests/test_claim_tx_pipeline.rs</files>
  <action>Use the exact task title above per D-16 and D-20. Reread the `MANDATORY pre-read` block for `035-30` in `035-TODO.md`. Run the full sender validation wave against the wallet-owned seam, canonical helper, raw path, request-bound validated entrypoint, card-only validated entrypoint, converged adapters, and updated docs. Treat any surviving legacy sender formula, approval-level collapse, or missing regression proof as a blocker.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-30` closes only when the sender lane has repository-backed validation evidence for the canonical seam, raw/request/card approval-level split, downstream adapters, formulas, and docs.</done>
</task>

<task type="auto">
  <name>035-31 Sender Workflow Acceptance Gate</name>
  <files>.planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-16 and D-20. Reread the `MANDATORY pre-read` block for `035-31` in `035-TODO.md`. Close the sender lane only if the validation wave proves the canonical helper is the one formula owner, the raw path and request-bound validated path remain source-backed and unchanged where required, the card-only validated entrypoint exists as a separate wallet-local approval surface, and no adapter, export, or doc surface still claims alternate ownership.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-31` closes only when the sender lane is fully validated and acceptance can be claimed without hidden compatibility drift, documentation drift, or approval-level collapse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sender validation evidence -> stealth lane start | Weak acceptance here can carry stale sender semantics into later work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-30 | T | sender validation wave | mitigate | Require regression evidence across misuse, stage-3, and tx-pipeline seams before acceptance |
| T-035-31 | R | sender acceptance gate | mitigate | Fail closed unless one canonical sender path remains across code, tests, and docs |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The sender lane is closed only after the canonical wallet-owned sender seam is proven across code, tests, and docs.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-13-SUMMARY.md`.
</output>