---
phase: 035-mix2-fixes
plan: 10
type: execute
wave: 10
depends_on:
  - 035-09
files_modified:
  - .planning/phases/035-mix2-fixes/035-4-fix-spec.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/core/stealth/output_build.rs
  - crates/z00z_wallets/src/core/tx/builder.rs
  - crates/z00z_wallets/tests/test_s5_misuse_gate.rs
autonomous: true
requirements:
  - PH35-SND
must_haves:
  truths:
    - Sender output construction remains wallet-owned under the live stealth seam.
    - Raw construction, request-bound validated construction, and the dedicated card-only validated entrypoint remain separate approval-level surfaces before legacy adapters converge.
  artifacts:
    - crates/z00z_wallets/src/core/stealth/output.rs
    - crates/z00z_wallets/src/core/stealth/output_build.rs
    - crates/z00z_wallets/src/core/tx/builder.rs
    - crates/z00z_wallets/tests/test_s5_misuse_gate.rs
  key_links:
    - `output_build.rs` validated entrypoint -> wallet tx builder must replace legacy sender assumptions without introducing a second seam.
---

# Phase 035 Plan 10

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the first sender-workflow slice for Phase 035 by landing canonical tasks `035-22` through `035-24` in order.

Purpose: freeze the sender seam, promote the canonical helper, and make the validated card-only entrypoint the live construction boundary.
Output: a wallet-owned sender-construction seam with an explicit validated entrypoint and misuse coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-4-fix-spec.md
@crates/z00z_wallets/src/core/stealth/output.rs
@crates/z00z_wallets/src/core/stealth/output_build.rs
@crates/z00z_wallets/src/core/tx/builder.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>035-22 Sender Seam Freeze</name>
  <files>crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/core/stealth/output_build.rs, .planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Sender derivation and output construction remain wallet-owned under `core::stealth`.
    - No new adapter-only or `z00z_core::tx` construction seam appears.
  </behavior>
  <action>Use the exact task title above per D-18 through D-20. Reread the `MANDATORY pre-read` block for `035-22` in `035-TODO.md`. Freeze the sender seam on the live `output.rs` and `output_build.rs` boundary, preserve the existing `owner_tag`, `tag16`, `leaf_ad`, `enc_pack`, hedged-`r`, and duplicate-`R` semantics, and reject any move that pushes output construction into a parallel or higher-level adapter layer.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-22` closes only when the live wallet-owned stealth seam is explicit and no alternate sender-construction owner remains implied.</done>
</task>

<task type="auto" tdd="true">
  <name>035-23 Canonical Helper And Approval Extension</name>
  <files>crates/z00z_wallets/src/core/stealth/output_build.rs, crates/z00z_wallets/src/core/tx/builder.rs, .planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - The canonical helper carries request approval semantics without changing the live wire contract.
    - Builder callers are prepared to consume the canonical helper instead of legacy sender shortcuts.
  </behavior>
  <action>Use the exact task title above per D-19 and D-20. Reread the `MANDATORY pre-read` block for `035-23` in `035-TODO.md`. Extend the canonical helper in `output_build.rs` so request validation and approval semantics stay explicit at the sender seam, keep builder integration truthful, and do not widen the task into downstream adapter convergence yet.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-23` closes only when the canonical helper exposes the approved sender checks and builder-facing integration points without semantic drift.</done>
</task>

<task type="auto" tdd="true">
  <name>035-24 Validated Card-Only Entrypoint</name>
  <files>crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/core/stealth/output_build.rs, crates/z00z_wallets/tests/test_s5_misuse_gate.rs, .planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - The validated card-only entrypoint is added as a third public approval surface alongside the existing raw and request-bound validated entrypoints.
    - Misuse coverage proves raw, request-bound validated, and card-only validated construction remain intentionally distinct.
  </behavior>
  <action>Use the exact task title above per D-20. Reread the `MANDATORY pre-read` block for `035-24` in `035-TODO.md`. Land `build_card_stealth_output_validated(...)` on the existing stealth seam, route it through `approve_card(...)`, canonical output construction, and sender self-check, keep the raw builder and request-bound validated entrypoint behavior unchanged at the observable API level, and extend `test_s5_misuse_gate.rs` or equivalent coverage so approval-level separation is frozen by tests rather than documentation alone.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-24` closes only when the dedicated card-only validated entrypoint is live, tested, and clearly distinct from both the raw builder path and the existing request-bound validated entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tx builder -> stealth output seam | Caller pressure can create a second construction owner or bypass strict checks |
| raw sender path -> validated entrypoint | Misuse can look acceptable if strict approval coverage is missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-22 | T | wallet-owned stealth seam | mitigate | Keep sender construction in `core::stealth` and forbid new adapter-only seams |
| T-035-23 | S | approval extension | mitigate | Bind request approval logic to the canonical helper instead of caller discretion |
| T-035-24 | E | validated entrypoint | mitigate | Freeze strict entrypoint semantics with misuse-vs-strict regression tests |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The sender lane now has one explicit wallet-owned construction seam, one explicit canonical helper, and one explicit validated card-only entrypoint.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-10-SUMMARY.md`.
</output>