---
phase: 035-mix2-fixes
plan: 12
type: execute
wave: 12
depends_on:
  - 035-11
files_modified:
  - .planning/phases/035-mix2-fixes/035-4-fix-spec.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - .planning/temp/Z00Z-ECC-SPEC_part1.md
  - .planning/temp/Z00Z-ECC-IDEAS.md
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/core/stealth/output_build.rs
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
  - crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
  - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
autonomous: true
requirements:
  - PH35-SND
must_haves:
  truths:
    - Downstream adapters and simulator-facing callers regress only against the canonical sender seam.
    - Sender documentation and code comments match the live seam instead of historical overclaims.
  artifacts:
    - crates/z00z_simulator/src/scenario_1/stage_3.rs
    - crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
    - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
    - .planning/temp/Z00Z-ECC-SPEC_part1.md
    - .planning/temp/Z00Z-ECC-IDEAS.md
    - crates/z00z_wallets/src/core/stealth/output.rs
    - crates/z00z_wallets/src/core/stealth/output_build.rs
  key_links:
    - Simulator stage-3 sender flow and wallet docs must agree on the same canonical helper and validated entrypoint.
---

# Phase 035 Plan 12

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the third sender-workflow slice for Phase 035 by landing canonical tasks `035-28` and `035-29` in order.

Purpose: prove downstream callers consume the canonical sender seam and correct documentation to match the live implementation.
Output: adapter regression coverage plus truthful sender workflow documentation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-4-fix-spec.md
@crates/z00z_simulator/src/scenario_1/stage_3.rs
@.planning/temp/Z00Z-ECC-SPEC_part1.md
@.planning/temp/Z00Z-ECC-IDEAS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>035-28 Downstream Adapter Regression Sweep</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3.rs, crates/z00z_simulator/tests/test_stage3_nullifier_store.rs, crates/z00z_simulator/tests/test_claim_tx_pipeline.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Downstream simulator-facing sender callers continue to work through the canonical helper and validated entrypoint.
    - Regression tests fail if a caller revives a legacy adapter path.
  </behavior>
  <action>Use the exact task title above per D-18 through D-20. Reread the `MANDATORY pre-read` block for `035-28` in `035-TODO.md`. Sweep the downstream adapter and simulator surfaces that still build sender outputs, move them onto the canonical seam if needed, and tighten regression tests so stage-3 and tx-pipeline callers cannot drift back to legacy construction behavior.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-28` closes only when downstream adapter callers are pinned to the canonical seam and regression coverage catches any legacy reintroduction.</done>
</task>

<task type="auto">
  <name>035-29 Documentation Correction Wave</name>
  <files>.planning/temp/Z00Z-ECC-SPEC_part1.md, .planning/temp/Z00Z-ECC-IDEAS.md, crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/core/stealth/output_build.rs, .planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-17 through D-20. Reread the `MANDATORY pre-read` block for `035-29` in `035-TODO.md`. Rewrite the stale temp planning text and sender-facing code comments so they name the real canonical helper, the raw path, the request-bound validated entrypoint, the card-only validated entrypoint, and the wallet-owned seam without overclaiming broader semantics or preserving stale adapter wording.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-29` closes only when the temp planning docs and sender-facing code comments describe the live sender seam accurately and stop implying obsolete builder or adapter ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downstream adapters -> canonical sender seam | Callers can drift back to legacy formulas if not frozen by tests |
| docs -> live code | Documentation can reintroduce stale ownership claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-28 | T | downstream adapter regression sweep | mitigate | Lock simulator-facing callers and tests to the canonical sender seam |
| T-035-29 | R | documentation correction wave | mitigate | Rewrite docs to match the live helper/entrypoint ownership exactly |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
Downstream sender callers and documentation both describe and exercise the same canonical wallet-owned seam.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-12-SUMMARY.md`.
</output>