---
phase: 035-mix2-fixes
plan: 18
type: execute
wave: 18
depends_on:
  - 035-17
files_modified:
  - .planning/phases/035-mix2-fixes/035-6-renames.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_wallets/src/db/mod.rs
  - crates/z00z_wallets/src/db/wlt_io.rs
  - crates/z00z_wallets/src/db/wlt_store.rs
  - crates/z00z_wallets/src/db/wlt_validate.rs
  - crates/z00z_wallets/src/egui_views/add_wallet_tab_1.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_tests.rs
  - crates/z00z_wallets/src/core/key/bip32_manager.rs
  - crates/z00z_wallets/src/core/key/bip32_manager_tests.inc.rs
autonomous: true
requirements:
  - PH35-RNM
must_haves:
  truths:
    - Wallet DB and egui file renames land before symbol renames that depend on final filenames.
    - Module/path/include mirrors are updated exactly to the new filenames.
  artifacts:
    - crates/z00z_wallets/src/db/mod.rs
    - crates/z00z_wallets/src/db/wlt_io.rs
    - crates/z00z_wallets/src/db/wlt_store.rs
    - crates/z00z_wallets/src/db/wlt_validate.rs
    - crates/z00z_wallets/src/egui_views/add_wallet_tab_1.rs
  key_links:
    - Wallet DB file renames -> mod/path/include mirrors must move in lockstep.
---

# Phase 035 Plan 18

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the second curated rename slice for Phase 035 by landing canonical tasks `035-44` through `035-46` in order.

Purpose: finish the wallet DB and egui file renames, update mirror declarations, and then land the curated declaration renames.
Output: final filenames plus synchronized module/include mirrors and approved symbol names.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-6-renames.md
@crates/z00z_wallets/src/db/mod.rs
@crates/z00z_wallets/src/db/wlt_store.rs
@crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_tests.rs
@crates/z00z_wallets/src/core/key/bip32_manager.rs
</context>

<tasks>

<task type="auto">
  <name>035-44 File Rename Wave B - Wallet DB And Egui Canonical Files</name>
  <files>crates/z00z_wallets/src/db/wlt_io.rs, crates/z00z_wallets/src/db/wlt_store.rs, crates/z00z_wallets/src/db/wlt_validate.rs, crates/z00z_wallets/src/egui_views/add_wallet_tab_1.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-44` in `035-TODO.md`. Rename the curated wallet DB files carrying the `wlt` abbreviation and the egui tab files carrying unexplained `_1` suffixes, keeping this file move wave separate from later type/function rename work.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-44` closes only when the curated wallet DB and egui filenames match the approved canonical names and no row is partially renamed.</done>
</task>

<task type="auto">
  <name>035-45 Signature Rename Wave A - Module, Path, And Include Mirrors</name>
  <files>crates/z00z_wallets/src/db/mod.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_tests.rs, crates/z00z_wallets/src/core/key/bip32_manager.rs, crates/z00z_wallets/src/core/key/bip32_manager_tests.inc.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-45` in `035-TODO.md`. Update every curated `mod`, `#[path]`, and `include!` mirror to the new filenames exactly, with no extra symbol work and no stale filename strings left behind.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-45` closes only when every curated file-to-code mirror resolves to the new canonical filenames and no old mirror string remains live.</done>
</task>

<task type="auto">
  <name>035-46 Signature Rename Wave B - Types, Functions, And Methods</name>
  <files>.planning/phases/035-mix2-fixes/035-6-renames.md, crates/z00z_wallets/src/db/wlt_store.rs, crates/z00z_wallets/src/services/session_service.rs, crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_storage/src/serialization/build_temp_tree.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-46` in `035-TODO.md`. Apply only the curated declaration renames from the recovered table and high-confidence delta, preserve the approved suggested names exactly, and keep repeated split-file helpers aligned on one canonical spelling without inventing shorter variants on the fly.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-46` closes only when curated declaration renames land on the approved spellings and still satisfy the repository word-count rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final filenames -> mirror declarations | Module/include mirrors can break resolution if not updated in lockstep |
| curated names -> declaration renames | Ad hoc shortening can violate the approved rename table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-44 | T | wallet DB and egui file renames | mitigate | Finish final filename moves before any dependent symbol updates |
| T-035-45 | D | mirror declarations | mitigate | Rewrite `mod`, `#[path]`, and `include!` strings exactly to the new filenames |
| T-035-46 | R | declaration rename wave | mitigate | Apply only curated suggested names and recheck the five-word limit after edits |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The rename lane now has final filenames, synchronized mirrors, and curated declaration names ready for the final reference sweep and validation gate.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-18-SUMMARY.md`.
</output>