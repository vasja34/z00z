---
phase: 035-mix2-fixes
plan: 19
type: execute
wave: 19
depends_on:
  - 035-18
files_modified:
  - .planning/phases/035-mix2-fixes/035-a6-renames.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_storage/src/serialization/build_temp_tree.rs
  - crates/z00z_wallets/src/services/session_service.rs
  - crates/z00z_wallets/src/wasm/storage_backend.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_session.rs
  - crates/z00z_wallets/src/services/wallet_service_session_build.rs
autonomous: true
requirements:
  - PH35-RNM
must_haves:
  truths:
    - Cross-file references, imports, and re-exports all point to the curated renamed surfaces.
    - No-change rows stay unchanged even after the full rename sweep.
    - Rename closure is based on validation plus acceptance evidence, not on the raw matrix alone.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-a6-renames.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
    - representative renamed declaration files across simulator, storage, wallet services, wasm, and db session seams
  key_links:
    - Cross-file reference sweep -> validation wave -> acceptance gate must stay sequential and evidence-backed.
---

# Phase 035 Plan 19

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the final curated rename slice for Phase 035 by landing canonical tasks `035-47` through `035-49` in order.

Purpose: finish the cross-file sweep, validate the curated rename set, and close the rename lane only on repository-backed evidence.
Output: one fully rewired curated rename set plus explicit validation and acceptance closure.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-a6-renames.md
</context>

<tasks>

<task type="auto">
  <name>035-47 Cross-File Reference Sweep And No-Change Guard</name>
  <files>crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_storage/src/serialization/build_temp_tree.rs, crates/z00z_wallets/src/services/session_service.rs, crates/z00z_wallets/src/wasm/storage_backend.rs, .planning/phases/035-mix2-fixes/035-a6-renames.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-47` in `035-TODO.md`. Update all curated callsites, imports, re-exports, and references to the new file and declaration names, keep the explicit no-change rows frozen, and perform the sweep by declaration-backed path rather than by naive global text grouping.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-47` closes only when all curated references are rewired, no stale curated old name remains, and explicit no-change rows are still untouched.</done>
</task>

<task type="auto">
  <name>035-48 Rename Validation Wave</name>
  <files>.planning/phases/035-mix2-fixes/035-a6-renames.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-16, D-26, and D-27. Reread the `MANDATORY pre-read` block for `035-48` in `035-TODO.md`. Run the required validation wave for the curated rename set, reconfirm that the append-block approved names still satisfy the five-word limit after live implementation, and prove that only curated rows were executed.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>cargo clippy --all-targets --all-features</automated>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-48` closes only when the curated rename implementation validates cleanly, still satisfies the naming rules, and stays within the approved manifest.</done>
</task>

<task type="auto">
  <name>035-49 Rename Acceptance Gate</name>
  <files>.planning/phases/035-mix2-fixes/035-a6-renames.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-16, D-26, and D-27. Reread the `MANDATORY pre-read` block for `035-49` in `035-TODO.md`. Close the rename lane only when the curated scope, provenance, explicit no-change rows, cross-file sweep, and validation wave all agree. If any remaining old name belongs only to the raw matrix and not to the curated scope, record that distinction explicitly instead of widening execution at acceptance time.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>cargo clippy --all-targets --all-features</automated>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>verify explicit no-change rows remain unchanged and raw-matrix-only residue is not widened into execution scope</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-49` closes only when the rename lane can claim acceptance on curated rows alone, with repository-backed validation and without modifying any explicit no-change row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-file rename sweep -> validation gate | Missed references or over-broad rewrites can break the live tree or violate no-change protections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-47 | T | cross-file reference sweep | mitigate | Rewrite references by curated declaration path and preserve explicit no-change rows |
| T-035-48 | D | rename validation wave | mitigate | Require fmt/clippy/test evidence and recheck the five-word limit on implemented names |
| T-035-49 | R | rename acceptance gate | mitigate | Close only on curated-scope evidence and explicitly distinguish raw-matrix residue from accepted scope |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The curated rename lane closes only after reference rewrites, validation, and acceptance all agree on the same bounded rename scope.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-19-SUMMARY.md`.
</output>