---
phase: 035-mix2-fixes
plan: 3
type: execute
wave: 3
depends_on:
  - 035-02
files_modified:
  - .planning/phases/035-mix2-fixes/035-1-deferred.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
autonomous: true
requirements:
  - PH35-DEF
must_haves:
  truths:
    - Deferred-boundary validation runs after the intake freeze and honesty rules are in place.
    - Optional sidecar acceptance cannot close unless the deferred validation wave is already green.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-1-deferred.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
  key_links:
    - Deferred validation results must gate the optional sidecar acceptance decision.
---

# Phase 035 Plan 03

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the deferred validation slice for Phase 035 by landing canonical tasks `035-06` and `035-07` in order.

Purpose: prove the intake freeze and optional sidecar rules are enforceable before the phase moves into suffix and code lanes.
Output: a completed deferred-consistency validation wave and an explicit sidecar acceptance outcome.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-1-deferred.md
</context>

<tasks>

<task type="auto">
  <name>035-06 Deferred-Consistency Validation Wave</name>
  <files>.planning/phases/035-mix2-fixes/035-1-deferred.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-15 and D-16. Reread the `MANDATORY pre-read` block for `035-06` in `035-TODO.md`. Execute the full deferred-consistency validation wave against the already frozen no-import rules, source bindings, triage locks, sidecar boundaries, and closeout honesty constraints. Record only repository-backed pass or fail evidence and treat unresolved drift as a hard blocker for later lanes.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-06` closes only when every deferred-intake control named in `035-TODO.md` has repository-backed validation evidence and no unresolved drift remains hidden.</done>
</task>

<task type="auto">
  <name>035-07 Optional Sidecar Validation Gate</name>
  <files>.planning/phases/035-mix2-fixes/035-1-deferred.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-08, D-09, and D-24. Reread the `MANDATORY pre-read` block for `035-07` in `035-TODO.md`. Close the optional sidecar gate only after `035-06` is green. If `keep_path(...)` was activated, prove it stayed within the opportunistic housekeeping boundary; if it was not activated, prove the phase still closes without it. Do not let the sidecar gate rewrite sender or stealth scope.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-07` closes only when the optional sidecar outcome is explicit, validated, and unable to masquerade as semantic Phase 035 completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deferred validation evidence -> later lanes | Weak or missing validation can let unrelated work proceed on a false baseline |
| sidecar outcome -> phase closure | Optional housekeeping can be promoted into acceptance criteria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-07 | R | deferred validation wave | mitigate | Require repository-backed evidence for every intake control before later lane execution |
| T-035-08 | T | sidecar acceptance gate | mitigate | Make sidecar closure contingent on the completed deferred validation wave and explicit scoping proof |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
Deferred intake, optional housekeeping, and closeout honesty are fully validated before Phase 035 proceeds into suffix or code lanes.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-03-SUMMARY.md`.
</output>