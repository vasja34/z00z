---
phase: 035-mix2-fixes
plan: 2
type: execute
wave: 2
depends_on:
  - 035-01
files_modified:
  - .planning/phases/035-mix2-fixes/035-1-deferred.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
autonomous: true
requirements:
  - PH35-DEF
must_haves:
  truths:
    - Historical deferred triage stays locked to repository-backed relevance only.
    - The optional `keep_path(...)` sidecar remains optional and cannot masquerade as semantic Phase 035 closure.
    - Closeout wording cannot overclaim beyond the approved deferred boundary.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-1-deferred.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
  key_links:
    - `keep_path(...)` sidecar state must never bypass the explicit Phase 035 scope fence.
---

# Phase 035 Plan 02

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the second deferred-intake slice for Phase 035 by landing canonical tasks `035-03` through `035-05` in order.

Purpose: lock historical triage, isolate the optional storage sidecar, and freeze closeout honesty before validation begins.
Output: deferred-boundary docs and sidecar rules that cannot drift into hidden implementation scope.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-1-deferred.md
@crates/z00z_storage/src/assets/store_internal/store_query.rs
</context>

<tasks>

<task type="auto">
  <name>035-03 Historical Triage Lock-In</name>
  <files>.planning/phases/035-mix2-fixes/035-1-deferred.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-07 through D-10. Reread the `MANDATORY pre-read` block for `035-03` in `035-TODO.md`. Lock historical triage to the repository-backed categories already approved in `035-1-deferred.md`, keep resolved or stale backlog items outside the active execution lane, and document any exceptional carry-forward only if the source documents themselves are updated first.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-03` closes only when stale, resolved, and unrelated deferred items are explicitly non-authoritative for Phase 035 execution.</done>
</task>

<task type="auto">
  <name>035-04 Optional `keep_path(...)` Sidecar Gate</name>
  <files>.planning/phases/035-mix2-fixes/035-1-deferred.md, .planning/phases/035-mix2-fixes/035-TODO.md, crates/z00z_storage/src/assets/store_internal/store_query.rs</files>
  <action>Use the exact task title above per D-11, D-12, D-24, and D-25. Reread the `MANDATORY pre-read` block for `035-04` in `035-TODO.md`. Treat `keep_path(...)` cleanup as opportunistic housekeeping only, keep it outside sender and stealth semantic closure, and touch `store_query.rs` only if the sidecar is explicitly activated under the rules already frozen in the deferred lane.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-04` closes only when `keep_path(...)` is either explicitly scoped as a sidecar or explicitly left untouched without any semantic closure overclaim.</done>
</task>

<task type="auto">
  <name>035-05 Phase Closeout Honesty Rules</name>
  <files>.planning/phases/035-mix2-fixes/035-1-deferred.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-08, D-09, and D-16. Reread the `MANDATORY pre-read` block for `035-05` in `035-TODO.md`. Freeze closeout language so validation waves, acceptance gates, and final summaries can claim only what the repository-backed deferred boundary actually proves. Do not allow sidecar cleanup, stale notes, or speculative closure language to leak into Phase 035 completion conditions.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-05` closes only when Phase 035 closeout wording is constrained to repository-backed proof and explicitly fenced from unrelated leftovers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical triage notes -> execution backlog | Stale notes can be promoted into live work without authority |
| optional sidecar -> semantic closure | `keep_path(...)` cleanup can be misrepresented as sender/stealth completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-04 | T | historical triage lock | mitigate | Restrict carry-forward to repository-backed rows explicitly approved by `035-1-deferred.md` |
| T-035-05 | E | `keep_path(...)` sidecar | mitigate | Require explicit activation and forbid semantic Phase 035 closure claims from the sidecar |
| T-035-06 | R | closeout wording | mitigate | Freeze honest closeout language before validation and acceptance gates run |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
Historical triage, optional sidecar handling, and closeout honesty are all frozen before Phase 035 enters its deferred validation wave.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-02-SUMMARY.md`.
</output>