---
phase: 035-mix2-fixes
plan: 5
type: execute
wave: 5
depends_on:
  - 035-04
files_modified:
  - .planning/phases/035-mix2-fixes/035-2-suffixes.md
  - .planning/phases/035-mix2-fixes/035-6-renames.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
autonomous: true
requirements:
  - PH35-SFX
must_haves:
  truths:
    - Production-head cleanup scope is constrained to the curated suffix inventory.
    - Filename hygiene and exclusions are explicit before rename handoff begins.
    - Curated rename and retirement handoff is derived from suffix authority instead of raw inventory drift.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-2-suffixes.md
    - .planning/phases/035-mix2-fixes/035-6-renames.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
  key_links:
    - Suffix cleanup targets must feed the curated rename lane without widening execution scope.
---

# Phase 035 Plan 05

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the second suffix-inventory slice for Phase 035 by landing canonical tasks `035-10` through `035-12` in order.

Purpose: define the actual cleanup target, freeze filename/exclusion hygiene, and hand the approved residue into the rename lane without widening scope.
Output: curated suffix cleanup targets and a bounded rename/retirement handoff.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-2-suffixes.md
@.planning/phases/035-mix2-fixes/035-6-renames.md
</context>

<tasks>

<task type="auto">
  <name>035-10 Production-Head Cleanup Target</name>
  <files>.planning/phases/035-mix2-fixes/035-2-suffixes.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-24 through D-27. Reread the `MANDATORY pre-read` block for `035-10` in `035-TODO.md`. Convert the curated suffix inventory into the bounded production-head cleanup target without turning compatibility rows, vendor rows, or no-change rows into implicit rename work. Keep the production-head interpretation exactly aligned with `035-2-suffixes.md`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-10` closes only when the production-head cleanup target is explicit, curated, and fenced from compatibility or excluded rows.</done>
</task>

<task type="auto">
  <name>035-11 Filename And Exclusion Hygiene</name>
  <files>.planning/phases/035-mix2-fixes/035-2-suffixes.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-11` in `035-TODO.md`. Freeze filename hygiene, path exclusions, and any explicit non-goals so later rename work only acts on approved rows and does not blur filename cleanup with declaration or facade retirement.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-11` closes only when exclusions, file-name expectations, and protected rows are explicit enough to drive later rename waves deterministically.</done>
</task>

<task type="auto">
  <name>035-12 Curated Rename And Retirement Handoff</name>
  <files>.planning/phases/035-mix2-fixes/035-2-suffixes.md, .planning/phases/035-mix2-fixes/035-6-renames.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-12` in `035-TODO.md`. Handoff only the curated suffix residue that is approved for rename or retirement into `035-6-renames.md`, preserve path-backed provenance, and do not promote the raw matrix or excluded suffix rows into execution authority.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-12` closes only when the rename lane receives a curated, declaration-backed handoff and no suffix residue is widened silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suffix inventory -> cleanup target | Production-head scope can be widened beyond the curated suffix authority |
| suffix cleanup -> rename handoff | Raw inventory or excluded rows can slip into the curated rename lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-11 | T | production-head cleanup target | mitigate | Bind cleanup scope only to curated suffix rows with explicit exclusions |
| T-035-12 | E | rename handoff | mitigate | Feed only curated suffix residue into `035-6-renames.md` and keep raw inventory non-authoritative |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The suffix lane ends with explicit cleanup scope, explicit exclusions, and a bounded handoff into the rename lane.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-05-SUMMARY.md`.
</output>