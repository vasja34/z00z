---
phase: 035-mix2-fixes
plan: 16
type: execute
wave: 16
depends_on:
  - 035-15
files_modified:
  - .planning/phases/035-mix2-fixes/035-a5-fix-spec.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_wallets/src/core/address/stealth_scan_support.rs
  - crates/z00z_wallets/src/core/chain/receiver_card_record.rs
  - crates/z00z_wallets/src/core/address/stealth_scanner.rs
  - crates/z00z_wallets/tests/test_s5_record_gate.rs
autonomous: true
requirements:
  - PH35-STL
must_haves:
  truths:
    - The V2 memo receive path is enabled only on the approved live seam.
    - Stealth additions do not claim closure until validation and acceptance gates are green.
  artifacts:
    - crates/z00z_wallets/src/core/address/stealth_scan_support.rs
    - crates/z00z_wallets/src/core/chain/receiver_card_record.rs
    - crates/z00z_wallets/src/core/address/stealth_scanner.rs
    - crates/z00z_wallets/tests/test_s5_record_gate.rs
  key_links:
    - V2 memo receive-path logic and receiver-card record handling must stay aligned on the same live seam.
---

# Phase 035 Plan 16

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the final stealth-additions slice for Phase 035 by landing canonical tasks `035-38` through `035-40` in order.

Purpose: enable the V2 memo receive path and close the stealth lane only after validation-backed acceptance.
Output: live V2 receive-path behavior plus explicit stealth validation and acceptance closure.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-a5-fix-spec.md
@crates/z00z_wallets/src/core/address/stealth_scan_support.rs
@crates/z00z_wallets/src/core/chain/receiver_card_record.rs
@crates/z00z_wallets/src/core/address/stealth_scanner.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>035-38 V2 Memo Receive-Path Enablement</name>
  <files>crates/z00z_wallets/src/core/address/stealth_scan_support.rs, crates/z00z_wallets/src/core/address/stealth_scanner.rs, crates/z00z_wallets/src/core/chain/receiver_card_record.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - The receive path recognizes and processes the approved V2 memo contract.
    - V1 compatibility remains explicit rather than implicit.
  </behavior>
  <action>Use the exact task title above per D-21 and D-22. Reread the `MANDATORY pre-read` block for `035-38` in `035-TODO.md`. Replace the current automatic `V2Memo` rejection path once the canonical decoder is complete, wire wallet receive support on `stealth_scan_support.rs`, `stealth_scanner.rs`, and `receiver_card_record.rs` so V2 memo stays private decrypted metadata and is not copied into public leaf fields, and keep malformed or oversized memo payloads fail-closed.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-38` closes only when the V2 memo receive path works on the live seam and keeps V1 behavior explicit and intact.</done>
</task>

<task type="auto">
  <name>035-39 Stealth Additions Validation Wave</name>
  <files>.planning/phases/035-mix2-fixes/035-a5-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md, crates/z00z_wallets/tests/, crates/z00z_core/src/assets/</files>
  <action>Use the exact task title above per D-16 and D-21. Reread the `MANDATORY pre-read` block for `035-39` in `035-TODO.md`. Run the full stealth additions validation wave over receiver-secret narrowing, derivation freeze, V2 memo contract, and receive-path enablement. Treat any unexpected widening of secret exposure or memo semantics as a blocker. Keep validation aligned with the broad wallet-test and core-asset surfaces named in `035-TODO.md`, and treat any dedicated V2 memo regression test as a proposed target only if task `035-38` or the live implementation actually creates it.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>if V2 memo receive support touches cross-crate behavior, run a targeted named test binary rather than a plain filter to avoid false-green `running 0 tests` output</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-39` closes only when the stealth lane has repository-backed validation evidence for every approved addition and no out-of-scope expansion remains hidden.</done>
</task>

<task type="auto">
  <name>035-40 Stealth Additions Acceptance Gate</name>
  <files>.planning/phases/035-mix2-fixes/035-a5-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md, crates/z00z_wallets/src/core/chain/receiver_card_record.rs, crates/z00z_wallets/tests/test_s5_record_gate.rs</files>
  <action>Use the exact task title above per D-16 and D-21 through D-23. Reread the `MANDATORY pre-read` block for `035-40` in `035-TODO.md`. Close the stealth lane only if the validation wave proves the approved additions are complete on the live seam, the out-of-scope list remains intact, and no broader memo, routing, or backend semantics were smuggled into acceptance.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-40` closes only when the stealth lane can claim acceptance on the approved additions alone and without hidden scope widening.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| V2 memo receive path -> wallet runtime | Memo support can widen secret exposure or version semantics unexpectedly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-38 | I | V2 memo receive-path enablement | mitigate | Implement only on the approved live seam with explicit V1 compatibility behavior |
| T-035-39 | T | stealth additions validation wave | mitigate | Validate every approved addition and treat widening as a blocker |
| T-035-40 | R | stealth acceptance gate | mitigate | Fail closed unless only the approved additions are present at acceptance time |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The stealth lane closes only after approved additions are enabled, validated, and accepted on the live seam without scope widening.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-16-SUMMARY.md`.
</output>