---
phase: 035-mix2-fixes
plan: 4
type: execute
wave: 4
depends_on:
  - 035-03
files_modified:
  - .planning/phases/035-mix2-fixes/035-2-suffixes.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
autonomous: true
requirements:
  - PH35-SFX
must_haves:
  truths:
    - Suffix inventory authority is frozen before any cleanup or rename handoff starts.
    - The declaration-backed inventory is the only allowed basis for production-head cleanup scope.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-2-suffixes.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
  key_links:
    - Curated suffix inventory -> production-head interpretation must remain one-to-one.
---

# Phase 035 Plan 04

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the first suffix-inventory slice for Phase 035 by landing canonical tasks `035-08` and `035-09` in order.

Purpose: freeze suffix authority and lock the declaration-backed inventory before any cleanup or rename handoff occurs.
Output: one canonical suffix inventory authority surface with explicit production-head interpretation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-2-suffixes.md
</context>

<tasks>

<task type="auto">
  <name>035-08 Suffix Authority Freeze</name>
  <files>.planning/phases/035-mix2-fixes/035-2-suffixes.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-13, D-14, D-26, and D-27. Reread the `MANDATORY pre-read` block for `035-08` in `035-TODO.md`. Freeze the suffix lane so `035-2-suffixes.md` remains the only authority for suffix inventory, production-head interpretation, exclusions, and cleanup guidance. Do not let raw workspace grep output replace the curated lane authority.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-08` closes only when the suffix lane has one explicit authority surface and no raw-inventory fallback remains implied.</done>
</task>

<task type="auto">
  <name>035-09 Declaration-Backed Inventory Lock-In</name>
  <files>.planning/phases/035-mix2-fixes/035-2-suffixes.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-09` in `035-TODO.md`. Lock the suffix inventory to declaration-backed and path-backed rows only, preserve the curated exclusions, and keep repeated or split symbols path-qualified so later cleanup and rename waves cannot merge them incorrectly.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-09` closes only when every active suffix row is declaration-backed, path-qualified where needed, and bounded by the curated exclusions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated suffix inventory -> cleanup scope | Uncurated rows can be promoted into active cleanup work |
| path-backed declaration set -> rename handoff | Split or repeated symbols can be merged incorrectly if path identity is lost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-09 | T | suffix authority surface | mitigate | Freeze `035-2-suffixes.md` as the sole cleanup authority before implementation |
| T-035-10 | D | declaration-backed inventory | mitigate | Require live declaration/path proof for every row before it can enter cleanup or rename work |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The suffix lane now has a frozen authority surface and a declaration-backed inventory that later cleanup tasks cannot widen accidentally.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-04-SUMMARY.md`.
</output>