---
phase: 035-mix2-fixes
plan: 17
type: execute
wave: 17
depends_on:
  - 035-16
files_modified:
  - .planning/phases/035-mix2-fixes/035-a6-renames.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_core/src/assets/asset_tests.rs
  - crates/z00z_core/src/genesis/genesis_tests.rs
  - crates/z00z_crypto/src/aead_tests.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_tests.rs
  - crates/z00z_storage/src/checkpoint/artifact_tests.rs
  - crates/z00z_utils/src/io/fs_tests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_tests.rs
autonomous: true
requirements:
  - PH35-RNM
must_haves:
  truths:
    - Rename scope is frozen to the curated manifest and high-confidence delta only.
    - Test/support file renames run first and remain separate from later signature renames.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-a6-renames.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
    - representative curated test/support files across core, crypto, simulator, storage, utils, and wallets
  key_links:
    - Curated rename manifest -> file rename wave A must remain one-way and file-first.
---

# Phase 035 Plan 17

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the first curated rename slice for Phase 035 by landing canonical tasks `035-41` through `035-43` in order.

Purpose: freeze rename scope, split the manifest into executable lanes, and finish the file-first test/support rename wave before symbol changes begin.
Output: one frozen rename authority surface plus completed file rename wave A.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-a6-renames.md
</context>

<tasks>

<task type="auto">
  <name>035-41 Rename Scope Freeze</name>
  <files>.planning/phases/035-mix2-fixes/035-a6-renames.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-41` in `035-TODO.md`. Freeze rename authority to the recovered curated table plus high-confidence delta, preserve provenance and explicit no-change rows, and keep the raw 814-row matrix inventory-only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-41` closes only when rename authority is frozen to curated rows and raw inventory cannot be treated as direct execution scope.</done>
</task>

<task type="auto">
  <name>035-42 Live Rename Manifest And Lane Split</name>
  <files>.planning/phases/035-mix2-fixes/035-a6-renames.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-42` in `035-TODO.md`. Re-resolve every curated rename against the live tree, split file renames from signature renames, and mark path/module/include mirrors that must move together so later waves stay deterministic.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-42` closes only when the live rename manifest is declaration-backed, path-qualified, and split into file-first and signature-after lanes.</done>
</task>

<task type="auto">
  <name>035-43 File Rename Wave A - Test And Support Files</name>
  <files>crates/z00z_core/src/assets/asset_tests.rs, crates/z00z_core/src/genesis/genesis_tests.rs, crates/z00z_crypto/src/aead_tests.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_tests.rs, crates/z00z_storage/src/checkpoint/artifact_tests.rs, crates/z00z_utils/src/io/fs_tests.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_tests.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-26 and D-27. Reread the `MANDATORY pre-read` block for `035-43` in `035-TODO.md`. Execute the curated test/support file rename wave across the approved rows only, keep crate/path ordering deterministic, and finish every file move in this wave before any symbol-level rename wave starts.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-43` closes only when the curated test/support file rows have been renamed and the filesystem is ready for later mirror and signature updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated rename manifest -> file rename execution | Raw inventory or no-change rows can be promoted into active rename work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-41 | T | rename scope freeze | mitigate | Freeze scope to curated rows and preserve explicit no-change protections |
| T-035-42 | R | manifest lane split | mitigate | Re-resolve rows against the live tree and split file vs signature waves clearly |
| T-035-43 | D | file rename wave A | mitigate | Rename only curated test/support files and finish file moves before symbol edits |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The rename lane starts from a frozen curated scope and finishes the file-first test/support rename wave before any mirror or symbol rewrite begins.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-17-SUMMARY.md`.
</output>