---
phase: 035-mix2-fixes
plan: 8
type: execute
wave: 8
depends_on:
  - 035-07
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/035-mix2-fixes/035-3-garbage-filter.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - .planning/phases/035-mix2-fixes/035-08-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PH35-GBG
must_haves:
  truths:
    - Debug-dump retirement cannot over-delete live debug or migration seams.
    - Compatibility and migration keep-set rules remain explicit after hard-garbage removal.
    - Current-path review is advisory only until the canonical filter says a row can move.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-3-garbage-filter.md
    - crates/z00z_wallets/src/db/redb_wallet_store_debug_export.rs
    - crates/z00z_simulator/src/scenario_1/runner_verify.rs
    - crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs
    - crates/z00z_crypto/src/claim/statement.rs
    - crates/z00z_crypto/src/claim/proof.rs
    - crates/z00z_crypto/src/claim/v2.rs
    - crates/z00z_storage/src/assets/store_internal/redb_backend_state.rs
    - crates/z00z_wallets/src/core/backup/backup_wire.rs
    - crates/z00z_wallets/src/core/backup/wallet_backup.rs
    - crates/z00z_wallets/src/core/key/seed_cipher_params.rs
    - crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
  key_links:
    - Current-path-only review must not override the compatibility keep-set.
---

# Phase 035 Plan 08

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the second garbage-filter slice for Phase 035 by landing canonical tasks `035-17` through `035-19` in order.

Purpose: review debug-dump retirement, lock the compatibility keep-set, and keep current-path review subordinate to the canonical filter.
Output: a bounded post-removal review of debug, compatibility, and current-path semantics.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-3-garbage-filter.md
@crates/z00z_wallets/src/db/redb_wallet_store_debug_export.rs
@crates/z00z_simulator/src/scenario_1/runner_verify.rs
@crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs
@crates/z00z_crypto/src/claim/statement.rs
@crates/z00z_crypto/src/claim/proof.rs
@crates/z00z_crypto/src/claim/v2.rs
@crates/z00z_storage/src/assets/store_internal/redb_backend_state.rs
@crates/z00z_wallets/src/core/backup/backup_wire.rs
@crates/z00z_wallets/src/core/backup/wallet_backup.rs
@crates/z00z_wallets/src/core/key/seed_cipher_params.rs
@crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
</context>

<tasks>

<task type="auto">
  <name>035-17 Debug-Dump Retirement Review</name>
  <files>.planning/phases/035-mix2-fixes/035-3-garbage-filter.md, .planning/phases/035-mix2-fixes/035-TODO.md, crates/z00z_wallets/src/db/redb_wallet_store_debug_export.rs, crates/z00z_simulator/src/scenario_1/runner_verify.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs</files>
  <action>Use the exact task title above per D-24 and D-25. Reread the `MANDATORY pre-read` block for `035-17` in `035-TODO.md`. Review the debug-dump retirement slice only against the explicit garbage-filter rows, preserve any still-approved debug or migration surfaces, and keep the review tied to current live code rather than stale audit assumptions.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-17` closes only when the debug-dump slice is reviewed against the canonical garbage matrix and no live seam is retired by guesswork.</done>
</task>

<task type="auto">
  <name>035-18 Compatibility And Migration Keep-Set Freeze</name>
  <files>.planning/phases/035-mix2-fixes/035-3-garbage-filter.md, .planning/phases/035-mix2-fixes/035-TODO.md, crates/z00z_crypto/src/claim/statement.rs, crates/z00z_crypto/src/claim/proof.rs, crates/z00z_crypto/src/claim/v2.rs, crates/z00z_storage/src/assets/store_internal/redb_backend_state.rs, crates/z00z_wallets/src/core/backup/backup_wire.rs, crates/z00z_wallets/src/core/backup/wallet_backup.rs, crates/z00z_wallets/src/core/key/seed_cipher_params.rs, crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs</files>
  <action>Use the exact task title above per D-24 and D-25. Reread the `MANDATORY pre-read` block for `035-18` in `035-TODO.md`. Freeze the compatibility and migration keep-set exactly as classified in `035-3-garbage-filter.md`, with explicit protection for rows that remain live because of backup, storage, or wallet compatibility behavior. Do not reinterpret current-path usage as deletion authority.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-18` closes only when the compatibility keep-set is explicit, repository-backed, and insulated from current-path overreach.</done>
</task>

<task type="auto">
  <name>035-19 Current-Path-Only Source Drift Handoff</name>
  <files>.planning/phases/035-mix2-fixes/035-3-garbage-filter.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-24 and D-25. Reread the `MANDATORY pre-read` block for `035-19` in `035-TODO.md`. Keep current-path-only analysis as a source-drift review aid, not deletion authority. If a live-path check later differs from the canonical filter, update the canonical filter first; do not let this task silently become a second garbage authority. Optional sidecar checks such as `store_query.rs::keep_path(...)` stay outside the default Plan 08 closeout surface unless the canonical filter explicitly reopens them.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-19` closes only when current-path analysis is explicitly subordinate to the canonical garbage filter and cannot drive deletions on its own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| current-path review -> keep-set decisions | Advisory analysis can be mistaken for deletion authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-17 | T | debug-dump retirement review | mitigate | Review only rows explicitly classified in the garbage filter and preserve live debug/migration seams |
| T-035-18 | E | compatibility keep-set | mitigate | Freeze compatibility and migration rows explicitly before any current-path review influences them |
| T-035-19 | R | current-path drift handoff | mitigate | Treat current-path analysis as advisory only and require canonical-filter updates before action |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The garbage lane now distinguishes hard removal, compatibility keep-set, and advisory current-path review without allowing one to impersonate the others.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-08-SUMMARY.md`.
</output>