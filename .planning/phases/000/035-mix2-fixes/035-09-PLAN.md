---
phase: 035-mix2-fixes
plan: 9
type: execute
wave: 9
depends_on:
  - 035-08
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/035-mix2-fixes/035-09-SUMMARY.md
  - .planning/phases/035-mix2-fixes/035-3-garbage-filter.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
autonomous: true
requirements:
  - PH35-GBG
must_haves:
  truths:
    - The garbage lane has a completed validation wave before closure is claimed.
    - Current-path closure depends on the validated garbage filter, not on informal review alone.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-3-garbage-filter.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
  key_links:
    - Garbage validation must gate the lane closeout and any source-drift conclusions.
---

# Phase 035 Plan 09

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the garbage validation slice for Phase 035 by landing canonical tasks `035-20` and `035-21` in order.

Purpose: prove that the garbage lane is consistent before it claims closure or feeds later lanes.
Output: one explicit garbage validation wave and one explicit current-path closure decision.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-3-garbage-filter.md
</context>

<tasks>

<task type="auto">
  <name>035-20 Garbage-Filter Validation Wave</name>
  <files>.planning/phases/035-mix2-fixes/035-3-garbage-filter.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-16, D-24, and D-25. Reread the `MANDATORY pre-read` block for `035-20` in `035-TODO.md`. Run the full garbage-filter validation wave across hard removals, debug-dump review, compatibility keep-set rules, and current-path advisory logic. Treat any mismatch between the canonical filter and live code evidence as a blocker until the canonical filter is corrected.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-20` closes only when the garbage lane has repository-backed validation evidence for every removal, keep-set, and advisory rule it claims.</done>
</task>

<task type="auto">
  <name>035-21 Current-Path Closure Gate</name>
  <files>.planning/phases/035-mix2-fixes/035-3-garbage-filter.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-16, D-24, and D-25. Reread the `MANDATORY pre-read` block for `035-21` in `035-TODO.md`. Close the garbage lane only if the validation wave proves the current-path interpretation stayed advisory, the compatibility keep-set stayed protected, and the removed rows stayed within the explicit garbage classification. Record the exact blocker if any of those conditions fail.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-21` closes only when the garbage lane is validated, current-path review is still subordinate to the canonical filter, and no compatibility-live seam was retired by drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| garbage validation -> later lanes | Incorrect closure here can hide over-deletion or false keep-set claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-20 | T | garbage validation wave | mitigate | Require explicit evidence for every removal, keep-set, and advisory rule before closure |
| T-035-21 | R | current-path closure gate | mitigate | Fail closed unless advisory current-path review, protected keep-set, and bounded removals all remain true |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The garbage lane is closed only on validated, repository-backed classification and without current-path overreach.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-09-SUMMARY.md`.
</output>