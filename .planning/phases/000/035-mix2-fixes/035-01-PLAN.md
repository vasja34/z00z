---
phase: 035-mix2-fixes
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/035-mix2-fixes/035-01-PLAN.md
  - .planning/phases/035-mix2-fixes/035-01-SUMMARY.md
  - .planning/phases/035-mix2-fixes/035-1-deferred.md
  - .planning/phases/035-mix2-fixes/035-CONTEXT.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PH35-DEF
must_haves:
  truths:
    - Deferred intake is frozen to the canonical Phase 035 source set with no inherited historical backlog.
    - The canonical `035-TODO.md` inventory plus the live six-document source mapping are treated as the only planning and execution authority.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-1-deferred.md
    - .planning/phases/035-mix2-fixes/035-CONTEXT.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
  key_links:
    - 035-1-deferred.md -> 035-TODO.md task ranges must stay one-to-one and in order.
---

# Phase 035 Plan 01

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the first deferred-intake slice for Phase 035 by landing canonical tasks `035-01` and `035-02` in order.

Purpose: freeze the authority boundary before any lane-specific execution starts.
Output: a no-drift deferred intake baseline plus an explicit live source binding for the full phase.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-1-deferred.md
</context>

<tasks>

<task type="auto">
  <name>035-01 Canonical Deferred-Intake Freeze</name>
  <files>.planning/phases/035-mix2-fixes/035-1-deferred.md, .planning/phases/035-mix2-fixes/035-TODO.md, .planning/phases/035-mix2-fixes/035-CONTEXT.md, .planning/STATE.md</files>
  <action>Use the exact task title above per D-01 through D-05. Before editing, reread the `MANDATORY pre-read` block for `035-01` in `035-TODO.md`: section `Purpose`, section `Intake Decision`, and section `Final Recommendation` in `035-1-deferred.md`. Freeze deferred intake so Phase 035 inherits no historical deferred work by default, keeps the six phase-local sources as the only approved execution authority, and records any non-bypassable blocker explicitly instead of silently widening scope. Preserve the existing task wording and the task-to-source range mapping from D-14.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>review `035-TODO.md` to confirm the execution rules, dependency chain, and completion gate do not claim any required inherited deferred item</automated>
    <automated>verify no numbered task in `035-TODO.md` requires reopening older `deferred-items.md` artifacts</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-01` closes only when deferred intake is fail-closed, no historical ledger is imported implicitly, and the no-import rule is repository-backed in the phase docs.</done>
</task>

<task type="auto">
  <name>035-02 Live Phase-Source Binding</name>
  <files>.planning/phases/035-mix2-fixes/035-1-deferred.md, .planning/phases/035-mix2-fixes/035-CONTEXT.md, .planning/phases/035-mix2-fixes/035-TODO.md, .planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-5-fix-spec.md</files>
  <action>Use the exact task title above per D-03, D-12, D-13, and D-14. Before editing, reread the `MANDATORY pre-read` block for `035-02` in `035-TODO.md`: section `Recommended Phase 035 Scope Boundary`, section `Why Phase 035 Should Stay Clean`, and section `Final Recommendation` in `035-1-deferred.md`; then reread the `MANDATORY supporting pre-read`: `035-4-fix-spec.md` sections `Objective`, `Verified Gaps To Fix`, and `Target Architecture`, plus `035-5-fix-spec.md` sections `Objective`, `Verified short additions worth doing now`, and `Verified non-short additions`. Bind the live Phase 035 surface to `035-TODO.md` plus the six mapped source documents, keep the source-range ownership explicit, and reject any alternate backlog, second taxonomy, or planner-invented implementation layer. Carry forward every task-local control named in `035-TODO.md` without weakening or paraphrasing it.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>review all non-optional task headings in `035-TODO.md` to confirm they map either to one of the six canonical Phase 035 source documents or, for substantive implementation tasks, to sender canonicalization or stealth additions from the live fix specs</automated>
    <automated>verify no non-optional Phase 035 task cites a historical deferred source as its implementation authority</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-02` closes only when the six-document source set, the fixed task ranges, and the canonical TODO inventory are all explicitly bound together with no competing source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical deferred notes -> Phase 035 scope | Old blockers can be reintroduced as hidden work if intake is not frozen |
| phase-local source set -> TODO inventory | Source drift can silently rewrite task meaning or order |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-01 | T | deferred intake boundary | mitigate | Freeze no-import semantics in `035-1-deferred.md`, `035-CONTEXT.md`, and `035-TODO.md` before lane work starts |
| T-035-02 | S | alternate planning inventory | mitigate | Bind planning authority to the mapped six-source set plus canonical TODO inventory only |
| T-035-03 | R | phase scope claims | mitigate | Require explicit blocker reporting instead of silent scope weakening |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
Phase 035 starts from one explicit deferred-intake authority surface and one explicit live source map, with no inherited backlog drift.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-01-SUMMARY.md`.
</output>