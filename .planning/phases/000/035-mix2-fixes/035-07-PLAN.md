---
phase: 035-mix2-fixes
plan: 7
type: execute
wave: 7
depends_on:
  - 035-06
files_modified:
  - .planning/phases/035-mix2-fixes/035-3-garbage-filter.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_wallets/src/db/redb_wallet_store_debug_export.rs
  - crates/z00z_crypto/src/claim/statement.rs
autonomous: true
requirements:
  - PH35-GBG
must_haves:
  truths:
    - Garbage classification is frozen before any deletion or retirement work starts.
    - Hard-garbage removal acts only on rows explicitly classified as removable.
  artifacts:
    - .planning/phases/035-mix2-fixes/035-3-garbage-filter.md
    - .planning/phases/035-mix2-fixes/035-TODO.md
    - crates/z00z_wallets/src/db/redb_wallet_store_debug_export.rs
    - crates/z00z_crypto/src/claim/statement.rs
  key_links:
    - Garbage filter classifications must drive code removal; they cannot be inferred ad hoc from current usage only.
---

# Phase 035 Plan 07

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the first garbage-filter slice for Phase 035 by landing canonical tasks `035-15` and `035-16` in order.

Purpose: freeze the garbage classification surface and remove only the rows already classified as hard garbage.
Output: one explicit garbage authority surface plus a bounded hard-removal cluster.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-3-garbage-filter.md
@crates/z00z_wallets/src/db/redb_wallet_store_debug_export.rs
@crates/z00z_crypto/src/claim/statement.rs
</context>

<tasks>

<task type="auto">
  <name>035-15 Garbage Classification Freeze</name>
  <files>.planning/phases/035-mix2-fixes/035-3-garbage-filter.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <action>Use the exact task title above per D-24 and D-25. Reread the `MANDATORY pre-read` block for `035-15` in `035-TODO.md`. Freeze the garbage lane so `035-3-garbage-filter.md` remains the sole authority on what is hard garbage, debug-only residue, or compatibility-live keep-set. Do not let current-path convenience or grep drift replace the curated classification matrix.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-15` closes only when the garbage lane has one explicit classification authority and no ad hoc deletion rule remains implied.</done>
</task>

<task type="auto">
  <name>035-16 Hard-Garbage Removal Cluster</name>
  <files>.planning/phases/035-mix2-fixes/035-3-garbage-filter.md, .planning/phases/035-mix2-fixes/035-TODO.md, crates/z00z_wallets/src/db/redb_wallet_store_debug_export.rs, crates/z00z_crypto/src/claim/statement.rs</files>
  <action>Use the exact task title above per D-24 and D-25. Reread the `MANDATORY pre-read` block for `035-16` in `035-TODO.md`. Remove or retire only the rows in the curated filter that are explicitly marked immediate garbage. Preserve every compatibility-live and migration-live seam, and keep the codebase on the current live seam rather than opening a parallel replacement layer.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-16` closes only when every removed row is explicitly classified as removable and no compatibility-live seam is deleted by interpretation drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| garbage classification -> code deletion | Misclassified rows can cause live compatibility paths to be removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-15 | T | garbage filter authority | mitigate | Freeze `035-3-garbage-filter.md` as the only removal authority |
| T-035-16 | D | hard-garbage removal cluster | mitigate | Remove only rows explicitly marked removable and preserve all compatibility-live seams |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The garbage lane begins from a frozen classification matrix and removes only the explicit hard-garbage cluster.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-07-SUMMARY.md`.
</output>