---
phase: 035-mix2-fixes
plan: 11
type: execute
wave: 11
depends_on:
  - 035-10
files_modified:
  - .planning/phases/035-mix2-fixes/035-4-fix-spec.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_wallets/src/core/tx/builder.rs
  - crates/z00z_wallets/src/core/tx/output_flow.rs
  - crates/z00z_wallets/src/core/stealth/mod.rs
  - crates/z00z_wallets/src/core/stealth/test_output.rs
  - crates/z00z_wallets/src/core/stealth/test_output_extra.rs
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
autonomous: true
requirements:
  - PH35-SND
must_haves:
  truths:
    - Legacy builder and replayable bundle adapters converge on the canonical stealth helper/formula seam without reviving shadow sender construction.
    - Stealth exports and unit coverage preserve raw, request-bound validated, and card-only validated surfaces without reviving aliases.
  artifacts:
    - crates/z00z_wallets/src/core/tx/builder.rs
    - crates/z00z_wallets/src/core/tx/output_flow.rs
    - crates/z00z_wallets/src/core/stealth/mod.rs
    - crates/z00z_wallets/src/core/stealth/test_output.rs
    - crates/z00z_wallets/src/core/stealth/test_output_extra.rs
    - crates/z00z_simulator/src/scenario_1/stage_3.rs
  key_links:
    - builder/output_flow/stage_3 callsites must reuse the same canonical stealth helper/formula ownership, with any stateless compatibility lanes documented explicitly.
---

# Phase 035 Plan 11

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the second sender-workflow slice for Phase 035 by landing canonical tasks `035-25` through `035-27` in order.

Purpose: converge legacy builder and replayable bundle adapters onto the canonical stealth helper/formula seam and freeze export coverage on that path.
Output: migrated builder/runtime callsites plus explicit export/unit coverage on the canonical sender surfaces, with stateless compatibility lanes documented honestly.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-4-fix-spec.md
@crates/z00z_wallets/src/core/tx/builder.rs
@crates/z00z_wallets/src/core/tx/output_flow.rs
@crates/z00z_wallets/src/core/stealth/output.rs
@crates/z00z_wallets/src/core/stealth/output_build.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>035-25 Legacy Builder Adapter Convergence</name>
  <files>crates/z00z_wallets/src/core/tx/builder.rs, crates/z00z_wallets/src/core/stealth/output_build.rs, .planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Legacy builder entrypoints delegate to the canonical stealth helper seam without keeping a second formula owner.
    - No builder path retains a shadow sender-construction formula.
  </behavior>
  <action>Use the exact task title above per D-18 through D-20. Reread the `MANDATORY pre-read` block for `035-25` in `035-TODO.md`. Converge the legacy tx builder adapter onto the canonical helper layer introduced in the previous plan, keep wallet-owned semantics on the tx-output sender seam, and remove any remaining builder-side construction logic that would create a second formula owner.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-25` closes only when builder callers reach the canonical stealth helper and no alternate builder-owned construction path remains live.</done>
</task>

<task type="auto" tdd="true">
  <name>035-26 Replayable Bundle Adapter Convergence</name>
  <files>crates/z00z_wallets/src/core/tx/output_flow.rs, crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_simulator/src/scenario_1/stage_3.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Replayable bundle construction consumes the same canonical stealth helper/formula seam as the builder path.
    - Simulator/runtime callers do not preserve a stale adapter-only formula.
  </behavior>
  <action>Use the exact task title above per D-18 through D-20. Reread the `MANDATORY pre-read` block for `035-26` in `035-TODO.md`. Converge replayable bundle and runtime adapter callsites in `output_flow.rs` and `stage_3.rs` onto the canonical stealth construction path, keep any compatibility shims truthful and temporary, and do not widen the work into documentation or acceptance gates yet.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-26` closes only when replayable bundle and runtime sender callsites reuse the same canonical stealth helper/formula seam as the builder path, with any stateless compatibility lane made explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>035-27 Stealth Export And Unit Coverage</name>
  <files>crates/z00z_wallets/src/core/stealth/mod.rs, crates/z00z_wallets/src/core/stealth/test_output.rs, crates/z00z_wallets/src/core/stealth/test_output_extra.rs, .planning/phases/035-mix2-fixes/035-4-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Public and crate-local exports preserve the raw path, request-bound validated path, and card-only validated path with explicit approval-level separation.
    - Unit coverage freezes the exported behavior against the validated helper contract and fail-closed card validation semantics.
  </behavior>
  <action>Use the exact task title above per D-18 through D-20. Reread the `MANDATORY pre-read` block for `035-27` in `035-TODO.md`. Export the new validated card-only entrypoint from `stealth/mod.rs`, add success and fail-closed tests in `test_output.rs` and `test_output_extra.rs`, preserve the existing request-bound validated behavior, and keep all exported names, docs, and tests aligned with the wallet-owned approval-level split rather than with legacy adapter names.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-27` closes only when stealth exports and unit coverage preserve the canonical approval-level split and no stale alias or collapsed validated surface remains authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy builder/runtime adapters -> stealth seam | Old adapters can preserve stale sender formulas |
| exports/tests -> canonical helper | Coverage drift can bless the wrong seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-25 | T | legacy builder adapters | mitigate | Route builder entrypoints through the canonical helper/formula seam only and keep wallet-owned validation semantics on the tx-output sender path |
| T-035-26 | S | replayable bundle adapters | mitigate | Align runtime and replayable bundle construction on the same canonical seam |
| T-035-27 | R | stealth export and unit coverage | mitigate | Freeze exports and unit tests against the canonical helper contract |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
Legacy builder, runtime bundle, and export paths all converge on one canonical sender seam with coverage proving the migration.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-11-SUMMARY.md`.
</output>