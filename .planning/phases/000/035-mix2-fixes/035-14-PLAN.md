---
phase: 035-mix2-fixes
plan: 14
type: execute
wave: 14
depends_on:
  - 035-13
files_modified:
  - .planning/phases/035-mix2-fixes/035-5-fix-spec.md
  - .planning/phases/035-mix2-fixes/035-TODO.md
  - crates/z00z_wallets/src/core/key/stealth_keys_receiver.rs
  - crates/z00z_wallets/tests/test_view_key_contract.rs
  - crates/z00z_wallets/tests/test_e2e_send_scan.rs
  - crates/z00z_wallets/tests/test_scenario1_semantics.rs
autonomous: true
requirements:
  - PH35-STL
must_haves:
  truths:
    - Stealth scope stays limited to the approved additions in `035-5-fix-spec.md`.
    - Receiver-secret exposure is inventoried and narrowed on the existing live seam.
  artifacts:
    - crates/z00z_wallets/src/core/key/stealth_keys_receiver.rs
    - crates/z00z_wallets/tests/test_view_key_contract.rs
    - crates/z00z_wallets/tests/test_e2e_send_scan.rs
    - crates/z00z_wallets/tests/test_scenario1_semantics.rs
    - .planning/phases/035-mix2-fixes/035-5-fix-spec.md
  key_links:
    - Receiver-secret exposure inventory must drive narrowing work on the same live receive-path seam.
---

# Phase 035 Plan 14

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the first stealth-additions slice for Phase 035 by landing canonical tasks `035-32` through `035-34` in order.

Purpose: freeze stealth scope and narrow receiver-secret exposure without widening into out-of-scope routing or backend work.
Output: one bounded stealth scope plus live receiver-secret inventory and narrowing on the existing seam.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/035-mix2-fixes/035-CONTEXT.md
@.planning/phases/035-mix2-fixes/035-TODO.md
@.planning/phases/035-mix2-fixes/035-5-fix-spec.md
@crates/z00z_wallets/src/core/key/stealth_keys_receiver.rs
@crates/z00z_wallets/tests/test_view_key_contract.rs
@crates/z00z_wallets/tests/test_e2e_send_scan.rs
@crates/z00z_wallets/tests/test_scenario1_semantics.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>035-32 Stealth Scope Freeze</name>
  <files>.planning/phases/035-mix2-fixes/035-5-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Phase 035 stealth work remains limited to receiver-secret narrowing, derivation vectors, and V2 memo support.
    - PIR, OPRF, routing expansion, and backend migrations remain explicitly out of scope.
  </behavior>
  <action>Use the exact task title above per D-21 through D-23. Reread the `MANDATORY pre-read` block for `035-32` in `035-TODO.md`. Freeze the stealth lane to the approved additions in `035-5-fix-spec.md`, keep the out-of-scope list explicit, and prevent any planner or executor drift into broader routing or backend redesign.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-32` closes only when the stealth lane is fenced to the approved additions and every wider branch is still explicit non-scope.</done>
</task>

<task type="auto" tdd="true">
  <name>035-33 Receiver-Secret Exposure Inventory</name>
  <files>crates/z00z_wallets/src/core/key/stealth_keys_receiver.rs, crates/z00z_wallets/tests/test_view_key_contract.rs, crates/z00z_wallets/tests/test_e2e_send_scan.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs, .planning/phases/035-mix2-fixes/035-5-fix-spec.md, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Receiver-secret exposure points are inventoried from the live receive-path seam.
    - The inventory distinguishes internal wallet use from broader export or runtime exposure.
  </behavior>
  <action>Use the exact task title above per D-21 and D-22. Reread the `MANDATORY pre-read` block for `035-33` in `035-TODO.md`. Inventory every live `reveal_receiver_secret(...)` exposure point starting from `stealth_keys_receiver.rs`, run the exact caller inventory over `crates/z00z_wallets/src` and `crates/z00z_wallets/tests`, classify each hit as production-internal, helper, or test-only, and avoid speculative routing or protocol expansions.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "reveal_receiver_secret" crates/z00z_wallets/src crates/z00z_wallets/tests</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-33` closes only when receiver-secret exposure is enumerated on the live seam with a repository-backed internal-vs-external distinction.</done>
</task>

<task type="auto" tdd="true">
  <name>035-34 Receiver-Secret Narrowing Seam</name>
  <files>crates/z00z_wallets/src/core/key/stealth_keys_receiver.rs, crates/z00z_wallets/tests/test_view_key_contract.rs, crates/z00z_wallets/tests/test_e2e_send_scan.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs, .planning/phases/035-mix2-fixes/035-TODO.md</files>
  <behavior>
    - Receiver-secret exposure is narrowed without breaking the live wallet receive path.
    - Any remaining secret-bearing path is explicit and wallet-private.
  </behavior>
  <action>Use the exact task title above per D-21 and D-22. Reread the `MANDATORY pre-read` block for `035-34` in `035-TODO.md`. Narrow `reveal_receiver_secret(...)` to the smallest internal seam justified by the completed caller inventory, migrate external tests toward direct `ReceiverSecret::from_bytes(...)` reconstruction where that preserves intent, and keep any remaining test seam explicit instead of preserving a broad public convenience method.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>`035-34` closes only when receiver-secret exposure is strictly narrower on the live seam and any remaining secret access is clearly wallet-private.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wallet-private receiver secret -> receive-path helpers | Secret-bearing values can leak into broader runtime surfaces if narrowing is incomplete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-32 | E | stealth scope freeze | mitigate | Keep out-of-scope branches explicit and refuse wider stealth redesign work |
| T-035-33 | I | receiver-secret inventory | mitigate | Inventory all exposure points on the live seam before narrowing them |
| T-035-34 | I | receiver-secret narrowing seam | mitigate | Reduce exposure while preserving wallet-private access only where strictly required |
</threat_model>

<verification>
Run the bootstrap gate first, then the release-style simulator regression gate, then the repeated YOLO review loop until two consecutive clean passes are reached.
</verification>

<success_criteria>
The stealth lane begins from a bounded scope and a narrowed live receiver-secret seam without widening into unrelated protocol work.
</success_criteria>

<output>
After completion, create `.planning/phases/035-mix2-fixes/035-14-SUMMARY.md`.
</output>