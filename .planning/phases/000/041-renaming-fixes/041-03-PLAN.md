---
phase: 041-renaming-fixes
plan: 03
type: execute
wave: 3
depends_on: [041-02]
files_modified:
  - crates/z00z_wallets/tests/test_spend_statement.rs
  - crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_wallets/src/core/tx/claim_tx.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
autonomous: true
requirements:
  - RENAME-041-01
  - RENAME-041-02
  - RENAME-041-03
  - RENAME-041-04
must_haves:
  truths:
    - "Threat-anchor tasks 041-T-01 through 041-T-04 execute in strict order with no behavior drift."
    - "Admission-boundary semantics remain unchanged: full package gate, two-step claim gate, digest routing-only semantics, and statement-envelope spend scope."
    - "Every auto task follows the mandatory verify order: bootstrap gate first, then broader release cargo test, then repeated YOLO review runs until two consecutive clean passes."
  artifacts:
    - path: .planning/phases/041-renaming-fixes/041-03-PLAN.md
      provides: Executable ordered plan for tasks 041-T-01 through 041-T-04 with mandatory gates.
  key_links:
    - from: crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs
      to: crates/z00z_wallets/src/core/tx/tx_verifier.rs
      via: local verifier vs full-package verifier boundary tests
      pattern: "TxVerifierImpl::verify and verify_full_tx_package"
    - from: crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
      to: crates/z00z_simulator/src/claim_pkg_consumer.rs
      via: two-step claim validation contract
      pattern: "ClaimTxVerifierImpl::verify and claim_source_contract_for_item"
    - from: crates/z00z_wallets/tests/test_spend_statement.rs
      to: crates/z00z_wallets/src/core/tx/tx_digest.rs
      via: digest-auth drift invariants
      pattern: "tx_digest_hex remains stable under auth/spend drift"
---

<!-- markdownlint-disable MD022 MD025 MD032 MD033 MD041 MD047 MD055 MD056 MD058 MD060 -->

# 041-03 PLAN

<objective>
Execute the first four threat-anchor tasks from the canonical 041 TODO backlog without changing task names or runtime behavior.

Purpose: lock verifier-boundary safety semantics with explicit negative-path coverage before the final rename safety gate task.
Output: completed and validated task chain for 041-T-01, 041-T-02, 041-T-03, and 041-T-04.
</objective>

<execution_context>
@.github/skills/gsd-plan-phase/SKILL.md
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/041-renaming-fixes/041-CONTEXT.md
@.planning/phases/041-renaming-fixes/041-TODO.md
@.planning/phases/041-renaming-fixes/041-01-PLAN.md
@.planning/phases/041-renaming-fixes/041-02-PLAN.md
</context>

<global_execution_gates>
- Do not change task names or wording from the canonical TODO task headers.
- Execute tasks strictly sequentially: 041-T-01 -> 041-T-02 -> 041-T-03 -> 041-T-04.
- Do not change verifier behavior, admission policy, runtime capabilities, or trait/public-contract boundaries.
- Keep Phase 041 threat-boundary intent aligned to decisions D-01 through D-14.
- If a commit is required during this plan, use /z00z-git-versioning only.
</global_execution_gates>

<tasks>

<task type="auto">
  <name>041-T-01 — Confirm and anchor digest-auth-drift test coverage</name>
  <files>crates/z00z_wallets/tests/test_spend_statement.rs</files>
  <action>
AS IS from 041-TODO.md:

Spec references: Threat T-3; 041-threats-audit.md section 3 verdict; D-03.

MANDATORY pre-read:
- 041-threats-audit.md section 3 — tx_digest_hex stability under auth drift.
- crates/z00z_wallets/tests/test_spend_statement.rs lines 211–245 — test_package_digest_ignores_auth.
- crates/z00z_wallets/tests/test_spend_statement.rs lines 295–325 — test_digest_ignores_auth_hex.
- crates/z00z_wallets/tests/test_spend_statement.rs lines 326–358 — test_digest_ignores_spend_blobs.

Implementation checklist:
- [ ] Read the three existing tests to confirm each one asserts that tx_digest_hex is unchanged after mutating auth.spend_sig_hex, receiver_card_compact, and spend blobs respectively.
- [ ] Add a single-line comment above each test anchoring it to Threat T-3: // Threat T-3 anchor: digest is routing identity; auth drift must not change it.
- [ ] Do not change test logic, assertions, or names.

Files:
- crates/z00z_wallets/tests/test_spend_statement.rs

Tests:
- test_package_digest_ignores_auth must still pass.
- test_digest_ignores_auth_hex must still pass.
- test_digest_ignores_spend_blobs must still pass.

Exit condition:
- All three tests pass with the anchor comment added.
- cargo test -p z00z_wallets test_package_digest_ignores_auth test_digest_ignores_auth_hex test_digest_ignores_spend_blobs returns green.
  </action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>If bootstrap_tests.sh fails: stop, fix issues, and rerun bootstrap_tests.sh before any broader validation.</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_wallets test_package_digest_ignores_auth test_digest_ignores_auth_hex test_digest_ignores_spend_blobs</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 1; fix all issues and warnings.</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 2 after fixes and revalidation.</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 3; continue until at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>All three digest-auth-drift tests remain green and carry the exact Threat T-3 anchor comment with no logic drift.</done>
</task>

<task type="auto">
  <name>041-T-02 — Add local-verifier bypass boundary test</name>
  <files>crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs</files>
  <action>
AS IS from 041-TODO.md:

Spec references: Threat T-1 (CRITICAL); 041-threats-audit.md section 1; D-01; D-13.

MANDATORY pre-read:
- 041-threats-audit.md section 1 — admission bypass via local verifier only.
- crates/z00z_wallets/src/core/tx/tx_verifier.rs lines 103–130 — TxVerifierImpl::verify vs verify_full_tx_package split.
- crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs lines 161–176 — test_full_verifier_spend_contract (the closest existing positive-path test).
- crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_lifecycle.rs line 187 — canonical verify_full_tx_package call site.

Implementation checklist:
- [ ] In test_tx_verifier_suite.rs add a test named test_local_verifier_alone_is_not_final_admission.
- [ ] The test must build a tx payload that passes TxVerifierImpl::verify (local structure, balance, signatures, range proofs) but whose spend proof or spend auth is absent or stale.
- [ ] Assert that TxVerifierImpl::verify returns Ok for that payload.
- [ ] Assert that verify_full_tx_package returns an error or a non-admissible result for the same payload when the spend layer is absent.
- [ ] Add a comment: // Threat T-1 anchor: local verifier is not final admission; verify_full_tx_package is the canonical gate.
- [ ] Do not change any existing test.

Files:
- crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs

Tests:
- test_local_verifier_alone_is_not_final_admission must pass.
- All pre-existing tests in the suite must continue to pass.

Exit condition:
- cargo test -p z00z_wallets test_local_verifier_alone_is_not_final_admission returns green.
- All pre-existing verifier suite tests remain green.
  </action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>If bootstrap_tests.sh fails: stop, fix issues, and rerun bootstrap_tests.sh before any broader validation.</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_wallets test_local_verifier_alone_is_not_final_admission</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 1; fix all issues and warnings.</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 2 after fixes and revalidation.</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 3; continue until at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>New local-vs-full admission boundary test is green, existing verifier suite coverage remains green, and canonical gate semantics are explicit.</done>
</task>

<task type="auto">
  <name>041-T-03 — Add raw claim verifier rebind-gap test</name>
  <files>crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, crates/z00z_wallets/src/core/tx/claim_tx.rs</files>
  <action>
AS IS from 041-TODO.md:

Spec references: Threat T-2 (HIGH); 041-threats-audit.md section 2; D-02; D-13.

MANDATORY pre-read:
- 041-threats-audit.md section 2 — raw claim verify without persisted-store rebind.
- crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs lines 142 and 277 — ClaimTxVerifierImpl::verify scope boundary.
- crates/z00z_wallets/src/core/tx/claim_tx.rs line 227 — claim tx wire boundary.
- crates/z00z_simulator/src/claim_pkg_consumer.rs lines 210 and 278 — the canonical two-step consumer pattern (consistency + store rebind).

Implementation checklist:
- [ ] Locate or create a test module in crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs (or in a sibling test file if the module shape demands it).
- [ ] Add a test named test_raw_claim_verifier_alone_is_not_final_admission.
- [ ] The test must construct a claim package whose internal claim-source proof and statement are self-consistent but whose root is synthetic (not bound to the live persisted store).
- [ ] Assert that ClaimTxVerifierImpl::verify (or the equivalent raw verify surface) returns Ok / a passing report for the synthetic package.
- [ ] Assert that without claim_source_contract_for_item rebinding, no persisted-store validation occurs — i.e., document that the synthetic root passes the verifier unchallenged.
- [ ] Add a comment: // Threat T-2 anchor: raw verifier passes internal consistency only; store rebinding via claim_source_contract_for_item is the mandatory second step for final admission.
- [ ] Do not change any existing test or any verifier behavior.

Files:
- crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
- Optionally crates/z00z_wallets/src/core/tx/claim_tx.rs if a doc comment is added to the claim wire boundary.

Tests:
- test_raw_claim_verifier_alone_is_not_final_admission must pass.
- All pre-existing claim tx verifier tests must continue to pass.

Exit condition:
- cargo test -p z00z_wallets test_raw_claim_verifier_alone_is_not_final_admission returns green.
- Pre-existing tests remain green.
  </action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>If bootstrap_tests.sh fails: stop, fix issues, and rerun bootstrap_tests.sh before any broader validation.</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_wallets test_raw_claim_verifier_alone_is_not_final_admission</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 1; fix all issues and warnings.</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 2 after fixes and revalidation.</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 3; continue until at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>Raw claim verifier rebind-gap test is green, pre-existing claim verifier tests remain green, and D-02 two-step claim semantics are explicitly anchored.</done>
</task>

<task type="auto">
  <name>041-T-04 — Add partial spend contract insufficiency test</name>
  <files>crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/tx_verifier.rs</files>
  <action>
AS IS from 041-TODO.md:

Spec references: Threat T-4 (MEDIUM); 041-threats-audit.md section 4; D-04; D-13.

MANDATORY pre-read:
- 041-threats-audit.md section 4 — public spend contract is not a full tx verifier.
- crates/z00z_wallets/src/core/tx/tx_verifier.rs lines 103–130 — verify_full_tx_package composition of local checks plus spend contract.
- crates/z00z_wallets/src/core/tx/tx_verifier.rs line 121 — verify_tx_public_spend_contract call site within the full verifier.
- crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs lines 178–195 — test_public_spend_valid_package (positive-path only).

Implementation checklist:
- [ ] In test_tx_verifier_suite.rs add a test named test_partial_spend_contract_is_not_full_admission.
- [ ] The test must construct a tx package whose public spend envelope is self-consistent but whose local structure validation (e.g., balance, role, output key) would be rejected by TxVerifierImpl::verify.
- [ ] Assert that verify_tx_public_spend_contract (called directly) returns a passing result for the envelope-only check.
- [ ] Assert that verify_full_tx_package (which composes both layers) returns an error for the same payload.
- [ ] Add a comment: // Threat T-4 anchor: verify_tx_public_spend_contract checks statement-envelope scope only; verify_full_tx_package is required for complete admission.
- [ ] In crates/z00z_wallets/src/core/tx/spend_verification.rs add a doc comment on verify_tx_public_spend_contract clarifying: "Validates the spend statement envelope only. Caller must also run TxVerifierImpl::verify for full admission; see verify_full_tx_package."
- [ ] In crates/z00z_wallets/src/core/tx/tx_verifier.rs add a doc comment on verify_tx_public_spend_contract re-export or call site confirming it is a sub-check, not a complete gate.

Files:
- crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs
- crates/z00z_wallets/src/core/tx/spend_verification.rs
- crates/z00z_wallets/src/core/tx/tx_verifier.rs

Tests:
- test_partial_spend_contract_is_not_full_admission must pass.
- test_public_spend_valid_package must continue to pass.
- All pre-existing verifier suite tests must remain green.

Exit condition:
- cargo test -p z00z_wallets test_partial_spend_contract_is_not_full_admission returns green.
- Pre-existing suite remains green.
- cargo clippy -p z00z_wallets emits zero new warnings from touched files.
  </action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>If bootstrap_tests.sh fails: stop, fix issues, and rerun bootstrap_tests.sh before any broader validation.</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_wallets test_partial_spend_contract_is_not_full_admission test_public_spend_valid_package</automated>
    <automated>cargo clippy -p z00z_wallets</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 1; fix all issues and warnings.</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 2 after fixes and revalidation.</automated>
    <automated>Run /GSD-Review-Tasks-Execution in YOLO mode pass 3; continue until at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>Partial spend contract insufficiency path is covered and green, related positive test remains green, and helper-scope doc boundaries are explicit without behavior changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| verifier helper to admission gate | Local or partial verifier results must not be treated as final admission. |
| raw claim verify to persisted-store rebind | Internal consistency without store rebinding can create false admission confidence. |
| digest identity to tamper detection | Digest stability under auth drift must not be interpreted as payload integrity validation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-041-03-01 | T | local verifier boundary | mitigate | Add explicit negative-path test for local-pass/full-fail contract in 041-T-02. |
| T-041-03-02 | T | claim verifier boundary | mitigate | Add explicit synthetic-root rebind-gap test in 041-T-03. |
| T-041-03-03 | I | digest semantics | mitigate | Anchor digest-auth drift tests without logic drift in 041-T-01. |
| T-041-03-04 | E | spend contract helper misuse | mitigate | Add partial-envelope/full-verifier split test and helper-scope docs in 041-T-04. |
</threat_model>

<verification>
- Confirm sequential execution order is preserved: 041-T-01, 041-T-02, 041-T-03, 041-T-04.
- Confirm every auto task verify block follows mandatory order: bootstrap gate first, broader release cargo test second, repeated YOLO review loop third.
- Confirm no runtime behavior or admission-policy change is introduced by this plan.
- Confirm commit operations, if needed, use /z00z-git-versioning only.
</verification>

<success_criteria>
- Tasks 041-T-01 through 041-T-04 are executable as written with unchanged task names and wording.
- Threat-boundary semantics D-01 through D-04 remain explicit and unchanged.
- All required verification gates are encoded in each task.
- Final gate task 041-T-05 remains scheduled for the next plan artifact to satisfy the <=4 task cap for this plan.
</success_criteria>

<output>
After completion, create .planning/phases/041-renaming-fixes/041-03-SUMMARY.md
</output>
