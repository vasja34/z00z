# Z00Z Signature Suffix Audit

**Scope:** live Rust function declarations ending in `_with`, `_to`, `_from`, `_after`, `_as`, `_not`, `_at`, `_into`, `_of`, or `_without`, excluding vendored `crates/z00z_crypto/tari/`
**Date:** 2026-04-29
**Files scanned:** 40 matched files under `crates/**/*.rs`
**Declaration rows emitted:** 54
**Action counts:** rename=33, keep=5, trait-locked=16

> Note: rows were built from a declaration-only live workspace scan and then re-checked against local source context before assigning rename advice. This is a semantic suffix audit, not a bulk suffix trim.



## ✍️ Rename Plan

| # | Kind | Old Name | File | Line | New Name | Alternatives Considered | Decision Rationale | Comments |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | private fn | `read_stop_after` | `crates/z00z_wallets/bin/z00z_wallet_egui.rs` | 69 | `read_stop_timeout` | `read_stop_duration` (precise type, weaker operational intent); `read_stop_after_secs` (matches env var, suggests raw seconds return); `read_stop_timeout` (chosen: names the stop control as a timeout) | The helper reads `Z00Z_EGUI_STOP_AFTER_SEC` and returns an `Option<Duration>`, so timeout wording is more direct than a dangling `_after` suffix. | Local env-var reader. |
| 5 | private fn | `mk_cfg_with` | `crates/z00z_simulator/tests/test_claim_acceptance.rs` | 34 | `build_scenario_fixture` | `prepare_scenario_fixture` (good setup verb, weaker on output artifact creation); `write_scenario_fixture` (over-focuses on file writes); `build_scenario_fixture` (chosen: covers layout, config mutation, and returned paths) | The helper creates a temp scenario layout, writes config bytes, and returns the fixture paths, so a fixture-builder name is the best fit. | If this is renamed, consider a follow-up cleanup for the wrapper `mk_cfg`. |
| 6 | private fn | `reset_outputs_dir_at` | `crates/z00z_simulator/src/scenario_1/runner.rs` | 114 | `recreate_outputs_dir` | `reset_outputs_dir` (familiar, but less explicit about delete+create); `prepare_outputs_dir` (too soft for destructive behavior); `recreate_outputs_dir` (chosen: matches `remove_dir_all` + `create_dir_all`) | The helper destroys and recreates the directory at a validated path, so `recreate` is the precise action word. | Local filesystem helper. |
| 8 | private async fn | `update_activity_at` | `crates/z00z_wallets/src/services/wallet_service_session_lifecycle.rs` | 164 | `touch_last_activity` | `set_last_activity_ms` (exact field name, more storage-centric); `record_activity_time` (clear, heavier); `touch_last_activity` (chosen: matches session-lifecycle intent) | The method refreshes the in-memory last-activity marker, so `touch` reads better at callers than a trailing `_at` timestamp name. | Rename in lockstep with row 19. |
| 11 | private async fn | `register_unlocked_wallet_at` | `crates/z00z_wallets/src/services/wallet_service_session_guards.rs` | 8 | `track_unlocked_wallet` | `insert_unlocked_wallet` (container-centric, less service-oriented); `store_unlocked_wallet_state` (explicit, heavy); `track_unlocked_wallet` (chosen: matches runtime state tracking) | The helper inserts an unlocked wallet into the session-state map and timestamps it, so tracking language fits the service layer best. | Rename in lockstep with row 20. |
| 13 | pub(super) fn | `root_at` | `crates/z00z_storage/src/assets/store_internal/test_whitebox_help.rs` | 121 | `root_for_version` | `root_by_version` (map-flavored, slightly colder call sites); `historical_root` (drops the explicit input key); `root_for_version` (chosen: exact lookup semantics) | This helper returns the historical root for a given version, so `for_version` is clearer than bare `_at`. | Whitebox helper used by tests. |
| 14 | pub(super) fn | `proof_at` | `crates/z00z_storage/src/assets/store_internal/test_whitebox_help.rs` | 128 | `path_proof_for_version` | `proof_for_path` (drops the version dimension); `proof_by_version_path` (awkward word order); `path_proof_for_version` (chosen: names both inputs) | The helper resolves a proof by both version and path, so the new name must carry both axes explicitly. | Whitebox helper used by tests. |
| 15 | private fn | `item_from` | `crates/z00z_storage/src/assets/store_internal/tx_plan_batches.rs` | 115 | `item_for_path` | `lookup_item_for_path` (clear, slightly verbose); `store_item_for_path` (sounds like allocation rather than query); `item_for_path` (chosen: simple optional lookup helper) | The helper conditionally fetches a `StoreItem` for an optional path, so `for_path` is the tightest semantic label. | Internal query helper. |
| 18 | private fn | `meets_after` | `crates/z00z_storage/src/assets/store_internal/store_query.rs` | 304 | `is_after_page_token` | `passes_after_cursor` (good predicate, weaker type coupling); `after_page_token` (too fragmentary); `is_after_page_token` (chosen: boolean predicate that names the pagination token) | The method is a pure boolean check against `req.after()`, so the name should read as a predicate instead of a vague `meets_*` phrase. | Internal pagination helper. |
| 19 | pub(crate) async fn | `update_activity_at` | `crates/z00z_wallets/src/services/wallet_service_session_runtime.rs` | 164 | `touch_last_activity` | `set_last_activity_ms` (exact field name, more storage-centric); `record_activity_time` (clear, heavier); `touch_last_activity` (chosen: matches session-lifecycle intent) | This runtime twin performs the same last-activity refresh as row 8 and should keep the same semantic name. | Rename together with row 8. |
| 20 | pub(crate) async fn | `register_unlocked_wallet_at` | `crates/z00z_wallets/src/services/wallet_service_session_runtime.rs` | 194 | `track_unlocked_wallet` | `insert_unlocked_wallet` (container-centric, less service-oriented); `store_unlocked_wallet_state` (explicit, heavy); `track_unlocked_wallet` (chosen: matches runtime state tracking) | This runtime twin performs the same unlocked-wallet registration as row 11 and should keep the same semantic name. | Rename together with row 11. |
| 21 | test fn | `test_tx_details_response_without` | `crates/z00z_wallets/src/adapters/rpc/types/tx.rs` | 341 | `test_tx_details_no_receipt` | `test_tx_details_receipt_absent` (formal, slightly stiff); `test_pending_tx_details` (drops the missing-receipt invariant); `test_tx_details_no_receipt` (chosen: names the exact absent field) | The scenario is specifically a pending response with `receipt: None`, so the test name should say `no_receipt` outright. | JSON codec round-trip test. |
| 22 | private fn | `tag_of` | `crates/z00z_wallets/tests/test_e2e_tag_auth.rs` | 248 | `extract_tag16` | `asset_tag16` (getter-like, less explicit about unwrap); `read_tag16` (okay, weaker extraction cue); `extract_tag16` (chosen: tiny helper that pulls `tag16` out of an asset) | The helper unwraps the optional `tag16` field and returns it directly, so extraction language is the clearest. | Test helper. |
| 23 | private fn | `init_rpc_crypto_with` | `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs` | 379 | `init_rpc_crypto_configured` | `init_rpc_crypto_params` (nouny, less idiomatic); `init_rpc_crypto_custom` (vague about what is custom); `init_rpc_crypto_configured` (chosen: explicit companion to zero-arg `init_rpc_crypto`) | This helper exists only to support a parameterized initialization path under the plain `init_rpc_crypto()` wrapper, so `configured` is the most accurate distinction. | Rename test call at line 401 together with the helper. |
| 24 | private fn | `rel_of` | `crates/z00z_wallets/tests/test_e2e_public_path.rs` | 92 | `relative_path_string` | `display_relative_path` (good, but emphasizes formatting over return type); `path_relative_string` (awkward order); `relative_path_string` (chosen: exact output semantics) | The helper returns a `String` representation of a stripped relative path, so the name should say both `relative_path` and `string`. | Test helper. |
| 25 | private fn | `leaf_of` | `crates/z00z_wallets/tests/test_e2e_self_tax.rs` | 147 | `build_asset_leaf` | `leaf_from_output` (drops the extra aid/sid/proof inputs); `make_asset_leaf` (fine, slightly softer builder verb); `build_asset_leaf` (chosen: constructs a full `AssetLeaf`) | The helper assembles a new `AssetLeaf` from an output plus extra metadata, so a builder verb is the right fit. | Test helper. |
| 26 | private fn | `asset_of` | `crates/z00z_wallets/tests/test_e2e_self_tax.rs` | 160 | `build_asset_from_leaf` | `asset_from_leaf` (clear, but easy to confuse with broader domain converters); `make_asset_for_leaf` (goal-focused, less exact); `build_asset_from_leaf` (chosen: explicit leaf-to-asset helper) | The helper converts one `AssetLeaf` plus an amount into a test asset, so the source object belongs in the name. | Same semantic rename as row 29. |
| 27 | private fn | `scan_of` | `crates/z00z_wallets/tests/test_e2e_self_tax.rs` | 205 | `scanner_for_keys` | `build_scanner` (too generic); `stealth_scanner_for_keys` (clear, longer); `scanner_for_keys` (chosen: exact helper output) | The helper is only a thin wrapper around `StealthOutputScanner::from_keys`, so the name should say that directly. | Test helper. |
| 28 | private fn | `shift_at` | `crates/z00z_wallets/tests/test_tx_assetpack.rs` | 33 | `rotate_tail_left` | `shift_bytes_left` (drops the keep-byte-to-end behavior); `rotate_bytes_from_index` (clear, longer); `rotate_tail_left` (chosen: captures the tail rotation semantics) | The helper rotates the slice tail left while moving the kept byte to the end, so `rotate_tail_left` is more accurate than a bare positional suffix. | Test helper. |
| 29 | private fn | `asset_of` | `crates/z00z_wallets/tests/test_s6_recv_examples.rs` | 39 | `build_asset_from_leaf` | `asset_from_leaf` (clear, but easy to confuse with broader domain converters); `make_asset_for_leaf` (goal-focused, less exact); `build_asset_from_leaf` (chosen: explicit leaf-to-asset helper) | This helper matches the row 26 conversion pattern and should keep the same clearer name. | Rename together with row 26 if shared helpers are extracted later. |
| 30 | pub(super) fn | `store_wallet_updated_at` | `crates/z00z_wallets/src/db/redb_wallet_store_meta.rs` | 83 | `store_wallet_updated_ms` | `store_wallet_update_time` (clear, but less aligned with the u64 millis field); `write_wallet_updated_ms` (good, more I/O flavored); `store_wallet_updated_ms` (chosen: mirrors the stored value exactly) | The function persists a millisecond timestamp, so the new name should say `ms` instead of trailing `_at`. | Metadata-table helper. |
| 31 | private fn | `time_at` | `crates/z00z_wallets/src/adapters/rpc/methods/test_tx_impl_suite.rs` | 24 | `mock_time_with_offset` | `time_for_offset` (drops the MockTimeProvider role); `offset_time_provider` (nouny); `mock_time_with_offset` (chosen: names both helper role and argument) | The helper constructs an `Arc<MockTimeProvider>` from a base timestamp plus an offset, so the test fixture role belongs in the name. | Test helper. |
| 32 | private fn | `package_json_with` | `crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs` | 68 | `package_json_for_asset` | `build_package_json` (too generic next to `package_json()`); `package_json_for_class` (ignores the amount arg); `package_json_for_asset` (chosen: helper builds one asset package from class+amount) | The helper packages a single synthetic asset into JSON, so the name should say what is being packaged instead of trailing `_with`. | Test helper called by `package_json()`. |
| 35 | test fn | `test_load_not_initialized_not` | `crates/z00z_wallets/src/core/storage/scan_storage_impl.rs` | 134 | `test_load_uninitialized_fails` | `test_load_not_found` (reflects error, weaker setup intent); `test_load_requires_init` (clear, but indirect); `test_load_uninitialized_fails` (chosen: removes the double-negative cleanly) | The current name obscures the setup with a `not_*_not` pattern; the replacement says exactly that load fails before initialization. | Scan-storage test. |
| 36 | test fn | `test_reset_not_initialized_not` | `crates/z00z_wallets/src/core/storage/scan_storage_impl.rs` | 229 | `test_reset_uninitialized_fails` | `test_reset_not_found` (reflects error, weaker setup intent); `test_reset_requires_init` (clear, but indirect); `test_reset_uninitialized_fails` (chosen: removes the double-negative cleanly) | The current name obscures the setup with a `not_*_not` pattern; the replacement says exactly that reset fails before initialization. | Scan-storage test. |
| 43 | test fn | `test_get_tip_height_not` | `crates/z00z_wallets/src/core/chain/chain_client_impl.rs` | 104 | `test_get_tip_height_errors` | `test_get_tip_height_fails` (fine, slightly less tied to `Result`); `test_tip_height_request_errors` (okay, shifts subject); `test_get_tip_height_errors` (chosen: matches `assert!(result.is_err())`) | The test only asserts the error path, so the name should say `errors` explicitly instead of ending in `_not`. | Chain-client test. |
| 44 | private fn | `evict_expired_at` | `crates/z00z_wallets/src/core/address/address_manager/address_manager_impl_runtime_maintenance.rs` | 167 | `evict_expired_entries` | `purge_expired_entries` (equally good, slightly stronger tone); `collect_expired_entries` (wrong focus, the helper also mutates state); `evict_expired_entries` (chosen: names the actual mutation) | The method evicts cached entries using the supplied `now` timestamp, so the time suffix adds no value. | Internal maintenance helper. |
| 45 | test fn | `test_insufficient_balance_is_not` | `crates/z00z_wallets/src/core/wallet/test_errors_suite.rs` | 10 | `test_insufficient_balance_non_transient` | `test_insufficient_balance_not_transient` (okay, slightly clunkier); `test_insufficient_balance_stable_error` (less tied to the API contract); `test_insufficient_balance_non_transient` (chosen: matches `is_transient()` exactly) | The assertion is specifically about `WalletError::is_transient()`, so `non_transient` is the sharpest possible label. | Wallet-error test. |
| 46 | test fn | `test_crypto_error_is_not` | `crates/z00z_wallets/src/core/wallet/test_errors_suite.rs` | 25 | `test_crypto_error_non_transient` | `test_crypto_error_not_transient` (okay, slightly clunkier); `test_crypto_error_stable` (less tied to the API contract); `test_crypto_error_non_transient` (chosen: matches `is_transient()` exactly) | This test makes the same non-transient assertion as row 45 and should use the same vocabulary. | Wallet-error test. |
| 47 | private fn | `claim_from` | `crates/z00z_wallets/src/core/claim/nullifier_store.rs` | 226 | `claim_from_entry` | `map_entry_to_claim` (clear transform, slightly heavier); `build_claim_from_entry` (verbose); `claim_from_entry` (chosen: concise source-to-result mapping) | The helper constructs a `NullifierClaim` from a `NullifierEntry`, so the source type should appear directly in the name. | Local mapping helper. |
| 50 | test fn | `test_validate_spend_asset_not` | `crates/z00z_wallets/src/core/wallet/policy.rs` | 248 | `test_validate_spend_disallowed_asset` | `test_validate_spend_asset_rejected` (less exact than the error type); `test_disallowed_asset_rejected` (drops the validated action); `test_validate_spend_disallowed_asset` (chosen: mirrors `AssetNotAllowed`) | The failure mode is specifically `PolicyError::AssetNotAllowed`, so the new name should say `disallowed_asset` instead of a generic negative suffix. | Policy test. |
| 51 | test fn | `test_validate_spend_recipient_not` | `crates/z00z_wallets/src/core/wallet/policy.rs` | 267 | `test_validate_spend_disallowed_recipient` | `test_validate_spend_recipient_rejected` (less exact than the error type); `test_disallowed_recipient_rejected` (drops the validated action); `test_validate_spend_disallowed_recipient` (chosen: mirrors `RecipientNotAllowed`) | The failure mode is specifically `PolicyError::RecipientNotAllowed`, so the new name should say `disallowed_recipient` instead of a generic negative suffix. | Policy test. |
| 54 | test fn | `test_bounds_record_decode_as` | `crates/z00z_wallets/src/db/tests/redb_wallet_store.rs` | 2641 | `test_corrupt_object_record_rejected` | `test_tampered_object_record_rejected` (covers only half the scenario); `test_invalid_object_record_rejected` (good, but weaker on corruption); `test_corrupt_object_record_rejected` (chosen: covers short bytes and tampered ciphertext) | The test injects both invalid record bytes and a tampered encrypted envelope, so `corrupt_object_record` is the most faithful description. | Existing name is semantically stale. |

---

## 🔎 Summary

1. The blocked set is the 16 trait-locked or format-locked rows. They should not be renamed mechanically.
2. The low-risk rename tranche is the test/helper surface: rows 1, 5, 21-32, 35-36, 43-46, 50-51, and 54.
3. The medium-risk internal rename tranche is rows 6, 8, 11, 13-15, 18-20, 23, 30, 44, and 47 because each has local callers but no trait lock.
4. Rows 8 and 19, 11 and 20, and 26 and 29 should be renamed together to preserve internal consistency.
