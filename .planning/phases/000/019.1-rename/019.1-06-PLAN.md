<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 019.1-rename
plan: 06
type: execute
wave: 4
depends_on:
  - 019.1-01
  - 019.1-02
  - 019.1-04
  - 019.1-05
files_modified:
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl.rs
  - crates/z00z_wallets/src/core/stealth/test_output.rs
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/core/stealth/test_output_extra.rs
  - crates/z00z_wallets/src/core/claim/test_nullifier_store.rs
  - crates/z00z_wallets/src/core/claim/nullifier_store.rs
  - crates/z00z_wallets/src/core/tx/test_claim_tx.rs
  - crates/z00z_wallets/src/core/tx/claim_tx.rs
  - crates/z00z_wallets/src/core/address/test_stealth_request.rs
  - crates/z00z_wallets/src/core/address/stealth_request.rs
  - crates/z00z_wallets/src/core/address/test_stealth_card.rs
  - crates/z00z_wallets/src/core/address/stealth_card.rs
  - crates/z00z_wallets/src/core/address/stealth_scanner/test_stealth_scanner.rs
  - crates/z00z_wallets/src/core/address/stealth_scanner.rs
  - crates/z00z_wallets/tests/test_proof_blob_fix.rs
  - crates/z00z_wallets/tests/test_stealth_scan_support.rs
autonomous: true
requirements:
  - None
source_plan: .planning/phases/019.1-rename/RENAME-PLAN.md
crate: z00z_wallets
must_haves:
  truths:
    - The audited wallets test-only files use the test_ prefix and every parent `#[path]` declaration resolves correctly.
    - The concrete wallets test functions listed in the audit use the test_ prefix after execution.
    - The broader wallets test suites listed in the batch section are rechecked so no additional rename debt is left behind.
  artifacts:
    - renamed `test_*` wallet files exist under crates/z00z_wallets/src and crates/z00z_wallets/tests.
    - parent wallet modules reference the renamed file paths correctly.
    - the audited wallets test suites use only `test_`-prefixed names in the touched areas.
  key_links:
    - key_impl.rs, output.rs, nullifier_store.rs, claim_tx.rs, stealth_request.rs, stealth_card.rs, and stealth_scanner.rs resolve renamed test files through updated `#[path]` declarations.
    - wallet integration tests keep importing the renamed support files correctly.
    - any broader gate failure is classified against the known pre-existing blocker recorded in STATE.md.
---

<objective>
Apply the canonical `z00z_wallets` rename audit after storage, core, crypto, and utils have absorbed their rename fallout.

Purpose: close the largest test-surface crate before the simulator plan consumes the renamed wallet APIs and support modules.
Output: renamed wallet test files, updated `#[path]` declarations, and normalized wallet test names.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/019.1-rename/019.1-CONTEXT.md
@.planning/phases/019.1-rename/019.1-RESEARCH.md
@.planning/phases/019.1-rename/019.1-VALIDATION.md
@.planning/phases/019.1-rename/RENAME-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@crates/z00z_wallets/src/core/address/stealth_scanner.rs
@crates/z00z_wallets/src/core/tx/claim_tx.rs
@crates/z00z_wallets/src/lib.rs

<interfaces>
Wallet rename contract from the canonical audit includes:
- rename the audited test-only files to the `test_` form
- update every parent `#[path]` declaration to the renamed file
- prefix the concrete and batch-listed wallet test functions with `test_`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename wallets test-only files and `#[path]` declarations</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs, crates/z00z_wallets/src/adapters/rpc/methods/key_impl.rs, crates/z00z_wallets/src/core/stealth/test_output.rs, crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/core/stealth/test_output_extra.rs, crates/z00z_wallets/src/core/claim/test_nullifier_store.rs, crates/z00z_wallets/src/core/claim/nullifier_store.rs, crates/z00z_wallets/src/core/tx/test_claim_tx.rs, crates/z00z_wallets/src/core/tx/claim_tx.rs, crates/z00z_wallets/src/core/address/test_stealth_request.rs, crates/z00z_wallets/src/core/address/stealth_request.rs, crates/z00z_wallets/src/core/address/test_stealth_card.rs, crates/z00z_wallets/src/core/address/stealth_card.rs, crates/z00z_wallets/src/core/address/stealth_scanner/test_stealth_scanner.rs, crates/z00z_wallets/src/core/address/stealth_scanner.rs, crates/z00z_wallets/tests/test_proof_blob_fix.rs, crates/z00z_wallets/tests/test_stealth_scan_support.rs</files>
  <action>Apply D-01, D-02, D-03, D-04, and D-07 to the `z00z_wallets` file-rename rows in `RENAME-PLAN.md`. Rename the audited wallet test-only files to the `test_` form and update every `#[path]`, sibling module link, and integration-test reference that points at them.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The renamed wallet test files are discoverable through updated `#[path]` declarations, no stale path references remain in the touched wallet files, and any broader-gate failure is classified before closure.</done>
</task>

<task type="auto">
  <name>Task 2: Prefix the concrete wallets test functions named in the audit</name>
  <files>crates/z00z_wallets/src/core/tx/test_claim_tx.rs, crates/z00z_wallets/src/core/address/stealth_scanner/test_stealth_scanner.rs</files>
  <action>Apply the concrete test-function rows from the `z00z_wallets` section of `RENAME-PLAN.md` exactly as written. Prefix the listed non-`test_` functions with `test_`, keep bodies unchanged, and update any local helper references affected by the renamed test functions.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The concrete audited wallet test functions all start with `test_`, the touched wallet suites still compile and run with unchanged behavior, and any broader-gate failure is classified before closure.</done>
</task>

<task type="auto">
  <name>Task 3: Recheck the batch-listed wallets suites and classify broader failures</name>
  <files>crates/z00z_wallets/src/services/wallet_paths.rs, crates/z00z_wallets/src/adapters/rpc/dispatcher_handlers.rs, crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs, crates/z00z_wallets/src/adapters/rpc/methods/wallet_impl.rs, crates/z00z_wallets/src/adapters/rpc/methods/network_impl.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs, crates/z00z_wallets/tests/test_rpc_logging_acceptance.rs, crates/z00z_wallets/tests/test_stealth_reference_bridge.rs, crates/z00z_wallets/tests/test_rpc_persistence_spec_e3.rs, crates/z00z_wallets/tests/test_rpc_logging_risk_policy.rs, crates/z00z_wallets/tests/test_claim_state_compat.rs, crates/z00z_wallets/tests/test_tx_tamper.rs, crates/z00z_wallets/tests/test_rpc_logging_configured_path.rs, crates/z00z_wallets/tests/test_create_wallet_crypto_e2e.rs, crates/z00z_wallets/tests/test_rpc_logging_request_id.rs, crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs, crates/z00z_wallets/tests/test_key_manager.rs, crates/z00z_wallets/tests/test_rpc_scenarios_minimal.rs, crates/z00z_wallets/tests/test_claim_import_reason_codes.rs, crates/z00z_wallets/tests/test_tx_wrong_root.rs, crates/z00z_wallets/tests/test_claim_core_safety.rs, crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs, crates/z00z_wallets/tests/test_phase24_gate.rs, crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs, crates/z00z_wallets/tests/test_app_service_create_wallet.rs, crates/z00z_wallets/tests/test_rpc_logging_file_sink.rs, crates/z00z_wallets/tests/test_redb_wlt_open.rs, crates/z00z_wallets/tests/test_claim_state_core.rs, crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs, crates/z00z_wallets/tests/test_phase12_dual_keys.rs, crates/z00z_wallets/tests/test_rpc_logging_replay_audit.rs, crates/z00z_wallets/tests/test_phase15_regress.rs, crates/z00z_wallets/tests/test_tx_roundtrip.rs, crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs, crates/z00z_wallets/tests/test_wlt_validator.rs, crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs, crates/z00z_wallets/tests/test_tx_store_integration.rs, crates/z00z_wallets/tests/test_rpc_logging_e2e_print.rs, crates/z00z_wallets/src/lib.rs</files>
  <action>Use the batch-list section of `RENAME-PLAN.md` as the closure pass for `z00z_wallets`. Prefix any remaining non-`test_` tests surfaced in the explicitly listed files, keep the scope inside wallets, and if the broader gate still fails, classify whether the failure comes from new rename fallout or the known pre-existing blocker already recorded in `.planning/STATE.md`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The wallets batch-listed suites no longer contain audited naming debt, and any remaining broader-gate failure is explicitly classified before the task closes.</done>
</task>

</tasks>

<verification>
Confirm `z00z_wallets` absorbed only the audited naming changes, `#[path]` links resolve correctly, and any remaining release failure is classified against the known blocker instead of being silently accepted.
</verification>

<success_criteria>
`z00z_wallets` matches its section of `RENAME-PLAN.md`, the release gate passes after the mandatory bootstrap gate or any remaining blocker is explicitly pre-existing, and the simulator plan can consume the renamed wallet surface last.
</success_criteria>

<output>
After completion, create `.planning/phases/019.1-rename/019.1-06-SUMMARY.md`.
</output>
