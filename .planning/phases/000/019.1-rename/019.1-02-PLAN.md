<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 019.1-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/src/hash.rs
  - crates/z00z_crypto/src/kdf.rs
  - crates/z00z_crypto/src/ecdh.rs
  - crates/z00z_crypto/src/ecdh_stealth.rs
  - crates/z00z_crypto/src/validation.rs
  - crates/z00z_crypto/src/commitments.rs
  - crates/z00z_crypto/examples/compute_golden.rs
  - crates/z00z_crypto/tests/domain_separation.rs
  - crates/z00z_crypto/tests/golden_vectors.rs
  - crates/z00z_crypto/tests/spec_golden_vectors.rs
  - crates/z00z_crypto/tests/test_h2scalar.rs
  - crates/z00z_crypto/tests/test_stealth_reference.rs
  - crates/z00z_crypto/src/range_proofs.rs
  - crates/z00z_crypto/src/commitments.rs
  - crates/z00z_crypto/src/claim/verifier.rs
  - crates/z00z_crypto/src/claim/proof.rs
autonomous: true
requirements:
  - None
source_plan: .planning/phases/019.1-rename/RENAME-PLAN.md
crate: z00z_crypto
must_haves:
  truths:
    - The exported z00z_crypto facade no longer exposes the ambiguous public names listed in the crypto section of RENAME-PLAN.md.
    - Same-crate source, test, and example call sites compiled by `cargo test -p z00z_crypto` are updated together with the renamed crypto API.
    - The targeted crypto unit tests use the test_ prefix after the rename pass.
    - The read-only Tari vendor subtree remains untouched.
  artifacts:
    - crates/z00z_crypto/src/lib.rs re-exports the renamed crypto helpers and error types.
    - crates/z00z_crypto/src/hash.rs, kdf.rs, ecdh.rs, ecdh_stealth.rs, validation.rs, and commitments.rs hold the renamed same-crate source fallout.
    - crates/z00z_crypto/tests/*.rs and examples/compute_golden.rs no longer reference the old symbol names.
    - the audited crypto test files contain only `test_`-prefixed test functions.
  key_links:
    - crates/z00z_crypto/src/lib.rs stays aligned with renamed exports from hash.rs, kdf.rs, ecdh.rs, ecdh_stealth.rs, and validation.rs.
    - crates/z00z_crypto/src/ecdh_stealth.rs stays aligned with the renamed `derive_dh_key` helper.
    - same-crate tests and examples stay aligned with the renamed root exports and helper functions.
    - no file under crates/z00z_crypto/tari/ is modified or referenced for edits.
---

<objective>
Apply the canonical `z00z_crypto` rename audit as one of the two dependency-root plans for phase 019.1.

Purpose: land the low-level crypto API renames before any consumer crate absorbs the new names.
Output: renamed crypto exports, updated crate facades, normalized crypto test names, and a clean vendor boundary.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/019.1-rename/019.1-CONTEXT.md
@.planning/phases/019.1-rename/019.1-RESEARCH.md
@.planning/phases/019.1-rename/019.1-VALIDATION.md
@.planning/phases/019.1-rename/RENAME-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@crates/z00z_crypto/src/lib.rs
@crates/z00z_crypto/src/hash.rs
@crates/z00z_crypto/src/kdf.rs
@crates/z00z_crypto/src/ecdh.rs
@crates/z00z_crypto/src/ecdh_stealth.rs
@crates/z00z_crypto/src/validation.rs
@crates/z00z_crypto/src/commitments.rs
@crates/z00z_crypto/tests/domain_separation.rs
@crates/z00z_crypto/tests/golden_vectors.rs
@crates/z00z_crypto/tests/spec_golden_vectors.rs
@crates/z00z_crypto/tests/test_h2scalar.rs
@crates/z00z_crypto/tests/test_stealth_reference.rs
@crates/z00z_crypto/examples/compute_golden.rs

<interfaces>
Crypto rename contract from the canonical audit:
- Public/root export fallout: `h2s_zk` -> `hash_to_scalar_zk`, `h2scalar_zk` -> `hash_to_scalar_domain`, `derive_ctx` -> `derive_domain_hash`, `derive_k_dh` -> `derive_dh_key`, `ValidationErr` -> `ValidationError`
- Internal compatibility cleanup: `EcdhErr` -> `EcdhError` inside `ecdh_stealth.rs`; do not overstate it as a root-facade contract change unless a real public re-export is confirmed during execution.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename exported crypto symbols and crate facades</name>
  <files>crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/src/hash.rs, crates/z00z_crypto/src/kdf.rs, crates/z00z_crypto/src/ecdh.rs, crates/z00z_crypto/src/ecdh_stealth.rs, crates/z00z_crypto/src/validation.rs, crates/z00z_crypto/src/commitments.rs, crates/z00z_crypto/tests/domain_separation.rs, crates/z00z_crypto/tests/golden_vectors.rs, crates/z00z_crypto/tests/spec_golden_vectors.rs, crates/z00z_crypto/tests/test_h2scalar.rs, crates/z00z_crypto/tests/test_stealth_reference.rs, crates/z00z_crypto/examples/compute_golden.rs</files>
  <action>Apply D-01, D-02, D-05, D-07, and D-08 to the `z00z_crypto` symbol rows in `RENAME-PLAN.md`. Rename the exported hashing, KDF, ECDH, and validation symbols, update root and module `pub use` surfaces, and update every same-crate source, example, doctest, and test call site compiled by `cargo test -p z00z_crypto`. Keep `crates/z00z_crypto/tari/` completely untouched.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_crypto --release</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The renamed crypto exports compile inside `z00z_crypto`, the old ambiguous names are removed from the confirmed same-crate call sites, the vendor subtree is untouched, and any broader-gate failure is classified before closure.</done>
</task>

<task type="auto">
  <name>Task 2: Prefix the audited crypto test functions</name>
  <files>crates/z00z_crypto/src/range_proofs.rs, crates/z00z_crypto/src/commitments.rs, crates/z00z_crypto/src/ecdh.rs, crates/z00z_crypto/src/ecdh_stealth.rs, crates/z00z_crypto/src/aead.rs, crates/z00z_crypto/src/claim/verifier.rs, crates/z00z_crypto/src/claim/proof.rs, crates/z00z_crypto/src/claim/statement.rs, crates/z00z_crypto/src/claim/prover.rs, crates/z00z_crypto/src/validation.rs, crates/z00z_crypto/src/domains.rs, crates/z00z_crypto/src/hash.rs</files>
  <action>Apply the crypto test-function prefix pass from `RENAME-PLAN.md` exactly as listed. Rename every remaining non-`test_` test function in the named files, keep the bodies unchanged, and do not expand scope beyond the audited crypto files.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_crypto --release</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The audited crypto test functions all start with `test_`, the release gate is green after the bootstrap gate, and any broader-gate failure is classified before closure.</done>
</task>

</tasks>

<verification>
Confirm the crypto facade changed only where the audit requires, the vendor boundary stayed intact, and same-crate tests or examples do not retain stale imports before dependent crates update later waves.
</verification>

<success_criteria>
`z00z_crypto` matches its section of `RENAME-PLAN.md`, release validation passes after the mandatory bootstrap gate, and the summary records any consumer crates that still need follow-up import updates.
</success_criteria>

<output>
After completion, create `.planning/phases/019.1-rename/019.1-02-SUMMARY.md`.
</output>
