---
status: testing
phase: 019.1-rename
source:
  - 019.1-01-SUMMARY.md
  - 019.1-02-SUMMARY.md
  - 019.1-03-SUMMARY.md
  - 019.1-04-SUMMARY.md
  - 019.1-05-SUMMARY.md
  - 019.1-06-SUMMARY.md
started: 2026-03-26T15:10:40Z
updated: 2026-03-26T15:10:40Z
---

# Phase 019.1 UAT

## Current Test

number: 1
name: Core Rename Fallout Stays Green
expected: |
  The audited core fallout is clean: the asset leaf helper uses the renamed
  crypto scalar facade name, no phantom core crypto-definition files were
  added, and the current core slice still behaves as a clean consumer of the
  rename.
awaiting: user response

## Tests

### 1. Core Rename Fallout Stays Green

expected: The audited core fallout is clean: the asset leaf helper uses the renamed crypto scalar facade name, no phantom core crypto-definition files were added, and the current core slice still behaves as a clean consumer of the rename.
result: pending

### 2. Crypto Facade Rename Holds Under Release Tests

expected: The public crypto facade exposes the renamed hash, KDF, ECDH, and validation surface, the audited crypto tests stay on the `test_` form, and the crate passes its release test gate without touching the Tari vendor subtree.
result: pending

### 3. Simulator Claim Package Vocabulary Is Consistent

expected: The simulator consumer API, scenario stages, tests, and interoperability example all use the renamed claim-package helper vocabulary, including `claim_leaves`, and no old helper spellings remain inside `crates/z00z_simulator`.
result: pending

### 4. Storage Rename Slice Passes As a Local Crate

expected: The storage rename slice is clean locally: renamed `test_` files resolve through their parent modules and paths, the audited test prefixes are normalized, and the storage release suite passes without storage-local fallout.
result: pending

### 5. Utils Rename Slice Passes As a Local Crate

expected: The utils crate uses `test_` filenames and normalized audited test names across the touched source and integration surfaces, crate-local docs no longer reference stale file names, and the utils release suite passes.
result: pending

### 6. Wallet Rename Slice Passes While Broader Failure Stays Classified

expected: The wallet rename slice is locally clean: renamed test-only files and `#[path]` links resolve, wallet-side storage rename fallout is absorbed in the audited tests, the wallet release suite passes, and the remaining broader failure stays isolated to stale simulator imports in `stage_6.rs` and `stage_8.rs`.
result: pending

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps

None.
