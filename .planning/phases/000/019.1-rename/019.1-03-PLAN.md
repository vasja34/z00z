<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 019.1-rename
plan: 03
type: execute
wave: 5
depends_on:
  - 019.1-01
  - 019.1-02
  - 019.1-04
  - 019.1-05
  - 019.1-06
files_modified:
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_simulator/src/claim_pkg_store.rs
  - crates/z00z_simulator/src/scenario_1/stage_6.rs
  - crates/z00z_simulator/src/scenario_1/stage_8.rs
  - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
  - crates/z00z_simulator/tests/test_stage4_claim_gate.rs
  - crates/z00z_simulator/tests/support/test_stage4_support.rs
autonomous: true
requirements:
  - None
source_plan: .planning/phases/019.1-rename/RENAME-PLAN.md
crate: z00z_simulator
must_haves:
  truths:
    - The simulator helper names from the audit are renamed and all local call sites compile against the new names.
    - Existing simulator test files remain compliant with the `test_` prefix rule.
    - The simulator stage and claim-package flows still run through the exact release-style validation commands required by the project.
  artifacts:
    - crates/z00z_simulator/src/claim_pkg_consumer.rs contains the renamed package and leaves helpers.
    - crates/z00z_simulator/src/claim_pkg_store.rs reflects any local import or call-site fallout from the helper renames.
    - simulator release validation outputs can still be produced with the project-approved commands.
  key_links:
    - claim_pkg_consumer.rs and claim_pkg_store.rs stay aligned on renamed helper calls.
    - simulator tests continue to import the renamed helpers without adding new rename debt.
    - release-style simulator validation remains aligned with wallet_debug_dump expectations.
---

<objective>
Apply the canonical `z00z_simulator` rename audit as the top-level orchestration slice after all lower crates have absorbed their rename fallout.

Purpose: close the phase on the highest-level consumer crate using the final names from crypto, core, storage, utils, and wallets.
Output: renamed simulator claim-package helpers, updated call sites, and release-style simulator validation evidence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/019.1-rename/019.1-CONTEXT.md
@.planning/phases/019.1-rename/019.1-RESEARCH.md
@.planning/phases/019.1-rename/019.1-VALIDATION.md
@.planning/phases/019.1-rename/RENAME-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@crates/z00z_simulator/src/claim_pkg_consumer.rs
@crates/z00z_simulator/src/claim_pkg_store.rs

<interfaces>
Simulator rename contract from the canonical audit:
- `wrap_claim_pkgs` -> `wrap_claim_packages`
- `verify_claim_pkgs` -> `verify_claim_packages`
- `load_claim_pkgs` -> `load_claim_packages`
- `claim_leafs` -> `claim_leaves`
- `load_claim_leafs` -> `load_claim_leaves`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename simulator claim-package helpers</name>
  <files>crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/src/claim_pkg_store.rs</files>
  <action>Apply D-01, D-02, D-07, D-08, and D-10 to the `z00z_simulator` rows in `RENAME-PLAN.md`. Rename the package helper functions and the `leafs` plural exactly as audited, update every local call site and import inside the simulator crate, and keep the scope inside the simulator slice only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The simulator helper renames compile, the corrected `claim_leaves` vocabulary is used consistently, the project-approved simulator validation commands still run, and any unrelated broader-gate failure is clearly classified before closure.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm simulator test naming stays compliant and close the top-level gate</name>
  <files>crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/src/scenario_1/stage_6.rs, crates/z00z_simulator/src/scenario_1/stage_8.rs, crates/z00z_simulator/tests/test_claim_tx_pipeline.rs, crates/z00z_simulator/tests/test_stage4_claim_gate.rs, crates/z00z_simulator/tests/support/test_stage4_support.rs</files>
  <action>Use the compliant-file section of `RENAME-PLAN.md` as a no-change audit gate. Spot-check `crates/z00z_simulator/src/claim_pkg_consumer.rs`, `crates/z00z_simulator/src/scenario_1/stage_6.rs`, `crates/z00z_simulator/src/scenario_1/stage_8.rs`, `crates/z00z_simulator/tests/test_claim_tx_pipeline.rs`, `crates/z00z_simulator/tests/test_stage4_claim_gate.rs`, and `crates/z00z_simulator/tests/support/test_stage4_support.rs` to confirm the filenames and sampled inline tests already meet the `test_` rule. Update only imports or references broken by Task 1, and do not add gratuitous renames outside the audited helper surface.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The simulator crate remains fully compliant with the rename audit, no extra naming debt is left behind, and release-style validation is green or any unrelated failure is clearly classified.</done>
</task>

</tasks>

<verification>
Confirm the simulator absorbed all downstream rename fallout last, using the exact release-style commands expected by the repo for Scenario 1 validation.
</verification>

<success_criteria>
`z00z_simulator` matches its section of `RENAME-PLAN.md`, helper names are updated consistently, and the crate passes the required release-style simulator gates after the bootstrap gate.
</success_criteria>

<output>
After completion, create `.planning/phases/019.1-rename/019.1-03-SUMMARY.md`.
</output>
