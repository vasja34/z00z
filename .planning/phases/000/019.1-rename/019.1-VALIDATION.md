---
phase: 019.1
slug: rename
status: audited
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-26
updated: 2026-03-26
source:
	- 019.1-01-SUMMARY.md
	- 019.1-02-SUMMARY.md
	- 019.1-03-SUMMARY.md
	- 019.1-04-SUMMARY.md
	- 019.1-05-SUMMARY.md
	- 019.1-06-SUMMARY.md
---

<!-- markdownlint-disable MD001 MD022 MD032 MD033 MD041 MD047 -->

# Phase 019.1 — Validation Strategy

📌 This validation record was reconstructed from the six phase summaries and refreshed after the later simulator cleanup removed the stale import blocker that was still recorded in `019.1-03-SUMMARY.md` at the time that summary was written.

📌 The record was refreshed again after a late `019.1-01` audit found and repaired the remaining `z00z_core` assets and genesis parent-module rename tail, then re-ran the bootstrap and crate-local release gates.

📌 The record was refreshed one more time after a late `019.1-02` audit found and repaired the remaining current-tree crypto test-file rename tail in the exact plan-listed files, then re-ran the crypto, simulator, scenario, and workspace release gates.

📌 Phase `019.1-rename` is a rename-only phase. Existing crate and workspace release gates provide the required automated verification coverage; no new phase-specific test files were required to reach Nyquist compliance.

---

## Test Infrastructure

| Property | Value |
| -------- | ----- |
| **Framework** | Rust workspace via `cargo test` |
| **Config file** | `Cargo.toml` |
| **Quick run command** | `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` |
| **Full suite command** | `cargo test --release --features test-fast --features wallet_debug_dump` |
| **Supplemental commands** | `cargo test -p z00z_core --release`; `cargo test -p z00z_crypto --release`; `cargo test -p z00z_storage --release`; `cargo test -p z00z_utils --release`; `cargo test -p z00z_wallets --release`; `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump` |
| **Estimated runtime** | variable — release-style gates |

---

## Sampling Rate

- 📌 After every crate-slice task closure: run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
- 📌 After every crate-local rename slice: run the matching crate release gate, for example `cargo test -p z00z_storage --release`.
- 📌 Before final phase sign-off: run `cargo test --release --features test-fast --features wallet_debug_dump`.
- 📌 Before simulator sign-off: run both `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump` and `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump`.
- 📌 Max feedback latency: do not advance beyond a crate slice without at least one green automated gate for that slice.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
| ------- | ---- | ---- | ----------- | --------- | ----------------- | ----------- | ------ |
| 019.1-BOOT | all | mixed | Mandatory bootstrap gate completes before broader validation | bootstrap | `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` | ✅ | ✅ green |
| 019.1-01 | 01 | 2 | `z00z_core` absorbs same-crate crypto rename fallout, closes current-tree `test_` path or function debt, and does not invent phantom core crypto files | crate release | `cargo test -p z00z_core --release` | ✅ | ✅ green |
| 019.1-02 | 02 | 1 | `z00z_crypto` facade and same-crate tests compile against the renamed public surface | crate release | `cargo test -p z00z_crypto --release` | ✅ | ✅ green |
| 019.1-03A | 03 | 5 | `z00z_simulator` claim-package helper rename fallout compiles under the exact simulator release test gate | crate release | `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump` | ✅ | ✅ green |
| 019.1-03B | 03 | 5 | Scenario 1 still runs after the rename fallout closes across simulator dependencies | scenario run | `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump` | ✅ | ✅ green |
| 019.1-04 | 04 | 3 | `z00z_storage` renamed public contracts, test-only file paths, and `test_` functions stay crate-local green | crate release | `cargo test -p z00z_storage --release` | ✅ | ✅ green |
| 019.1-05 | 05 | 1 | `z00z_utils` renamed test files and audited logger or os-hardening test prefixes stay green | crate release | `cargo test -p z00z_utils --release` | ✅ | ✅ green |
| 019.1-06 | 06 | 4 | `z00z_wallets` repaired `#[path]` declarations and audited rename fallout stay green | crate release | `cargo test -p z00z_wallets --release` | ✅ | ✅ green |
| 019.1-WIDE | all | final | Workspace-wide downstream rename fallout is closed after simulator stale-import cleanup | workspace release | `cargo test --release --features test-fast --features wallet_debug_dump` | ✅ | ✅ green |

📌 Status legend: `✅ green` means automated evidence exists in the phase summaries or in the post-summary validation refresh performed during this audit.

---

## Wave 0 Requirements

📌 Existing infrastructure covers all phase requirements.

📌 No new test files were generated for phase `019.1-rename` because the phase changes are rename-only and the existing bootstrap, crate-local release, simulator release, and workspace release gates already exercise the touched surfaces.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
| -------- | ----------- | ---------- | ----------------- |
| Rename scope stays inside `RENAME-PLAN.md` recommendations | Verify each crate wrapper only applies the listed rename recommendations | Scope control is a review property, not a runtime assertion | Review the six plan summaries against `RENAME-PLAN.md` and confirm touched files only absorb audited rename fallout |
| `crates/z00z_crypto/tari/` remains untouched | Preserve the vendor boundary | Vendor-boundary preservation is a repository diff property, not a crate test | Confirm plan 02 summary records the Tari subtree as untouched and that no touched-file evidence points under `crates/z00z_crypto/tari/` |
| No behavior change beyond rename fallout | Verify the phase is rename-only and behavior-preserving | Automated gates show green behavior, but intent preservation still needs human judgment | Review touched-file summaries and confirm edits are limited to symbol, path, file-name, and `test_` prefix fallout while all release gates remain green |
| Slash-command review evidence | `/GSD-Review-Tasks-Execution --yolo` requirement from plans | The slash command was not invokable from the terminal-only environment used during execution | Accept the summaries that either record substitute review passes or explicitly classify the slash command as unavailable in this environment |

---

## Validation Gates

- ✅ Verify each crate wrapper only applies the rename recommendations already listed in `RENAME-PLAN.md`.
- ✅ Verify public API renames update module facades and re-exports where required.
- ✅ Verify test-only file renames keep `test_` prefix rules intact.
- ✅ Verify the late `z00z_core` assets or genesis closeout leaves no remaining audited non-`test_` parent-module file references or current-tree test-function debt.
- ✅ Verify `crates/z00z_crypto/tari/` remains untouched.
- ✅ Verify no behavior changes are introduced by the rename-only phase.

---

## Validation Sign-Off

- [x] All tasks have automated verification evidence or explicit manual-only classification.
- [x] Sampling continuity preserved: no crate slice closed without an automated gate.
- [x] Existing infrastructure covers all phase requirements; no Wave 0 scaffolding is needed.
- [x] No watch-mode validation paths were used.
- [x] Final workspace-wide release gate is green after simulator stale-import cleanup.
- [x] `nyquist_compliant: true` is set in frontmatter.

**Approval:** approved 2026-03-26

---

## Validation Audit 2026-03-26

| Metric | Count |
| ------ | ----- |
| Gaps found | 6 |
| Resolved | 6 |
| Escalated | 0 |
| New test files required | 0 |

📌 Audit outcomes:

- Rebuilt the validation artifact from plan `01` through `06` summaries into a Nyquist-style coverage map.
- Added missing explicit automated evidence for `cargo test -p z00z_core --release` so the core slice no longer depends only on broad-gate inference.
- Refreshed the core slice again after the late `019.1-01` audit repaired the remaining assets and genesis parent-module rename tail and confirmed no remaining audited non-`test_` test-function names in the current tree.
- Refreshed the crypto slice again after the late `019.1-02` audit repaired the remaining plan-listed crypto test-file rename tail and confirmed `cargo test -p z00z_crypto --release` stayed green.
- Refreshed simulator and workspace gate status after the later cleanup that removed the stale import blocker in `stage_6.rs` and `stage_8.rs`.
- Confirmed the simulator release gate is green on a serial rerun after one transient parallel-only rustdoc crate-resolution race, so the final evidence uses the stable rerun rather than the noisy concurrent pass.
- Classified vendor-boundary, rename-only intent, and slash-command review evidence as manual-only checks instead of leaving them implicit.
