---
phase: 019.1-rename
plan: 01
subsystem: z00z_core
tags:
  - rename
  - core
  - tests
  - verification
dependency-graph:
  requires:
    - 019.1-02
    - 019.1-05
  provides:
    - z00z_core same-crate crypto fallout cleanup
    - z00z_core asset leaf import normalization
  affects:
    - downstream rename waves in z00z_storage, z00z_wallets, and z00z_simulator
tech-stack:
  added: []
  patterns:
    - same-crate fallout
    - facade import cleanup
    - current-tree validation
key-files:
  created: []
  modified:
    - crates/z00z_core/src/assets/leaf.rs
    - crates/z00z_core/tests/assets/mod.rs
    - crates/z00z_core/tests/genesis/mod.rs
decisions:
  - Treat `z00z_core` as current-tree fallout only and do not invent phantom crypto-definition files.
  - Keep the downstream rename waves separate so the core slice stays scoped to real consumer fallout.
  - Apply the late audit repair only to the current-tree assets and genesis suites declared by their parent `mod.rs` files.
metrics:
  completed_date: 2026-03-26
  task_commits:
    - 2e11812e
---

# Phase 019.1 Plan 01: z00z_core Rename Summary

📌 `z00z_core` now absorbs the renamed crypto scalar helper in `src/assets/leaf.rs`, and the late audit refresh closed the remaining current-tree assets and genesis `test_` rename tail without inventing phantom core crypto files.

## Outcome

✅ The core asset leaf helper now uses `hash_to_scalar_zk` instead of the old exported name, and the surviving same-crate crypto fallout remains limited to real current-tree consumers.

✅ The late audit found one missed `019.1-01` tail: remaining non-prefixed test-only files still declared from `crates/z00z_core/tests/assets/mod.rs` and `crates/z00z_core/tests/genesis/mod.rs`. That tail is now closed by renaming the current-tree files to `test_*.rs` and updating the parent module declarations to resolve them via `#[path = "test_*.rs"]`.

✅ A targeted current-tree scan found no remaining non-`test_` test-function names in the audited `z00z_core` assets or genesis surfaces covered by this plan.

✅ Fresh validation evidence after the late audit repair is green: `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`, `cargo test -p z00z_core --release`, and the previously recorded workspace-wide release gate `cargo test --release --features test-fast --features wallet_debug_dump`.

## Verification

- ✅ `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- ✅ `cargo test -p z00z_core --release`
- ✅ `cargo test --release --features test-fast --features wallet_debug_dump`
- ✅ `crates/z00z_core/src/assets/leaf.rs` now references the renamed crypto facade symbol.
- ✅ `crates/z00z_core/tests/assets/mod.rs` and `crates/z00z_core/tests/genesis/mod.rs` now resolve the renamed current-tree test files.
- ✅ The current-tree assets and genesis rename tail no longer references the old non-`test_` filenames through their parent modules.
- ✅ A targeted scan found no remaining non-`test_` test-function names in the audited core surfaces.
- ✅ No new `z00z_core` crypto-definition files were created.
- ✅ The core slice remained limited to current-tree fallout.

## Deviations from Plan

📌 A late audit found that Task 2 had not fully closed the parent-module-declared assets and genesis rename tail. The missing file renames and `#[path]` updates were applied during this refresh, and the plan is now fully aligned with the current tree.

## Known Stubs

None.

## Self-Check

PASSED
