---
phase: 019.1-rename
plan: 02
subsystem: z00z_crypto
tags:
  - rename
  - crypto
  - tests
  - verification
  - release
dependency-graph:
  requires: []
  provides:
    - z00z_crypto exported symbol rename fallout
    - z00z_crypto test-function prefix normalization
  affects:
    - downstream rename waves in z00z_core, z00z_storage, z00z_wallets, and z00z_simulator
tech-stack:
  added: []
  patterns:
    - facade rename
    - symbol cleanup
    - test_ prefix
    - vendor-boundary preservation
key-files:
  created: []
  modified:
    - crates/z00z_crypto/src/lib.rs
    - crates/z00z_crypto/src/hash.rs
    - crates/z00z_crypto/src/kdf.rs
    - crates/z00z_crypto/src/ecdh.rs
    - crates/z00z_crypto/src/ecdh_stealth.rs
    - crates/z00z_crypto/src/validation.rs
    - crates/z00z_crypto/src/commitments.rs
    - crates/z00z_crypto/src/aead.rs
    - crates/z00z_crypto/src/domains.rs
    - crates/z00z_crypto/src/range_proofs.rs
    - crates/z00z_crypto/src/claim/verifier.rs
    - crates/z00z_crypto/src/claim/proof.rs
    - crates/z00z_crypto/src/claim/statement.rs
    - crates/z00z_crypto/src/claim/prover.rs
    - crates/z00z_crypto/examples/compute_golden.rs
    - crates/z00z_crypto/tests/test_domain_separation.rs
    - crates/z00z_crypto/tests/test_golden_vectors.rs
    - crates/z00z_crypto/tests/test_spec_golden_vectors.rs
    - crates/z00z_crypto/tests/test_h2scalar.rs
    - crates/z00z_crypto/tests/test_stealth_reference.rs
decisions:
  - Keep the Tari vendor subtree untouched and treat `EcdhError` as an internal compatibility cleanup rather than a public facade change.
  - Record downstream rename waves as follow-up work instead of expanding the crypto-root scope.
metrics:
  completed_date: 2026-03-26
  task_commits:
    - 6c7fef5b
---

# Phase 019.1 Plan 02: z00z_crypto Rename Summary

📌 `z00z_crypto` now exports the renamed hash, KDF, ECDH, and validation surface from the public facade, and the audited crypto test files use the `test_` prefix without changing behavior.

## Outcome

✅ Task 1 renamed the exported crypto symbols and facade re-exports in `lib.rs`, updated the same-crate source, example, and test call sites, and preserved the read-only Tari boundary.

✅ Task 2 normalized the audited crypto test functions to `test_` in the files listed by the plan, including the source-local test surfaces under `aead`, `claim`, `domains`, `hash`, `kdf`, `range_proofs`, and `validation`.

✅ The late closeout audit found one remaining current-tree filename tail in the exact plan-02 crypto test list. The three audited integration files were renamed to `test_domain_separation.rs`, `test_golden_vectors.rs`, and `test_spec_golden_vectors.rs`, and crate-local plus broad release gates were re-run after that repair.

✅ The crypto crate itself passes `cargo test -p z00z_crypto --release`, and the broader gates passed as well: `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` and `cargo test --release --features test-fast --features wallet_debug_dump`.

## Verification

- ✅ `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- ✅ `cargo test -p z00z_crypto --release`
- ✅ `cargo test --release --features test-fast --features wallet_debug_dump`
- ✅ The renamed crypto facade no longer exposes the audited ambiguous names in the touched public surface.
- ✅ `crates/z00z_crypto/tari/` remained untouched.
- ✅ Search results after the rename still show downstream consumer waves needing their own follow-up updates in `z00z_core`, `z00z_storage`, `z00z_wallets`, and `z00z_simulator`.

## Deviations from Plan

### Late Closeout Repair

- The initial plan-02 summary closed too early while three explicit plan-listed crypto integration files still used non-`test_` filenames in the current tree. That tail was repaired during the final audit and revalidated with fresh release-style gates.

## Known Stubs

None.

## Self-Check

PASSED
