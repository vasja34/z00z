<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 019.1-rename
plan: 01
type: execute
wave: 2
depends_on:
  - 019.1-02
  - 019.1-05
files_modified:
  - crates/z00z_core/src/assets/leaf.rs
  - crates/z00z_core/src/assets/wire.rs
  - crates/z00z_core/tests/assets/mod.rs
  - crates/z00z_core/src/assets/test_wire.rs
  - crates/z00z_core/tests/assets/test_assets.rs
  - crates/z00z_core/tests/assets/test_metadata_limits.rs
  - crates/z00z_core/tests/assets/test_property_based.rs
  - crates/z00z_core/tests/genesis/mod.rs
  - crates/z00z_core/tests/genesis/test_genesis.rs
autonomous: true
requirements:
  - None
source_plan: .planning/phases/019.1-rename/RENAME-PLAN.md
crate: z00z_core
must_haves:
  truths:
    - z00z_core does not invent new crypto-definition files; it only absorbs same-crate fallout from the renamed z00z_crypto APIs.
    - The confirmed same-crate core consumers of renamed crypto APIs compile against the new names after 019.1-02 lands.
    - The targeted z00z_core test-only files use the test_ prefix and their parent module declarations resolve correctly.
    - The surviving core test-function debt is resolved only against files that actually exist in the current z00z_core tree.
  artifacts:
    - crates/z00z_core/src/assets/leaf.rs contains the renamed crypto import or call-site fallout that survives after the z00z_crypto pass.
    - crates/z00z_core/src/assets/wire.rs plus the tests assets/genesis entry files point at the renamed test modules.
    - no new z00z_core hash, kdf, ecdh, ecdh_stealth, or validation source files are created during this plan.
  key_links:
    - crates/z00z_core/src/assets/leaf.rs stays aligned with the renamed z00z_crypto hashing API.
    - crates/z00z_core/tests/assets/mod.rs resolves renamed integration files under tests/assets/.
    - crates/z00z_core/src/assets/wire.rs plus the tests/assets and tests/genesis parents resolve renamed integration files.
---

<objective>
Apply the canonical `z00z_core` rename audit as the first domain-layer consumer after the root crates settle.

Purpose: absorb the renamed `z00z_crypto` surface inside real `z00z_core` consumer files before storage, wallets, and simulator update their imports.
Output: corrected core import fallout, renamed test-module file names, and normalized `test_`-prefixed tests with no behavior change.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/019.1-rename/019.1-CONTEXT.md
@.planning/phases/019.1-rename/019.1-RESEARCH.md
@.planning/phases/019.1-rename/019.1-VALIDATION.md
@.planning/phases/019.1-rename/RENAME-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@crates/z00z_core/src/assets/leaf.rs
@crates/z00z_core/src/assets/wire.rs
@crates/z00z_core/tests/assets/mod.rs
@crates/z00z_core/tests/genesis/mod.rs

<interfaces>
Current codebase-fit constraints for the canonical core slice:
- `RENAME-PLAN.md` still carries crypto-style symbol rows under `z00z_core`, but the current `z00z_core` tree does not contain `src/hash.rs`, `src/kdf.rs`, `src/ecdh.rs`, `src/ecdh_stealth.rs`, or `src/validation.rs`.
- The currently confirmed same-crate fallout lives in `crates/z00z_core/src/assets/leaf.rs`, which imports `h2s_zk` from `z00z_crypto`.
- The executor must not create phantom core crypto-definition files; any additional current-tree core consumers found by search are valid fallout targets, but ownership of crypto definitions remains in `z00z_crypto`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Absorb same-crate core fallout from renamed crypto APIs</name>
  <files>crates/z00z_core/src/assets/leaf.rs</files>
  <action>Apply D-01, D-02, D-07, and D-08 to the real current-tree `z00z_core` fallout from the `z00z_crypto` rename set. First confirm that the canonical core symbol rows no longer map to local source definitions in the current core tree; do not create `src/hash.rs`, `src/kdf.rs`, `src/ecdh.rs`, `src/ecdh_stealth.rs`, or `src/validation.rs`. Update only surviving `z00z_core` imports or call sites found by search, starting with `crates/z00z_core/src/assets/leaf.rs`, and record any source-audit drift in the task summary.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_core --release</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The current `z00z_core` tree compiles against the renamed crypto APIs, no phantom core crypto modules were introduced, any source-audit drift is recorded, and any broader-gate failure is classified before the task closes.</done>
</task>

<task type="auto">
  <name>Task 2: Rename core test-only files and parent module links</name>
  <files>crates/z00z_core/src/assets/wire.rs, crates/z00z_core/src/assets/test_wire.rs, crates/z00z_core/tests/assets/mod.rs, crates/z00z_core/tests/assets/test_assets.rs, crates/z00z_core/tests/assets/test_metadata_limits.rs, crates/z00z_core/tests/assets/test_property_based.rs, crates/z00z_core/tests/genesis/mod.rs, crates/z00z_core/tests/genesis/test_genesis.rs</files>
  <action>Apply D-03, D-04, and D-07 to the core file-rename rows in `RENAME-PLAN.md`. Rename the targeted test-only files to the `test_` form, then apply the same prefix rule to every remaining non-prefixed test-only file declared from `crates/z00z_core/tests/assets/mod.rs` and `crates/z00z_core/tests/genesis/mod.rs`. Update every parent `mod` or path declaration that points at the renamed files, and do not add new test logic.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_core --release</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>The renamed core test files are discoverable by Cargo through their updated parent modules, the batch rule has been applied to the current-tree assets and genesis suites, the old filenames are no longer referenced, and any broader-gate failure is classified before the task closes.</done>
</task>

<task type="auto">
  <name>Task 3: Close the surviving core test-function debt without inventing files</name>
  <files>crates/z00z_core/src/assets/leaf.rs, crates/z00z_core/tests/assets/mod.rs, crates/z00z_core/tests/genesis/mod.rs</files>
  <action>Apply the core `test_` prefix pass only against files that actually exist in the current `z00z_core` tree. Use `crates/z00z_core/tests/assets/mod.rs`, `crates/z00z_core/tests/genesis/mod.rs`, and a targeted search of `crates/z00z_core/src/**` to enumerate the real remaining non-`test_` functions, then rename those functions without changing bodies. Treat the crypto-style file list in `RENAME-PLAN.md` as source-audit drift to record, not as instructions to create new core modules.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_core --release</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution --yolo (run at least 3 times and continue until 2 consecutive runs report no significant issues; fix every warning and issue between runs)</automated>
  </verify>
  <done>All remaining current-tree core test functions surfaced by the audit closeout start with `test_`, the crate release gate is green, and any broader-gate failure is classified before the task closes.</done>
</task>

</tasks>

<verification>
Confirm `z00z_core` stays behavior-preserving, absorbs only real same-crate fallout from the crypto rename, and does not invent new ownership of crypto symbol definitions.
</verification>

<success_criteria>
`z00z_core` closes the current-tree fallout implied by its section of `RENAME-PLAN.md`, local release tests pass after the mandatory bootstrap gate, and the summary clearly records any source-audit drift or downstream crates that must absorb renamed imports later.
</success_criteria>

<output>
After completion, create `.planning/phases/019.1-rename/019.1-01-SUMMARY.md`.
</output>
