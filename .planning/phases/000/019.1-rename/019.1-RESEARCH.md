<!-- markdownlint-disable MD001 MD022 MD032 MD033 MD047 -->
# Phase 019.1: 019.1-rename - Research

**Status:** Existing audit only; no external research required

## Summary

No external research is required for this phase. The rename recommendations are already enumerated in `.planning/phases/019.1-rename/RENAME-PLAN.md`, and this phase only needs crate-scoped GSD wrappers that preserve those recommendations as-is.

## Codebase Findings

- `z00z_crypto` and `z00z_utils` are the dependency roots for this rename phase.
- `z00z_core` consumes `z00z_crypto` and `z00z_utils`, so it should land after those roots.
- `z00z_storage` depends on `z00z_core`, `z00z_crypto`, and `z00z_utils`.
- `z00z_wallets` depends on `z00z_core`, `z00z_crypto`, `z00z_storage`, and `z00z_utils`.
- `z00z_simulator` sits at the top of the stack and should absorb downstream rename fallout last.

## Verification Findings

- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` must be the mandatory fail-fast gate for every auto task.
- The broader `cargo test --release --features test-fast --features wallet_debug_dump` gate is still required when the touched crate participates in the wallet or simulator surface.
- `.planning/STATE.md` already records a pre-existing compile blocker in `crates/z00z_wallets/src/lib.rs`; plan execution must distinguish that blocker from new rename regressions.
- The execution review loop must include `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least three times, with two consecutive clean runs before task closure.

## Research Boundaries

- Do not widen scope beyond naming cleanup.
- Do not change the recommendations in `RENAME-PLAN.md`.
- Do not treat vendor code under `crates/z00z_crypto/tari/` as editable.
- Do not collapse the phase into one monolithic execution plan; keep one executable wrapper per crate.
