<!-- markdownlint-disable MD007 MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->
---
phase: 032-crypto-audit-scenario-1
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/032-crypto-audit-scenario-1/032-SEMANTIC-FREEZE.md
  - crates/z00z_wallets/tests/test_scenario1_semantics.rs
  - crates/z00z_wallets/src/core/address/stealth_scan_support.rs
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/core/stealth/output_build.rs
  - crates/z00z_wallets/src/core/stealth/output_validator.rs
  - crates/z00z_wallets/src/core/address/stealth_card.rs
  - crates/z00z_wallets/src/core/address/stealth_request.rs
  - crates/z00z_wallets/src/core/address/stealth_trust.rs
autonomous: true
requirements:
  - PH32-SEM
  - PH32-HONEST
must_haves:
  truths:
    - No Phase 032 code change can proceed on ambiguous `leaf_ad_id`, `s_out`, receiver-card, or request-tag semantics.
    - Scenario 1 trust language is frozen before remediation so later plans cannot silently overclaim sender ignorance, trustless spend verification, or STARK/FRI support.
    - Receiver-card validation, TOFU/pinning rules, rotation handling, and request-bound tag behavior become accepted-flow rules instead of documentation-only suggestions.
    - Wallet-side tests prove the frozen semantics instead of leaving them in prose only.
  artifacts:
    - path: .planning/phases/032-crypto-audit-scenario-1/032-SEMANTIC-FREEZE.md
      provides: Canonical semantic freeze and trust-language contract for Phase 032.
    - path: crates/z00z_wallets/tests/test_scenario1_semantics.rs
      provides: Regression coverage for `leaf_ad_id`, `s_out`, receiver binding, and request/card divergence.
  key_links:
    - from: .planning/phases/032-crypto-audit-scenario-1/032-CONTEXT.md
      to: .planning/phases/032-crypto-audit-scenario-1/032-SEMANTIC-FREEZE.md
      via: Wave 0 blocked-status and semantic-freeze gate
      pattern: Wave 0|Wave 1|leaf_ad_id|s_out|request-bound tag16
    - from: .planning/phases/032-crypto-audit-scenario-1/prompt.txt
      to: crates/z00z_wallets/tests/test_scenario1_semantics.rs
      via: umbrella-phase semantic track made executable
      pattern: stealth|ownership|receiver_secret|tag16
---

<objective>
Freeze the Scenario 1 semantic contract and truth-language before any proof-boundary remediation lands.

Purpose: make later claim, spend, checkpoint, and hygiene work execute against one canonical set of semantics instead of drifting docs, tests, and code comments.
Output: one semantic-freeze artifact and one wallet-side regression suite that pins the frozen semantics.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/032-crypto-audit-scenario-1/032-CONTEXT.md
@.planning/phases/032-crypto-audit-scenario-1/032-TODO.md
@.planning/phases/032-crypto-audit-scenario-1/prompt.txt
@crates/z00z_wallets/src/core/address/stealth_scan_support.rs
@crates/z00z_wallets/src/core/stealth/output.rs
@crates/z00z_wallets/src/core/stealth/output_build.rs
@crates/z00z_wallets/src/core/stealth/output_validator.rs
@crates/z00z_wallets/src/core/address/stealth_card.rs
@crates/z00z_wallets/src/core/address/stealth_request.rs
@crates/z00z_wallets/src/core/address/stealth_trust.rs
</context>

If execution requires checkpoint commits while semantic freeze evidence is being stabilized, use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the canonical Scenario 1 semantic freeze artifact</name>
  <files>.planning/phases/032-crypto-audit-scenario-1/032-SEMANTIC-FREEZE.md, crates/z00z_wallets/src/core/address/stealth_scan_support.rs, crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/core/stealth/output_build.rs, crates/z00z_wallets/src/core/stealth/output_validator.rs, crates/z00z_wallets/src/core/address/stealth_card.rs, crates/z00z_wallets/src/core/address/stealth_request.rs, crates/z00z_wallets/src/core/address/stealth_trust.rs</files>
  <action>Create `032-SEMANTIC-FREEZE.md` as the single Wave 0 source of truth for `leaf_ad_id`, `s_out`, receiver-card/request binding, request-bound `tag16`, TOFU/pinning semantics, mandatory request/card validation, and the exact line between wallet-local ownership rules and public proof claims. Update the listed wallet modules only as needed to align exported docs/comments/helper names with the freeze: sender knowledge of `s_out` must be stated honestly, receiver-secret-gated ownership must be stated as wallet-local until a later verifier proves it, request/card validation language must stop implying stronger guarantees than the current code enforces, and `output_build.rs` plus `output_validator.rs` must be identified as the accepted-flow constructors/validators for request-bound behavior.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "leaf_ad_id|s_out|receiver_secret|tag16|trustless|STARK|FRI" .planning/phases/032-crypto-audit-scenario-1/032-SEMANTIC-FREEZE.md crates/z00z_wallets/src/core/address crates/z00z_wallets/src/core/stealth -g '*.md' -g '*.rs'</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`032-SEMANTIC-FREEZE.md` exists, the wallet-facing semantic seams match it, and unsupported trust language is no longer implicit in the touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the frozen semantics in wallet-side regression tests</name>
  <files>crates/z00z_wallets/tests/test_scenario1_semantics.rs, crates/z00z_wallets/src/core/address/stealth_scan_support.rs, crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/core/stealth/output_build.rs, crates/z00z_wallets/src/core/stealth/output_validator.rs, crates/z00z_wallets/src/core/address/stealth_card.rs, crates/z00z_wallets/src/core/address/stealth_request.rs, crates/z00z_wallets/src/core/address/stealth_trust.rs</files>
  <action>Add a focused regression suite that proves the frozen semantics instead of narrating them: `leaf_ad_id` is the canonical decrypt-associated-data boundary, sender-side `s_out` knowledge remains true, foreign-card/request mismatch fails accepted-flow validation, TOFU/pinning and rotation/mismatch handling are explicit, payment-request mode is the normal privacy path, and request-bound tags diverge from card-bound behavior in the approved way. Tighten only the smallest constructor/validator seams needed in `output_build.rs` and `output_validator.rs`; do not start claim-path or checkpoint rewrites in this plan.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --features test-fast test_scenario1_semantics -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The regression suite fails if `leaf_ad_id`, `s_out`, receiver-card/request validation, TOFU/pinning rules, or request-bound tag semantics drift from the frozen contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wallet semantics -> docs/tests | Ambiguous semantics can later justify insecure overclaims or wrong verifier inputs. |
| receiver-card/request policy -> ownership language | Misstated policy can imply public guarantees that only wallet-local code enforces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-032-01 | Tampering | semantic definitions | mitigate | Freeze `leaf_ad_id`, `s_out`, and receiver binding in one artifact plus regression tests. |
| T-032-02 | Spoofing | trust-language surfaces | mitigate | Remove or rewrite unsupported trustless/sender-ignorance/STARK claims in touched seams. |
| T-032-03 | Repudiation | future plan acceptance | mitigate | Make Wave 0 artifact the required citation target for later claim/spend/checkpoint plans. |
</threat_model>

<verification>
Wave 0 closes only when the semantic freeze is explicit, tested, and strong enough that later plans do not have to rediscover or reinterpret the same boundaries.
</verification>

<success_criteria>
Phase 032 has one authoritative semantic contract, and later remediation work cannot silently re-open `leaf_ad_id`, `s_out`, or trust-language ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/032-crypto-audit-scenario-1/032-01-SUMMARY.md`
</output>