<!-- markdownlint-disable MD007 MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->
---
phase: 032-crypto-audit-scenario-1
plan: "05"
type: execute
wave: 4
depends_on:
  - 032-03
  - 032-04
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
  - crates/z00z_storage/src/checkpoint/build.rs
  - crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs
  - crates/z00z_storage/src/checkpoint/artifact_final.rs
  - crates/z00z_storage/src/checkpoint/codec.rs
  - crates/z00z_storage/src/checkpoint/store_fs.rs
  - crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
autonomous: true
requirements:
  - PH32-CHECKPOINT
must_haves:
  truths:
    - Stage 6 and Stage 11 no longer accept checkpoints or bundles through `PassProof`/`NoSpent` placeholder logic.
    - Checkpoint draft, finalize, and load acceptance are tied to authoritative spent-set and proof semantics instead of non-empty bytes.
    - Integration tests fail if a placeholder proof or empty spent-set contract tries to pass as valid.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
      provides: Placeholder-free checkpoint/bundle verification path.
    - path: crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
      provides: End-to-end rejection coverage for placeholder checkpoint acceptance.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
      to: crates/z00z_storage/src/checkpoint/build.rs
      via: authoritative checkpoint proof and spent-set semantics
      pattern: PassProof|NoSpent|cp_proof|spent
    - from: crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
      to: crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
      via: final apply path honoring the new checkpoint gate
      pattern: stage_11|apply|checkpoint
---

<objective>
Remove placeholder checkpoint acceptance and require authoritative proof/spent-set semantics end to end.

Purpose: close the Phase 032 gap where Stage 6/11 can still accept non-empty proof bytes and placeholder spent-state semantics as if they were real verification.
Output: one placeholder-free checkpoint acceptance path and end-to-end rejection coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/032-crypto-audit-scenario-1/032-CONTEXT.md
@.planning/phases/000/028-crypto-audit-storage/028-CONTEXT.md
@crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs
@crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
@crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
@crates/z00z_storage/src/checkpoint/build.rs
@crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs
@crates/z00z_storage/src/checkpoint/artifact_final.rs
@crates/z00z_storage/src/checkpoint/codec.rs
@crates/z00z_storage/src/checkpoint/store_fs.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove placeholder checkpoint proof and spent-set acceptance paths</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs, crates/z00z_storage/src/checkpoint/build.rs, crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs, crates/z00z_storage/src/checkpoint/artifact_final.rs, crates/z00z_storage/src/checkpoint/codec.rs, crates/z00z_simulator/tests/test_checkpoint_acceptance.rs</files>
  <action>Delete or fence `PassProof`, `NoSpent`, and any equivalent non-empty-bytes / placeholder-spent success path from checkpoint and bundle validation. Replace them with the authoritative proof/spent-set contract Phase 032 allows, including explicit rejection when proof bytes exist without valid checkpoint semantics or when spent-set state is placeholder-only. Extend this from draft construction into `artifact_proof_draft.rs`, `artifact_final.rs`, and `codec.rs` so stored proof objects and decoded artifacts also fail closed when they do not match the canonical proof transcript.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_checkpoint_acceptance -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Checkpoint/bundle verification can no longer pass through `PassProof`, `NoSpent`, or equivalent placeholder logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the new checkpoint gate in Stage 11 apply flow</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_11_apply.rs, crates/z00z_storage/src/checkpoint/store_fs.rs, crates/z00z_simulator/tests/test_checkpoint_acceptance.rs</files>
  <action>Update the final apply path so Stage 11 rejects bundles that fail the new checkpoint/spent-set gate instead of inheriting old placeholder acceptance. Extend the same rejection semantics through storage finalize/load in `store_fs.rs`, so replayed, malformed, synthetic, or transcript-mismatched checkpoint artifacts fail closed after persistence as well. Add integration coverage for valid checkpoint flow, non-empty invalid proof flow, missing spent-set evidence, stale/replayed checkpoint state, and load-time proof mismatch.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_checkpoint_acceptance -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Stage 11 apply honors the authoritative checkpoint gate and rejects placeholder or replay-style acceptance paths.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle/checkpoint bytes -> acceptance | Placeholder proof/spent semantics can let invalid state transition as valid. |
| Stage 6 -> Stage 11 apply | Even if an earlier gate tightens, final apply may still re-accept stale placeholder state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-032-13 | Elevation of privilege | Stage 6 bundle lane | mitigate | Remove placeholder proof and spent-state success paths from bundle validation. |
| T-032-14 | Tampering | checkpoint bytes | mitigate | Require authoritative proof/spent semantics instead of non-empty byte acceptance. |
| T-032-15 | Replay | Stage 11 apply | mitigate | Add replay/stale-state rejection tests to final apply flow. |
</threat_model>

<verification>
Wave 4 closes only when checkpoint and spent-set acceptance remain authoritative all the way through Stage 11 apply.
</verification>

<success_criteria>
Scenario 1 checkpoints are accepted only when backed by the authoritative proof/spent-state contract, never by placeholder success paths.
</success_criteria>

<output>
After completion, create `.planning/phases/032-crypto-audit-scenario-1/032-05-SUMMARY.md`
</output>