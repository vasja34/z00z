<!-- markdownlint-disable MD007 MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->
---
phase: 032-crypto-audit-scenario-1
plan: "04"
type: execute
wave: 3
depends_on:
  - 032-03
files_modified:
  - crates/z00z_wallets/src/core/tx/witness_gate.rs
  - crates/z00z_wallets/src/core/tx/spending.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/prover.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs
  - crates/z00z_wallets/tests/test_spend_witness_gate.rs
  - crates/z00z_simulator/tests/test_scenario1_spend_gate.rs
autonomous: true
requirements:
  - PH32-SPEND
must_haves:
  truths:
    - Scenario 1 can no longer describe spend authorization as publicly verified when only structural witness checks exist, and plan completion is blocked unless a real public verifier replaces that boundary.
    - Wallet and simulator tests fail if a spend path passes with placeholder witness structure instead of the required public verification contract.
    - The proved statement explicitly binds previous root, input refs, output leaves, `leaf_ad_id`, owner-tag relation, asset-id relation, balance equation, range proofs, nullifier semantics, `chain_id`, version, and transcript/public-input framing.
  artifacts:
    - path: crates/z00z_wallets/src/core/tx/spend_verification.rs
      provides: Real public spend-verifier boundary with explicit public-input contract.
    - path: crates/z00z_wallets/tests/test_spend_witness_gate.rs
      provides: Negative coverage for structural-only witness acceptance.
  key_links:
    - from: crates/z00z_wallets/src/core/tx/witness_gate.rs
      to: crates/z00z_wallets/src/core/tx/spending.rs
      via: wallet spend authorization gate
      pattern: verify_spend_witness_gate|SpendCs
    - from: crates/z00z_wallets/tests/test_spend_witness_gate.rs
      to: crates/z00z_simulator/tests/test_scenario1_spend_gate.rs
      via: wallet + simulator agreement on spend-verifier truthfulness
      pattern: spend gate|public verifier|placeholder
---

<objective>
Replace the structural spend gate with a real public spend verifier rooted in the existing spend-verification seams.

Purpose: close the Phase 032 gap where spend authorization is described too strongly relative to what `SpendCs` currently checks.
Output: one real public-verifier path, one explicit proved statement, and release tests that fail on structural-only acceptance.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/032-crypto-audit-scenario-1/032-CONTEXT.md
@.planning/phases/032-crypto-audit-scenario-1/032-SEMANTIC-FREEZE.md
@crates/z00z_wallets/src/core/tx/witness_gate.rs
@crates/z00z_wallets/src/core/tx/spending.rs
@crates/z00z_wallets/src/core/tx/spend_verification.rs
@crates/z00z_wallets/src/core/tx/prover.rs
@crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the spend gate honest and fail closed</name>
  <files>crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/src/core/tx/spending.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/prover.rs, crates/z00z_wallets/tests/test_spend_witness_gate.rs</files>
  <action>Refactor the spend boundary so accepted flows stop at a real public verifier in the existing `spend_verification.rs` and `prover.rs` seams, with `witness_gate.rs` demoted to witness preparation only. Replace structural-only success conditions with a verifier that binds previous root, input refs, output leaves, `leaf_ad_id`, owner-tag relation, asset-id relation, balance equation, range proofs, nullifier semantics, `chain_id`, version, and explicit transcript/public-input framing. Add targeted negative tests for malformed, replayed, or structurally valid but unauthenticated witness data. If the existing proof stack cannot support this, the task must fail closed as a blocked remediation item rather than close on an honesty downgrade.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --features test-fast test_spend_witness_gate -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Accepted Scenario 1 spend flows pass only through the real public verifier, and structural-only witness checks cannot close the phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror the honest spend gate in Scenario 1 integration tests</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs, crates/z00z_simulator/tests/test_scenario1_spend_gate.rs, crates/z00z_wallets/tests/test_spend_witness_gate.rs</files>
  <action>Add Scenario 1 integration tests that prove simulator flows agree with the updated wallet spend verifier: valid flows still pass, but placeholder/public-verifier overclaims no longer pass acceptance. The tests must prove the same public-input contract as the wallet verifier, including `leaf_ad_id` and transcript-binding expectations. Keep checkpoint placeholder cleanup out of this plan except where the spend verifier needs a canonical previous-root input.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_spend_gate -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Scenario 1 test coverage matches the wallet spend gate and rejects structural-only or overclaimed public-verifier success paths.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| witness bytes -> public spend acceptance | Structural checks can be mistaken for public proof verification. |
| wallet gate -> simulator narrative | Integration paths may continue overclaiming even if wallet code is corrected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-032-10 | Spoofing | spend witness gate | mitigate | Remove or fence structural-only success conditions and require explicit accepted verifier evidence. |
| T-032-11 | Tampering | simulator acceptance tests | mitigate | Add integration tests that fail when the simulator drifts back to placeholder/public-verifier overclaims. |
| T-032-12 | Repudiation | public verification language | mitigate | Rename or downgrade outputs so they truthfully represent the current spend-verifier guarantee. |
</threat_model>

<verification>
Wave 3 closes only when wallet and simulator tests agree that structural witness checks are not enough to claim public spend verification and a real verifier enforces accepted flows.
</verification>

<success_criteria>
Scenario 1 spend acceptance is genuinely backed by the required public-verifier evidence. If that verifier cannot be delivered through the named seams, the plan remains blocked and cannot be treated as complete.
</success_criteria>

<output>
After completion, create `.planning/phases/032-crypto-audit-scenario-1/032-04-SUMMARY.md`
</output>