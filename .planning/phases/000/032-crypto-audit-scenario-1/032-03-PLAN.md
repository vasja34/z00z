<!-- markdownlint-disable MD007 MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->
---
phase: 032-crypto-audit-scenario-1
plan: "03"
type: execute
wave: 2
depends_on:
  - 032-02
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_wallets/src/core/tx/claim_auth.rs
  - crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
  - crates/z00z_storage/tests/test_claim_source_proof.rs
autonomous: true
requirements:
  - PH32-CLAIM-TRUST
must_haves:
  truths:
    - Scenario 1 no longer manufactures a simulator-local claim root and then treats it as authoritative.
    - Claim package production and consumption use the same storage-owned source root/proof contract.
    - The claim-authority trust anchor is either explicitly bound through the live `claim_auth.rs` seam or explicitly labeled immutable simulator-only policy.
    - Forged or mismatched roots, proofs, or authority packages fail in simulator and storage tests.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
      provides: Producer-side authoritative source root/proof binding.
    - path: crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
      provides: Negative coverage for forged/mismatched claim package trust inputs.
  key_links:
    - from: crates/z00z_storage/src/assets/store_internal/store_query.rs
      to: crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
      via: `claim_source_root()` and `claim_source_proof(...)` as the authoritative producer inputs
      pattern: claim_source_root|claim_source_proof
    - from: crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
      to: crates/z00z_simulator/src/claim_pkg_consumer.rs
      via: producer/consumer root and proof contract continuity
      pattern: proof_hex|claim_authority_sig_hex|claim_source
---

<objective>
Route Scenario 1 claim production and consumption through the storage-owned authoritative root/proof boundary.

Purpose: remove the simulator-local trust root that currently lets a locally consistent claim package masquerade as an authoritative one.
Output: one producer/consumer path that shares the same storage-owned source-root truth and negative tests for forged/mismatched packages.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/032-crypto-audit-scenario-1/032-CONTEXT.md
@.planning/phases/000/025-crypto-audit-crypto/025-CONTEXT.md
@.planning/phases/000/028-crypto-audit-storage/028-CONTEXT.md
@crates/z00z_storage/src/assets/store_internal/store_query.rs
@crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
@crates/z00z_simulator/src/claim_pkg_consumer.rs
@crates/z00z_wallets/src/core/tx/claim_auth.rs
</context>

If execution requires checkpoint commits while authoritative root ownership is being stabilized, use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace simulator-local root minting with storage-owned claim-source roots and proofs</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs, crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_storage/tests/test_claim_source_proof.rs</files>
  <action>Remove the temporary `AssetStore::new()` singleton-store proof construction from `claim_pkg.rs`. Make Stage 3 obtain the authoritative source root and membership proof from the storage-owned claim-source boundary that survives outside the local build scope, and persist the signed root/proof inputs in a form the consumer can re-check exactly. Keep ownership with storage: simulator code may request the root/proof, but it must not invent new proof-root semantics.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release test_claim_source_proof -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_claim_pkg_crypto_support -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Stage 3 claim production no longer depends on a simulator-local root that cannot be re-trusted downstream.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make claim package consumption reject mismatched trust anchors and forged root/proof combinations</name>
  <files>crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_wallets/src/core/tx/claim_auth.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs, crates/z00z_storage/tests/test_claim_source_proof.rs</files>
  <action>Update claim package consumption so it re-checks the signed root, proof root, storage-owned authoritative root, and authority trust anchor as one fail-closed contract. The execution path must either bind the live authority public key through `claim_auth.rs` to an explicit config/chain-state authority anchor, or label the current anchor immutable simulator-only policy and reject production-looking usage. Add negative cases for forged root bytes, wrong authority signature, wrong proof version, wrong authenticated tuple, wrong authority anchor, and stale/mismatched storage proofs. Preserve compatibility only where the package is explicitly marked non-authoritative and rejected from accepted Scenario 1 flows.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_claim_pkg_crypto_support -- --nocapture
cargo test -p z00z_storage --release test_claim_source_proof -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Accepted claim packages prove continuity between the storage-owned root/proof boundary and the signed claim tuple, and forged packages fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator producer -> storage root/proof owner | Local proof-root minting can create a root that is self-consistent but globally untrusted. |
| claim package -> consumer | Signed and encoded package fields can drift from storage-owned truth unless re-validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-032-07 | Spoofing | simulator-local root | mitigate | Require Stage 3 to request the authoritative root/proof from storage-owned APIs instead of minting one locally. |
| T-032-08 | Tampering | claim package root/proof fields | mitigate | Re-check signed root, proof root, and storage-owned authoritative root at consume time. |
| T-032-09 | Elevation of privilege | compatibility/non-authoritative lanes | mitigate | Fence non-authoritative package modes out of accepted Scenario 1 flows. |
</threat_model>

<verification>
Wave 2 claim-trust work closes only when producer and consumer use the same storage-owned root/proof truth and forged packages fail in targeted release tests.
</verification>

<success_criteria>
Scenario 1 claim packages can no longer be described as authoritative unless they are rooted in the same storage-owned proof boundary that the consumer verifies.
</success_criteria>

<output>
After completion, create `.planning/phases/032-crypto-audit-scenario-1/032-03-SUMMARY.md`
</output>