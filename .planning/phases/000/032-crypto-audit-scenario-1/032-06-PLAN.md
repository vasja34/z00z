<!-- markdownlint-disable MD007 MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->
---
phase: 032-crypto-audit-scenario-1
plan: "06"
type: execute
wave: 5
depends_on:
  - 032-01
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_2_utils/artifacts.rs
  - crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs
  - crates/z00z_simulator/src/config.rs
  - crates/z00z_simulator/src/config_accessors.rs
  - crates/z00z_simulator/tests/test_stage2_secret_artifacts.rs
  - crates/z00z_simulator/tests/test_transport_rng_boundaries.rs
autonomous: true
requirements:
  - PH32-SECRET
must_haves:
  truths:
    - Simulator secret artifacts are no longer emitted by default or without explicit guarded debug intent.
    - Deterministic seeded RNG behavior remains simulator-only and cannot leak as a production-grade randomness claim.
    - Tests fail if plaintext wallet secrets or unsafe debug defaults reappear.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_2_utils/artifacts.rs
      provides: Guarded secret-artifact behavior.
    - path: crates/z00z_simulator/tests/test_stage2_secret_artifacts.rs
      provides: Regression coverage for plaintext secret leakage.
  key_links:
    - from: crates/z00z_simulator/src/config.rs
      to: crates/z00z_simulator/src/scenario_1/stage_2_utils/artifacts.rs
      via: explicit debug/secret dump configuration gate
      pattern: wallet_debug_dump|secret|artifact
    - from: crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs
      to: crates/z00z_simulator/tests/test_transport_rng_boundaries.rs
      via: deterministic RNG boundary made explicit and tested
      pattern: SeqSecureRngProvider|seed|rng
---

<objective>
Harden simulator secret handling and make debug-only entropy behavior explicit instead of ambient.

Purpose: close the Phase 032 gap where Stage 2 can still dump plaintext wallet secrets and seeded simulator randomness can be misread as a stronger cryptographic claim.
Output: guarded secret-artifact behavior plus regression tests for secret leakage and RNG-boundary honesty.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/032-crypto-audit-scenario-1/032-CONTEXT.md
@.planning/phases/000/031-refactor-architecture/031-CONTEXT.md
@crates/z00z_simulator/src/scenario_1/stage_2_utils/artifacts.rs
@crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs
@crates/z00z_simulator/src/config.rs
@crates/z00z_simulator/src/config_accessors.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard Stage 2 secret artifacts behind explicit debug intent</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_2_utils/artifacts.rs, crates/z00z_simulator/src/config.rs, crates/z00z_simulator/src/config_accessors.rs, crates/z00z_simulator/tests/test_stage2_secret_artifacts.rs</files>
  <action>Refactor Stage 2 artifact emission so passwords, seed phrases, receiver secrets, and equivalent wallet material are never written unless an explicit guarded debug path is enabled. Make the default safe, preserve the user-preferred `wallet_debug_dump` feature as the only intentional opt-in, and add regression tests that fail if plaintext secrets are emitted under default Scenario 1 execution.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_stage2_secret_artifacts -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Default simulator execution no longer emits plaintext wallet secrets, and guarded debug dumping is explicit and test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make deterministic simulator RNG boundaries explicit and regression-tested</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs, crates/z00z_simulator/tests/test_transport_rng_boundaries.rs, crates/z00z_simulator/src/config.rs, crates/z00z_simulator/src/config_accessors.rs</files>
  <action>Document and enforce that `SeqSecureRngProvider` and any equivalent seeded randomness paths are simulator-only fixtures. Add tests proving deterministic seeds are opt-in, remain bounded to Scenario 1 debug/test flows, and are never described as production entropy or privacy guarantees.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_transport_rng_boundaries -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Seeded RNG behavior is clearly simulator-only, configuration-gated, and protected by regression tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 2 artifacts -> filesystem | Plaintext secret output can leak wallet material beyond intended debug flows. |
| simulator RNG fixtures -> crypto claims | Seeded deterministic randomness can be mistaken for production-safe entropy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-032-16 | Information disclosure | Stage 2 secret artifacts | mitigate | Default-deny secret artifact emission and require explicit guarded debug opt-in. |
| T-032-17 | Repudiation | RNG/trust language | mitigate | Make deterministic RNG boundaries explicit in config, code comments, and tests. |
| T-032-18 | Elevation of privilege | debug defaults | mitigate | Ensure `wallet_debug_dump` or equivalent gates are the only routes to secret dumps or seeded debug behavior. |
</threat_model>

<verification>
Wave 5 closes only when default simulator execution is safe from plaintext secret leakage and seeded RNG behavior is explicitly bounded to debug/test flows.
</verification>

<success_criteria>
Scenario 1 no longer leaks secrets by default and no longer implies stronger entropy/privacy guarantees than the simulator debug configuration actually provides.
</success_criteria>

<output>
After completion, create `.planning/phases/032-crypto-audit-scenario-1/032-06-SUMMARY.md`
</output>