<!-- markdownlint-disable MD007 MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->
---
phase: 032-crypto-audit-scenario-1
plan: "02"
type: execute
wave: 1
depends_on:
  - 032-01
files_modified:
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/tests/test_claim_v2_contract.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
autonomous: true
requirements:
  - PH32-CLAIM-BIND
must_haves:
  truths:
    - The authority signature can no longer validate a claim statement that omits the authoritative source root or the rest of the authenticated claim tuple.
    - Claim statement bytes remain canonical and versioned after the new binding fields are introduced.
    - Wallet claim helpers populate and verify the expanded statement without fallback compatibility ambiguity.
  artifacts:
    - path: crates/z00z_crypto/src/claim/v2.rs
      provides: Canonical `ClaimStmtV2` serialization/hash contract with authenticated root binding.
    - path: crates/z00z_crypto/tests/test_claim_v2_contract.rs
      provides: Contract tests for roundtrip stability and root/tuple binding failures.
  key_links:
    - from: .planning/phases/032-crypto-audit-scenario-1/032-CONTEXT.md
      to: crates/z00z_crypto/src/claim/v2.rs
      via: D-05, D-05a, and Wave 2 claim authenticity gate
      pattern: source_root|claim tuple|authority signature
    - from: crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
      to: crates/z00z_crypto/src/claim/v2.rs
      via: wallet claim statement construction and verification
      pattern: build_claim_stmt|chk_source|verify_claim_authority
---

<objective>
Extend the canonical claim statement so the authority signature binds the full authenticated claim tuple, not just a proof-compatible subset.

Purpose: close the live Phase 032 gap where `claim_source` exists but the signed statement still omits the authoritative root binding.
Output: one expanded `ClaimStmtV2` contract plus wallet helper updates and contract tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/032-crypto-audit-scenario-1/032-CONTEXT.md
@.planning/phases/032-crypto-audit-scenario-1/032-SEMANTIC-FREEZE.md
@crates/z00z_crypto/src/claim/v2.rs
@crates/z00z_crypto/tests/test_claim_v2_contract.rs
@crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs
@crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
</context>

If execution requires checkpoint commits while the claim contract is being stabilized, use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `ClaimStmtV2` and its canonical hash with authoritative root binding</name>
  <files>crates/z00z_crypto/src/claim/v2.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/tests/test_claim_v2_contract.rs</files>
  <action>Add the authoritative source-root binding and any missing scenario/ruleset tuple fields to `ClaimStmtV2`, its byte serialization, and `claim_stmt_hash_v2`. Preserve one canonical encoding only; reject ambiguous or legacy-shape decoding on the new path. Expand the claim-v2 contract tests to prove roundtrip stability, hash stability, and failure on wrong root / wrong tuple / wrong version combinations.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release test_claim_v2_contract -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`ClaimStmtV2` cannot be signed or verified without the authoritative root and the full authenticated claim tuple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update wallet claim helpers to build and verify the expanded statement fail-closed</name>
  <files>crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs, crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, crates/z00z_crypto/tests/test_claim_v2_contract.rs</files>
  <action>Refactor wallet claim-statement construction and proof/header verification so the expanded `ClaimStmtV2` is populated from real claim package state and checked fail-closed. The verifier must reject packages whose source-root, asset-id, source commitment, chain_id, or ruleset/version tuple drift from the signed statement. Keep the verification owned by the canonical `claim_v2` contract; do not reintroduce the older placeholder claim-proof surface.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --features test-fast claim_tx -- --nocapture
cargo test -p z00z_crypto --release test_claim_v2_contract -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Wallet claim helpers produce and verify the expanded signed statement without compatibility-only success paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim statement -> authority signature | Missing tuple/root fields let a valid signature bless the wrong proof root or claim scope. |
| wallet helper -> crypto contract | Helper drift can populate a partial statement even after the crypto contract expands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-032-04 | Tampering | `ClaimStmtV2` bytes | mitigate | Add authoritative root + tuple fields to canonical bytes and reject mismatched decoding. |
| T-032-05 | Spoofing | authority signature | mitigate | Require the signature hash to cover the full authenticated claim tuple. |
| T-032-06 | Elevation of privilege | wallet claim helper path | mitigate | Fail closed in wallet helper construction/verification when any bound field drifts. |
</threat_model>

<verification>
Wave 1 claim-contract work closes only when the canonical statement bytes and the wallet helper path both prove the same root-bound authenticated tuple.
</verification>

<success_criteria>
No Scenario 1 claim package can pass the updated claim-v2 contract while omitting or drifting the authoritative source root or any other authenticated claim tuple field.
</success_criteria>

<output>
After completion, create `.planning/phases/032-crypto-audit-scenario-1/032-02-SUMMARY.md`
</output>