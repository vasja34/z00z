---
phase: 056-HJMT-storage-aggregator
plan: 056-06
type: execute
wave: 6
depends_on:
  - 056-05
files_modified:
  - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - crates/z00z_simulator/src/scenario_1/runner_verify.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_utils/flow.rs
  - crates/z00z_simulator/tests/test_scenario_settlement.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
requirements:
  - 056-G10
autonomous: true
must_haves:
  truths:
    - "The simulator must prove the real runtime plane instead of hiding it behind scaffolding."
    - "`scenario_config.yaml` is the executable config surface and `scenario_design.yaml` is the user-facing explanation surface."
    - "Every required trace must resolve to one config-digest set, one route-table digest, one journal-lineage view, and one process-topology view."
    - "Trace artifacts are evidence only; they do not replace runtime or storage semantic truth."
  artifacts:
    - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
    - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
    - crates/z00z_simulator/src/scenario_1/runner.rs
    - crates/z00z_simulator/src/scenario_1/runner_verify.rs
  key_links:
    - "The simulator cannot claim correctness unless it runs the same config and topology contract as the runtime slices it is proving."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 056-06 Plan: Simulator Stage Sync And Runtime Evidence

<objective>
Turn the simulator into a real runtime observability lane for Phase 056.

Purpose: prove the runtime plane with real YAML/config loading, real topology
state, and explicit traces instead of a hidden side model.
Output: synchronized `scenario_config.yaml` / `scenario_design.yaml`, runtime
trace set, and phase-owned evidence linkage.
</objective>

<context>
@.planning/phases/056-HJMT-storage- aggregator/056-TODO.md
@.planning/phases/056-HJMT-storage- aggregator/056-CONTEXT.md
@.planning/phases/056-HJMT-storage- aggregator/056-SOURCE-AUDIT.md
@docs/tech-papers/Z00Z-HJMT-Upgrade.md
@crates/z00z_simulator/src/config.rs
@crates/z00z_simulator/src/scenario_1/scenario_config.yaml
@crates/z00z_simulator/src/scenario_1/scenario_design.yaml
@crates/z00z_simulator/src/scenario_1/runner.rs
@crates/z00z_simulator/src/scenario_1/runner_verify.rs
</context>

<coverage_contract>
This plan owns `056-TODO.md` `Workstream 6 - Simulator stage sync and runtime
evidence`, the trace-artifact rows, the simulator rows in release blockers, and
the scenario/design synchronization rules. It must preserve decisions D-14,
D-15, D-18, and D-19.

Literal TODO bullets that must stay explicit here:
- execution profiles `SIM-SMALL`, `SIM-MEDIUM`, and `SIM-CACHE-EDGE`
- `SIM-BATCH-1000` stays reserved for later benchmark/readiness phases only
- `cfg_flow.json`, `tx_flow.json`, `route_flow.json`, `plan_flow.json`,
  `journal_flow.json`, `scope_flow.json`, `proc_flow.json`, and
  `recovery_flow.json`
- deterministic scenario evidence for `SIM-SMALL` and `SIM-MEDIUM`
- capacity-relative queue/cache evidence for `SIM-CACHE-EDGE`
</coverage_contract>

<tasks>
<task type="auto">
<name>Make `scenario_1` load and verify the Phase 056 runtime plane through real config and trace artifacts</name>
<read_first>
- `056-CONTEXT.md` decisions D-14 and D-15
- `056-TODO.md` sections `Workstream 6`, `Required artifacts`, `Required execution profiles`, and `Required scenario coverage`
- `crates/z00z_simulator/src/scenario_1/scenario_config.yaml`, `scenario_design.yaml`, `runner.rs`, and `runner_verify.rs`
- `docs/tech-papers/Z00Z-HJMT-Upgrade.md` sections `Appendix E.5`, `12`, `12.1`, and `14.1`
</read_first>
<files>
- `crates/z00z_simulator/src/scenario_1/scenario_config.yaml`
- `crates/z00z_simulator/src/scenario_1/scenario_design.yaml`
- `crates/z00z_simulator/src/scenario_1/runner.rs`
- `crates/z00z_simulator/src/scenario_1/runner_verify.rs`
- `crates/z00z_simulator/src/scenario_1/stage_13_utils/flow.rs`
- `crates/z00z_simulator/tests/test_scenario_settlement.rs`
- `crates/z00z_simulator/tests/test_scenario1_stage_surface.rs`
</files>
<action>
Extend the current simulator so `scenario_1` loads the Phase 056 runtime plane
from real config surfaces, runs the accepted topology/process/runtime contract,
and emits the required trace set:
`cfg_flow.json`, `tx_flow.json`, `route_flow.json`, `plan_flow.json`,
`journal_flow.json`, `scope_flow.json`, `proc_flow.json`, and
`recovery_flow.json`. Keep `scenario_design.yaml` synchronized with the
executable stage/runtime contract in the same slice. Validate that every trace
resolves back to one config-digest set, one route-table digest, one
journal-lineage view, and one process-topology view.
</action>
<acceptance_criteria>
- The simulator runs the real runtime plane rather than a hidden scaffold.
- The full trace set exists and links to config/route/journal/process evidence.
- `scenario_design.yaml` stays truthful about the executable stage/runtime
  contract.
- Missing or stale traces fail the phase.
</acceptance_criteria>
<verify>
1. Run bootstrap first.
2. Run `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools --test test_scenario_settlement -- --nocapture`.
3. Run `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools --test test_scenario1_stage_surface -- --nocapture`.
4. Run `cargo test --release`.
5. Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and stop only after at least 2 consecutive runs show no significant issues.
</verify>
<done>
The simulator is a real observability lane for the runtime plane and the design
doc is synchronized with the executable contract.
</done>
</task>
</tasks>

<threat_model>
- Misleading evidence: traces may point at the wrong config or lineage.
- Concept drift: simulator stages may evolve without the design doc.
- Hidden model risk: the simulator may prove a fake in-process path instead of
  the accepted runtime path.
</threat_model>

<output>
Create `.planning/phases/056-HJMT-storage- aggregator/056-06-SUMMARY.md` when
done.
</output>
