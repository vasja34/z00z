---
phase: 056-HJMT-storage-aggregator
plan: 056-01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_rollup_node/src/config.rs
  - crates/z00z_rollup_node/src/runtime.rs
  - crates/z00z_rollup_node/src/status.rs
  - crates/z00z_runtime/aggregators/src/service.rs
  - crates/z00z_runtime/aggregators/src/placement.rs
  - crates/z00z_runtime/aggregators/src/types.rs
  - crates/z00z_simulator/src/config.rs
  - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
requirements:
  - 056-G1
  - 056-G2
autonomous: true
must_haves:
  truths:
    - "`SIM-5A7S` is a canonical acceptance fixture only; the runtime must remain topology-generic."
    - "Aggregator topology must be modeled as separate OS processes, not a shared-memory thread/task mesh."
    - "The existing owner split stays intact: `z00z_rollup_node` composes, `z00z_runtime/aggregators` owns route/placement metadata, `z00z_storage` owns settlement truth, and `z00z_simulator` owns evidence."
    - "The exact repo homes for `aggregator-config.yaml`, `planner-config.yaml`, and `storage-config.yaml` are proposed until this slice verifies them."
  artifacts:
    - .planning/phases/056-HJMT-storage- aggregator/056-CONTEXT.md
    - .planning/phases/056-HJMT-storage- aggregator/056-SOURCE-AUDIT.md
    - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
    - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  key_links:
    - "Topology, owner homes, and process model must freeze before route, failover, or evidence work can close honestly."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 056-01 Plan: Runtime Topology, Composition Root, And Config-Home Freeze

<objective>
Freeze the first executable runtime topology contract for Phase 056 without
inventing a second orchestration or config layer.

Purpose: establish one honest `SIM-5A7S` acceptance fixture, one separate-OS-
process runtime model, and one verified config-home strategy that later slices
can extend safely.
Output: topology fixture shape, process/lifecycle owner map, proposed config
home verification, and no-shared-memory guardrails.
</objective>

<context>
@.planning/phases/056-HJMT-storage- aggregator/056-TODO.md
@.planning/phases/056-HJMT-storage- aggregator/056-CONTEXT.md
@.planning/phases/056-HJMT-storage- aggregator/056-SOURCE-AUDIT.md
@docs/tech-papers/Z00Z-HJMT-Upgrade.md
@crates/z00z_rollup_node/src/config.rs
@crates/z00z_rollup_node/src/runtime.rs
@crates/z00z_runtime/aggregators/src/service.rs
@crates/z00z_runtime/aggregators/src/placement.rs
@crates/z00z_runtime/aggregators/src/types.rs
@crates/z00z_simulator/src/config.rs
@crates/z00z_simulator/src/scenario_1/scenario_config.yaml
@crates/z00z_simulator/src/scenario_1/scenario_design.yaml
</context>

<coverage_contract>
This plan owns `056-TODO.md` sections `Canonical SIM-5A7S runtime profile`,
`Process and config contract`, `Workstream 1 - Process topology and composition
root`, and the topology/process portions of release blockers and the
acceptance subset. It must preserve context decisions D-02 through D-06,
D-15, D-16, and D-19. It must not claim that new YAML homes already exist
until the slice verifies and lands them.

Literal TODO bullets that must stay explicit here:
- `AggregatorId(0)..AggregatorId(4)` and `ShardId(0)..ShardId(6)`
- one dual-primary owner plus standby coverage for every shard
- separate PID, port, data dir, journal path, log path, startup command, and
  restart command per aggregator
- explicit `aggregator-config.yaml`, `planner-config.yaml`, and
  `storage-config.yaml` path strategy
</coverage_contract>

<tasks>
<task type="auto">
<name>Freeze topology, process isolation, and config-home strategy on live owner seams</name>
<read_first>
- `056-CONTEXT.md` Phase Boundary, Implementation Decisions, and TODO Coverage Contract
- `056-TODO.md` sections `Canonical SIM-5A7S runtime profile`, `Process and config contract`, and `Workstream 1`
- `docs/tech-papers/Z00Z-HJMT-Upgrade.md` sections `1.7 Whole-System Structure View`, `5.2.2 Protocol Truth Versus Runtime Placement`, `5.4.3 Runtime Placement Objects And Lawful Failover`, `Appendix E.6`, and `Appendix E.7`
- `crates/z00z_rollup_node/src/runtime.rs` and `crates/z00z_runtime/aggregators/README.md`
</read_first>
<files>
- `crates/z00z_rollup_node/src/config.rs`
- `crates/z00z_rollup_node/src/runtime.rs`
- `crates/z00z_rollup_node/src/status.rs`
- `crates/z00z_runtime/aggregators/src/service.rs`
- `crates/z00z_runtime/aggregators/src/placement.rs`
- `crates/z00z_runtime/aggregators/src/types.rs`
- `crates/z00z_simulator/src/config.rs`
- `crates/z00z_simulator/src/scenario_1/scenario_config.yaml`
- `crates/z00z_simulator/src/scenario_1/scenario_design.yaml`
- proposed `config/hjmt_runtime/` home for `aggregator-config.yaml`, `planner-config.yaml`, and `storage-config.yaml`
</files>
<action>
Add the topology and config packet needed to make the phase executable without
creating a parallel layer. Resolve the final config-home layout under the
proposed `config/hjmt_runtime/` root or an equally narrow verified existing
owner path. Model each aggregator as a separate OS process with its own data
dir, journal path, log path, and lifecycle. Define the canonical `SIM-5A7S`
fixture as acceptance evidence while keeping the runtime generic over positive
topologies loaded from YAML. Extend existing runtime and simulator config
surfaces instead of creating an ad-hoc planner or process registry.
</action>
<acceptance_criteria>
- The repository has one explicit `SIM-5A7S` fixture contract with five
  aggregators, seven shards, and at least one aggregator owning two primaries.
- The accepted model is separate OS processes, not shared-memory orchestration.
- Proposed config homes are either verified and landed or left explicitly
  proposed with one owner path decision still open for the next slice.
- No new orchestration root or duplicate process authority layer is introduced.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first.
2. Run targeted topology/lifecycle guards such as the future `test_hjmt_topology.rs`, `test_hjmt_process.rs`, and `test_hjmt_node_lifecycle.rs`, plus current source-shape guards that cover `scenario_config.yaml` and `scenario_design.yaml`.
3. Run `cargo test -p z00z_rollup_node --release --features test-params-fast` and `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools` once the slice lands runtime-visible Rust changes.
4. Run `cargo test --release` as the broad corroborating gate.
5. Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and stop only after at least 2 consecutive runs show no significant issues.
</verify>
<done>
The topology/process/config contract is frozen on live owner seams and later
slices can assume one honest process model and one honest config-home strategy.
</done>
</task>
</tasks>

<threat_model>
- Spoofing: a test-only in-process mesh could be mistaken for the canonical
  runtime; this slice forbids that model.
- Tampering: config-home ambiguity could create parallel YAML loaders and drift;
  this slice resolves or explicitly proposes the owner path.
- Repudiation: later slices could claim topology assumptions that were never
  frozen; `SIM-5A7S` prevents that.
</threat_model>

<output>
Create `.planning/phases/056-HJMT-storage- aggregator/056-01-SUMMARY.md` when
done.
</output>
