---
phase: 056-HJMT-storage-aggregator
plan: 056-03
type: execute
wave: 3
depends_on:
  - 056-02
files_modified:
  - crates/z00z_runtime/aggregators/src/ingress.rs
  - crates/z00z_runtime/aggregators/src/service.rs
  - crates/z00z_storage/src/settlement/store.rs
  - crates/z00z_storage/src/settlement/hjmt_store.rs
  - crates/z00z_storage/src/settlement/hjmt_commit.rs
  - crates/z00z_storage/src/settlement/hjmt_plan.rs
  - crates/z00z_storage/src/settlement/model.rs
requirements:
  - 056-G5
  - 056-G6
autonomous: true
must_haves:
  truths:
    - "Runtime submits semantic work plus committed routing context only."
    - "Storage remains the only owner of bucket derivation, subtree lifecycle, root transition, and proof truth."
    - "First-seen `definition_id`, `serial_id`, terminal, and right objects are live runtime cases, not later-phase placeholders."
    - "`scope_flow.json` is mandatory evidence and must not expose storage-private tree internals as protocol truth."
  artifacts:
    - crates/z00z_runtime/aggregators/src/ingress.rs
    - crates/z00z_runtime/aggregators/src/service.rs
    - crates/z00z_storage/src/settlement/store.rs
    - crates/z00z_storage/src/settlement/hjmt_commit.rs
  key_links:
    - "If semantic handoff is wrong, failover and simulator evidence become misleading regardless of their own tests."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 056-03 Plan: Semantic Storage Handoff And Dynamic Scope Birth

<objective>
Bind the runtime/storage boundary to one semantic handoff path and preserve
dynamic scope birth without duplicating storage truth.

Purpose: make runtime admission and storage application agree on one lawful
semantic handoff path.
Output: semantic-only ingress/handoff contract, first-scope coverage, and
`scope_flow.json` evidence.
</objective>

<context>
@.planning/phases/056-HJMT-storage- aggregator/056-TODO.md
@.planning/phases/056-HJMT-storage- aggregator/056-CONTEXT.md
@.planning/phases/056-HJMT-storage- aggregator/056-SOURCE-AUDIT.md
@docs/tech-papers/Z00Z-HJMT-Upgrade.md
@crates/z00z_runtime/aggregators/src/ingress.rs
@crates/z00z_runtime/aggregators/src/service.rs
@crates/z00z_storage/src/settlement/store.rs
@crates/z00z_storage/src/settlement/hjmt_store.rs
@crates/z00z_storage/src/settlement/hjmt_commit.rs
@crates/z00z_storage/src/settlement/hjmt_plan.rs
</context>

<coverage_contract>
This plan owns `056-TODO.md` `Workstream 3 - Storage handoff and dynamic scope
birth`, the semantic-handoff portions of the acceptance subset, and the
scope-birth scenario rows. It must preserve context decisions D-03, D-09,
D-10, D-18, and D-19.

Literal TODO bullets that must stay explicit here:
- first-seen `definition_id` birth
- first-seen `serial_id` birth
- first terminal or right object under an already routed shard
- mixed existing-scope and first-seen-scope batch
- duplicate terminal id in one batch reject
- path or leaf mismatch reject under live runtime admission
- replay of the first scope-creating batch after restart in coordination with
  `056-04`
- `scope_flow.json` fields for batch/tx id, shard id, routing generation,
  first-seen status, and post-commit root progression
</coverage_contract>

<tasks>
<task type="auto">
<name>Land one semantic runtime-to-storage handoff and prove first-scope birth on the live storage seam</name>
<read_first>
- `056-CONTEXT.md` decisions D-09 and D-10
- `056-TODO.md` `Workstream 3`, `Required scenario coverage`, and `Required artifacts`
- `docs/tech-papers/Z00Z-HJMT-Upgrade.md` sections `8.2 Storage Backend Boundary`, `8.3 Access Rules`, `8.4 Protocol Types Versus Storage Types`, and `Appendix D.6`
- `crates/z00z_storage/src/settlement/store.rs`, `hjmt_store.rs`, `hjmt_commit.rs`, and `hjmt_plan.rs`
</read_first>
<files>
- `crates/z00z_runtime/aggregators/src/ingress.rs`
- `crates/z00z_runtime/aggregators/src/service.rs`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_storage/src/settlement/hjmt_store.rs`
- `crates/z00z_storage/src/settlement/hjmt_commit.rs`
- `crates/z00z_storage/src/settlement/hjmt_plan.rs`
- `crates/z00z_storage/src/settlement/model.rs`
- phase-owned `scope_flow.json` runtime evidence path
</files>
<action>
Bind runtime admission and storage execution to one semantic handoff path that
passes committed route/generation context without letting runtime own subtree
or proof semantics. Cover first-seen `definition_id`, `serial_id`, and first
terminal/right creation as normal runtime cases. Emit `scope_flow.json` with
batch/tx id, shard id, routing generation, first-seen markers, and post-commit
root progression while keeping storage-private `HjmtTreeId`-style internals out
of protocol truth.
</action>
<acceptance_criteria>
- Runtime-to-storage handoff stays semantic-only.
- Storage remains the sole semantic/proof authority.
- First-scope birth is covered under normal execution and restart-aware tests.
- `scope_flow.json` proves the required progression without exposing private
  storage authority as public truth.
</acceptance_criteria>
<verify>
1. Run bootstrap first.
2. Run targeted storage/runtime tests that cover first-scope birth, mixed existing/new scope batches, and duplicate terminal/path mismatch rejects.
3. Run `cargo test -p z00z_storage --release --features test-params-fast` and the targeted runtime crate tests for the handoff path.
4. Run `cargo test --release`.
5. Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and stop only after at least 2 consecutive runs show no significant issues.
</verify>
<done>
There is one semantic runtime-to-storage handoff path and dynamic scope birth is
proven as a live Phase 056 case.
</done>
</task>
</tasks>

<threat_model>
- Elevation of privilege: runtime could try to own storage subtree or proof
  truth.
- Integrity failure: first-scope birth could diverge between runtime traces and
  durable storage state.
- Information leak: `scope_flow.json` could expose storage-private internals as
  protocol truth.
</threat_model>

<output>
Create `.planning/phases/056-HJMT-storage- aggregator/056-03-SUMMARY.md` when
done.
</output>
