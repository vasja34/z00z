---
phase: 017-scenario-1
plan: 05
type: execute
wave: 4
depends_on:
  - 017-04
files_modified:
  - crates/z00z_storage/tests/assets_suite.rs
  - crates/z00z_storage/tests/snapshot_suite.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs
  - crates/z00z_simulator/tests/test_stage4_tamper.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
  - crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
autonomous: true
requirements:
  - SCN1-04
  - SCN1-05
must_haves:
  truths:
    - Reopen after save/load preserves the same canonical root and the same canonical-path lookup semantics across storage and simulator artifacts.
    - Tampered witness, snapshot, and checkpoint artifacts are rejected by automated tests.
    - The unified Scenario 1 gate proves the final storage-backed path end to end under release-style validation.
  artifacts:
    - path: crates/z00z_storage/tests/assets_suite.rs
      provides: Canonical lookup and reopen parity coverage for storage-owned search semantics.
    - path: crates/z00z_storage/tests/snapshot_suite.rs
      provides: Snapshot reload and root-parity coverage after persisted reopen.
    - path: crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
      provides: Release-style end-to-end validation for the storage-backed Scenario 1 path.
  key_links:
    - from: crates/z00z_storage/tests/assets_suite.rs
      to: crates/z00z_storage/src/assets/store.rs
      via: save/load/search parity checks over canonical path semantics
      pattern: AssetLookup|AssetListReq|load|reload|root
    - from: crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
      to: crates/z00z_simulator/src/scenario_1/runner.rs
      via: end-to-end scenario execution under release-style flags
      pattern: run_with_paths|wallet_debug_dump|test-fast
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Add reload-parity, tamper, and unified acceptance coverage for the storage-backed Scenario 1 path.

Purpose: Prove that the new storage-backed flow survives reopen, preserves canonical lookup semantics, and rejects tampered artifacts before the phase is considered complete.
Output: Storage and simulator parity regressions, tamper regressions, and unified end-to-end acceptance coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/017-scenario-1/017-CONTEXT.md
@.planning/phases/017-scenario-1/017-04-PLAN.md
@.planning/phases/016-jmt-search-and-redb/016-03-SUMMARY.md
@crates/z00z_storage/tests/assets_suite.rs
@crates/z00z_storage/tests/snapshot_suite.rs
@crates/z00z_simulator/tests/test_stage4_tamper.rs
@crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
@crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reopen and canonical-lookup parity coverage across storage and simulator artifacts</name>
  <files>crates/z00z_storage/tests/assets_suite.rs, crates/z00z_storage/tests/snapshot_suite.rs, crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs</files>
  <behavior>
    - Test 1: reopening persisted storage preserves the same canonical root and the same canonical-path lookup order for the same query set.
    - Test 2: reopening checkpoint or snapshot artifacts preserves the same replay inputs and bridge-visible pre-state semantics used before close.
  </behavior>
  <action>Per D-17, D-18, and G-04, extend the current storage and simulator regression suites so save/load/search plus checkpoint or snapshot reopen prove the same canonical root and the same canonical-path lookup results after reopen. Reuse the deterministic ordering behavior already established in phase 016 instead of inventing new parity semantics.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test assets_suite -- --nocapture</automated>
    <automated>cargo test -p z00z_storage --test snapshot_suite -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage6_checkpoint_storage_bridge -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test assets_suite -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test snapshot_suite -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage6_checkpoint_storage_bridge -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-05-PLAN.md current_task="Task 1: Add reopen and canonical-lookup parity coverage across storage and simulator artifacts"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Automated coverage proves reopen parity for canonical roots and canonical-path lookups across storage and simulator artifact flows.</done>
</task>

<task type="auto">
  <name>Task 2: Add tamper and unified acceptance coverage for final artifacts</name>
  <files>crates/z00z_simulator/tests/test_stage4_tamper.rs, crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs, crates/z00z_simulator/tests/test_scenario1_unified_gate.rs</files>
  <behavior>
    - Test 1: witness, snapshot, checkpoint, and final-link artifacts fail validation when bytes, roots, or cross-file bindings are corrupted.
    - Test 2: the unified Stage 4 through Stage 8 acceptance lane proves final artifacts appear only after the storage-backed path succeeds and remain absent on any gated failure.
  </behavior>
  <action>Per D-19, G-05, and G-06, extend the existing simulator tamper and final-gate suites so witness, snapshot, and checkpoint artifacts fail when corrupted, and update the unified scenario gate to validate the storage-backed Stage 4 through Stage 8 path under release-style flags. Do not add a second end-to-end harness; keep the existing unified gate as the final acceptance surface.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage4_tamper -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage4_tamper -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-05-PLAN.md current_task="Task 2: Add tamper and unified acceptance coverage for final artifacts"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Tampered witness, snapshot, and checkpoint artifacts are rejected automatically, and the unified Scenario 1 gate validates the final storage-backed pipeline under release-style flags.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_storage --test assets_suite -- --nocapture`, `cargo test -p z00z_storage --test snapshot_suite -- --nocapture`, `cargo test -p z00z_simulator --test test_stage4_tamper -- --nocapture`, `cargo test -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture`, `cargo test -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test assets_suite -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test snapshot_suite -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage4_tamper -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture`, and `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture`.
</verification>

<success_criteria>
Phase 017 has automated evidence that storage-backed Scenario 1 survives reopen, preserves canonical lookups, and rejects tampered artifact flows before final acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/017-scenario-1/017-05-SUMMARY.md`
</output>