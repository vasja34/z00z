---
phase: 017-scenario-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_wallets/src/core/tx/state_update.rs
  - crates/z00z_wallets/tests/test_tx_wrong_root.rs
  - crates/z00z_wallets/tests/test_tx_tamper.rs
  - crates/z00z_wallets/tests/test_tx_roundtrip.rs
autonomous: true
requirements:
  - SCN1-02
must_haves:
  truths:
    - The first wallet/storage boundary rejects wrong-root, wrong-path, and tampered witness bytes before any state application begins.
    - Resolved inputs stay bound to full `AssetPath` semantics rather than compact refs alone.
    - The wallet-facing checkpoint summary path consumes canonical storage witness bytes, not opaque simulator shims.
  artifacts:
    - path: crates/z00z_wallets/src/core/tx/state_update.rs
      provides: Canonical typed wallet/storage witness boundary for `MemberWit`, `ResolvedInput`, and `prepare_tx_sum`.
    - path: crates/z00z_wallets/tests/test_tx_wrong_root.rs
      provides: Wrong-root and wrong-path regression coverage at the first wallet/storage boundary.
    - path: crates/z00z_wallets/tests/test_tx_tamper.rs
      provides: Tamper rejection coverage for wallet-facing witness material.
  key_links:
    - from: crates/z00z_wallets/src/core/tx/state_update.rs
      to: crates/z00z_storage/src/checkpoint/mod.rs
      via: `MemberWit`, `ResolvedInput`, and checkpoint summary preparation
      pattern: chk_blob|resolve_inputs|prepare_tx_sum
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Strengthen the wallet witness boundary around canonical storage proofs and full path binding.

Purpose: Make the first downstream consumer of pre-state data enforce the same path-bound proof contract that storage already owns.
Output: Tightened `state_update` contract plus wallet regressions for wrong-root, wrong-path, and tampered witness cases.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/017-scenario-1/017-CONTEXT.md
@crates/z00z_wallets/src/core/tx/state_update.rs
@crates/z00z_storage/src/checkpoint/mod.rs
@crates/z00z_storage/src/assets/store.rs

<interfaces>
From crates/z00z_wallets/src/core/tx/state_update.rs:
```rust
pub struct MemberWit;
pub struct ResolvedInput;
fn resolve_inputs(prev_root: CheckRoot, inputs: &[TxInputWire], resolver: &impl InputResolver) -> Result<Vec<ResolvedInput>, StateErr>;
pub fn prepare_tx_sum(prev_root: CheckRoot, resolver: &impl InputResolver, inputs: &[TxInputWire], outputs: &[AssetLeaf], tx_proof: &[u8]) -> Result<TxPkgSum, StateErr>;
```

From crates/z00z_storage/src/checkpoint/mod.rs:
```rust
pub use self::build::{InputResolver, MemberIdx, MemberWit, ResolvedInput, prepare_tx_sum};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce canonical storage witness checks at the first wallet boundary</name>
  <files>crates/z00z_wallets/src/core/tx/state_update.rs, crates/z00z_wallets/tests/test_tx_wrong_root.rs, crates/z00z_wallets/tests/test_tx_tamper.rs</files>
  <behavior>
    - Test 1: wrong-root and wrong-path witness cases fail inside `resolve_inputs` or `ResolvedInput::new` before state application begins.
    - Test 2: tampered witness bytes fail through the same typed path-bound contract instead of passing as opaque non-empty blobs.
  </behavior>
  <action>Per D-05 through D-07, extend the verified current `state_update.rs` contract so the first wallet boundary consumes canonical storage witness bytes and preserves full `AssetPath` binding. Reuse `MemberWit`, `ResolvedInput`, and `chk_blob` integration instead of adding a second proof-decoding boundary elsewhere in wallets or simulator code.</action>
  <verify>
    <automated>cargo test -p z00z_wallets --test test_tx_wrong_root -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --test test_tx_tamper -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_tx_wrong_root -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_tx_tamper -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-02-PLAN.md current_task="Task 1: Enforce canonical storage witness checks at the first wallet boundary"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>The first wallet/storage boundary rejects wrong-root, wrong-path, and tampered witness cases through the existing typed contract.</done>
</task>

<task type="auto">
  <name>Task 2: Align wallet roundtrip coverage with full path-bound pre-state resolution</name>
  <files>crates/z00z_wallets/tests/test_tx_roundtrip.rs, crates/z00z_wallets/src/core/tx/state_update.rs</files>
  <behavior>
    - Test 1: roundtrip coverage proves `prepare_tx_sum` resolves each compact input through canonical path-bound witness material and preserves the expected pre-state root.
    - Test 2: a mismatched compact ref or path binding cannot roundtrip as a valid resolved input when canonical storage witness bytes disagree.
  </behavior>
  <action>Add or update roundtrip coverage so `prepare_tx_sum` and downstream wallet summary logic prove compact refs are resolved only through full path-bound pre-state data. Keep `TxInputWire` compact in the public wire contract; do not inline full storage leaves into regular tx wire format unless the existing contract explicitly changes.</action>
  <verify>
    <automated>cargo test -p z00z_wallets --test test_tx_roundtrip -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_tx_roundtrip -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-02-PLAN.md current_task="Task 2: Align wallet roundtrip coverage with full path-bound pre-state resolution"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Wallet regression coverage proves regular transfer preparation stays compact on the wire while remaining fully path-bound at the pre-state boundary.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_wallets --test test_tx_wrong_root -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_tamper -- --nocapture`, `cargo test -p z00z_wallets --test test_tx_roundtrip -- --nocapture`, and the matching `--release --features test-fast --features wallet_debug_dump` commands. Treat the wallet gate as incomplete unless wrong-root, tamper, and roundtrip coverage all pass in both default and release lanes.
</verification>

<success_criteria>
The first wallet/storage boundary becomes a reliable typed gate for canonical storage proofs and full path binding, without widening the compact tx wire contract.
</success_criteria>

<output>
After completion, create `.planning/phases/017-scenario-1/017-02-SUMMARY.md`
</output>