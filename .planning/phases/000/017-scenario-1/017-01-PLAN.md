---
phase: 017-scenario-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
  - crates/z00z_simulator/src/scenario_1/stage_4.rs
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_simulator/tests/test_claim_persist.rs
  - crates/z00z_simulator/tests/test_stage4_root_support.rs
  - crates/z00z_simulator/tests/test_stage4_tamper.rs
autonomous: true
requirements:
  - SCN1-01
  - SCN1-02
must_haves:
  truths:
    - Claim publication reaches storage-owned canonical `AssetPath` rows before the scenario reports success.
    - Stage 4 persists canonical `PrepSnapshot` state and emits `checkpoint_prep.json` only as a reference to that snapshot.
    - Root drift or tampered witness bytes are rejected before downstream execution can reuse Stage 4 artifacts.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_3.rs
      provides: Scenario-owned claim publication flow wired to the storage-owned publish seam.
    - path: crates/z00z_simulator/src/scenario_1/stage_4.rs
      provides: Stage 4 transport builder that persists canonical snapshot state and transport references.
    - path: crates/z00z_simulator/tests/test_stage4_root_support.rs
      provides: Regression coverage for canonical Stage 4 root and snapshot-reference behavior.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_3.rs
      to: crates/z00z_simulator/src/claim_pkg_consumer.rs
      via: claim package publish handoff
      pattern: publish_claims_store|load_claim_pkgs
    - from: crates/z00z_simulator/src/scenario_1/stage_4.rs
      to: crates/z00z_storage/src/snapshot/store.rs
      via: canonical prep snapshot persistence and reload reference
      pattern: PrepSnapshot|PrepFsStore|checkpoint_prep.json
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Lock claim publication and Stage 4 transport onto storage-owned artifacts.

Purpose: Eliminate simulator-local ambiguity at the front of the Scenario 1 pipeline so later plans can rely on canonical claim publish and canonical pre-state transport.
Output: Claim publish integration, Stage 4 snapshot-reference transport, and regression coverage for root drift and witness tamper.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/017-scenario-1/017-CONTEXT.md
@.planning/phases/016-jmt-search-and-redb/016-03-SUMMARY.md
@crates/z00z_simulator/src/scenario_1/stage_3.rs
@crates/z00z_simulator/src/scenario_1/stage_4.rs
@crates/z00z_simulator/src/claim_pkg_consumer.rs
@crates/z00z_storage/src/snapshot/store.rs

<interfaces>
From crates/z00z_simulator/src/claim_pkg_consumer.rs:
```rust
pub fn load_claim_pkgs(path: &Path) -> Result<Vec<ClaimTxPackage>, String>;
pub fn publish_claims_store(path: &Path, store: &mut AssetStore) -> Result<ClaimStorePub, String>;
```

From crates/z00z_simulator/src/scenario_1/stage_4.rs:
```rust
const PREP_FILE: &str = "checkpoint_prep.json";
fn build_canon_snapshot(prep: &PrepFile) -> Result<(PrepSnapshot, PrepSnapshotId), String>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Scenario 1 claim publication through the storage-owned publish seam</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3.rs, crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/tests/test_claim_persist.rs</files>
  <behavior>
    - Test 1: verified Stage 3 claim packages publish through `publish_claims_store` and create canonical `definition_id -> serial_id -> asset_id` rows.
    - Test 2: claim publication failure fails the stage before downstream transfer preparation begins.
  </behavior>
  <action>Per D-01, D-03, and D-04, extend the existing Stage 3 claim flow so the scenario uses the verified current claim publish seam in `crates/z00z_simulator/src/claim_pkg_consumer.rs` instead of simulator-only claim bookkeeping. Keep `AssetStore::apply_ops` as the canonical write boundary. Do not introduce a second claim publish path or a scenario-local alias map. Reuse existing claim package verification before any publish call.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_claim_persist -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_claim_persist -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-01-PLAN.md current_task="Task 1: Route Scenario 1 claim publication through the storage-owned publish seam"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Stage 3 publishes verified claim outputs into storage-owned canonical rows and fails closed if publication cannot complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep Stage 4 on the canonical snapshot-reference transport path</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_4.rs, crates/z00z_simulator/tests/test_stage4_root_support.rs, crates/z00z_simulator/tests/test_stage4_tamper.rs</files>
  <behavior>
    - Test 1: Stage 4 writes `checkpoint_prep.json` as a stable reference to persisted `PrepSnapshot` bytes, not as the canonical proof payload itself.
    - Test 2: Stage 4 rejects root drift and tampered witness material before Stage 6 or Stage 7 can consume the transport artifacts.
  </behavior>
  <action>Per D-05 through D-07 and D-11 through D-14, refactor Stage 4 so canonical pre-state ownership stays in storage-backed `PrepSnapshot` bytes and `checkpoint_prep.json` stays reference-only. Remove compatibility-root style proof shaping from the canonical path; if compatibility helpers must temporarily survive for non-canonical helpers, keep them off the path consumed by later stages. Reuse the existing Stage 4 transport builder instead of creating a second prep generator.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage4_root_support -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage4_tamper -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage4_root_support -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage4_tamper -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-01-PLAN.md current_task="Task 2: Keep Stage 4 on the canonical snapshot-reference transport path"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Stage 4 persists canonical snapshot state, emits transport-only prep references, and rejects root or witness drift before downstream stages run.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_claim_persist -- --nocapture`, `cargo test -p z00z_simulator --test test_stage4_root_support -- --nocapture`, `cargo test -p z00z_simulator --test test_stage4_tamper -- --nocapture`, and the matching `--release --features test-fast --features wallet_debug_dump` commands.
</verification>

<success_criteria>
Scenario 1 begins from a storage-owned claim publish path and a canonical Stage 4 snapshot-reference transport contract, with tamper and root drift blocked before execution continues.
</success_criteria>

<output>
After completion, create `.planning/phases/017-scenario-1/017-01-SUMMARY.md`
</output>