---
phase: 017-scenario-1
plan: 04
type: execute
wave: 3
depends_on:
  - 017-03
files_modified:
  - crates/z00z_simulator/src/scenario_1/mod.rs
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - proposed: crates/z00z_simulator/src/scenario_1/stage_7.rs
  - proposed: crates/z00z_simulator/src/scenario_1/stage_8.rs
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/tests/test_s7_examples.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
autonomous: true
requirements:
  - SCN1-03
  - SCN1-05
must_haves:
  truths:
    - The runner and design document register Stage 7 and Stage 8 as real executable stages after Stage 6.
    - Stage 7 owns the canonical storage-backed `resolve -> verify -> apply` path for regular transfer execution.
    - Stage 8 persists final checkpoint artifacts separately from draft, link, exec-input, and audit-only helpers.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_7.rs
      status: proposed
      provides: Canonical storage-backed apply stage for regular transfer execution.
    - path: crates/z00z_simulator/src/scenario_1/stage_8.rs
      status: proposed
      provides: Final checkpoint artifact separation and export stage.
    - path: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      provides: Executable stage order and acceptance notes for Stage 7 and Stage 8.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/mod.rs
      to: proposed crates/z00z_simulator/src/scenario_1/stage_7.rs
      via: stage module export
      pattern: pub mod stage_7
    - from: crates/z00z_simulator/src/scenario_1/runner.rs
      to: proposed crates/z00z_simulator/src/scenario_1/stage_8.rs
      via: stage map registration
      pattern: stage_7|stage_8
    - from: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      to: crates/z00z_simulator/src/scenario_1/runner.rs
      via: stage numbering and rust entry alignment
      pattern: stage: 7|stage: 8|rust_entry
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Wire Stage 7 and Stage 8 into the scenario runner and separate final checkpoint artifacts from draft outputs.

Purpose: Make the Stage 7 and Stage 8 decomposition real in runtime code so Scenario 1 can distinguish transport, canonical apply, and final checkpoint surfaces.
Output: New stage modules, runner and design wiring, and gating tests for final artifact separation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/017-scenario-1/017-CONTEXT.md
@.planning/phases/017-scenario-1/017-03-PLAN.md
@crates/z00z_simulator/src/scenario_1/mod.rs
@crates/z00z_simulator/src/scenario_1/runner.rs
@crates/z00z_simulator/src/scenario_1/scenario_design.yaml
@crates/z00z_storage/src/checkpoint/store.rs

<interfaces>
From crates/z00z_simulator/src/scenario_1/mod.rs:
```rust
pub mod runner;
pub mod stage_1;
pub mod stage_2;
pub mod stage_3;
pub mod stage_4;
pub mod stage_5;
pub mod stage_6;
```

From crates/z00z_simulator/src/scenario_1/runner.rs:
```rust
let mut stage_map = BTreeMap::new();
stage_map.insert(1, stage_1::run as _);
...
stage_map.insert(6, stage_6::run_bundle as _);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add executable Stage 7 storage-backed apply wiring</name>
  <files>crates/z00z_simulator/src/scenario_1/mod.rs, crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/src/scenario_1/stage_7.rs, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_s7_examples.rs</files>
  <behavior>
    - Test 1: the runner and design document register Stage 7 after Stage 6 with a real storage-backed `resolve -> verify -> apply` entry point.
    - Test 2: Stage 7 examples or scenario tests exercise the new stage instead of hiding canonical apply inside Stage 6.
  </behavior>
  <action>Per D-08 and D-09, add `stage_7.rs`, export it from `mod.rs`, register it in `runner.rs`, and update `scenario_design.yaml` so Stage 7 owns canonical storage-backed apply behavior. Reuse the adapter delivered by plan 03; do not re-implement `resolve -> verify -> apply` inside the simulator itself.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_s7_examples -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_s7_examples -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-04-PLAN.md current_task="Task 1: Add executable Stage 7 storage-backed apply wiring"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Stage 7 is a real executable stage in Scenario 1 and owns the canonical storage-backed apply path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Stage 8 final checkpoint artifact separation</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_8.rs, crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs</files>
  <behavior>
    - Test 1: final checkpoint artifact, link, exec-input, and audit files are emitted only when the finalization path succeeds.
    - Test 2: draft-only outputs remain distinct from final checkpoint surfaces and cannot be confused as interchangeable sources of truth.
  </behavior>
  <action>Per D-10, D-13, and G-06, add `stage_8.rs` and wire it into `runner.rs` and `scenario_design.yaml` so final checkpoint artifact creation is isolated from draft and bridge artifacts. Reuse `CheckpointFsStore` surfaces for final artifact, link, and audit persistence. Do not overload Stage 6 with finalization logic after Stage 8 exists.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-04-PLAN.md current_task="Task 2: Add Stage 8 final checkpoint artifact separation"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Stage 8 persists final checkpoint surfaces separately from draft artifacts and blocks publication when finalization fails.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_s7_examples -- --nocapture`, `cargo test -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture`, and the matching `--release --features test-fast --features wallet_debug_dump` commands.
</verification>

<success_criteria>
Scenario 1 runtime and design artifacts explicitly separate Stage 7 canonical apply from Stage 8 final checkpoint publication.
</success_criteria>

<output>
After completion, create `.planning/phases/017-scenario-1/017-04-SUMMARY.md`
</output>