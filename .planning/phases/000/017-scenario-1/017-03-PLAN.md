---
phase: 017-scenario-1
plan: 03
type: execute
wave: 2
depends_on:
  - 017-01
  - 017-02
files_modified:
  - crates/z00z_storage/src/checkpoint/mod.rs
  - crates/z00z_storage/src/checkpoint/store.rs
  - proposed: crates/z00z_storage/src/checkpoint/asset_state_bridge.rs
  - crates/z00z_simulator/src/scenario_1/stage_6.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs
autonomous: true
requirements:
  - SCN1-03
must_haves:
  truths:
    - Canonical regular-transfer apply ownership moves into a storage-backed adapter over `AssetStore` instead of staying in `SimState`.
    - Stage 6 remains a draft and reload bridge and no longer owns the canonical `resolve -> verify -> apply` state transition.
    - Storage-backed execution reuses existing checkpoint-state traits instead of inventing a second simulator-local state engine.
  artifacts:
    - path: crates/z00z_storage/src/checkpoint/asset_state_bridge.rs
      status: proposed
      provides: Proposed storage-backed adapter over `AssetStore` for checkpoint-state traits.
    - path: crates/z00z_simulator/src/scenario_1/stage_6.rs
      provides: Stage 6 handoff into storage-backed execution without keeping `SimState` as apply owner.
    - path: crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs
      provides: Regression coverage for the storage-backed bridge and draft-only behavior.
  key_links:
    - from: crates/z00z_storage/src/checkpoint/mod.rs
      to: proposed crates/z00z_storage/src/checkpoint/asset_state_bridge.rs
      via: checkpoint-state trait implementation over `AssetStore`
      pattern: AssetState|MemberIdx|InputResolver|SpentIdx
    - from: crates/z00z_simulator/src/scenario_1/stage_6.rs
      to: proposed crates/z00z_storage/src/checkpoint/asset_state_bridge.rs
      via: Stage 6 bridge into storage-backed execution inputs
      pattern: CheckpointFsStore|build_cp_draft|prepare_tx_sum|asset_state_bridge
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Move canonical regular-transfer apply ownership into a storage-backed adapter and refactor the Stage 6 handoff.

Purpose: Replace the remaining simulator-owned execution seam with a storage-backed adapter that matches the checkpoint-state contracts already used by wallet and storage code.
Output: Storage-backed checkpoint-state adapter, Stage 6 handoff refactor, and bridge regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/017-scenario-1/017-CONTEXT.md
@.planning/phases/017-scenario-1/017-01-PLAN.md
@.planning/phases/017-scenario-1/017-02-PLAN.md
@crates/z00z_storage/src/checkpoint/mod.rs
@crates/z00z_storage/src/checkpoint/store.rs
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_simulator/src/scenario_1/stage_6.rs

<interfaces>
From crates/z00z_storage/src/checkpoint/mod.rs:
```rust
pub use self::build::{apply_batch_checkpoint, AssetState, InputResolver, MemberIdx, SpentIdx};
pub use self::store::{CheckpointFsStore, CheckpointStore};
```

From crates/z00z_storage/src/assets/store.rs:
```rust
pub fn apply_ops(&mut self, ops: Vec<StoreOp>) -> Result<AssetStateRoot, AssetStoreError>;
pub fn root(&self) -> Result<AssetStateRoot, AssetStoreError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the storage-backed checkpoint-state adapter over AssetStore</name>
  <files>crates/z00z_storage/src/checkpoint/mod.rs, crates/z00z_storage/src/checkpoint/store.rs, crates/z00z_storage/src/checkpoint/asset_state_bridge.rs</files>
  <behavior>
    - Test 1: a storage-backed adapter can satisfy the checkpoint-state traits needed for regular transfer execution without introducing a second mutable state engine.
    - Test 2: adapter construction preserves `AssetStore` root ownership and can surface lookup or spent-index failures through typed checkpoint errors.
  </behavior>
  <action>Per D-01, D-02, D-09, and G-03, add a proposed new internal adapter file `crates/z00z_storage/src/checkpoint/asset_state_bridge.rs` that implements the existing checkpoint-state traits over `AssetStore`. Keep the adapter storage-owned and internal to the checkpoint module. Do not duplicate checkpoint logic in simulator code and do not widen the public checkpoint facade more than necessary.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test checkpoint_store_api -- --nocapture</automated>
    <automated>cargo test -p z00z_storage --test checkpoint_root_binding -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test checkpoint_store_api -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test checkpoint_root_binding -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-03-PLAN.md current_task="Task 1: Define the storage-backed checkpoint-state adapter over AssetStore"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>The checkpoint module has one storage-backed adapter target for regular-transfer execution and does not rely on a parallel simulator-owned state engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Stage 6 into a bridge that hands off to storage-backed execution</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_6.rs, crates/z00z_storage/src/checkpoint/asset_state_bridge.rs, crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs</files>
  <behavior>
    - Test 1: Stage 6 still writes draft and exec-input artifacts, but canonical apply ownership is delegated to the storage-backed adapter.
    - Test 2: draft-only mode keeps final artifact surfaces absent while the storage-backed bridge remains the only apply path.
  </behavior>
  <action>Per D-08 through D-10 and G-03, keep Stage 6 as the checkpoint-draft and reload bridge, but make it call the storage-backed adapter instead of retaining `SimState` as canonical execution owner. Preserve existing draft-only behavior where relevant and extend `test_stage6_checkpoint_storage_bridge.rs` rather than creating a parallel Stage 6 bridge test suite.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage6_checkpoint_storage_bridge -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage6_checkpoint_storage_bridge -- --nocapture</automated>
    <automated>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/017-scenario-1/017-03-PLAN.md current_task="Task 2: Refactor Stage 6 into a bridge that hands off to storage-backed execution"` from `.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times, fix all issues and warnings after each run, and stop only after at least 2 consecutive runs report no significant issues in the code.</automated>
  </verify>
  <done>Stage 6 becomes a storage-backed draft bridge and no longer owns canonical regular-transfer apply semantics.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_storage --test checkpoint_store_api -- --nocapture`, `cargo test -p z00z_storage --test checkpoint_root_binding -- --nocapture`, `cargo test -p z00z_storage --test checkpoint_replay_inputs -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test checkpoint_store_api -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test checkpoint_root_binding -- --nocapture`, `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_storage --test checkpoint_replay_inputs -- --nocapture`, `cargo test -p z00z_simulator --test test_stage6_checkpoint_storage_bridge -- --nocapture`, and `cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage6_checkpoint_storage_bridge -- --nocapture`.
</verification>

<success_criteria>
Canonical regular-transfer execution is owned by storage-backed checkpoint-state code, while Stage 6 is reduced to a transport and reload bridge.
</success_criteria>

<output>
After completion, create `.planning/phases/017-scenario-1/017-03-SUMMARY.md`
</output>