---
phase: 051-HJMT-Facade
plan: 051-05
type: execute
wave: 5
depends_on:
  - 051-01
  - 051-02
  - 051-03
  - 051-04
files_modified:
  - crates/z00z_storage/src/assets/README.MD
  - crates/z00z_storage/src/assets/root-types.md
  - docs/Z00Z-JMT-Design.md
  - .planning/phases/051-HJMT-Facade/051-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
requirements:
  - PH51-BACKEND-FACADE
  - PH51-COMPAT-BACKEND
  - PH51-ROOT-TAXONOMY
  - PH51-PROOF-ENVELOPE
  - PH51-GUARDRAILS
  - PH51-EQUIVALENCE
  - PH51-CHECKPOINT-RELOAD
  - PH51-ROLLOUT-HANDOFF
autonomous: true
must_haves:
  - Phase 051 closeout records what shipped and what is deferred.
  - Documentation says compatibility backend is reference-only.
  - Future forest backend rollout starts from the facade boundary, not a second authority layer.
  - Fixed bucket policy, forest commit journal, crash recovery, and config rollout are recorded as future work unless implemented by a later approved context update.
user_setup: none
---

<!-- markdownlint-disable MD060 -->

# 051-05 Plan: Docs Closeout And Forest Handoff

<objective>
Close Phase 051 with truthful docs, roadmap/state evidence, and a bounded
handoff to future HJMT forest implementation phases.
</objective>

<execution_context>
Read all prior Phase 051 plan summaries when they exist, plus
`.planning/phases/051-HJMT-Facade/051-CONTEXT.md`,
`.planning/phases/051-HJMT-Facade/051-TODO.md`, and
`docs/Z00Z-JMT-Design.md` sections 2.7, 3.3, 7.3, 9.1, 9.3, and 12.
</execution_context>

<context>
Phase 051 is a facade and compatibility-reference phase. The closeout must not
claim that the production forest backend, fixed buckets, adaptive buckets,
commit journal, crash recovery, configuration-gated rollout, deletion-proof
family, non-existence-proof family, `RightLeaf`, or `FeeEnvelope` shipped.
</context>

<tasks>
<task type="auto">
<name>Update storage docs for facade and root semantics</name>
<read_first>
- `crates/z00z_storage/src/assets/README.MD`
- `crates/z00z_storage/src/assets/root-types.md`
- `docs/Z00Z-JMT-Design.md`
- `.planning/phases/051-HJMT-Facade/051-CONTEXT.md`
</read_first>
<files>
- `crates/z00z_storage/src/assets/README.MD`
- `crates/z00z_storage/src/assets/root-types.md`
- `docs/Z00Z-JMT-Design.md`
</files>
<action>
Update storage-facing docs so they describe the new facade, compatibility
backend, root taxonomy, proof-envelope version boundary, and downstream
guardrails in English. State that `AssetStateRoot` is the live public root,
`CheckRoot` is checkpoint-facing evidence, `backend_root` is proof-local or
diagnostic, and `SettlementStateRoot` is future terminology only. Keep
`docs/Z00Z-JMT-Design.md` aligned by noting that Phase 051 implements the Phase
1 boundary and does not implement the physical forest rollout. Also state that
`RightLeaf` and `FeeEnvelope` remain distinct future contract families, and
that fixed bucket policy, forest commit journal, crash recovery, and
configuration-gated backend enablement are future work behind this facade unless
an approved later context update expands Phase 051 before execution.
</action>
<acceptance_criteria>
- Storage docs mention the facade and compatibility backend by their source
  names.
- Docs state that backend roots cannot replace `AssetStateRoot`.
- Docs state that compatibility mode is the reference backend, not target
  performance architecture.
- Docs do not claim that forest backend, fixed buckets, adaptive buckets,
  commit journal, or production rollout shipped in Phase 051.
- Docs do not claim that deletion/non-existence proof families, `RightLeaf`, or
  `FeeEnvelope` shipped in Phase 051 unless implementation evidence exists.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the failure, and rerun it before broader validation. Treat it as the early regression detector, not a replacement for broader cargo validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/051-HJMT-Facade/051-05-PLAN.md current_task="Update storage docs for facade and root semantics"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
Docs truthfully describe the facade, compatibility backend, and root semantics.
</done>
</task>

<task type="auto">
<name>Write closeout and future forest handoff</name>
<read_first>
- `.planning/phases/051-HJMT-Facade/051-CONTEXT.md`
- `.planning/phases/051-HJMT-Facade/051-01-PLAN.md`
- `.planning/phases/051-HJMT-Facade/051-02-PLAN.md`
- `.planning/phases/051-HJMT-Facade/051-03-PLAN.md`
- `.planning/phases/051-HJMT-Facade/051-04-PLAN.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
</read_first>
<files>
- `.planning/phases/051-HJMT-Facade/051-SUMMARY.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
</files>
<action>
Write `051-SUMMARY.md` after implementation and validation are complete.
Record the shipped facade, compatibility backend, root taxonomy, proof
envelope, guardrails, and golden corpus evidence. Update `ROADMAP.md` and
`STATE.md` to mark Phase 051 complete only after validation evidence exists.
Add a future forest-backend handoff section that says later phases must use the
facade and compatibility corpus, and must not create a second storage authority
or expose physical roots as public state roots. The handoff must explicitly list
fixed bucket policy, verifier-visible bucket metadata, physical forest backend,
child-before-parent publication, forest commit journal, crash-safe recovery,
dual-backend equivalence, configuration switch, deletion proofs, and
non-existence proofs as future work items behind the facade. Also record the
state-management anti-drift guardrails: no parallel claim-source carrier, no
second checkpoint verifier, no second wallet scan cursor model, no second replay
registry, no direct runtime DTO persistence lane, and no simulator closure that
substitutes for storage, validator, wallet, or nullifier seam evidence.
</action>
<acceptance_criteria>
- `051-SUMMARY.md` lists changed files, tests run, review-loop evidence, and
  known deferred forest work.
- `ROADMAP.md` marks Phase 051 complete only after summaries and validation
  evidence exist.
- `STATE.md` points to the next active phase or plan after Phase 051 and does
  not leave stale Phase 051 execution status.
- Handoff text names the facade and golden corpus as mandatory inputs for
  future forest backend phases.
- Handoff text maps `JMT-REQ-003` through `JMT-REQ-008` to future work unless
  implementation evidence exists in the Phase 051 summaries.
- Handoff text maps the referenced state-management execution baseline to
  Phase 051 guardrails and states which items are downstream handoff rather than
  HJMT facade implementation.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the failure, and rerun it before broader validation. Treat it as the early regression detector, not a replacement for broader cargo validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/051-HJMT-Facade/051-05-PLAN.md current_task="Write closeout and future forest handoff"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
Phase 051 has truthful closeout artifacts and future forest work is bounded by
the facade and compatibility corpus.
</done>
</task>
</tasks>

<threat_model>
- Closeout must not overclaim forest backend readiness.
- Future phases must not bypass the facade or compatibility corpus.
- Roadmap and state must not mark completion before validation evidence exists.
</threat_model>

<verification>
This is the final Phase 051 closure plan. Apply the mandatory bootstrap,
broader validation, and repeated task execution review loop before any complete
status is written.
</verification>

<success_criteria>
- Docs and closeout truthfully describe what Phase 051 shipped.
- Future forest work has a clear handoff and does not reopen facade decisions.
</success_criteria>

<output>
Docs updates, `051-SUMMARY.md`, and synchronized roadmap/state after execution.
</output>
