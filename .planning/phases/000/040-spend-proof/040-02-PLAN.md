---
phase: 040-spend-proof
plan: "02"
type: execute
wave: 2
depends_on:
  - "040-01"
  - "040-02"
files_modified:
  - crates/z00z_wallets/src/core/tx/prover.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/state_update.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_wallets/tests/test_spend_prover_contract.rs
  - crates/z00z_wallets/tests/test_tx_proof_verifier.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Stage 4 or wallet code can emit the canonical proof carrier from existing witness material."
    - "Checkpoint apply consumes the shared proof contract via `TxProofVerifier` instead of placeholder bytes."
    - "Simulator bridge code stays a temporary adapter over the real verifier path, not a parallel backend."
  artifacts:
    - "crates/z00z_wallets/src/core/tx/prover.rs"
    - "crates/z00z_wallets/src/core/tx/state_update.rs"
    - "crates/z00z_wallets/tests/test_spend_prover_contract.rs"
    - "crates/z00z_wallets/tests/test_tx_proof_verifier.rs"
  key_links:
    - "Stage 4 tx lane -> build_public_spend_contract(...) -> proof carrier"
    - "state_update::build_cp_draft(...) -> TxProofVerifier -> shared spend contract"
    - "Stage 6 bridge -> verify_full_tx_package(...) as temporary adapter"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Wire the concrete proof producer and consumer through the existing wallet, simulator, and checkpoint seams created by Plan 01.

Purpose: satisfy D-04, D-06, D-07, D-08, and D-10 by strengthening the existing producer/verifier path rather than re-architecting it.
Output: a concrete producer path rooted in existing witness material, a concrete verifier backend routed through `TxProofVerifier`, and fail-closed regression coverage for both.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/040-spend-proof/040-CONTEXT.md
@.planning/phases/040-spend-proof/040-TODO.md
@.planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
@.planning/phases/040-spend-proof/040-01-PLAN.md
@crates/z00z_wallets/src/core/tx/prover.rs
@crates/z00z_wallets/src/core/tx/spend_verification.rs
@crates/z00z_wallets/src/core/tx/state_update.rs
@crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
@crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs

<interfaces>
From prover.rs:
```rust
pub const SPEND_AUTH_CTX: &[u8] = b"Z00Z/SPEND_AUTH_V1";

pub fn sign_spend_authorization(
    identity_sk: &Z00ZScalar,
    statement: &[u8],
) -> ProverResult<Z00ZSchnorrSignature>;

pub fn verify_spend_authorization(
    identity_pk: &Z00ZRistrettoPoint,
    statement: &[u8],
    signature: &Z00ZSchnorrSignature,
) -> ProverResult<bool>;
```

From state_update.rs:
```rust
pub fn build_cp_draft(
    height: u64,
    snap_id: PrepSnapshotId,
    snapshot: &PrepSnapshot,
    replay: &[PrepReplayEntry],
    link: &CheckpointLink,
    exec: &CheckpointExecInput,
    proof_chk: &impl TxProofVerifier,
    spent_idx: &impl SpentIndex,
) -> Result<CheckpointDraft, StateError>;
```

From bundle_lane_impl.rs:
```rust
pub(crate) fn load_tx_pkg(path: &Path) -> Result<TxPackage, String>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>040-03 Producer Path</name>
  <files>
    crates/z00z_wallets/src/core/tx/prover.rs
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
    crates/z00z_wallets/tests/test_spend_prover_contract.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `114-156`, `421-440`, `585-618`, `755-785`, `840-870`, and `931-961` in full, because that pre-read is mandatory in `040-TODO.md`. Per D-04, D-06, D-07, and D-08, implement `040-03` by keeping the producer on the existing wallet/Stage-4 path. Feed the stronger carrier and canonical statement from Plan 01 into the live producer seam without reconstructing a new witness system. Reuse `SPEND_AUTH_CTX`, current identity signing, and `build_public_spend_contract(...)` so Stage 4 emits the proof carrier from existing witness material and fails closed on mismatched package/witness data.</action>
  <verify>
    <automated>Run the focused producer gates `cargo test -p z00z_wallets --release --features test-fast test_spend_prover_contract -- --nocapture` and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_spend_gate -- --nocapture` first so producer/verifier contract validation order matches `040-TODO.md`. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun those focused producer gates before closing the task so the canonical-statement emission and mismatched witness/package rejection rules remain explicit. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-03 Producer Path"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>Stage 4 or equivalent wallet code emits the canonical regular-spend proof carrier from existing witness material, and `test_spend_prover_contract.rs` proves canonical statement emission plus fail-closed witness/package mismatch rejection.</done>
</task>

<task type="auto">
  <name>040-04 Verifier Path</name>
  <files>
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_wallets/src/core/tx/state_update.rs
    crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
    crates/z00z_wallets/tests/test_tx_proof_verifier.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `421-468`, `585-631`, `701-728`, `840-870`, and `931-961` in full, because that pre-read is mandatory in `040-TODO.md`. Per D-04, D-05, D-06, D-07, D-08, and D-10, implement `040-04` through the shared checkpoint-facing verifier seam. Add the concrete proof-consumption logic behind `TxProofVerifier` and route it into checkpoint apply via `build_cp_draft(...)`/state-update hooks, rejecting suite mismatch, statement mismatch, malformed proof bytes, wrong resolved input, wrong root, and wrong output statement. Keep the current Stage-6 simulator check as a temporary adapter over the real verifier path; do not invent a standalone `CheckpointProof` object or a parallel membership model.</action>
  <verify>
    <automated>Run the focused verifier gates `cargo test -p z00z_wallets --release --features test-fast test_tx_proof_verifier -- --nocapture && cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_checkpoint_acceptance -- --nocapture && cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_spend_gate -- --nocapture` first so producer/verifier contract validation order matches `040-TODO.md`. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun those focused verifier gates before closing the task so the full reject matrix from `040-TODO.md` remains explicit. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-04 Verifier Path"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>Checkpoint-facing verification consumes the shared regular-spend proof contract through `TxProofVerifier`, placeholder proof bytes no longer stand in for proof semantics, and the verifier reject matrix is covered by focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 4 producer -> persisted tx package | Producer output becomes a reusable proof-bearing package consumed later by simulator and checkpoint hooks |
| checkpoint apply -> proof verifier | State mutation depends on accurate shared proof verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-040-05 | T | Stage 4 producer path | mitigate | Emit the stronger carrier from existing witness inputs and reject mismatched witness/package inputs before persistence |
| T-040-06 | S | spend authorization | mitigate | Reuse `SPEND_AUTH_CTX` and canonical statement signing so producer and verifier authenticate the same transcript |
| T-040-07 | E | checkpoint verifier path | mitigate | Route verification through `TxProofVerifier` inside `state_update.rs` instead of a parallel verifier object |
| T-040-08 | D | Stage 6 adapter | mitigate | Keep adapter status explicit and subordinate to the real verifier backend with regression tests guarding mismatch rejection |

</threat_model>

<verification>
Re-read `prover.rs`, `state_update.rs`, `bundle_lane_impl.rs`, and the focused verifier/prover tests together to confirm the producer and consumer now share one proof contract.
</verification>

<success_criteria>
`040-03` and `040-04` are closed through the existing wallet, simulator, and checkpoint seams, with no new proof object or shadow verifier introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/040-spend-proof/040-02-SUMMARY.md`.
</output>
