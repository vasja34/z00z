---
status: complete
phase: 040-spend-proof
source: 040-01-SUMMARY.md, 040-02-SUMMARY.md, 040-03-SUMMARY.md, 040-04-SUMMARY.md, 040-05-SUMMARY.md, 040-06-SUMMARY.md, 040-07-SUMMARY.md, 040-08-SUMMARY.md, 040-09-SUMMARY.md, 040-VALIDATION.md
started: 2026-04-25T00:00:00Z
updated: 2026-04-29T00:00:00Z
---

# Phase 040 UAT

## Internal Relation UAT Reset

`040-09-SUMMARY.md` remains the last completed baseline, but this UAT artifact
now tracks the active `040-10` internal theorem-relation closure sweep.

Internal theorem-relation closure readiness is measured against the canonical
`regular_spend_theorem_bpplus` suite only.

## Current Test

[testing complete]

## Tests

### 1. Proof Carrier Foundation

expected: A regular spend package now carries one explicit versioned proof/auth carrier through the canonical tx wire. Unknown carrier versions reject, stronger-mode empty placeholders reject, and the package digest remains the only public proof-binding root instead of a bare wire digest.
result: [passed]

### 2. Canonical Producer And Verifier Contract

expected: The live wallet or Stage-4 producer emits the canonical spend proof/auth carrier from existing witness material, and the public verifier rejects malformed proof bytes, statement drift, root drift, and input-binding drift without introducing a parallel proof architecture.
result: [passed]

### 3. Nullifier And Full-Verifier Boundary

expected: Regular-spend nullifiers stay deterministic and chain-scoped, remain separate from claim nullifiers, and both wallet and simulator paths prove that local wire validation alone is insufficient when the public spend contract fails.
result: [passed]

### 4. Stage 4-6-11 Roundtrip Continuity

expected: The proof-bearing Stage-4 package survives Stage-6 reload and Stage-11 handoff without statement drift, and stage-surface wording stays honest about the current checkpoint-facing boundary.
result: [passed]

### 5. Checkpoint Reuse And Shortcut Guard

expected: The regular-spend path still reuses the existing checkpoint pipeline instead of inventing a standalone checkpoint-proof object, and the shipped package does not grow shortcut surfaces such as standalone proof authority, receiver_cards, or a separate C_fee contract.
result: [passed]

### 6. Internal Theorem-Relation Closeout Alignment

expected: Phase 040 current-authority files, runtime artifacts, and tests all
agree on one canonical internal theorem-relation suite, explicit membership
witness composition against `prev_root`, no semantic aliases, and no completed
claim for public/trustless proof-of-knowledge, checkpoint theorem finality, or
rollup settlement closure.
result: [passed for internal relation; public proof closure remains open]

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

The internal theorem-relation UAT gate is green for the wallet and simulator
scope: the backend proof-generation seam now rejects witnesses that do not
satisfy typed statement public inputs, explicit membership against `prev_root`,
nullifier, balance, and range-relation checks. Phase 040 must still keep the
stronger public/trustless proof-of-knowledge, checkpoint theorem finality, and
rollup settlement closure as open follow-up scope rather than completed UAT.

## Verify-Work Closeout

2026-04-29: `gsd-verify-work 040` resumed the existing UAT artifact, found
six passed checkpoints, zero issues, zero pending tests, zero skipped tests, and
zero blocked tests, then marked the UAT session complete. No gap-diagnosis or
gap-closure planning was required. The security gate remains separate because
security enforcement is enabled and no `040-SECURITY.md` artifact exists yet.

2026-04-29: Follow-up review hardened direct backend public-relation checks,
fixed output theorem leaf projection to use the `leaf_ad_id` namespace, and
kept public/trustless proof-of-knowledge, checkpoint theorem finality, and
rollup settlement closure as explicit open boundaries. Current validation is
green for `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`,
`cargo test -p z00z_wallets --release --features test-fast --test test_spend_proof_backend -- --nocapture`,
`cargo test -p z00z_wallets --release --features test-fast --test test_tx_proof_verifier -- --nocapture`,
`cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture`, and
`cargo test --release --features test-fast --features wallet_debug_dump`.
