---
phase: 040-spend-proof
plan: "10"
type: execute
wave: 10
status: in_progress
depends_on:
  - "040-09"
files_modified:
  - .planning/phases/040-spend-proof/040-10-PLAN.md
  - .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
  - .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
  - .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
  - .planning/phases/040-spend-proof/040-CONTEXT.md
  - .planning/phases/040-spend-proof/040-TODO.md
  - .planning/phases/040-spend-proof/040-VALIDATION.md
  - .planning/phases/040-spend-proof/040-TEST-SPEC.md
  - .planning/phases/040-spend-proof/040-UAT.md
  - crates/z00z_wallets/src/core/tx/tx_wire_types.rs
  - crates/z00z_wallets/src/core/tx/mod.rs
  - crates/z00z_wallets/src/core/tx/prover.rs
  - crates/z00z_wallets/src/core/tx/spend_proof_backend.rs
  - crates/z00z_wallets/src/core/tx/spend_rules.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/state_update.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/src/core/tx/witness_gate.rs
  - crates/z00z_wallets/tests/support/test_phase040_spend_proof_support.rs
  - crates/z00z_wallets/tests/test_spend_proof_wire.rs
  - crates/z00z_wallets/tests/test_spend_statement.rs
  - crates/z00z_wallets/tests/test_spend_proof_backend.rs
  - crates/z00z_wallets/tests/test_spend_prover_contract.rs
  - crates/z00z_wallets/tests/test_spend_nullifier_semantics.rs
  - crates/z00z_wallets/tests/test_tx_proof_verifier.rs
  - crates/z00z_wallets/tests/test_tx_pass.rs
  - crates/z00z_wallets/tests/test_tx_poison.rs
  - crates/z00z_wallets/tests/test_tx_drift.rs
  - crates/z00z_wallets/tests/test_e2e_send_scan.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_simulator/src/scenario_1/stage_11.rs
  - crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
  - crates/z00z_simulator/src/scenario_1/outputs/transactions/tx_alice_to_bob_pkg.json
  - crates/z00z_simulator/tests/test_genesis_unit.rs
  - crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
  - crates/z00z_simulator/tests/test_scenario1_tx_proof_roundtrip.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_simulator/tests/test_tx_handoff_integration.rs
  - crates/z00z_rollup_node/src/lib.rs
  - crates/z00z_rollup_node/tests/test_settlement_theorem.rs
autonomous: true
scope:
  - "internal theorem-relation authority reset"
  - "single canonical theorem suite with no semantic v1/v2 branches"
  - "spend backend relation and carrier migration"
  - "authoritative prev_root membership composition inside the backend witness path"
  - "simulator runtime wiring for membership-aware proof generation"
  - "public proof-of-knowledge, checkpoint theorem finality, and rollup settlement closure kept open"
requested_mode: "internal-theorem-relation-closure"
files_expected:
  - .planning/phases/040-spend-proof/040-10-PLAN.md
  - .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
  - .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
  - .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
  - .planning/phases/040-spend-proof/040-CONTEXT.md
  - .planning/phases/040-spend-proof/040-TODO.md
  - .planning/phases/040-spend-proof/040-VALIDATION.md
  - .planning/phases/040-spend-proof/040-TEST-SPEC.md
  - .planning/phases/040-spend-proof/040-UAT.md
  - crates/z00z_wallets/src/core/tx/tx_wire_types.rs
  - crates/z00z_wallets/src/core/tx/spend_proof_backend.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
  - crates/z00z_rollup_node/src/lib.rs
truths:
  - "The live wallet spend seam now uses `regular_spend_theorem_bpplus` and `CanonicalSpendProofBackend`; `040-09-SUMMARY.md` remains the honest statement-bound historical baseline."
  - "The current target of this plan is internal theorem-relation closure, not public/trustless theorem closure."
  - "The internal theorem relation is the theorem-lift of the canonical spend statement and current spend rules, with the minimal base witness `receiver_secret + ordered s_in[i]` and an explicit membership sub-witness sourced from the existing `prev_root` resolver path."
  - "Phase 040 may claim wallet/simulator internal relation closure only after membership, nullifier, balance, range, and statement-only tamper tests pass; public proof-of-knowledge, checkpoint theorem finality, and rollup settlement admission remain open."
must_haves:
  truths:
    - "Do not keep a semantic dual path alive: no live `StatementBoundSpendProofBackend`, no live theorem-v2 branch, and no current semantic aliasing between statement-bound and theorem carriers."
    - "Do not modify vendor code under `crates/z00z_crypto/tari/`; any proving-surface expansion must happen in workspace-owned adapter code."
    - "Do not invent sender-secret witness slots or speculative theorem inputs; freeze the witness table from live producer paths first, then add only the explicit membership sub-witness required for `prev_root` composition."
    - "Do not treat `test_scenario1_stage_surface.rs` as sufficient proof by itself; internal relation closure must also prove the live tx package and simulator runtime path on executable code, while Stage 11 theorem finality and rollup settlement remain open."
    - "Do not leave legacy/dead compatibility code on the proof path: no state-only acceptance shortcut, no dormant fallback verifier, no deprecated legacy decoder, and no hidden migration shim may survive once the canonical theorem path lands."
  artifacts:
    - ".planning/phases/040-spend-proof/040-Spend-Proof-Spec.md"
    - ".planning/phases/040-spend-proof/040-INTEGRITY-GATES.md"
    - ".planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md"
    - ".planning/phases/040-spend-proof/040-CONTEXT.md"
    - ".planning/phases/040-spend-proof/040-TODO.md"
    - "crates/z00z_wallets/src/core/tx/spend_proof_backend.rs"
    - "crates/z00z_wallets/src/core/tx/spend_verification.rs"
    - "crates/z00z_simulator/src/scenario_1/stage_11_apply.rs"
    - "crates/z00z_rollup_node/src/lib.rs"
  key_links:
    - "canonical suite id -> `regular_spend_theorem_bpplus`"
    - "theorem contract -> lifted `verify_spend_rules(...)` over canonical statement `S` and witness table `W`"
    - "membership source -> existing `prev_root` checkpoint/pre-state resolver path, upgraded into an explicit theorem sub-witness"
---

<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD047 MD060 -->

<objective>
Execute the Phase 040 internal theorem-relation closure plan that removes the
statement-bound legacy branch from the wallet spend backend and lands one
canonical internal relation path for proof generation.

Unlike `040-09-PLAN.md`, this file is not an honest-partial reopen. It is the
execution blueprint for the narrowed closure target the user corrected:

1. one canonical theorem suite with no semantic `*_v1` or `*_v2` branches,
2. one explicit internal theorem relation `T(S, W)` instead of a statement-bound
  envelope at proof-generation time,
3. authoritative `prev_root` membership composition inside the theorem path,
4. simulator runtime wiring through the existing Stage 4 package path,
5. explicit non-closure for public/trustless proof-of-knowledge, checkpoint
  theorem finality, and rollup settlement proof closure, and
6. focused runtime and tamper coverage on the canonical internal path.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/gsd-plan-phase/SKILL.md
@.github/skills/smart-tests-bootstrap/SKILL.md
@.github/skills/z00z-git-versioning/SKILL.md
@.github/skills/doublecheck/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/040-spend-proof/040-CONTEXT.md
@.planning/phases/040-spend-proof/040-TODO.md
@.planning/phases/040-spend-proof/040-09-NEXT.md
@.planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
@.planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
@.planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
@.planning/phases/040-spend-proof/040-VALIDATION.md
@.planning/phases/040-spend-proof/040-TEST-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>040-10-01 Final Authority Reset And Canonical Theorem Freeze</name>
  <files>
    .planning/phases/040-spend-proof/040-10-PLAN.md
    .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
    .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
    .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
    .planning/phases/040-spend-proof/040-CONTEXT.md
    .planning/phases/040-spend-proof/040-TODO.md
    .planning/phases/040-spend-proof/040-VALIDATION.md
    .planning/phases/040-spend-proof/040-TEST-SPEC.md
    .planning/phases/040-spend-proof/040-UAT.md
  </files>
  <action>Rewrite the Phase 040 authority chain so the final target is a single
  canonical theorem path instead of a residual-handoff closeout. Freeze all of
  the following before any Rust edits begin: the canonical suite id
  `regular_spend_theorem_bpplus`; the canonical artifact semantics for
  `TxProofWire` or `SpendProofArtifact`; the exact theorem contract `T(S, W)` as
  the theorem-lift of the canonical spend statement and spend rules; and the
  witness-source table grounded in live producer paths. The frozen witness table
  must keep the minimal base witness `receiver_secret + ordered s_in[i]` and may
  only add an explicit membership sub-witness sourced from the existing
  `prev_root` resolver path. Ban semantic legacy aliases such as
  `regular_spend_statement_bound_v1`, `regular_spend_theorem_bpplus_v1`,
  `theorem_v2`, or any dual-path wording in current phase authorities.</action>
  <verify>
    <automated>Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Then run `rg -n "regular_spend_statement_bound_v1|regular_spend_theorem_bpplus_v1|theorem_v2|StatementBoundSpendProofBackend|still open|residual future work|statement-bound envelope" .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md .planning/phases/040-spend-proof/040-CONTEXT.md .planning/phases/040-spend-proof/040-TODO.md .planning/phases/040-spend-proof/040-VALIDATION.md .planning/phases/040-spend-proof/040-TEST-SPEC.md .planning/phases/040-spend-proof/040-UAT.md`. The resulting current-authority files must either contain none of those legacy phrases or must mark them strictly as historical retraction context. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-10-PLAN.md current_task="040-10-01 Final Authority Reset And Canonical Theorem Freeze"` in YOLO mode at least 3 times, and stop running it only after at least 2 consecutive runs show no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>All current Phase 040 authority files freeze one canonical theorem target,
  one theorem contract, and one grounded witness-source table, with no live
  semantic `*_v1` or `*_v2` closure language left in the current phase
  authorities.</done>
</task>

<task type="auto">
  <name>040-10-02 Canonical Spend Carrier And Backend Migration</name>
  <files>
    crates/z00z_wallets/src/core/tx/tx_wire_types.rs
    crates/z00z_wallets/src/core/tx/mod.rs
    crates/z00z_wallets/src/core/tx/prover.rs
    crates/z00z_wallets/src/core/tx/spend_proof_backend.rs
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_wallets/src/core/tx/tx_verifier.rs
    crates/z00z_wallets/tests/support/test_phase040_spend_proof_support.rs
    crates/z00z_wallets/tests/test_spend_proof_wire.rs
    crates/z00z_wallets/tests/test_spend_proof_backend.rs
    crates/z00z_wallets/tests/test_spend_prover_contract.rs
    crates/z00z_wallets/tests/test_tx_proof_verifier.rs
    crates/z00z_simulator/src/scenario_1/outputs/transactions/tx_alice_to_bob_pkg.json
    crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  </files>
  <action>Replace the shipped statement-bound carrier and backend with one live
  canonical theorem carrier on the existing `TxProofWire` path. Remove
  `StatementBoundSpendProofBackend` from the live seam, retire theorem-v2-only
  framing and negative-only fixture naming, and converge the runtime and test
  surface on `regular_spend_theorem_bpplus` as the only semantic suite id. If a
  low-level codec byte remains necessary for serialization safety, keep exactly
  one canonical encoding path and delete all alternative decoders,
  bridge shadow layers, fallback readers, witness-replay adapters, and dead
  branch-selecting legacy code once the canonical path lands. Do not leave any
  user-visible or branch-selecting `*_v1` or `*_v2` proof semantics in code,
  tests, or runtime artifacts. The backend migration is not complete until the
  backend test matrix proves theorem-artifact acceptance and legacy rejection
  without witness replay.</action>
  <progress>2026-04-28: the canonical backend `prove()` path now rejects witness
  values that do not satisfy the typed public inputs carried by the canonical
  statement. This closes the immediate local witness-insensitivity bug in proof
  generation, but it is not a final full-ZK theorem closure because public
  verification still checks a deterministic artifact over public statement
  bytes and does not yet verify a cryptographic proof of knowledge for the
  private witness or membership sub-witness.</progress>
  <progress>2026-04-29: review found that direct backend public-relation checks
  compared input theorem `leaf_ad_id` values against output state-key asset IDs
  because typed output leaves were built through the state-key `asset_wire_to_leaf`
  projection. The statement builder and Phase 040 test fixture now project output
  `AssetLeaf.asset_id` as the output `leaf_ad_id` namespace for theorem checks,
  and `test_backend_rejects_forged_overlap` proves forged deterministic artifacts
  reject on input/output leaf overlap. Focused validation passed with
  `cargo test -p z00z_wallets --release --features test-fast --test test_spend_proof_backend -- --nocapture`
  and `cargo test -p z00z_wallets --release --features test-fast --test test_tx_proof_verifier -- --nocapture`.</progress>
  <progress>2026-04-29: stage-surface and broad release validation are green after
  the UAT wording sync. Validation passed with `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture`,
  `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`, and
  `cargo test --release --features test-fast --features wallet_debug_dump`.
  Current closeout remains limited to internal theorem-relation readiness; public
  proof-of-knowledge, checkpoint theorem finality, and full rollup settlement
  proof closure stay open.</progress>
  <verify>
    <automated>Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump`. After the broad gate, run `cargo test -p z00z_wallets --release --features test-fast --test test_spend_proof_wire -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_spend_statement -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_spend_proof_backend -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_spend_prover_contract -- --nocapture`, and `cargo test -p z00z_wallets --release --features test-fast --test test_tx_proof_verifier -- --nocapture`. Then run `rg -n "regular_spend_statement_bound_v1|regular_spend_theorem_bpplus_v1|theorem_v2|StatementBoundSpendProofBackend" crates/z00z_wallets/src/core/tx crates/z00z_wallets/tests crates/z00z_simulator/src/scenario_1/outputs/transactions/tx_alice_to_bob_pkg.json crates/z00z_simulator/tests/test_scenario1_stage_surface.rs`. That search must return no live semantic branches. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-10-PLAN.md current_task="040-10-02 Canonical Spend Carrier And Backend Migration"` in YOLO mode at least 3 times, and stop running it only after at least 2 consecutive runs show no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The only live spend carrier and backend path is the canonical theorem path,
  and no statement-bound or theorem-v2 semantic branch, bridge shadow layer,
  fallback reader, witness-replay adapter, or dead legacy proof code remains in
  live code, tests, or runtime transaction artifacts.</done>
</task>

<task type="auto">
  <name>040-10-03 Public Input, Membership, And Nullifier Closure</name>
  <files>
    crates/z00z_wallets/src/core/tx/spend_rules.rs
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_wallets/src/core/tx/state_update.rs
    crates/z00z_wallets/src/core/tx/tx_verifier.rs
    crates/z00z_wallets/src/core/tx/witness_gate.rs
    crates/z00z_wallets/tests/test_spend_statement.rs
    crates/z00z_wallets/tests/test_spend_nullifier_semantics.rs
    crates/z00z_wallets/tests/test_tx_proof_verifier.rs
    crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
  </files>
  <action>Close the theorem-content gap, not just the carrier gap. The canonical
  theorem must prove the public input surface, authoritative membership against
  `prev_root`, digest-root discipline, and replay-safe nullifier semantics as
  one coherent relation. Do not leave input membership as an external checkpoint
  assumption. Do not add `receiver_cards` to the persisted tx package. Do not
  split fee into a new `C_fee` contract. Keep the canonical package digest as
  the only public proof-binding root and make every recomputation drift fail
  closed on the verifier seam. The wallet, public-verifier, and simulator test
  matrices for this task must prove theorem-artifact acceptance and legacy
  rejection without witness replay, local theorem emulation, or a state-only
  acceptance shortcut.</action>
  <verify>
    <automated>Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump`. After the broad gate, run `cargo test -p z00z_wallets --release --features test-fast --test test_spend_statement -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_spend_nullifier_semantics -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_tx_proof_verifier -- --nocapture`, and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_checkpoint_acceptance -- --nocapture`. Then run `rg -n "prev_root|receiver_cards|C_fee|build_tx_package_digest|compute_tx_digest_from_wire|nullifier" crates/z00z_wallets/src/core/tx crates/z00z_wallets/tests crates/z00z_simulator/tests/test_checkpoint_acceptance.rs`. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-10-PLAN.md current_task="040-10-03 Public Input, Membership, And Nullifier Closure"` in YOLO mode at least 3 times, and stop running it only after at least 2 consecutive runs show no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The canonical theorem path is self-contained over its public input surface,
  explicit membership composition, digest discipline, and replay-safe nullifier
  semantics, with no external membership assumption left implicit and no
  witness-replay or state-only acceptance fallback left live.</done>
</task>

<task type="auto">
  <name>040-10-04 Checkpoint Pipeline Internal Continuity On The Existing Pipeline</name>
  <files>
    crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_validation_gates.rs
    crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
    crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
    crates/z00z_simulator/src/scenario_1/stage_11.rs
    crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
    crates/z00z_simulator/tests/test_scenario1_tx_proof_roundtrip.rs
    crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
    crates/z00z_simulator/tests/test_tx_handoff_integration.rs
  </files>
  <action>Do not start this task until `040-10-02` and `040-10-03` have landed
  with executable theorem-artifact acceptance and legacy rejection evidence on
  the wallet and public-verifier seams. Preserve and strengthen the existing
  Stage 4 -> Stage 6 -> Stage 11 package-coupled pipeline so it consumes the
  same canonical internal theorem relation as wallet production and public
  verification. This task must close internal continuity and replay-regression
  evidence on the existing pipeline without inventing a shadow
  `CheckpointProof` lane. The current checkpoint seam remains package-coupled
  continuity/admission evidence; checkpoint theorem finality stays an explicit
  open boundary until a real public/trustless checkpoint theorem path lands.</action>
  <verify>
    <automated>Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump`. After the broad gate, run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_tx_proof_roundtrip -- --nocapture`, `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_checkpoint_acceptance -- --nocapture`, and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_tx_handoff_integration -- --nocapture`. Then run `rg -n "Stage 11|checkpoint|proof_suite|receiver_cards|C_fee|shadow proof|parallel" crates/z00z_simulator/src/scenario_1 crates/z00z_simulator/tests/test_scenario1_tx_proof_roundtrip.rs crates/z00z_simulator/tests/test_checkpoint_acceptance.rs crates/z00z_simulator/tests/test_tx_handoff_integration.rs`. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-10-PLAN.md current_task="040-10-04 Checkpoint Pipeline Internal Continuity On The Existing Pipeline"` in YOLO mode at least 3 times, and stop running it only after at least 2 consecutive runs show no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The existing Stage 4 -> Stage 6 -> Stage 11 path consumes the same
  canonical internal theorem relation used by the wallet and verifier, remains
  free of statement-bound or package-only fallbacks, and documents checkpoint
  theorem finality as open follow-up scope.</done>
</task>

<task type="auto">
  <name>040-10-05 Rollup Public-Artifact Binding Guard</name>
  <files>
    crates/z00z_rollup_node/src/lib.rs
    crates/z00z_rollup_node/src/lifecycle.rs
    crates/z00z_rollup_node/src/status.rs
    crates/z00z_rollup_node/src/rpc.rs
    crates/z00z_rollup_node/tests/test_settlement_theorem.rs
  </files>
  <action>Do not start this task until `040-10-04` has landed on the existing
  package-coupled pipeline; rollup public-artifact binding is downstream of the
  wallet theorem backend and checkpoint continuity evidence, not a parallel
  substitute. Strengthen the workspace-owned rollup node so settlement
  admission binds the transaction package, public spend artifact, checkpoint
  artifact, checkpoint link, and execution input together instead of treating
  output range proofs alone as settlement closure. This task may add
  workspace-owned rollup helpers and tests, but it must not fork theorem
  semantics into a rollup-specific proof contract and must keep full
  public/trustless rollup settlement proof closure as an explicit open
  boundary.</action>
  <verify>
    <automated>Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump`. After the broad gate, run `cargo test -p z00z_rollup_node --release --lib -- --nocapture` and `cargo test -p z00z_rollup_node --release --test test_settlement_theorem -- --nocapture`. Then run `rg -n "OutputProof|verify_output_proof|verify_batch_proofs|settlement|checkpoint|proof_suite" crates/z00z_rollup_node/src crates/z00z_rollup_node/tests`. The final rollup seam must no longer rely only on `OutputProof` range-proof helpers, but full rollup settlement proof closure must remain documented as open. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-10-PLAN.md current_task="040-10-05 Rollup Public-Artifact Binding Guard"` in YOLO mode at least 3 times, and stop running it only after at least 2 consecutive runs show no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The rollup node verifies public-artifact binding across package, spend
  artifact, checkpoint artifact, link, and execution input, output-only
  range-proof helpers are no longer the terminal settlement admission boundary,
  and full rollup settlement proof closure remains open.</done>
</task>

<task type="auto">
  <name>040-10-06 Init, Runtime, E2E, And Internal Closeout Sync</name>
  <files>
    .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
    .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
    .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
    .planning/phases/040-spend-proof/040-CONTEXT.md
    .planning/phases/040-spend-proof/040-TODO.md
    .planning/phases/040-spend-proof/040-VALIDATION.md
    .planning/phases/040-spend-proof/040-TEST-SPEC.md
    .planning/phases/040-spend-proof/040-UAT.md
    crates/z00z_simulator/tests/test_genesis_unit.rs
    crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
    crates/z00z_simulator/tests/test_scenario1_tx_proof_roundtrip.rs
    crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
    crates/z00z_simulator/tests/test_tx_handoff_integration.rs
    crates/z00z_wallets/tests/test_tx_pass.rs
    crates/z00z_wallets/tests/test_tx_poison.rs
    crates/z00z_wallets/tests/test_tx_drift.rs
    crates/z00z_wallets/tests/test_e2e_send_scan.rs
    crates/z00z_simulator/src/scenario_1/outputs/transactions/tx_alice_to_bob_pkg.json
  </files>
  <action>Run the final init, runtime, and e2e closure sweep on the canonical
  internal theorem path, then synchronize every Phase 040 truth surface to the
  bounded internal closeout boundary. This task must update exact-literal
  stage-surface tests and owning docs together; changing only the docs or only
  the test is not sufficient. Final closure evidence must include a live
  tx-package guard, executable runtime proof, and e2e acceptance evidence for
  the wallet/simulator internal relation. Current phase docs must remove the
  statement-bound live suite and legacy semantic aliases while continuing to
  say that public/trustless proof-of-knowledge, checkpoint theorem finality,
  and full rollup settlement proof closure remain open.</action>
  <verify>
    <automated>Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump`, `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump`, and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`. After that, run the focused init and e2e matrix: `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_genesis_unit -- --nocapture`, `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_tx_proof_roundtrip -- --nocapture`, `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_checkpoint_acceptance -- --nocapture`, `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_tx_handoff_integration -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_tx_pass -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_tx_poison -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_tx_drift -- --nocapture`, and `cargo test -p z00z_wallets --release --features test-fast --test test_e2e_send_scan -- --nocapture`. Then run `rg -n "regular_spend_statement_bound_v1|regular_spend_theorem_bpplus_v1|theorem_v2|residual future work|statement-bound envelope" .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md .planning/phases/040-spend-proof/040-CONTEXT.md .planning/phases/040-spend-proof/040-TODO.md .planning/phases/040-spend-proof/040-VALIDATION.md .planning/phases/040-spend-proof/040-TEST-SPEC.md .planning/phases/040-spend-proof/040-UAT.md crates/z00z_wallets/src/core/tx crates/z00z_wallets/tests crates/z00z_simulator/src/scenario_1/outputs/transactions/tx_alice_to_bob_pkg.json crates/z00z_simulator/tests/test_scenario1_stage_surface.rs crates/z00z_rollup_node/src`. Only historical audit reports may retain those older phrases; current authority may retain explicit open-boundary wording for public proof-of-knowledge, checkpoint theorem finality, and rollup settlement proof closure. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-10-PLAN.md current_task="040-10-06 Init, Runtime, E2E, And Internal Closeout Sync"` in YOLO mode at least 3 times, and stop running it only after at least 2 consecutive runs show no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The canonical internal theorem path is the only live wallet/simulator proof
  path across init, runtime, package, checkpoint-adapter, and e2e surfaces;
  current Phase 040 docs no longer carry legacy statement-bound semantics and
  explicitly keep public/trustless proof-of-knowledge, checkpoint theorem
  finality, and full rollup settlement proof closure open.</done>
</task>

</tasks>

<migration_note>
This file uses the same execution grammar as `040-09-PLAN.md`, but it replaces
the honest-partial residual-handoff target with an internal theorem-relation
closure target. The scope is explicit and must be reflected in the owning spec,
context, TODO, validation, and closeout ledgers before Rust edits begin.
</migration_note>

# 040-10 Internal Theorem-Relation Closure Blueprint

## Scope Note

`040-09-NEXT.md` correctly concluded that the then-current live seam was still
statement-bound and that the immediate missing layer was a real backend relation
plus authoritative membership composition. This plan turns that report into the
internal relation closure blueprint requested by the user.

The important distinction is scope:

1. `040-09-NEXT.md` described the current truthful boundary.
2. `040-10-PLAN.md` defines the implementation required to remove that
  boundary on the internal wallet proof-generation path.

This plan therefore does not describe public/trustless theorem closure as
already closed. Instead, it specifies the exact work required to make the
internal backend relation true.

## 040-09-NEXT Coverage Freeze

`040-09-NEXT.md` recommended five concrete requirements for the next plan. This
plan is valid only if every one of them remains explicit and enforceable:

1. exact public/private witness contract -> frozen in `040-10-01`, Canonical
  Target Freeze, and Exact Theorem Contract
2. decision on the approved stack versus a seam change -> Option C is required;
  Option D is disallowed unless the spec is rewritten first
3. authoritative membership composition against `prev_root` -> frozen in
  `040-10-03`
4. theorem-artifact acceptance plus legacy rejection without witness replay ->
  required in `040-10-02` and `040-10-03` backend, public-verifier, and
  simulator matrices
5. checkpoint theorem finality and rollup settlement proof closure are stronger
  future seams and must stay open until an actual public/trustless proof path
  is implemented

## Objective

Close the internal proof-generation relation gap on one canonical path.

The required final state is:

1. one canonical semantic suite id,
2. one canonical theorem carrier,
3. one canonical theorem contract `T(S, W)`,
4. one canonical membership composition against `prev_root`,
5. one simulator runtime path that passes membership witnesses into proof
  generation, and
6. one focused runtime/e2e matrix that proves the legacy branch is gone while
  leaving public proof-of-knowledge, checkpoint theorem finality, and rollup
  settlement closure open.

## Pros/Cons Decision Gate

Every material decision in this plan must be recorded with explicit pros and
cons in execution notes, review notes, and summary artifacts.

### Option A: Keep the honest statement-bound reopen and hand internal relation closure forward

Pros:

- minimizes immediate code churn,
- preserves current live behavior.

Cons:

- does not satisfy the requested internal relation closure target,
- preserves residual proof gaps,
- keeps legacy semantic names and old truth surfaces alive.

Verdict:

- **Disallowed** for this plan.

### Option B: Keep both statement-bound and theorem paths alive during migration

Pros:

- may reduce immediate rollout risk,
- can ease fixture migration.

Cons:

- violates the user requirement for one canonical path,
- leaves semantic `*_v1` or `*_v2` branches alive,
- increases audit surface and drift risk.

Verdict:

- **Disallowed**. No temporary codec bridge, dormant bridge shadow layer,
  deprecated decoder, or hidden fallback reader may survive as part of the
  final state.

### Option C: One canonical internal theorem path on the existing spend pipeline with workspace-owned adapter expansion if needed

Pros:

- satisfies the requested internal relation closure target,
- preserves the one-pipeline architecture,
- allows the theorem surface to grow in workspace-owned code without touching
  vendor Tari code,
- closes the statement-bound wallet backend gap under one contract while keeping
  checkpoint theorem finality and rollup settlement proof closure separate.

Cons:

- requires a spec-first authority rewrite,
- forces a broader validation and artifact migration sweep,
- may require new rollup settlement tests and helpers.

Verdict:

- **Required path** for this plan.

### Option D: Replace the proving surface with a new parallel proof system immediately

Pros:

- could provide a more expressive theorem surface.

Cons:

- introduces concept drift relative to the current spend pipeline,
- risks creating a shadow proof lane,
- widens the phase before the existing seam is fully exhausted.

Verdict:

- **Disallowed** unless Option C proves impossible and the spec is explicitly
  rewritten first.

## Repository-Proven Starting Point

The current repository proves all of the following:

1. the live wallet suite is `regular_spend_theorem_bpplus`,
2. the live default backend is `CanonicalSpendProofBackend`,
3. backend proof generation rejects statement-only, wrong-root, membership,
  nullifier, balance, and range-relation tampering,
4. the verifier still validates a deterministic canonical artifact rather than
  a public/trustless cryptographic proof of witness knowledge,
5. Stage 11 remains the existing package-coupled acceptance seam, and
6. `z00z_rollup_node` currently proves output range proofs, not full settlement
  on the spend theorem boundary.

This plan starts from that live baseline and keeps the remaining stronger
public, checkpoint, and rollup gaps explicit.

## Canonical Target Freeze

The frozen internal target for this plan is:

- canonical suite id: `regular_spend_theorem_bpplus`
- no live semantic aliases: no `regular_spend_statement_bound_v1`, no
  `regular_spend_theorem_bpplus_v1`, no `theorem_v2`
- canonical theorem carrier: one live `TxProofWire` or `SpendProofArtifact`
  contract with no legacy semantic branches
- canonical internal relation: one theorem `T(S, W)` that checks the exact spend
  relation currently enforced across canonical statement construction, spend
  rules, nullifier derivation, output range proof checks, and checkpoint root
  continuity
- canonical witness table:
  - base spend witness: `receiver_secret + ordered s_in[i]`
  - membership sub-witness: the explicit path or opening material required to
    bind each referenced input commitment to `prev_root`
  - prohibited inflation: speculative sender-secret slots or unrelated derived
    fields

## Exact Theorem Contract

The plan must freeze the theorem contract in executable terms before landing the
backend. The minimum contract is:

`T(S, W)` proves all of the following simultaneously:

1. `S` is the canonical spend statement built from the live package digest,
   chain or root scope, resolved input refs, public output leaf fields, and
   fee-bearing balance surface.
2. For each spend input `i`, the ordered witness secret `s_in[i]` authorizes
   the input spend relation carried today by the canonical spend rules.
3. `receiver_secret` binds the receiver-facing owner or view relation that the
   current spend rules already depend on.
4. Each referenced input commitment is proven under the explicit membership
   sub-witness against `prev_root`; membership is not left as an external
   checkpoint assumption.
5. Output commitments and output range proofs match the carried public leaf
   fields.
6. The balance equation over inputs, outputs, and fee holds on the same
   canonical root.
7. Nullifier derivation is deterministic in the scoped domain and replay-safe at
   the state boundary.

## No-Dual-Lane Rule

This internal relation scope is closed only if all of the following are true at
closeout:

1. no live statement-bound backend remains,
2. no live theorem-v2 branch remains,
3. no current phase authority file claims public/trustless theorem closure,
4. checkpoint theorem finality remains explicitly open unless a real public
  proof path lands, and
5. rollup settlement seam remains explicitly open unless a real settlement proof
  path lands, and
6. no state-only acceptance shortcut, bridge shadow layer, fallback reader,
   witness-replay adapter, or dead legacy proof code remains anywhere on the
   canonical proof path.

## Validation Commands

The following commands are mandatory anchors for execution:

```bash
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test --release --features test-fast --features wallet_debug_dump
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump
cargo test -p z00z_wallets --release --features test-fast --test test_spend_proof_wire -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_spend_statement -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_spend_proof_backend -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_spend_prover_contract -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_spend_nullifier_semantics -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_tx_proof_verifier -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_tx_pass -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_tx_poison -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_tx_drift -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --test test_e2e_send_scan -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_genesis_unit -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_tx_proof_roundtrip -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_checkpoint_acceptance -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_tx_handoff_integration -- --nocapture
cargo test -p z00z_rollup_node --release --lib -- --nocapture
cargo test -p z00z_rollup_node --release --test test_settlement_theorem -- --nocapture
```

## Completion Gate

Phase 040 is final-closeout ready only when all of the following are true:

- the only live semantic suite is `regular_spend_theorem_bpplus`,
- the theorem contract `T(S, W)` is frozen and executed on the live backend,
- authoritative membership composition against `prev_root` is part of theorem
  proof generation,
- Stage 11 consumes the same package-coupled internal theorem relation as the
  wallet and verifier without becoming checkpoint theorem finality,
- `z00z_rollup_node` verifies public-artifact binding across package,
  checkpoint artifact, link, and execution input without claiming full rollup
  settlement proof closure,
- init, runtime, and e2e matrices are green on the canonical path,
- current Phase 040 authority files keep public/trustless proof-of-knowledge,
  checkpoint theorem finality, and full rollup settlement proof closure as
  explicit open boundaries,
- no live code, tests, or runtime artifact carries the old statement-bound or
  version-suffixed theorem semantics, and
- no bridge shadow layer, dead legacy decoder, witness-replay fallback, or
  state-level acceptance shortcut survives on the proof path.
