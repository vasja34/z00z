---
phase: 040-spend-proof
plan: "06"
type: execute
wave: 6
depends_on:
  - "040-01"
  - "040-03"
  - "040-04"
  - "040-05"
  - "040-06"
  - "040-07"
  - "040-08"
files_modified:
  - .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
  - crates/z00z_wallets/src/core/tx/state_update.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Checkpoint pipeline reuse stays explicit and no separate regular-tx proof layer is introduced outside the existing checkpoint artifact path."
    - "Every missing-code item is closed as implemented or explicitly out of scope under the existing hooks."
    - "The prohibited-shortcut checklist is re-run before phase closeout."
  artifacts:
    - ".planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md"
    - "crates/z00z_simulator/tests/test_checkpoint_acceptance.rs"
  key_links:
    - "TxProofVerifier -> build_cp_draft(...) -> checkpoint acceptance"
    - "040 missing-code list -> primary task outcomes"
    - "shortcut checklist -> final stage-surface / closeout gate"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Finish the Phase 040 closeout gates by proving checkpoint-pipeline reuse, resolving the remaining missing-code ledger honestly, and re-running the shortcut checklist before completion.

Purpose: satisfy D-05, D-06, D-07, D-08, and D-09 by closing the governance and evidence tasks without silently widening scope or overclaiming completion.
Output: a closeout-gates ledger, explicit checkpoint-reuse regression coverage, a missing-code resolution map, and a re-run shortcut checklist tied to the canonical spec/backlog pair.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/040-spend-proof/040-CONTEXT.md
@.planning/phases/040-spend-proof/040-TODO.md
@.planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
@.planning/phases/040-spend-proof/040-05-PLAN.md
@crates/z00z_wallets/src/core/tx/state_update.rs
@crates/z00z_wallets/src/core/tx/tx_verifier.rs
@crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
@crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
@crates/z00z_simulator/tests/test_scenario1_stage_surface.rs

<interfaces>
From state_update.rs:
```rust
pub fn build_cp_draft(
    height: u64,
    snap_id: PrepSnapshotId,
    snapshot: &PrepSnapshot,
    replay: &[PrepReplayEntry],
    link: &CheckpointLink,
    exec: &CheckpointExecInput,
    proof_chk: &impl TxProofVerifier,
    spent_idx: &impl SpentIndex,
) -> Result<CheckpointDraft, StateError>;
```

From tx_verifier.rs:
```rust
pub fn verify_full_tx_package(tx_bytes: &[u8]) -> TxVerifierResult<VerificationResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>040-12 Checkpoint-Pipeline Reuse</name>
  <files>
    .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
    crates/z00z_wallets/src/core/tx/state_update.rs
    crates/z00z_wallets/src/core/tx/tx_verifier.rs
    crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
    crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `585-631` and `657-672` in full, because that pre-read is mandatory before closing `040-09` through `040-14` in `040-TODO.md`. Per D-06, D-07, and D-08, create `.planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md` and close `040-12` by proving the regular-spend path still reuses the typed `prev_root` checkpoint pipeline, `TxProofVerifier`, and package-admission hooks without introducing a separate regular-tx proof layer outside the existing checkpoint artifact path. Add or extend checkpoint acceptance regressions that show package admission and checkpoint apply remain the two explicit verification seams, and document that decision in the closeout ledger.</action>
  <verify>
    <automated>Run the focused checkpoint-reuse gates `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_checkpoint_acceptance -- --nocapture` and `cargo test -p z00z_wallets --release --features test-fast test_tx_verifier_suite -- --nocapture` first so closeout validation preserves the TODO ordering. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun those focused checkpoint-reuse gates before closing the task so the two-seam verification rule stays explicit. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-12 Checkpoint-Pipeline Reuse"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The closeout ledger and checkpoint tests both prove Phase 040 reuses the existing checkpoint pipeline and that no separate regular-tx proof layer was introduced outside the existing checkpoint artifact path.</done>
</task>

<task type="auto">
  <name>040-13 Missing-Code Closure Tasks</name>
  <files>
    .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `585-631` and `657-672` in full, because that pre-read is mandatory before closing `040-09` through `040-14` in `040-TODO.md`. Per D-02, D-05, D-06, D-09, and the `040-13` owners, extend `040-CLOSEOUT-GATES.md` with an explicit missing-code closure matrix that maps each missing-code item to its real outcome: non-empty `TxProofWire` closed by `040-01`, concrete regular-tx prover closed by `040-03`, concrete regular-tx verifier closed by `040-04`, any separate regular-tx proof layer outside the existing checkpoint artifact path kept explicitly out of scope because proof consumption closes through existing checkpoint hooks in `040-04`/`040-06`, and unified output constructor treated only as the bounded `040-08` follow-up. Treat `040-Spend-Proof-Spec.md` and `040-TODO.md` as normative review inputs for this closeout task, not mutable implementation outputs; if wording drift is discovered, record it fail-closed in `040-CLOSEOUT-GATES.md` and escalate it for a separate planning review instead of self-healing the contract here.</action>
  <verify>
    <automated>Run the focused closure-evidence checks `cargo test -p z00z_wallets --release --features test-fast test_tx_verifier_suite -- --nocapture`, `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_checkpoint_acceptance -- --nocapture`, and `rg -n 'non-empty TxProofWire|concrete regular tx prover|concrete regular tx verifier|regular-tx proof layer|checkpoint artifact path|unified output constructor' .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md .planning/phases/040-spend-proof/040-TODO.md .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md` first so closeout validation preserves the TODO ordering. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun those focused wallet/simulator/code-alignment checks before closing the task so the closure matrix is backed by live seams rather than text alone, and rerun the smallest affected focused Rust tests if any truth-alignment note touched code-facing behavior. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-13 Missing-Code Closure Tasks"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The missing-code list is closed honestly in one ledger, and any drift discovered against spec/TODO is recorded fail-closed and escalated for separate planning review rather than mutated during closeout.</done>
</task>

<task type="auto">
  <name>040-14 Prohibited Shortcut Checklist</name>
  <files>
    .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md
    crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `585-631` and `657-672` in full, because that pre-read is mandatory before closing `040-09` through `040-14` in `040-TODO.md`. Per D-03, D-05, D-06, D-08, and the explicit closeout rule in `040-CONTEXT.md`, re-run the four prohibited shortcuts before marking `040-07` complete and before the phase-closeout summary: no STARK-support claims before the non-empty carrier is wired through `TxProofVerifier`; no `receiver_cards` in the regular persisted package; no separate `C_fee` contract without synchronized verifier/tests/spec migration; and no mixed wallet `compute_leaf_ad()` / crypto `derive_leaf_ad()` runtime path without a documented migration plan. Record the result in `040-CLOSEOUT-GATES.md`, and keep `test_scenario1_stage_surface.rs` aligned so stage-surface wording cannot overclaim around any shortcut. Before writing the phase-closeout summary, also re-check the Phase 040 completion gate from `040-TODO.md`: `040-01` through `040-07` complete, all mandatory tests green, spec/TODO/context still aligned, no retired-draft dependency, and this shortcut checklist re-run before marking `040-07` complete.</action>
  <verify>
    <automated>Run the focused shortcut gates `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_stage_surface -- --nocapture` and `rg -n "STARK|receiver_cards|C_fee|compute_leaf_ad|derive_leaf_ad|040-01 through 040-07|mandatory tests|retired draft" .planning/phases/040-spend-proof/040-CLOSEOUT-GATES.md .planning/phases/040-spend-proof/040-TODO.md .planning/phases/040-spend-proof/040-CONTEXT.md` first so closeout validation preserves the TODO ordering. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun those focused shortcut/completion-gate checks before closing the task so every prohibited shortcut and every completion-gate clause is recorded explicitly. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-14 Prohibited Shortcut Checklist"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The shortcut checklist is re-run explicitly before closeout, every prohibition has a recorded outcome, and no stage-surface wording overclaims past the actual implemented Phase 040 boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint pipeline -> closeout evidence | Closeout must prove reuse of existing trust boundaries instead of silently widening them |
| phase artifacts -> final completion claims | Spec/backlog/checklist truth must stay aligned so Phase 040 is not overclosed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-040-21 | E | checkpoint proof boundary | mitigate | Prove reuse of `TxProofVerifier` + checkpoint hooks and forbid any separate regular-tx proof layer outside the existing checkpoint artifact path |
| T-040-22 | R | missing-code closure truth | mitigate | Maintain one explicit closure matrix tied to primary tasks and update canonical artifacts narrowly when wording drifts |
| T-040-23 | I | final shortcut checklist | mitigate | Re-run all four prohibitions and bind stage-surface wording/tests to those outcomes |
| T-040-24 | T | phase closeout claims | mitigate | Keep `040-CLOSEOUT-GATES.md`, `040-TODO.md`, and `040-Spend-Proof-Spec.md` aligned before any completion summary is accepted |

</threat_model>

<verification>
Re-read `040-CLOSEOUT-GATES.md`, the touched checkpoint tests, `040-TODO.md`, and `040-Spend-Proof-Spec.md` together and confirm the phase can close without introducing a separate regular-tx proof layer outside the existing checkpoint artifact path or overclaiming shortcut closure.
</verification>

<success_criteria>
Tasks `040-12` through `040-14` convert the remaining governance and evidence work into explicit closeout artifacts and regressions, leaving no silent blocker or hidden shortcut unresolved.
</success_criteria>

<output>
After completion, create `.planning/phases/040-spend-proof/040-06-SUMMARY.md`.
</output>
