---
phase: 040-spend-proof
plan: "03"
type: execute
wave: 3
depends_on:
  - "040-02"
  - "040-03"
  - "040-04"
files_modified:
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/spend_rules.rs
  - crates/z00z_wallets/src/core/tx/state_update.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs
  - crates/z00z_wallets/tests/test_spend_nullifier_semantics.rs
  - crates/z00z_simulator/tests/test_scenario1_spend_gate.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Regular-spend replay closure is explicit and is specified honestly against the live asset-id spent model or an explicitly added nullifier-persistence hook."
    - "The full regular package verifier is the only canonical admission entry point."
    - "Local wire checks alone cannot be mistaken for full spend validation."
  artifacts:
    - "crates/z00z_wallets/src/core/tx/spend_rules.rs"
    - "crates/z00z_wallets/src/core/tx/tx_verifier.rs"
    - "crates/z00z_wallets/tests/test_spend_nullifier_semantics.rs"
    - "crates/z00z_simulator/tests/test_scenario1_spend_gate.rs"
  key_links:
    - "derive_spend_nullifier(...) -> replay-closure decision at state_update/storage boundary"
    - "verify_full_tx_package(...) -> simulator spend-gate regressions"
    - "test_tx_verifier_suite.rs -> broad full-verifier contract"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Close replay/nullifier enforcement and make the canonical full-package verifier unavoidable for regular-spend admission.

Purpose: satisfy D-04, D-05, D-06, D-07, and D-08 by pushing replay safety into the state-transition boundary and by making local wire verification insufficient on its own.
Output: deterministic regular-spend nullifier enforcement, simulator regressions for the canonical verifier, and full-verifier coverage that blocks local-only validation drift.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/040-spend-proof/040-CONTEXT.md
@.planning/phases/040-spend-proof/040-TODO.md
@.planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
@.planning/phases/040-spend-proof/040-02-PLAN.md
@crates/z00z_wallets/src/core/tx/spend_rules.rs
@crates/z00z_wallets/src/core/tx/state_update.rs
@crates/z00z_wallets/src/core/tx/tx_verifier.rs
@crates/z00z_simulator/tests/test_scenario1_spend_gate.rs

<interfaces>
From spend_rules.rs:
```rust
pub fn derive_spend_nullifier(chain_id: u32, s_in: &[u8; 32]) -> [u8; 32];
pub fn verify_spend_rules(stmt: &SpendStmt) -> Result<(), SpendRuleErr>;
```

From tx_verifier.rs:
```rust
pub fn verify_full_tx_package(tx_bytes: &[u8]) -> TxVerifierResult<VerificationResult>;
pub fn verify_package_public_spend_contract(pkg: &TxPackage) -> TxVerifierResult<()>;
```

From state_update.rs:
```rust
pub fn apply_batch_checkpoint(
    height: u64,
    state: &mut impl AssetState,
    txs: &[TxPkgSum],
    proof_chk: &impl TxProofVerifier,
    spent_idx: &impl SpentIndex,
) -> Result<Checkpoint, StateError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>040-05 Nullifier Semantics</name>
  <files>
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_wallets/src/core/tx/spend_rules.rs
    crates/z00z_wallets/src/core/tx/state_update.rs
    crates/z00z_wallets/tests/test_spend_nullifier_semantics.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `585-618`, `729-754`, `871-891`, and `931-961` in full, because that pre-read is mandatory in `040-TODO.md`. Per D-04, D-05, D-06, D-07, and the replay/nullifier rules in `040-CONTEXT.md`, implement `040-05` by keeping regular-spend nullifiers separate from claim nullifiers while carrying nullifier commitments through the proof/statement surface and defining replay/uniqueness closure honestly against the current asset-id spent model or an explicitly added regular-spend nullifier persistence hook. Reuse `derive_spend_nullifier(...)`, `verify_spend_rules(...)`, and the existing `SpentIndex`/state-update lane for the live asset-spent boundary rather than implying that `SpentIndex` already stores regular-spend nullifiers. Make same-scope determinism, scope drift, and the chosen post-acceptance replay rejection boundary explicit in code and tests.</action>
  <verify>
    <automated>Run the focused nullifier gates `cargo test -p z00z_wallets --release --features test-fast test_spend_nullifier_semantics -- --nocapture` and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_checkpoint_acceptance -- --nocapture` first so nullifier validation order matches `040-TODO.md`. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun those focused nullifier/replay gates before closing the task so same-scope determinism, scope drift, and replay rejection remain explicit. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-05 Nullifier Semantics"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>Regular-spend nullifier derivation is deterministic and scope-aware, the post-acceptance replay closure is implemented and documented against the correct storage boundary, and claim-nullifier semantics stay separate in code and tests.</done>
</task>

<task type="auto">
  <name>040-06 Full Regular Package Verification Entry Point</name>
  <files>
    crates/z00z_wallets/src/core/tx/tx_verifier.rs
    crates/z00z_wallets/src/core/tx/mod.rs
    crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs
    crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
    crates/z00z_simulator/tests/test_scenario1_spend_gate.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `399-420`, `516-554`, `585-618`, `701-728`, `892-911`, and `931-947` in full, because that pre-read is mandatory in `040-TODO.md`. Per D-04, D-06, D-07, D-08, and the residual follow-up note already recorded in `040-TODO.md`, keep `verify_full_tx_package(...)` as the single canonical admission entry point and close the remaining simulator-side regression gap. Ensure structurally valid packages without the required spend proof fail the canonical full verifier, and add the simulator case where local wire checks pass but the public spend contract fails. Keep range-proof mode explicit during backend integration, and do not let any caller reintroduce a local-only verification shortcut.</action>
  <verify>
    <automated>Run the focused spend-gate checks `cargo test -p z00z_wallets --release --features test-fast test_tx_verifier_suite -- --nocapture` and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_spend_gate -- --nocapture` first so simulator spend-gate validation order matches `040-TODO.md`. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun the focused wallet/simulator gates for the touched slice before closing the task. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-06 Full Regular Package Verification Entry Point"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>`verify_full_tx_package(...)` remains the only canonical full regular-package verifier, wallet coverage rejects proofless structural packages, and simulator coverage proves that local wire success is insufficient when the public spend contract fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spend nullifier -> checkpoint/state mutation | Replay detection must survive beyond local verification |
| local wire checks -> full admission | Callers may mistakenly trust structural validation as full spend validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-040-09 | R | regular-spend nullifier surface | mitigate | Enforce same-scope determinism, scope drift, and replay rejection with focused wallet tests |
| T-040-10 | T | state replay boundary | mitigate | Make the replay-closure boundary explicit against the live asset-id spent model and add a distinct nullifier persistence hook only if that stronger theorem is required |
| T-040-11 | I | full verifier callers | mitigate | Keep `verify_full_tx_package(...)` as the only canonical entry point and add simulator regressions for local-only false positives |
| T-040-12 | D | range-proof backend integration | mitigate | Keep range-proof mode explicit in simulator regressions so backend drift cannot hide behind local structure checks |

</threat_model>

<verification>
Re-read `spend_rules.rs`, `state_update.rs`, `tx_verifier.rs`, and `test_scenario1_spend_gate.rs` together and confirm replay safety and canonical-admission semantics now line up.
</verification>

<success_criteria>
`040-05` and `040-06` are closed with an explicit, non-overclaimed replay-closure boundary and one canonical full regular-package verification entry point.
</success_criteria>

<output>
After completion, create `.planning/phases/040-spend-proof/040-03-SUMMARY.md`.
</output>
