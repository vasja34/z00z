---
phase: 040-spend-proof
plan: "05"
type: execute
wave: 5
depends_on:
  - "040-02"
  - "040-03"
  - "040-04"
  - "040-06"
files_modified:
  - .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/spend_rules.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/tests/test_spend_statement.rs
  - crates/z00z_wallets/tests/test_spend_prover_contract.rs
  - crates/z00z_wallets/tests/test_tx_proof_verifier.rs
  - crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "The new proof statement preserves the current shipped public theorem boundary, and any remaining verifier-side theorem gap stays recorded fail-closed."
    - "Public input surface is preserved and any drift fails closed."
    - "Package digest remains the only public/persisted proof-binding root."
  artifacts:
    - ".planning/phases/040-spend-proof/040-INTEGRITY-GATES.md"
    - "crates/z00z_wallets/tests/test_spend_statement.rs"
    - "crates/z00z_wallets/tests/test_tx_proof_verifier.rs"
    - "crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs"
  key_links:
    - "verify_spend_rules(...) -> canonical statement theorem preservation"
    - "SpendInputLeaf / AssetLeaf public fields -> statement surface"
    - "build_tx_package_digest(...) -> full verifier rejection path"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Close the integrity-gate tasks that preserve theorem shape, public inputs, and digest-root discipline after the core proof path is working.

Purpose: satisfy D-01, D-04, D-06, D-07, D-08, and D-09 by making the Phase 040 integrity clauses executable, reviewable, and tied to the real code paths.
Output: one phase-local integrity-gate ledger plus explicit regressions for theorem preservation, public-input preservation, and digest-root discipline.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/040-spend-proof/040-CONTEXT.md
@.planning/phases/040-spend-proof/040-TODO.md
@.planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
@.planning/phases/040-spend-proof/040-04-PLAN.md
@crates/z00z_wallets/src/core/tx/spend_verification.rs
@crates/z00z_wallets/src/core/tx/spend_rules.rs
@crates/z00z_wallets/src/core/tx/tx_verifier.rs

<interfaces>
From spend_rules.rs:
```rust
pub fn verify_spend_rules(stmt: &SpendStmt) -> Result<(), SpendRuleErr>;
```

From tx_verifier.rs:
```rust
pub fn verify_full_tx_package(tx_bytes: &[u8]) -> TxVerifierResult<VerificationResult>;
pub fn verify_package_public_spend_contract(pkg: &TxPackage) -> TxVerifierResult<()>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>040-09 Proof-Theorem Preservation</name>
  <files>
    .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_wallets/src/core/tx/spend_rules.rs
    crates/z00z_wallets/tests/test_spend_statement.rs
    crates/z00z_wallets/tests/test_spend_prover_contract.rs
    crates/z00z_wallets/tests/test_tx_proof_verifier.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `585-631` and `657-672` in full, because that pre-read is mandatory before closing `040-09` through `040-14` in `040-TODO.md`. Per D-01, D-06, D-07, and D-09, implement `040-09` by creating `.planning/phases/040-spend-proof/040-INTEGRITY-GATES.md` as the evidence ledger for tasks `040-09` through `040-11`, then tie the canonical statement/prover/verifier path back to the current `verify_spend_rules(...)` equations. Prove the new statement binds the existing spend-rule theorem exactly, and reject any producer/verifier change that widens, drops, or reorders those equations without a spec update. Do not consult or resurrect the retired `7-Spend-Proof.md` draft.</action>
  <verify>
    <automated>Run the focused integrity gates `cargo test -p z00z_wallets --release --features test-fast test_spend_statement -- --nocapture && cargo test -p z00z_wallets --release --features test-fast test_spend_prover_contract -- --nocapture && cargo test -p z00z_wallets --release --features test-fast test_tx_proof_verifier -- --nocapture` first so integrity validation follows the TODO ordering. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun the focused wallet gates for the touched slice before closing the task. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-09 Proof-Theorem Preservation"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>`040-INTEGRITY-GATES.md` records the theorem-preservation contract, and focused tests prove the landed canonical statement/prover/verifier drift guards. Verifier-side theorem parity remains open until a theorem-carrying backend replaces the current statement-bound artifact.</done>
</task>

<task type="auto">
  <name>040-10 Public Input Surface Preservation</name>
  <files>
    .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_wallets/tests/test_spend_statement.rs
    crates/z00z_wallets/tests/test_tx_proof_verifier.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `585-631` and `657-672` in full, because that pre-read is mandatory before closing `040-09` through `040-14` in `040-TODO.md`. Per D-06, D-07, and the Phase 040 public-input requirements, extend the same integrity ledger and tests so the proof statement remains bound to the resolved input ids and public leaf fields carried by `SpendInputLeaf`, the output `AssetLeaf` fields already checked by `verify_self_decrypt()`, and explicit chain/root scope. Add fail-closed verification for any drift between carried public inputs and recomputed tx facts.</action>
  <verify>
    <automated>Run the focused public-input gates `cargo test -p z00z_wallets --release --features test-fast test_spend_statement -- --nocapture && cargo test -p z00z_wallets --release --features test-fast test_tx_proof_verifier -- --nocapture` first so integrity validation follows the TODO ordering. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun the focused wallet gates for the touched slice before closing the task. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-10 Public Input Surface Preservation"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The statement and verifier fail closed on public-input drift, and `040-INTEGRITY-GATES.md` records the preserved input/output/scope surface explicitly.</done>
</task>

<task type="auto">
  <name>040-11 Digest-Root Discipline</name>
  <files>
    .planning/phases/040-spend-proof/040-INTEGRITY-GATES.md
    crates/z00z_wallets/src/core/tx/tx_verifier.rs
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_wallets/src/core/tx/test_tx_verifier_suite.rs
    crates/z00z_wallets/tests/test_spend_statement.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `585-631` and `657-672` in full, because that pre-read is mandatory before closing `040-09` through `040-14` in `040-TODO.md`. Per D-06, D-07, D-08, and the `040-11` owner tasks, keep `build_tx_package_digest()` as the only public/persisted proof-binding root, keep `compute_tx_digest_from_wire()` internal-only, and add full-verifier rejection coverage for any proof path that tries to bind only the wire digest. Record the final digest-discipline rule in `040-INTEGRITY-GATES.md` so closeout can audit it without reopening design scope.</action>
  <verify>
    <automated>Run the focused digest-discipline gates `cargo test -p z00z_wallets --release --features test-fast test_tx_verifier_suite -- --nocapture && cargo test -p z00z_wallets --release --features test-fast test_spend_statement -- --nocapture` first so integrity validation follows the TODO ordering. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun the focused wallet gates for the touched slice before closing the task. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-11 Digest-Root Discipline"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The code and tests prove package digest is the only public/persisted proof-binding root, and the integrity ledger records the wire-digest helper as internal-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theorem-level spend rules -> canonical statement/proof path | Core arithmetic/security relations must remain identical after proof integration |
| public input surface -> verifier recomputation | Any drift between carried inputs and recomputed tx facts must fail closed |
| internal digest helpers -> persisted/public roots | Helper hashes must not escape into authoritative proof-binding roots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-040-17 | T | theorem preservation | mitigate | Tie the statement/prover/verifier path back to `verify_spend_rules(...)` and record it in `040-INTEGRITY-GATES.md` |
| T-040-18 | I | public input surface | mitigate | Add explicit tests for input/output/scope drift and require fail-closed rejection |
| T-040-19 | E | digest-root discipline | mitigate | Keep `build_tx_package_digest()` authoritative and reject any wire-digest-only proof path |
| T-040-20 | R | closeout auditability | mitigate | Maintain one phase-local integrity ledger that maps each integrity gate to code/tests and summaries |

</threat_model>

<verification>
Re-read `040-INTEGRITY-GATES.md`, `spend_verification.rs`, `spend_rules.rs`, `tx_verifier.rs`, and their focused tests together and confirm theorem/public-input/digest-root integrity now has executable proof.
</verification>

<success_criteria>
Tasks `040-10` and `040-11` are converted from abstract integrity clauses into explicit code/test gates backed by a phase-local evidence ledger, and `040-09` stays explicitly partial until the verifier path gains real theorem-carrying proof over the witness-only relations named in `verify_spend_rules(...)`.
</success_criteria>

<output>
After completion, create `.planning/phases/040-spend-proof/040-05-SUMMARY.md`.
</output>
