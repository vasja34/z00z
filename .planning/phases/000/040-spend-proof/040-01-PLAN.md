---
phase: 040-spend-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_wallets/src/core/tx/tx_wire_types.rs
  - crates/z00z_wallets/src/core/tx/mod.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/tests/test_spend_proof_wire.rs
  - crates/z00z_wallets/tests/test_spend_statement.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "TxProofWire carries one versioned regular-spend proof payload without a parallel proof object."
    - "Producer and verifier consume the same canonical spend statement."
    - "Package digest remains the only public proof-binding root."
  artifacts:
    - "crates/z00z_wallets/src/core/tx/tx_wire_types.rs"
    - "crates/z00z_wallets/src/core/tx/spend_verification.rs"
    - "crates/z00z_wallets/tests/test_spend_proof_wire.rs"
    - "crates/z00z_wallets/tests/test_spend_statement.rs"
  key_links:
    - "TxProofWire -> SpendProofWire -> build_public_spend_contract(...)"
    - "encode_spend_statement(...) -> verify_tx_public_spend_contract(...)"
    - "build_tx_package_digest(...) stays authoritative over any wire-level helper"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Land the canonical proof-carrier and spend-statement foundations in the existing wallet tx lane before any producer or verifier expansion begins.

Purpose: satisfy D-01, D-02, D-03, D-06, D-07, D-08, and D-10 by strengthening the live carrier and statement seams instead of inventing a parallel proof architecture.
Output: a versioned non-empty regular-spend proof carrier, one canonical spend statement builder, and focused regression tests proving fail-closed digest discipline.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/040-spend-proof/040-CONTEXT.md
@.planning/phases/040-spend-proof/040-TODO.md
@.planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
@crates/z00z_wallets/src/core/tx/tx_wire_types.rs
@crates/z00z_wallets/src/core/tx/spend_verification.rs

<interfaces>
From tx_wire_types.rs:
```rust
pub struct SpendProofWire {
    pub ver: u8,
    pub prev_root_hex: String,
    pub inputs: Vec<SpendInputProofWire>,
}

pub struct SpendAuthWire {
    pub ver: u8,
    pub receiver_card_compact: String,
    pub spend_sig_hex: String,
}

pub struct TxProofWire {
    pub spend: Option<SpendProofWire>,
}

pub struct TxAuthWire {
    pub spend: Option<SpendAuthWire>,
}
```

From spend_verification.rs:
```rust
fn encode_spend_statement(
    chain_id: u32,
    tx_version: u8,
    tx: &TxWire,
    card: &ReceiverCard,
    proof: &SpendProofWire,
) -> Result<Vec<u8>, SpendPublicErr>;

pub fn build_public_spend_contract(
    receiver_keys: &ReceiverKeys,
    chain_id: u32,
    tx_version: u8,
    tx: &TxWire,
    prev_root: CheckRoot,
    proof_inputs: Vec<SpendInputProofWire>,
) -> Result<(TxProofWire, TxAuthWire), SpendPublicErr>;

pub fn verify_tx_public_spend_contract(
    chain_id: u32,
    tx_version: u8,
    tx: &TxWire,
) -> Result<(), SpendPublicErr>;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>040-01 Proof Carrier Contract</name>
  <files>
    crates/z00z_wallets/src/core/tx/tx_wire_types.rs
    crates/z00z_wallets/src/core/tx/mod.rs
    crates/z00z_wallets/tests/test_spend_proof_wire.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `209-256`, `585-618`, `675-700`, `788-814`, and `931-961` in full, because that pre-read is mandatory in `040-TODO.md`. Per D-01, D-02, D-03, D-06, D-07, D-08, and D-10, keep the existing `TxProofWire`/`TxAuthWire` ownership and upgrade the regular-spend proof carrier in place instead of creating any parallel proof object. Implement the `040-01` contract exactly as named in `040-TODO.md`: add the non-empty, versioned carrier fields required by the spec, preserve package framing compatibility, keep unknown-version rejection explicit, and wire fail-closed behavior so empty placeholder proof paths reject once the stronger carrier mode is active. Export only the minimum stable surface through `mod.rs`; do not create `spend_proof_backend.rs` or any shadow carrier module.</action>
  <verify>
    <automated>Run the focused proof-carrier gate `cargo test -p z00z_wallets --release --features test-fast test_spend_proof_wire -- --nocapture` first so the TODO wire-level assertions execute before any broad sweep. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun the focused proof-carrier gate before closing the task so the TODO wire-level assertions remain explicit. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-01 Proof Carrier Contract"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>`TxProofWire` carries a versioned regular-spend payload without introducing a second proof container, and `test_spend_proof_wire.rs` proves roundtrip, unknown-version rejection, and stronger-mode empty-placeholder rejection.</done>
</task>

<task type="auto">
  <name>040-02 Canonical Spend Statement</name>
  <files>
    crates/z00z_wallets/src/core/tx/spend_verification.rs
    crates/z00z_wallets/src/core/tx/tx_verifier.rs
    crates/z00z_wallets/src/core/tx/mod.rs
    crates/z00z_wallets/tests/test_spend_statement.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `278-309`, `399-420`, `476-554`, `585-618`, `815-839`, and `931-947` in full, because that pre-read is mandatory in `040-TODO.md`. Per D-01, D-03, D-06, D-07, D-08, and D-10, keep the live `encode_spend_statement(...)`, `build_public_spend_contract(...)`, and `verify_tx_public_spend_contract(...)` seam in `spend_verification.rs`, but make it the canonical regular-spend statement contract required by `040-02`. Bind package digest, chain/root scope, resolved input refs, output public fields, public commitment aggregates, and current range-proof semantics into one statement shape that both producer and verifier reuse. Preserve the rule from `040-11` that `compute_tx_digest_from_wire()` stays an internal helper only; do not accept ad hoc JSON fragments, bare wire digest roots, or unversioned proof transcripts.</action>
  <verify>
    <automated>Run the focused statement checks `cargo test -p z00z_wallets --release --features test-fast test_spend_statement -- --nocapture && cargo test -p z00z_wallets --release --features test-fast test_tx_verifier_suite -- --nocapture` first so wallet-level validation order matches `040-TODO.md`. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun the same focused statement checks before closing the task so the package-stability, drift, and bare-wire-digest rejection rules from `040-TODO.md` stay explicit. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-02 Canonical Spend Statement"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>One canonical spend-statement builder exists, both producer and verifier can consume it, and `test_spend_statement.rs` proves package-stable statements, digest drift sensitivity, output-field drift sensitivity, and rejection of bare wire digest as the only public root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tx wire -> public spend statement | Untrusted package bytes cross into the signed/verifiable spend contract |
| digest helper -> public root contract | Internal wire helpers must not become the public proof-binding root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-040-01 | T | `TxProofWire` carrier | mitigate | Reject unknown versions and stronger-mode empty placeholders before downstream verification |
| T-040-02 | I | `encode_spend_statement(...)` | mitigate | Bind package digest, scope, inputs, and outputs into one canonical framed statement |
| T-040-03 | R | verifier evidence surface | mitigate | Keep `test_spend_proof_wire.rs` and `test_spend_statement.rs` as replayable proof of the contract |
| T-040-04 | E | helper digest path | mitigate | Keep wire digest helper internal and fail closed on any proof path that binds only helper digest material |

</threat_model>

<verification>
Re-read `tx_wire_types.rs`, `spend_verification.rs`, and `tx_verifier.rs` together and confirm the carrier and statement still describe one canonical regular-spend contract with package digest as the only public binding root.
</verification>

<success_criteria>
`040-01` and `040-02` are closed in the live codebase without adding a parallel proof architecture, and their focused wallet tests prove carrier and statement fail closed.
</success_criteria>

<output>
After completion, create `.planning/phases/040-spend-proof/040-01-SUMMARY.md`.
</output>
