---
phase: 040-spend-proof
plan: "04"
type: execute
wave: 4
depends_on:
  - "040-03"
  - "040-04"
  - "040-05"
  - "040-06"
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
  - crates/z00z_simulator/tests/test_scenario1_tx_proof_roundtrip.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_wallets/src/core/tx/builder.rs
  - crates/z00z_wallets/src/core/tx/output_flow.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Stage 4 through Stage 11 preserves the proof-bearing package without statement drift."
    - "Stage-surface wording remains honest about current proof scope and replay closure."
    - "Optional output-constructor cleanup stays bounded and does not change proof-facing or receiver-facing behavior."
  artifacts:
    - "crates/z00z_simulator/tests/test_scenario1_tx_proof_roundtrip.rs"
    - "crates/z00z_simulator/tests/test_scenario1_stage_surface.rs"
    - "crates/z00z_wallets/src/core/tx/builder.rs"
    - "crates/z00z_wallets/src/core/tx/output_flow.rs"
  key_links:
    - "Stage 4 tx package -> Stage 6 bridge -> Stage 11 handoff"
    - "verify_stage7_handoff(...) -> build_cp_draft(...)"
    - "output builder helpers remain subordinate to proof-stable semantics"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Prove end-to-end simulator continuity for the regular-spend proof path and only then evaluate the bounded output-constructor follow-up.

Purpose: satisfy D-04, D-05, D-06, D-07, and D-08 by closing the Stage 4/6/11 continuity story before any optional output-builder cleanup is attempted.
Output: roundtrip simulator proof continuity tests, honest stage-surface wording locks, and a bounded post-proof output-constructor follow-up that does not alter existing semantics.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/040-spend-proof/040-CONTEXT.md
@.planning/phases/040-spend-proof/040-TODO.md
@.planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
@.planning/phases/040-spend-proof/040-03-PLAN.md
@crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
@crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
@crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
@crates/z00z_wallets/src/core/tx/builder.rs
@crates/z00z_wallets/src/core/tx/output_flow.rs

<interfaces>
From bundle_lane_impl.rs:
```rust
pub(crate) fn load_tx_pkg(path: &Path) -> Result<TxPackage, String>;
```

From stage_11_apply.rs:
```rust
fn verify_stage7_handoff(load: &Stage7Load) -> Result<(), String>;

let draft = build_cp_draft(
    stage_id as u64,
    load.snap_id,
    &load.prep,
    &load.replay,
    &link,
    &load.exec,
    &proof_verifier,
    &spent_index,
)?;
```

From output_flow.rs:
```rust
pub fn output_range_ctx_hash(
    leaf: &AssetLeaf,
    chain_id: u32,
    root_ver: u8,
    proof_ver: u8,
    policy_ver: u32,
) -> [u8; 32];

pub fn bind_output_wire(mut wire: AssetWire, leaf: &AssetLeaf) -> Result<AssetWire, String>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>040-07 End-to-End Roundtrip And Surface Locks</name>
  <files>
    crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
    crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
    crates/z00z_simulator/src/scenario_1/stage_11_apply.rs
    crates/z00z_simulator/tests/test_scenario1_tx_proof_roundtrip.rs
    crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `114-156`, `572-584`, and `931-961` in full, because that pre-read is mandatory in `040-TODO.md`. Per D-04, D-05, D-06, D-07, and the sequential review gates in `040-CONTEXT.md`, implement `040-07` by proving the Stage-4 carrier survives Stage 6 reload and Stage 11 handoff without statement drift. Extend the already landed `test_scenario1_tx_proof_roundtrip.rs` ownership seam to prove producer/verifier compatibility across Stage 4, Stage 6, and Stage 11. Extend `test_scenario1_stage_surface.rs` so the public wording stays honest about current proof scope and so post-`040-05` wording reflects the actual replay-closure truth instead of preserving a stale pre-closure disclaimer. Keep checkpoint draft persistence blocked on package/proof/exec drift; do not weaken Stage-11 rollback behavior.</action>
  <verify>
    <automated>Run the focused simulator gates `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_tx_proof_roundtrip -- --nocapture && cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_stage_surface -- --nocapture && cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_checkpoint_acceptance -- --nocapture` first so simulator roundtrip/stage-surface validation order matches `040-TODO.md`. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun the focused simulator gates for the touched slice before closing the task. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-07 End-to-End Roundtrip And Surface Locks"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>The simulator proves proof-bearing package continuity from Stage 4 through Stage 11, and stage-surface wording stays explicit about real proof scope while remaining synchronized with the actual post-`040-05` replay-closure state.</done>
</task>

<task type="auto">
  <name>040-08 Optional Output-Constructor Follow-Up</name>
  <files>
    crates/z00z_wallets/src/core/tx/builder.rs
    crates/z00z_wallets/src/core/tx/output_flow.rs
  </files>
  <action>Before editing, read `040-Spend-Proof-Spec.md` lines `321-382`, `619-631`, `657-672`, and `912-930` in full, because that pre-read is mandatory in `040-TODO.md`. Per D-04, D-06, D-08, and the bounded-follow-up rule in `040-TODO.md`, touch `builder.rs` and `output_flow.rs` only after `040-07` is green and only if needed to reconcile `build_tx_stealth_output()` with `build_output_leaf()`. Preserve `leaf_ad`, `tag16`, commitment, enc-pack, and range-proof semantics exactly. Do not widen this task into a redesign of output construction, and do not mix wallet `compute_leaf_ad()` with crypto `derive_leaf_ad()` without an explicit migration note.</action>
  <verify>
    <automated>Run the focused wallet gates `cargo test -p z00z_wallets --release --features test-fast --test test_s5_sender_examples -- --nocapture`, `cargo test -p z00z_wallets --release --features test-fast --test test_stealth_output -- --nocapture`, and `cargo test -p z00z_wallets --release --features test-fast --test test_e2e_send_scan -- --nocapture` first so optional follow-up validation preserves the TODO ordering. If stage wording moved, also run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_stage_surface -- --nocapture` before the broad sweep. After that, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`. If this task changes Rust or tests, then run `cargo test --release --features test-fast --features wallet_debug_dump`; if the broad gate fails, record whether the blocker is phase-scoped or pre-existing. When the broad gate passes, still rerun those focused wallet gates before closing the task. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/040-spend-proof/040-TODO.md current_task="040-08 Optional Output-Constructor Follow-Up"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. Any commit created for this task must route through `/z00z-git-versioning`, not raw `git commit`.</automated>
  </verify>
  <done>Any output-builder cleanup remains strictly bounded, preserves existing proof-facing and receiver-facing behavior, and does not pull output-constructor unification ahead of proof-stable seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 4 package -> Stage 6 reload -> Stage 11 apply | Proof-bearing package continuity must survive file serialization and simulator handoff |
| optional output cleanup -> proof-facing behavior | Late builder changes must not mutate already-validated proof/output semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-040-13 | T | simulator handoff path | mitigate | Add explicit roundtrip tests across Stage 4, Stage 6, and Stage 11 and keep `verify_stage7_handoff(...)` strict |
| T-040-14 | I | stage-surface wording | mitigate | Extend `test_scenario1_stage_surface.rs` so documentation/UX claims cannot outrun implementation truth |
| T-040-15 | T | output constructor follow-up | mitigate | Limit builder/output edits to semantics-preserving cleanup after proof seams are already stable |
| T-040-16 | E | checkpoint draft persistence | mitigate | Preserve existing drift rejection before draft emission and reject any downgrade to best-effort publish behavior |

</threat_model>

<verification>
Re-read the Stage 4, Stage 6, and Stage 11 paths plus the new simulator tests together and confirm the proof-bearing package remains one canonical lane before and after any optional builder cleanup.
</verification>

<success_criteria>
`040-07` proves simulator continuity honestly, and `040-08` stays a bounded post-proof cleanup rather than a redesign.
</success_criteria>

<output>
After completion, create `.planning/phases/040-spend-proof/040-04-SUMMARY.md`.
</output>
