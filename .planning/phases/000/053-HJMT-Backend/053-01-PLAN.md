---
phase: 053-HJMT-Backend
plan: 053-01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_storage/tests/test_live_guardrails.rs
  - crates/z00z_storage/src/settlement/mod.rs
  - crates/z00z_storage/src/settlement/types_identity.rs
  - crates/z00z_storage/src/settlement/types_record.rs
  - crates/z00z_storage/src/settlement/root-types.md
  - crates/z00z_storage/src/settlement/README.MD
requirements:
  - PH53-01
autonomous: true
must_haves:
  truths:
    - Phase 053 live settlement contracts are required by tests instead of blocked as future-only names.
    - Public guardrails still prevent downstream callers from treating physical HJMT layout as semantic authority.
  artifacts:
    - crates/z00z_storage/tests/test_live_guardrails.rs
    - crates/z00z_storage/src/settlement/mod.rs
    - crates/z00z_storage/src/settlement/types_identity.rs
    - crates/z00z_storage/src/settlement/types_record.rs
  key_links:
    - Guardrail tests import the public storage exports that later plans implement.
    - Storage docs describe live settlement contracts and physical-layout non-authority.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-01 Plan: Live-Contract Guardrails

<objective>
Replace Phase 052 future-only guardrails with Phase 053 live-contract guardrails.

Purpose: establish the public semantic vocabulary before runtime rewiring.
Output: guardrail tests, initial public exports, and docs that require live settlement contracts.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@.planning/phases/053-HJMT-Backend/053-SOURCE-AUDIT.md
@docs/tech-papers/Z00Z-HJMT-Design.md
@crates/z00z_storage/tests/test_live_guardrails.rs
@crates/z00z_storage/src/settlement/mod.rs
@crates/z00z_storage/src/settlement/types_identity.rs
@crates/z00z_storage/src/settlement/types_record.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Replace future-only guards with live settlement contract guards</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` sections 053-01, Live-Code Acceptance Definition, Failure Conditions, and Verification Order
- `docs/tech-papers/Z00Z-HJMT-Design.md` sections on generalized settlement roots, right leaves, fee envelopes, and non-existence proofs
- `.github/requirements/Z00Z_DESIGN_FOUNDATION.md` public API, error, no-panic, and Rustdoc rules
</read_first>
<files>
- `crates/z00z_storage/tests/test_live_guardrails.rs`
- `crates/z00z_storage/src/settlement/mod.rs`
- `crates/z00z_storage/src/settlement/types_identity.rs`
- `crates/z00z_storage/src/settlement/types_record.rs`
- `crates/z00z_storage/src/settlement/root-types.md`
- `crates/z00z_storage/src/settlement/README.MD`
</files>
<action>
Implement D-01 by rewriting the Phase 052 future-only guardrails into Phase 053 live-contract guardrails. Require public exports for `SettlementStateRoot`, `RootGeneration`, `SettlementPath`, `TerminalId`, `SettlementLeaf`, `RightLeaf`, `FeeEnvelope`, `AdaptiveBucket`, `BucketEpoch`, `SplitProof`, `MergeProof`, and `PolicyTransitionProof`. Add compile-time and source-shape guards proving `SettlementStateRoot` is not a bare alias to `AssetStateRoot`, `RightLeaf` is not a bare alias or wrapper for `AssetLeaf`, and `FeeEnvelope` does not appear inside right meaning. Preserve downstream guardrails that block public authority over `TreeId`, `ForestTreeId`, namespace bytes, backend roots, branch ordering, RedB keys, cache state, and raw bucket internals. Keep docs aligned with the hard-cutover vocabulary.
</action>
<acceptance_criteria>
- Tests require the Phase 053 live exports and fail if they are removed or downgraded to aliases.
- Guardrails still block physical HJMT layout authority from public/downstream surfaces.
- Docs say Phase 053 promotes the live generalized settlement backend and do not describe these contracts as future-only.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --features wallet_debug_dump --test test_live_guardrails -- --nocapture</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-01-PLAN.md current_task="Replace future-only guards with live settlement contract guards" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-01-PLAN.md current_task="Replace future-only guards with live settlement contract guards" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-01-PLAN.md current_task="Replace future-only guards with live settlement contract guards" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
The repository requires Phase 053 live settlement contracts and still rejects physical-layout authority leakage.
</done>
</task>
</tasks>

<threat_model>
- Spoofing: aliases can spoof old asset roots as settlement roots; guardrails require distinct typed contracts.
- Tampering: downstream physical ids can be mistaken for semantic roots; public surface guards keep them private.
- Repudiation: docs and tests can drift; source-shape tests bind the live vocabulary.
</threat_model>

<verification>
Plan execution must produce `053-01-SUMMARY.md` with bootstrap, broad cargo, focused guardrail test, and review-loop evidence.
</verification>

<success_criteria>
- Live settlement contract names are required by code and tests.
- Phase 052 future-only language is removed from active guardrails.
- Physical layout remains private and non-authoritative.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-01-SUMMARY.md` when done.
</output>
