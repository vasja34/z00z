---
phase: 053-HJMT-Backend
plan: 053-12
type: execute
wave: 12
depends_on:
  - 053-11
files_modified:
  - crates/z00z_storage/src/settlement/hjmt_journal.rs
  - crates/z00z_storage/src/settlement/hjmt_commit.rs
  - crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
  - crates/z00z_storage/src/settlement/redb_backend_helpers.rs
  - crates/z00z_storage/src/settlement/live_recovery_tests.rs
  - crates/z00z_storage/tests/test_fee_replay.rs
  - crates/z00z_storage/tests/test_redb_reload.rs
requirements:
  - PH53-12
autonomous: true
must_haves:
  truths:
    - Journal recovery binds settlement root generation, proof version, bucket epoch, policy-bound fee replay state, and durable replay metadata.
    - Partial, downgraded, mismatched, or reordered durable rows fail closed.
  artifacts:
    - crates/z00z_storage/src/settlement/hjmt_journal.rs
    - crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
    - crates/z00z_storage/src/settlement/live_recovery_tests.rs
    - crates/z00z_storage/tests/test_fee_replay.rs
  key_links:
    - Commit, fee, proof, adaptive policy, cache, scheduler, and RedB paths share the same journal status model.
    - Recovery reconstructs semantic settlement state before accepting any historical proof.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-12 Plan: Journal And Recovery

<objective>
Extend journal and recovery for generalized settlement state.

Purpose: durable recovery must validate every settlement generation, proof, fee, policy, and cache binding before accepting state.
Output: journal rows, status transitions, recovery checks, and crash tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@crates/z00z_storage/src/settlement/hjmt_journal.rs
@crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
@crates/z00z_storage/src/settlement/redb_backend_helpers.rs
@crates/z00z_storage/src/settlement/live_recovery_tests.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Extend journal rows and recovery validation for settlement state</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` section 053-12
- `crates/z00z_storage/src/settlement/hjmt_journal.rs`
- `crates/z00z_storage/src/settlement/redb_backend_hjmt.rs`
- `crates/z00z_storage/src/settlement/redb_backend_helpers.rs`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/hjmt_journal.rs`
- `crates/z00z_storage/src/settlement/hjmt_commit.rs`
- `crates/z00z_storage/src/settlement/redb_backend_hjmt.rs`
- `crates/z00z_storage/src/settlement/redb_backend_helpers.rs`
- `crates/z00z_storage/src/settlement/live_recovery_tests.rs`
- `crates/z00z_storage/tests/test_fee_replay.rs`
- `crates/z00z_storage/tests/test_redb_reload.rs`
</files>
<action>
Implement D-12 by extending the live HJMT journal and recovery path with root generation, proof envelope generation, fee replay count and digest sealing, pending and active replay-metadata validation, previous-root and next-root continuity, and forward-only status transitions. Reject missing fields, downgraded generation, wrong proof version, stale replay metadata, partial pending checkpoint state, and committed status without all required child rows.
</action>
<acceptance_criteria>
- Crash tests at each journal status recover to the previous accepted settlement root or the fully committed next settlement root.
- Recovery rejects partial or mismatched fee, policy, proof, and cache rows with typed errors.
- Recovered state can produce the same inclusion, deletion, non-existence, split, merge, and policy-transition proofs.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --lib live_hjmt_</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --test test_fee_replay</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --test test_redb_reload</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-12-PLAN.md current_task="Extend journal rows and recovery validation for settlement state" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-12-PLAN.md current_task="Extend journal rows and recovery validation for settlement state" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-12-PLAN.md current_task="Extend journal rows and recovery validation for settlement state" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
Journal and recovery bind every generalized settlement durability field and reject partial or downgraded state.
</done>
</task>
</tasks>

<threat_model>
- Tampering: missing or edited journal rows can create false committed roots; required digest and status checks reject them.
- Repudiation: crash timing can obscure accepted root; recovery chooses previous or complete next state only.
- Elevation of privilege: stale fee replay or policy rows can authorize new transitions; digest and epoch checks reject them.
</threat_model>

<verification>
Execution must include crash matrix, partial-row rejection, and recovered proof evidence.
</verification>

<success_criteria>
- Recovery is deterministic and fail-closed.
- Durable state contains the full generalized settlement contract.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-12-SUMMARY.md` when done.
</output>
