---
status: testing
phase: 053-HJMT-Backend
source:
  - 053-SUMMARY.md
  - 053-15-SUMMARY.md
  - 053-16-SUMMARY.md
  - 053-18-SUMMARY.md
  - 053-19-SUMMARY.md
  - 053-20-SUMMARY.md
started: 2026-06-06T11:50:50+03:00
updated: 2026-06-06T11:50:50+03:00
---

## Current Test

number: 1
name: HJMT Default And Alias Rejection
expected: |
  Run the live storage-consumer path with `Z00Z_SETTLEMENT_BACKEND_MODE` unset,
  then with explicit `hjmt`, and then with a stale alias such as
  `compatibility` or `forest`. Unset and explicit `hjmt` should work, stale
  aliases should reject, and no compatibility, forest, or dual-verify runtime
  lane should remain selectable as live behavior.
awaiting: user response

## Tests

### 1. HJMT Default And Alias Rejection
expected: Run the live storage-consumer path with `Z00Z_SETTLEMENT_BACKEND_MODE` unset, then with explicit `hjmt`, and then with a stale alias such as `compatibility` or `forest`. Unset and explicit `hjmt` should work, stale aliases should reject, and no compatibility, forest, or dual-verify runtime lane should remain selectable as live behavior.
result: [pending]

### 2. Mixed Settlement Scenario Surface
expected: Run the current `scenario_1` HJMT flow. It should finish successfully with mixed asset and right settlement artifacts, and the Stage 13 output should use settlement-native fields such as `root_generation`, `proof_family`, `leaf_family`, `settlement_path`, and `terminal_id` instead of asset-era root or path names.
result: [pending]

### 3. Rights Config, Genesis, And Ingestion
expected: The canonical rights-enabled config and genesis inputs should load cleanly, export a deterministic mixed settlement corpus and manifest, and storage ingestion should materialize right leaves rather than silently collapsing the data into an asset-only store view.
result: [pending]

### 4. Proof Families Stay Exact And Fail Closed
expected: Inclusion, deletion, and non-existence proofs should verify only for the exact committed settlement state. Tampered family, root, generation, or path inputs should reject, and an old non-existence proof should stop validating after the formerly absent path is inserted.
result: [pending]

### 5. Adaptive Policy And Privacy Evidence
expected: Split, merge, and policy-transition lanes should succeed only through the live HJMT APIs, while proof-visible occupancy evidence remains bounded and public metrics never expose raw occupancy counters as authority.
result: [pending]

### 6. Reload, Recovery, And Replay Protection
expected: Reloading or reopening committed storage should preserve semantic roots, right rows, and fee replay state. Journal drift, stale pending transitions, or corrupted cache or recovery rows should reject or roll back fail-closed instead of silently healing.
result: [pending]

### 7. Downstream Semantic Guardrails
expected: Wallet, simulator, and downstream checkpoint flows should accept semantic settlement roots and storage-owned proof artifacts only. `RightLeaf` must not appear as wallet-owned asset inventory, and raw backend or tree-layout ids must never become public authority.
result: [pending]

### 8. Executable Docs And Metrics Stay Live
expected: README examples, docs, benchmark lanes, and metrics surfaces should align with the live HJMT API and bounded diagnostics. The docs must not describe generalized settlement contracts as future-only or compatibility-only behavior.
result: [pending]

### 9. Legacy Runtime Purge Truth
expected: Compatibility, simple-JMT, and dual-verify runtime tails should be gone from the live surface. Remaining old-mode strings should survive only as reject-list coverage or historical planning text, not as executable runtime options or live docs claims.
result: [pending]

## Summary

total: 9
passed: 0
issues: 0
pending: 9
skipped: 0
blocked: 0

## Gaps

none
