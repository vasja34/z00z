---
phase: 053-HJMT-Backend
plan: 053-04
type: execute
wave: 4
depends_on:
  - 053-03
files_modified:
  - crates/z00z_storage/src/assets/fee_envelope.rs
  - crates/z00z_storage/src/assets/mod.rs
  - crates/z00z_storage/src/assets/proof.rs
  - crates/z00z_storage/src/assets/store/hjmt_journal.rs
  - crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
  - crates/z00z_storage/tests/test_fee_envelope.rs
  - crates/z00z_storage/tests/test_fee_replay.rs
requirements:
  - PH53-04
autonomous: true
must_haves:
  truths:
    - `FeeEnvelope` validates processing support without proving ownership, wallet control, or right validity.
    - Fee replay protection is durable and bound to the settlement transition it supports.
  artifacts:
    - crates/z00z_storage/src/assets/fee_envelope.rs
    - crates/z00z_storage/tests/test_fee_envelope.rs
    - crates/z00z_storage/tests/test_fee_replay.rs
  key_links:
    - Transition validation consumes `FeeEnvelope` separately from `RightLeaf`.
    - Journal and RedB rows persist replay protection evidence for fee envelopes.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-04 Plan: FeeEnvelope Contract

<objective>
Implement `FeeEnvelope` as a separate processing-support contract.

Purpose: keep fees, sponsorship, and relay support out of right meaning and ownership evidence.
Output: fee contract, validation, replay durability, and negative tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@docs/tech-papers/Z00Z-HJMT-Design.md
@crates/z00z_storage/src/assets/proof.rs
@crates/z00z_storage/src/assets/store/hjmt_journal.rs
@crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Add FeeEnvelope validation and durable replay protection</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` section 053-04
- `docs/tech-papers/Z00Z-HJMT-Design.md` FeeEnvelope separation notes
- `crates/z00z_storage/src/assets/store/hjmt_journal.rs`
</read_first>
<files>
- `crates/z00z_storage/src/assets/fee_envelope.rs`
- `crates/z00z_storage/src/assets/mod.rs`
- `crates/z00z_storage/src/assets/proof.rs`
- `crates/z00z_storage/src/assets/store/hjmt_journal.rs`
- `crates/z00z_storage/src/settlement/redb_backend_hjmt.rs`
- `crates/z00z_storage/tests/test_fee_envelope.rs`
- `crates/z00z_storage/tests/test_fee_replay.rs`
</files>
<action>
Implement D-04 by adding a versioned `FeeEnvelope` with payer or sponsor binding, budget, fee domain, expiry, nonce, replay protection, transition binding, and failure semantics. Validate fee envelopes before settlement transitions commit, persist replay digests in journal/RedB state, and ensure fee rejection does not mutate settlement roots. Add negative tests proving fee support cannot imply ownership, wallet control, right validity, or spend authority, and proving replayed or stale envelopes fail closed.
</action>
<acceptance_criteria>
- Valid fee envelopes support eligible transitions without becoming right meaning.
- Replayed, expired, wrong-domain, wrong-transition, and over-budget fee envelopes fail closed.
- Rejected fee envelopes leave settlement roots and journal status unchanged.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --features wallet_debug_dump --test test_fee_envelope --test test_fee_replay -- --nocapture</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-04-PLAN.md current_task="Add FeeEnvelope validation and durable replay protection" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-04-PLAN.md current_task="Add FeeEnvelope validation and durable replay protection" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-04-PLAN.md current_task="Add FeeEnvelope validation and durable replay protection" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
Fee support is live, replay-protected, durable, and separate from right and ownership semantics.
</done>
</task>
</tasks>

<threat_model>
- Spoofing: a fee sponsor can be misread as owner; API and tests keep fee support non-authoritative for ownership.
- Tampering: fee envelopes can be replayed on other transitions; nonce and transition binding prevent replay.
- Denial of service: invalid fee envelopes can partially mutate state; validation occurs before commit.
</threat_model>

<verification>
Plan execution must include fee separation, replay, stale envelope, and no-mutation-on-reject evidence.
</verification>

<success_criteria>
- `FeeEnvelope` is separate from `RightLeaf`.
- Fee validation is durable and fail-closed.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-04-SUMMARY.md` when done.
</output>
