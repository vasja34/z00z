---
phase: 053-HJMT-Backend
plan: 053-02
type: execute
wave: 2
depends_on:
  - 053-01
files_modified:
  - crates/z00z_storage/src/assets/types_identity.rs
  - crates/z00z_storage/src/assets/types_record.rs
  - crates/z00z_storage/src/assets/store_internal/store_roots.rs
  - crates/z00z_storage/src/assets/store/hjmt_commit.rs
  - crates/z00z_storage/src/assets/store/hjmt_proof.rs
  - crates/z00z_storage/src/checkpoint/build.rs
  - crates/z00z_storage/src/checkpoint/build_state.rs
  - crates/z00z_storage/tests/test_settlement_root.rs
requirements:
  - PH53-02
autonomous: true
must_haves:
  truths:
    - `SettlementStateRoot` is the storage semantic root for settlement generation and is bound into checkpoint/proof paths.
    - Mixed-generation, downgrade, and asset-root substitution paths fail closed.
  artifacts:
    - crates/z00z_storage/src/assets/types_identity.rs
    - crates/z00z_storage/src/assets/store_internal/store_roots.rs
    - crates/z00z_storage/tests/test_settlement_root.rs
  key_links:
    - Root generation metadata flows from forest commit into proof and checkpoint builders.
    - Tests tamper root generation and reject old asset-root substitution.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-02 Plan: Settlement Root Generation

<objective>
Promote `SettlementStateRoot` and `RootGeneration::SettlementV1` into the live root model.

Purpose: every proof, checkpoint, and store root must bind the same semantic settlement generation.
Output: root generation implementation and regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@docs/tech-papers/Z00Z-HJMT-Design.md
@crates/z00z_storage/src/assets/types_identity.rs
@crates/z00z_storage/src/assets/types_record.rs
@crates/z00z_storage/src/assets/store_internal/store_roots.rs
@crates/z00z_storage/src/checkpoint/build.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Bind settlement root generation through store, proof, and checkpoint surfaces</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` section 053-02
- `docs/tech-papers/Z00Z-HJMT-Design.md` root taxonomy and proof transcript sections
- `crates/z00z_storage/src/assets/store_internal/store_roots.rs`
- `crates/z00z_storage/src/checkpoint/build_state.rs`
</read_first>
<files>
- `crates/z00z_storage/src/assets/types_identity.rs`
- `crates/z00z_storage/src/assets/types_record.rs`
- `crates/z00z_storage/src/assets/store_internal/store_roots.rs`
- `crates/z00z_storage/src/assets/store/hjmt_commit.rs`
- `crates/z00z_storage/src/assets/store/hjmt_proof.rs`
- `crates/z00z_storage/src/checkpoint/build.rs`
- `crates/z00z_storage/src/checkpoint/build_state.rs`
- `crates/z00z_storage/tests/test_settlement_root.rs`
</files>
<action>
Implement D-02 by adding explicit settlement root generation metadata to root records, commit outputs, proof transcripts, and checkpoint state. Make `SettlementStateRoot` the only semantic root accepted for the new generation. Reject asset-root substitution, missing generation metadata, generation downgrade, mixed-generation batches, and checkpoint/proof root mismatches with typed errors. Preserve diagnostic backend roots only when they are bound to the semantic settlement transcript and cannot act as caller authority.
</action>
<acceptance_criteria>
- Store commits return and persist `SettlementStateRoot` with `RootGeneration::SettlementV1`.
- Checkpoint and proof builders reject mismatched or missing settlement root generation.
- Tests cover old asset-root substitution, downgrade, mixed-generation batches, and proof/checkpoint mismatch rejection.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --features wallet_debug_dump --test test_settlement_root -- --nocapture</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-02-PLAN.md current_task="Bind settlement root generation through store, proof, and checkpoint surfaces" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-02-PLAN.md current_task="Bind settlement root generation through store, proof, and checkpoint surfaces" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-02-PLAN.md current_task="Bind settlement root generation through store, proof, and checkpoint surfaces" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
Settlement root generation is bound across store, proof, and checkpoint paths and old-generation substitutions fail closed.
</done>
</task>
</tasks>

<threat_model>
- Spoofing: callers can present old asset roots as settlement roots; typed generation checks reject them.
- Tampering: proof/checkpoint transcripts can be rebound to a different generation; transcript binding blocks the swap.
- Elevation of privilege: diagnostic backend roots can become semantic authority; API shape keeps them diagnostic only.
</threat_model>

<verification>
Plan execution must include root-generation positive and negative test evidence plus the mandatory D-21 review sequence.
</verification>

<success_criteria>
- `SettlementStateRoot` is the live semantic root for the settlement generation.
- Proof and checkpoint code cannot accept mixed or downgraded root generations.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-02-SUMMARY.md` when done.
</output>
