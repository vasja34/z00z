---
phase: 053-HJMT-Backend
plan: 053-11
type: execute
wave: 11
depends_on:
  - 053-10
files_modified:
  - crates/z00z_storage/src/settlement/hjmt_scheduler.rs
  - crates/z00z_storage/src/settlement/hjmt_commit.rs
  - crates/z00z_storage/src/settlement/hjmt_store.rs
  - crates/z00z_storage/src/settlement/hjmt_proof.rs
  - crates/z00z_storage/src/settlement/hjmt_policy.rs
  - crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
  - crates/z00z_storage/src/settlement/store_rows.rs
  - crates/z00z_storage/tests/test_async_scheduler.rs
requirements:
  - PH53-11
autonomous: true
must_haves:
  truths:
    - Forest commits, proof generation, policy transitions, and warm reload can run through a bounded scheduler.
    - Scheduler concurrency cannot change root determinism, proof bytes, or commit ordering.
  artifacts:
    - crates/z00z_storage/src/settlement/hjmt_scheduler.rs
    - crates/z00z_storage/tests/test_async_scheduler.rs
  key_links:
    - Commit, proof, RedB, and cache warmup paths submit work through the same bounded scheduler contract.
    - Scheduler outputs are sorted and checked before publication.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-11 Plan: Forest Scheduler

<objective>
Add bounded async scheduling for the generalized HJMT forest.

Purpose: production parallelism must improve throughput without nondeterministic roots or unbounded blocking.
Output: `ForestScheduler`, deterministic execution, cancellation, backpressure, and tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@crates/z00z_storage/src/settlement/hjmt_commit.rs
@crates/z00z_storage/src/settlement/hjmt_store.rs
@crates/z00z_storage/src/settlement/hjmt_proof.rs
@crates/z00z_storage/src/settlement/hjmt_policy.rs
@crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Add bounded deterministic ForestScheduler</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` sections Async And Parallel Execution and 053-11
- `crates/z00z_storage/src/settlement/hjmt_commit.rs`
- `crates/z00z_storage/src/settlement/redb_backend_hjmt.rs`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
- `crates/z00z_storage/src/settlement/hjmt_commit.rs`
- `crates/z00z_storage/src/settlement/hjmt_store.rs`
- `crates/z00z_storage/src/settlement/hjmt_proof.rs`
- `crates/z00z_storage/src/settlement/hjmt_policy.rs`
- `crates/z00z_storage/src/settlement/redb_backend_hjmt.rs`
- `crates/z00z_storage/src/settlement/store_rows.rs`
- `crates/z00z_storage/tests/test_async_scheduler.rs`
</files>
<action>
Implement D-11 by adding `ForestScheduler` for bounded child commits, proof generation, HJMT policy-transition work, and warm reload. Use deterministic input ordering, sorted output joins, bounded queue depth, cancellation propagation, backpressure errors, and `spawn_blocking` or existing project-safe blocking boundaries for RedB work. Ensure scheduler failures fail closed before root publication and cannot leave partial journal status as committed.
</action>
<acceptance_criteria>
- Serial and scheduled execution produce identical roots, proof bytes, policy transition proofs, and cache digests.
- Saturation returns typed backpressure errors without partially committed roots.
- Cancellation and RedB blocking tests leave journal/recovery state consistent.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --test test_async_scheduler</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-11-PLAN.md current_task="Add bounded deterministic ForestScheduler" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-11-PLAN.md current_task="Add bounded deterministic ForestScheduler" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-11-PLAN.md current_task="Add bounded deterministic ForestScheduler" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
Forest scheduling is bounded, deterministic, cancellation-safe, and RedB-safe.
</done>
</task>
</tasks>

<threat_model>
- Tampering: unordered parallel joins can fork roots; sorted deterministic joins prevent it.
- Denial of service: unbounded queues can exhaust resources; bounded depth and backpressure are required.
- Repudiation: cancellation can leave ambiguous journal state; failure paths remain uncommitted and recoverable.
</threat_model>

<verification>
Execution must include serial-vs-scheduled equality, backpressure, cancellation, and RedB blocking evidence.
</verification>

<success_criteria>
- Parallel execution is a performance layer, not a source of semantic nondeterminism.
- Scheduler failure paths are fail-closed.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-11-SUMMARY.md` when done.
</output>
