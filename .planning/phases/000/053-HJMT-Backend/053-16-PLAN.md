---
phase: 053-HJMT-Backend
plan: 053-16
type: execute
wave: 16
depends_on:
  - 053-15
files_modified:
  - crates/z00z_storage/tests/test_golden_corpus.rs
  - crates/z00z_storage/tests/test_property_corpus.rs
  - crates/z00z_storage/tests/fixtures/test_settlement_corpus_fixture.json
  - crates/z00z_storage/fuzz/fuzz_targets/settlement_proofs.rs
  - crates/z00z_storage/fuzz/seeds/settlement_proofs/README.md
  - crates/z00z_core/tests/genesis/test_settlement_corpus.rs
  - crates/z00z_simulator/tests/test_scenario_settlement.rs
requirements:
  - PH53-16
autonomous: true
must_haves:
  truths:
    - Golden corpus and property tests cover assets, rights, fees, proof families, adaptive buckets, reload, and downstream rejection.
    - Fuzz seeds exercise malformed settlement paths, proof families, right leaves, fee envelopes, and policy transitions.
  artifacts:
    - crates/z00z_storage/tests/fixtures/test_settlement_corpus_fixture.json
    - crates/z00z_storage/tests/test_golden_corpus.rs
    - crates/z00z_storage/fuzz/fuzz_targets/settlement_proofs.rs
  key_links:
    - Core-generated settlement corpus feeds storage golden tests and simulator scenario assertions.
    - Fuzz targets use storage-owned decoders and verifier APIs, not downstream parser copies.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-16 Plan: Corpus, Property Tests, And Fuzz Seeds

<objective>
Build the regression corpus for the generalized settlement backend.

Purpose: Phase 053 needs durable coverage beyond one scenario path.
Output: golden vectors, property tests, fuzz target/seeds, and cross-crate corpus checks.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@crates/z00z_storage/src/settlement/proof.rs
@crates/z00z_core/src/genesis/genesis_settlement_manifest.rs
@crates/z00z_simulator/src/scenario_1/stage_13_utils/test_mod.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Add golden corpus, property tests, and fuzz seeds for settlement proofs</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` section 053-16
- `crates/z00z_storage/src/settlement/proof.rs`
- `crates/z00z_core/src/genesis/genesis_settlement_manifest.rs`
</read_first>
<files>
- `crates/z00z_storage/tests/test_golden_corpus.rs`
- `crates/z00z_storage/tests/test_property_corpus.rs`
- `crates/z00z_storage/tests/fixtures/test_settlement_corpus_fixture.json`
- `crates/z00z_storage/fuzz/fuzz_targets/settlement_proofs.rs`
- `crates/z00z_storage/fuzz/seeds/settlement_proofs/README.md`
- `crates/z00z_core/tests/genesis/test_settlement_corpus.rs`
- `crates/z00z_simulator/tests/test_scenario_settlement.rs`
</files>
<action>
Implement D-16 by adding a deterministic HJMT-only mixed asset/right corpus that does not depend on the Phase 052 asset-only oracle. Cover asset put/delete, right create/transfer/consume/revoke, fee-envelope accept/reject, inclusion proof, deletion proof, non-existence proof, split proof, merge proof, policy-transition proof, RedB reload, cache warmup, scheduler ordering, and wallet `RightLeaf` rejection. Add a model oracle for generalized settlement roots independent of the implementation under test, a negative corpus proving legacy asset root/path/proof inputs reject without state mutation, property tests for permitted operation reordering, path/terminal determinism, asset/right family separation, non-existence present-key rejection, fee replay rejection, policy epoch monotonicity, reject-path state preservation, and reload idempotence. Add a fuzz target and seed corpus that exercise storage-owned decoders and verifier APIs for malformed settlement paths, proof envelopes, right leaves, fee envelopes, occupancy evidence, epoch drift, root drift, and policy transition proofs.
</action>
<acceptance_criteria>
- Golden corpus can be regenerated deterministically from core genesis and storage APIs and matches the independent model oracle.
- Property tests fail on family confusion, stale fee replay, present-key absence, wrong epoch, and reload drift.
- Legacy asset-only sequences reject at the HJMT boundary and every reject path preserves roots and rows.
- Fuzz target compiles and seed docs identify the storage-owned decoder/verifier entry points.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --features wallet_debug_dump test_golden_corpus test_property_corpus</automated>
<automated>cargo test -p z00z_core --release --features test-fast --test genesis_tests</automated>
<automated>cargo test -p z00z_simulator --release --features wallet_debug_dump test_scenario_settlement</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-16-PLAN.md current_task="Add golden corpus, property tests, and fuzz seeds for settlement proofs" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-16-PLAN.md current_task="Add golden corpus, property tests, and fuzz seeds for settlement proofs" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-16-PLAN.md current_task="Add golden corpus, property tests, and fuzz seeds for settlement proofs" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
Generalized settlement behavior has golden, property, and fuzz-seed coverage across storage, core, and simulator paths.
</done>
</task>
</tasks>

<threat_model>
- Tampering: corpus vectors can be hand-edited; deterministic regeneration and digest checks detect drift.
- Spoofing: malformed proof bytes can target downstream parsers; fuzzing uses storage-owned decoders only.
- Denial of service: fuzz cases can trigger parser panics; no-panic typed errors are required.
</threat_model>

<verification>
Execution must include golden corpus, property tests, fuzz compile evidence, and D-21 review output.
</verification>

<success_criteria>
- Corpus coverage catches cross-crate drift.
- Settlement proof decoders and verifiers are fuzz-ready.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-16-SUMMARY.md` when done.
</output>
