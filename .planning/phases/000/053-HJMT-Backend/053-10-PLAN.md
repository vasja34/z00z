---
phase: 053-HJMT-Backend
plan: 053-10
type: execute
wave: 10
depends_on:
  - 053-09
files_modified:
  - crates/z00z_storage/src/assets/store/hjmt_cache.rs
  - crates/z00z_storage/src/assets/store/hjmt_commit.rs
  - crates/z00z_storage/src/assets/store/hjmt_store.rs
  - crates/z00z_storage/src/assets/store/hjmt_proof.rs
  - crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
  - crates/z00z_storage/tests/test_forest_cache.rs
  - crates/z00z_storage/tests/test_cache_recompute.rs
requirements:
  - PH53-10
autonomous: true
must_haves:
  truths:
    - Forest cache hits are private performance state and never semantic authority.
    - Dirty-set invalidation and recomputation failures fail closed without changing settlement roots.
  artifacts:
    - crates/z00z_storage/src/assets/store/hjmt_cache.rs
    - crates/z00z_storage/tests/test_forest_cache.rs
  key_links:
    - Commit, proof, RedB reload, and warmup paths all use the same cache validation contract.
    - Cache entries are keyed by root generation, path, family, codec version, payload hash, policy id, and bucket epoch as applicable.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-10 Plan: Forest Cache Plane

<objective>
Implement the private forest cache plane for settlement HJMT performance.

Purpose: production cache machinery must improve repeated derivations without becoming semantic authority.
Output: cache layers, invalidation, warmup, recompute checks, metrics hooks, and tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@docs/tech-papers/Z00Z-HJMT-Design.md
@crates/z00z_storage/src/assets/store/hjmt_commit.rs
@crates/z00z_storage/src/assets/store/hjmt_store.rs
@crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Add recomputable cache layers, dirty invalidation, and warmup</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` sections Performance And Cache Contract and 053-10
- `crates/z00z_storage/src/assets/store/hjmt_commit.rs`
- `crates/z00z_storage/src/assets/store/hjmt_store.rs`
</read_first>
<files>
- `crates/z00z_storage/src/assets/store/hjmt_cache.rs`
- `crates/z00z_storage/src/assets/store/hjmt_commit.rs`
- `crates/z00z_storage/src/assets/store/hjmt_store.rs`
- `crates/z00z_storage/src/assets/store/hjmt_proof.rs`
- `crates/z00z_storage/src/settlement/redb_backend_hjmt.rs`
- `crates/z00z_storage/tests/test_forest_cache.rs`
- `crates/z00z_storage/tests/test_cache_recompute.rs`
</files>
<action>
Implement D-10 by adding `ForestCache` layers for stable roots, parent leaves, terminal encodings, proof segments, journal digests, policy transition evidence, and reload warmup. Key cache entries by root generation, settlement path, leaf family, codec version, payload hash, policy id, bucket epoch, and proof family as applicable. Add dirty-set invalidation, bounded cache size, recomputation checks, and fail-closed handling for stale or inconsistent cache entries. Ensure cache state is private diagnostics/performance state and never part of public semantic authority.
</action>
<acceptance_criteria>
- Cache hits and misses produce the same settlement roots and proof transcripts as cold recomputation.
- Dirty updates invalidate all affected parent, proof, policy, and journal-digest entries.
- Corrupt, stale, wrong-generation, and wrong-epoch cache entries fail closed and leave durable state unchanged.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --features wallet_debug_dump test_forest_cache test_cache_recompute</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-10-PLAN.md current_task="Add recomputable cache layers, dirty invalidation, and warmup" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-10-PLAN.md current_task="Add recomputable cache layers, dirty invalidation, and warmup" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-10-PLAN.md current_task="Add recomputable cache layers, dirty invalidation, and warmup" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
Forest cache is live, private, recomputable, bounded, and fail-closed.
</done>
</task>
</tasks>

<threat_model>
- Tampering: stale cache entries can alter roots; recomputation checks reject inconsistent cache state.
- Information disclosure: cache keys can leak path activity; cache remains private diagnostics state.
- Denial of service: unbounded cache can exhaust memory; bounded sizing and warmup limits are enforced.
</threat_model>

<verification>
Execution must include cold/hot equality, stale-cache rejection, dirty invalidation, and warmup evidence.
</verification>

<success_criteria>
- Cache improves performance without changing semantic roots or proof results.
- Cache corruption cannot become accepted settlement state.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-10-SUMMARY.md` when done.
</output>
