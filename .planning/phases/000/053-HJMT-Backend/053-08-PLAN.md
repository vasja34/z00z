---
phase: 053-HJMT-Backend
plan: 053-08
type: execute
wave: 8
depends_on:
  - 053-07
files_modified:
  - crates/z00z_storage/src/settlement/types_record.rs
  - crates/z00z_storage/src/settlement/hjmt_policy.rs
  - crates/z00z_storage/src/settlement/hjmt_commit.rs
  - crates/z00z_storage/src/settlement/hjmt_journal.rs
  - crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
  - crates/z00z_storage/src/settlement/store_rows.rs
  - crates/z00z_storage/src/settlement/live_recovery_tests.rs
  - crates/z00z_storage/tests/test_hjmt_adaptive_policy_proofs.rs
  - crates/z00z_storage/tests/test_redb_reload.rs
requirements:
  - PH53-08
autonomous: true
must_haves:
  truths:
    - Adaptive buckets are live with epoch-bound split, merge, and policy-transition proofs.
    - Bucket policy changes are replayable, recoverable, and verifier-owned.
  artifacts:
    - crates/z00z_storage/src/settlement/types_record.rs
    - crates/z00z_storage/src/settlement/hjmt_policy.rs
    - crates/z00z_storage/tests/test_hjmt_adaptive_policy_proofs.rs
  key_links:
    - Bucket epoch and policy id bind commit planning, proof generation, journal rows, and RedB reload.
    - Split, merge, and policy transition proofs verify old and new roots explicitly.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-08 Plan: Adaptive Bucket Policy Proofs

<objective>
Implement adaptive buckets, bucket epochs, and policy-transition proof families.

Purpose: bucket policy changes must be live, provable, and recoverable under the generalized settlement root.
Output: adaptive bucket metadata, split/merge/policy proofs, rollback rules, and tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@docs/tech-papers/Z00Z-HJMT-Design.md
@crates/z00z_storage/src/settlement/hjmt_policy.rs
@crates/z00z_storage/src/settlement/hjmt_commit.rs
@crates/z00z_storage/src/settlement/hjmt_journal.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Add adaptive bucket epochs and split, merge, and policy-transition proofs</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` section 053-08
- `docs/tech-papers/Z00Z-HJMT-Design.md` adaptive bucket and policy-transition sections
- `crates/z00z_storage/src/settlement/hjmt_policy.rs`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/types_record.rs`
- `crates/z00z_storage/src/settlement/hjmt_policy.rs`
- `crates/z00z_storage/src/settlement/hjmt_commit.rs`
- `crates/z00z_storage/src/settlement/hjmt_journal.rs`
- `crates/z00z_storage/src/settlement/redb_backend_hjmt.rs`
- `crates/z00z_storage/src/settlement/store_rows.rs`
- `crates/z00z_storage/src/settlement/live_recovery_tests.rs`
- `crates/z00z_storage/tests/test_hjmt_adaptive_policy_proofs.rs`
- `crates/z00z_storage/tests/test_redb_reload.rs`
</files>
<action>
Implement D-08 by adding `AdaptiveBucket`, `BucketEpoch`, `SplitProof`, `MergeProof`, and `PolicyTransitionProof`. Bind bucket metadata to parent serial, bucket range or prefix, policy id, epoch, previous root, next root, occupancy evidence, and settlement root generation. Add hysteresis and deterministic ordering for split/merge decisions, rollback handling, recovery replay, and typed rejection for wrong epoch, wrong old root, wrong new root, stale policy id, and unordered transition batches.
</action>
<acceptance_criteria>
- Split, merge, and policy-transition proofs verify only under the expected old/new root pair and bucket epoch.
- Crash/replay tests preserve the same settlement root after policy transition recovery.
- Adaptive policy state persists and reloads without accepting stale Phase 052 bucket rows.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_tools</automated>
<automated>cargo test -p z00z_storage --release --features test-fast</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-08-PLAN.md current_task="Add adaptive bucket epochs and split, merge, and policy-transition proofs" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-08-PLAN.md current_task="Add adaptive bucket epochs and split, merge, and policy-transition proofs" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-08-PLAN.md current_task="Add adaptive bucket epochs and split, merge, and policy-transition proofs" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
Adaptive bucket policy changes are live, epoch-bound, verifier-validating, and recoverable.
</done>
</task>
</tasks>

<threat_model>
- Tampering: policy proofs can be replayed across epochs; epoch and old/new root binding rejects replay.
- Information disclosure: split timing can leak activity; occupancy evidence is bounded and handed to the privacy plan.
- Denial of service: split/merge churn can destabilize roots; hysteresis and deterministic ordering limit churn.
</threat_model>

<verification>
Execution must include split, merge, policy-transition, recovery, and stale-row rejection evidence.
</verification>

<success_criteria>
- Adaptive buckets are production settlement state, not a sidecar policy note.
- Policy transitions are proof-bound and replayable.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-08-SUMMARY.md` when done.
</output>
