---
phase: 053-HJMT-Backend
plan: 053-13
type: execute
wave: 13
depends_on:
  - 053-12
files_modified:
  - crates/z00z_storage/src/settlement/redb_backend_helpers.rs
  - crates/z00z_storage/src/settlement/store.rs
  - crates/z00z_storage/src/settlement/store_types.rs
  - crates/z00z_storage/src/settlement/store_codec.rs
  - crates/z00z_storage/src/error.rs
  - crates/z00z_storage/tests/test_redb_reload.rs
  - crates/z00z_storage/tests/test_hjmt_proofs.rs
  - crates/z00z_storage/tests/test_fee_replay.rs
requirements:
  - PH53-13
autonomous: true
must_haves:
  truths:
    - RedB reload reconstructs generalized settlement state and rejects Phase 052 rows as unsupported generation.
    - Historical proofs and cache warmup use durable settlement metadata, not compatibility projection.
  artifacts:
    - crates/z00z_storage/tests/test_redb_reload.rs
    - crates/z00z_storage/tests/test_hjmt_proofs.rs
    - crates/z00z_storage/tests/test_fee_replay.rs
  key_links:
    - RedB rows feed recovery, historical proof generation, serialization restore, and cache warmup through settlement contracts.
    - Unsupported-generation errors are typed and test-covered.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 053-13 Plan: RedB Reload And Historical Proofs

<objective>
Finish durable RedB persistence, reload, historical proof, and cache warmup support.

Purpose: persisted generalized settlement state must reload exactly and reject old Phase 052 rows without conversion.
Output: RedB row schema, reload validation, historical proofs, cache warmup, and tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/phases/053-HJMT-Backend/053-TODO.md
@.planning/phases/053-HJMT-Backend/053-CONTEXT.md
@crates/z00z_storage/src/settlement/redb_backend_hjmt.rs
@crates/z00z_storage/src/settlement/store_rows.rs
@crates/z00z_storage/src/serialization/restore.rs
</context>

<coverage_contract>
This plan implements the complete matching section of `.planning/phases/053-HJMT-Backend/053-TODO.md`, not only the summarized bullets in this file. Every checklist bullet, nested detail, acceptance bullet, test bullet, non-goal, failure condition, completion-gate item, and referenced-doc constraint applicable to this plan is mandatory. Respect `053-CONTEXT.md` decisions D-22, D-23, D-24, D-25, and D-26: extend or replace existing repository seams in place, do not duplicate existing codebase logic, do not create a parallel authority layer, and treat any non-existing file listed here as a proposed target that must be created only when no existing module owns that responsibility.
</coverage_contract>

<tasks>
<task type="auto">
<name>Persist settlement rows, reject old-generation rows, and reload historical proof state</name>
<read_first>
- `.planning/phases/053-HJMT-Backend/053-TODO.md` section 053-13
- `crates/z00z_storage/src/settlement/redb_backend_hjmt.rs`
- `crates/z00z_storage/src/serialization/artifact.rs`
- `crates/z00z_storage/src/serialization/restore.rs`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/redb_backend_helpers.rs`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_storage/src/settlement/store_types.rs`
- `crates/z00z_storage/src/settlement/store_codec.rs`
- `crates/z00z_storage/src/error.rs`
- `crates/z00z_storage/tests/test_redb_reload.rs`
- `crates/z00z_storage/tests/test_hjmt_proofs.rs`
- `crates/z00z_storage/tests/test_fee_replay.rs`
</files>
<action>
Implement D-13 by adding durable RedB rows for settlement root generation, settlement paths, terminal ids, asset/right leaf family, fee replay state, proof envelope generation, bucket epoch, policy transition id, bounded occupancy evidence, cache warm rows, and historical proof metadata. Reload rows into the generalized store only after generation and digest validation. Reject Phase 052 asset-root rows with typed unsupported-generation errors and no conversion lane. Ensure historical inclusion, deletion, non-existence, split, merge, and policy-transition proofs can be produced from reloaded state.
</action>
<acceptance_criteria>
- RedB close/reopen preserves root, proof, fee, bucket, policy, and cache-warm evidence.
- Phase 052 rows are rejected with typed unsupported-generation errors.
- Historical proof tests pass after reload and after cache warmup.
</acceptance_criteria>
<verify>
<automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
<automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
<automated>cargo test -p z00z_storage --release --features test-fast --test test_redb_reload --test test_hjmt_proofs --test test_fee_replay</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-13-PLAN.md current_task="Persist settlement rows, reject old-generation rows, and reload historical proof state" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-13-PLAN.md current_task="Persist settlement rows, reject old-generation rows, and reload historical proof state" --yolo</automated>
<automated>/GSD-Review-Tasks-Execution current_spec=.planning/phases/053-HJMT-Backend/053-13-PLAN.md current_task="Persist settlement rows, reject old-generation rows, and reload historical proof state" --yolo</automated>
Bootstrap is the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Run the review prompt in YOLO mode at least 3 times, fix all issues and warnings, and stop only after at least 2 consecutive runs show no significant issues. If committing is needed, use `/z00z-git-versioning`.
</verify>
<done>
RedB persistence reloads generalized settlement state exactly and rejects old-generation rows without conversion.
</done>
</task>
</tasks>

<threat_model>
- Tampering: persisted rows can be edited across generations; typed generation and digest checks reject them.
- Repudiation: historical proof state can drift after reload; historical proof tests bind proof metadata to root history.
- Denial of service: warmup can load excessive cache rows; bounded warmup uses cache limits from 053-10.
</threat_model>

<verification>
Execution must include close/reopen, unsupported-generation rejection, historical proof, and cache warmup evidence.
</verification>

<success_criteria>
- Durable reload is settlement-native.
- Old rows are rejected, not converted.
</success_criteria>

<output>
Create `.planning/phases/053-HJMT-Backend/053-13-SUMMARY.md` when done.
</output>
