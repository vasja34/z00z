---
phase: 033-crypto-audit-scenario-2
plan: 19
type: execute
wave: 19
depends_on:
  - 033-18
files_modified:
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/tests/test_e2e_req_flow.rs
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
autonomous: true
requirements:
  - PH32-CLAIM-TRUST
  - PH32-SEM
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 57-59 are executed in order.
    - Request-bound privacy promotion, real claim authority, and genesis-membership fix work stay distinct.
    - Existing abstractions are treated as building blocks, not as already-complete closure.
  artifacts:
    - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    - crates/z00z_wallets/src/services/wallet_service.rs
    - crates/z00z_wallets/tests/test_e2e_req_flow.rs
    - crates/z00z_crypto/src/claim/v2.rs
    - crates/z00z_storage/src/assets/store_internal/store_query.rs
  key_links:
    - Request-bound mode must be promoted from available option toward normal privacy path.
    - Signature primitives exist but live anchored authority lifecycle does not yet.
    - Existing statement binding must be preserved while helper continuity is replaced.
---

# Phase 033 Plan 19

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 57-59 so the first concrete fix-set trio translates repository-backed caution into implementation-ready boundaries.

Purpose: turn the request-bound, claim-authority, and genesis-membership findings into explicit upgrade paths grounded in current seams.
Output: concrete, non-overclaiming fix-set plans for request-bound routing, claim authority lifecycle, and genesis membership proofs.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-3.md
@.planning/phases/033-crypto-audit-scenario-2/033-18-PLAN.md

Live code anchors:

- `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs`
- `crates/z00z_wallets/src/services/wallet_service.rs`
- `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
- `crates/z00z_crypto/src/claim/v2.rs`
- `crates/z00z_storage/src/assets/store_internal/store_query.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 57: Request-bound tag fix set</name>
  <files>crates/z00z_wallets/src/services/wallet_service_actions_receive.rs, crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/tests/test_e2e_req_flow.rs</files>
  <behavior>
    - Request-bound machinery is acknowledged as existing.
    - The fix set promotes request-bound mode toward the normal privacy path without claiming default-path closure already exists.
  </behavior>
  <action>Use the exact task title above. Current caution reason: request-bound machinery exists, but default-path closure is not proven. Safe final reading from the source table: promote request-bound mode from available option to normal privacy path. Define the fix set around `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs`, `crates/z00z_wallets/src/services/wallet_service.rs`, and `crates/z00z_wallets/tests/test_e2e_req_flow.rs` so the promotion path is explicit and evidence-backed.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 57 closes only when the request-bound fix set is framed as promoting the normal privacy path rather than claiming default-path closure already exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 58: Real claim authority fix set</name>
  <files>crates/z00z_crypto/src/claim/v2.rs, crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_simulator/src/claim_pkg_consumer.rs</files>
  <behavior>
    - Existing signature primitives are acknowledged.
    - The missing element is a live anchored authority lifecycle, not the signature primitive itself.
  </behavior>
  <action>Use the exact task title above. Current caution reason: signature primitive exists; trust anchor does not. Safe final reading from the source table: replace simulator-fixed authority roots with live anchored authority lifecycle. Build the fix set directly on `ClaimStmtV2`, store query seams, and the claim consumer so the missing authority anchor is explicit.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 58 closes only when the real claim-authority fix set is described as adding a live anchored authority lifecycle over existing signature primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 59: Genesis membership fix set</name>
  <files>crates/z00z_crypto/src/claim/v2.rs, crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs</files>
  <behavior>
    - Existing statement binding strength is preserved.
    - The fix set replaces helper continuity with authoritative membership proofs.
  </behavior>
  <action>Use the exact task title above. Current caution reason: field binding is already stronger than the thesis suggests. Safe final reading from the source table: preserve current statement binding and replace helper continuity with authoritative membership proofs. Tie the fix set to claim statement binding, store membership continuity, and claim verifier proof seams only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 59 closes only when the genesis-membership fix set preserves existing statement binding and focuses new work on replacing helper continuity with authoritative proofs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing request/authority/membership primitives -> closure claims | Existing building blocks can be mistaken for completed fix sets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-57 | I | Request-bound fix-set scope | mitigate | Frame the work as promoting the normal privacy path, not claiming default closure |
| T-033-58 | I | Claim authority lifecycle | mitigate | Separate signature primitives from missing trust-anchor lifecycle |
| T-033-59 | I | Genesis membership fix-set scope | mitigate | Preserve strong statement binding while focusing new work on authoritative membership proofs |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate, then repeated YOLO review until two consecutive clean runs are achieved.
</verification>

<success_criteria>
Canonical tasks 57-59 are executed in order and the first concrete fix sets remain grounded in existing seams without overstating current closure.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-19-SUMMARY.md`
</output>