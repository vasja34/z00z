---
phase: 033-crypto-audit-scenario-2
plan: 14
type: execute
wave: 14
depends_on:
  - 033-13
files_modified:
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/state_checkpoint.rs
  - crates/z00z_simulator/src/scenario_1/stage_11.rs
  - crates/z00z_simulator/src/scenario_1/stage_12.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
autonomous: true
requirements:
  - PH32-SPEND
  - PH32-CHECKPOINT
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 40-42 are executed in order.
    - The missing nullifier semantics, package-coupled checkpoint continuity, and operator-boundary protection remain separate findings.
    - No checkpoint compatibility payload is misdescribed as standalone authoritative backend proof.
  artifacts:
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
    - crates/z00z_wallets/src/core/tx/state_checkpoint.rs
    - crates/z00z_simulator/src/scenario_1/stage_11.rs
    - crates/z00z_simulator/src/scenario_1/stage_12.rs
  key_links:
    - The regular spend contract still lacks nullifier semantics.
    - Stage11/12 continuity remains package-coupled and anti-substitution.
    - Operator-boundary protection remains checkpoint-time package-coupled protection rather than standalone backend authority.
---

# Phase 033 Plan 14

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 40-42 so the missing spend-statement element and the exact checkpoint/operator boundary remain pinned to live code truth.

Purpose: isolate the two major public-boundary gaps before the replay/export and later caution/fix-set plans build on them.
Output: explicit nullifier-gap wording, package-coupled continuity proof boundaries, and precise operator-boundary protection language.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-2.md
@.planning/phases/033-crypto-audit-scenario-2/033-13-PLAN.md

Live code anchors:

- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
- `crates/z00z_wallets/src/core/tx/state_checkpoint.rs`
- `crates/z00z_simulator/src/scenario_1/stage_11.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12.rs`
- `crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 40: The Missing Spend-Statement Element</name>
  <files>crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/spend_rules.rs, .planning/REQUIREMENTS.md</files>
  <behavior>
    - The missing public spend-statement element is always described as nullifier field / nullifier semantics.
    - The delivered verifier is never described as missing proof/auth data in general.
  </behavior>
  <action>Use the exact task title above. Current blocker: the regular spend contract is missing a nullifier field / nullifier semantics surface, so the delivered public verifier remains narrower than original `PH32-SPEND`. Gap closure path from the source table: move Q19 to checkpoint continuity versus compatibility-looking proof bytes only after freezing the nullifier-gap statement. Tie the requirement wording and spend-verification comments/tests to the nullifier semantics gap only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 40 closes only when the spend-statement gap is always named as nullifier semantics and never diluted into a vaguer missing-verifier story.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 41: Checkpoint Continuity Or Compatibility-Looking Proof Bytes</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_11.rs, crates/z00z_simulator/src/scenario_1/stage_12.rs, crates/z00z_wallets/src/core/tx/state_checkpoint.rs</files>
  <behavior>
    - Stage11 enforces package-coupled continuity of stage4 proof, input refs, and stage6 bridge outputs.
    - Compatibility-looking proof bytes alone remain insufficient.
  </behavior>
  <action>Use the exact task title above. Current blocker: stage11 enforces package-coupled continuity of stage4 proof, input refs, and stage6 bridge outputs; compatibility-looking proof bytes alone are not accepted, but this is still not a standalone backend. Gap closure path from the source table: use Q20 to state the exact operator-boundary protection without overstating backend authority. Preserve the package-coupled continuity statement in state-checkpoint and stage11/12 flows, and add tests if any compatibility-looking proof-byte branch remains under-covered.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 41 closes only when checkpoint continuity is stated as package-coupled and compatibility-looking proof bytes are explicitly rejected as insufficient by themselves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 42: Real Protection Against The Operator Boundary</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_12.rs, crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs, .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md</files>
  <behavior>
    - The real protection remains package-coupled anti-substitution at checkpoint time.
    - Standalone backend authority is not claimed.
  </behavior>
  <action>Use the exact task title above. Current blocker: real protection is package-coupled anti-substitution at checkpoint time, not standalone backend authority. Gap closure path from the source table: theme 5 can move on only after this exact operator-boundary wording is frozen. Keep stage12 and RedB validation references aligned to anti-substitution and fail-closed revalidation, and block any wording that implies a fully authoritative standalone checkpoint backend already exists.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 42 closes only when operator-boundary protection is defined as package-coupled anti-substitution and no standalone-backend authority claim remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public spend verifier -> PH32-SPEND interpretation | Missing nullifier semantics can be hidden by vague verifier language |
| stage11/stage12 package continuity -> backend authority narrative | Package-coupled protection can be overstated as authoritative backend proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-40 | I | Spend-statement gap language | mitigate | Freeze the gap on nullifier semantics only |
| T-033-41 | T | Checkpoint proof-byte compatibility | mitigate | Keep package-coupled continuity tests and reject proof-byte-only interpretations |
| T-033-42 | I | Operator-boundary protection narrative | mitigate | Preserve anti-substitution wording and ban standalone backend authority claims |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate, then repeated YOLO review until two consecutive clean runs are achieved.
</verification>

<success_criteria>
Canonical tasks 40-42 are executed in order, nullifier semantics stay the exact spend gap, and checkpoint/operator protection remains package-coupled and narrow.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-14-SUMMARY.md`
</output>