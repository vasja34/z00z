---
phase: 033-crypto-audit-scenario-2
plan: 6
type: execute
wave: 6
depends_on:
  - 033-05
files_modified:
  - crates/z00z_storage/src/error.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
  - crates/z00z_storage/src/assets/store_internal/store_rows.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
  - crates/z00z_simulator/src/scenario_1/storage_view.rs
autonomous: true
requirements:
  - PH32-CLAIM-TRUST
  - PH32-CHECKPOINT
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 16-18 are executed in order.
    - Draft/final artifact truth, persistence injectivity, and replay/stale resistance remain separate seams.
    - Any weak raw-artifact surface is documented separately from the stronger persisted path.
  artifacts:
    - crates/z00z_storage/src/error.rs
    - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
    - crates/z00z_storage/src/assets/store_internal/store_rows.rs
    - crates/z00z_storage/tests/test_redb_rehydrate.rs
  key_links:
    - Draft/final state claims must match the storage-owned checkpoint class boundaries.
    - Rehydrate validation must guard proof-byte, link, and replay consistency.
    - Weak raw checkpoint identity surfaces must not be confused with canonical persisted binding.
---

# Phase 033 Plan 06

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 16-18 so draft/final truth, persistence binding, and replay/stale resistance are explicit, fail closed, and correctly scoped.

Purpose: provide the checkpoint-storage baseline required before later caution/high-severity plans touch authoritative checkpoint claims.
Output: stronger draft/final separation evidence, explicit weak/raw-surface caveats, and replay/rehydrate regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-1.md
@.planning/phases/033-crypto-audit-scenario-2/033-05-PLAN.md

Live code anchors:
- `crates/z00z_storage/src/error.rs`
- `crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs`
- `crates/z00z_storage/src/assets/store_internal/store_rows.rs`
- `crates/z00z_storage/tests/test_redb_rehydrate.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 16: Draft Versus Final Truth</name>
  <files>crates/z00z_simulator/src/scenario_1/storage_view.rs, crates/z00z_simulator/src/scenario_1/stage_12.rs, crates/z00z_storage/src/error.rs</files>
  <behavior>
    - Draft and final remain separated by class-specific loading, proof-bound finalization, and canonical statement/id/link checks.
    - Draft-only outputs cannot masquerade as final authoritative state.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_simulator/src/scenario_1/storage_view.rs`, `crates/z00z_simulator/src/scenario_1/stage_12.rs`, and `crates/z00z_storage/src/error.rs`. Current delivered boundary: draft and final remain separated by class-specific loading, proof-bound finalization, and canonical statement/id/link checks; draft-only outputs do not masquerade as final authoritative state. Missing invariant: keep those boundaries explicit in tests/docs and isolate any remaining compatibility artifacts as compatibility-only. Forbidden overclaim: do not describe explicit legacy-final compatibility artifacts as proof that draft/final separation is weak. Blocker text: draft and final remain separated by class-specific loading, proof-bound finalization, and canonical statement/id/link checks. Gap-closure path: distinguish draft-vs-final separation from the separate existence of explicit legacy-final compatibility artifacts. Required proof surfaces: finalize/load tests in `storage_view.rs` and `stage_12.rs` plus error-taxonomy assertions in `error.rs`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 16 closes only when draft-only and finalized checkpoint surfaces remain unambiguous and compatibility artifacts are documented as compatibility-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17: Injective Persistence Contract</name>
  <files>crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs, crates/z00z_storage/tests/test_redb_rehydrate.rs, crates/z00z_storage/src/error.rs</files>
  <behavior>
    - Weak raw checkpoint identity by proof payload is documented as weaker than the persisted path.
    - The canonical RedB-persisted path retains stronger binding to statement, exec identity, and bound root.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs`, `crates/z00z_storage/tests/test_redb_rehydrate.rs`, and `crates/z00z_storage/src/error.rs`. Current delivered boundary: raw checkpoint artifacts are not fully injective by proof payload because checkpoint identity ignores cp_proof bytes, while the canonical RedB-persisted path adds stronger fail-closed binding to statement, exec identity, and bound root. Missing invariant: keep the weak raw artifact surface explicitly separate from the stronger persisted path and add tests/docs that pin this distinction. Forbidden overclaim: do not call the raw artifact surface fully injective by proof payload. Blocker text: raw checkpoint artifacts are not fully injective by proof payload because checkpoint identity ignores cp_proof bytes. Gap-closure path: separate the weak raw artifact surface from the stronger canonical persisted rehydrate path. Required proof surfaces: proof-tamper rehydrate tests in `test_redb_rehydrate.rs` and docs/test assertions describing the distinction.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 17 closes only when the repository clearly distinguishes the weak raw surface from the stronger persisted binding and tests prove the persisted path stays fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18: Replay And Stale-Artifact Resistance</name>
  <files>crates/z00z_storage/src/assets/store_internal/store_rows.rs, crates/z00z_storage/tests/test_redb_rehydrate.rs, crates/z00z_simulator/src/scenario_1/storage_view.rs</files>
  <behavior>
    - Stale checkpoint tuples and replayed proof-bearing bundles fail closed.
    - Old spent-state row claims stay as narrow as the actual test/code evidence allows.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_storage/src/assets/store_internal/store_rows.rs`, `crates/z00z_storage/tests/test_redb_rehydrate.rs`, and `crates/z00z_simulator/src/scenario_1/storage_view.rs`. Current delivered boundary: stale checkpoint tuples and replayed proof-bearing bundles are fail-closed by id/root/link/rehydrate checks, while old spent-state rows are strongly code-backed but not covered by one explicit rehydrate-then-replay end-to-end test. Missing invariant: add or tighten the explicit rehydrate-then-replay test for spent-row replay if the broader statement is retained; otherwise narrow the spent-row wording. Forbidden overclaim: do not state broader stale-artifact closure than the test matrix proves. Blocker text: old spent-state rows are strongly code-backed but not covered by one explicit rehydrate-then-replay end-to-end test. Gap-closure path: keep the strong replay/bundle closure, but state the spent-row portion more narrowly unless the missing end-to-end test is added. Required proof surfaces: replay and stale-artifact tests across `store_rows.rs`, `test_redb_rehydrate.rs`, and `storage_view.rs` rehydrate/load flows.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 18 closes only when replay/stale claims match the exact rehydrate and replay evidence, especially for spent-row behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint artifact class -> storage reload | Draft/final class drift must not permit silent acceptance |
| persisted exec/link/root state -> replay path | Rehydrate/replay inputs are untrusted until revalidated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-16 | T | Draft/final checkpoint classing | mitigate | Keep class-specific load/finalize checks explicit and regression-tested |
| T-033-17 | I | Raw checkpoint identity | mitigate | Document and test that only the persisted path has stronger binding to statement/exec/root |
| T-033-18 | R | Replay/stale protection | mitigate | Add or narrow spent-row replay claims until explicit rehydrate-then-replay proof exists |
</threat_model>

<verification>
Bootstrap first, then release-style cargo testing for Rust/test work, then run YOLO task-execution review repeatedly until two consecutive clean passes exist.
</verification>

<success_criteria>
Canonical tasks 16-18 are executed in order, draft/final state remains unambiguous, raw-vs-persisted injectivity is honestly scoped, and replay/stale resistance matches the actual evidence set.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-06-SUMMARY.md`
</output>