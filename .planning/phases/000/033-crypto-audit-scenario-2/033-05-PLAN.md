---
phase: 033-crypto-audit-scenario-2
plan: 5
type: execute
wave: 5
depends_on:
  - 033-04
files_modified:
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/spend_rules.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/exec_input_builder.rs
  - crates/z00z_simulator/src/scenario_1/stage_12.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
autonomous: true
requirements:
  - PH32-SPEND
  - PH32-CHECKPOINT
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 13-15 are executed in order.
    - The exact open PH32 element remains nullifier semantics unless real new scope lands.
    - Placeholder-lane closure is proven on the accepted path without claiming more than the current stack delivers.
  artifacts:
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
    - crates/z00z_wallets/src/core/tx/spend_rules.rs
    - crates/z00z_simulator/src/scenario_1/stage_6_utils/exec_input_builder.rs
    - crates/z00z_simulator/src/scenario_1/stage_12.rs
  key_links:
    - Nullifier semantics remain the exact missing public-contract element until implemented.
    - Layered whole-chain status must stay bucketed instead of flattened.
    - Placeholder success closures must stay scoped to accepted current-stack paths.
---

# Phase 033 Plan 05

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 13-15 so the exact remaining PH32 gap, layered closure buckets, and placeholder-lane status are all explicit and test-backed.

Purpose: freeze the semantic baseline before the later replay/docs/caution/high-severity plans attempt reclassification or closure.
Output: exact nullifier-gap tracking, layered status buckets, and accepted-path placeholder-closure evidence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-1.md
@.planning/phases/033-crypto-audit-scenario-2/033-04-PLAN.md

Live code anchors:
- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
- `crates/z00z_wallets/src/core/tx/spend_rules.rs`
- `crates/z00z_simulator/src/scenario_1/stage_6_utils/exec_input_builder.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13: The Requirement That Remains Open</name>
  <files>crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/spend_rules.rs, .planning/REQUIREMENTS.md</files>
  <behavior>
    - The exact still-open PH32 element remains nullifier semantics inside the regular spend public contract.
    - Proof/auth boundaries already delivered are not accidentally reclassified as missing.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/spend_verification.rs`, `crates/z00z_wallets/src/core/tx/spend_rules.rs`, and `PH32-SPEND` in `.planning/REQUIREMENTS.md`. Current delivered boundary: the exact missing element is nullifier semantics inside the regular spend public contract; current proof/auth boundary is real but `PH32-SPEND` stays open until nullifier binding is added or the requirement is narrowed. Missing invariant: preserve this exact statement in code/tests/docs and only widen if nullifier semantics truly land. Forbidden overclaim: do not imply the current proof/auth or spend verification is missing in general. Blocker text: the exact missing element is nullifier semantics inside the regular spend public contract. Gap-closure path: keep the answer narrowly on the nullifier gap; do not imply proof/auth or current spend verification is missing in general. Required proof surfaces: spend contract tests and requirement wording checks.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 13 closes only when the repository points to nullifier semantics as the single open PH32 spend element and no broader missingness claim remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 14: Full-Chain Crypto Closure Versus Partial Security</name>
  <files>.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_wallets/tests/test_s5_closure_gate.rs</files>
  <behavior>
    - The whole-chain answer stays explicitly bucketed into delivered, partial, and not-proved/out-of-scope classes.
    - No artifact flattens the validator leg into fully live crypto or claims end-to-end trustless closure.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `crates/z00z_simulator/src/scenario_1/scenario_design.yaml`, and `crates/z00z_wallets/tests/test_s5_closure_gate.rs`. Current delivered boundary: the honest whole-chain story is layered, with real crypto at claim/stealth/scan/spend seams while ownership continuity, checkpoint handoff, and replay/spent gating stay partly structural and major trustless-proof claims remain open. Missing invariant: keep the three-bucket classification synchronized across active docs/tests/status notes. Forbidden overclaim: do not flatten the validator leg into fully live crypto or full end-to-end trustless closure. Blocker text: the honest whole-chain story is layered. Gap-closure path: classify by bucket; do not flatten the validator leg into fully live crypto or claim full end-to-end trustless closure. Required proof surfaces: `test_s5_closure_gate.rs` plus synchronized wording across `033-CONTEXT.md`, `ROADMAP.md`, `STATE.md`, and `scenario_design.yaml`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 14 closes only when the delivered/partial/not-proved bucket model is explicit and synchronized across the audited artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15: Placeholder Success Paths Truly Closed</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_6_utils/exec_input_builder.rs, crates/z00z_simulator/src/scenario_1/stage_12.rs, crates/z00z_storage/tests/test_redb_rehydrate.rs</files>
  <behavior>
    - Placeholder proof/spent-state success lanes stay closed on the accepted current-stack path.
    - Accepted-path closure is not restated as recursive-proof or broader PH32-SPEND completion.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_simulator/src/scenario_1/stage_6_utils/exec_input_builder.rs`, `crates/z00z_simulator/src/scenario_1/stage_12.rs`, and `crates/z00z_storage/tests/test_redb_rehydrate.rs`. Current delivered boundary: checkpoint acceptance is now bound to stage4 package revalidation plus exact proof/input/output continuity and replay-consistent spent-state checks; placeholder proof/spent-state success lanes are closed on the accepted path. Missing invariant: preserve accepted-path closure in tests/docs and reject any wording that turns it into full recursive-proof or broader PH32-SPEND closure. Forbidden overclaim: do not expand accepted-path closure into stronger backend authority than the code actually provides. Blocker text: placeholder proof/spent-state success lanes are closed on the accepted path. Gap-closure path: keep scope on accepted current-stack checkpoint path; do not expand into recursive-proof or full PH32-SPEND claims. Required proof surfaces: stage12 finalize tests and rehydrate/replay failure tests.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 15 closes only when accepted-path placeholder closures remain test-backed and no artifact upgrades them into a broader theorem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement language -> verifier contract | Narrative drift can misstate the exact still-open spend invariant |
| stage4/stage12 checkpoint path -> public summary | Accepted-path closure can be misrepresented as broader backend authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-13 | I | PH32-SPEND status | mitigate | Keep nullifier semantics as the exact open public-contract element until implemented |
| T-033-14 | R | Whole-chain closure summary | mitigate | Enforce delivered/partial/not-proved buckets in tests/docs/status artifacts |
| T-033-15 | E | Placeholder-lane wording | mitigate | Keep accepted-path placeholder closure scoped to current-stack evidence only |
</threat_model>

<verification>
Bootstrap first, then release-style cargo testing for Rust/test changes, then repeat YOLO execution review until two clean consecutive passes exist.
</verification>

<success_criteria>
Canonical tasks 13-15 are executed in order, the single open PH32 spend element remains explicit, and placeholder-lane closure stays honest and bounded.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-05-SUMMARY.md`
</output>