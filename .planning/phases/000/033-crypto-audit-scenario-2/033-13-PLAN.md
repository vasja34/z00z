---
phase: 033-crypto-audit-scenario-2
plan: 13
type: execute
wave: 13
depends_on:
  - 033-12
files_modified:
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/spend_rules.rs
  - crates/z00z_simulator/src/scenario_1/stage_4.rs
  - crates/z00z_wallets/tests/test_scenario1_semantics.rs
autonomous: true
requirements:
  - PH32-SPEND
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 37-39 are executed in order.
    - Post-scan exclusivity, spend-boundary scope, and semantic-acceptance boundaries remain distinct.
    - No accepted-path proof is widened into a public trustless theorem.
  artifacts:
    - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
    - crates/z00z_wallets/src/core/tx/spend_rules.rs
    - crates/z00z_simulator/src/scenario_1/stage_4.rs
  key_links:
    - Receiver-secret plus `s_out` exclusivity stays wallet-local after scan.
    - The current persisted public spend contract stays narrower than full PH32-SPEND.
    - Structural plausibility must stay separate from semantic acceptance before state mutation.
---

# Phase 033 Plan 13

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 37-39 so exclusivity after scan, the exact spend boundary, and semantic-acceptance gating remain narrow and test-backed.

Purpose: finish the live public-spend interpretation before the missing-nullifier and checkpoint-backend slices begin.
Output: preserved wallet-local exclusivity wording, exact spend-boundary scope, and direct semantic-acceptance guards.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-2.md
@.planning/phases/033-crypto-audit-scenario-2/033-12-PLAN.md

Live code anchors:

- `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs`
- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
- `crates/z00z_wallets/src/core/tx/spend_rules.rs`
- `crates/z00z_simulator/src/scenario_1/stage_4.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 37: Exclusivity After Scan</name>
  <files>crates/z00z_wallets/src/services/wallet_service_actions_receive.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs</files>
  <behavior>
    - Post-scan exclusivity stays wallet-local and receiver-secret plus `s_out` gated.
    - No public trustless exclusivity theorem is implied.
  </behavior>
  <action>Use the exact task title above. Current blocker: exclusivity is wallet-local and not yet a public trustless guarantee. Gap closure path from the source table: keep post-scan handling direct and preserve the receiver-secret plus `s_out` ownership gate without overclaiming public exclusivity. Anchor the work in receive-action semantics, spend-verification guards, and Scenario 1 semantic tests. Add or tighten wording/tests so exclusivity is always stated as wallet-local accepted-path behavior.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 37 closes only when post-scan exclusivity is kept explicitly wallet-local and no artifact upgrades it into a public theorem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 38: What The Spend Boundary Actually Proves</name>
  <files>crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/spend_rules.rs, .planning/REQUIREMENTS.md</files>
  <behavior>
    - The current persisted public spend contract is stated precisely.
    - Input membership continuity, nullifier semantics, and stronger standalone backend claims remain outside the delivered boundary.
  </behavior>
  <action>Use the exact task title above. Current blocker: current persisted public spend contract proved; authoritative input membership continuity, nullifier semantics, and stronger standalone backend remain outside this boundary. Gap closure path from the source table: keep Q17 scoped to fail-closed semantic acceptance before state mutation. Align spend-verification, spend-rules, and requirement wording so the boundary always refers to the delivered persisted public spend contract and never to the broader still-open closure.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 38 closes only when the spend boundary is described as the delivered persisted public contract and the outside-of-boundary elements stay explicitly outside.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 39: Structural Plausibility Versus Semantic Acceptance</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_4.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_state_checkpoint.rs</files>
  <behavior>
    - Structurally plausible but semantically incomplete spend artifacts stay fail-closed before state mutation.
    - Any earlier stage4 file writes remain explicitly weaker than semantic acceptance.
  </behavior>
  <action>Use the exact task title above. Current blocker: no accepted tx-package or checkpoint-persistence lane remains for semantically incomplete spend artifacts, but some earlier stage4 file writes still happen and some negative branches are code-proven more strongly than test-proven. Gap closure path from the source table: keep Q18 focused on the exact missing public-input element in the regular spend statement. Tie stage4 persistence behavior to spend-verification fail-closed semantics, and add direct negative coverage where the code is stronger than the current tests.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 39 closes only when semantic acceptance is visibly stronger than structural plausibility and any weaker earlier writes are documented/tested as non-authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan-local ownership -> public exclusivity narrative | Wallet-local exclusivity can be overstated as public closure |
| stage4 structural persistence -> spend acceptance | Structurally plausible artifacts must not be mistaken for semantically accepted artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-37 | I | Post-scan exclusivity wording | mitigate | Keep exclusivity tied to receiver-secret plus `s_out` wallet-local gates |
| T-033-38 | I | Spend-boundary summary | mitigate | Freeze the delivered boundary around persisted public spend checks and exclude broader claims |
| T-033-39 | T | Structural-vs-semantic acceptance | mitigate | Add direct negative tests where stage4 structural persistence exists before semantic acceptance |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate, then repeated YOLO review until two consecutive clean runs are achieved.
</verification>

<success_criteria>
Canonical tasks 37-39 are executed in order, the spend boundary remains precisely scoped, and structurally plausible artifacts remain fail-closed before semantic acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-13-SUMMARY.md`
</output>