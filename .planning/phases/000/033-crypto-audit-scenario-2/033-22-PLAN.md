---
phase: 033-crypto-audit-scenario-2
plan: 22
type: execute
wave: 22
depends_on:
  - 033-21
files_modified:
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/spend_rules.rs
  - .planning/REQUIREMENTS.md
  - .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
autonomous: true
requirements:
  - PH32-SPEND
  - PH32-HONEST
must_haves:
  truths:
    - Canonical task 64 is executed as a dedicated high-severity plan.
    - The audit row is preserved verbatim, including its title/blocker pairing.
    - The remediation remains limited to nullifier-semantics closure or honest narrowing.
  artifacts:
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
    - crates/z00z_wallets/src/core/tx/spend_rules.rs
    - .planning/REQUIREMENTS.md
    - .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
  key_links:
    - Preserve the audit row exactly; do not silently rename or swap the title/body pairing.
    - The regular public spend contract remains the live seam used for this remediation row.
---

# Phase 033 Plan 22

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical task 64 as a dedicated high-severity plan while preserving the exact audit-row wording and remediation path.

Purpose: isolate the high-severity nullifier-semantics spend finding into its own plan without letting row-pairing ambiguity leak into the rest of the phase.
Output: a dedicated implementation-ready path for the task-64 audit row, preserved verbatim and tied to the live public spend contract.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-32FULL-AUDIT.md
@.planning/phases/033-crypto-audit-scenario-2/033-21-PLAN.md

Live code anchors:

- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
- `crates/z00z_wallets/src/core/tx/spend_rules.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 64: Checkpoint Proof Acceptance Is Compatibility-Payload Only</name>
  <files>crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/spend_rules.rs, .planning/REQUIREMENTS.md, .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md</files>
  <source>033-32FULL-AUDIT.md :: canonical row 64 with crossed title/body pairing preserved verbatim</source>
  <behavior>
    - The exact audit row is preserved verbatim, including its title.
    - The exact blocker/gap path from the audit row is also preserved verbatim and treated as the governing remediation input.
  </behavior>
  <action>Use the exact task title above with no wording changes. Preserve the task-64 audit row pairing exactly as written in `033-32FULL-AUDIT.md`; do not silently rename or swap it with task 65. Missing evidence or blocker from the task-64 audit row: the regular public spend contract verifies real proof/auth data but still carries no nullifier semantics. Gap closure path from the same audit row: extend the spend statement, persisted wire contract, and verifier to bind nullifier semantics, or narrow the requirement honestly. Build this plan directly on the spend-verification seam, spend rules, and the open PH32-SPEND requirement text.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 64 closes only when the row is preserved verbatim and the remediation path is isolated to nullifier-semantics binding or explicit honest narrowing of the spend requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-row wording -> remediation plan | Silent title/blocker swapping would corrupt the canonical task mapping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-64 | R | High-severity task mapping | mitigate | Preserve the row verbatim and bind remediation only to the stated nullifier-semantics gap |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate, then repeated YOLO review until two consecutive clean runs are achieved.
</verification>

<success_criteria>
Canonical task 64 is isolated into its own plan, the audit-row pairing is preserved verbatim, and the remediation scope stays limited to the live spend/nullifier gap.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-22-SUMMARY.md`
</output>