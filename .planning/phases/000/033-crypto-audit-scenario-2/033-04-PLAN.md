---
phase: 033-crypto-audit-scenario-2
plan: 4
type: execute
wave: 4
depends_on:
  - 033-03
files_modified:
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/core/tx/state_checkpoint.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_simulator/src/scenario_1/stage_12.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
autonomous: true
requirements:
  - PH32-SPEND
  - PH32-CHECKPOINT
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 10-12 are executed in order.
    - The current public spend boundary and the package-coupled checkpoint handoff are documented/tested as real but narrow.
    - Theft-window analysis stays distinct from proof continuity.
  artifacts:
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
    - crates/z00z_wallets/src/core/tx/state_checkpoint.rs
    - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
    - crates/z00z_simulator/src/scenario_1/stage_12.rs
  key_links:
    - Public spend-boundary wording must match the actual verifier contract.
    - Stage 11/12 checkpoint continuity must stay package-coupled and anti-substitution, not standalone-backend authority.
    - Theft-prevention claims must stay separate from withholding and public-proof closure.
---

# Phase 033 Plan 04

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 10-12 so the live public spend boundary, theft-window analysis, and handoff continuity are frozen as truthful current-stack claims.

Purpose: provide a precise baseline before the later open-gap and checkpoint backend plans attempt broader closure.
Output: evidence-backed boundary tests and wording guards for spend, theft, and checkpoint continuity.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-1.md
@.planning/phases/033-crypto-audit-scenario-2/033-03-PLAN.md

Live code anchors:
- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
- `crates/z00z_wallets/src/core/tx/state_checkpoint.rs`
- `crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10: What The Current Public Boundary Actually Proves</name>
  <files>crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_spend_witness_gate.rs, crates/z00z_simulator/tests/test_scenario1_stage_surface.rs</files>
  <behavior>
    - The current public spend boundary remains real and test-backed.
    - Repository wording stays narrower than full `PH32-SPEND` or universal trustless-verifier closure.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/spend_verification.rs`, `crates/z00z_wallets/tests/test_spend_witness_gate.rs`, and `crates/z00z_simulator/tests/test_scenario1_spend_gate.rs`. Current delivered boundary: the current public spend boundary is real and test-backed, but intentionally narrower than full `PH32-SPEND` or universal trustless-verifier closure. Missing invariant: codify the narrow boundary in tests/docs and only widen if new verifier semantics truly land. Forbidden overclaim: do not claim full PH32-SPEND closure from the current public verifier contract alone. Blocker text: current public spend boundary is real and test-backed, but intentionally narrower than full `PH32-SPEND` or universal trustless-verifier closure. Gap-closure path: distinguish delivered public-contract checks from still-open nullifier and end-to-end ownership claims. Required proof surfaces: public spend contract tests and wording guards.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 10 closes only when the repository explicitly proves the current public boundary without implying wider nullifier or trustless-verifier closure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 11: Theft Windows Before And After Publication</name>
  <files>crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_asset_ownership_security.rs</files>
  <behavior>
    - Local receiver-bound ownership and handoff tamper rejection stay separated from withholding/pre-publication risk.
    - The repository does not collapse layered theft defenses into a full public theorem.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/witness_gate.rs`, `crates/z00z_wallets/src/core/tx/spend_verification.rs`, and `crates/z00z_wallets/tests/test_asset_ownership_security.rs`. Current delivered boundary: local receiver-bound ownership and handoff tamper rejection are strong, but pre-publication withholding and full public anti-theft closure remain open. Missing invariant: preserve separate treatment of withholding, wallet-local anti-theft, and open public-verifier closure in tests/docs; widen only if a new public proof boundary actually lands. Forbidden overclaim: do not flatten theft prevention into one universal statement. Blocker text: local receiver-bound ownership and handoff tamper rejection are strong, but pre-publication withholding and full public anti-theft closure remain open. Gap-closure path: keep theft prevention separate from withholding and from the still-open universal public-verifier claim. Required proof surfaces: wallet tamper tests plus docs/test assertions that keep the risk categories separate.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 11 closes only when all artifacts keep withholding, wallet-local anti-theft, and public-verifier closure as distinct statements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12: Proof Continuity Across Handoff</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs, crates/z00z_simulator/src/scenario_1/stage_12.rs, crates/z00z_wallets/src/core/tx/state_checkpoint.rs</files>
  <behavior>
    - Accepted current-stack handoff preserves spend meaning through Stage 11 via exact proof/input/output continuity.
    - No artifact/document claims standalone checkpoint authorization beyond the package-coupled path.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs`, `crates/z00z_simulator/src/scenario_1/stage_12.rs`, and `crates/z00z_wallets/src/core/tx/state_checkpoint.rs`. Current delivered boundary: accepted current-stack path preserves spend meaning through Stage 11 via package revalidation and exact proof/input/output continuity; later exec artifacts stay package-coupled rather than standalone auth carriers. Missing invariant: keep tests/docs constrained to package-coupled continuity and refuse any standalone backend language until a real backend exists. Forbidden overclaim: do not describe Stage 11/12 artifacts as authoritative backend closure. Blocker text: accepted current-stack path preserves spend meaning through stage11 via package revalidation and exact proof/input/output continuity; later exec artifacts stay package-coupled rather than standalone auth carriers. Gap-closure path: keep scope on accepted package-coupled path; do not overstate standalone checkpoint authorization. Required proof surfaces: stage 6/12 continuity tests and wording guards.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 12 closes only when stage handoff artifacts are explicitly package-coupled, anti-substitution tested, and free of standalone-backend overclaim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public spend contract -> documentation/tests | Narrow verifier guarantees can be silently widened by narrative drift |
| stage6 package -> stage12 checkpoint publication | Package-coupled continuity must not be represented as standalone backend proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-10 | I | Public spend boundary | mitigate | Freeze boundary wording/tests to the current explicit verifier contract only |
| T-033-11 | R | Theft-window narrative | mitigate | Keep withholding and public anti-theft closure separate from wallet-local defenses |
| T-033-12 | E | Checkpoint handoff | mitigate | Preserve package-coupled continuity language until a real standalone backend is implemented |
</threat_model>

<verification>
Bootstrap first, then release-style cargo testing for Rust/test deltas, then repeat YOLO task-execution review until two consecutive clean results exist.
</verification>

<success_criteria>
Canonical tasks 10-12 are executed in order and every artifact now states exactly what the current public spend and checkpoint-handoff boundaries prove, no more and no less.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-04-SUMMARY.md`
</output>