---
phase: 033-crypto-audit-scenario-2
plan: 7
type: execute
wave: 7
depends_on:
  - 033-06
files_modified:
  - crates/z00z_wallets/src/core/tx/witness_gate.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_simulator/src/scenario_1/stage_2.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs
  - crates/z00z_simulator/tests/test_stage2_secret_artifacts.rs
autonomous: true
requirements:
  - PH32-SEM
  - PH32-SECRET
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 19-21 are executed in order.
    - Post-scan theft resistance stays compositional, and secret-export claims stay scoped to the default/debug split.
    - The debug lane remains explicit, gated, and private-path only.
  artifacts:
    - crates/z00z_wallets/src/core/tx/witness_gate.rs
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
    - crates/z00z_simulator/src/scenario_1/stage_2.rs
    - crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs
  key_links:
    - Post-scan ownership wording must not outrun the public spend boundary.
    - Default stage-2 outputs must stay free of plaintext wallet-secret artifacts.
    - `wallet_debug_dump` must remain the only debug-secret export lane.
---

# Phase 033 Plan 07

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 19-21 so theft-resistance, default secret silence, and the explicit debug lane remain truthful and test-backed.

Purpose: finish the first secret/ownership slice before moving into RNG, documentation-honesty, and late caution work.
Output: compositional anti-theft wording, preserved default-safe secret handling, and explicit debug-lane constraints.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-1.md
@.planning/phases/033-crypto-audit-scenario-2/033-06-PLAN.md

Live code anchors:

- `crates/z00z_wallets/src/core/tx/witness_gate.rs`
- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
- `crates/z00z_simulator/src/scenario_1/stage_2.rs`
- `crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 19: Post-Scan And Post-Spend Theft Resistance</name>
  <files>crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_asset_ownership_security.rs</files>
  <behavior>
    - Post-scan/post-spend theft resistance stays real but compositional.
    - The open trustless ownership/nullifier theorem remains explicitly open.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/witness_gate.rs`, `crates/z00z_wallets/src/core/tx/spend_verification.rs`, `crates/z00z_wallets/tests/test_asset_ownership_security.rs`, `crates/z00z_wallets/tests/test_scenario1_semantics.rs`, and `crates/z00z_simulator/tests/test_scenario1_spend_gate.rs`. Current delivered boundary: post-scan/post-spend theft resistance is real but layered, with receiver-secret-gated wallet ownership plus current-stack public spend and stage11 replay/tamper checks blocking theft on the accepted path, while the final trustless ownership/nullifier theorem remains open. Missing invariant: keep the compositional defense model synchronized across tests/docs, or add new public-proof machinery if the stronger theorem is pursued. Forbidden overclaim: do not collapse the layered defense into a finished universal trustless-verifier claim. Blocker text: the final trustless ownership/nullifier theorem remains open. Gap-closure path: describe the defense as compositional; do not collapse it into a finished universal trustless-verifier claim. Required proof surfaces: wallet ownership tests, public spend tests, and replay/tamper evidence.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 19 closes only when the repository states and tests post-scan/post-spend theft resistance as a compositional current-stack defense rather than a universal theorem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 20: Default Secret Silence</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_2.rs, crates/z00z_simulator/tests/test_stage2_secret_artifacts.rs, crates/z00z_simulator/tests/test_wallet_integration.rs</files>
  <behavior>
    - Default Scenario 1 runs emit no plaintext wallet-secret artifact.
    - Broader ordinary persistence/export surfaces are not falsely claimed as eliminated.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_simulator/src/scenario_1/stage_2.rs`, `crates/z00z_simulator/tests/test_stage2_secret_artifacts.rs`, and `crates/z00z_simulator/tests/test_wallet_integration.rs`. Current delivered boundary: the default public plaintext wallet-secret markdown artifact is gone, but normal wallet-state/export/encrypted backup surfaces still exist outside this narrower claim. Missing invariant: keep the closure claim narrow to the removed default plaintext artifact lane and add test/docs guards against broader wording drift. Forbidden overclaim: do not state that all ordinary secret-bearing persistence is eliminated. Blocker text: the reviewed Stage 2 files do not prove a single unique remaining ordinary secret-bearing persistence lane because normal wallet-state, export, and encrypted backup surfaces still exist. Gap-closure path: keep the closed claim narrow to no default public plaintext secret artifact. Required proof surfaces: default-lane absence tests and wording scans.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 20 closes only when default-lane plaintext-secret absence remains test-backed and no artifact generalizes it into total secret-persistence elimination.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21: Explicit Debug Lane Only</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs, crates/z00z_simulator/README.md, crates/z00z_simulator/src/config_accessors.rs</files>
  <behavior>
    - The remaining secret-export lane is explicitly feature-gated, private-path-only, permission-guarded, and debug-branded.
    - Default runs stay free of the debug export artifact.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs`, `crates/z00z_simulator/src/config_accessors.rs`, `crates/z00z_simulator/README.md`, `crates/z00z_simulator/tests/test_stage2_secret_artifacts.rs`, and `crates/z00z_simulator/tests/test_claim_persist.rs`. Current delivered boundary: the remaining secret-export lane is explicitly feature-gated, private-path-only, permission-guarded, debug-branded, and absent by default across Stage 2 and later debug helpers. Missing invariant: keep every surviving debug export under the same feature-gated/private-lane policy and tighten docs/tests if any unguarded path remains. Forbidden overclaim: do not generalize the debug-lane policy into unrelated wallet persistence behavior. Blocker text: keep the claim on the simulator debug-export surface; do not generalize it into unrelated wallet persistence behavior. Gap-closure path: preserve the simulator debug-export lane as the only explicit secret-export lane. Required proof surfaces: feature-gate tests and README/config wording checks.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 21 closes only when the simulator debug-export lane remains the sole explicit secret-export path and the default lane stays clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wallet-local ownership -> public anti-theft docs | Layered defenses can be overstated as universal closure |
| simulator default lane -> debug feature lane | Secret-export surfaces must stay segregated by feature and path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-19 | I | Post-scan theft narrative | mitigate | Keep the defense explicitly compositional and separate from the open universal theorem |
| T-033-20 | I | Default secret handling | mitigate | Keep the closure claim limited to the default plaintext artifact lane |
| T-033-21 | E | Debug export lane | mitigate | Preserve feature gating, private paths, and permission checks for every debug export |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate for Rust/test work, then repeat YOLO review until two consecutive clean passes exist.
</verification>

<success_criteria>
Canonical tasks 19-21 are executed in order, the simulator default lane stays free of plaintext secret artifacts, and the debug-secret surface remains explicitly gated and narrow.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-07-SUMMARY.md`
</output>