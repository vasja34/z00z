---
phase: 033-crypto-audit-scenario-2
plan: 20
type: execute
wave: 20
depends_on:
  - 033-19
files_modified:
  - crates/z00z_wallets/src/core/tx/state_checkpoint.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
  - crates/z00z_simulator/src/scenario_1/stage_12.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs
  - crates/z00z_simulator/src/rng_mode.rs
autonomous: true
requirements:
  - PH32-CHECKPOINT
  - PH32-SECRET
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 60-62 are executed in order.
    - Checkpoint integrity, secret lifecycle, and RNG/credential/config consolidation remain distinct consolidation passes.
    - Existing abstractions are reused rather than replaced with speculative new design.
  artifacts:
    - crates/z00z_wallets/src/core/tx/state_checkpoint.rs
    - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
    - crates/z00z_simulator/src/scenario_1/stage_12.rs
    - crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs
    - crates/z00z_simulator/src/rng_mode.rs
  key_links:
    - Checkpoint integrity already fail-closes more than the old placeholder thesis suggests.
    - Default plaintext removal stays preserved while debug-lane confinement and retention policy remain the unfinished work.
    - RNG/config abstractions already exist and need consistent application rather than reinvention.
---

# Phase 033 Plan 20

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 60-62 so the last general fix-set trio becomes an implementation-ready consolidation pass.

Purpose: finish the non-high-severity fix-set planning before the final three high-severity audit rows are isolated into dedicated plans.
Output: concrete consolidation work for checkpoint integrity, secret lifecycle, and RNG/config consistency.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-3.md
@.planning/phases/033-crypto-audit-scenario-2/033-19-PLAN.md

Live code anchors:

- `crates/z00z_wallets/src/core/tx/state_checkpoint.rs`
- `crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12.rs`
- `crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs`
- `crates/z00z_simulator/src/rng_mode.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 60: Checkpoint integrity fix set</name>
  <files>crates/z00z_wallets/src/core/tx/state_checkpoint.rs, crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs, crates/z00z_simulator/src/scenario_1/stage_12.rs</files>
  <behavior>
    - Existing fail-closed checkpoint integrity is preserved.
    - The fix set targets authoritative proof and spent backends instead of repeating placeholder-runtime language.
  </behavior>
  <action>Use the exact task title above. Current caution reason: live path already fail-closes more than the old placeholder thesis. Safe final reading from the source table: finish authoritative proof and spent backends instead of describing the runtime as fully placeholder-based. Build the fix set on checkpoint state, RedB rehydrate validation, and stage12 finalization seams only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 60 closes only when the checkpoint-integrity fix set is framed as finishing authoritative proof/spent backends over an already fail-closed runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 61: Secret lifecycle fix set</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs, crates/z00z_simulator/src/config_accessors.rs, crates/z00z_simulator/README.md</files>
  <behavior>
    - Default plaintext hardening remains preserved.
    - The remaining work is debug-lane confinement, wrapping, and retention policy.
  </behavior>
  <action>Use the exact task title above. Current caution reason: default plaintext path is already removed, but the debug lane remains sensitive. Safe final reading from the source table: keep default lane hardened and finish debug-lane confinement, wrapping, and retention policy. Tie the fix set to `crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs`, `crates/z00z_simulator/src/config_accessors.rs`, and `crates/z00z_simulator/README.md` only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 61 closes only when the secret-lifecycle fix set keeps default hardening intact and isolates the remaining work to debug-lane policy and handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 62: RNG, credential, and config fix set</name>
  <files>crates/z00z_simulator/src/rng_mode.rs, crates/z00z_simulator/src/config.rs, crates/z00z_simulator/src/config_accessors.rs</files>
  <behavior>
    - Existing abstractions are acknowledged as already present.
    - The work is a consistency/consolidation pass, not a brand-new design.
  </behavior>
  <action>Use the exact task title above exactly as written in the source table, including commas. Current caution reason: replacement abstractions already exist but are not applied consistently. Safe final reading from the source table: this is a consolidation pass over live abstractions, not a brand-new design. Build the fix set around `crates/z00z_simulator/src/rng_mode.rs`, `crates/z00z_simulator/src/config.rs`, and `crates/z00z_simulator/src/config_accessors.rs` only; do not introduce speculative abstractions.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 62 closes only when the RNG/credential/config fix set is explicitly defined as a consistency pass over the live abstractions already in the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing checkpoint/secret/RNG controls -> remediation plan | Existing strong controls can be mischaracterized as absent or fully complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-60 | I | Checkpoint-integrity remediation scope | mitigate | Frame the work as finishing authoritative proof/spent backends over an already fail-closed path |
| T-033-61 | I | Secret-lifecycle remediation scope | mitigate | Preserve default hardening and isolate remaining work to debug-lane handling |
| T-033-62 | I | RNG/config remediation scope | mitigate | Keep the work as a consolidation pass over existing abstractions |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate, then repeated YOLO review until two consecutive clean runs are achieved.
</verification>

<success_criteria>
Canonical tasks 60-62 are executed in order and the final non-high-severity fix sets remain grounded in existing live abstractions and current boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-20-SUMMARY.md`
</output>