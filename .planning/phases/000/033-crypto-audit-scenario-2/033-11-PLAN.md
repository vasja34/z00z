---
phase: 033-crypto-audit-scenario-2
plan: 11
type: execute
wave: 11
depends_on:
  - 033-10
files_modified:
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_wallets/src/core/tx/witness_gate.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
autonomous: true
requirements:
  - PH32-CLAIM-TRUST
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 31-33 are executed in order.
    - Distinct reject paths, the helper seam that keeps claim trust partial, and the narrower anti-theft rule stay separate.
    - The exact-title rule survives the canonical-slot-31 numbering anomaly.
  artifacts:
    - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
    - crates/z00z_simulator/src/claim_pkg_consumer.rs
    - crates/z00z_storage/src/assets/store_internal/store_query.rs
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
  key_links:
    - Canonical task 31 must keep the exact title `Distinct Claim Reject Paths` even though the source display number repeats 30.
    - Persisted claim continuity stays the only path to stronger trust closure.
    - The narrower anti-theft rule must remain wallet-local and receiver-secret-gated.
---

# Phase 033 Plan 11

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 31-33 so late claim reject-path, partial-trust seam, and narrower anti-theft wording stay aligned with the canonical slot mapping.

Purpose: finish the first claim-trust clarification pass before ownership/privacy tasks move back onto the request/card and leaf-binding surfaces.
Output: explicit slot-31 traceability, narrowed claim-trust seam wording, and preserved wallet-local anti-theft language.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-2.md
@.planning/phases/033-crypto-audit-scenario-2/033-10-PLAN.md

Live code anchors:

- `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`
- `crates/z00z_simulator/src/claim_pkg_consumer.rs`
- `crates/z00z_storage/src/assets/store_internal/store_query.rs`
- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 31: Distinct Claim Reject Paths</name>
  <files>crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/tests/test_claim_pkg_runtime.rs</files>
  <source>033-EXAM-QUESTIONS-AND-ANSWERS-2.md :: canonical slot 31 with repeated source display `TASKS = 30`</source>
  <behavior>
    - Canonical slot 31 keeps the exact title `Distinct Claim Reject Paths` despite the repeated source display number 30.
    - Reject-path distinctness remains category-level and seam-message-level, not one synthetic top-level label.
  </behavior>
  <action>Use the exact task title above and preserve canonical slot 31 traceability per D-08. Existing verified targets: `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`, `crates/z00z_wallets/src/core/tx/test_claim_tx.rs`, `crates/z00z_simulator/src/claim_pkg_consumer.rs`, and `crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs`. Current delivered boundary: distinctness is category-level and seam-message-level, not one unique top-level label per mutation. Missing invariant: keep this exact distinction in tests/docs and add stricter exact-message tests for stale-proof variants only if the current category-level evidence proves insufficient. Forbidden overclaim: do not invent a single global reject label if the live verifier exposes category-level distinctions instead. Blocker text: distinctness is category-level and seam-message-level, not one unique top-level label per mutation. Gap-closure path: keep the verifier fail-closed ordering and add stricter exact-message tests for stale-proof variants only when category-level evidence proves insufficient. Required proof surfaces: slot-31 traceability plus reject-message tests.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 31 closes only when canonical slot 31 is preserved and reject-path distinctness is stated/tested at the right granularity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 32: The Seam That Keeps Claim Trust Partial</name>
  <files>crates/z00z_storage/src/assets/store_internal/store_query.rs, .planning/REQUIREMENTS.md, .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md</files>
  <behavior>
    - The exact seam keeping claim trust partial remains the helper-owned continuity boundary.
    - Stronger claim-trust wording stays blocked until persisted continuity lands.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_storage/src/assets/store_internal/store_query.rs` (`claim_source_contract_for_item(...)`) and the open `PH32-CLAIM-TRUST` requirement in `.planning/REQUIREMENTS.md`. Current delivered boundary: persisted storage-backed claim continuity is still absent behind the helper seam. Missing invariant: keep the exact helper-owned seam named everywhere claim-trust partiality is discussed, or replace it with a persisted continuity implementation. Forbidden overclaim: do not describe the current seam as authoritative storage-backed continuity. Blocker text: persisted storage-backed claim continuity is still absent behind the helper seam. Gap-closure path: replace the helper-owned seam with persisted continuity, or formally narrow and re-approve the broader claim-trust requirement. Required proof surfaces: requirement/context traceability to the live helper seam.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 32 closes only when the precise helper seam keeping claim trust partial is explicitly named and no artifact suggests stronger continuity than the code proves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 33: Sender Knowledge And The Narrower Anti-Theft Rule</name>
  <files>crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_asset_ownership_security.rs</files>
  <behavior>
    - The honest anti-theft rule remains the receiver-secret-gated wallet-local rule.
    - `sender ignorance` language stays banned.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/witness_gate.rs`, `crates/z00z_wallets/src/core/tx/spend_verification.rs`, `crates/z00z_wallets/tests/test_asset_ownership_security.rs`, and `crates/z00z_wallets/tests/test_scenario1_semantics.rs`. Current delivered boundary: the anti-theft rule is wallet-local, not a public theorem. Missing invariant: keep `sender ignorance` language forbidden and preserve the receiver-secret gate as the narrower honest rule across code/docs/tests. Forbidden overclaim: do not restate the current anti-theft rule as public trustless closure. Blocker text: the anti-theft rule is wallet-local, not a public theorem. Gap-closure path: keep `sender ignorance` language forbidden and describe the receiver-secret gate as the narrower honest rule. Required proof surfaces: wording guards and wallet-local anti-theft tests.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 33 closes only when sender-ignorance wording is absent and the narrower wallet-local anti-theft rule is preserved everywhere it appears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim reject taxonomy -> user-facing conclusions | Numbering/label drift can blur fail-closed classes |
| helper continuity seam -> claim-trust status | Partial-trust seams must remain explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-31 | R | Canonical task 31 traceability | mitigate | Keep exact title plus slot-31 mapping in all execution/summaries |
| T-033-32 | I | Claim-trust partial seam | mitigate | Name the helper-owned continuity seam explicitly until persisted continuity lands |
| T-033-33 | I | Anti-theft wording | mitigate | Keep sender-ignorance forbidden and preserve the narrower wallet-local rule |
</threat_model>

<verification>
Bootstrap first, then release-style cargo testing for Rust/test changes, then repeat YOLO review until two consecutive clean runs exist.
</verification>

<success_criteria>
Canonical tasks 31-33 are executed in order, slot-31 traceability is preserved, claim-trust partiality is tied to the live helper seam, and the narrower anti-theft rule remains explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-11-SUMMARY.md`
</output>