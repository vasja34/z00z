---
phase: 033-crypto-audit-scenario-2
plan: 15
type: execute
wave: 15
depends_on:
  - 033-14
files_modified:
  - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
  - crates/z00z_simulator/src/scenario_1/stage_2.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs
  - crates/z00z_simulator/src/rng_mode.rs
autonomous: true
requirements:
  - PH32-CHECKPOINT
  - PH32-SECRET
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 43-46 are executed in order.
    - Replay closure, default secret-export discipline, deterministic randomness boundaries, and honest status language remain distinct.
    - Later documentation work inherits these narrow boundaries rather than restating them broadly.
  artifacts:
    - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
    - crates/z00z_storage/tests/test_redb_rehydrate.rs
    - crates/z00z_simulator/src/scenario_1/stage_2.rs
    - crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs
    - crates/z00z_simulator/src/rng_mode.rs
  key_links:
    - Replay/stale closure remains inside helper-owned claim, current-stack spend, and package-coupled checkpoint boundaries.
    - Default plaintext secret export stays closed without implying broader secret-bearing persistence removal.
    - Deterministic randomness docs stay scoped to audited stage-2 fixture rules unless broader unification lands.
---

# Phase 033 Plan 15

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 43-46 so replay, default secret export, deterministic randomness, and status-language honesty are frozen before the documentation/caution wave.

Purpose: complete the core evidence-boundary pass before the later fix-set and caution plans translate it into documentation policy.
Output: narrow replay/export/RNG/status language anchored to live code and active artifacts.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-2.md
@.planning/phases/033-crypto-audit-scenario-2/033-14-PLAN.md

Live code anchors:

- `crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs`
- `crates/z00z_storage/tests/test_redb_rehydrate.rs`
- `crates/z00z_simulator/src/scenario_1/stage_2.rs`
- `crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs`
- `crates/z00z_simulator/src/rng_mode.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 43: Replay And Stale-Artifact Closure</name>
  <files>crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs, crates/z00z_storage/tests/test_redb_rehydrate.rs, .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md</files>
  <behavior>
    - Concrete replay/stale classes remain closed.
    - The closure stays explicitly inside helper-owned claim, current-stack spend, and package-coupled checkpoint boundaries.
  </behavior>
  <action>Use the exact task title above. Current blocker: concrete replay/stale classes are closed, but only inside helper-owned claim, current-stack spend, and package-coupled checkpoint boundaries. Gap closure path from the source table: keep documentation narrow unless persisted claim continuity, spend nullifier semantics, and standalone checkpoint backend land. Strengthen RedB rehydrate replay tests where needed, and keep the boundary statement narrow in active artifacts.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 43 closes only when replay/stale closure is preserved and its scope remains explicitly boundary-limited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 44: Default Secret-Export Discipline</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_2.rs, crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs, crates/z00z_simulator/README.md</files>
  <behavior>
    - The default-safe result is scoped to the plaintext debug artifact lane.
    - Encrypted operational export and backup surfaces remain explicitly outside this narrower claim.
  </behavior>
  <action>Use the exact task title above. Current blocker: the default-safe result is proven for the plaintext debug artifact lane; encrypted operational export and backup surfaces still exist outside this narrower claim. Gap closure path from the source table: keep documentation scoped to non-default plaintext export closure unless broader secret-bearing persistence policy is separately audited. Preserve stage2/stage3_finalize behavior and narrow README/status wording accordingly.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 44 closes only when default secret-export discipline remains tied to the plaintext debug lane and excludes broader secret-bearing persistence claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 45: Deterministic Randomness Boundaries</name>
  <files>crates/z00z_simulator/src/rng_mode.rs, crates/z00z_simulator/src/config.rs, crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs</files>
  <behavior>
    - Stage 2 fixture randomness is clearly bounded.
    - Broader simulator-stage unification remains explicitly unfinished.
  </behavior>
  <action>Use the exact task title above. Current blocker: stage 2 fixture boundary is well-bounded, but the repository still lacks one unified randomness-selector rule across every simulator stage. Gap closure path from the source table: keep docs scoped to the audited stage-2 simulator fixture unless stage-3 and generic config semantics are unified. Align `rng_mode`, simulator config, and stage2 transport wording/tests to that exact boundary.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 45 closes only when deterministic randomness wording remains stage-2-scoped unless broader selector unification is actually implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 46: Honest Status Language Across Artifacts</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md</files>
  <behavior>
    - Anti-overclaim language remains explicit in the narrative/control artifacts.
    - Logs/manifests stay evidentiary limiters rather than semantic sources of truth.
  </behavior>
  <action>Use the exact task title above. Current blocker: anti-overclaim language is explicit and strong, but concentrated in closeout, verification, validation, and status-note artifacts rather than every log or manifest. Gap closure path from the source table: keep manifests/logs framed as evidentiary limiters and keep the semantic disclaimer burden on the narrative/control artifacts. Update roadmap/state/context wording so this policy is consistent and explicit.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 46 closes only when status language remains explicitly honest in the control artifacts and logs/manifests are not treated as semantic closure by themselves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay/rehydrate proof surfaces -> closure narrative | Closed replay classes can be overstated beyond their current scope |
| debug export / RNG fixture controls -> broader docs | Narrow operational controls can be generalized into unsupported repo-wide claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-43 | R | Replay/stale closure summary | mitigate | Keep replay closure explicitly bound to the current claim/spend/checkpoint boundaries |
| T-033-44 | I | Default secret-export wording | mitigate | Scope closure to the plaintext debug artifact lane only |
| T-033-45 | I | Deterministic RNG documentation | mitigate | Keep the selector story stage-2-scoped unless broader unification lands |
| T-033-46 | R | Status/control artifacts | mitigate | Keep semantic disclaimers in roadmap/state/context and demote logs/manifests to evidence only |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate, then repeated YOLO review until two consecutive clean runs are achieved.
</verification>

<success_criteria>
Canonical tasks 43-46 are executed in order, replay/export/RNG/status claims stay narrow, and the documentation wave inherits explicit anti-overclaim rules.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-15-SUMMARY.md`
</output>