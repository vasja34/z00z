---
phase: 033-crypto-audit-scenario-2
plan: 8
type: execute
wave: 8
depends_on:
  - 033-07
files_modified:
  - crates/z00z_utils/src/rng/traits.rs
  - crates/z00z_utils/src/rng/deterministic.rs
  - crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs
  - crates/z00z_simulator/src/rng_mode.rs
  - crates/z00z_wallets/tests/test_s5_closure_gate.rs
autonomous: true
requirements:
  - PH32-SEM
  - PH32-SECRET
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 22-24 are executed in order.
    - Deterministic RNG remains strong-but-bounded and never silently promoted to universal secure entropy.
    - Verification-discipline and whole-scheme summaries stay narrower than broad-suite or end-to-end closure claims.
  artifacts:
    - crates/z00z_utils/src/rng/traits.rs
    - crates/z00z_utils/src/rng/deterministic.rs
    - crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs
    - crates/z00z_simulator/src/rng_mode.rs
  key_links:
    - Simulator deterministic RNG adapters must stay visibly scoped to reproducibility/test flows.
    - Closeout/verification artifacts must stay honest about what was rerun and what remains broad-suite unsupported.
    - Whole-scheme security language must keep delivered/partial/not-proved buckets.
---

# Phase 033 Plan 08

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 22-24 so RNG boundaries, verification discipline, and the whole-scheme security answer stay explicitly bounded and honest.

Purpose: finish the repository-wide language/control slice before governance and late caution plans reuse it.
Output: bounded deterministic RNG semantics, explicit review/verification discipline, and a stable three-bucket security answer.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-1.md
@.planning/phases/033-crypto-audit-scenario-2/033-07-PLAN.md

Live code anchors:

- `crates/z00z_utils/src/rng/traits.rs`
- `crates/z00z_utils/src/rng/deterministic.rs`
- `crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs`
- `crates/z00z_simulator/src/rng_mode.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 22: Seeded RNG Stays Bounded</name>
  <files>crates/z00z_utils/src/rng/traits.rs, crates/z00z_utils/src/rng/deterministic.rs, crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs</files>
  <behavior>
    - Simulator/test deterministic RNG remains explicitly reproducibility-scoped.
    - `SeqSecureRngProvider` or adjacent adapters do not silently imply universal secure-entropy approval.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_utils/src/rng/traits.rs`, `crates/z00z_utils/src/rng/deterministic.rs`, `crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs`, and `crates/z00z_simulator/src/rng_mode.rs`. Current delivered boundary: simulator and utility layers strongly bound seeded reproducibility to simulator/test flows, but simulator-local deterministic `SeqSecureRngProvider` still presents through a secure-RNG trait shape. Missing invariant: keep the bound strong and simulator-scoped, make the `None == zero-seed` or mock-mode nuance explicit, and remove any language that turns this into impossible-by-construction universal safety. Forbidden overclaim: do not call the deterministic adapter universally secure for production entropy. Blocker text: the boundary depends on scoping and discipline rather than on a universal impossible-by-construction type rule. Gap-closure path: state the bound as strong and simulator-scoped, not absolute. Required proof surfaces: RNG-mode tests, trait/docs wording, and stage-2 transport coverage.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 22 closes only when deterministic RNG semantics remain explicitly bounded to approved reproducibility flows and no stronger safety wording survives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 23: Verification Discipline Versus Overclaim</name>
  <files>.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md, crates/z00z_wallets/tests/test_s5_sender_examples.rs, crates/z00z_wallets/tests/test_s5_closure_gate.rs</files>
  <behavior>
    - Closeout/reporting artifacts keep bootstrap-first targeted rerun truth explicit.
    - The repository does not drift back into a broad-suite PASS story unsupported by evidence.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md`, `crates/z00z_wallets/tests/test_s5_sender_examples.rs`, and `crates/z00z_wallets/tests/test_s5_closure_gate.rs`. Current delivered boundary: closeout artifacts record bootstrap-first targeted reruns and review-loop completion, but this is still mostly self-reported rather than re-proved from one complete raw-log bundle. Missing invariant: preserve the targeted-closeout contract, reference exactly what was rerun, and refuse any broader PASS narrative unless new evidence is produced. Forbidden overclaim: do not convert targeted sign-off into broad-suite green wording. Blocker text: the sign-off is mostly self-reported at the closeout-artifact layer rather than fully re-proved from a complete raw-log bundle in the same evidence set. Gap-closure path: treat the sign-off as an honest targeted-closeout contract, not as a claimed broad-suite PASS narrative. Required proof surfaces: narrative wording guards and any explicit log-bundle references added here.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 23 closes only when verification artifacts state exactly what was checked and stop short of unsupported broad-suite success claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 24: Is The Whole Scheme Really Secure</name>
  <files>.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md, .planning/ROADMAP.md, crates/z00z_wallets/tests/test_s5_closure_gate.rs</files>
  <behavior>
    - The whole-scheme answer remains a three-way classification: delivered, partial, and not proved/out of scope.
    - No artifact states blanket end-to-end closure without those caveats.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md`, `.planning/ROADMAP.md`, and `crates/z00z_wallets/tests/test_s5_closure_gate.rs`. Current delivered boundary: the honest answer is a three-way classification; the blanket “cryptographically closed end to end” sentence is overclaim if stated without caveats. Missing invariant: keep this three-bucket answer synchronized across roadmap/context/summary artifacts and enforce wording guards where the sentence could drift back into blanket closure language. Forbidden overclaim: do not state blanket end-to-end closure. Blocker text: the blanket “cryptographically closed end to end” sentence is overclaim if stated without caveats. Gap-closure path: preserve the three-bucket summary and keep not-proved/out-of-scope items out of any positive end-to-end closure claim. Required proof surfaces: wording guards and synchronized narrative artifacts.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 24 closes only when the repository answers the whole-scheme question with the same delivered/partial/not-proved structure everywhere it appears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deterministic RNG adapters -> production assumptions | Reproducibility helpers must not look like approved production entropy |
| closeout narratives -> reader expectations | Review/validation text can silently overclaim unexecuted coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-22 | I | Deterministic RNG APIs | mitigate | Keep reproducibility-only language and tests explicit in traits, providers, and simulator adapters |
| T-033-23 | R | Verification summaries | mitigate | Freeze targeted-closeout wording and reject unsupported broad-suite PASS phrasing |
| T-033-24 | I | Whole-scheme security answer | mitigate | Enforce the three-bucket delivered/partial/not-proved narrative across artifacts |
</threat_model>

<verification>
Bootstrap first, then release-style cargo testing for Rust/test work, then repeat YOLO review until two consecutive clean runs are reached.
</verification>

<success_criteria>
Canonical tasks 22-24 are executed in order, deterministic RNG remains explicitly bounded, and the repository keeps an honest three-bucket answer for the overall security story.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-08-SUMMARY.md`
</output>