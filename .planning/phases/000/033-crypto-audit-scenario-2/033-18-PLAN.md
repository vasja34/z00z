---
phase: 033-crypto-audit-scenario-2
plan: 18
type: execute
wave: 18
depends_on:
  - 033-17
files_modified:
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
  - crates/z00z_simulator/src/scenario_1/stage_12.rs
  - crates/z00z_core/src/assets/wire_pkg.rs
autonomous: true
requirements:
  - PH32-CLAIM-TRUST
  - PH32-CHECKPOINT
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 54-56 are executed in order.
    - Genesis membership continuity, checkpoint placeholder boundary, and receiver identity binding fixes stay distinct.
    - Existing stronger field-binding/building-block progress is preserved without overstating repo-wide closure.
  artifacts:
    - crates/z00z_storage/src/assets/store_internal/store_query.rs
    - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
    - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
    - crates/z00z_simulator/src/scenario_1/stage_12.rs
    - crates/z00z_core/src/assets/wire_pkg.rs
  key_links:
    - Zero-root shortcut rejection remains below authoritative persisted genesis membership.
    - Checkpoint runtime is beyond literal placeholders but still unfinished.
    - Identity binding building blocks must be lifted into uniform fail-closed policy.
---

# Phase 033 Plan 18

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 54-56 so the next caution/fix-set cluster preserves current strengths while isolating the remaining policy and authority gaps.

Purpose: prepare the transition from caution answers into concrete identity/authority fix-set planning.
Output: stable narrow answers for genesis membership, checkpoint placeholder boundaries, and receiver identity binding policy work.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-3.md
@.planning/phases/033-crypto-audit-scenario-2/033-17-PLAN.md

Live code anchors:

- `crates/z00z_storage/src/assets/store_internal/store_query.rs`
- `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`
- `crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12.rs`
- `crates/z00z_core/src/assets/wire_pkg.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 54: Genesis membership continuity</name>
  <files>crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md</files>
  <behavior>
    - Zero-root shortcuts remain rejected on the accepted path.
    - The repo still stops short of authoritative persisted genesis membership continuity.
  </behavior>
  <action>Use the exact task title above exactly as written in the source table, including lowercase `membership continuity`. Current caution reason: accepted path rejects zero-root shortcuts but still stops at helper-owned continuity. Safe final reading from the source table: source-root continuity is stronger than placeholder roots, but not yet authoritative persisted genesis membership. Keep the answer tied to the helper-owned continuity seam and the stronger-than-placeholder field binding already present.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 54 closes only when genesis membership continuity is described as stronger than placeholders but still below authoritative persisted membership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 55: Checkpoint placeholder boundary</name>
  <files>crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs, crates/z00z_simulator/src/scenario_1/stage_12.rs, .planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md</files>
  <behavior>
    - The live checkpoint path is beyond literal placeholders.
    - The boundary remains unfinished rather than unchanged-placeholder runtime.
  </behavior>
  <action>Use the exact task title above exactly as written in the source table, including lowercase `placeholder boundary`. Current caution reason: live path moved beyond literal placeholders, but the boundary is still incomplete. Safe final reading from the source table: the right finding is `unfinished boundary`, not `unchanged old placeholder runtime`. Tie this answer to stage12 and RedB rehydrate validation only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 55 closes only when checkpoint placeholder language is replaced by an explicit unfinished-boundary reading.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 56: Receiver identity binding fix set</name>
  <files>crates/z00z_core/src/assets/wire_pkg.rs, crates/z00z_wallets/src/services/wallet_service_actions_receive.rs, crates/z00z_wallets/src/core/tx/witness_gate.rs</files>
  <behavior>
    - Existing building blocks are acknowledged.
    - The missing step is repo-wide fail-closed policy, not basic mechanism absence.
  </behavior>
  <action>Use the exact task title above. Current caution reason: important building blocks already exist, but repo-wide fail-closed policy is not proven. Safe final reading from the source table: finish the policy layer that makes identity binding uniformly mandatory. Use asset-wire, receive-service, and witness-gate seams to define the fix-set without claiming the policy is already repository-wide.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 56 closes only when the fix set is framed as a uniform fail-closed policy completion pass over already-existing identity-binding building blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stronger-than-placeholder continuity -> authoritative membership claims | Existing strength can be overstated as final authority |
| existing identity-binding primitives -> repo-wide policy claims | Available building blocks can be mistaken for completed uniform policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-54 | I | Genesis membership summary | mitigate | Keep helper-owned continuity distinct from authoritative persisted membership |
| T-033-55 | I | Checkpoint placeholder narrative | mitigate | State unfinished-boundary truth and reject unchanged-placeholder wording |
| T-033-56 | I | Receiver identity binding fix scope | mitigate | Frame the work as policy completion over existing primitives |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate, then repeated YOLO review until two consecutive clean runs are achieved.
</verification>

<success_criteria>
Canonical tasks 54-56 are executed in order, the caution answers remain narrow, and the receiver-identity fix set is framed as policy completion rather than brand-new design.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-18-SUMMARY.md`
</output>