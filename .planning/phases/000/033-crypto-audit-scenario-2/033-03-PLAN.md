---
phase: 033-crypto-audit-scenario-2
plan: 3
type: execute
wave: 3
depends_on:
  - 033-02
files_modified:
  - crates/z00z_core/src/assets/wire_pkg.rs
  - crates/z00z_wallets/src/core/tx/witness_gate.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/tests/test_e2e_req_flow.rs
autonomous: true
requirements:
  - PH32-SEM
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 7-9 are executed in order with the exact source titles preserved.
    - `leaf_ad_id`, request/card routing, and end-to-end ownership stay distinct planning seams.
    - The mandatory carry-forward caution row attached to canonical task 9 survives into execution guidance.
  artifacts:
    - crates/z00z_core/src/assets/wire_pkg.rs
    - crates/z00z_wallets/src/core/tx/witness_gate.rs
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
    - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  key_links:
    - `leaf_ad_id` must remain the canonical decrypt/auth namespace.
    - Request-bound and card-bound routes must not be collapsed into one privacy claim.
    - Task 9 must carry the `Real theft-resistance boundary` caution row as a hard guardrail.
---

# Phase 033 Plan 03

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 7-9 so associated-data identity, privacy-route separation, and chain-wide ownership wording remain honest and test-backed.

Purpose: freeze the canonical decryption namespace and routing semantics before later spend/checkpoint/doc plans rely on them.
Output: exact-title execution for the `leaf_ad_id`, request/card, and ownership chain tasks plus the mandatory caution carry-forward.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-1.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-3.md
@.planning/phases/033-crypto-audit-scenario-2/033-02-PLAN.md

Live code anchors:
- `crates/z00z_core/src/assets/wire_pkg.rs`
- `crates/z00z_wallets/src/core/tx/witness_gate.rs`
- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
- `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 7: Associated-Data Identity Freeze</name>
  <files>crates/z00z_core/src/assets/wire_pkg.rs, crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/tests/test_spend_witness_gate.rs</files>
  <behavior>
    - Canonical owned/spendable flow rejects or loses ownership on `leaf_ad_id` drift.
    - The repository keeps the claim scoped to canonical flow unless broader artifact-surface proof is added.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_core/src/assets/wire_pkg.rs`, `crates/z00z_wallets/src/core/tx/witness_gate.rs`, and `crates/z00z_wallets/tests/test_spend_witness_gate.rs`. Current delivered boundary: canonical owned/spendable flow rejects or loses ownership on `leaf_ad_id` drift, but the stronger universal claim about every crafted artifact surface is not separately pinned. Missing invariant: either add broader artifact-surface tests or keep the claim explicitly scoped to the canonical flow. Forbidden overclaim: do not generalize the current evidence into a universal theorem. Blocker text: canonical owned/spendable flow rejects or loses ownership on `leaf_ad_id` drift, but the stronger universal claim about every crafted artifact surface is not separately pinned. Gap-closure path: keep the claim scoped to canonical flow or add broader artifact-surface tests. Required proof surfaces: witness-gate tamper tests and any additional artifact-surface drift tests if the broader claim is pursued.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 7 closes only when `leaf_ad_id` drift behavior is either broadened with proof or narrowed explicitly to the canonical flow without wording drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Request Privacy Versus Card Fallback</name>
  <files>crates/z00z_wallets/src/services/wallet_service_actions_receive.rs, crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/tests/test_e2e_req_flow.rs</files>
  <behavior>
    - Request-bound metadata remains the preferred accepted privacy lane.
    - Card-bound/base scan behavior stays a compatibility lane unless equivalence is separately proved.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs`, `crates/z00z_wallets/src/services/wallet_service.rs`, `crates/z00z_wallets/tests/test_e2e_req_flow.rs`, and `crates/z00z_wallets/tests/test_e2e_tag_auth.rs`. Current delivered boundary: request-bound metadata is the preferred privacy lane for accepted request/invoice flows, but card-bound or base build/scan paths remain live compatibility behavior rather than a removed stub. Missing invariant: either demote card-bound APIs/scan order to clearly non-default compatibility mode or add proof that the remaining card-only lane carries equivalent privacy guarantees. Forbidden overclaim: do not collapse request-bound and card-bound paths into one privacy statement. Blocker text: request-bound metadata is the preferred privacy lane for accepted request or invoice flows, but card-bound or base build and scan paths remain live compatibility behavior rather than a removed stub. Gap-closure path: demote card-bound behavior or prove equivalent guarantees. Required proof surfaces: request-flow tests in `test_e2e_req_flow.rs`, route-selection tests in `test_e2e_tag_auth.rs`, and documentation wording checks.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 8 closes only when route-selection behavior and repository wording clearly distinguish request-bound accepted privacy from card-bound compatibility behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: End-To-End Ownership Through The Chain</name>
  <files>crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_asset_ownership_security.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs</files>
  <behavior>
    - Bob-only ownership stays stable across accepted build/publish/scan/spend-preparation paths.
    - The mandatory carry-forward caution row `Real theft-resistance boundary` constrains wording and acceptance criteria for this task.
  </behavior>
  <action>Use the exact task title above and preserve the mandatory carry-forward source row attached to canonical task 9. Existing verified targets: `crates/z00z_wallets/src/core/tx/spend_verification.rs`, `crates/z00z_wallets/tests/test_asset_ownership_security.rs`, `crates/z00z_wallets/tests/test_scenario1_semantics.rs`, and `crates/z00z_simulator/tests/test_scenario1_spend_gate.rs`. Current delivered boundary: Bob-only ownership stays stable across accepted build/publish/scan and Bob-secret-gated spend preparation, but the validator verifies a public spend contract rather than independently re-proving the same two-factor ownership invariant end to end. Missing invariant: either deliver a validator-facing proof boundary that closes receiver-secret plus output-secret ownership, or formally narrow the chain-wide ownership claim to the current accepted spend-contract scope. Forbidden overclaim: do not convert the current story into sender ignorance of `s_out` or public trustless exclusivity. Blocker text: validator verifies a public spend contract rather than independently re-proving the same two-factor ownership invariant end to end. Gap-closure path: deliver a validator-facing ownership proof boundary or formally narrow the claim. Required proof surfaces: wallet ownership tests, spend-boundary tests, and explicit carry-forward caution evidence from `033-EXAM-QUESTIONS-AND-ANSWERS-3.md`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 9 closes only when the chain-wide ownership claim is either truly extended to a validator-facing proof boundary or explicitly narrowed to the honest spend-contract scope while the caution row remains preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| asset wire / witness input -> spend verifier | `leaf_ad_id` drift must not become a silent acceptance path |
| wallet request/card route selection -> privacy claims | Compatibility lanes must not be documented as equivalent privacy guarantees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-07 | T | `leaf_ad_id` boundary | mitigate | Add artifact-surface tests or narrow the claim to canonical flow only |
| T-033-08 | I | Request/card route docs | mitigate | Keep request-bound and card-bound behavior distinct in code/tests/docs |
| T-033-09 | E | Ownership claim language | mitigate | Preserve the task-9 caution row and forbid sender-ignorance/public-exclusivity overclaim |
</threat_model>

<verification>
Run bootstrap first, then the release-style cargo test gate for Rust/test work, then repeat YOLO review until two consecutive clean runs exist.
</verification>

<success_criteria>
Canonical tasks 7-9 are executed in sequence, `leaf_ad_id` stays the canonical decrypt boundary, request/card behavior remains distinct, and task 9 keeps the mandatory caution row as an active guardrail.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-03-SUMMARY.md`
</output>