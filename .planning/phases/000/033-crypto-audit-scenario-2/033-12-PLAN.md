---
phase: 033-crypto-audit-scenario-2
plan: 12
type: execute
wave: 12
depends_on:
  - 033-11
files_modified:
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/tests/test_scenario1_semantics.rs
  - crates/z00z_wallets/tests/test_e2e_req_flow.rs
autonomous: true
requirements:
  - PH32-SEM
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 34-36 are executed in order.
    - Receiver-secret ownership, decrypt-associated asset binding, and request/card routing stay distinct and narrow.
    - No plan collapses wallet-local ownership into public theorem or card compatibility into request-bound privacy equivalence.
  artifacts:
    - crates/z00z_wallets/src/core/tx/spend_verification.rs
    - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    - crates/z00z_wallets/src/services/wallet_service.rs
    - crates/z00z_wallets/tests/test_scenario1_semantics.rs
  key_links:
    - Receiver-held secrets remain the ownership gate unless a public proof boundary is actually added.
    - `leaf_ad_id` stays the canonical decrypt-associated asset binding surface.
    - Request-bound routing remains the preferred privacy path while card-bound behavior stays distinct.
---

# Phase 033 Plan 12

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 34-36 so ownership-gate wording, decrypt-associated binding, and request/card routing remain precise after the claim-trust clarification pass.

Purpose: complete the mid-phase wallet-local semantics cluster before moving to exclusivity/spend/checkpoint late-phase slices.
Output: explicit receiver-secret ownership boundaries, canonical `leaf_ad_id` decrypt binding, and distinct request/card route handling.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-2.md
@.planning/phases/033-crypto-audit-scenario-2/033-11-PLAN.md

Live code anchors:

- `crates/z00z_wallets/src/core/tx/spend_verification.rs`
- `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs`
- `crates/z00z_wallets/src/services/wallet_service.rs`
- `crates/z00z_wallets/tests/test_scenario1_semantics.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 34: Receiver-Held Secret As The Ownership Gate</name>
  <files>crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_asset_ownership_security.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs</files>
  <behavior>
    - The receiver-held secret remains the wallet-local ownership gate on accepted paths.
    - No artifact upgrades that gate into a public theorem unless a public verifier is added.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/spend_verification.rs`, `crates/z00z_wallets/tests/test_asset_ownership_security.rs`, and `crates/z00z_wallets/tests/test_scenario1_semantics.rs`. Current delivered boundary: the ownership gate is proven only for accepted wallet-local paths. Missing invariant: preserve wallet-local wording everywhere or add a new public verifier that proves the same invariant end to end. Forbidden overclaim: do not describe the receiver-held secret as already proven at the public-verifier boundary. Blocker text: the ownership gate is proven only for accepted wallet-local paths. Gap-closure path: preserve `wallet-local ownership rule` wording until a public verifier proves the same invariant end to end. Required proof surfaces: wallet ownership tests and wording guards.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 34 closes only when receiver-secret ownership stays explicitly wallet-local unless new public proof machinery really lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 35: Canonical Decrypt-Associated Asset Binding</name>
  <files>crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs</files>
  <behavior>
    - `leaf_ad_id` remains the canonical decrypt-associated asset binding surface across shipped wallet/scan/report/spend-witness paths.
    - Drift on accepted paths remains rejected or downgraded exactly as the evidence says.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/spend_verification.rs`, `crates/z00z_wallets/src/core/tx/witness_gate.rs`, `crates/z00z_wallets/tests/test_scenario1_semantics.rs`, and `crates/z00z_wallets/tests/test_spend_witness_gate.rs`. Current delivered boundary: the strongest proof is limited to shipped wallet/scan/report/spend-witness bridge paths. Missing invariant: keep `leaf_ad_id` as the canonical decrypt boundary across those accepted paths and reject any broader wording unless additional artifact-surface proof is added. Forbidden overclaim: do not widen the accepted-path proof into repository-wide total closure. Blocker text: the strongest proof is limited to shipped wallet, scan, report, and spend-witness bridge paths. Gap-closure path: keep `leaf_ad_id` as the canonical decrypt boundary and reject drift across those accepted paths. Required proof surfaces: Scenario 1 semantics tests and witness/spend tamper tests.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 35 closes only when `leaf_ad_id` remains explicitly canonical on the accepted decrypt/report/spend paths and no artifact widens that proof set without new evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 36: Request-Bound Route Versus Card-Bound Route</name>
  <files>crates/z00z_wallets/src/services/wallet_service_actions_receive.rs, crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/tests/test_e2e_req_flow.rs</files>
  <behavior>
    - Request-bound and card-bound tag contexts remain distinct.
    - Request-bound stays the preferred privacy route without claiming card-path equivalence.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs`, `crates/z00z_wallets/src/services/wallet_service.rs`, `crates/z00z_wallets/tests/test_e2e_req_flow.rs`, and `crates/z00z_wallets/tests/test_e2e_tag_auth.rs`. Current delivered boundary: the privacy consequence is implied from route-binding failure, not separately proved as its own theorem. Missing invariant: keep request-bound and card-bound tag contexts and approval rules distinct in code/tests/docs; only claim equivalence if separately proved. Forbidden overclaim: do not merge request-bound preference into card-path equivalence. Blocker text: the privacy consequence is implied from route-binding failure, not separately proved as its own theorem. Gap-closure path: keep request-bound and card-bound tag contexts and approval rules distinct. Required proof surfaces: request-flow tests in `test_e2e_req_flow.rs`, route-selection tests in `test_e2e_tag_auth.rs`, and wording guards around route selection.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 36 closes only when request-bound versus card-bound behavior remains explicitly distinct in logic, tests, and wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wallet-local ownership -> public statement | Receiver-secret ownership can be overstated beyond the accepted path |
| request/card route handling -> privacy narrative | Distinct route semantics can be collapsed into unsupported equivalence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-34 | I | Ownership gate wording | mitigate | Keep the gate wallet-local until a public verifier proves it |
| T-033-35 | T | `leaf_ad_id` binding | mitigate | Preserve accepted-path drift rejection and refuse broader proof claims without evidence |
| T-033-36 | I | Request/card privacy semantics | mitigate | Keep route contexts distinct and block unsupported equivalence claims |
</threat_model>

<verification>
Bootstrap first, then release-style cargo testing for Rust/test changes, then repeat YOLO review until two consecutive clean runs exist.
</verification>

<success_criteria>
Canonical tasks 34-36 are executed in order, ownership stays receiver-secret-gated and wallet-local, and request/card route semantics remain distinct and honest.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-12-SUMMARY.md`
</output>