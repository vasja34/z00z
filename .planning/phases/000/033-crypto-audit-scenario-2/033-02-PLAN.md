---
phase: 033-crypto-audit-scenario-2
plan: 2
type: execute
wave: 2
depends_on:
  - 033-01
files_modified:
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
  - crates/z00z_wallets/src/core/stealth/ecdh.rs
  - crates/z00z_wallets/src/core/tx/witness_gate.rs
  - crates/z00z_simulator/README.md
autonomous: true
requirements:
  - PH32-SEM
  - PH32-CLAIM-TRUST
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 4-6 are executed in order using the exact source titles.
    - Publish-bound continuity, anti-theft wording, and `s_out` semantics remain separate and narrow.
    - Legacy wording drift is removed before later ownership/routing plans depend on it.
  artifacts:
    - crates/z00z_simulator/src/claim_pkg_consumer.rs
    - crates/z00z_simulator/src/scenario_1/stage_3.rs
    - crates/z00z_wallets/src/core/stealth/ecdh.rs
    - crates/z00z_simulator/README.md
  key_links:
    - Bundle/load continuity must stay explicit at the claim-package seam.
    - `derive_s_out(...)` semantics must stay canonical across code and docs.
    - Anti-theft wording must stay narrower than a validator-level theorem.
---

# Phase 033 Plan 02

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 4-6 so publish continuity, anti-theft language, and output-secret semantics stop drifting across code, tests, and docs.

Purpose: preserve honest semantics before the later ownership and privacy-routing slices use these seams.
Output: live continuity checks, narrowed anti-theft wording, and a single canonical `s_out` contract.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-1.md
@.planning/phases/033-crypto-audit-scenario-2/033-01-PLAN.md

Live code anchors:
- `crates/z00z_simulator/src/claim_pkg_consumer.rs`
- `crates/z00z_simulator/src/scenario_1/stage_3.rs`
- `crates/z00z_wallets/src/core/stealth/ecdh.rs`
- `crates/z00z_wallets/src/core/tx/witness_gate.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4: Publish-Bound Claim Continuity</name>
  <files>crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/tests/test_claim_persist.rs, crates/z00z_core/src/assets/wire_pkg.rs</files>
  <behavior>
    - Bundle version and canonical discriminator fields are enforced on load.
    - Serde defaults cannot silently create a valid-looking claim-continuity package.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_simulator/src/claim_pkg_consumer.rs`, `crates/z00z_simulator/tests/test_claim_persist.rs`, and `crates/z00z_core/src/assets/wire_pkg.rs`. Current delivered boundary: bundle-wrapper version is not enforced on load, and some discriminator values may still arrive through serde defaults instead of explicit serialized fields. Missing invariant: enforce bundle version at load time and add serialization-shape tests that require explicit canonical discriminator fields. Forbidden overclaim: do not call continuity publish-bound while defaulted or implicit discriminator shapes are still accepted. Blocker text: bundle-wrapper version is not enforced on load, and some discriminator values may arrive through serde defaults instead of explicit serialized fields. Gap-closure path: enforce bundle version at load time and add serialization-shape tests in `test_claim_persist.rs` that require explicit canonical discriminator fields at the `wire_pkg.rs` parsing seam. Required proof surfaces: decode/load tests plus round-trip serialization-shape assertions.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 4 closes only when package load rejects wrong bundle version and implicit discriminator drift, with repository-backed serialization-shape tests proving the behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Sender Knowledge Versus Anti-Theft</name>
  <files>crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/tests/test_asset_ownership_security.rs</files>
  <behavior>
    - Anti-theft wording stays receiver-secret-gated and wallet-local unless a validator-level proof boundary is actually delivered.
    - Tests and docs stop implying sender ignorance or universal public anti-theft closure.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/witness_gate.rs`, `crates/z00z_wallets/src/core/tx/spend_verification.rs`, and `crates/z00z_wallets/tests/test_asset_ownership_security.rs`. Current delivered boundary: receiver-secret-gated two-factor ownership is proven only at wallet-local/spend-rule/witness-prep seams, not as a validator-level trustless invariant. Missing invariant: either deliver a validator-facing public proof boundary that proves the same exclusion end to end, or formally narrow the requirement/docs/tests to the wallet-local rule. Forbidden overclaim: do not restate the current wallet-local rule as sender ignorance or public theorem. Blocker text: receiver-secret-gated two-factor ownership is proven only at wallet-local / spend-rule / witness-prep seams, not as a finished validator-level trustless invariant. Gap-closure path: deliver a validator-level public proof boundary or formally narrow the claim. Required proof surfaces: wallet ownership tests, spend-verification rejection tests, and doc wording checks.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 5 closes only when the repository either proves a validator-facing anti-theft boundary or narrows every affected artifact to the honest wallet-local rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Canonical Output-Secret Semantics</name>
  <files>crates/z00z_wallets/src/core/stealth/ecdh.rs, crates/z00z_simulator/src/scenario_1/stage_3.rs, crates/z00z_simulator/README.md</files>
  <behavior>
    - `derive_s_out(k_dh, r_pub, serial_id)` remains the single live `s_out` contract.
    - Legacy `random32` wording disappears from docs and simulator artifacts.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/stealth/ecdh.rs` and `crates/z00z_simulator/src/scenario_1/stage_3.rs`. Current delivered boundary: the accepted live code is already frozen on deterministic `derive_s_out(k_dh, r_pub, serial_id)`, but legacy temp docs still describe a competing `random32` model. Missing invariant: remove or rewrite every remaining legacy `random32` artifact so the repository has one explicit output-secret model. Forbidden overclaim: do not describe the old competing model as still canonical anywhere. Blocker text: accepted live code is frozen on deterministic `derive_s_out(k_dh, r_pub, serial_id)`, but legacy temp docs still describe a competing `random32` model. Gap-closure path: remove the legacy `random32` artifacts so the repository has one explicit output-secret model everywhere. Required proof surfaces: source scan showing no surviving legacy wording plus targeted stealth-flow tests if code changes are needed.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 6 closes only when code, docs, and simulator notes all present one canonical `s_out` model and no competing legacy wording remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim package bytes -> continuity loader | Load-time defaults or version drift must not create accepted continuity |
| wallet-local ownership rule -> docs/tests | Overclaim can silently widen security guarantees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-04 | T | Claim continuity loader | mitigate | Require explicit bundle/discriminator fields and reject implicit serde defaults |
| T-033-05 | I | Anti-theft claim language | mitigate | Keep wording wallet-local unless a validator-facing proof boundary is actually implemented |
| T-033-06 | R | `s_out` documentation | mitigate | Remove the legacy `random32` story and freeze one canonical output-secret contract |
</threat_model>

<verification>
Bootstrap first, then the release-style cargo test gate for Rust/test work, then three-or-more YOLO review passes with two consecutive clean results.
</verification>

<success_criteria>
Canonical tasks 4-6 are executed in order, publish continuity rejects implicit/defaulted shapes, anti-theft language stays honest, and `s_out` semantics are singular across the repository.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-02-SUMMARY.md`
</output>