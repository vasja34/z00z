---
phase: 033-crypto-audit-scenario-2
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_simulator/tests/test_claim_pkg_runtime.rs
autonomous: true
requirements:
  - PH32-CLAIM-BIND
  - PH32-CLAIM-TRUST
  - PH32-HONEST
must_haves:
  truths:
    - Canonical tasks 1-3 are executed in order without renaming their titles or widening their security claims.
    - Full-claim tuple binding, authoritative claim-source continuity, and reject-class precision are treated as separate seams.
    - The plan produces fail-closed tests before any closure claim is allowed.
  artifacts:
    - crates/z00z_crypto/src/claim/v2.rs
    - crates/z00z_storage/src/assets/store_internal/store_query.rs
    - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
    - crates/z00z_simulator/src/claim_pkg_consumer.rs
  key_links:
    - ClaimStmtV2 signature bytes must align with the wallet claim verifier and simulator claim-package consumer.
    - Storage-owned claim source root/proof retrieval must replace or formally narrow helper-owned reconstruction.
    - Reject-class tests must observe the live verifier boundary, not a lower-level helper only.
---

# Phase 033 Plan 01

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Land the first claim-integrity slice for Phase 033 by executing canonical tasks 1-3 in order.

Purpose: make the tuple-binding, claim-source authority, and reject-class seams explicit and fail closed before later Phase 033 plans build on them.
Output: concrete code/test work on the live claim signature, storage continuity, and verifier rejection surfaces.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-1.md

Live code anchors:
- `crates/z00z_crypto/src/claim/v2.rs`
- `crates/z00z_storage/src/assets/store_internal/store_query.rs`
- `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`
- `crates/z00z_simulator/src/claim_pkg_consumer.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full Tuple Or Partial Story</name>
  <files>crates/z00z_crypto/src/claim/v2.rs, crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, crates/z00z_simulator/tests/test_claim_pkg_runtime.rs</files>
  <behavior>
    - Signed claim bytes keep the full authenticated tuple bound end to end.
    - Post-sign drift in `claim_source_asset_id` or `chain_id` is rejected at the end-to-end verifier boundary.
  </behavior>
  <action>Use the exact task title above per D-04/D-05. Existing verified targets: `crates/z00z_crypto/src/claim/v2.rs`, `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`, and `crates/z00z_wallets/src/core/tx/test_claim_tx.rs`. Current delivered boundary: the canonical signed tuple exists, but targeted end-to-end drift tests are still missing. Missing invariant: mutate `claim_source_asset_id` and post-sign `chain_id` after signing and assert the precise reject class at the live verifier boundary. Forbidden overclaim: do not state that the full tuple is fully proved until those negative tests pass. Blocker text: missing targeted negative tests for `claim_source_asset_id` and post-sign `chain_id` drift at the end-to-end verifier boundary. Gap-closure path: add explicit verifier tests that mutate those fields after signing and assert the reject class. Required proof surfaces: direct mutation tests in `test_claim_tx.rs` plus the live claim verifier path.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 1 closes only when the live verifier rejects both tuple-drift mutations with stable fail-closed evidence and the repo wording stays narrower than the proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authoritative Store Or Local Reconstruction</name>
  <files>crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/tests/test_claim_persist.rs</files>
  <behavior>
    - Accepted Scenario 1 claim paths source claim roots and proofs from storage-owned continuity.
    - Simulator-local one-item synthetic reconstruction is removed from the accepted path or formally narrowed out of the requirement claim.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_storage/src/assets/store_internal/store_query.rs` (`AssetStore::claim_source_contract_for_item(...)`, `claim_source_root`, `claim_source_proof`) and `crates/z00z_simulator/src/claim_pkg_consumer.rs`. Current delivered boundary: accepted flow still depends on synthetic one-item claim-membership reconstruction instead of persisted storage-backed continuity. Missing invariant: bind claim-source root/proof derivation to persisted membership state, or formally narrow `PH32-CLAIM-TRUST` in the requirements/docs if that architectural move cannot be completed here. Forbidden overclaim: do not describe helper-owned reconstruction as authoritative continuity. Blocker text: accepted flow still depends on synthetic one-item claim-membership reconstruction instead of persisted storage-backed continuity. Gap-closure path: move claim-source root/proof derivation onto persisted store-backed state or formally narrow `PH32-CLAIM-TRUST`. Required proof surfaces: persisted-state regression tests, stale/mismatched root rejection, and claim consumer acceptance tests.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 2 closes only when claim-source continuity is storage-owned on the accepted path or the broader requirement is explicitly narrowed with repository-backed proof and no residual helper-authority overclaim remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Precise Reject Semantics</name>
  <files>crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/tests/test_claim_pkg_runtime.rs</files>
  <behavior>
    - Root-version and proof-version claim failures stay distinguishable at the verifier boundary.
    - Stale-proof consumer expectations stop relying on multiple interchangeable fail-closed substrings.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs` and `crates/z00z_simulator/src/claim_pkg_consumer.rs`. Current delivered boundary: fail-closed branches exist, but dedicated end-to-end tests for `SourceRootVer` and `SourceProofVer` are missing and the stale-proof consumer test is too loose. Missing invariant: add explicit verifier tests for `SourceRootVer` and `SourceProofVer`, and stabilize stale-proof classifier expectations at the consumer seam. Forbidden overclaim: do not claim precise reject semantics while tests still allow broad substring matches. Blocker text: missing dedicated end-to-end tests for verifier-side root-version and proof-version branches; stale-proof consumer test allows multiple fail-closed substrings. Gap-closure path: add explicit verifier tests for `SourceRootVer` and `SourceProofVer`, and stabilize consumer stale-proof classifier expectations. Required proof surfaces: named reject-class tests at the verifier seam plus consumer stale-proof coverage.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 3 closes only when the live claim verifier exposes stable, separately tested reject branches for root/proof version drift and the consumer stale-proof classifier is deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator claim package -> wallet verifier | Untrusted claim-package bytes cross into acceptance logic |
| storage claim-source state -> verifier | Root/proof authority must not be synthesized or silently widened |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-01 | T | Claim signature tuple | mitigate | Add post-sign drift tests for asset-id and chain-id changes before acceptance |
| T-033-02 | I | claim_source_contract_for_item | mitigate | Move root/proof derivation to persisted storage-backed continuity or formally narrow the requirement |
| T-033-03 | R | Reject classification | mitigate | Pin exact verifier reject branches with dedicated tests and deterministic consumer assertions |
</threat_model>

<verification>
Run bootstrap first, then the release-style cargo test gate when Rust/test changes land, then run `/GSD-Review-Tasks-Execution` in YOLO mode until two consecutive clean passes are reached.
</verification>

<success_criteria>
Canonical tasks 1-3 are closed in sequence with exact-title traceability, storage-backed claim trust is either implemented or honestly narrowed, and reject semantics are explicitly fail-closed and test-stable.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-01-SUMMARY.md`
</output>