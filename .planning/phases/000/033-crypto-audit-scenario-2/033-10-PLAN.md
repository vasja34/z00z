---
phase: 033-crypto-audit-scenario-2
plan: 10
type: execute
wave: 10
depends_on:
  - 033-09
files_modified:
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_simulator/tests/test_claim_pkg_runtime.rs
autonomous: true
requirements:
  - PH32-CLAIM-BIND
  - PH32-CLAIM-TRUST
must_haves:
  truths:
    - Canonical tasks 28-30 are executed in order.
    - The full authenticated tuple, tuple-drift behavior, and self-consistency-vs-authority distinction remain separate claim seams.
    - Persisted continuity stays the only acceptable path to stronger claim-trust closure.
  artifacts:
    - crates/z00z_crypto/src/claim/v2.rs
    - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
    - crates/z00z_storage/src/assets/store_internal/store_query.rs
    - crates/z00z_simulator/src/claim_pkg_consumer.rs
  key_links:
    - Claim signature bytes must continue to bind the full authenticated tuple.
    - Drift tests must cover plausible package-shape mutations, not only happy-path fields.
    - Helper self-consistency must not be treated as persisted authority.
---

# Phase 033 Plan 10

<!-- markdownlint-disable MD022 MD032 MD033 MD047 MD055 MD056 MD060 -->

<objective>
Execute canonical tasks 28-30 so the claim tuple, drift behavior, and authority seam are re-checked after the earlier governance/closure gate.

Purpose: keep the Phase 033 claim core consistent before late claim-trust and caution/high-severity plans depend on it.
Output: strengthened tuple/drift tests and an explicit persisted-authority boundary for claim trust.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/033-crypto-audit-scenario-2/033-CONTEXT.md
@.planning/phases/033-crypto-audit-scenario-2/033-EXAM-QUESTIONS-AND-ANSWERS-2.md
@.planning/phases/033-crypto-audit-scenario-2/033-09-PLAN.md

Live code anchors:

- `crates/z00z_crypto/src/claim/v2.rs`
- `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`
- `crates/z00z_storage/src/assets/store_internal/store_query.rs`
- `crates/z00z_simulator/src/claim_pkg_consumer.rs`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 28: Full Authenticated Claim Tuple</name>
  <files>crates/z00z_crypto/src/claim/v2.rs, crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, crates/z00z_simulator/tests/test_claim_pkg_runtime.rs</files>
  <behavior>
    - The authority signature continues to bind the full authenticated claim tuple.
    - Direct signature-mutation coverage exists for any remaining untested tuple elements.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_crypto/src/claim/v2.rs` (`ClaimStmtV2`) and `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`. Current delivered boundary: the canonical byte-frame signature contract still covers the full tuple, but not every field has a dedicated direct signature-mutation test. Missing invariant: add or tighten direct mutation tests for any still-uncovered tuple field while preserving the canonical byte frame. Forbidden overclaim: do not mark the full tuple exhaustively tested if uncovered tuple fields remain. Blocker text: not every field has a dedicated direct signature-mutation test. Gap-closure path: the canonical byte-frame signature contract still covers the full tuple, but the tests must prove it. Required proof surfaces: direct mutation tests at the signature/verifier seam.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 28 closes only when any retained full-tuple claim is backed by explicit direct mutation coverage for the relevant fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 29: Tuple Drift Under Plausible Package Shape</name>
  <files>crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/tests/test_claim_pkg_runtime.rs, crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs</files>
  <behavior>
    - Plausible package-shape drift remains rejected even when recipient-binding and asset-path variants are mutated.
    - Coverage for recipient-binding drift matches root/proof drift rigor.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_simulator/src/claim_pkg_consumer.rs`, `crates/z00z_simulator/tests/test_claim_pkg_runtime.rs`, and `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`. Current delivered boundary: recipient-binding drift coverage is narrower than root/proof drift coverage. Missing invariant: add broader direct mutation tests for recipient-binding and asset-path variants that still look like plausible packages. Forbidden overclaim: do not describe tuple-drift coverage as uniform until recipient-binding variants are proven. Blocker text: recipient-binding drift coverage is narrower than root and proof drift coverage. Gap-closure path: add broader direct mutation tests for recipient-binding and asset-path variants. Required proof surfaces: plausible-package-shape drift tests across the live consumer/verifier seam.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 29 closes only when plausible package-shape drift is covered as strongly as the existing root/proof drift classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 30: Self-Consistency Versus Authority</name>
  <files>crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_simulator/src/claim_pkg_consumer.rs, .planning/REQUIREMENTS.md</files>
  <behavior>
    - Helper self-consistency remains distinct from persisted authoritative continuity.
    - Claim-trust closure is not widened until storage-backed continuity lands.
  </behavior>
  <action>Use the exact task title above. Existing verified targets: `crates/z00z_storage/src/assets/store_internal/store_query.rs` (`claim_source_contract_for_item(...)`) and `crates/z00z_simulator/src/claim_pkg_consumer.rs`. Current delivered boundary: persisted storage-backed continuity is still not proven. Missing invariant: either replace the helper-derived seam with persisted continuity or keep `PH32-CLAIM-TRUST` narrowed to the helper-owned boundary. Forbidden overclaim: do not call helper self-consistency authoritative claim continuity. Blocker text: persisted storage-backed continuity is still not proven. Gap-closure path: replace the helper-derived seam with persisted continuity or formally narrow the requirement. Required proof surfaces: requirement/docs linkage to the live helper seam and any new persisted-state tests if the stronger path is implemented.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>/GSD-Review-Tasks-Execution using /.github/prompts/gsd-review-tasks-execution.prompt.md in YOLO mode at least 3 times; stop only after at least 2 consecutive runs report no significant issues</automated>
  </verify>
  <done>Task 30 closes only when helper self-consistency is no longer mistaken for authority, either by implementation or honest narrowing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signed claim tuple -> claim-package consumer | Plausible package drift must not bypass tuple binding |
| helper reconstruction -> claim-trust status | Local self-consistency must not be mistaken for storage-backed authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-28 | T | Authenticated claim tuple | mitigate | Add direct mutation coverage for uncovered tuple fields |
| T-033-29 | T | Plausible package-shape drift | mitigate | Extend drift tests to recipient-binding and asset-path variants |
| T-033-30 | I | Claim authority seam | mitigate | Keep helper self-consistency and persisted continuity as separate states until storage-backed continuity lands |
</threat_model>

<verification>
Bootstrap first, then release-style cargo testing for Rust/test changes, then repeat YOLO review until two consecutive clean runs exist.
</verification>

<success_criteria>
Canonical tasks 28-30 are executed in order, tuple/drift coverage is explicit, and helper self-consistency is no longer described as authority.
</success_criteria>

<output>
After completion, create `.planning/phases/033-crypto-audit-scenario-2/033-10-SUMMARY.md`
</output>