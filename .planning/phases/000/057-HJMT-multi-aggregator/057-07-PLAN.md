---
phase: 057-HJMT-multi-aggregator
plan: 057-07
type: execute
wave: 7
depends_on:
  - 057-06
files_modified:
  - crates/z00z_runtime/aggregators/README.md
  - crates/z00z_runtime/aggregators/tests/test_live_guardrails.rs
  - crates/z00z_storage/tests/test_live_guardrails.rs
  - crates/z00z_simulator/tests/test_scenario_settlement.rs
  - .planning/phases/057-HJMT-multi-aggregator/057-CONTEXT.md
  - .planning/phases/057-HJMT-multi-aggregator/057-SOURCE-AUDIT.md
  - .planning/phases/057-HJMT-multi-aggregator/057-07-PLAN.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
requirements:
  - 057-G1
  - 057-G2
  - 057-G3
  - 057-G4
  - 057-G5
  - 057-G6
  - 057-G7
  - 057-G8
  - 057-G9
  - 057-G10
  - 057-G11
autonomous: true
must_haves:
  truths:
    - "The post-closeout continuation must keep one canonical `bind_publication_contract(...)` entrypoint for publication binding and reject any downstream digest fork."
    - "Validator, watcher, and simulator continuation evidence must reuse the live publication-binding story instead of growing a second authority lane."
    - "This renormalized `057-07` slice must sync the planning ledgers honestly; no parallel scratchpad or duplicate closeout path is allowed."
    - "If a closeout commit or release-flow commit is needed, use `/z00z-git-versioning` only after the evidence matrix is green."
  artifacts:
    - crates/z00z_runtime/aggregators/tests/test_live_guardrails.rs
    - crates/z00z_storage/tests/test_live_guardrails.rs
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "This slice is the renormalized closeout continuation that proves the earlier plans still collapse to one live publication-binding path and one final ledger story."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 057-07 Plan: Canonical Authority Guardrails And Renormalized Closeout Sync

<objective>
Close the explicit post-closeout continuation for Phase 057 on one canonical
publication-binding and evidence-authority path.

Purpose: prevent a false `057-06`-only closeout where the live validator or
watcher or simulator continuation work exists in the tree but the packet still
describes `057-07` as superseded. This slice renormalizes the packet honestly,
adds anti-drift source-shape guardrails for the shared `PublicationBinding`
path, and refreshes the planning ledgers only after evidence stays green.
Output: canonical authority guardrails, validated continuation evidence, and
refreshed Phase 057 planning state.
</objective>

<context>
@.planning/phases/057-HJMT-multi-aggregator/057-TODO.md
@.planning/phases/057-HJMT-multi-aggregator/057-CONTEXT.md
@.planning/phases/057-HJMT-multi-aggregator/057-SOURCE-AUDIT.md
@.planning/phases/057-HJMT-multi-aggregator/057-TEST-SPEC.md
@.planning/phases/057-HJMT-multi-aggregator/057-TESTS-TASKS.md
@docs/tech-papers/Z00Z-HJMT-Upgrade.md
@docs/tech-papers/Z00Z-HJMT-Fixture-Checklist.md
@crates/z00z_runtime/aggregators/README.md
@crates/z00z_runtime/aggregators/tests/test_live_guardrails.rs
@crates/z00z_storage/tests/test_live_guardrails.rs
@crates/z00z_simulator/tests/test_scenario_settlement.rs
</context>

<coverage_contract>
This plan owns the renormalized post-closeout continuation above the original
`057-06` matrix: anti-drift proof for one shared publication-binding path, one
validator or watcher or simulator digest story, and one honest Phase 057
ledger state. It rechecks the closeout-facing TODO sections `Required tests
and benchmark slices`, `Required artifacts`, and `Exit criteria` from the live
continuation tree before `STATE.md` and `ROADMAP.md` move.

Literal TODO bullets that must stay explicit here:
- the simulator continuation lane and its `val_flow.json` or `watch_flow.json`
  linkage back to the inherited Phase 056 packet
- no second publication digest lane, no second proof truth lane, and no second
  validator or watcher or simulator authority lane
- one canonical `bind_publication_contract(...)` path for all module
  structures and functions that need publication binding
- all exit-criteria rows that forbid detached publication artifacts or
  duplicate authority paths
</coverage_contract>

<tasks>
<task type="auto">
<name>Prove the shared publication-binding path and sync the renormalized closeout only after continuation evidence is complete</name>
<read_first>
- `057-CONTEXT.md` gate map and execution packet
- `057-TODO.md` sections `Required tests and benchmark slices`, `Required artifacts`, and `Exit criteria`
- `057-TEST-SPEC.md` and `057-TESTS-TASKS.md` rows for `057-SC-11`, `057-SC-12`, and `057-SC-13`
- `docs/tech-papers/Z00Z-HJMT-Fixture-Checklist.md` `Completion Contract` and `Release Gate`
- `057-03-SUMMARY.md` through `057-06-SUMMARY.md`
</read_first>
<files>
- `crates/z00z_runtime/aggregators/README.md`
- `crates/z00z_runtime/aggregators/tests/test_live_guardrails.rs`
- `crates/z00z_storage/tests/test_live_guardrails.rs`
- `crates/z00z_simulator/tests/test_scenario_settlement.rs`
- live validator, watcher, and simulator continuation sources and trace reports
- `.planning/phases/057-HJMT-multi-aggregator/057-CONTEXT.md`
- `.planning/phases/057-HJMT-multi-aggregator/057-SOURCE-AUDIT.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
</files>
<action>
Add source-shape guardrails that fail if publication binding starts from any
path other than the runtime-owned `bind_publication_contract(...)` helper or if
validators or watchers recompute local digest truth. Recheck the live
simulator continuation evidence for `val_flow.json` and `watch_flow.json`, then
renormalize `057-CONTEXT.md`, `057-SOURCE-AUDIT.md`, `ROADMAP.md`, and
`STATE.md` so `057-07` is the honest post-closeout continuation instead of a
stale superseded note. If a release-style or versioning commit is required
during closeout, route it through `/z00z-git-versioning`.
</action>
<acceptance_criteria>
- Runtime, validator, watcher, and simulator continuation surfaces prove one
  shared publication-binding story with no local digest fork.
- Anti-drift source-shape checks fail if a second publication or proof or
  downstream truth lane appears.
- The planning packet and status ledgers state `057-07` honestly as the live
  post-closeout continuation.
- Planning state updates happen only after the continuation evidence is green.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop, fix the regression, and rerun bootstrap before any broader validation.
2. Run `cargo test -p z00z_aggregators --release --features test-params-fast --test test_live_guardrails -- --nocapture`.
3. Run `cargo test -p z00z_storage --release --features test-params-fast --test test_live_guardrails -- --nocapture`.
4. Run `cargo test -p z00z_validators --release`.
5. Run `cargo test -p z00z_watchers --release`.
6. Run `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools --test test_scenario_settlement -- --nocapture`.
7. Run `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools --test test_scenario1_stage_surface -- --nocapture`.
8. Run `cargo test --release` once the closeout slice lands Rust or test changes.
9. Run `cargo doc --no-deps` if public APIs or normative docs changed during the packet.
10. Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and stop only after at least 2 consecutive runs show no significant issues.
11. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
Phase 057 can be marked complete through `057-07` only after this slice records
one evidence-backed renormalized closeout.
</done>
</task>
</tasks>

<threat_model>
- False supersession: a live continuation may exist in code while the packet
  still claims the plan is historical only.
- Evidence drift: validator or watcher or simulator continuation traces could
  diverge from the shared publication-binding contract.
- Guardrail drift: downstream code could recompute publication bindings or
  digest truth locally and create a silent second authority path.
</threat_model>

<output>
Create `.planning/phases/057-HJMT-multi-aggregator/057-07-SUMMARY.md` when
done.
</output>
