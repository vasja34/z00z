---
phase: 057-HJMT-multi-aggregator
plan: 057-03
type: execute
wave: 2
depends_on:
  - 057-01
files_modified:
  - config/hjmt_runtime/sim_5a7s/manifest.json
  - config/hjmt_runtime/sim_5a7s/aggregators/agg-0/aggregator-config.yaml
  - config/hjmt_runtime/sim_5a7s/aggregators/agg-1/aggregator-config.yaml
  - config/hjmt_runtime/sim_5a7s/aggregators/agg-2/aggregator-config.yaml
  - config/hjmt_runtime/sim_5a7s/aggregators/agg-3/aggregator-config.yaml
  - config/hjmt_runtime/sim_5a7s/aggregators/agg-4/aggregator-config.yaml
  - config/hjmt_runtime/sim_5a7s/planner/planner-config.yaml
  - config/hjmt_runtime/sim_5a7s/storage/storage-config.yaml
  - crates/z00z_runtime/aggregators/src/service.rs
  - crates/z00z_runtime/aggregators/src/recovery.rs
  - crates/z00z_rollup_node/src/runtime.rs
  - crates/z00z_rollup_node/src/status.rs
  - crates/z00z_simulator/src/config.rs
  - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
  - crates/z00z_simulator/src/scenario_1/runtime_observability.rs
  - crates/z00z_simulator/tests/test_hjmt_runtime_config.rs
  - crates/z00z_simulator/tests/test_scenario_settlement.rs
requirements:
  - 057-G5
autonomous: true
must_haves:
  truths:
    - "`SIM-5A7S-PUB` is the mandatory publication acceptance fixture, but the publication engine must remain topology-generic for any positive YAML-loaded transition that preserves lineage and route invariants."
    - "Publication starts from committed shard-local results and Phase 056 route/config/process lineage; it must not synthesize leaf fixtures or invent a second topology authority path."
    - "The new publication trace packet is evidence only and must link back to the Phase 056 trace packet rather than replacing it."
  artifacts:
    - config/hjmt_runtime/sim_5a7s/manifest.json
    - config/hjmt_runtime/sim_5a7s/planner/planner-config.yaml
    - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
    - crates/z00z_simulator/src/scenario_1/runtime_observability.rs
  key_links:
    - "This slice turns the live 5x7 runtime lineage into one honest public publication lane and proves that publication behavior is config-driven rather than hard-coded."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 057-03 Plan: SIM-5A7S-PUB Publication Integration And Trace Packet

<objective>
Turn the Phase 056 `SIM-5A7S` runtime packet into the executable
`SIM-5A7S-PUB` publication packet.

Purpose: prove that the live 5x7 runtime topology can publish seven canonical
public shard leaves and that publication behavior stays driven by YAML-loaded
topology and route state instead of hard-coded fixture logic.
Output: `SIM-5A7S-PUB`, publication digests and traces, one additional positive
topology transition without code edits, and replayable linkage back to Phase
056 runtime evidence.
</objective>

<context>
@.planning/phases/057-HJMT-multi-aggregator/057-TODO.md
@.planning/phases/057-HJMT-multi-aggregator/057-CONTEXT.md
@.planning/phases/057-HJMT-multi-aggregator/057-SOURCE-AUDIT.md
@.planning/phases/056-HJMT-storage- aggregator/056-06-SUMMARY.md
@.planning/phases/056-HJMT-storage- aggregator/056-07-SUMMARY.md
@docs/tech-papers/Z00Z-HJMT-Design.md
@config/hjmt_runtime/sim_5a7s/manifest.json
@crates/z00z_rollup_node/src/runtime.rs
@crates/z00z_simulator/src/scenario_1/scenario_config.yaml
@crates/z00z_simulator/src/scenario_1/runtime_observability.rs
</context>

<coverage_contract>
This plan owns gate `057-G5`, the `SIM-5A7S-PUB` profile table, the publication
artifact rows, and the acceptance-subset requirement that one additional
positive topology transition loaded from YAML must change publication behavior
without code edits while preserving lineage and route invariants.

Literal TODO bullets that must stay explicit here:
- seven active `ShardRootLeafV1` records in ascending `ShardId` order
- one canonical `CheckpointPublicationV1` with ordered leaf set, route
  binding, prior-root linkage, and monotonicity proof
- `5x7 -> 6x7` join exemplars remain mandatory, but the engine must accept any
  positive YAML-loaded topology transition that preserves the invariants
- `SIM-5A7S-PUB` and any join-or-leave rows must stay attached to the inherited
  runtime on/off matrix rather than a second publication-only matrix
- YAML surfaces must define old topology, new topology, route-generation
  boundaries, owner/standby roles, planned join mode, transfer target, and
  publication activation point
- evidence includes publication digest, leaf bytes, activation checkpoint,
  config digests, process ids, journal paths, owner/standby assignments,
  process exit/restart verdicts, validator and watcher verdicts, and trace
  linkage back to the Phase 056 route, journal, scope, config, and process
  packet
</coverage_contract>

<tasks>
<task type="auto">
<name>Build the real `SIM-5A7S-PUB` publication lane from the checked-in runtime home and trace it end to end</name>
<read_first>
- `057-CONTEXT.md` `Phase Boundary`, `Implementation Decisions`, `Canonical References`, `Mandatory global cross-read before any implementation`, and `Mandatory Phase 057 cross-read that every numbered plan must keep active`
- `057-TODO.md` sections `Canonical SIM-5A7S-PUB publication profile`, `Phase-owned acceptance subset`, `Workstream 5`, and `Required artifacts`
- `docs/tech-papers/Z00Z-HJMT-Upgrade.md` sections `6.3.3`, `6.3.4`, `6.5`, `6.6`, `6.7`, and `Appendix E.7`
- `057-SOURCE-AUDIT.md` live-path corrections and literal bullet ledger
- `docs/tech-papers/Z00Z-HJMT-Design.md` sections `8.3`, `9.2`, and `13`
- `config/hjmt_runtime/sim_5a7s/manifest.json`, `planner-config.yaml`, `storage-config.yaml`, and the checked-in aggregator YAML surfaces
</read_first>
<files>
- `config/hjmt_runtime/sim_5a7s/manifest.json`
- `config/hjmt_runtime/sim_5a7s/aggregators/agg-0/aggregator-config.yaml`
- `config/hjmt_runtime/sim_5a7s/aggregators/agg-1/aggregator-config.yaml`
- `config/hjmt_runtime/sim_5a7s/aggregators/agg-2/aggregator-config.yaml`
- `config/hjmt_runtime/sim_5a7s/aggregators/agg-3/aggregator-config.yaml`
- `config/hjmt_runtime/sim_5a7s/aggregators/agg-4/aggregator-config.yaml`
- proposed new `config/hjmt_runtime/sim_5a7s/aggregators/agg-5/aggregator-config.yaml`, or the direct-successor checked-in topology-home surface if join activation lands under a different YAML root
- `config/hjmt_runtime/sim_5a7s/planner/planner-config.yaml`
- `config/hjmt_runtime/sim_5a7s/storage/storage-config.yaml`
- `crates/z00z_runtime/aggregators/src/service.rs`
- `crates/z00z_runtime/aggregators/src/recovery.rs`
- `crates/z00z_rollup_node/src/runtime.rs`
- `crates/z00z_rollup_node/src/status.rs`
- `crates/z00z_simulator/src/config.rs`
- `crates/z00z_simulator/src/scenario_1/scenario_config.yaml`
- `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
- `crates/z00z_simulator/tests/test_hjmt_runtime_config.rs`
- `crates/z00z_simulator/tests/test_scenario_settlement.rs`
</files>
<action>
Wire the real publication lane onto the checked-in HJMT runtime home. Extend
the YAML surfaces, runtime status projection, and simulator observability so
`SIM-5A7S-PUB` publishes exactly seven active shard leaves in canonical order,
records one canonical publication digest, and emits the new Phase 057 evidence
surfaces from real runtime lineage rather than synthetic leaf fixtures. Prove
at least one additional positive YAML-loaded topology transition changes
publication, join, or transfer behavior without code edits while still
satisfying route-generation and lineage invariants. Keep any join-or-leave
rows attached to the inherited runtime on/off matrix, and make the evidence
packet carry config digests, process ids, journal paths, owner/standby
assignments, and process exit/restart verdicts together with the new
publication traces.
</action>
<acceptance_criteria>
- `SIM-5A7S-PUB` publishes exactly seven active shard leaves in canonical
  order from the live Phase 056 runtime packet.
- Publication digest, leaf bytes, and trace linkage are replayable from the
  checked-in runtime home.
- At least one positive non-`5x7` topology transition changes publication or
  join behavior through YAML alone and preserves the required invariants.
- The YAML-owned evidence explicitly records old topology, new topology,
  route-generation boundaries, owner/standby roles, planned join mode,
  transfer target, publication activation point, and the inherited runtime
  on/off matrix rows.
- No synthetic leaf authority or duplicate topology authority path is
  introduced.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate.
2. Run targeted publication-integration suites: `cargo test -p z00z_rollup_node --release --features test-params-fast --test test_hjmt_preflight -- --nocapture`, `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools --test test_hjmt_runtime_config -- --nocapture`, `cargo test -p z00z_simulator --release --features test-params-fast --features wallet_debug_tools --test test_scenario_settlement -- --nocapture`, and `cargo test -p z00z_aggregators --release --features test-params-fast --test test_hjmt_publish -- --nocapture` once the runtime-owned publication lane lands.
3. Run `cargo test --release`.
4. Run `cargo doc --no-deps` if this slice changes public node/runtime/simulator APIs or public docs.
5. Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and stop only after at least 2 consecutive runs show no significant issues.
6. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The repository has one honest `SIM-5A7S-PUB` publication lane and one
config-driven proof that publication behavior is not hard-coded to the single
acceptance fixture.
</done>
</task>
</tasks>

<threat_model>
- Hard-coded fixture logic could masquerade as a topology-generic publication
  engine.
- Synthetic leaf or digest generation could bypass the real Phase 056 lineage.
- Detached trace emission could make the simulator prove a publication packet
  that no real runtime home can reproduce.
</threat_model>

<output>
Create `.planning/phases/057-HJMT-multi-aggregator/057-03-SUMMARY.md` when
done.
</output>
