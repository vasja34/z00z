---
phase: 057-HJMT-multi-aggregator
plan: 057-06
type: execute
wave: 5
depends_on:
  - 057-01
  - 057-02
  - 057-03
  - 057-04
  - 057-05
files_modified:
  - crates/z00z_storage/benches/settlement_shard.rs
  - crates/z00z_storage/benches/settlement_hjmt.rs
  - crates/z00z_storage/benches/settlement_nested.rs
  - crates/z00z_storage/benches/settlement_proofs.rs
  - crates/z00z_storage/tests/test_bench_lanes.rs
  - crates/z00z_simulator/tests/test_scenario_settlement.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - .planning/ROADMAP.md
  - .planning/STATE.md
requirements:
  - 057-G1
  - 057-G2
  - 057-G3
  - 057-G4
  - 057-G5
  - 057-G6
  - 057-G7
  - 057-G8
  - 057-G9
  - 057-G10
  - 057-G11
autonomous: true
must_haves:
  truths:
    - "Phase 057 closes only on explicit evidence that every gate, fixture class, profile, trace, and scenario row is satisfied together."
    - "Benchmark and publication evidence must extend existing bench and simulator owner homes rather than creating a second harness."
    - "Planning state updates happen only after the full evidence matrix is green and review-clean."
  artifacts:
    - docs/tech-papers/Z00Z-HJMT-Fixture-Checklist.md
    - crates/z00z_storage/benches/settlement_shard.rs
    - crates/z00z_storage/benches/settlement_hjmt.rs
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "This slice is the closeout proof that the Phase 057 publication contract is real, replayable, and still aligned with the roadmap and state ledgers."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 057-06 Plan: Fixture, Benchmark, Validation, And Closeout Sync

<objective>
Close the phase only on one complete evidence matrix.

Purpose: prevent a false closeout where the publication contract exists in
pieces but the required fixtures, scenario rows, bench lanes, traces, or
planning-state updates are incomplete or stale.
Output: final gate-to-evidence matrix, benchmark and scenario corroboration,
fixture ownership closure, and planning-state sync.
</objective>

<context>
@.planning/phases/057-HJMT-multi-aggregator/057-TODO.md
@.planning/phases/057-HJMT-multi-aggregator/057-CONTEXT.md
@.planning/phases/057-HJMT-multi-aggregator/057-SOURCE-AUDIT.md
@.planning/phases/057-HJMT-multi-aggregator/057-TEST-SPEC.md
@.planning/phases/057-HJMT-multi-aggregator/057-TESTS-TASKS.md
@docs/tech-papers/Z00Z-HJMT-Upgrade.md
@docs/tech-papers/Z00Z-HJMT-Fixture-Checklist.md
@docs/tech-papers/Z00Z-HJMT-Design.md
@crates/z00z_storage/benches/settlement_shard.rs
@crates/z00z_storage/benches/settlement_hjmt.rs
@crates/z00z_simulator/tests/test_scenario_settlement.rs
@crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
</context>

<coverage_contract>
This plan owns the cross-cutting `057-TODO.md` sections `Required tests and
benchmark slices`, `Required execution profiles`, `Required scenario coverage`,
`Required artifacts`, `Fixture ownership`, and `Exit criteria`. It provides the
final closeout proof that gates `057-G1` through `057-G11` were actually
discharged by the earlier slices.

Literal TODO bullets that must stay explicit here:
- required tests `test_hjmt_root_generation.rs`,
  `test_hjmt_historical_proofs.rs`, `test_hjmt_join.rs`,
  `test_hjmt_publish.rs`, and `test_hjmt_migrate.rs`
- continuation of the Phase 056 multi-aggregator simulation lane
- publication and root-of-roots benchmark lane in an accepted Phase 057 bench
  home or direct successor, plus the integrated shard-parallel commit lane in
  `crates/z00z_storage/benches/settlement_shard.rs`
- execution profiles `SIM-SMALL`, `SIM-MEDIUM`, and `SIM-CACHE-EDGE`, with
  `SIM-BATCH-1000` reserved-only
- required scenario rows including pre-shard versus post-shard confusion
  reject, `6.8.1` and `6.8.2` replay, `6.8.3` reject, `6.8.4` join
  continuity, `6.8.5` mid-window failure containment, `5x7` checkpoint
  publication, route-generation transfer, crash recovery, first-seen scope
  before standby or carry-forward, and validator or watcher continuity
- publication traces `leaf_flow.json`, `proof_flow.json`, `pub_flow.json`,
  `val_flow.json`, and `watch_flow.json` that resolve back to
  `cfg_flow.json`, `tx_flow.json`, `route_flow.json`, `plan_flow.json`,
  `journal_flow.json`, `scope_flow.json`, `proc_flow.json`, and
  `recovery_flow.json`
- config and process evidence including config digests, process ids, journal
  paths, owner or standby assignments, and process exit or restart verdicts
- fixture ids `SRL-G-*`, `SRL-T-*`, `CPP-G-*`, `CPP-T-*`, and `FOV-G-002`
  through `FOV-G-004`
- upgrade `12.1` fixture classes `Current HJMT root set`,
  `5x7 checkpoint-publication evidence`, `join-as-standby evidence`,
  `join-as-owner evidence`, `shard-transfer evidence`, and
  `root-generation migration evidence`
- exit-criteria carryover for executable contracts, canonical seven-leaf
  publication, distinct join states, committed route generations, exact
  carry-forward, scenario/design synchronization, and no detached publication
  artifacts
</coverage_contract>

<tasks>
<task type="auto">
<name>Prove the full Phase 057 matrix and sync roadmap or state only after the packet is green</name>
<read_first>
- `057-CONTEXT.md` gate map, execution packet, `Mandatory global cross-read before any implementation`, and `Mandatory Phase 057 cross-read that every numbered plan must keep active`
- `057-TODO.md` sections `Required tests and benchmark slices`, `Required execution profiles`, `Required scenario coverage`, `Required artifacts`, `Fixture ownership`, and `Exit criteria`
- `docs/tech-papers/Z00Z-HJMT-Upgrade.md` sections `9.1`, `9.2`, `9.3`, `12`, `12.1`, `14`, and `14.1`
- `057-SOURCE-AUDIT.md` coverage summary, live-path corrections, and second doublecheck result
- `057-TEST-SPEC.md` and `057-TESTS-TASKS.md`
- `docs/tech-papers/Z00Z-HJMT-Fixture-Checklist.md` `Completion Contract` and `Release Gate`
- `docs/tech-papers/Z00Z-HJMT-Design.md` sections `9.2`, `13`, and `13.3`
- all earlier `057-0X-SUMMARY.md` outputs
</read_first>
<files>
- `crates/z00z_storage/benches/settlement_shard.rs`
- `crates/z00z_storage/benches/settlement_hjmt.rs`
- `crates/z00z_storage/benches/settlement_nested.rs`
- `crates/z00z_storage/benches/settlement_proofs.rs`
- `crates/z00z_storage/tests/test_bench_lanes.rs`
- `crates/z00z_simulator/tests/test_scenario_settlement.rs`
- `crates/z00z_simulator/tests/test_scenario1_stage_surface.rs`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- phase-owned manifests, traces, and fixture reports produced by earlier plans
</files>
<action>
Close the phase on one explicit proof bundle: all required fixture classes, all
required publication traces, the simulator continuation lanes, the required
profiles `SIM-SMALL`, `SIM-MEDIUM`, and `SIM-CACHE-EDGE`, the reserved-only
non-claim status of `SIM-BATCH-1000`, the publication or root-of-roots
benchmark lanes in the existing bench homes or their direct successors, and the
required config or process evidence rows. Prove that no required artifact is
stale, missing, cross-linked, or detached from Phase 056 route, journal, scope,
or process lineage, including the inherited lineage packet `cfg_flow.json`,
`tx_flow.json`, `route_flow.json`, `plan_flow.json`, `journal_flow.json`,
`scope_flow.json`, `proc_flow.json`, and `recovery_flow.json`, or backed by a
second benchmark harness. Keep the
co-owned runtime on/off matrix
join-or-leave rows attached to the same evidence packet rather than a second
matrix. Refresh `ROADMAP.md` and `STATE.md` only after the full matrix is
green and review-clean. This slice closes Phase 057 evidence only; it must not
claim final readiness or release verdicts that Phase 058 still owns.
</action>
<acceptance_criteria>
- Every gate `057-G1` through `057-G11` is explicitly closed with evidence.
- All TODO-required fixtures, traces, profiles, and scenario rows are mapped to
  passing evidence.
- The final bundle includes config digests, process ids, journal paths,
  owner/standby assignments, process exit/restart verdicts, and the inherited
  runtime on/off matrix join-or-leave rows where TODO marks them co-owned.
- The final bundle proves every publication trace resolves back to the exact
  Phase 056 lineage packet `cfg_flow.json`, `tx_flow.json`, `route_flow.json`,
  `plan_flow.json`, `journal_flow.json`, `scope_flow.json`, `proc_flow.json`,
  and `recovery_flow.json`.
- `ShardRootLeafV1` and `CheckpointPublicationV1` remain executable contracts,
  not merely named types.
- `SIM-5A7S-PUB` exists and proves seven active shard leaves in canonical
  order.
- Join-as-standby and join-as-owner remain distinct and evidence-backed, route
  migration uses committed route generations and activation checkpoints, and
  carry-forward preserves unchanged shard-leaf bytes exactly.
- Scenario code and `scenario_design.yaml` stay synchronized.
- No publication artifact is detached from Phase 056 route, journal, scope, or
  process lineage.
- Bench and validation work extends the current owner homes only, including the
  four current storage bench anchors or their direct successors, with
  `SIM-BATCH-1000` left explicitly reserved-only.
- Planning state is updated only after the evidence matrix is complete, and the
  closeout does not overclaim Phase 058 readiness or release status.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate.
2. Run the targeted storage, aggregator, validator, watcher, node, and simulator suites created by earlier plans, plus `cargo test -p z00z_storage --release --features test-params-fast --test test_bench_lanes -- --nocapture`.
3. Run the required bench compile and execution lanes in `crates/z00z_storage/benches/settlement_shard.rs` and the Phase 057 publication bench home or direct successor, then run `cargo test --release`.
4. Run `cargo doc --no-deps` if public APIs or docs changed during the closeout packet.
5. Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and stop only after at least 2 consecutive runs show no significant issues.
6. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
Phase 057 can be marked complete only after this slice records one full
evidence-backed closeout.
</done>
</task>
</tasks>

<threat_model>
- False closure: one or more earlier slices may pass while the full publication
  matrix is still incomplete.
- Evidence drift: traces, fixtures, or benchmark outputs could be stale or
  detached from the final runtime lineage.
- Harness drift: benchmark or validation work could move into a second home and
  stop proving the live owner seam.
</threat_model>

<output>
Create `.planning/phases/057-HJMT-multi-aggregator/057-06-SUMMARY.md` when
done.
</output>
