---
status: testing
phase: 057-HJMT-multi-aggregator
source:
  - 057-01-SUMMARY.md
  - 057-02-SUMMARY.md
  - 057-03-SUMMARY.md
  - 057-04-SUMMARY.md
  - 057-05-SUMMARY.md
  - 057-06-SUMMARY.md
  - 057-07-SUMMARY.md
started: 2026-06-14T08:53:50Z
updated: 2026-06-14T08:53:50Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Canonical Publication Contracts
expected: |
  Running the root-generation and publication contract checks should show one
  canonical `ShardRootLeafV1` story and one canonical
  `CheckpointPublicationV1` story. Pre-shard versus post-shard confusion,
  stale route digests, reordered shards, duplicate shard rows, wrong prior
  roots, and monotonicity violations should all reject, and the checked
  manifests should still match live bytes.
awaiting: user response

## Tests

### 1. Canonical Publication Contracts
expected: Running the root-generation and publication contract checks should show one canonical `ShardRootLeafV1` story and one canonical `CheckpointPublicationV1` story. Pre-shard versus post-shard confusion, stale route digests, reordered shards, duplicate shard rows, wrong prior roots, and monotonicity violations should all reject, and the checked manifests should still match live bytes.
result: [pending]

### 2. Layered Public Proof Compatibility
expected: Replaying the public proof examples should verify only when both layers are valid: the shard-local proof and the matching public shard-leaf inclusion. Older proofs should still verify against their original public root after later lawful publications, while cross-shard or wrong-lineage counterexamples fail closed.
result: [pending]

### 3. YAML-Driven `SIM-5A7S-PUB` Publication Lane
expected: The canonical `SIM-5A7S-PUB` scenario should publish seven active shard leaves in canonical order from the inherited Phase 056 runtime packet, and at least one additional non-`5x7` topology change should come purely from YAML with different observed behavior, not from hard-coded logic.
result: [pending]

### 4. Trace Pack And Design Sync
expected: The simulator evidence packet should emit `leaf_flow.json`, `proof_flow.json`, `pub_flow.json`, `val_flow.json`, and `watch_flow.json` that all resolve back to the same Phase 056 lineage packet, and `scenario_design.yaml` should stay in sync with the executed stage graph.
result: [pending]

### 5. Lawful Join And Transfer Transitions
expected: Standby join should mirror lineage without creating new public authority, owner activation should happen only after route generation `N+1` plus one activation checkpoint, and transfer to either a remaining or a new aggregator should stay generation-bound.
result: [pending]

### 6. Carry-Forward, Crash Recovery, And Scope Continuity
expected: Under mid-window failure, unchanged carried-forward shard leaves should stay byte-identical, crash recovery should expose only one lawful publication outcome, and first-seen `definition_id` or `serial_id` births should surface only through touched shard roots, shard leaves, and checkpoint digests.
result: [pending]

### 7. Downstream Digest Sameness
expected: Validators, watchers, and simulator traces should all expose the same publication digest and the same binding or verdict story, including honest `draft_only` or `incomplete` publication states with no watcher-local or validator-local reinterpretation.
result: [pending]

### 8. Bench Homes, Profiles, And Single Authority Path
expected: The accepted storage bench homes should contain the Phase 057 root-of-roots publication lanes and shard-parallel lanes, `SIM-SMALL`, `SIM-MEDIUM`, and `SIM-CACHE-EDGE` should remain live, `SIM-BATCH-1000` should remain reserved-only, and guardrails should fail if any second publication-binding, proof, validator, watcher, or simulator authority lane appears.
result: [pending]

## Summary

total: 8
passed: 0
issues: 0
pending: 8
skipped: 0
blocked: 0

## Gaps

<!-- YAML format for plan-phase --gaps consumption -->
