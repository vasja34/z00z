---
phase: 061-Wallet-Refactoring
plan: 061-10
type: execute
wave: 9
depends_on:
  - 061-02
  - 061-03
  - 061-04
  - 061-05
  - 061-06
  - 061-07
  - 061-08
  - 061-09
files_modified:
  - crates/z00z_wallets/src/domains/definitions.rs
  - crates/z00z_wallets/src/domains/hashing.rs
  - crates/z00z_wallets/src/domains/test_definitions.rs
  - crates/z00z_wallets/src/domains/test_hashing.rs
  - crates/z00z_wallets/src/services/wallet_paths.rs
  - crates/z00z_wallets/src/services/test_wallet_paths_suite.rs
  - crates/z00z_wallets/src/egui_views/tab_registry.rs
  - crates/z00z_wallets/src/egui_views/wallet_tab_staking.rs
  - crates/z00z_wallets/tests/test_rename_guards.rs
  - crates/z00z_storage/tests/test_live_guardrails.rs
  - .planning/phases/061-Wallet-Refactoring/061-CONTEXT.md
requirements:
  - 061-D02
  - 061-D06
  - 061-D08
  - 061-D09
  - 061-D14
autonomous: true
must_haves:
  truths:
    - "This is the final cleanup and proof slice; it closes remaining leaf debt and validates the whole tree."
    - "Domains and egui assets are real source anchors; cleanup must move them or prove intentional ownership."
    - "`app_settings_tab_2.rs` is deleted only after live-reference proof."
    - "The final tree audit must prove no Rust file remains deeper than `src/<domain>/file.rs`."
  artifacts:
    - crates/z00z_wallets/src/domains/mod.rs
    - crates/z00z_wallets/src/egui_views/mod.rs
    - .planning/phases/061-Wallet-Refactoring/061-TODO.md
  key_links:
    - "This slice turns the whole refactor packet from plausible to proven."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 061-10 Plan: Domains, Egui, Remaining Cleanup, And Final Tree Closeout

<objective>
Close the remaining domains or egui leaf debt and prove the final one-level
wallet tree contract across the whole crate.
</objective>

<context>
@.planning/phases/061-Wallet-Refactoring/061-TODO.md
@.planning/phases/061-Wallet-Refactoring/061-CONTEXT.md
@crates/z00z_wallets/src/domains/mod.rs
@crates/z00z_wallets/src/domains/test_definitions.rs
@crates/z00z_wallets/docs/domains_snapshot.txt
@crates/z00z_wallets/src/egui_views/mod.rs
@crates/z00z_wallets/src/egui_views/wallet_tab_staking.rs
@crates/z00z_wallets/src/egui_views/ui_config.rs
@crates/z00z_wallets/src/egui_views/ui_theme.rs
@crates/z00z_wallets/docs/egui_views.tar.gz
@crates/z00z_wallets/src/services/test_wallet_paths_suite.rs
</context>

<coverage_contract>
This slice owns the final cleanup and proof obligations from decisions `D-02`,
`D-06`, `D-08`, `D-09`, and `D-14`. It closes remaining domains and egui debt,
retires last stale placeholders, and proves the final tree contract with
phase-wide validation.
</coverage_contract>

<tasks>
<task type="auto">
<name>Finish remaining cleanup and prove the final one-level tree contract</name>
<read_first>
- `.planning/phases/061-Wallet-Refactoring/061-TODO.md` rows for `domains`, `egui_views`, delete candidates, and verification plan
- `.planning/phases/061-Wallet-Refactoring/061-CONTEXT.md`
- `crates/z00z_wallets/src/domains/test_definitions.rs`
- `crates/z00z_wallets/docs/domains_snapshot.txt`
- `crates/z00z_wallets/src/egui_views/ui_config.rs`
- `crates/z00z_wallets/src/egui_views/ui_theme.rs`
- `crates/z00z_wallets/src/services/test_wallet_paths_suite.rs`
</read_first>
<files>
- files under `crates/z00z_wallets/src/domains/`
- files under `crates/z00z_wallets/src/egui_views/`
- remaining non-Rust assets still under `crates/z00z_wallets/src/`
- `crates/z00z_wallets/docs/domains_snapshot.txt`
- `crates/z00z_wallets/docs/egui_views.tar.gz`
- `.planning/phases/061-Wallet-Refactoring/061-TODO.md`
</files>
<action>
Finish the remaining domains and egui cleanup, including the
`wallet_tab_stacking.rs -> wallet_tab_staking.rs` typo correction, intentional
retirement of `app_settings_tab_2.rs` when live references are gone, movement of
the domain snapshot and egui theme assets out of `src/` into
`crates/z00z_wallets/docs/`, including the final live
`crates/z00z_wallets/docs/egui_views.tar.gz` bundle, and any
last TODO cleanup notes required to reflect the shipped tree honestly. Then run
the phase-wide closeout audit: verify no Rust file remains deeper than
`src/<domain>/file.rs`, no duplicate-target or stale-name debt remains, and the
wallet crate still passes the planned broad validation gates from `VER-002`
through `VER-010`.
</action>
<acceptance_criteria>
- Remaining domains and egui Rust files follow the one-level tree rule.
- `wallet_tab_stacking.rs` is corrected without leaving split name debt.
- `app_settings_tab_2.rs` is removed only after reference-backed proof.
- Final tree-wide audits prove no remaining multi-level Rust subtree under
  `crates/z00z_wallets/src`.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run the slice-specific checks: `cargo fmt --all --check`, `cargo check --release -p z00z_wallets --all-targets --all-features`, `cargo test --release -p z00z_wallets --all-targets --all-features`, `cargo test --release`, `find crates/z00z_wallets/src -mindepth 3 -type f -name '*.rs'`, `rg -n "#\\[path *=|^(pub\\s+)?mod " crates/z00z_wallets/src -g "*.rs"`, `rg -No "#\\[path *= *\\\"[^\\\"]+\\\"\\]" crates/z00z_wallets/src -g "*.rs" | sed -E 's/.*\\\"([^\\\"]+)\\\".*/\\1/' | sort | uniq -cd`, `rg -n "adapters::rpc|db::redb_wallet_store|wallet_service::" crates/z00z_wallets/src -g "*.rs"`, `rg -n "db::redb_wallet_crypto|crate::db::redb_wallet_crypto|pub mod redb_wallet_crypto" crates/z00z_wallets/src -g "*.rs"`, `rg -n "z00z\\.crypto\\.redb_wallet_crypto|RedbWallet.*Domain|Z00ZRedbWalletAadIdDomain" crates/z00z_wallets/src -g "*.rs"`, `rg -n "asset_rpc_|tx_rpc_" crates/z00z_wallets/src/rpc -g "*.rs"`, `rg -n "claim_helpers|claim_tx_helpers" crates/z00z_wallets/src/tx -g "*.rs"`, and `rg -n "wallet_tab_stacking|app_settings_tab_2" crates/z00z_wallets/src crates/z00z_wallets/tests`.
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
4. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The wallet crate satisfies the final one-level source-tree contract, and the
Phase 061 rename packet is ready for summary-backed closeout.
</done>
</task>
</tasks>

<threat_model>
- The phase can appear complete while hidden multi-level Rust files or orphaned
  non-Rust assets still remain under `src/`.
- Final typo or placeholder cleanup can leave split names behind if grep-based
  proof is skipped.
- Without the final tree-wide audit, earlier slice success can mask residual
  nested layout debt.
</threat_model>

<verification>
This slice is valid only if the final tree audit proves the wallet crate now
matches the one-level structural rule end to end.
</verification>

<success_criteria>
- No Rust file remains deeper than `src/<domain>/file.rs`.
- Remaining names and placeholders are cleaned up with proof.
- Broad wallet validation still passes after the whole packet.
</success_criteria>

<output>
Create `.planning/phases/061-Wallet-Refactoring/061-10-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `.planning/phases/061-Wallet-Refactoring/061-10-SUMMARY.md`
- Final proof that the wallet source tree is one-level end to end
- Final cleanup of remaining domains, egui, and placeholder debt
