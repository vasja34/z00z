---
phase: 061-Wallet-Refactoring
plan: 061-07
type: execute
wave: 6
depends_on:
  - 061-01
  - 061-05
  - 061-06
files_modified:
  - crates/z00z_wallets/src/receiver/mod.rs
  - crates/z00z_wallets/src/persistence/mod.rs
  - crates/z00z_wallets/src/security/password.rs
  - crates/z00z_wallets/src/security/vault_secret_store_impl.rs
  - crates/z00z_wallets/src/persistence/receipts.rs
  - crates/z00z_wallets/src/persistence/scans.rs
  - crates/z00z_wallets/bin/gen_password_bloom.rs
  - crates/z00z_wallets/config/security/password_denylist.bloom
  - crates/z00z_wallets/config/security/common-passwords.txt
requirements:
  - 061-D02
  - 061-D03
  - 061-D06
  - 061-D08
  - 061-D09
  - 061-D14
autonomous: true
must_haves:
  truths:
    - "Receiver and persistence flattening happen only after services and RPC hubs are stable."
    - "Receipt and scan duplicate files are removed only after live-reference proof."
    - "Security datasets move out of `src/` together with the runtime loaders and the generator binary."
    - "Local facades remain readable and one-level after the move."
  artifacts:
    - crates/z00z_wallets/src/receiver/mod.rs
    - crates/z00z_wallets/src/persistence/mod.rs
    - crates/z00z_wallets/src/security/password.rs
    - crates/z00z_wallets/bin/gen_password_bloom.rs
  key_links:
    - "This slice owns local facade cleanup and duplicate storage retirement after the hub waves settle."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 061-07 Plan: Receiver, Persistence, And Security Vault Flattening

<objective>
Flatten the receiver, persistence, and security-vault local trees, and retire
duplicate receipt or scan storage shims only after live-reference proof.
</objective>

<context>
@.planning/phases/061-Wallet-Refactoring/061-TODO.md
@.planning/phases/061-Wallet-Refactoring/061-CONTEXT.md
@crates/z00z_wallets/src/receiver/mod.rs
@crates/z00z_wallets/src/persistence/mod.rs
@crates/z00z_wallets/src/security/password.rs
@crates/z00z_wallets/bin/gen_password_bloom.rs
@crates/z00z_wallets/src/services/wallet_service.rs
</context>

<coverage_contract>
This slice owns receiver, persistence, and security-vault flattening from
decisions `D-02`, `D-03`, `D-06`, `D-08`, `D-09`, and `D-14`. It must close
duplicate receipt and scan file debt only when live references prove the old
files are no longer authoritative.
</coverage_contract>

<tasks>
<task type="auto">
<name>Flatten receiver and persistence trees, then retire duplicate storage shims with proof</name>
<read_first>
- `.planning/phases/061-Wallet-Refactoring/061-TODO.md` rows for `receiver/**/*`, `persistence/**/*`, and security files
- `.planning/phases/061-Wallet-Refactoring/061-CONTEXT.md`
- `crates/z00z_wallets/src/receiver/mod.rs`
- `crates/z00z_wallets/src/persistence/mod.rs`
- `crates/z00z_wallets/src/security/password.rs`
- `crates/z00z_wallets/bin/gen_password_bloom.rs`
</read_first>
<files>
- files under `crates/z00z_wallets/src/receiver/`
- files under `crates/z00z_wallets/src/persistence/`
- `crates/z00z_wallets/src/security/password.rs`
- `crates/z00z_wallets/src/security/vault.rs`
- `crates/z00z_wallets/src/security/vault_*.rs`
- `crates/z00z_wallets/config/security/*`
- `crates/z00z_wallets/bin/gen_password_bloom.rs`
</files>
<action>
Flatten the local receiver and persistence subtrees to one-level domain files,
including the `claim_own` -> `stealth_ownership_check`, `nfc_utils` ->
`nfc_ndef`, receipt, scan, and asset-storage lanes. Update the corresponding
local facades and service or RPC consumers, then remove duplicate receipt or
scan storage shims only after explicit grep-backed proof that no live module
declarations or tests still depend on them. Move the password denylist text
and Bloom artifact out of `src/` into one canonical non-`src/` home together
with the runtime loader and the generator binary so security data no longer
violates the phase tree rule.
</action>
<acceptance_criteria>
- Receiver and persistence Rust files are one-level within their domains.
- Duplicate receipt and scan shims are removed only after reference-backed
  proof.
- Security password assets no longer live under `src/`, and both runtime and
  generator code use the new canonical location.
- Local receiver and persistence facades stay readable and compile without
  nested layout assumptions.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run the slice-specific checks: `cargo check --release -p z00z_wallets --all-targets --all-features`, `rg -n "claim_own|nfc_utils|src/security/password_denylist|src/security/common-passwords|src/security/vault/|src/receiver/(card|manager|ownership|request|scan)/|src/persistence/(assets|receipts|scans|tx)/" crates/z00z_wallets/src crates/z00z_wallets/tests crates/z00z_wallets/bin crates/z00z_wallets`, and `find crates/z00z_wallets/src \\( -type f -path '*/receiver/*/*' -o -type f -path '*/persistence/*/*/*' \\)`. Then run `cargo test --release` when this slice changes Rust or test files.
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
4. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
Receiver, persistence, and security-vault files are flat, duplicate storage
shims are retired with proof, and security datasets no longer live under `src/`.
</done>
</task>
</tasks>

<threat_model>
- Duplicate storage files can silently preserve a second truth if removal is not
  reference-backed.
- Security asset moves can break password validation at runtime if generator and
  loader paths diverge.
- Receiver path churn can leak into service or RPC flows if local facades are
  not updated atomically.
</threat_model>

<verification>
This slice is valid only if local receiver and persistence facades are flat and
no duplicate storage or security-asset authority remains under `src/`.
</verification>

<success_criteria>
- Receiver and persistence trees are one-level.
- Duplicate storage shims are retired with evidence.
- Security assets move out of `src/` without runtime drift.
</success_criteria>

<output>
Create `.planning/phases/061-Wallet-Refactoring/061-07-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `.planning/phases/061-Wallet-Refactoring/061-07-SUMMARY.md`
- Flat receiver and persistence domain layouts
- One canonical non-`src/` home for the password denylist assets
