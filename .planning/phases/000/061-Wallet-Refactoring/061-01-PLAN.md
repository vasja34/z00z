---
phase: 061-Wallet-Refactoring
plan: 061-01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/061-Wallet-Refactoring/061-TODO.md
  - crates/z00z_wallets/src/services/app/test_app_service_suite.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/test_mod.rs
  - crates/z00z_wallets/tests/test_common/test_mod.rs
requirements:
  - 061-D01
  - 061-D06
  - 061-D07
  - 061-D09
  - 061-D13
  - 061-D14
autonomous: true
must_haves:
  truths:
    - "`061-TODO.md` remains the canonical Phase 061 authority; this slice reconciles live drift instead of replacing it."
    - "No bulk tree move starts before stale rows, delete candidates, and anchor-sensitive tests are frozen against the live workspace."
    - "Non-Rust relocation work is routed to owning waves; this slice freezes the inventory and reference contract only."
    - "Delete candidates are not removed until references are proven absent or replaced."
  artifacts:
    - .planning/phases/061-Wallet-Refactoring/061-TODO.md
    - .planning/phases/061-Wallet-Refactoring/061-CONTEXT.md
    - crates/z00z_wallets/src/services/app/test_app_service_suite.rs
    - crates/z00z_wallets/src/db/redb_wallet_store/test_mod.rs
  key_links:
    - "This is the gate before all structural rename waves."
    - "It owns stale-plan drift, anchor inventory, and delete-candidate truth."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 061-01 Plan: Preflight Audit, Anchor Freeze, And Drift Reconciliation

<objective>
Freeze the live-tree truth before any broad rename wave lands. The output is an
explicit contract for stale TODO rows, delete candidates, include or path
anchors, and non-Rust artifact ownership.
</objective>

<context>
@.planning/phases/061-Wallet-Refactoring/061-TODO.md
@.planning/phases/061-Wallet-Refactoring/061-CONTEXT.md
@crates/z00z_wallets/🔐-разбор-WLT.md
@crates/z00z_wallets/🔐-разбор-кошелька-Z00Z.md
@crates/z00z_wallets/src/services/app/test_app_service_suite.rs
@crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs
@crates/z00z_wallets/src/db/redb_wallet_store/test_mod.rs
@crates/z00z_wallets/src/db/redb/schema/redb-schema.yaml
@docs/tech-papers/TODO-Wallet-idea.md
@crates/z00z_core/src/assets/assets_config.yaml
@crates/z00z_wallets/src/domains/definitions/test_mod.rs
@crates/z00z_wallets/src/domains/domains_snapshot.txt
@crates/z00z_wallets/src/security/password.rs
@crates/z00z_wallets/tests/test_common/test_mod.rs
@crates/z00z_wallets/tests/test_common/test_range_proof_env.inc
@crates/z00z_wallets/tests/test_common/test_rpc_logger.inc
@crates/z00z_wallets/tests/test_common/test_wallet_env.inc
@crates/z00z_wallets/tests/test_common/test_wallet_env_lock.inc
</context>

<coverage_contract>
This slice owns the live-truth preflight contract from decisions `D-01`,
`D-06`, `D-07`, `D-09`, `D-13`, and `D-14`. It must reconcile stale service
rows, classify delete candidates, and freeze source-anchor-sensitive sites
before any mass file move begins. It also owns the TODO baseline inventory
compare from `VER-001` and the entry recheck of `CHECK-001` through
`CHECK-010`.
</coverage_contract>

<tasks>
<task type="auto">
<name>Audit stale rows, delete candidates, and source-anchor-sensitive paths</name>
<read_first>
- `.planning/phases/061-Wallet-Refactoring/061-TODO.md` sections `Constraints`, `Implementation Notes`, and `Verification Plan`
- `.planning/phases/061-Wallet-Refactoring/061-CONTEXT.md`
- `crates/z00z_wallets/src/services/app/test_app_service_suite.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store/test_mod.rs`
- `crates/z00z_wallets/src/db/redb/schema/redb-schema.yaml`
- `docs/tech-papers/TODO-Wallet-idea.md`
- `crates/z00z_core/src/assets/assets_config.yaml`
- `crates/z00z_wallets/src/domains/definitions/test_mod.rs`
- `crates/z00z_wallets/src/domains/domains_snapshot.txt`
- `crates/z00z_wallets/src/security/password.rs`
- `crates/z00z_wallets/tests/test_common/test_mod.rs`
- `crates/z00z_wallets/tests/test_common/test_range_proof_env.inc`
- `crates/z00z_wallets/tests/test_common/test_rpc_logger.inc`
- `crates/z00z_wallets/tests/test_common/test_wallet_env.inc`
- `crates/z00z_wallets/tests/test_common/test_wallet_env_lock.inc`
</read_first>
<files>
- `.planning/phases/061-Wallet-Refactoring/061-TODO.md`
- `crates/z00z_wallets/src/services/app/test_app_service_suite.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store/test_mod.rs`
- `crates/z00z_wallets/src/db/redb/schema/redb-schema.yaml`
- `docs/tech-papers/TODO-Wallet-idea.md`
- `crates/z00z_core/src/assets/assets_config.yaml`
- `crates/z00z_wallets/src/domains/domains_snapshot.txt`
- `crates/z00z_wallets/tests/test_common/test_mod.rs`
- `crates/z00z_wallets/tests/test_common/test_range_proof_env.inc`
- `crates/z00z_wallets/tests/test_common/test_rpc_logger.inc`
- `crates/z00z_wallets/tests/test_common/test_wallet_env.inc`
- `crates/z00z_wallets/tests/test_common/test_wallet_env_lock.inc`
</files>
<action>
Audit the live tree against `061-TODO.md` before broad code motion. Re-run the
TODO table-to-tree baseline compare, confirm the planner's current-file and
coverage invariants, reconcile the known stale service-wrapper and stealth-path
rows, freeze which files and tests use `include!`, `include_str!`, `#[path]`,
or hard-coded relative source paths, and classify delete candidates such as
`app_settings_tab_2.rs` and the duplicate receipt or scan storage shims as
`safe to remove`, `requires replacement`, or `blocked by live reference`. If
the TODO needs correction, keep it minimal and canonical; otherwise record the
drift explicitly in the summary so later slices do not execute against false
path assumptions. This anchor freeze includes the `tests/test_common` helper
corpus so later slices know which helpers are truly path-sensitive
(`test_rpc_logger.inc`) and which are observational or env-only guards. It also
freezes the exact schema, external wallet-doc, external asset-config, and
domain-snapshot filenames that later slices must relocate or rewrite
atomically.
</action>
<acceptance_criteria>
- Live-tree drift around service wrappers, stale stealth paths, and other stale
  TODO rows is explicitly reconciled in canonical phase artifacts.
- The TODO table-to-tree baseline compare is rerun, and any deviation from
  `CHECK-001` through `CHECK-010` is either disproved or explicitly recorded as
  drift before edits continue.
- Delete candidates have reference-backed classifications before any removal
  wave starts.
- Anchor-sensitive tests and modules are enumerated so later slices know which
  path updates must land atomically.
- Later plans can cite this slice as the live-tree truth source.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run the slice-specific checks: `find crates/z00z_wallets/src -type f -name "*.rs" | sort`, `comm -13 <(awk -F'`' '/^\\|[[:space:]]*[0-9]+[[:space:]]*\\| `crates\\/z00z_wallets\\/src\\//{print $2}' .planning/phases/061-Wallet-Refactoring/061-TODO.md | sort) <(find crates/z00z_wallets/src -type f -name "*.rs" | sort)`, `comm -23 <(awk -F'`' '/^\\|[[:space:]]*[0-9]+[[:space:]]*\\| `crates\\/z00z_wallets\\/src\\//{print $2}' .planning/phases/061-Wallet-Refactoring/061-TODO.md | sort) <(find crates/z00z_wallets/src -type f -name "*.rs" | sort)`, `rg -n "wallet_service_types|wallet_service_store|wallet_service_session|wallet_service_actions|facade_zkpack|zkpack|app_settings_tab_2|storage\\.rs|storage_impl\\.rs|wallet_config\\.yaml|redb-schema\\.yaml|domains_snapshot\\.txt" .planning/phases/061-Wallet-Refactoring/061-TODO.md crates/z00z_wallets/src crates/z00z_wallets/tests`, and `cargo check --release -p z00z_wallets --all-targets --all-features`. Then run `cargo test --release` when this slice changes Rust or test files.
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
4. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The phase has a trustworthy live-tree starting point, and later rename waves no
longer depend on stale path assumptions.
</done>
</task>
</tasks>

<threat_model>
- Executing the rename table against non-existent wrapper files would create
  false confidence and duplicate rework.
- Removing duplicate-looking files before reference proof can silently break
  wallet persistence or tests.
- Moving files without an anchor inventory would break `include!` or
  `include_str!` surfaces far away from the edited module.
</threat_model>

<verification>
This slice is valid only if the later plans can execute against live-tree truth
instead of stale planner assumptions.
</verification>

<success_criteria>
- Stale rows are reconciled.
- Delete candidates are classified with evidence.
- Anchor-sensitive modules and tests are frozen before bulk moves begin.
</success_criteria>

<output>
Create `.planning/phases/061-Wallet-Refactoring/061-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `.planning/phases/061-Wallet-Refactoring/061-01-SUMMARY.md`
- Phase-local drift ledger for stale service-wrapper rows and delete candidates
- Explicit anchor inventory for `include!`, `include_str!`, and `#[path]`
  sensitive wallet files
