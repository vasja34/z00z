---
phase: 061-Wallet-Refactoring
plan: 061-08
type: execute
wave: 7
depends_on:
  - 061-01
  - 061-07
files_modified:
  - crates/z00z_wallets/src/key/mod.rs
  - crates/z00z_wallets/src/key/bip.rs
  - crates/z00z_wallets/src/key/manager.rs
  - crates/z00z_wallets/src/key/receiver.rs
  - crates/z00z_wallets/src/key/seed.rs
  - crates/z00z_wallets/docs/KEYS-DERIVATION.md
requirements:
  - 061-D02
  - 061-D03
  - 061-D06
  - 061-D08
  - 061-D14
autonomous: true
must_haves:
  truths:
    - "The key tree is include-heavy and is isolated from tx/wallet churn on purpose."
    - "BIP, manager, receiver, and seed lanes flatten to one-level names without losing the current key facade."
    - "Key docs currently living under `src/` move with the owning code or are rewritten in the same wave."
    - "Test `.inc.rs` and BIP anchor paths move atomically with the files that include them."
  artifacts:
    - crates/z00z_wallets/src/key/mod.rs
    - crates/z00z_wallets/src/key/bip.rs
    - crates/z00z_wallets/src/key/manager.rs
    - crates/z00z_wallets/src/key/seed.rs
  key_links:
    - "This slice isolates the densest include network outside RPC."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 061-08 Plan: Key Tree Flattening And Seed Or BIP Anchor Preservation

<objective>
Flatten the key tree into one-level domain files while preserving the live key
facade, test `.inc.rs` inputs, and key-doc anchors.
</objective>

<context>
@.planning/phases/061-Wallet-Refactoring/061-TODO.md
@.planning/phases/061-Wallet-Refactoring/061-CONTEXT.md
@crates/z00z_wallets/src/key/mod.rs
@crates/z00z_wallets/src/key/bip.rs
@crates/z00z_wallets/src/key/manager.rs
@crates/z00z_wallets/src/key/receiver.rs
@crates/z00z_wallets/src/key/seed.rs
@crates/z00z_wallets/docs/KEYS-DERIVATION.md
</context>

<coverage_contract>
This slice owns key-tree flattening from decisions `D-02`, `D-03`, `D-06`,
`D-08`, and `D-14`. It must keep the key facade coherent while moving BIP,
manager, receiver, and seed helpers into one-level files.
</coverage_contract>

<tasks>
<task type="auto">
<name>Flatten BIP, manager, receiver, and seed key files together with their include anchors</name>
<read_first>
- `.planning/phases/061-Wallet-Refactoring/061-TODO.md` key rows
- `.planning/phases/061-Wallet-Refactoring/061-CONTEXT.md`
- `crates/z00z_wallets/src/key/mod.rs`
- `crates/z00z_wallets/src/key/bip.rs`
- `crates/z00z_wallets/src/key/manager.rs`
- `crates/z00z_wallets/src/key/receiver.rs`
- `crates/z00z_wallets/src/key/seed.rs`
</read_first>
<files>
- files under `crates/z00z_wallets/src/key/`
- key docs under `crates/z00z_wallets/docs/`
</files>
<action>
Flatten the key tree into one-level `src/key/*.rs` files, including BIP,
manager, receiver, and seed helpers. Keep the key facade and re-exports
coherent while updating `include!`, `.inc.rs`, and relative doc anchors in the
same slice. Move key docs such as the BIP docs and `KEYS-DERIVATION.md` to the
canonical `crates/z00z_wallets/docs/` area, or rewrite their anchors in place
only long enough to complete that move in the same slice, so the final tree no
longer depends on non-Rust key assets under `src/`.
</action>
<acceptance_criteria>
- BIP, manager, receiver, and seed Rust files are one-level under `src/key/`.
- Test `.inc.rs` inputs and relative include anchors still resolve correctly.
- Key docs no longer violate the phase tree rule, and their code anchors remain
  intentional.
- The live key facade and re-export surface still compile for callers.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run the slice-specific checks: `cargo check --release -p z00z_wallets --all-targets --all-features`, `rg -n "src/key/(bip/|manager/|receiver/|seed/)|KEYS-DERIVATION|KEYS-Bip44-UserGuide|KEYS-GUIDE|KEYS_EXPALNATION|bip44_derivation|include!" crates/z00z_wallets/src crates/z00z_wallets/tests crates/z00z_wallets/docs/key`, and `find crates/z00z_wallets/src/key -type f -name '*.rs' | sed 's#^crates/z00z_wallets/src/key/##' | awk -F/ 'NF > 1 {print}'`. Then run `cargo test --release` when this slice changes Rust or test files.
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
4. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The key tree is flat, include-heavy key tests still resolve, and key docs no
longer depend on a nested `src/` asset layout.
</done>
</task>
</tasks>

<threat_model>
- Include-heavy key code can compile partially while leaving latent path bugs in
  `.inc.rs` tests or seed helpers.
- Moving key docs without updating their anchors would break code-to-doc
  traceability and future audits.
- Mixing key churn with tx/wallet churn would blur responsibility when failures
  appear.
</threat_model>

<verification>
This slice is valid only if the key tree is physically flat and its include or
doc anchors remain coherent.
</verification>

<success_criteria>
- One-level `src/key/*.rs` layout exists.
- Include-heavy key tests still resolve.
- Key docs no longer live under `src/` as orphaned assets.
</success_criteria>

<output>
Create `.planning/phases/061-Wallet-Refactoring/061-08-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `.planning/phases/061-Wallet-Refactoring/061-08-SUMMARY.md`
- Flat key-module layout under `src/key/`
- Updated key test and doc anchors for the new canonical files
