---
phase: 034-mix1-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_storage/tests/test_claim_source_proof.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_wallets/src/core/tx/test_claim_tx.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
  - crates/z00z_wallets/tests/support/test_s5_sender_examples_support.rs
autonomous: true
requirements:
  - PH34-CLAIM-CONTINUITY
must_haves:
  truths:
    - Live claim packages consume persisted storage membership evidence instead of a helper-owned one-item tree.
    - Wallet and simulator claim verification reject claim-source root or proof drift against the persisted seam.
  artifacts:
    - store_query.rs exposes a persisted-membership-backed claim_source_contract_for_item path.
    - claim package producer and verifier callers use one canonical storage-backed claim seam.
  key_links:
    - AssetStore::claim_source_contract_for_item(...) -> wallet claim verifier.
    - Scenario 1 claim package producer -> claim_pkg_consumer verifier.
---

# Phase 034 Plan 01

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the Q63 claim-continuity migration in the same order as `034-TODO.md`.

Purpose: remove the helper-owned authority path without creating a parallel claim verifier layer.
Output: persisted claim-source seam in storage plus migrated wallet and simulator claim consumers.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
@.planning/phases/034-mix1-fixes/034-33AUDIT.md
@.planning/phases/000/033-crypto-audit-scenario-2/033-21-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>034-01 Persisted Claim-Source Contract Seam</name>
  <files>crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_storage/tests/test_claim_source_proof.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-01` in `034-TODO.md`. Replace the helper-owned off-store one-item reconstruction inside `claim_source_contract_for_item(...)` with a storage-backed membership seam that derives the authoritative root and proof from persisted store state. Keep canonical asset-path binding, fail closed on missing membership or path drift, and preserve deterministic proof bytes and root encoding so downstream callers do not need compatibility glue. This task is not complete until every live producer, consumer, verifier, and fixture helper that still reconstructs claim-source material from output-leaf or asset-wire data is either migrated or explicitly downgraded to non-authoritative status, including `authoritative_claim_item(...)`, `derive_claim_source_root(...)`, and every `make_claim_source_proof(...)` helper still used in live seams or phase-owned tests. Extend `test_claim_source_proof.rs` exactly with the positive persisted-membership roundtrip and the negative stale-membership rejection cases listed in `034-TODO.md`. Do not introduce a parallel claim-source verifier or a shadow proof format.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run `cargo test -p z00z_storage --release test_claim_source_proof -- --nocapture` and any exact test targets added in `crates/z00z_storage/tests/test_claim_source_proof.rs`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump` and, if the executor changes cross-crate contracts beyond simulator-facing coverage, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>`claim_source_contract_for_item(...)` is backed by persisted membership state, the helper-only tree is no longer the authority path, and the new storage tests prove both the positive and negative cases required by `034-TODO.md`.</done>
</task>

<task type="auto">
  <name>034-02 Claim Producer And Verifier Migration</name>
  <files>crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, crates/z00z_wallets/src/core/tx/test_claim_tx.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs, crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs, crates/z00z_wallets/tests/support/test_s5_sender_examples_support.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-02` in `034-TODO.md`. Migrate all live claim package producer and verifier call sites to the new storage-backed claim-source seam from `034-01`. Remove duplicated local `make_claim_source_proof(...)`-style helpers where they still encode the helper-owned authority path, and treat `authoritative_claim_item(...)` plus `derive_claim_source_root(...)` as migration-critical symbols rather than documentation-only evidence. Keep claim verification fail closed on source-root or proof drift, and preserve the existing truthful seam names instead of inventing a new claim package abstraction. Extend the simulator and wallet coverage exactly as listed in `034-TODO.md`, including the negative verifier case after migration in `crates/z00z_wallets/src/core/tx/test_claim_tx.rs` so wallet-side source-root drift stays explicitly frozen.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_claim_pkg_crypto_support -- --nocapture` and `cargo test -p z00z_wallets --release test_claim_tx -- --nocapture`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; if the migrated claim seam changes broader cross-crate behavior, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Live claim package construction and verification consume one storage-backed claim seam, no caller treats the helper-owned continuity path as the authority surface, every live producer, consumer, verifier, and fixture helper that still reconstructs claim-source material is either migrated or explicitly non-authoritative, and the required simulator plus wallet tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage -> wallet claim verifier | Persisted proof material crosses from storage ownership into wallet verification. |
| simulator claim producer -> claim consumer | Scenario 1 claim artifacts cross from producer code into verifier-facing consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-01 | Tampering | store_query.rs claim source contract | mitigate | Bind roots and proofs to persisted membership state and reject path or proof drift before emission. |
| T-034-02 | Spoofing | claim package producer/consumer seam | mitigate | Remove helper-owned fallback helpers, migrate `authoritative_claim_item(...)` and `derive_claim_source_root(...)` away from off-store authority assumptions, and require all live callers to use the storage-backed proof seam. |
| T-034-03 | Repudiation | claim continuity tests | mitigate | Add positive and negative regression tests that freeze the authority path and failure mode explicitly. |
</threat_model>

<verification>
Both tasks must complete in order. The plan is only green when the storage-backed seam exists, migrated callers no longer rely on helper-only authority, bootstrap tests pass first, targeted release-style tests pass, and the repeated YOLO review loop converges cleanly twice in a row.</verification>

<success_criteria>
Q63 is implemented in live code instead of preserved as a wording-only narrowing, and no Phase 034 claim path introduces a duplicate proof layer or helper-authority fallback.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-01-SUMMARY.md`.
</output>