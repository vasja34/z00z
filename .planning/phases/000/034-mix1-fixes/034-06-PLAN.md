---
phase: 034-mix1-fixes
plan: 06
type: execute
wave: 6
depends_on:
  - 034-05
files_modified:
  - crates/z00z_wallets/tests/test_spend_witness_gate.rs
  - crates/z00z_wallets/tests/test_scenario1_semantics.rs
  - crates/z00z_simulator/tests/test_scenario1_spend_gate.rs
  - crates/z00z_storage/tests/test_checkpoint_finalization.rs
  - crates/z00z_storage/tests/test_checkpoint_store_api.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
  - crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
autonomous: true
requirements:
  - PH34-SPEND-NULLIFIER
  - PH34-CHECKPOINT-BACKEND
must_haves:
  truths:
    - Q64 spend-nullifier semantics are proven by code and tests.
    - Q65 checkpoint backend authority is proven by code and tests.
  artifacts:
    - spend validation tests cover valid deterministic nullifiers, malformed hex, signed-field drift, structural mismatch, and duplicate nullifiers.
    - checkpoint tests cover authoritative positive path, compatibility-only negative path, and reload drift rejection.
  key_links:
    - witness gate tests -> Scenario 1 spend gate tests.
    - checkpoint finalization tests -> rehydrate and promotion tests.
---

# Phase 034 Plan 06

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Run the core semantic validation waves for the spend-nullifier and checkpoint-backend closures.

Purpose: convert the implemented Q64 and Q65 seams into reproducible, phase-owned evidence before documentation is reclassified.
Output: green spend and checkpoint validation waves with exact negative-case coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
</context>

<tasks>

<task type="auto">
  <name>034-10 Spend Nullifier Test Wave</name>
  <files>crates/z00z_wallets/tests/test_spend_witness_gate.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs, crates/z00z_simulator/tests/test_scenario1_spend_gate.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-10` in `034-TODO.md`. Run and tighten the wallet and simulator spend tests after `034-03` through `034-05` so they prove deterministic nullifier derivation, missing or malformed nullifier rejection, signed-field drift rejection on the public seam, structural deterministic-mismatch rejection, duplicate rejection, and preserved behavior after sender-authority migration. Keep the wording guard in `test_scenario1_semantics.rs` honest: it should move from "gap still open" to the shipped closure only when the implementation already proves it.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
  2. Run `cargo test -p z00z_wallets --release --test test_spend_witness_gate -- --nocapture`, `cargo test -p z00z_wallets --release --test test_scenario1_semantics -- --nocapture`, and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_spend_gate -- --nocapture`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; because this is a core semantic gate, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>The Q64 blocker is closed by code and tests, including positive deterministic nullifier coverage and all required negative cases for missing, malformed, duplicate, and signed-field drift on the public seam, plus deterministic mismatch coverage on the witness and structural paths.</done>
</task>

<task type="auto">
  <name>034-11 Checkpoint Backend Test Wave</name>
  <files>crates/z00z_storage/tests/test_checkpoint_finalization.rs, crates/z00z_storage/tests/test_checkpoint_store_api.rs, crates/z00z_storage/tests/test_redb_rehydrate.rs, crates/z00z_simulator/tests/test_checkpoint_acceptance.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-11` in `034-TODO.md`. Run and tighten the storage, reload, and simulator checkpoint tests after `034-06` and `034-07` so they prove authoritative backend proof acceptance, reject compatibility-only payload authority, reject reload drift, and reject proof-system mismatch, statement-shape drift, exec-identity drift, snapshot or link-tuple drift, and payload-shape drift wherever those invariants cross finalize, load, reload, and simulator promotion seams. Keep the tests bound to the live checkpoint seam and stage promotion path rather than inventing extra validator facades.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
  2. Run `cargo test -p z00z_storage --release --test test_checkpoint_finalization -- --nocapture`, `cargo test -p z00z_storage --release --test test_checkpoint_store_api -- --nocapture`, `cargo test -p z00z_storage --release --test test_redb_rehydrate -- --nocapture`, and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_checkpoint_acceptance -- --nocapture`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; because this is a core semantic gate, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>The Q65 blocker is closed by code and tests, including authoritative positive coverage and the compatibility-only, reload-drift, proof-system mismatch, statement-shape drift, exec-identity drift, snapshot or link-tuple drift, and payload-shape negative paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wallet spend verifier -> simulator spend gate | Semantic drift between internal and Scenario 1 acceptance must be detected. |
| storage checkpoint validation -> simulator checkpoint acceptance | Authoritative backend truth must survive reload and promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-15 | Tampering | spend nullifier tests | mitigate | Cover missing, malformed, duplicate, and signed-field drift on wallet and simulator public seams, while separately covering deterministic mismatch on the witness and structural checks. |
| T-034-16 | Tampering | checkpoint backend tests | mitigate | Cover compatibility-only payload rejection, reload drift, proof-system mismatch, statement-shape drift, exec-identity drift, snapshot or link-tuple drift, and payload-shape drift in storage and simulator tests. |
| T-034-17 | Denial of service | semantic regression escape | mitigate | Require release-style validation and repeated review passes before docs can be reclassified. |
</threat_model>

<verification>
This plan is green only when both semantic validation waves are fully green, broader release-style checks are green, and the repeated YOLO review loop converges cleanly twice consecutively.</verification>

<success_criteria>
Phase 034 has repository-backed proof that both core semantic blockers, Q64 and Q65, are no longer only narrowed wording claims.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-06-SUMMARY.md`.
</output>