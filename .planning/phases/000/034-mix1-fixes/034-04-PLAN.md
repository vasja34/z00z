---
phase: 034-mix1-fixes
plan: 04
type: execute
wave: 4
depends_on:
  - 034-03
files_modified:
  - crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs
  - crates/z00z_storage/src/checkpoint/artifact_final.rs
  - crates/z00z_storage/src/checkpoint/codec.rs
  - crates/z00z_wallets/src/core/tx/state_checkpoint.rs
  - crates/z00z_wallets/src/core/tx/state_update.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
  - crates/z00z_simulator/src/scenario_1/stage_12.rs
  - crates/z00z_storage/tests/test_checkpoint_finalization.rs
  - crates/z00z_storage/tests/test_checkpoint_store_api.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
  - crates/z00z_simulator/tests/test_checkpoint_acceptance.rs
autonomous: true
requirements:
  - PH34-CHECKPOINT-BACKEND
must_haves:
  truths:
    - Checkpoint proof acceptance is defined by one backend-owned acceptance contract.
    - Finalize, seal, reload, and Scenario 1 promotion all consume the same backend-bound truth.
  artifacts:
    - checkpoint draft/final/codec surfaces encode the backend-owned acceptance contract.
    - wallet and storage checkpoint acceptance paths reject compatibility-only authority.
  key_links:
    - checkpoint artifact encoding -> wallet finalize/load acceptance.
    - storage reload validation -> Scenario 1 stage_12 promotion path.
---

# Phase 034 Plan 04

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the Q65 checkpoint-backend closure chain in the same order as `034-TODO.md`.

Purpose: remove compatibility-payload and externally supplied verifier trust as the closure story while preserving truthful backward-format handling.
Output: one backend-owned checkpoint acceptance contract plus wallet, storage, and simulator integration.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
@.planning/phases/034-mix1-fixes/034-33AUDIT.md
@.planning/phases/000/033-crypto-audit-scenario-2/033-23-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>034-06 Authoritative Checkpoint Proof Backend Contract</name>
  <files>crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs, crates/z00z_storage/src/checkpoint/artifact_final.rs, crates/z00z_storage/src/checkpoint/codec.rs, crates/z00z_wallets/src/core/tx/state_checkpoint.rs, crates/z00z_storage/tests/test_checkpoint_finalization.rs, crates/z00z_storage/tests/test_checkpoint_store_api.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-06` in `034-TODO.md`. Define one backend-owned checkpoint acceptance contract inside the live checkpoint seam. Preserve explicit proof-system typing, statement shape, exec identity, snapshot or link tuple, and payload-shape invariants, keep legacy compatibility parsing only where strictly needed for backward format handling, and remove compatibility payload bytes plus externally supplied verifier trust as the closure story. Extend the checkpoint finalization and store API tests exactly with the authoritative positive path, explicit proof-system mismatch rejection, statement-shape drift rejection, exec-identity drift rejection, snapshot or link-tuple drift rejection, payload-shape drift rejection, and legacy-decode-but-non-authoritative classification listed in `034-TODO.md` and frozen by `034-CONTEXT.md`.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run `cargo test -p z00z_storage --release test_checkpoint_finalization -- --nocapture` and `cargo test -p z00z_storage --release test_checkpoint_store_api -- --nocapture` plus any exact tests added in those suites.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; if the backend contract propagates across wider checkpoint consumers, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Finalized checkpoint acceptance is defined by one backend-owned contract over proof-system typing, statement shape, exec identity, snapshot or link tuple, and payload-shape invariants, and the compatibility-only path is no longer the authority surface.</done>
</task>

<task type="auto">
  <name>034-07 Checkpoint Finalize Or Load Integration</name>
  <files>crates/z00z_wallets/src/core/tx/state_checkpoint.rs, crates/z00z_wallets/src/core/tx/state_update.rs, crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs, crates/z00z_simulator/src/scenario_1/stage_12.rs, crates/z00z_storage/tests/test_redb_rehydrate.rs, crates/z00z_simulator/tests/test_checkpoint_acceptance.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-07` in `034-TODO.md`. Bind wallet-side checkpoint finalize/load acceptance to the backend-owned acceptance contract from `034-06`, bind storage reload validation to the same contract, reject drift between draft, artifact, reload, and simulator promotion surfaces, and update Scenario 1 stage 12 helpers to exercise the backend-owned path instead of the compatibility-only wording path. Keep the implementation within the live wallet/storage/simulator seams; do not invent a standalone verifier facade or overclaim a generic new proof system unless code actually introduces one. Add the reload-drift and simulator acceptance tests listed in `034-TODO.md`, including proof-system mismatch, statement-shape drift, exec-identity drift, snapshot or link-tuple drift, and payload-shape drift wherever those seams consume the acceptance contract.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run `cargo test -p z00z_storage --release test_redb_rehydrate -- --nocapture` and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_checkpoint_acceptance -- --nocapture`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; because this task changes finalize, reload, and stage promotion seams, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Checkpoint finalize, seal, reload, and Scenario 1 promotion all consume the same backend-owned acceptance path and reject proof-system mismatch, statement-shape drift, exec-identity drift, snapshot or link-tuple drift, payload-shape drift, backend drift, and compatibility-only authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint artifact bytes -> wallet/state acceptance | Encoded proof material crosses from persisted artifact state into wallet logic. |
| storage reload -> simulator promotion | Rehydrated checkpoint state is promoted into Scenario 1 runtime flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-10 | Tampering | checkpoint artifact encoding | mitigate | Bind finalize/load acceptance to one backend-owned acceptance contract and reject proof-system mismatch, statement-shape drift, exec-identity drift, snapshot or link-tuple drift, payload-shape drift, and mismatched backend or proof material. |
| T-034-11 | Spoofing | externally supplied verifier trust | mitigate | Remove external verifier trust as the closure story and verify through the backend-owned acceptance contract. |
| T-034-12 | Repudiation | reload/promotion drift | mitigate | Add explicit negative tests for reload drift and simulator promotion failure. |
</threat_model>

<verification>
This plan is green only when checkpoint acceptance is backend-bound everywhere it matters, storage and simulator negative tests prove drift rejection, legacy decode stays explicitly non-authoritative, release-style validation is green, and the repeated YOLO review loop converges cleanly twice consecutively.</verification>

<success_criteria>
Q65 is closed by repository-backed code and tests rather than by compatibility wording, no new checkpoint verifier facade is introduced, and the plan does not overclaim a generic standalone proof system unless code actually adds one.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-04-SUMMARY.md`.
</output>