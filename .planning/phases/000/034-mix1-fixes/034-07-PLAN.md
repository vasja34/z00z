---
phase: 034-mix1-fixes
plan: 07
type: execute
wave: 7
depends_on:
  - 034-06
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/034-mix1-fixes/034-CONTEXT.md
  - .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md
  - .planning/temp/Z00Z-ECC-IDEAS.md
  - .planning/temp/Z00Z-ECC-SPEC_part1.md
  - crates/z00z_simulator/src/scenario_1/stage_12.rs
  - crates/z00z_wallets/src/core/tx/state_checkpoint.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_wallets/tests/test_scenario1_semantics.rs
autonomous: true
requirements:
  - PH34-DOC-ALLOWLIST
must_haves:
  truths:
    - Active requirements and stage-surface wording describe the implemented Phase 034 truth.
    - Historical append-only audit artifacts remain untouched.
  artifacts:
    - live requirement and planning-doc wording is updated only after semantic closure waves are green.
    - stage-surface and scenario semantics tests freeze the new wording.
  key_links:
    - requirements/docs wording -> simulator stage-surface tests.
    - checkpoint wording in code -> documentation allowlist truth.
---

# Phase 034 Plan 07

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Reclassify the documentation allowlist only after the semantic closure waves are green and then freeze the new wording with tests.

Purpose: close Q47 honestly without rewriting historical audit evidence.
Output: updated live requirement/docs wording plus green wording-guard tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
@.planning/phases/034-mix1-fixes/034-fix-spec-4.md
</context>

<tasks>

<task type="auto">
  <name>034-12 Documentation Allowlist And Wording Reclassification</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/034-mix1-fixes/034-CONTEXT.md, .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md, .planning/temp/Z00Z-ECC-IDEAS.md, .planning/temp/Z00Z-ECC-SPEC_part1.md, crates/z00z_simulator/src/scenario_1/stage_12.rs, crates/z00z_wallets/src/core/tx/state_checkpoint.rs, crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-12` in `034-TODO.md`. Re-evaluate what may stay in active documentation only after Plans 05 and 06 are green. Update live requirements, `034-CONTEXT.md`, live code wording targets, and the concrete planning docs named by `034-fix-spec-4.md` so they describe the new implemented truth without overclaiming beyond the tested seams. Those future-phase and temp planning docs remain explicit update targets for this task because `034-TODO.md` names them directly. Explicitly remove stale wording that still advertises helper-owned sender authority or pre-closure checkpoint blockers from the in-scope surfaces, and leave append-only historical audit artifacts historical and untouched.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run the exact wording and semantics tests affected by this task, including `cargo test -p z00z_wallets --release test_scenario1_semantics -- --nocapture` and any targeted guard tests that read the updated checkpoint wording and active phase-local wording in `034-CONTEXT.md`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; because wording guards cross simulator and docs, also run `cargo test --release --features test-fast --features wallet_debug_dump` if the executor changes simulator-facing wording sources.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Live documentation targets are updated only after code and prior waves prove the new truth, and no historical append-only audit artifact is rewritten.</done>
</task>

<task type="auto">
  <name>034-13 Documentation And Stage-Surface Test Wave</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/034-mix1-fixes/034-CONTEXT.md, .planning/phases/040-spend-proof/040-Spend-Proof-Spec.md, .planning/temp/Z00Z-ECC-IDEAS.md, .planning/temp/Z00Z-ECC-SPEC_part1.md, crates/z00z_simulator/tests/test_scenario1_stage_surface.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-13` in `034-TODO.md`. Run the wording and documentation guard tests after `034-12`, proving that active requirement text matches implemented closure, active `034-CONTEXT.md` truth matches implemented closure, stage-surface wording matches implemented closure, active code wording surfaces no longer advertise helper-owned claim authority or pre-closure checkpoint blockers, the concrete planning docs touched by `034-12` no longer point future work at `builder.rs` or `output_flow.rs` as canonical sender-construction authority, and historical append-only artifacts remain untouched.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_stage_surface -- --nocapture` and `cargo test -p z00z_wallets --release test_scenario1_semantics -- --nocapture`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; because this is the live wording gate, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Q47 is closed honestly, the active documentation allowlist reflects the shipped, tested Phase 034 truth, and active phase-local context no longer advertises stale authority or pre-closure blockers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live code truth -> active docs | Documentation may overclaim if it is updated before semantic closure is actually proven. |
| wording guards -> historical audit artifacts | Active wording updates must not rewrite append-only evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-18 | Repudiation | requirements/docs wording | mitigate | Update only active docs after semantic closure waves pass and freeze wording with tests. |
| T-034-19 | Tampering | historical audit artifacts | mitigate | Leave append-only historical artifacts untouched and validate only live requirement/docs surfaces. |
</threat_model>

<verification>
This plan is green only when the active wording surfaces reflect the implemented truth, historical artifacts stay unchanged, release-style validation is green, and the repeated YOLO review loop converges cleanly twice consecutively.</verification>

<success_criteria>
The documentation allowlist is honestly reclassified after the technical closures land, and no stale `core::tx` sender-authority wording remains canonical in active planning docs.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-07-SUMMARY.md`.
</output>