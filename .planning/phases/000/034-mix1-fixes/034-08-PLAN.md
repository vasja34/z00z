---
phase: 034-mix1-fixes
plan: 08
type: execute
wave: 8
depends_on:
  - 034-07
files_modified:
  - .planning/phases/034-mix1-fixes/034-VALIDATION.md
  - .planning/phases/034-mix1-fixes/034-CLOSEOUT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_storage/tests/test_search_api.rs
autonomous: true
requirements:
  - PH34-CLOSURE-PROOF
  - PH34-KEEP-PATH-SIDECAR
must_haves:
  truths:
    - Phase 034 has one repository-backed closure package for Q63, Q64, Q65, and Q47.
    - The optional `keep_path(...)` cleanup stays explicitly outside the semantic closure story.
  artifacts:
    - phase-local validation artifact exists.
    - phase-local closeout summary exists.
    - optional keep_path refactor, if executed, is local and behavior-preserving.
  key_links:
    - semantic validation waves -> phase-local closure artifact.
    - keep_path refactor -> search API regression tests.
---

# Phase 034 Plan 08

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Produce the phase-local closure proof and, only after that, optionally execute the tiny keep-path sidecar.

Purpose: separate semantic closure evidence from non-semantic cleanup while still scheduling all required TODO items.
Output: closeout artifacts plus optional local `keep_path(...)` simplification.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
@.planning/phases/034-mix1-fixes/034-deferred.md
</context>

<tasks>

<task type="auto">
  <name>034-14 Closure, Regression, And Phase-Proof Sweep</name>
  <files>.planning/phases/034-mix1-fixes/034-VALIDATION.md, .planning/phases/034-mix1-fixes/034-CLOSEOUT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-14` in `034-TODO.md`. Run the full targeted regression sweep for claim continuity, regular spend semantics, legacy sender migration, checkpoint backend closure, and wording guards. Confirm the old partial-closure blockers are now either implemented or explicitly re-narrowed by source update. Produce the phase-local verification and closeout artifacts required to retire Q63, Q64, Q65, and Q47. The closeout package must explicitly reconcile every mandatory main-chain task from `034-01` through `034-14`, marking each one as completed or validated with repository evidence; if any extreme blocker remains, fail closed by updating the canonical source and leave the phase open rather than reporting semantic closure. As part of closeout, synchronize `.planning/ROADMAP.md` and `.planning/STATE.md` so repository-level phase status matches the phase-local closure package. The closeout artifact must remain repository-backed and must not treat any optional sidecar as semantic closure evidence.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run the targeted wallet, storage, and simulator tests from `034-09` through `034-13` exactly as recorded in the phase-local validation artifact, then verify the closeout artifact explicitly reconciles `034-01` through `034-14` and that `.planning/ROADMAP.md` and `.planning/STATE.md` reflect the same Phase 034 status.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`, then run `cargo test --release --features test-fast --features wallet_debug_dump` as the final semantic sweep.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Phase 034 has one repository-backed closure package proving that the old partial blockers are no longer the active truth, that every mandatory task from `034-01` through `034-14` is explicitly reconciled as completed or validated, that repository-level status files are synchronized, and that optional sidecars are excluded from the semantic closure claim.</done>
</task>

<task type="auto">
  <name>034-15 Optional Keep-Path Complexity Sidecar</name>
  <files>crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_storage/tests/test_search_api.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-15` in `034-TODO.md` and the optional tiny debt specification in `034-deferred.md`. Refactor `keep_path(...)` into smaller local predicates or guard helpers inside the same storage query seam without changing list semantics, filter order, range handling, or paging behavior. Keep the cleanup local to `store_query.rs`; do not widen it into a search API redesign or a broad cleanup pass. In all phase reporting, keep this sidecar explicitly non-blocking and outside the semantic completion story.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run `cargo test -p z00z_storage --release test_search_api -- --nocapture` and any exact search API cases added for scope, range, and paging invariants.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump` only if the executor changes simulator-visible search fixtures; otherwise the storage target is sufficient for this sidecar.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>`keep_path(...)` is structurally simpler, search behavior is unchanged, and the sidecar remains explicitly outside the semantic completion story for Phase 034.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| semantic validation outputs -> phase closeout docs | Closure artifacts must truthfully represent what the code and tests now prove. |
| local search helper refactor -> search API behavior | The sidecar must not change observable listing or paging semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-20 | Repudiation | closeout artifacts | mitigate | Build the closure package directly from the targeted regression sweep and exclude sidecars from semantic claims. |
| T-034-21 | Tampering | keep_path(...) refactor | mitigate | Keep the refactor local and prove identical search behavior with exact search API regression tests. |
</threat_model>

<verification>
This plan is green only when the phase-local closure artifact is complete, optional keep_path cleanup remains behavior-preserving and explicitly non-semantic, and the repeated YOLO review loop converges cleanly twice consecutively.</verification>

<success_criteria>
Phase 034 has a repository-backed closure package, and the optional keep-path sidecar, if executed, remains clearly separated from semantic closure evidence.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-08-SUMMARY.md`.
</output>