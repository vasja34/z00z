---
phase: 034-mix1-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 034-01
files_modified:
  - crates/z00z_crypto/src/domains.rs
  - crates/z00z_wallets/src/core/tx/tx_wire_types.rs
  - crates/z00z_wallets/src/core/tx/spend_rules.rs
  - crates/z00z_wallets/src/core/tx/witness_gate.rs
  - crates/z00z_wallets/src/core/tx/spend_verification.rs
  - crates/z00z_wallets/tests/test_spend_witness_gate.rs
  - crates/z00z_wallets/tests/test_scenario1_semantics.rs
  - crates/z00z_simulator/tests/test_scenario1_spend_gate.rs
autonomous: true
requirements:
  - PH34-SPEND-NULLIFIER
must_haves:
  truths:
    - The regular public spend contract exposes one deterministic nullifier semantics surface end to end.
    - The public verifier rejects malformed, duplicate, or post-signature nullifier drift, and the witness plus structural rule layer reject deterministic `chain_id || s_in` mismatches.
  artifacts:
    - domains.rs contains one dedicated regular-spend nullifier domain.
    - SpendInputProofWire exposes `nullifier_hex`, and the structural spend surface carries the nullifier plus the `chain_id` input needed to mirror `chain_id || s_in`.
    - witness_gate.rs and spend_verification.rs emit and verify deterministic nullifiers.
    - One canonical nullifier-derivation helper owns the exact domain symbol, fixed-width `chain_id` encoding, and byte framing shared by witness, public, and structural paths.
  key_links:
    - deterministic nullifier derivation -> witness bridge public inputs.
    - public verifier -> structural rule verifier alignment.
    - `chain_id || s_in` derivation -> structural spend-rule recomputation.
---

# Phase 034 Plan 02

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the Q64 spend-nullifier closure chain in the same order as `034-TODO.md`.

Purpose: implement the exact regular-spend closure work without introducing a second spend proof system.
Output: deterministic nullifier domain, wire contract, witness bridge, signed public verifier field, and rule-layer enforcement.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
@.planning/phases/034-mix1-fixes/034-fix-spec-4.md
@.planning/phases/000/033-crypto-audit-scenario-2/033-22-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>034-03 Regular-Spend Nullifier Domain And Wire Contract</name>
  <files>crates/z00z_crypto/src/domains.rs, crates/z00z_wallets/src/core/tx/tx_wire_types.rs, crates/z00z_wallets/src/core/tx/spend_rules.rs, crates/z00z_wallets/tests/test_scenario1_semantics.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-03` in `034-TODO.md`. Add one dedicated regular-spend nullifier domain under the existing tx and consensus-domain family in `z00z_crypto`. Freeze one exact `hash_domain!` symbol, one fixed-width `chain_id` encoding, and one canonical helper for the new regular-spend derivation so later tasks do not clone the framing logic manually. Extend `SpendInputProofWire` with `nullifier_hex` while preserving the current framing and serial semantics, then extend the structural spend surface with the explicit nullifier and the `chain_id` input required to mirror the canonical derivation inside `verify_spend_rules(...)`. Preserve exact canonical naming from `034-fix-spec-4.md` and do not reuse claim-path nullifier helpers as-is. Add the internal and semantic guard tests listed in `034-TODO.md`.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run the exact unit and integration tests touched by this task, including internal `spend_rules.rs` tests that cover the required `chain_id` and nullifier state, plus `cargo test -p z00z_wallets --release test_scenario1_semantics -- --nocapture`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; if the wire-contract change affects broader release validation, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>The regular-spend wire contract and structural rule contract both expose one explicit nullifier surface, and no duplicate or shadow field exists.</done>
</task>

<task type="auto">
  <name>034-04 Regular-Spend Verifier And Rule Integration</name>
  <files>crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/src/core/tx/spend_verification.rs, crates/z00z_wallets/src/core/tx/spend_rules.rs, crates/z00z_wallets/tests/test_spend_witness_gate.rs, crates/z00z_simulator/tests/test_scenario1_spend_gate.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-04` in `034-TODO.md`. Add deterministic nullifier derivation using the exact helper, domain symbol, and fixed-width `chain_id` encoding frozen in `034-03`, emit `nullifier_hex` through the witness bridge and public proof construction path, and reject missing or malformed nullifier hex, signed nullifier drift, and duplicate nullifiers inside one spend contract. Mirror deterministic `chain_id || s_in` recomputation in `verify_spend_rules(...)` by calling the same canonical helper from the structural inputs instead of cloning raw concatenation or using a weaker field-only fallback so public and structural contracts cannot drift. Use the existing spend verifier and witness gate; do not introduce a second verifier surface. Add the exact negative and positive tests listed in `034-TODO.md`, keeping public-boundary rejection parity for missing, malformed, duplicate, and signed-field drift, plus structural-layer rejection for deterministic mismatch and duplicate inputs.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run the exact wallet tests touched by this task, including `cargo test -p z00z_wallets --release test_spend_witness_gate -- --nocapture` and the internal `spend_verification.rs` cases.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_scenario1_spend_gate -- --nocapture`, then `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; if the change crosses wider workspace seams, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>The live regular public spend contract, witness bridge, and structural spend rules all bind the same deterministic regular-spend nullifier semantics surface through one canonical helper, one exact domain symbol, and one fixed-width `chain_id` framing. The public verifier fail closes on missing, malformed, duplicate, and post-signature nullifier drift, while the witness and structural paths fail close on deterministic mismatch and duplicate nullifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tx wire -> public spend verifier | Untrusted spend input fields are parsed and validated before acceptance. |
| witness bridge -> rule verifier | Derived nullifier semantics must stay aligned across public and structural validation paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-04 | Tampering | tx_wire_types.rs nullifier field | mitigate | Add one explicit nullifier field and reject missing, malformed, or mismatched values on both the public verifier and structural rule paths. |
| T-034-05 | Denial of service | spend_verification.rs duplicate nullifiers | mitigate | Fail closed on duplicate nullifiers inside one spend contract before downstream state mutation, with the same rejection semantics mirrored in structural verification. |
| T-034-06 | Elevation of privilege | alternate spend verifier seam | mitigate | Reuse the existing witness gate and public verifier instead of introducing a parallel proof layer. |
</threat_model>

<verification>
Both tasks must run in order. The plan is only green when the same deterministic nullifier semantics surface exists across domains, wire types, witness derivation, public verification, and structural rule verification, with the user-mandated repeat-review loop clean twice consecutively.</verification>

<success_criteria>
Q64 is closed by code and tests rather than by wording-only narrowing, and the implementation extends the truthful live spend seam instead of creating a duplicate verifier stack.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-02-SUMMARY.md`.
</output>