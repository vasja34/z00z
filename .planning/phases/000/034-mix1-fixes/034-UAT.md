---
status: complete
phase: 034-mix1-fixes
source:
  - 034-01-SUMMARY.md
  - 034-02-SUMMARY.md
  - 034-03-SUMMARY.md
  - 034-04-SUMMARY.md
  - 034-05-SUMMARY.md
  - 034-06-SUMMARY.md
  - 034-07-SUMMARY.md
  - 034-08-SUMMARY.md
  - 034-09-SUMMARY.md
started: 2026-04-10T00:00:00Z
updated: 2026-04-11T00:00:00Z
---

# Phase 034 UAT

## Current Test

number: 0
name: No pending UAT step remains
expected: |
  Phase 034 UAT is closed on the summary-backed execution chain through
  `034-09-SUMMARY.md`. If revalidation is needed, rerun the exact commands
  recorded in the Phase 034 summaries and validation artifact.
awaiting: none

## Tests

### 1. Claim continuity stays bound to persisted membership and rejects empty bundles fail closed

expected: Run `cargo test -p z00z_storage --release --test test_claim_source_proof -- --nocapture` and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_claim_gate -- --nocapture`. The storage-backed claim-source path should stay authoritative, synthetic one-item fallback should not become accepted authority, and empty or malformed claim bundles should fail closed instead of loading or patching successfully.
result: [passed]

### 2. Spend nullifier semantics stay chain-bound and tampered Stage 4 packages reject before deserialize

expected: Run `cargo test -p z00z_wallets --release --test test_spend_witness_gate -- --nocapture`, `cargo test -p z00z_wallets --release --test test_scenario1_semantics -- --nocapture`, and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_spend_gate -- --nocapture`. Missing, malformed, duplicate, or drifted `nullifier_hex` values should fail closed, and tampered Stage 4 package chain metadata should reject on the verified-package path instead of being accepted after raw deserialize.
result: [passed]

### 3. Sender construction remains stealth-owned and the old tx builder authority stays retired

expected: Run `cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump --test test_s5_spec6_bridge --test test_spend_witness_gate --test test_s7_examples` and inspect the active public story in `crates/z00z_wallets/src/core/stealth/mod.rs` plus `crates/z00z_wallets/src/core/tx/mod.rs`. Live callers should keep building outputs through the stealth-owned surface, while historical `core::tx::builder::*` and `core::tx::output_flow::*` public construction authority should stay retired rather than quietly coming back.
result: [passed]

### 4. Checkpoint acceptance remains backend-owned across finalize reload and Scenario 1 promotion

expected: Run `cargo test -p z00z_storage --release --test test_checkpoint_finalization -- --nocapture`, `cargo test -p z00z_storage --release --test test_checkpoint_store_api -- --nocapture`, `cargo test -p z00z_storage --release --test test_redb_rehydrate -- --nocapture`, and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_checkpoint_acceptance -- --nocapture`. Compat-only proof objects and unsupported proof-system bytes should reject fail closed, while finalize, persisted reload, and simulator promotion should stay aligned to the same backend-owned payload truth.
result: [passed]

### 5. Phase-owned semantic validation still runs through exact integration binaries instead of false-green filters

expected: Inspect `.planning/phases/034-mix1-fixes/034-06-PLAN.md` and rerun the named commands from `034-06-SUMMARY.md` with explicit `--test` binaries. The validation story should execute real integration tests rather than `running 0 tests`, and the broader simulator release gate should remain green on `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`.
result: [passed]

### 6. Active documentation now describes the closed Phase 034 seams truthfully

expected: Inspect `.planning/REQUIREMENTS.md`, `.planning/phases/034-mix1-fixes/034-CONTEXT.md`, `.planning/phases/040-spend-proof/040-Spend-Proof-Spec.md`, `crates/z00z_simulator/src/scenario_1/stage_12.rs`, and `crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs`. The active wording should match the implemented truth for claim continuity, spend nullifier closure, stealth-owned sender authority, and backend-coupled checkpoint acceptance without falling back to stale blocker wording or overclaiming standalone proof authority.
result: [passed]

### 7. Phase 034 closeout package proves the main semantic chain is closed and leaves only optional sidecars

expected: Inspect `.planning/phases/034-mix1-fixes/034-VALIDATION.md`, `.planning/phases/034-mix1-fixes/034-CLOSEOUT.md`, `.planning/phases/034-mix1-fixes/034-08-SUMMARY.md`, `.planning/phases/034-mix1-fixes/034-TODO.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`. The active planning truth should show the Phase 034 main semantic chain as closed through `034-08`, while `034-09` is framed only as optional post-closure hygiene rather than unfinished semantic work.
result: [passed]

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None.
