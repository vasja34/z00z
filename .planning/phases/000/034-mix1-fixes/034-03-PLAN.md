---
phase: 034-mix1-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - 034-02
files_modified:
  - crates/z00z_wallets/src/core/stealth/output.rs
  - proposed new crates/z00z_wallets/src/core/stealth/output_leaf.rs only if the owner split is proven necessary
  - crates/z00z_wallets/src/core/tx/builder.rs
  - crates/z00z_wallets/src/core/tx/output_flow.rs
  - crates/z00z_wallets/src/core/tx/mod.rs
  - crates/z00z_wallets/src/core/tx/witness_gate.rs
  - crates/z00z_wallets/src/core/tx/lifecycle.rs
  - crates/z00z_wallets/examples/wallet_reload.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/output_construction_balance.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_test_support.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_tests.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bridge_output_router.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs
  - crates/z00z_simulator/tests/test_claim_acceptance.rs
  - crates/z00z_wallets/src/services/wallet_service_tests.rs
  - crates/z00z_wallets/src/core/stealth/test_output_extra.rs
  - crates/z00z_wallets/tests/test_s5_sender_examples.rs
  - crates/z00z_wallets/tests/test_s5_spec6_bridge.rs
  - crates/z00z_wallets/tests/test_adversarial.rs
  - crates/z00z_wallets/tests/test_phase14_pipeline.rs
  - crates/z00z_wallets/tests/test_phase15_regress.rs
  - crates/z00z_wallets/tests/test_spend_witness_gate.rs
  - crates/z00z_wallets/tests/test_tx_serial.rs
  - crates/z00z_wallets/tests/test_s5_leaf_gate.rs
autonomous: true
requirements:
  - PH34-SENDER-AUTHORITY
must_haves:
  truths:
    - `core::stealth` becomes the only public sender-construction authority.
    - Live callers no longer treat legacy `core::tx` wrappers as canonical construction owners.
  artifacts:
    - stealth-owned full-leaf adapter exists where needed.
    - `core::tx` exports no longer advertise legacy construction authority.
    - simulator, example, lifecycle, and wallet callers are migrated.
  key_links:
    - stealth-owned builder helpers -> witness gate and lifecycle callers.
    - simulator Stage 4/6 routing -> stealth-owned construction path.
---

# Phase 034 Plan 03

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Execute the legacy sender-authority retirement as one linear task, exactly as scoped in `034-TODO.md`.

Purpose: move construction ownership to the already-live `core::stealth` surface without blind deletion or behavior drift.
Output: migrated callers, narrowed `core::tx` facade, and retired legacy construction authority.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
@.planning/phases/034-mix1-fixes/034-fix-spec-4.md
</context>

<tasks>

<task type="auto">
  <name>034-05 Legacy Sender-Construction Authority Retirement</name>
  <files>crates/z00z_wallets/src/core/stealth/output.rs, proposed new crates/z00z_wallets/src/core/stealth/output_leaf.rs only if the owner split is proven necessary, crates/z00z_wallets/src/core/tx/builder.rs, crates/z00z_wallets/src/core/tx/output_flow.rs, crates/z00z_wallets/src/core/tx/mod.rs, crates/z00z_wallets/src/core/tx/witness_gate.rs, crates/z00z_wallets/src/core/tx/lifecycle.rs, crates/z00z_wallets/examples/wallet_reload.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/output_construction_balance.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_test_support.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_tests.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bridge_output_router.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs, crates/z00z_simulator/tests/test_claim_acceptance.rs, crates/z00z_wallets/src/services/wallet_service_tests.rs, crates/z00z_wallets/src/core/stealth/test_output_extra.rs, crates/z00z_wallets/tests/test_s5_sender_examples.rs, crates/z00z_wallets/tests/test_s5_spec6_bridge.rs, crates/z00z_wallets/tests/test_adversarial.rs, crates/z00z_wallets/tests/test_phase14_pipeline.rs, crates/z00z_wallets/tests/test_phase15_regress.rs, crates/z00z_wallets/tests/test_spend_witness_gate.rs, crates/z00z_wallets/tests/test_tx_serial.rs, crates/z00z_wallets/tests/test_s5_leaf_gate.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-05` in `034-TODO.md`. Re-home genuinely low-level leaf-building helpers under `core::stealth`, introduce one canonical full-leaf adapter under `core::stealth` only if the owner split is proven necessary, and migrate all live simulator, example, lifecycle, and wallet callers away from `sender_create_output_for(...)`, `create_output_bundle(...)`, and `create_output_bundle_with_rng(...)` as public construction authority. Run a workspace grep over all legacy entrypoints listed in `034-TODO.md` before deleting or hard-blocking any wrapper, migrate every live caller that still treats `core::tx` as the construction owner, then narrow `output_flow.rs` to tx-level bridge utilities and retire `derive_fee_commitment(...)` only if migrated callers no longer need it. Completion also requires a public-surface guard: `core::tx` must no longer re-export the retired construction entrypoints as canonical public APIs, and any temporary shim that remains must fail closed with an explicit migration error instead of staying silently usable. Preserve behavior, serial constraints, and adversarial fail-closed properties, including the migrated witness-gate helper path. Do not delete `output_flow.rs` blindly and do not create a new parallel construction layer.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run the exact wallet and simulator tests named in `034-TODO.md`, including `test_spend_witness_gate`, `test_s5_sender_examples`, `test_s5_spec6_bridge`, `test_adversarial`, `test_phase14_pipeline`, `test_phase15_regress`, `test_tx_serial`, `test_s5_leaf_gate`, and the Stage 4 runtime tests touched by the migration.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`, then run `cargo test --release --features test-fast --features wallet_debug_dump` because this task migrates cross-crate caller ownership.
4. Run a workspace grep proving no live caller still depends on the retired legacy construction wrappers as canonical owner surfaces.
5. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
6. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>`core::stealth` is the only public sender-construction authority, `core::tx` no longer teaches or re-exports legacy construction APIs as canonical entry points, any retained shim fails closed with an explicit migration error, and the required migration plus grep proof is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public caller -> sender construction API | Callers must resolve to one canonical owner surface. |
| simulator Stage 4/6 helpers -> wallet construction helpers | Runtime wiring must not preserve hidden fallback authority through legacy wrappers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-07 | Spoofing | `core::tx` public exports | mitigate | Remove or hard-block legacy public construction entrypoints after all live callers migrate. |
| T-034-08 | Tampering | output construction helpers | mitigate | Preserve low-level helper behavior under `core::stealth` and revalidate serial and adversarial tests. |
| T-034-09 | Repudiation | legacy-wrapper grep proof | mitigate | Require a workspace grep sweep and source-text guard updates so the owner surface cannot silently drift back. |
</threat_model>

<verification>
This plan is green only when all migrated callers stay on the stealth-owned path, no live caller uses the old `core::tx` owner surface, release-style validation is green, and the repeated YOLO review loop converges cleanly twice consecutively.</verification>

<success_criteria>
The Phase 034 sender-authority requirement is satisfied without blind deletion, without a new parallel layer, and without reintroducing stale `builder.rs` or `output_flow.rs` authority claims.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-03-SUMMARY.md`.
</output>