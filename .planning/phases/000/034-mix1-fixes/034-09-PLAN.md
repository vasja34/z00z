---
phase: 034-mix1-fixes
plan: 09
type: execute
wave: 9
depends_on:
  - 034-08
files_modified:
  - crates/z00z_wallets/src/db/redb_wallet_crypto.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_open_session.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs
  - crates/z00z_wallets/src/core/backup/backup_wire.rs
  - crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
  - crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
  - crates/z00z_wallets/src/core/key/seed_cipher_params.rs
  - crates/z00z_wallets/src/core/key/seed_cipher_container.rs
  - crates/z00z_storage/src/checkpoint/artifact_stmt.rs
  - crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs
  - crates/z00z_storage/src/checkpoint/artifact_final.rs
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs
  - crates/z00z_crypto/src/crypto_constants.rs
  - crates/z00z_crypto/src/types_validation.rs
autonomous: true
requirements:
  - PH34-ID-SIGNATURE-HYGIENE
  - PH34-SUFFIX-COLLAPSE
must_haves:
  truths:
    - Optional sidecars are inventory-backed and remain outside the semantic closure story.
    - Non-Tari rename hygiene and production-current suffix collapse do not rewrite protected vendor code or on-wire semantics.
  artifacts:
    - a fresh non-Tari >5-word identifier inventory exists.
    - a live legacy-collision map exists before any production-current suffix collapse starts.
    - suffix changes use `034-suffixes-V1-Vn.md` as the authoritative inventory, with any normalized English execution list treated as a subordinate implementation aid.
  key_links:
      - rename inventory -> bounded workspace cleanup batches, targeted crate tests, and grep guards.
      - collision map -> blocker retirement before suffix collapse.
      - suffix inventory -> compatibility paths kept for live read/open/migration support.
---

# Phase 034 Plan 09

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Schedule the two post-closure hygiene sidecars exactly as optional, inventory-backed work.

Purpose: preserve the user's instruction to schedule every TODO item while keeping post-closure hygiene explicitly separate from Q63/Q64/Q65/Q47 evidence.
Output: non-Tari identifier-hygiene cleanup and production-current suffix collapse, if executed, with honest compatibility preservation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
@.planning/phases/034-mix1-fixes/034-suffixes-V1-Vn.md
</context>

<tasks>

<task type="auto">
  <name>034-16 Optional Bounded 5-Word Signature Compliance Wave</name>
  <files>workspace-wide non-Tari Rust files matched by the fresh identifier inventory; explicit exclusion: crates/z00z_crypto/tari/**; do not pre-commit to additional files before the inventory proves they are in scope</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-16` in `034-TODO.md` and the local Rust rules in `.github/copilot-instructions.md`. Build a fresh declaration-backed inventory of live Rust signature-like identifiers longer than 5 words across workspace crates, explicitly excluding `crates/z00z_crypto/tari/**`, and freeze one bounded execution inventory before renames begin. Execute that inventory as a full workspace cleanup batch, rename only current live non-Tari violations needed to satisfy the policy, preserve behavior and public meaning, and update call sites, assertions, grep guards, and source-text contract surfaces honestly. Do not treat this wave as evidence that the semantic blockers were closed.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run targeted crate tests for every renamed surface and run workspace grep checks that prove the old overlong names no longer remain canonical outside intentional historical references.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump` if the rename inventory touches simulator-facing guards; otherwise run only the targeted crates plus `cargo test --release --features test-fast --features wallet_debug_dump` when the executor confirms cross-crate rename fallout.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>One bounded workspace-wide execution inventory is fully consumed, all selected live non-Tari >5-word signature violations are renamed consistently, test and text guards are updated, and the wave remains explicitly outside the semantic completion story for Phase 034.</done>
</task>

<task type="auto">
  <name>034-17 Optional Legacy Collision Retirement Sidecar</name>
  <files>Only the Rust files, tests, and docs proven by the collision map to hold legacy unsuffixed blockers that occupy future production-current canonical targets; current confirmed examples include `crates/z00z_crypto/src/claim/proof.rs` and `crates/z00z_wallets/src/core/backup/wallet_backup_container.rs` plus any directly affected compatibility shims, tests, and docs.</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-17` in `034-TODO.md` and the fixed table plus bottom-line rules in `034-suffixes-V1-Vn.md`. Use that document as the authoritative suffix inventory for this sidecar, then build a current-tree collision map for every production-current suffix target whose future unsuffixed canonical name is already occupied by a live legacy symbol. Retire, rename, or narrow only those legacy blockers needed to free the later canonical targets, without changing persisted version values, header bytes, discriminators, or live compatibility semantics. Update directly affected tests, source-text guards, grep expectations, and documentation wherever the retired legacy blockers were still described as canonical.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run targeted crate tests for every retired or renamed legacy blocker and any compatibility tests still needed for old-format support.
3. Run workspace grep checks and source-text guards proving the retired legacy blockers no longer occupy the future canonical targets needed by the later suffix collapse, and update affected documentation references in the same pass. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump` if simulator-facing guards are touched; otherwise run the targeted crates and the broader `cargo test --release --features test-fast --features wallet_debug_dump` only when cross-crate fallout requires it.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Live legacy blockers that would collide with the planned unsuffixed production-current names are retired, renamed, or narrowed safely, affected tests and text guards are updated, and the sidecar remains explicitly outside the semantic completion story for Phase 034.</done>
</task>

<task type="auto">
  <name>034-18 Optional Production-Current Suffix Collapse Sidecar</name>
  <files>Only the Rust files, tests, and docs that `034-suffixes-V1-Vn.md` marks as production-current suffix surfaces plus any directly affected compatibility readers or migration paths; candidate examples currently mentioned by the inventory include `crates/z00z_wallets/src/db/redb_wallet_crypto.rs`, `crates/z00z_wallets/src/db/redb_wallet_store_open_session.rs`, `crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs`, `crates/z00z_wallets/src/core/backup/backup_wire.rs`, `crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs`, `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs`, `crates/z00z_wallets/src/core/key/seed_cipher_params.rs`, `crates/z00z_wallets/src/core/key/seed_cipher_container.rs`, `crates/z00z_storage/src/checkpoint/artifact_stmt.rs`, `crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs`, `crates/z00z_storage/src/checkpoint/artifact_final.rs`, `crates/z00z_crypto/src/claim/v2.rs`, `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`, `crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs`, `crates/z00z_crypto/src/crypto_constants.rs`, and `crates/z00z_crypto/src/types_validation.rs`</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-18` in `034-TODO.md` and the fixed table plus bottom-line rules in `034-suffixes-V1-Vn.md`. Use that document as the authoritative suffix inventory for this sidecar, optionally normalizing its live rows into a stable English execution inventory for implementation convenience, and treat any file list as provisional until the source inventory proves it is live. After the legacy blockers from `034-17` are gone, rename production-current Rust-facing structs, constants, tags, and helper types to unsuffixed canonical names where the authoritative inventory marks them as current production shapes, without changing persisted version values, header bytes, discriminators, or compatibility semantics. Update directly affected tests, source-text guards, grep expectations, and documentation wherever the renamed production-current surfaces are still referenced. Delete or retire reserved-future suffix-bearing code only where the authoritative source-backed inventory says the current production path does not still select them for read, import, open-session, or migration support. Do not blindly remove compatibility readers or migration constants still marked live.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run targeted crate tests for every renamed production-current surface and targeted compatibility tests for every reserved-future path kept for old-format read or migration support.
3. Run workspace grep checks and source-text guards proving the old suffixed production-current names no longer remain canonical outside intentional compatibility surfaces, and update affected documentation references in the same pass. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump` if simulator-facing guards are touched; otherwise run the targeted crates and the broader `cargo test --release --features test-fast --features wallet_debug_dump` only when cross-crate fallout requires it.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Production-current suffix-bearing names have unsuffixed canonical Rust-facing surfaces, reserved-future compatibility paths are kept or retired exactly per the source-backed inventory, and the sidecar remains explicitly outside the semantic completion story for Phase 034.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identifier inventory -> renamed call sites | Rename hygiene must not spill into protected vendor code or semantic behavior changes. |
| suffix inventory -> compatibility paths | Production-current cleanup must not remove live read/open/migration support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-22 | Tampering | rename hygiene sidecar | mitigate | Use a fresh non-Tari inventory, update all guards honestly, and re-run targeted tests. |
| T-034-23 | Tampering | legacy collision retirement sidecar | mitigate | Use `034-suffixes-V1-Vn.md` plus a current-tree collision map to retire only the live blockers needed for the later suffix collapse. |
| T-034-25 | Tampering | suffix collapse sidecar | mitigate | Use `034-suffixes-V1-Vn.md` as the authoritative suffix inventory after the legacy blockers are removed, and preserve live compatibility paths exactly where still required. |
| T-034-24 | Elevation of privilege | protected Tari vendor surface | accept | Explicitly exclude `crates/z00z_crypto/tari/**` from the rename sidecar and keep vendor code untouched. |
</threat_model>

<verification>
This plan is green only when the bounded rename wave, the legacy collision retirement sidecar, and the later suffix collapse all remain inventory-backed, behavior-preserving, and clearly outside the semantic closure story, with the repeated YOLO review loop converging cleanly twice consecutively.</verification>

<success_criteria>
All 17 Phase 034 TODO items are scheduled one after another, including the optional post-closure sidecars, without collapsing protected vendor boundaries or compatibility semantics.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-09-SUMMARY.md`.
</output>