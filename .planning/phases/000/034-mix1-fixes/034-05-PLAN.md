---
phase: 034-mix1-fixes
plan: 05
type: execute
wave: 5
depends_on:
  - 034-04
files_modified:
  - crates/z00z_storage/tests/test_claim_source_proof.rs
  - crates/z00z_wallets/tests/test_spend_witness_gate.rs
  - crates/z00z_storage/tests/test_checkpoint_finalization.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
  - crates/z00z_wallets/tests/test_s5_sender_examples.rs
  - crates/z00z_simulator/tests/test_claim_acceptance.rs
autonomous: true
requirements:
  - PH34-CLAIM-CONTINUITY
  - PH34-SPEND-NULLIFIER
  - PH34-CHECKPOINT-BACKEND
must_haves:
  truths:
    - Each major Phase 034 seam has one truthful primary test home.
    - Q63 claim continuity is proven against persisted membership, not helper-owned reconstruction.
  artifacts:
    - Selected seam-home tests are explicit and duplicate stale helper paths are removed.
    - Claim continuity validation tests are green on the canonical seam-home files.
  key_links:
    - seam-home selection -> later validation waves.
    - claim continuity tests -> migrated storage-backed claim seam.
---

# Phase 034 Plan 05

<!-- markdownlint-disable MD033 MD041 MD022 MD047 MD055 MD056 MD060 -->

<objective>
Lock the validation harness before broader closeout and run the first claim-focused validation wave.

Purpose: freeze one truthful test home per seam so later validation cannot drift back to old helper-based assumptions.
Output: harness lock-in and green Q63 validation wave.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/034-mix1-fixes/034-CONTEXT.md
@.planning/phases/034-mix1-fixes/034-TODO.md
</context>

<tasks>

<task type="auto">
  <name>034-08 Harness And Seam-Reuse Lock-In</name>
  <files>crates/z00z_storage/tests/test_claim_source_proof.rs, crates/z00z_wallets/tests/test_spend_witness_gate.rs, crates/z00z_storage/tests/test_checkpoint_finalization.rs, crates/z00z_simulator/tests/test_scenario1_stage_surface.rs, crates/z00z_wallets/tests/test_s5_sender_examples.rs, crates/z00z_simulator/tests/test_claim_acceptance.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-08` in `034-TODO.md`. Assign one canonical primary test home for each major seam: claim continuity, spend nullifier semantics, legacy sender migration, checkpoint backend, and wording guards. Treat `crates/z00z_wallets/tests/test_s5_sender_examples.rs` and `crates/z00z_simulator/tests/test_claim_acceptance.rs` as mandatory sender-migration seam-home proof anchors. Remove duplicate fixture helpers where they preserve the old truth, but do not widen this task into new product logic. The selected seam homes must stay aligned with the file-first order and phase-local anti-drift rules already frozen in `034-CONTEXT.md`.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run the exact seam-home tests touched by this task, including the claim, spend, checkpoint, stage-surface, and sender-migration suites selected as canonical homes.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; if the harness lock-in changes cross-crate seam routing, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>Every major Phase 034 seam has one truthful primary test home, including explicit sender seam-home proof anchors, and no ambiguous duplicate helper path remains.</done>
</task>

<task type="auto">
  <name>034-09 Claim Continuity Test Wave</name>
  <files>crates/z00z_storage/tests/test_claim_source_proof.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs</files>
  <action>Before edits, reread the mandatory pre-read sections named under `034-09` in `034-TODO.md`. Run the storage and simulator claim-continuity tests against the migrated seam from Plans 01 and 05. Prove that persisted membership state, not helper reconstruction, is the live source of truth. Keep the validation task narrow: it should amplify and tighten the existing canonical test homes rather than creating additional validation surfaces.</action>
  <verify>
    <automated>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`.
2. Run `cargo test -p z00z_storage --release test_claim_source_proof -- --nocapture` and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump test_claim_pkg_crypto_support -- --nocapture`.
3. Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`; if validation fallout crosses beyond simulator, also run `cargo test --release --features test-fast --features wallet_debug_dump`.
4. Use `.github/prompts/gsd-review-tasks-execution.prompt.md` to run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue until 2 consecutive runs report no significant issues.
5. Use `/z00z-git-versioning` for task commits; do not use raw `git commit`.</automated>
  </verify>
  <done>The Q63 claim-continuity blocker is no longer reproducible in its old form and the selected seam-home tests prove persisted membership is authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical seam-home tests -> future validation waves | Later validation depends on the chosen test homes staying truthful. |
| storage-backed claim seam -> simulator validation | Claim continuity is proven across crate boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-034-13 | Repudiation | seam-home selection | mitigate | Freeze one canonical test home per seam and remove duplicate helpers that preserve old truths. |
| T-034-14 | Tampering | claim continuity regression coverage | mitigate | Strengthen positive and negative Q63 tests on the selected seam homes only. |
</threat_model>

<verification>
This plan is green only when the seam-home selection is unambiguous, the Q63 validation wave is green, release-style checks are green, and the repeated YOLO review loop converges cleanly twice consecutively.</verification>

<success_criteria>
The Phase 034 validation harness is locked to truthful seam homes and the claim continuity migration has repository-backed validation proof before later waves proceed.</success_criteria>

<output>
After completion, create `.planning/phases/034-mix1-fixes/034-05-SUMMARY.md`.
</output>