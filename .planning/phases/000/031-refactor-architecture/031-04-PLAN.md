<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "04"
type: execute
wave: 1
depends_on:
  - 031-01
files_modified:
  - crates/z00z_networks/rpc/src/lib.rs
  - crates/z00z_networks/rpc/src/dispatcher.rs
  - crates/z00z_networks/rpc/src/transport.rs
  - crates/z00z_networks/rpc/src/local_transport.rs
  - crates/z00z_networks/rpc/src/error.rs
  - crates/z00z_networks/onionnet/Cargo.toml
  - crates/z00z_networks/onionnet/README.md
  - crates/z00z_networks/onionnet/src/lib.rs
autonomous: true
requirements:
  - PH31-NET
must_haves:
  truths:
    - `z00z_networks_rpc` remains a transport-focused crate rather than an accidental catch-all network contract.
    - OnionNet ownership is documented explicitly as a node-owned privacy overlay and not silently merged into generic RPC semantics.
    - Transport lifecycle, auth, retry, and peer-identity seams are named explicitly enough that later implementation can land without reshuffling the namespace.
    - OnionNet ownership notes are paired with a crate-shaped placeholder seam that already matches the Phase 115 module map so future overlay work lands behind explicit interfaces instead of ad hoc modules.
  artifacts:
    - path: crates/z00z_networks/rpc/src/lib.rs
      provides: Explicit RPC transport facade with limited scope.
    - path: crates/z00z_networks/onionnet/README.md
      provides: Proposed new ownership note for the overlay namespace while code remains intentionally minimal.
    - path: crates/z00z_networks/onionnet/Cargo.toml
      provides: Dedicated OnionNet crate boundary under the existing network namespace.
    - path: crates/z00z_networks/onionnet/src/lib.rs
      provides: Crate-shaped placeholder seam with the Phase 115 module map reserved in place.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_networks/rpc/src/lib.rs
      via: D-11 through D-14 network ownership requirements
      pattern: D-11|D-12|D-13|D-14|rpc|onionnet
    - from: .planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
      to: crates/z00z_networks/rpc/src/transport.rs
      via: caller proof for transport-lifecycle and auth seams
      pattern: rpc|transport|dispatcher
---

<objective>
Make the `z00z_networks` ownership model explicit before wallet and simulator cleanup depend on it.

Purpose: prevent `rpc` from becoming the accidental whole-network boundary and preserve OnionNet as a node-owned privacy overlay with an explicit namespace contract.
Output: one explicit RPC transport contract and one written OnionNet ownership note.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/115-onionnet/115-OnionSpec.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@.planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
@crates/z00z_networks/rpc/src/lib.rs
@crates/z00z_networks/rpc/src/dispatcher.rs
@crates/z00z_networks/rpc/src/transport.rs
@crates/z00z_networks/rpc/src/local_transport.rs
@crates/z00z_networks/rpc/src/error.rs
</context>

If execution needs checkpoint commits while ownership notes and transport interfaces are synchronized, use `/z00z-git-versioning` rather than ad hoc git flows.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow the RPC crate to transport and dispatch concerns only</name>
  <files>crates/z00z_networks/rpc/src/lib.rs, crates/z00z_networks/rpc/src/dispatcher.rs, crates/z00z_networks/rpc/src/transport.rs, crates/z00z_networks/rpc/src/local_transport.rs, crates/z00z_networks/rpc/src/error.rs</files>
  <action>Refactor the crate root and module docs so `z00z_networks_rpc` clearly owns RPC transport, request dispatch, and transport-local testing helpers only. Name the missing but required higher-level concerns explicitly, including peer identity, authentication, retry policy, and connection lifecycle, so they are not smuggled into dispatcher or transport abstractions later. Do not create a fake full network stack in this plan; instead, make the current contract precise and intentionally limited.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "peer|auth|retry|connection|transport|dispatcher|local transport|rpc" crates/z00z_networks/rpc/src -g '*.rs'
cargo test -p z00z_networks_rpc --release -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The RPC crate documents and enforces its limited ownership scope instead of acting like the whole network stack.</done>
</task>

<task type="auto">
  <name>Task 2: Write the OnionNet ownership note without inventing a fake implementation</name>
  <files>crates/z00z_networks/onionnet/Cargo.toml, crates/z00z_networks/onionnet/README.md, crates/z00z_networks/onionnet/src/lib.rs</files>
  <action>Create the currently absent OnionNet crate under `crates/z00z_networks/onionnet` so Phase 031 leaves behind the same namespace shape that Phase 115 intends to implement. The ownership note under `README.md` must still state that OnionNet is a node-owned privacy overlay, not a separate application service and not a transport alias for RPC, but the placeholder seam must now be crate-shaped rather than a loose folder stub. Reserve the Phase 115 module map directly in `src/lib.rs` so future implementation lands in place under `config`, `identity`, `bootstrap`, `transport_quic`, `link_crypto`, `packet`, `sphinx_path`, `session`, `bridge_api`, `edge`, `relay`, `exit`, and `telemetry` instead of forcing a later namespace redesign.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "OnionNet|overlay|RPC|transport|peer identity|privacy|placeholder|interface|transport_quic|sphinx_path|bridge_api|telemetry" crates/z00z_networks/onionnet crates/z00z_networks/rpc -g '*.md' -g '*.rs' -g 'Cargo.toml'</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The `onionnet` namespace now has an explicit ownership note plus a crate-shaped placeholder seam that already reserves the Phase 115 module boundaries, and later phases cannot treat it as an undefined folder or force a namespace move.</done>
</task>

</tasks>

<verification>
Wave 1 network work closes only when `rpc` has one explicit limited contract and `onionnet` has one explicit ownership note.
</verification>

<success_criteria>
The repository has a clear `rpc` versus `onionnet` ownership split without needing a crate reshuffle.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-04-SUMMARY.md`
</output>