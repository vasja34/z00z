<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "02"
type: execute
wave: 1
depends_on:
  - 031-01
files_modified:
  - crates/z00z_core/src/lib.rs
  - crates/z00z_core/src/assets/mod.rs
  - crates/z00z_core/src/assets/wire_pkg.rs
  - crates/z00z_core/src/assets/wire_pkg_serde_impls.rs
  - crates/z00z_core/src/genesis/mod.rs
  - crates/z00z_core/README.md
autonomous: true
requirements:
  - PH31-CORE
must_haves:
  truths:
    - `z00z_core` stops exposing a broad stable root facade driven by wildcard asset exports.
    - Tooling, config loading, and genesis helpers remain available only through explicit module entrypoints instead of broad root aliases.
    - The upstream-cap or bounded JSON import contract stays explicit and fail-closed while the facade narrows.
    - Phase 031 must explicitly choose and prove one hardening branch for shared JSON import paths: either upstream size caps are proven on every caller path, or bounded decode plus type-specific ceilings and oversized-payload rejection tests are added at the owning core seam.
  artifacts:
    - path: crates/z00z_core/src/lib.rs
      provides: Curated core facade with explicit public ownership.
    - path: crates/z00z_core/src/assets/wire_pkg.rs
      provides: Asset-wire and bounded-decode ownership anchor.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-INVENTORY.md
      to: crates/z00z_core/src/lib.rs
      via: root export inventory and caller proof for `pub use assets::*`
      pattern: pub use assets::|wildcard root export
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_core/src/assets/wire_pkg.rs
      via: D-36 upstream-cap and bounded JSON preservation
      pattern: D-36|bounded JSON|upstream-cap
---

<objective>
Narrow the `z00z_core` public root to one curated domain facade without losing the existing bounded-wire protections.

Purpose: remove broad stable exports before more callers bind to them while preserving the import ceiling and fail-closed decode rules already protecting core asset state.
Output: explicit root exports, preserved bounded wire ownership, and updated crate-level boundary documentation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@.planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
@crates/z00z_core/src/lib.rs
@crates/z00z_core/src/assets/mod.rs
@crates/z00z_core/src/assets/wire_pkg.rs
@crates/z00z_core/src/assets/wire_pkg_serde_impls.rs
@crates/z00z_core/src/genesis/mod.rs
@crates/z00z_core/README.md
</context>

If execution requires commit checkpoints while callers are being migrated to curated exports, use `/z00z-git-versioning` instead of raw git commit flows.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace broad root exports with an explicit `z00z_core` facade</name>
  <files>crates/z00z_core/src/lib.rs, crates/z00z_core/src/assets/mod.rs, crates/z00z_core/src/genesis/mod.rs</files>
  <action>Use the Wave 0 caller inventory to replace broad root re-exports with one curated set of stable domain exports. Remove `pub use assets::*` from the root surface and expose only the asset, genesis, domain, and hashing contracts that are proven active callers. Do not widen root access to CLI, generation, or tooling helpers while doing this. Where existing callers need migration, migrate them in-tree during this plan instead of preserving new compatibility aliases.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "pub use .*\\*|pub use assets::\\*" crates/z00z_core/src/lib.rs crates/z00z_core/src/assets -g '*.rs'
cargo test -p z00z_core --release --test assets_tests -- --nocapture
cargo test -p z00z_core --release --test genesis_tests -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The `z00z_core` root surface is curated, explicit, and free of wildcard stable exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the bounded JSON and upstream-cap ownership while the facade narrows</name>
  <files>crates/z00z_core/src/assets/wire_pkg.rs, crates/z00z_core/src/assets/wire_pkg_serde_impls.rs, crates/z00z_core/README.md</files>
  <action>Keep the import ceiling and bounded decode semantics explicit inside the asset-wire ownership seam. If the current contract is split across helpers, consolidate the ownership note so it is obvious which file enforces upstream-cap checks and how malformed or oversized payloads fail closed. Phase 031 must not stop at documentation here: either prove that every caller of the shared asset or genesis JSON import path already enters through an upstream size cap, or implement the fail-closed alternative by adding bounded JSON decode, type-specific ceilings, and oversized-payload rejection tests at the owning core boundary. Update the crate README to document the narrowed root and the final chosen hardening branch so bounded wire parsing remains asset-owned rather than becoming a generic utils or caller concern.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "cap|limit|max|bounded|serde|json|decode_asset_pkg_json|JsonCodec" crates/z00z_core/src/assets crates/z00z_core/src/genesis -g '*.rs'
cargo run -p z00z_core --release --example asset_config_loading --quiet
cargo run -p z00z_core --release --example asset_snapshot --quiet</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task. Review evidence must state which hardening branch won: caller-proven upstream size caps, or bounded decode with explicit oversize rejection tests.</review>
  </verify>
  <done>The narrowed `z00z_core` facade no longer hides where bounded decode policy lives, the ownership note is written into the crate docs, and the plan records one explicit fail-closed decision for shared JSON import hardening instead of leaving the branch unresolved.</done>
</task>

</tasks>

<verification>
Wave 1 core work closes only when the root facade is explicit and the bounded wire contract remains visibly owned and tested.
</verification>

<success_criteria>
`z00z_core` exposes one deliberate stable surface, and its asset-wire decode contract remains fail-closed and crate-owned.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-02-SUMMARY.md`
</output>