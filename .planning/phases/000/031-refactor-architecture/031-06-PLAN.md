<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "06"
type: execute
wave: 2
depends_on:
  - 031-02
  - 031-03
  - 031-04
  - 031-05
files_modified:
  - crates/z00z_wallets/src/services/wallet_paths.rs
  - crates/z00z_wallets/src/services/wallet_service_store_create_unlock_open.rs
  - crates/z00z_wallets/src/services/wallet_service_session_guards.rs
  - crates/z00z_wallets/src/services/wallet_service_session_runtime.rs
  - crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  - crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs
  - crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs
autonomous: true
requirements:
  - PH31-WLT-ID
must_haves:
  truths:
    - Persisted wallet identity remains the single source of truth after discovery or reopen, with the stored `(PersistWalletId, network, chain)` tuple treated as authoritative compatibility data.
    - Runtime chain or network drift cannot silently redefine wallet identity or unlock semantics.
    - `lock_wallet` is explicitly session-bound or privileged-admin only; unauthenticated transport callers cannot mutate lock state.
  artifacts:
    - path: crates/z00z_wallets/src/services/wallet_service_store_create_unlock_open.rs
      provides: Persisted wallet identity and unlock ownership seam.
    - path: crates/z00z_wallets/src/services/wallet_service_session_guards.rs
      provides: Lock authorization and session-guard ownership seam.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_wallets/tests/test_redb_wlt_open.rs
      via: G-04 drift coverage and D-40 identity-drift blocker
      pattern: G-04|identity mismatch|Path|Bytes
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs
      via: G-05 lock-wallet transport denial requirement
      pattern: G-05|lock_wallet|unauthenticated
---

<objective>
Make persisted wallet identity authoritative and bind lock mutation to an explicit authorization posture.

Purpose: close the wallet drift and lock-surface blockers before other wallet boundary cleanup proceeds, so later facade changes do not preserve a weak identity or auth contract.
Output: authoritative identity flow, explicit lock authorization semantics, and exact regression anchors for drift and transport denial.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@crates/z00z_wallets/src/services/wallet_paths.rs
@crates/z00z_wallets/src/services/wallet_service_store_create_unlock_open.rs
@crates/z00z_wallets/src/services/wallet_service_session_guards.rs
@crates/z00z_wallets/src/services/wallet_service_session_runtime.rs
@crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
@crates/z00z_wallets/tests/test_redb_wlt_open.rs
@crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs
@crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs
</context>

If execution needs checkpoint commits while migrating auth or drift callers, use `/z00z-git-versioning` rather than raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce persisted wallet identity as the only post-discovery source of truth</name>
  <files>crates/z00z_wallets/src/services/wallet_paths.rs, crates/z00z_wallets/src/services/wallet_service_store_create_unlock_open.rs, crates/z00z_wallets/tests/test_redb_wlt_open.rs, crates/z00z_wallets/tests/test_open_wallet_source_discovery.rs, crates/z00z_wallets/tests/test_wallet_service_errors.rs</files>
  <action>Refactor the discovery and open paths so the persisted wallet identity governs follow-up opens, unlocks, derive, address-encoding, export, and compatibility checks. Treat the stored `(PersistWalletId, network, chain)` tuple as the canonical identity record after discovery. Runtime chain or network hints may validate against persisted identity, but they must not redefine it, and per-wallet derive or address paths must resolve chain and network through the persisted-identity owner rather than reading runtime `Z00Z_WALLET_*` configuration after `wallet_id` is known. Preserve both `WalletSource::Path` and `WalletSource::Bytes` drift coverage in the acceptance surface, add one exact bytes-lane negative anchor such as `open_wallet_bytes_rejects_identity_mismatch`, and make any compatibility-only fallback explicit and non-authoritative.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_redb_wlt_open test_open_fails_identity_mismatch -- --exact --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery open_wallet_discovers_unlocks -- --exact --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery open_wallet_bytes_unlocks -- --exact --nocapture
cargo test -p z00z_wallets --release --test test_open_wallet_source_discovery open_wallet_bytes_rejects_identity_mismatch -- --exact --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors derive_public_key_rejects_invalid_chain_config -- --exact --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors list_addresses_use_persisted_chain_after_open_wallet_source -- --exact --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task. Review evidence must confirm both derive and emitted address paths read the persisted wallet identity rather than runtime `Z00Z_WALLET_*` configuration once the wallet is known.</review>
  </verify>
  <done>Persisted wallet identity is authoritative for open, unlock, derive, and address flows, and both path and bytes drift lanes remain covered by exact regression anchors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind `lock_wallet` to an explicit authorized caller contract</name>
  <files>crates/z00z_wallets/src/services/wallet_service_session_guards.rs, crates/z00z_wallets/src/services/wallet_service_session_runtime.rs, crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs, crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs</files>
  <action>Make `lock_wallet` either session-bound or explicitly privileged-admin only. The transport surface must reject unauthenticated callers instead of silently allowing lock-state mutation as a convenience side effect. Keep enumeration behavior explicit, but do not treat list visibility as sufficient authorization for mutation. Update wiring and tests so the auth rule is demonstrable at the RPC boundary, not merely inside service internals, and add one exact denial anchor such as `lock_wallet_rejects_unauthenticated_transport_call` to `test_rpc_wiring_spec_a.rs`.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_rpc_wiring_spec_a app_wallet_list_wallets_accepts -- --exact --nocapture
cargo test -p z00z_wallets --release --test test_rpc_wiring_spec_a lock_wallet_rejects_unauthenticated_transport_call -- --exact --nocapture
rg -n "lock_wallet|unlock_wallet|session|auth|unauth" crates/z00z_wallets/src crates/z00z_wallets/tests -g '*.rs'</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`lock_wallet` now has an explicit authorization posture and a transport-level denial proof instead of a best-effort convention.</done>
</task>

</tasks>

<verification>
Wave 2 identity and auth work closes only when G-04 and G-05 are satisfied by exact anchors, not by inferred behavior.
</verification>

<success_criteria>
Wallet identity drift is fail-closed and lock-state mutation is explicitly authorization-bound.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-06-SUMMARY.md`
</output>