<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "09"
type: execute
wave: 3
depends_on:
  - 031-05
  - 031-06
  - 031-07
  - 031-08
files_modified:
  - crates/z00z_simulator/README.md
  - crates/z00z_simulator/src/lib.rs
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - crates/z00z_simulator/src/scenario_1/stage_2.rs
  - crates/z00z_simulator/src/scenario_1/stage_2_utils/artifacts.rs
  - crates/z00z_simulator/src/scenario_1/stage_2_utils/checks.rs
  - crates/z00z_simulator/tests/test_architecture_boundaries.rs
  - crates/z00z_simulator/tests/test_wallet_integration.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
autonomous: true
requirements:
  - PH31-SIM
must_haves:
  truths:
    - `z00z_simulator` is treated as the integration harness and enters other crates only through stable facades.
    - Plaintext wallet secret artifacts are removed from the default simulator contract or are explicitly debug-only and private.
    - Output reset or recursive delete behavior is constrained to an approved simulator sandbox root.
    - The simulator owns one written README-level boundary note and one reusable scenario contract instead of letting `scenario_1` internals become the accidental framework.
    - The simulator carries one persistent failing architecture guard so deep wallet or storage imports cannot quietly return after the cleanup wave.
  artifacts:
    - path: crates/z00z_simulator/README.md
      provides: Integration-harness boundary note and documented scenario contract.
    - path: crates/z00z_simulator/src/scenario_1/runner.rs
      provides: Output reset boundary and scenario entry contract.
    - path: crates/z00z_simulator/src/scenario_1/stage_2.rs
      provides: Stage-2 wallet artifact contract and secret-output owner.
    - path: crates/z00z_simulator/tests/test_architecture_boundaries.rs
      provides: Persistent regression guard for stable-facade-only simulator imports.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_simulator/src/scenario_1/stage_2.rs
      via: G-08 simulator secret-output gate and D-17 blocker
      pattern: G-08|D-17|wlt_secrets_debug.md
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_simulator/src/scenario_1/runner.rs
      via: G-09 reset sandbox rule and D-39 blocker
      pattern: G-09|D-39|reset_outputs_dir|remove_dir_all
    - from: .planning/phases/031-refactor-architecture/031-TODO.md
      to: crates/z00z_simulator/README.md
      via: empty-README finding plus required scenario contract
      pattern: README|integration harness|scenario contract|stable facades
---

<objective>
Keep the simulator as an integration harness with stable-facade imports, no default plaintext-secret outputs, and sandboxed output reset behavior.

Purpose: close the simulator blocker cluster so scenario execution remains useful for integration validation without normalizing unsafe secrets handling or filesystem cleanup semantics.
Output: facade-only simulator imports, a gated stage-2 secret artifact policy, and sandboxed output-reset logic with release-style tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@crates/z00z_simulator/README.md
@crates/z00z_simulator/src/lib.rs
@crates/z00z_simulator/src/scenario_1/runner.rs
@crates/z00z_simulator/src/scenario_1/stage_2.rs
@crates/z00z_simulator/src/scenario_1/stage_2_utils/artifacts.rs
@crates/z00z_simulator/src/scenario_1/stage_2_utils/checks.rs
@crates/z00z_simulator/tests/test_wallet_integration.rs
@crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
</context>

If execution needs checkpoint commits while the simulator contract is being tightened, use `/z00z-git-versioning` rather than raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove default plaintext secret artifacts from the Stage 2 contract or gate them explicitly</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_2.rs, crates/z00z_simulator/src/scenario_1/stage_2_utils/artifacts.rs, crates/z00z_simulator/src/scenario_1/stage_2_utils/checks.rs, crates/z00z_simulator/tests/test_wallet_integration.rs, crates/z00z_simulator/tests/test_scenario1_stage_surface.rs</files>
  <action>Refactor Stage 2 so `wlt_secrets_debug.md` is no longer an unconditional part of the default simulator contract. If a plaintext-secret artifact remains, it must be explicit, debug-only, private by contract, and written through a private-permission path instead of the generic public file path. Update stage-surface tests and secret checks so the public scenario contract proves the new behavior instead of documenting the old debug artifact as canonical output, and require one exact debug-lane proof that any retained plaintext artifact is created only behind the debug feature lane and with private file permissions.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --test test_wallet_integration stage2_rpc_no_secrets -- --exact --nocapture
cargo test -p z00z_simulator --release --features test-fast --test test_scenario1_stage_surface test_scenario1_stage_surface -- --exact --nocapture
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_wallet_integration stage2_debug_secret_artifact_is_private -- --exact --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Stage 2 no longer treats plaintext wallet secret output as part of the default public simulator contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce facade-only simulator imports, document the harness contract, and install a persistent boundary guard</name>
  <files>crates/z00z_simulator/README.md, crates/z00z_simulator/src/lib.rs, crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/tests/test_architecture_boundaries.rs</files>
  <action>Clean up simulator imports so it enters wallet and storage crates through stable facades only, not through deep service, db, or store-internal paths. At the same time, constrain `reset_outputs_dir` and any recursive delete operation to an approved simulator sandbox root so output cleanup cannot escape the intended directory tree. Write the currently missing or empty README-level boundary note so `z00z_simulator` is explicitly documented as the integration harness for scenario execution, and include one documented scenario contract that states which stage and artifact conventions new scenarios must reuse instead of copying `scenario_1` internals ad hoc. Do not leave the import rule as a one-shot review command: add one persistent failing architecture guard test or boundary-spec artifact that fails when deep wallet/service/db/store-internal imports reappear. The acceptance contract must include one explicit negative proof that a path outside the approved simulator sandbox is rejected before any recursive delete occurs.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "integration harness|stable facades|scenario contract|stage|artifact" crates/z00z_simulator/README.md
rg -n "z00z_wallets::.*services|z00z_wallets::.*db|z00z_storage::.*store_internal" crates/z00z_simulator/src -g '*.rs'
cargo test -p z00z_simulator --release --features test-fast --test test_architecture_boundaries -- --nocapture
rg -n "reset_outputs_dir|remove_dir_all" crates/z00z_simulator/src crates/z00z_simulator/tests -g '*.rs'
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test -p z00z_simulator --release --features test-fast --test test_wallet_integration reset_outputs_dir_rejects_outside_sandbox -- --exact --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The simulator acts like an integration harness through stable facades only, the README-level scenario contract is written down, the stable-facade rule has a persistent failing guard, and output reset is sandboxed.</done>
</task>

</tasks>

<verification>
Wave 3 simulator work closes only when G-07, G-08, and G-09 are satisfied by exact release-style evidence.
</verification>

<success_criteria>
`z00z_simulator` remains useful for end-to-end validation without shipping plaintext secret artifacts or unsandboxed recursive cleanup semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-09-SUMMARY.md`
</output>