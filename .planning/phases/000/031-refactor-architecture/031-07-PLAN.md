<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "07"
type: execute
wave: 2
depends_on:
  - 031-02
  - 031-03
  - 031-04
  - 031-05
  - 031-06
files_modified:
  - crates/z00z_wallets/src/adapters/rpc/types/mod.rs
  - crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs
  - crates/z00z_wallets/src/core/tx/multi_io.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/src/core/chain/receiver_card_record.rs
  - crates/z00z_wallets/src/db/redb_wallet_store.rs
  - crates/z00z_wallets/src/lib.rs
  - crates/z00z_wallets/tests/test_receiver_card_record.rs
  - crates/z00z_wallets/tests/test_rpc_types_serialization.rs
  - crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs
  - crates/z00z_wallets/tests/test_rpc_persistence_spec_e3.rs
  - crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs
autonomous: true
requirements:
  - PH31-WLT-RPC
must_haves:
  truths:
    - Wallet RPC and DTO boundaries remain edge-owned and do not become the de facto root wallet domain model.
    - The wallet crate root stops widening stable exports through transport-facing aliases that should remain edge-local.
    - RPC serialization, roundtrip wiring, and persistence expectations remain covered as transport contracts after the surface is narrowed.
    - Wallet-core transaction paths do not keep ad hoc non-seeded randomness or concrete JSON ownership once the RPC edge contract is narrowed.
    - Named wallet compatibility lanes are explicitly dispositioned: `wallet.key.export_public_material_v2` and `ReceiverCardRecordV1` either remain the canonical live contracts or are demoted before the suffix-removal wave, but they do not stay ambiguous.
  artifacts:
    - path: crates/z00z_wallets/src/adapters/rpc/types/mod.rs
      provides: Explicit DTO and transport-type boundary.
    - path: crates/z00z_wallets/src/lib.rs
      provides: Curated wallet root facade after transport demotion.
    - path: crates/z00z_wallets/src/core/tx/multi_io.rs
      provides: Wallet-core randomness ownership boundary after transport demotion.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_wallets/src/adapters/rpc/types/mod.rs
      via: D-31 wallet core versus RPC boundary rule
      pattern: D-31|DTO|RPC|wire types
    - from: .planning/phases/031-refactor-architecture/031-INVENTORY.md
      to: crates/z00z_wallets/src/lib.rs
      via: wallet root export inventory and shim caller proof
      pattern: adapters/rpc|types|pub use
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_wallets/src/core/tx/multi_io.rs
      via: D-41 non-seeded randomness rule and M-35 wallet audit follow-up
      pattern: D-41|M-35|thread_rng|multi_io
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_wallets/src/adapters/rpc/methods/key.rs
      via: D-32 canonical compatibility-lane rule and M-31 retained obligation
      pattern: D-32|M-31|export_public_material_v2|ReceiverCardRecordV1
---

<objective>
Keep wallet transport types at the RPC edge and stop them from drifting into the stable root domain facade.

Purpose: make the wallet crate’s public shape truthful by separating edge serialization and dispatcher wiring concerns from the stable service and core contracts.
Output: a curated wallet root, an explicit RPC types boundary, and preserved transport regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@.planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
@crates/z00z_wallets/src/adapters/rpc/types/mod.rs
@crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
@crates/z00z_wallets/src/adapters/rpc/methods/key.rs
@crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs
@crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs
@crates/z00z_wallets/src/core/chain/receiver_card_record.rs
@crates/z00z_wallets/src/core/tx/multi_io.rs
@crates/z00z_wallets/src/core/tx/tx_verifier.rs
@crates/z00z_wallets/src/db/redb_wallet_store.rs
@crates/z00z_wallets/src/lib.rs
@crates/z00z_wallets/tests/test_receiver_card_record.rs
@crates/z00z_wallets/tests/test_rpc_types_serialization.rs
@crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs
@crates/z00z_wallets/tests/test_rpc_persistence_spec_e3.rs
@crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs
</context>

If execution needs checkpoint commits while migrating RPC callers, use `/z00z-git-versioning` rather than raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep DTO and dispatcher types edge-owned inside the RPC namespace</name>
  <files>crates/z00z_wallets/src/adapters/rpc/types/mod.rs, crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs</files>
  <action>Refactor the RPC namespace so DTOs, serialization helpers, and dispatcher registration stay clearly edge-owned. Remove or demote any transport-facing type aliases that are currently reachable through broader wallet root exports when they are not part of the intended stable domain contract. Keep the dispatcher wiring readable and explicit instead of burying policy in transport-specific helper aliases.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_rpc_types_serialization -- --nocapture
cargo test -p z00z_wallets --release --test test_rpc_dispatcher_roundtrip -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The RPC namespace owns its DTOs and wiring explicitly, and transport edges do not bleed into unrelated wallet root exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Curate the wallet crate root after transport demotion</name>
  <files>crates/z00z_wallets/src/lib.rs, crates/z00z_wallets/tests/test_rpc_persistence_spec_e3.rs</files>
  <action>Use the Wave 0 caller inventory to trim the wallet root down to the service, core, and explicitly supported adapter surfaces that are genuinely stable. Preserve the RPC persistence contract and roundtrip semantics, but keep transport-only shapes behind the adapters namespace instead of elevating them to broad root-level convenience exports.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "pub use .*rpc|pub use .*types|pub use .*dispatcher" crates/z00z_wallets/src/lib.rs crates/z00z_wallets/src/adapters/rpc -g '*.rs'
cargo test -p z00z_wallets --release --test test_rpc_persistence_spec_e3 -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The wallet root is curated around stable callers while RPC-specific types remain adapters-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove ad hoc wallet-core randomness and keep verifier or store JSON ownership at explicit boundaries</name>
  <files>crates/z00z_wallets/src/core/tx/multi_io.rs, crates/z00z_wallets/src/core/tx/tx_verifier.rs, crates/z00z_wallets/src/db/redb_wallet_store.rs, crates/z00z_wallets/tests/test_rpc_persistence_spec_e3.rs</files>
  <action>Close the remaining wallet audit follow-up that is not purely transport-shaped. Replace the non-seeded `rand::thread_rng()` fallback in `core/tx/multi_io.rs` with an explicit repository-approved randomness owner or an explicit entropy requirement at the call boundary so wallet-core transaction assembly does not silently bypass the hardened randomness policy. In the same pass, audit `core/tx/tx_verifier.rs` and `db/redb_wallet_store.rs` so concrete JSON values remain edge-owned or codec-owned compatibility helpers instead of becoming the default verifier or persistence representation. If compatibility decode must remain inside the crate, keep it explicitly owner-scoped and named as such rather than leaving generic JSON-value ownership in core or store logic.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
! rg -n "thread_rng" crates/z00z_wallets/src/core/tx/multi_io.rs
! rg -n "serde_json::Value" crates/z00z_wallets/src/core/tx/tx_verifier.rs crates/z00z_wallets/src/db/redb_wallet_store.rs -g '*.rs'
cargo test -p z00z_wallets --release --test test_rpc_persistence_spec_e3 -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task. Review evidence must confirm that wallet-core transaction assembly no longer owns an ad hoc non-seeded randomness fallback and that verifier or persistence seams keep concrete JSON ownership only where an explicitly named codec-compat boundary requires it.</review>
  </verify>
  <done>The wallet boundary cleanup now includes explicit ownership for non-seeded randomness and for any remaining JSON compatibility decode inside verifier or persistence seams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Explicitly disposition the named wallet compatibility lanes before Wave 4 retirement</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/key.rs, crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs, crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs, crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs, crates/z00z_wallets/src/core/chain/receiver_card_record.rs, crates/z00z_wallets/tests/test_receiver_card_record.rs, crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs</files>
  <action>Close the retained D-32 ambiguity explicitly instead of leaving it to Wave 4 inference. Audit `wallet.key.export_public_material_v2` and `ReceiverCardRecordV1` as named compatibility lanes and decide whether each remains a canonical live contract or moves behind an explicit compat or experimental lane before suffix-removal work begins. Encode that disposition in the RPC method surface, dispatcher registration, receiver-card contract, and regression tests so the repository no longer carries both a retained obligation in context and an implicit plan gap. If a lane stays canonical, prove that no parallel alternate public lane competes with it; if it is not canonical, demote or gate it before Wave 4 rather than carrying the ambiguity forward.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "wallet.key.export_public_material_v2|ReceiverCardRecordV1" crates/z00z_wallets/src crates/z00z_wallets/tests -g '*.rs'
cargo test -p z00z_wallets --release --test test_receiver_card_record -- --nocapture
cargo test -p z00z_wallets --release --test test_rpc_wiring_spec_a -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task. Review evidence must state plainly whether `wallet.key.export_public_material_v2` and `ReceiverCardRecordV1` remain canonical live contracts or are demoted before the suffix-removal wave.</review>
  </verify>
  <done>The named wallet compatibility lanes are explicitly governed, and Wave 4 no longer inherits an unresolved disposition question for `wallet.key.export_public_material_v2` or `ReceiverCardRecordV1`.</done>
</task>

</tasks>

<verification>
Wave 2 wallet boundary cleanup closes only when DTO ownership is edge-local, the wallet root no longer widens transport aliases into the stable domain surface, and the remaining wallet-core RNG or JSON audit follow-up is explicitly closed.
</verification>

<success_criteria>
Wallet RPC remains fully test-covered as an adapter contract, the wallet crate’s stable public shape is truthful, and wallet-core randomness plus verifier or persistence ownership no longer contradict the edge-only boundary rule.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-07-SUMMARY.md`
</output>