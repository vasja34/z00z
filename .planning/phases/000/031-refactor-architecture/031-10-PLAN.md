<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "10"
type: execute
wave: 4
depends_on:
  - 031-02
  - 031-03
  - 031-04
  - 031-05
  - 031-06
  - 031-07
  - 031-08
  - 031-09
files_modified:
  - crates/z00z_utils/README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/031-refactor-architecture/031-VERIFICATION.md
  - .planning/phases/031-refactor-architecture/031-RETIREMENT.md
autonomous: true
requirements:
  - PH31-UTILS
  - PH31-CLOSEOUT
must_haves:
  truths:
    - `z00z_utils` ends Phase 031 with one written admission policy or README-level boundary note that explains what belongs there and what does not.
    - No shim or suffix retirement is accepted without Wave 0 caller proof and post-change validation evidence.
    - Phase closeout does not tolerate default-public stable facades that still expose `compat`, `legacy`, `shim`, or version-suffixed names unless they are explicitly listed as migration-only exceptions.
    - Final planning truth-sync updates `ROADMAP` and `REQUIREMENTS` only after validation is green.
  artifacts:
    - path: crates/z00z_utils/README.md
      provides: Proposed new explicit utils admission and boundary policy.
    - path: .planning/phases/031-refactor-architecture/031-VERIFICATION.md
      provides: Final validation matrix and gate evidence.
    - path: .planning/phases/031-refactor-architecture/031-RETIREMENT.md
      provides: Shim and suffix retirement decisions with rollback notes.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_utils/README.md
      via: D-22 through D-25 utils admission and G-10 closeout requirement
      pattern: D-22|D-23|D-24|D-25|G-10
    - from: .planning/phases/031-refactor-architecture/031-INVENTORY.md
      to: .planning/phases/031-refactor-architecture/031-RETIREMENT.md
      via: caller-proof requirement for every removed shim or suffix
      pattern: retire|suffix|shim|caller inventory
---

<objective>
Close Phase 031 with an explicit utils boundary policy, evidence-backed shim retirement, and synchronized planning truth.

Purpose: prevent the final wave from removing seams on assumption, and ensure `z00z_utils` does not quietly remain an undocumented catch-all while the rest of the architecture tightens.
Output: one written utils admission note, one verification artifact, one retirement artifact, and synchronized planning records.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@.planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
@crates/z00z_utils/src/lib.rs
@crates/z00z_utils/src/codec/json.rs
@crates/z00z_utils/src/compression.rs
@crates/z00z_utils/src/os_hardening.rs
</context>

If execution needs checkpoint commits during closeout, use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the `z00z_utils` admission policy as a README-level boundary note</name>
  <files>crates/z00z_utils/README.md, crates/z00z_utils/src/lib.rs, crates/z00z_utils/src/codec/json.rs, crates/z00z_utils/src/compression.rs, crates/z00z_utils/src/os_hardening.rs</files>
  <action>Create the currently absent `crates/z00z_utils/README.md` as the README-level boundary note for the crate so the plan does not pretend the artifact already exists. The note must document the allowed cross-cutting roles of the crate and explicitly reject policy megacrate drift. It must explain why JSON compatibility, compression, and OS hardening are admitted on different grounds, which concerns stay outside the crate, and how future additions should be evaluated before landing.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "json|compression|os_hardening|admission|boundary|policy|cross-cutting" crates/z00z_utils/README.md crates/z00z_utils/src -g '*.md' -g '*.rs'
cargo test -p z00z_utils --release -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`z00z_utils` ends the phase with one explicit boundary note instead of unwritten policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire proven shims and suffixes only after final validation turns green</name>
  <files>.planning/phases/031-refactor-architecture/031-VERIFICATION.md, .planning/phases/031-refactor-architecture/031-RETIREMENT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Create one final verification matrix and one retirement record. The verification artifact must cite the Wave 0 inventory, the exact gate outcomes from Waves 1 through 3, and the final release-style validation results. The retirement artifact must list every shim, suffix, provisional export, or default-public compatibility lane actually removed, the caller-proof row that justified removal, and the rollback condition if validation regresses. If any migration-only exception remains, list it explicitly with its owning file, why it is still required, and why it is not a default stable facade. Only after that evidence is complete may `ROADMAP` and `REQUIREMENTS` be updated to record the planned or completed state truthfully.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump
./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run
rg -n "shim|suffix|retire|rollback|G-0|W0|W1|W2|W3|W4|PH31-" .planning/phases/031-refactor-architecture .planning/ROADMAP.md .planning/REQUIREMENTS.md -g '*.md'
rg -n "pub use .*compat|pub use .*legacy|pub use .*shim|pub .*\b(V[0-9]+|v[0-9]+)\b" crates -g '*.rs'
rg -n "\bcompat_|\blegacy_|\bshim_|\b(V[0-9]+|v[0-9]+)\b" crates -g '*.rs'</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Phase 031 closes on evidence-backed retirement and synchronized planning truth, not on assumed completion.</done>
</task>

</tasks>

<verification>
Wave 4 closes only when G-10 is satisfied, max-safe verification is green, retirement evidence is caller-proof-backed, and any remaining migration-only exception is explicitly named and proven non-default.
</verification>

<success_criteria>
Phase 031 ends with a written `z00z_utils` boundary policy, validated seam retirement, zero surviving default-public compat or versioned stable facades, and synchronized planning artifacts that match reality.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-10-SUMMARY.md`
</output>