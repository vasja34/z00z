<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "05"
type: execute
wave: 2
depends_on:
  - 031-02
  - 031-03
  - 031-04
files_modified:
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/src/services/mod.rs
  - crates/z00z_wallets/src/services/wallet_service_store_create_unlock_open.rs
  - crates/z00z_wallets/src/services/wallet_service_store_support.rs
  - crates/z00z_wallets/src/services/wallet_service_session_derivation.rs
  - crates/z00z_wallets/src/services/wallet_service_session_guards.rs
  - crates/z00z_wallets/tests/test_phase30_split.rs
  - crates/z00z_wallets/tests/test_wallet_service_errors.rs
  - .planning/phases/031-refactor-architecture/031-05-SUMMARY.md
autonomous: true
requirements:
  - PH31-WLT-SEAMS
must_haves:
  truths:
    - The canonical wallet service entrypoint is no longer assembled through `include!` and no longer hides placeholder-heavy internal seams behind one stable-looking file.
    - The service surface remains explicit about which lanes are stable, provisional, or reachability-only.
    - Session, store-open, and reachability logic keep one coherent owner surface during the split.
    - The include-cleanup plan records whether review clarity improves and what compile-time or development-velocity cost the explicit module split introduces.
  artifacts:
    - path: crates/z00z_wallets/src/services/wallet_service.rs
      provides: Stable wallet service facade with explicit module ownership.
    - path: crates/z00z_wallets/src/services/mod.rs
      provides: Shallow caller surface for the services namespace.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_wallets/src/services/wallet_service.rs
      via: G-03 wallet service split and D-28 include assembly removal
      pattern: G-03|D-28|include!
    - from: .planning/phases/031-refactor-architecture/031-INVENTORY.md
      to: crates/z00z_wallets/src/services/mod.rs
      via: placeholder seam and root-facade caller proof
      pattern: wallet service|placeholder|reachability
---

<objective>
Replace `include!`-driven wallet service assembly with explicit module ownership and a truthful caller facade.

Purpose: stop a stub-heavy and placeholder-rich service root from masquerading as a stable public contract while preserving the caller-visible wallet services boundary.
Output: explicit wallet service modules, one shallow facade, and updated source-shape regression anchors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@.planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_wallets/src/services/mod.rs
@crates/z00z_wallets/src/services/wallet_service_store_create_unlock_open.rs
@crates/z00z_wallets/src/services/wallet_service_store_support.rs
@crates/z00z_wallets/src/services/wallet_service_session_derivation.rs
@crates/z00z_wallets/src/services/wallet_service_session_guards.rs
@crates/z00z_wallets/tests/test_phase30_split.rs
@crates/z00z_wallets/tests/test_wallet_service_errors.rs
</context>

If execution needs checkpoint commits during module migration, use `/z00z-git-versioning` rather than raw git flows.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove `include!` assembly from the canonical wallet service entrypoint</name>
  <files>crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/src/services/mod.rs, crates/z00z_wallets/src/services/wallet_service_store_create_unlock_open.rs, crates/z00z_wallets/src/services/wallet_service_store_support.rs, crates/z00z_wallets/src/services/wallet_service_session_derivation.rs, crates/z00z_wallets/src/services/wallet_service_session_guards.rs</files>
  <action>Refactor `wallet_service.rs` so it becomes an explicit facade over named service modules instead of an `include!`-assembled container. Preserve the current caller-facing service boundary, but make each internal seam explicit and owned by one module. Keep reachability-only helpers, placeholder surfaces, and store or session helpers visibly demoted instead of re-exporting them through a deceptively flat service root.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "^include!\\(" crates/z00z_wallets/src/services/wallet_service.rs
cargo test -p z00z_wallets --release --test test_phase30_split -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The canonical wallet service entrypoint is explicit, non-`include!`, and no longer hides placeholder-heavy internal seams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep source-shape and service-boundary guards aligned to the new service seam map</name>
  <files>crates/z00z_wallets/tests/test_phase30_split.rs, crates/z00z_wallets/tests/test_wallet_service_errors.rs</files>
  <action>Update service-shape and error-surface regression tests so they verify the new explicit module map rather than the old monolithic file shape. The tests must keep proving that the caller-visible service surface is shallow, that placeholder or reachability-only lanes remain visibly demoted, and that new deep-path imports are not required for service callers.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_phase30_split -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The service split is guarded by tests that validate the new seam map instead of the legacy assembly shape.</done>
</task>

<task type="auto">
  <name>Task 3: Measure compile-time and review-surface impact of the explicit service split</name>
  <files>crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/src/services/mod.rs, .planning/phases/031-refactor-architecture/031-05-SUMMARY.md</files>
  <action>Do not leave the `include!` cleanup justified by taste alone. Record the before-or-after impact of the explicit module split on review surface and compile-time or workflow friction so the TODO's measurement obligation is not silently dropped. Summarize whether review clarity improved, what compile-time or development-velocity cost was observed, and whether the split should remain the canonical pattern for future wallet service growth. The evidence may be lightweight, but it must be written down as part of the phase summary instead of being left implicit.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_phase30_split -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
rg -n "review clarity|compile-time|development velocity|include!|module split" .planning/phases/031-refactor-architecture/031-05-SUMMARY.md</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task. Review evidence must explicitly state whether the module split improved review clarity and what measurable cost or absence of cost was observed.</review>
  </verify>
  <done>The plan now retains the measurement obligation for the include cleanup instead of reducing it to a pure refactor narrative.</done>
</task>

</tasks>

<verification>
Wave 2 wallet seam cleanup closes only when `wallet_service.rs` is no longer `include!`-assembled and the updated service-shape guards are green under the required release-style order.
</verification>

<success_criteria>
Wallet service callers keep one shallow facade, but the implementation no longer depends on `include!` or on hidden placeholder-rich seams.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-05-SUMMARY.md`
</output>