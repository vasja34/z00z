<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/031-refactor-architecture/031-INVENTORY.md
  - .planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
  - crates/z00z_core/src/lib.rs
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_wallets/src/lib.rs
  - crates/z00z_storage/src/lib.rs
  - crates/z00z_simulator/src/lib.rs
  - crates/z00z_utils/src/lib.rs
  - crates/z00z_networks/rpc/src/lib.rs
autonomous: true
requirements:
  - PH31-INV
must_haves:
  truths:
    - No Phase 031 suffix removal, shim retirement, or public-surface narrowing starts before one explicit caller inventory exists.
    - The inventory covers version-suffixed APIs, legacy or compat seams, wildcard root exports, Tari leakage, include-driven assembly, and simulator deep imports.
    - The inventory is file-accurate and crate-scoped so later waves can prove retirement instead of assuming it.
  artifacts:
    - path: .planning/phases/031-refactor-architecture/031-INVENTORY.md
      provides: Canonical suffix, shim, and caller inventory for every reviewed crate surface.
    - path: .planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
      provides: Per-crate import and caller graph summaries for the touched root surfaces.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: .planning/phases/031-refactor-architecture/031-INVENTORY.md
      via: G-00 import-graph audits and Wave 0 caller proof
      pattern: G-00|W0|caller inventory|suffix/shim inventory
    - from: .planning/phases/031-refactor-architecture/031-TODO.md
      to: .planning/phases/031-refactor-architecture/031-INVENTORY.md
      via: strict version-retirement order and root-facade validation
      pattern: version|legacy|compat|shim|wildcard
---

<objective>
Freeze the Phase 031 execution inventory before any boundary cleanup starts.

Purpose: make Wave 0 the non-optional proof gate for every later facade, shim, and suffix decision.
Output: one canonical inventory artifact plus one import-graph artifact covering all reviewed Phase 031 crates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@crates/z00z_core/src/lib.rs
@crates/z00z_crypto/src/lib.rs
@crates/z00z_wallets/src/lib.rs
@crates/z00z_storage/src/lib.rs
@crates/z00z_simulator/src/lib.rs
@crates/z00z_utils/src/lib.rs
@crates/z00z_networks/rpc/src/lib.rs
</context>

If execution requires checkpoint commits while the inventory is being stabilized, use `/z00z-git-versioning` instead of ad hoc git commands.

<tasks>

<task type="auto">
  <name>Task 1: Produce the canonical Phase 031 seam inventory</name>
  <files>.planning/phases/031-refactor-architecture/031-INVENTORY.md, crates/z00z_core/src/lib.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_wallets/src/lib.rs, crates/z00z_storage/src/lib.rs, crates/z00z_simulator/src/lib.rs, crates/z00z_utils/src/lib.rs, crates/z00z_networks/rpc/src/lib.rs</files>
  <action>Create `031-INVENTORY.md` as the single Wave 0 source of truth for Phase 031. Inventory every caller-visible version-suffixed symbol, compat or legacy export, shim alias, wildcard root export, Tari-leaking public export, `include!`-assembled service seam, and simulator deep import that would be affected by facade narrowing or retirement. For each row, record the owning file, the current public status, the downstream caller pattern, whether the seam is stable, provisional, migration-only, or undocumented, the canonical surviving signature or facade if one exists, whether the current lane is canonical or non-canonical, and the exact later plan that is allowed to change or retire it.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "pub use .*\\*|compat|legacy|shim|V[0-9]+|v[0-9]+|include!\\(|tari_crypto::|pub use .*tari" crates/z00z_core crates/z00z_crypto crates/z00z_wallets crates/z00z_storage crates/z00z_simulator crates/z00z_utils crates/z00z_networks -g '*.rs'
rg -n "z00z_wallets::.*services|z00z_wallets::.*db|z00z_storage::.*store_internal" crates/z00z_simulator/src -g '*.rs'</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`031-INVENTORY.md` exists and every retirement-sensitive seam is mapped to one owning file, one caller pattern, and one later execution plan.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the per-crate import-graph proof required by Gate G-00</name>
  <files>.planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md, crates/z00z_core/src/lib.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_wallets/src/lib.rs, crates/z00z_storage/src/lib.rs, crates/z00z_simulator/src/lib.rs, crates/z00z_utils/src/lib.rs, crates/z00z_networks/rpc/src/lib.rs</files>
  <action>Create `031-IMPORT-GRAPH.md` with one summary per reviewed crate root. Each summary must list direct root re-exports, major inward callers, public surface hot paths, and the exact ambiguity that would block facade narrowing if left unresolved. The graph must explicitly justify whether `z00z_core` and `z00z_crypto` can proceed in parallel during Wave 1 and whether the three wallet plans can split responsibility without shared-caller drift.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "pub use |mod |use crate::|use z00z_" crates/z00z_core/src crates/z00z_crypto/src crates/z00z_wallets/src crates/z00z_storage/src crates/z00z_simulator/src crates/z00z_utils/src crates/z00z_networks/rpc/src -g '*.rs'
cargo test -p z00z_core --release --test assets_tests -- --nocapture
cargo test -p z00z_crypto --release --test test_hash_policy -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`031-IMPORT-GRAPH.md` exists, Gate G-00 is evidence-backed, and Wave 1 parallelism decisions are grounded in caller proof instead of intuition.</done>
</task>

</tasks>

<verification>
Wave 0 closes only when both inventory artifacts exist and every later plan can cite them instead of rediscovering scope.
</verification>

<success_criteria>
Phase 031 has one authoritative inventory and one authoritative import graph, and no later retirement or narrowing work starts without them.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-01-SUMMARY.md`
</output>