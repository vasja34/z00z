<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "03"
type: execute
wave: 1
depends_on:
  - 031-01
files_modified:
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/src/lib_api.rs
  - crates/z00z_crypto/src/aead.rs
  - crates/z00z_crypto/src/aead_test_only.rs
  - crates/z00z_crypto/src/aead_aad.rs
  - crates/z00z_crypto/src/backend_tari.rs
  - crates/z00z_crypto/src/domains.rs
  - crates/z00z_crypto/src/hash_domains.rs
  - crates/z00z_crypto/src/kdf.rs
  - crates/z00z_crypto/src/kdf_domains.rs
  - crates/z00z_crypto/README.md
autonomous: true
requirements:
  - PH31-CRYPTO
must_haves:
  truths:
    - `z00z_crypto` remains the sole owner of the public crypto facade, domain tags, transcript framing, AAD builders, and KDF info constants.
    - Tari-backed vendor details move behind an explicitly named non-default namespace and stop leaking through the stable root facade.
    - Test-only AEAD and caller-controlled nonce helpers are fully gated out of non-test profiles before Phase 031 closeout.
  artifacts:
    - path: crates/z00z_crypto/src/lib.rs
      provides: Stable crypto facade and vendor-isolation boundary.
    - path: crates/z00z_crypto/src/aead_test_only.rs
      provides: Test-only nonce and helper gating anchor.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-INVENTORY.md
      to: crates/z00z_crypto/src/lib.rs
      via: Tari leakage inventory and versioned shim caller proof
      pattern: tari_crypto::|pub use .*tari
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_crypto/src/aead_test_only.rs
      via: D-10 blocker and protected crypto seam invariants
      pattern: D-10|test-only AEAD|nonce
---

<objective>
Split the `z00z_crypto` public surface into one stable facade and one clearly demoted vendor or expert lane.

Purpose: stop the root crate from silently leaking Tari-owned concrete contracts and keep production crypto ownership unambiguous while Phase 031 cleans up legacy or versioned seams.
Output: explicit stable exports, demoted vendor passthroughs, and enforced test-only gating for nonce-controlled AEAD helpers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@.planning/phases/031-refactor-architecture/031-IMPORT-GRAPH.md
@crates/z00z_crypto/src/lib.rs
@crates/z00z_crypto/src/lib_api.rs
@crates/z00z_crypto/src/aead.rs
@crates/z00z_crypto/src/aead_test_only.rs
@crates/z00z_crypto/src/aead_aad.rs
@crates/z00z_crypto/src/backend_tari.rs
@crates/z00z_crypto/src/domains.rs
@crates/z00z_crypto/src/hash_domains.rs
@crates/z00z_crypto/src/kdf.rs
@crates/z00z_crypto/src/kdf_domains.rs
@crates/z00z_crypto/README.md
</context>

If execution requires checkpoint commits while crypto callers are migrated, use `/z00z-git-versioning` instead of raw git commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curate the stable `z00z_crypto` root and demote vendor passthroughs</name>
  <files>crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/src/lib_api.rs, crates/z00z_crypto/src/backend_tari.rs, crates/z00z_crypto/src/domains.rs, crates/z00z_crypto/src/hash_domains.rs, crates/z00z_crypto/src/kdf.rs, crates/z00z_crypto/src/kdf_domains.rs, crates/z00z_crypto/src/aead_aad.rs, crates/z00z_crypto/README.md</files>
  <action>Replace the current broad root re-export pattern with one stable facade that exposes only Z00Z-owned types and intentionally stable wrappers. Move any remaining Tari concrete re-exports into an explicit vendor or expert namespace that is not the default caller lane. While doing so, inventory every stable domain tag, transcript-frame helper, AAD builder, and KDF info or context constant that callers are allowed to bind to, keep that inventory owned by Z00Z modules rather than Tari passthroughs, preserve byte-stable semantics for those values across the split, and prove that downstream crates do not retain competing public copies or alternate public builders once the root is narrowed. Update the crate README so it names the stable facade, the expert lane, and the vendor lane distinctly, and so it stops implying that root imports are the right place to bind to Tari semantics.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "tari_crypto::|pub use .*tari|PedersenCommitmentFactory|CommitmentSignature|DiffieHellmanSharedSecret" crates/z00z_crypto/src/lib.rs crates/z00z_crypto/src/lib_api.rs crates/z00z_crypto/src/backend_tari.rs -g '*.rs'
rg -n "domain|transcript|aad|kdf|context" crates/z00z_crypto/src crates/z00z_core/src crates/z00z_storage/src crates/z00z_wallets/src crates/z00z_simulator/src -g '*.rs'
cargo test -p z00z_crypto --release --test test_hash_policy -- --nocapture
cargo test -p z00z_crypto --release --test zkpack_domain_verification -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The stable crypto root is Z00Z-owned, and any vendor passthrough still present is explicit, narrow, and non-default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate test-only AEAD and caller-controlled nonce helpers out of production profiles</name>
  <files>crates/z00z_crypto/src/aead.rs, crates/z00z_crypto/src/aead_test_only.rs, crates/z00z_crypto/src/lib.rs</files>
  <action>Make the test-only AEAD and nonce-controlled helper surface unambiguously non-production. `seal_with_nonce_TEST_ONLY` and any sibling helper must remain reachable only from cfg-gated non-production paths, and the stable root plus default-feature build must not expose them as accidental imports. If necessary, move helper exports behind cfg-gated modules or explicit test-only naming so non-test callers cannot accidentally link them. Keep one canonical production encryption owner and do not introduce a second public seal or open path while applying the gate. The acceptance contract must include one explicit negative proof that a ship-ready non-test profile cannot compile, import, or link a caller-supplied nonce helper outside `cfg(test)` or a closed internal test-support module.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "aead_test_only|seal_with_nonce_TEST_ONLY|cfg\(any\(test, doctest, feature = \"experimental-zkpack\"\)\)" crates/z00z_crypto/src -g '*.rs'
cargo test -p z00z_crypto --release --test test_public_surface -- --nocapture
cargo test -p z00z_crypto --release --test test_public_surface aead_test_only_helpers_hidden_outside_cfg_test -- --exact --nocapture
cargo test -p z00z_crypto --release --test security_edge_cases -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The D-10 blocker is resolved or explicitly fenced, and the production crypto surface cannot consume test-only AEAD helpers by accident.</done>
</task>

</tasks>

<verification>
Wave 1 crypto work closes only when the stable root no longer leaks vendor contracts and the non-production AEAD lane is demonstrably gated.
</verification>

<success_criteria>
`z00z_crypto` has one clear stable facade, one explicit non-default vendor lane, and no accidental production path to test-only AEAD helpers.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-03-SUMMARY.md`
</output>