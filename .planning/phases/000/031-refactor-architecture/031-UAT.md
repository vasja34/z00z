---
status: testing
phase: 031-refactor-architecture
source:
  - 031-05-SUMMARY.md
  - 031-06-SUMMARY.md
  - 031-07-SUMMARY.md
  - 031-08-SUMMARY.md
  - 031-09-SUMMARY.md
  - 031-10-SUMMARY.md
started: 2026-04-04T22:15:00Z
updated: 2026-04-04T22:15:00Z
---

# Phase 031 UAT

## Current Test

number: 1
name: Default simulator run keeps wallet secrets out of the public Stage 2 contract
expected: |
  Run `cargo test -p z00z_simulator --release --features test-fast --test test_wallet_integration stage2_rpc_no_secrets -- --exact --nocapture`
  or inspect the default Stage 2 output after a normal simulator run.

  The check should pass, and the public/default output contract must not expose plaintext wallet-secret artifacts such as `wlt_secrets_debug.md`.
awaiting: user response

## Tests

### 1. Default simulator run keeps wallet secrets out of the public Stage 2 contract

expected: Run `cargo test -p z00z_simulator --release --features test-fast --test test_wallet_integration stage2_rpc_no_secrets -- --exact --nocapture` or inspect the default Stage 2 output after a normal simulator run. The check should pass, and the public/default output contract must not expose plaintext wallet-secret artifacts such as `wlt_secrets_debug.md`.
result: [pending]

### 2. Debug secret artifact exists only in the private debug lane with private permissions

expected: Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_wallet_integration stage2_debug_secret_artifact_is_private -- --exact --nocapture` or inspect a debug-enabled Stage 2 run. The debug-only artifact may exist only under `wallets/private/`, and it must be written with private permissions rather than appearing in the public artifact surface.
result: [pending]

### 3. Simulator output reset refuses paths outside the approved sandbox

expected: Run `cargo test -p z00z_simulator --release --features test-fast --test test_wallet_integration reset_outputs_dir_rejects_outside_sandbox -- --exact --nocapture`. The check should pass only if `reset_outputs_dir()` rejects non-sandbox paths before any recursive delete or recreate side effect happens.
result: [pending]

### 4. Wallet identity and lock flows stay session-bound after open and unlock

expected: Run `cargo test -p z00z_wallets --release --features test-fast --test test_open_wallet_source_discovery -- --nocapture` together with `cargo test -p z00z_wallets --release --features test-fast --test test_rpc_wiring_spec_a -- --nocapture`. The open/unlock/list-address flow should keep using persisted wallet identity, and `wallet.session.lock_wallet` should reject unauthenticated transport calls while succeeding with a valid session token.
result: [pending]

### 5. Wallet RPC and simulator flows continue to work without a wallet_id compatibility bypass

expected: Run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_bob_flow -- --nocapture` or an equivalent scenario flow. The flow should complete without reintroducing a `wallet_id` backdoor on `wallet.session.lock_wallet`; simulator helpers must reuse unlock-returned session tokens instead.
result: [pending]

### 6. Storage checkpoint reload and rehydrate fail-close on missing or drifted replay evidence

expected: Run `cargo test -p z00z_storage --release --test test_checkpoint_store_api -- --nocapture` and `cargo test -p z00z_storage --release --test test_redb_rehydrate rehydrate_rejects_attested_proof_drift -- --exact --nocapture`. Both checks should pass only if missing rows or attested proof drift are rejected on reload rather than silently accepted.
result: [pending]

### 7. Phase 031 closeout artifacts remain synchronized to the final validation evidence

expected: Inspect `crates/z00z_utils/README.md`, `.planning/phases/031-refactor-architecture/031-VERIFICATION.md`, `.planning/phases/031-refactor-architecture/031-RETIREMENT.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md`. The boundary note, retirement evidence, and planning truth should all agree that Phase 031 is closed on fresh validation rather than stale blocker text.
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 0

## Gaps

[none yet]
