<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 031-refactor-architecture
plan: "08"
type: execute
wave: 3
depends_on:
  - 031-05
  - 031-06
  - 031-07
files_modified:
  - crates/z00z_storage/src/assets/store_internal/store_rows.rs
  - crates/z00z_storage/src/assets/store_internal/store_types.rs
  - crates/z00z_storage/src/checkpoint/store_fs.rs
  - crates/z00z_storage/src/checkpoint/store.rs
  - crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs
  - crates/z00z_storage/src/checkpoint/artifact_final.rs
  - crates/z00z_storage/tests/assets/test_store_api.rs
  - crates/z00z_storage/tests/test_checkpoint_root_binding.rs
  - crates/z00z_storage/tests/test_checkpoint_store_api.rs
  - crates/z00z_storage/tests/test_checkpoint_finalization.rs
  - crates/z00z_storage/tests/snapshot/test_replay_bound.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
  - crates/z00z_storage/tests/test_checkpoint_draft_final.rs
autonomous: true
requirements:
  - PH31-STORAGE
must_haves:
  truths:
    - The chosen Phase 031 model keeps checkpoint IDs statement-derived and treats `cp_proof` as verifier-bound compatibility payload only inside explicit attested artifacts, never as standalone canonical replay evidence.
    - `seal_artifact` and finalization paths preserve explicit proof-binding, replay semantics, and rehydrate consistency.
    - Storage hardening remains in `z00z_storage` without inventing a second owner for checkpoint semantics.
    - Storage-facing checkpoint tests prove storage-owned invariants without normalizing wallet-shaped assumptions where no wallet contract is under test.
    - The retained `ClaimNullifier` note stays a hardening observation only and is carried by explicit cross-crate evidence rather than by an unowned comment in planner context.
  artifacts:
    - path: crates/z00z_storage/src/checkpoint/store.rs
      provides: Canonical checkpoint ownership boundary.
    - path: crates/z00z_storage/src/checkpoint/artifact_final.rs
      provides: Finalized artifact semantics and proof-binding owner.
  key_links:
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_storage/src/checkpoint/store.rs
      via: G-06 storage proof-binding gate and D-37 blocker
      pattern: G-06|D-37|cp_proof|seal_artifact
    - from: .planning/phases/031-refactor-architecture/031-TODO.md
      to: crates/z00z_storage/tests/test_redb_rehydrate.rs
      via: replay-evidence and rehydrate validation map
      pattern: replay|rehydrate|checkpoint
    - from: .planning/phases/031-refactor-architecture/031-CONTEXT.md
      to: crates/z00z_storage/src/assets/store_internal/store_types.rs
      via: D-38 retained ClaimNullifier non-finding
      pattern: D-38|ClaimNullifier|derive_nullifier
---

<objective>
Make checkpoint proof semantics explicit and replay-safe across file-store, finalization, and rehydrate paths.

Purpose: close the storage blocker cluster so canonical checkpoint artifacts and replay evidence mean exactly what their names say and survive reload without semantic drift.
Output: explicit proof-binding semantics in checkpoint code and focused tests covering draft, final, store, and rehydrate behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/031-refactor-architecture/031-CONTEXT.md
@.planning/phases/031-refactor-architecture/031-TODO.md
@.planning/phases/031-refactor-architecture/031-INVENTORY.md
@crates/z00z_storage/src/assets/store_internal/store_rows.rs
@crates/z00z_storage/src/assets/store_internal/store_types.rs
@crates/z00z_storage/src/checkpoint/store_fs.rs
@crates/z00z_storage/src/checkpoint/store.rs
@crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs
@crates/z00z_storage/src/checkpoint/artifact_final.rs
@crates/z00z_storage/tests/assets/test_store_api.rs
@crates/z00z_storage/tests/test_checkpoint_root_binding.rs
@crates/z00z_storage/tests/test_checkpoint_store_api.rs
@crates/z00z_storage/tests/test_checkpoint_finalization.rs
@crates/z00z_storage/tests/snapshot/test_replay_bound.rs
@crates/z00z_storage/tests/test_redb_rehydrate.rs
@crates/z00z_storage/tests/test_checkpoint_draft_final.rs
@crates/z00z_wallets/src/core/claim/nullifier.rs
@crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
</context>

If execution needs checkpoint commits while checkpoint semantics are being normalized, use `/z00z-git-versioning` rather than raw git flows.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize checkpoint proof and finalization semantics around explicit binding rules</name>
  <files>crates/z00z_storage/src/checkpoint/store.rs, crates/z00z_storage/src/checkpoint/artifact_proof_draft.rs, crates/z00z_storage/src/checkpoint/artifact_final.rs, crates/z00z_storage/tests/test_checkpoint_store_api.rs, crates/z00z_storage/tests/test_checkpoint_finalization.rs, crates/z00z_storage/tests/test_checkpoint_draft_final.rs</files>
  <action>Adopt the explicit attestor-boundary model for Phase 031: checkpoint IDs remain statement-derived, while `cp_proof` stays verifier-bound compatibility payload only inside attested artifacts. Refactor checkpoint proof handling so canonical and attested flows do not keep semantically opaque `cp_proof` bytes as authoritative evidence. Phase 031 chooses the fail-closed canonical seal path: `seal_artifact()` succeeds only when the referenced snapshot row and exec-input row already exist as persisted replay evidence, and any weaker persistence lane must be named explicitly as draft-only or non-canonical instead of reusing the canonical seal contract. The finalized artifact path must state what is bound, what is replay evidence, and what is compatibility-only payload, and typed failures such as `ProofMix`, `ArtifactCompatMix`, and `RootMix` must remain the decisive reject surface when proof bytes drift from the statement-owned roots or exec inputs. Align `seal_artifact`, draft-proof, and finalization code so the storage layer speaks one consistent proof language.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "cp_proof|seal_artifact|ProofBlob|ProofMix|ArtifactCompatMix|RootMix|proof" crates/z00z_storage/src/checkpoint crates/z00z_storage/tests -g '*.rs'
cargo test -p z00z_storage --release --test test_checkpoint_store_api -- --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_store_api seal_artifact_rejects_missing_exec_row -- --exact --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_store_api seal_artifact_rejects_missing_snapshot_row -- --exact --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_finalization -- --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_draft_final -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Checkpoint draft, final, and store paths use one explicit proof-binding story rather than mixed opaque semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve replay-evidence and rehydrate semantics across persisted checkpoint flows</name>
  <files>crates/z00z_storage/src/checkpoint/store_fs.rs, crates/z00z_storage/src/checkpoint/store.rs, crates/z00z_storage/tests/test_redb_rehydrate.rs</files>
  <action>Keep replay evidence storage-owned and rehydrate-safe under the same explicit attestor-boundary model. The filesystem and durable-store checkpoint paths must preserve the same semantic guarantees after reload that they had before persistence. If compatibility bytes remain readable, make the upgrade or downgrade rule explicit instead of treating them as canonical proof material, and retain typed failures such as `LinkMix` and `ReplayMix` whenever persisted attested bundles drift from their statement-bound link or replay evidence. While updating these tests, remove wallet-shaped assertions or wallet-domain assumptions from the storage-focused regression surface wherever they are not required to prove a real storage invariant, so the test suite stops teaching reverse wallet knowledge as part of storage ownership.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "rehydrate|checkpoint|replay|artifact|LinkMix|ReplayMix" crates/z00z_storage/src/checkpoint crates/z00z_storage/tests -g '*.rs'
! rg -n "z00z_wallets" crates/z00z_storage/tests/test_checkpoint_store_api.rs crates/z00z_storage/tests/test_checkpoint_finalization.rs crates/z00z_storage/tests/test_redb_rehydrate.rs crates/z00z_storage/tests/test_checkpoint_draft_final.rs
cargo test -p z00z_storage --release --test test_redb_rehydrate -- --nocapture
cargo test -p z00z_storage --release --test test_redb_rehydrate rehydrate_rejects_attested_proof_drift -- --exact --nocapture
cargo test -p z00z_storage --release --features test-fast -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Replay evidence and rehydrate behavior remain storage-owned and consistent across persisted checkpoint paths, and the storage-focused regression surface no longer depends on wallet-shaped assertions that are not proving storage invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assign the retained storage validation map and keep `ClaimNullifier` as a bounded non-finding</name>
  <files>crates/z00z_storage/src/assets/store_internal/store_rows.rs, crates/z00z_storage/src/assets/store_internal/store_types.rs, crates/z00z_storage/tests/assets/test_store_api.rs, crates/z00z_storage/tests/test_checkpoint_root_binding.rs, crates/z00z_storage/tests/snapshot/test_replay_bound.rs, crates/z00z_wallets/src/core/claim/nullifier.rs, crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs</files>
  <action>Close the remaining retained storage-validation obligations that are broader than checkpoint finalization alone. Explicitly assign the asset insert or witness proof blob, snapshot validation, replay-entry recovery, and checkpoint root-binding anchors to this Wave 3 storage plan so the end-to-end validation map from the TODO is owned by concrete files and tests rather than only summarized in context. In the same pass, keep the `ClaimNullifier` branch as a bounded hardening observation instead of escalating it into a new vulnerability: storage may continue keying replay by `ClaimNullifier` only if the cross-crate evidence still shows that wallet-side derivation and proof verification bind the nullifier to `chain_id`. Any hardening added here must preserve that non-finding disposition and avoid inventing a new storage key shape that breaks the current derivation contract without evidence.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
rg -n "chk_blob|RootBindMix|validate_snapshot|replay_entries|ClaimNullifier" crates/z00z_storage/src crates/z00z_storage/tests crates/z00z_wallets/src/core -g '*.rs'
cargo test -p z00z_storage --release --test test_store_api -- --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_root_binding -- --nocapture
cargo test -p z00z_storage --release --test test_replay_bound -- --nocapture
cargo test -p z00z_storage --release --test test_redb_rehydrate -- --nocapture</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Stop only after at least 2 consecutive clean runs and fix every issue or warning before closing the task. Review evidence must show that the retained ClaimNullifier note is still supported by wallet-side chain-bound derivation rather than silently drifting into an unverified assumption.</review>
  </verify>
  <done>The retained storage validation map is fully owned by this plan, and the ClaimNullifier branch remains a deliberate non-finding with explicit cross-crate evidence instead of an unassigned planner note.</done>
</task>

</tasks>

<verification>
Wave 3 storage work closes only when G-06 is satisfied by code and tests, not by documentation alone.
</verification>

<success_criteria>
Checkpoint proof and replay semantics are explicit, truthful, and reload-safe across the canonical storage path.
</success_criteria>

<output>
After completion, create `.planning/phases/031-refactor-architecture/031-08-SUMMARY.md`
</output>