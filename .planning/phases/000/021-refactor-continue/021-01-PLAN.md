---
phase: 021-refactor-continue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_simulator/src/scenario_1/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_4.rs
  - crates/z00z_simulator/src/scenario_1/stage_5.rs
  - crates/z00z_simulator/src/scenario_1/stage_6.rs
  - crates/z00z_simulator/src/scenario_1/stage_7.rs
  - crates/z00z_simulator/src/scenario_1/stage_8.rs
  - crates/z00z_simulator/src/scenario_1/stage_9.rs
  - crates/z00z_simulator/src/scenario_1/stage_10.rs
autonomous: true
requirements:
  - SCN1-06
must_haves:
  truths:
    - Scenario 1 exposes one explicit logical module contract for each split-lane runtime stage from 4 through 10 instead of routing those stages through hidden second functions in oversized files.
    - Lane-specific waves 02–04 implement each facade and immediately update `runner.rs` and `mod.rs` to canonical module paths in the same wave; no forwarding shims are created at any point.
    - Each new facade file in this wave is an empty module shell only; the canonical `fn(&mut SimContext, &DesignStage) -> StageResult` entrypoint is added by the lane-specific waves (02–04), not here.
    - Stage 11 and Stage 12 remain untouched on `stage_11.rs` and `stage_12.rs`, per D-03.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/mod.rs
      provides: Explicit logical stage module exports for split-lane stages 4 through 10.
    - path: crates/z00z_simulator/src/scenario_1/stage_4.rs
      provides: Dedicated logical facade contract for canonical stage 4 claim publish.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/mod.rs
      to: crates/z00z_simulator/src/scenario_1/stage_4.rs
      via: module declarations for the split-lane facade surface
      pattern: pub mod stage_4|pub mod stage_5|pub mod stage_6|pub mod stage_7|pub mod stage_8|pub mod stage_9|pub mod stage_10
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Establish the explicit logical stage facade surface for Scenario 1 before any lane-specific code extraction.

Purpose: give the split lanes a stable logical module path so later split plans can work behind fixed interfaces without rewriting the public YAML or touching the already-correct Stage 11 and 12 files early.
Output: logical facade modules for stages 4 through 10 plus `mod.rs` declarations that downstream split plans can implement against.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/021-refactor-continue/021-CONTEXT.md
@.planning/phases/020-refactor-scenario-1/020-CONTEXT.md
@.planning/phases/020-refactor-scenario-1/020-04-SUMMARY.md
@crates/z00z_simulator/src/scenario_1/mod.rs
@crates/z00z_simulator/src/scenario_1/runner.rs

<interfaces>
From crates/z00z_simulator/src/scenario_1/runner.rs:
```rust
fn build_stage_map() -> BTreeMap<u32, StageFn>;
```

Current public entrypoints:
```rust
pub use stage_3::{run_claim_genesis, run_claim_prepare};
pub use stage_4::run_claim_publish;
pub use stage_5::run_tx_plan;
pub use stage_6::run_tx_prepare;
pub use stage_7::run_transfer_receive;
pub use stage_8::run_transfer_claim;
pub use stage_9::run_bundle_build;
pub use stage_10::run_bundle_publish;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce logical facade modules for the split lanes only</name>
  <files>crates/z00z_simulator/src/scenario_1/mod.rs, crates/z00z_simulator/src/scenario_1/stage_4.rs, crates/z00z_simulator/src/scenario_1/stage_5.rs, crates/z00z_simulator/src/scenario_1/stage_6.rs, crates/z00z_simulator/src/scenario_1/stage_7.rs, crates/z00z_simulator/src/scenario_1/stage_8.rs, crates/z00z_simulator/src/scenario_1/stage_9.rs, crates/z00z_simulator/src/scenario_1/stage_10.rs</files>
  <behavior>
    - Each split-lane runtime stage from 4 through 10 has an explicit, dedicated logical module file.
      - Each new facade file reserves one canonical `run_*` entrypoint path with the existing Scenario 1 stage signature; lane-specific waves 02–04 add the implementation.
    - In this scaffold wave, new facade files are created as empty Rust module shells with no function bodies and no delegation to existing container modules.
    - Stage 11 and Stage 12 remain on `stage_11.rs` and `stage_12.rs` with no new facade file introduced in this plan.
  </behavior>
    <action>Per D-01B, D-03, D-04, D-05, D-07A, and G-01 through G-03, choose the canonical stage-numbered facade strategy explicitly for the split lanes only and make it the contract that downstream split plans implement against. Keep one root facade module per logical runtime stage from 4 through 10, and reserve exactly one canonical `run_*` entrypoint path in each file for the existing Scenario 1 stage signature. Keep each root facade file as an empty Rust module shell in this scaffold wave only, with no function bodies and no delegation imports. Preserve the corresponding `pub mod` declaration pattern in `mod.rs`. Do not add any forwarding to legacy container modules. Do not change `runner.rs`, `scenario_design.yaml`, or any test files in this wave. Do not delete or restructure `stage_11.rs` or `stage_12.rs`.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_source_shape -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage5_source_shape -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs report no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>Empty facade module shells exist for stages 4 through 10, registered in `mod.rs` with no delegation or forwarding; lane waves implement and canonically wire them.</done>
</task>

</tasks>

<verification>
Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first, then the existing stage-surface source-shape tests in release mode to prove no early public-surface drift, then `cargo test --release --features test-fast --features wallet_debug_dump`, and keep running `/GSD-Review-Tasks-Execution` until two consecutive clean YOLO passes occur.
</verification>

<success_criteria>
Scenario 1 has stable empty facade module shells registered in `mod.rs` for the split lanes; lane waves 02–04 implement them and immediately update `runner.rs` dispatch canonically in the same wave. Stage 11/12 remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/021-refactor-continue/021-01-SUMMARY.md`
</output>