---
phase: 021-refactor-continue
plan: 05
type: execute
wave: 3
depends_on:
  - 021-01
  - 021-02
  - 021-03
  - 021-04
files_modified:
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/src/scenario_1/stage_4.rs
  - crates/z00z_simulator/src/scenario_1_story.md
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_simulator/tests/test_stage4_source_shape.rs
  - crates/z00z_simulator/tests/test_stage5_source_shape.rs
autonomous: true
requirements:
  - SCN1-07
  - SCN1-08
must_haves:
  truths:
    - `scenario_design.yaml` reads as a descriptive design document again rather than an executable control script.
    - The order of inputs, outputs, calls, actions, and checks in `scenario_design.yaml` matches the real code execution flow after the refactor.
    - `mod.rs`, `build_stage_map()`, and `fallback_design()` were updated to the dedicated stage files in waves 02–04. This wave aligns `scenario_design.yaml` and the YAML-asserting test guards to match the canonical stage module paths.
    - Release-style Scenario 1 validation remains green after the full stage split and YAML rewrite.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      provides: Descriptive post-refactor Scenario 1 design document aligned to the real code path.
    - path: crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
      provides: Final guardrail that stage names, `rust_entry` bindings, and executed order agree.
    - path: crates/z00z_simulator/src/scenario_1_story.md
      provides: Supplemental narrative doc synchronized to the final stage surface if it references the public stage map.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      to: crates/z00z_simulator/src/scenario_1/design_scenario_orig.yaml
      via: restored descriptive structure and section discipline
      pattern: stage|name|description|rust_entry|steps
    - from: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      to: crates/z00z_simulator/src/scenario_1/runner.rs
      via: stage order and `rust_entry` fidelity to real dispatch
      pattern: rust_entry|build_stage_map|fallback_stage
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Rewrite `scenario_design.yaml` back into a descriptive document and close the phase on final release-style validation.

Purpose: finish the refactor by making the human-facing design document trustworthy again while proving that the final code shape, stage surface, and release gates all agree.
Output: descriptive `scenario_design.yaml`, synchronized stage-surface guards, optional story-doc sync, and green release validation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/021-refactor-continue/021-CONTEXT.md
@crates/z00z_simulator/src/scenario_1/scenario_design.yaml
@crates/z00z_simulator/src/scenario_1/design_scenario_orig.yaml
@crates/z00z_simulator/src/scenario_1/runner.rs
@crates/z00z_simulator/src/scenario_1_story.md
@crates/z00z_simulator/tests/test_scenario1_stage_surface.rs

<interfaces>
Final stage contract must preserve:
```rust
fn build_stage_map() -> BTreeMap<u32, StageFn>;
```

Design document fidelity target:
```yaml
- stage: N
  name: "..."
  description: |
  rust_entry: "..."
  steps:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite `scenario_design.yaml` into descriptive sync with `design_scenario_orig.yaml` and the final code order</name>
  <files>crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/src/scenario_1_story.md</files>
  <behavior>
    - `scenario_design.yaml` is descriptive again, not executable-looking.
    - The document structure resembles `design_scenario_orig.yaml` while reflecting the actual post-refactor stage boundaries and entrypoints.
    - `scenario_design.yaml` `rust_entry` fields for stages 4 through 10 are updated to reference the dedicated stage modules established in waves 02–04.
    - Stages 11 and 12 stay mapped to the existing `stage_11.rs` and `stage_12.rs` public entrypoints established in wave 01.
    - If `scenario_1_story.md` references the public stage surface, it stays synchronized to the final naming and order.
  </behavior>
  <action>Per D-07A, D-08 through D-09, G-03 through G-05, and the user-specific requirements in `021-CONTEXT.md`, rewrite `scenario_design.yaml` so it follows the descriptive style of `design_scenario_orig.yaml` while documenting the real post-refactor code path. `mod.rs`, `runner.rs::build_stage_map()`, and `runner.rs::fallback_design()` were already updated to the dedicated stage files in waves 02–04; this wave aligns the YAML and test guards to match. The YAML must describe the actual runtime order of inputs, outputs, calls, actions, and checks for each stage, not a stale or aspirational flow. Keep `rust_entry` strings synchronized with the final dedicated module ownership from waves 02–04, preserve the existing `stage_11::run_apply` and `stage_12::run_finalize` surface for stages 11 and 12, and add any missing stage-local logging needed to make the descriptive YAML truthful. If `scenario_1_story.md` still mentions the old module names or stage routing, update it in the same wave so the workspace does not retain contradictory public docs.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_source_shape -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage5_source_shape -- --nocapture
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs report no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>`scenario_design.yaml` is descriptive, structurally aligned to `design_scenario_orig.yaml`, faithful to the real post-refactor code execution order, and the final public stage surface no longer depends on temporary compatibility shims for stages 4 through 10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the phase on final stage-surface, release-suite, and release-binary validation</name>
  <files>crates/z00z_simulator/tests/test_scenario1_stage_surface.rs, crates/z00z_simulator/tests/test_stage4_source_shape.rs, crates/z00z_simulator/tests/test_stage5_source_shape.rs</files>
  <behavior>
    - Final guard tests prove the public stage surface, source shape, executed order, and final module ownership all agree.
    - Release-style validation is the closure gate for the phase.
  </behavior>
  <action>Refresh the stage-surface and source-shape tests so they assert the final public stage map, the descriptive YAML fidelity expectations, the final module ownership split, and the absence of compatibility-routing through the old mixed containers for stages 4 through 10. Then run the release-only closure stack required by the workspace: bootstrap gate first, targeted source-shape/stage-surface tests, full release `cargo test`, and the release `scenario_1` binary. Do not close the phase on partial green signals.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_source_shape -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage5_source_shape -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs report no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>The final stage surface, descriptive YAML, release suite, and release binary all agree on the same post-refactor Scenario 1 contract.</done>
</task>

</tasks>

<verification>
Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first, then the final stage-surface/source-shape tests in release mode, then `cargo test --release --features test-fast --features wallet_debug_dump`, then `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump`, and keep `/GSD-Review-Tasks-Execution` running until two consecutive clean YOLO passes occur.
</verification>

<success_criteria>
`scenario_design.yaml` is a trustworthy descriptive document again, the `rust_entry` fields and stage-surface test guards reflect the dedicated module paths from waves 02–04, and the phase closes only on green release-style validation.
</success_criteria>

<output>
After completion, create `.planning/phases/021-refactor-continue/021-05-SUMMARY.md`
</output>