---
phase: 021-refactor-continue
plan: 02
type: execute
wave: 2
depends_on:
  - 021-01
files_modified:
  - crates/z00z_simulator/src/scenario_1/mod.rs
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
  - crates/z00z_simulator/src/scenario_1/stage_4.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/audit.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/bins.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/distribution.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/snapshot.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/state.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/wallet_flow.rs
  - crates/z00z_simulator/tests/test_claim_acceptance.rs
  - crates/z00z_simulator/tests/test_claim_emit.rs
  - crates/z00z_simulator/tests/test_claim_integration.rs
  - crates/z00z_simulator/tests/test_claim_persist.rs
  - crates/z00z_simulator/tests/test_claim_snapshot.rs
  - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
autonomous: true
requirements:
  - SCN1-06
must_haves:
  truths:
    - `claim_prepare` and `claim_publish` are implemented in separate files with explicit ownership instead of two public entrypoints in one monolith.
    - `run_claim_genesis` has an explicit home and remains intentional rather than surviving as accidental residue after the split.
    - `stage_3.rs` is permanently slimmed to claim-prepare and claim-genesis only; it contains no forwarding to a suffixed publish file. `runner.rs` and `mod.rs` are updated in this wave to dispatch stage 4 and re-export `run_claim_publish` from `stage_4::run_claim_publish` directly.
    - Claim artifacts, audit logs, snapshot shape, and wallet-visible semantics stay unchanged.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_3.rs
      provides: Claim-prepare stage and explicit `run_claim_genesis` ownership.
    - path: crates/z00z_simulator/src/scenario_1/stage_4.rs
      provides: Canonical claim-publish stage implementation.
    - path: crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
      provides: Shared claim package helpers that preserve current artifact semantics.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_3.rs
      to: crates/z00z_simulator/src/scenario_1/stage_4.rs
      via: explicit prepare-to-publish split with no duplicate public stage entrypoints
      pattern: run_claim_prepare|run_claim_publish|run_claim_genesis
    - from: crates/z00z_simulator/src/scenario_1/stage_4.rs
      to: crates/z00z_simulator/src/scenario_1/stage_3_utils/
      via: audit, distribution, snapshot, and wallet-flow helpers
      pattern: audit|distribution|snapshot|wallet_flow|claim_pkg
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Split the claim lane into dedicated prepare and publish files without changing claim semantics.

Purpose: close the Stage 3 gap left by Phase 020 and make the fate of `run_claim_genesis` explicit before the later tx, transfer, and bundle waves build on the same stage surface.
Output: a slim `stage_3.rs`, a canonical `stage_4.rs`, and claim tests that assert the split without semantic drift.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/021-refactor-continue/021-CONTEXT.md
@.planning/phases/021-refactor-continue/021-01-PLAN.md
@crates/z00z_simulator/src/scenario_1/stage_3.rs
@crates/z00z_simulator/src/scenario_1/stage_4.rs
@crates/z00z_simulator/src/scenario_1/stage_3_utils/mod.rs
@crates/z00z_simulator/tests/test_claim_acceptance.rs

<interfaces>
Current claim entrypoints:
```rust
pub fn run_claim_genesis(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
pub fn run_claim_prepare(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
pub fn run_claim_publish(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move claim-publish behavior into its dedicated facade and settle `run_claim_genesis` ownership</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3.rs, crates/z00z_simulator/src/scenario_1/stage_4.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/mod.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/audit.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/bins.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/distribution.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/snapshot.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/state.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/wallet_flow.rs</files>
  <behavior>
    - `stage_3.rs` stops owning claim-publish logic and is permanently slimmed to claim-prepare and claim-genesis only, with no compatibility forwarding.
    - `run_claim_publish` logic lives in canonical root `stage_4.rs`.
    - `run_claim_genesis` either remains in `stage_3.rs` or moves behind an explicitly documented helper, but its final home is deliberate and tested.
  </behavior>
  <action>Per D-01A, D-02, D-05, D-06, and G-01 through G-04, extract the publish-specific behavior from `stage_3.rs` into canonical root `stage_4.rs` and slim `stage_3.rs` to claim-prepare and claim-genesis only with no forwarding. Immediately update `runner.rs::build_stage_map()` and `runner.rs::fallback_design()` to dispatch stage 4 to `stage_4::run_claim_publish`. Update `mod.rs` to re-export `run_claim_publish` from `stage_4` instead of `stage_3`. `stage_3.rs` must not contain any compatibility forwarding after this wave. Reuse the existing `stage_3_utils` tree rather than inventing a second helper lineage. Do not change claim package semantics, audit log meaning, consumed-bin behavior, snapshot field names, or wallet import flow. Do not update `scenario_design.yaml` or `test_scenario1_stage_surface.rs` in this wave; those are synchronized with YAML in `021-05`.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_acceptance -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_emit -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_integration -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_persist -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_snapshot -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_tx_pipeline -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs report no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>The claim lane has dedicated prepare and publish files, `run_claim_genesis` has an explicit home in `stage_3.rs`, `stage_3.rs` is permanently slimmed to prepare/genesis with no forwarding, and `runner.rs` dispatches stage 4 directly to `stage_4::run_claim_publish`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock claim-lane regressions to the new file split without changing artifacts</name>
  <files>crates/z00z_simulator/tests/test_claim_acceptance.rs, crates/z00z_simulator/tests/test_claim_emit.rs, crates/z00z_simulator/tests/test_claim_integration.rs, crates/z00z_simulator/tests/test_claim_persist.rs, crates/z00z_simulator/tests/test_claim_snapshot.rs, crates/z00z_simulator/tests/test_claim_tx_pipeline.rs</files>
  <behavior>
    - Claim tests continue proving the same artifacts and behaviors after the file split.
    - Claim-focused tests call the dedicated stage modules directly. YAML-asserting surface guards (`test_scenario1_stage_surface.rs`) remain unchanged until `scenario_design.yaml` is updated in `021-05`.
  </behavior>
  <action>Update claim-facing tests so they assert the split stage ownership and unchanged artifacts instead of implicitly depending on the old two-entrypoint `stage_3.rs` layout. Keep the tests focused on semantic continuity: claim acceptance, emitted artifacts, persistence, snapshot contents, and tx-pipeline handoff must remain exactly as before. Do not update `scenario_design.yaml` or `test_scenario1_stage_surface.rs` in this wave; those are updated together with YAML in `021-05`.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_acceptance -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_emit -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_integration -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_persist -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_snapshot -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_tx_pipeline -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs report no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>Claim tests prove that the split changed source shape only, not claim behavior or artifact contracts.</done>
</task>

</tasks>

<verification>
Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first, then the claim-focused release tests, then `cargo test --release --features test-fast --features wallet_debug_dump`, and keep `/GSD-Review-Tasks-Execution` running until two consecutive clean YOLO passes occur.
</verification>

<success_criteria>
The claim lane no longer keeps claim-publish logic inside `stage_3.rs`, all claim semantics remain stable, `runner.rs` dispatches stage 4 to `stage_4::run_claim_publish` directly, and `stage_3.rs` contains no compatibility forwarding.
</success_criteria>

<output>
After completion, create `.planning/phases/021-refactor-continue/021-02-SUMMARY.md`
</output>