---
phase: 021-refactor-continue
plan: 04
type: execute
wave: 2
depends_on:
  - 021-01
files_modified:
  - crates/z00z_simulator/src/scenario_1/mod.rs
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - crates/z00z_simulator/src/scenario_1/stage_7.rs
  - crates/z00z_simulator/src/scenario_1/stage_8.rs
  - crates/z00z_simulator/src/scenario_1/stage_9.rs
  - crates/z00z_simulator/src/scenario_1/stage_10.rs
  - crates/z00z_simulator/src/scenario_1/stage_5_utils/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_5_utils/transfer_lane_impl.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_simulator/tests/support/test_stage5_support.rs
  - crates/z00z_simulator/tests/test_stage5_receive_bridge.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
  - crates/z00z_simulator/tests/test_stage7_jmt_wallet_scan.rs
  - crates/z00z_simulator/tests/test_stage8_proof_path.rs
autonomous: true
requirements:
  - SCN1-06
must_haves:
  truths:
    - `transfer_receive`, `transfer_claim`, `bundle_build`, and `bundle_publish` resolve through the canonical dedicated root stage files `stage_7.rs`, `stage_8.rs`, `stage_9.rs`, and `stage_10.rs`.
    - `stage_5.rs` and `stage_6.rs` remain the canonical tx-lane root facades from `021-03`; this wave must not empty, remove, or repurpose them.
    - The Stage 6 bridge remains the only downstream handoff contract even after the file split.
    - Downstream bridge-focused tests call the dedicated logical stage modules directly. YAML-asserting source-shape guards (`test_stage5_source_shape.rs`) remain unchanged until YAML is updated in `021-05`.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_7.rs
      provides: Dedicated transfer-receive stage implementation.
    - path: crates/z00z_simulator/src/scenario_1/stage_8.rs
      provides: Dedicated transfer-claim stage implementation.
    - path: crates/z00z_simulator/src/scenario_1/stage_9.rs
      provides: Dedicated bundle-build stage implementation.
    - path: crates/z00z_simulator/src/scenario_1/stage_10.rs
      provides: Dedicated bundle-publish stage implementation.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_9.rs
      to: crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs
      via: bridge artifact continuity and exec-input storage checks
      pattern: run_bundle_build|checkpoint_bridge_s6
    - from: crates/z00z_simulator/src/scenario_1/stage_10.rs
      to: crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
      via: final-gate and downstream apply/finalize continuity
      pattern: run_bundle_publish|run_apply|run_finalize
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Split the transfer and bundle lanes into dedicated files and keep the downstream storage/apply bridge explicit.

Purpose: remove the last public multi-stage Scenario 1 containers and preserve the Stage 6-to-Stage 7/8 bridge semantics that later tests depend on.
Output: validated canonical stage 7–10 root files, updated bridge/apply tests where needed, and preserved `stage_5.rs` plus `stage_6.rs` as tx-only facades from `021-03`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/021-refactor-continue/021-CONTEXT.md
@.planning/phases/021-refactor-continue/021-01-PLAN.md
@crates/z00z_simulator/src/scenario_1/stage_7.rs
@crates/z00z_simulator/src/scenario_1/stage_8.rs
@crates/z00z_simulator/src/scenario_1/stage_9.rs
@crates/z00z_simulator/src/scenario_1/stage_10.rs
@crates/z00z_simulator/src/scenario_1/stage_5_utils/mod.rs
@crates/z00z_simulator/src/scenario_1/stage_6_utils/mod.rs
@crates/z00z_simulator/tests/support/test_stage5_support.rs

<interfaces>
Current transfer and bundle entrypoints:
```rust
pub fn run_transfer_receive(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
pub fn run_transfer_claim(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
pub fn run_bundle_build(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
pub fn run_bundle_publish(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate the canonical transfer and bundle stage ownership at `stage_7.rs` through `stage_10.rs`</name>
  <files>crates/z00z_simulator/src/scenario_1/mod.rs, crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/src/scenario_1/stage_7.rs, crates/z00z_simulator/src/scenario_1/stage_8.rs, crates/z00z_simulator/src/scenario_1/stage_9.rs, crates/z00z_simulator/src/scenario_1/stage_10.rs, crates/z00z_simulator/src/scenario_1/stage_5_utils/mod.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/mod.rs, crates/z00z_simulator/src/scenario_1/stage_5_utils/transfer_lane_impl.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs</files>
  <behavior>
    - The transfer and bundle lanes resolve through `stage_7.rs`, `stage_8.rs`, `stage_9.rs`, and `stage_10.rs` with one logical public stage per root file.
    - `stage_5.rs` and `stage_6.rs` stay untouched as tx-only facades and must not regain transfer or bundle ownership.
    - Stage 6 bridge artifact semantics and downstream apply/finalize continuity remain unchanged.
  </behavior>
  <action>Per D-01A, D-02, D-04, D-05, D-06, and G-01 through G-05, validate the already-landed transfer and bundle split on this branch. The canonical public roots for stages 7, 8, 9, and 10 are `stage_7.rs`, `stage_8.rs`, `stage_9.rs`, and `stage_10.rs`, and `runner.rs::build_stage_map()` plus `runner.rs::fallback_design()` must dispatch to them directly. `mod.rs` must re-export from those dedicated root files. `stage_5.rs` and `stage_6.rs` must remain the tx-only facades established in `021-03`; they are not in scope for removal or empties in this wave. Do not update `scenario_design.yaml` or `test_stage5_source_shape.rs` in this wave; those are synchronized with YAML in `021-05`. Do not change the Stage 5 selected-output artifact contract, Stage 6 bridge artifact schema, exec-input persistence, or the downstream Stage 7/8 apply and finalize expectations.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage5_receive_bridge -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage6_checkpoint_storage_bridge -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage6_checkpoint_final_gate -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs report no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>Transfer and bundle stages each live behind canonical root files `stage_7.rs` through `stage_10.rs`, bridge/apply continuity remains intact, and `stage_5.rs` plus `stage_6.rs` remain tx-only facades.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update transfer, bundle, and downstream bridge tests to the dedicated stage files</name>
  <files>crates/z00z_simulator/tests/support/test_stage5_support.rs, crates/z00z_simulator/tests/test_stage5_receive_bridge.rs, crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs, crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs, crates/z00z_simulator/tests/test_stage7_jmt_wallet_scan.rs, crates/z00z_simulator/tests/test_stage8_proof_path.rs</files>
  <behavior>
    - Downstream bridge tests use the dedicated logical stage modules and preserve current bridge/apply expectations.
    - The final public source-shape test remains unchanged until `021-05`, but downstream bridge tests must still fail if transfer or bundle logic regresses semantically.
  </behavior>
  <action>Validate or update transfer, bundle, and downstream apply/finalize tests so they call the canonical dedicated stage files `stage_7.rs` through `stage_10.rs` and assert the current implementation ownership without weakening the existing continuity checks. Keep the regression focus on real artifacts: selected Bob output handling, checkpoint bridge persistence, Stage 7 JMT wallet scan continuity, and Stage 8 proof/finalization gates. Do not update `scenario_design.yaml` or `test_stage5_source_shape.rs` in this wave; those are updated together with YAML in `021-05`.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage5_source_shape -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage5_receive_bridge -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage6_checkpoint_storage_bridge -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage6_checkpoint_final_gate -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage7_jmt_wallet_scan -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage8_proof_path -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs report no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>Transfer and bundle tests prove the dedicated stage files while keeping the downstream Stage 7 and Stage 8 bridge semantics stable.</done>
</task>

</tasks>

<verification>
Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first, then the transfer/bundle release tests, then `cargo test --release --features test-fast --features wallet_debug_dump`, and keep `/GSD-Review-Tasks-Execution` running until two consecutive clean YOLO passes occur.
</verification>

<success_criteria>
The transfer and bundle lanes resolve through canonical root files `stage_7.rs` through `stage_10.rs`, downstream bridge/apply tests still pass on the dedicated-file layout, `runner.rs` dispatches stages 7–10 to dedicated files directly, and `stage_5.rs` plus `stage_6.rs` remain tx-only facades.
</success_criteria>

<output>
After completion, create `.planning/phases/021-refactor-continue/021-04-SUMMARY.md`
</output>