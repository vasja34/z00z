<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 028-crypto-audit-storage
plan: "02"
type: execute
wave: 2
depends_on:
  - 028-01
files_modified:
  - crates/z00z_storage/src/checkpoint/exec_input.rs
  - crates/z00z_storage/src/checkpoint/build.rs
  - crates/z00z_storage/src/checkpoint/store.rs
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend.rs
  - crates/z00z_storage/tests/test_checkpoint_replay_inputs.rs
  - crates/z00z_storage/tests/test_checkpoint_draft_build.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
autonomous: true
requirements:
  - PH28-EXEC-PROOF
  - PH28-TRUST-HOOK
must_haves:
  truths:
    - Canonical `CheckpointExecInput` artifacts preserve the real tx-proof bytes that upstream validation consumed.
    - Synthetic or index-derived proof bytes cannot reach persisted authoritative replay artifacts.
    - Store persistence and rehydrate paths treat ops-only or debug transcript generation as a distinct non-canonical path, not as `CheckpointExecInput`.
  artifacts:
    - path: crates/z00z_storage/src/checkpoint/exec_input.rs
      provides: Authoritative replay artifact contract bound to real tx-proof bytes.
    - path: crates/z00z_storage/src/assets/store.rs
      provides: Store-owned exec-artifact creation policy with no placeholder tx-proof bytes on the canonical path.
    - path: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      provides: Persistence of canonical exec bytes only.
    - path: crates/z00z_storage/tests/test_checkpoint_replay_inputs.rs
      provides: Replay-authenticity regressions for stored tx-proof bytes and link validation.
  key_links:
    - from: crates/z00z_storage/src/checkpoint/build.rs
      to: crates/z00z_storage/src/checkpoint/exec_input.rs
      via: consumption of canonical replay bytes whose `tx_proof` payload must already match the upstream-validated execution transcript
      pattern: build_cp_draft|CheckpointExecInput|CheckpointExecTx|tx_proof
    - from: crates/z00z_storage/src/assets/store.rs
      to: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      via: canonical exec-input generation and persisted exec bytes
      pattern: build_exec|plan_arts|exec_bytes|derive_exec_id
---

<objective>
Preserve real execution transcripts and stop persisting placeholder tx-proof bytes as authoritative replay artifacts.

Purpose: close the replay-authenticity blocker so persisted execution inputs are trustworthy replay objects rather than synthetic plan data wearing canonical names.
Output: one authoritative exec-input contract, one non-canonical escape hatch for any remaining synthetic planning data, and one persistence path that stores only canonical exec bytes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/028-crypto-audit-storage/028-FUSION.md
@.planning/phases/028-crypto-audit-storage/028-CONTEXT.md
@crates/z00z_storage/src/checkpoint/exec_input.rs
@crates/z00z_storage/src/checkpoint/build.rs
@crates/z00z_storage/src/checkpoint/store.rs
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/assets/store_internal/redb_backend.rs
@crates/z00z_storage/tests/test_checkpoint_replay_inputs.rs
@crates/z00z_storage/tests/test_checkpoint_draft_build.rs
@crates/z00z_storage/tests/test_redb_rehydrate.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze an authoritative exec-input contract around real upstream tx-proof bytes</name>
  <files>crates/z00z_storage/src/checkpoint/exec_input.rs
crates/z00z_storage/src/checkpoint/build.rs
crates/z00z_storage/tests/test_checkpoint_replay_inputs.rs
crates/z00z_storage/tests/test_checkpoint_draft_build.rs</files>
  <behavior>
    - Test 1: when `build_cp_draft(...)` consumes one canonical replay artifact, that artifact preserves exactly the tx-proof bytes that upstream verification already accepted.
    - Test 2: placeholder, index-derived, or synthetic tx-proof bytes are rejected from the canonical persisted `CheckpointExecInput` path.
    - Test 3: if a synthetic or planning transcript still exists for internal workflows, it is explicitly marked as proposed or non-canonical and cannot be serialized as canonical `CheckpointExecInput` bytes.
  </behavior>
  <action>Implement the execution-transcript decisions and Gate G-02 from `028-CONTEXT.md`. Refactor `CheckpointExecInput`, `CheckpointExecTx`, and the checkpoint-build or persistence seam so the authoritative replay artifact can only be created from real upstream tx-proof bytes. Thread those bytes from the accepted execution transcript into the canonical exec artifact instead of recreating them later from operation indexes. If a new helper is needed because no existing builder currently owns canonical replay emission, mark that helper as proposed in the plan first and keep it inside the existing storage checkpoint path. If `AssetStore::build_exec(...)` still needs a synthetic planning helper, rename or split it into a non-canonical artifact path; do not overload `CheckpointExecInput` for both purposes.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_checkpoint_replay_inputs -- --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_draft_build -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-02-PLAN.md current_task="Task 1: Freeze an authoritative exec-input contract around real upstream tx-proof bytes"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Canonical replay artifacts are built from real verified tx-proof bytes and no longer permit placeholder proof payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire persistence and reload so RedB stores only canonical exec artifacts</name>
  <files>crates/z00z_storage/src/assets/store.rs
crates/z00z_storage/src/checkpoint/store.rs
crates/z00z_storage/src/assets/store_internal/redb_backend.rs
crates/z00z_storage/tests/test_checkpoint_replay_inputs.rs
crates/z00z_storage/tests/test_redb_rehydrate.rs</files>
  <behavior>
    - Test 1: the persisted exec bytes and derived exec ID are stable only for canonical authoritative exec artifacts.
    - Test 2: RedB rehydrate and replay checks keep the same exec ID and reject any mismatch between persisted exec bytes and the checkpoint link.
    - Test 3: no production code path serializes the current `vec![(index as u8).saturating_add(1)]` placeholder proof pattern into canonical exec tables.
  </behavior>
  <action>Replace the current `plan_arts(...) -> build_exec(...)` placeholder path with one canonical persistence seam that receives authoritative exec bytes from the checkpoint build or seal workflow. Keep any remaining synthetic planning transcript out of `asset_cp_execs` and out of exec-ID derivation. Update RedB write or rehydrate logic, checkpoint store helpers, and replay tests so exec IDs derive from real replay payloads only. Keep the write boundary atomic and do not split exec persistence from the existing snapshot or checkpoint commit boundary.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_checkpoint_replay_inputs -- --nocapture
cargo test -p z00z_storage --release --test test_redb_rehydrate -- --nocapture
if rg -n 'vec!\[\(index as u8\)\.saturating_add\(1\)\]' crates/z00z_storage/src crates/z00z_storage/tests; then
  echo "unexpected placeholder proof pattern remains on canonical storage paths"
  exit 1
fi
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-02-PLAN.md current_task="Task 2: Rewire persistence and reload so RedB stores only canonical exec artifacts"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>RedB stores only authoritative exec-input bytes and replay reload keeps canonical exec IDs without any placeholder-proof path.</done>
</task>

</tasks>

<verification>
Close this plan only after persisted exec artifacts preserve real upstream tx-proof bytes and any remaining synthetic planning transcript is visibly distinct from canonical `CheckpointExecInput` semantics.
</verification>

<success_criteria>
Phase 028 has one authoritative replay artifact class whose bytes, IDs, and persisted meaning all come from the real verified execution transcript.
</success_criteria>

<output>
After completion, create `.planning/phases/028-crypto-audit-storage/028-02-SUMMARY.md`
</output>