<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 028-crypto-audit-storage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_storage/src/checkpoint/artifact.rs
  - crates/z00z_storage/src/checkpoint/build.rs
  - crates/z00z_storage/src/checkpoint/store.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend.rs
  - crates/z00z_storage/tests/test_checkpoint_finalization.rs
  - crates/z00z_storage/tests/test_checkpoint_draft_final.rs
  - crates/z00z_storage/tests/test_checkpoint_store_api.rs
autonomous: true
requirements:
  - PH28-CHK-PROOF
  - PH28-TRUST-HOOK
must_haves:
  truths:
    - Opaque checkpoint payloads cannot be mistaken for verified finality proofs by type name, docs, or load-time behavior.
    - Legacy `OPAQUE` checkpoint artifacts have one explicit compatibility policy instead of implicit reinterpretation.
    - The checkpoint builder and store path state clearly that tx-proof validity still depends on external verifier hooks unless a production-approved verifier adapter is supplied.
  artifacts:
    - path: crates/z00z_storage/src/checkpoint/artifact.rs
      provides: Versioned checkpoint statement and explicit proof-class semantics.
    - path: crates/z00z_storage/src/checkpoint/store.rs
      provides: Load and seal policy for legacy opaque artifacts and truthful final-artifact handling.
    - path: crates/z00z_storage/src/checkpoint/build.rs
      provides: Explicit trust-boundary contract for verifier and spent-index hooks.
    - path: crates/z00z_storage/tests/test_checkpoint_finalization.rs
      provides: Regression coverage for truthful finalization behavior and legacy opaque compatibility.
  key_links:
    - from: crates/z00z_storage/src/checkpoint/artifact.rs
      to: crates/z00z_storage/src/checkpoint/store.rs
      via: versioned proof-system semantics and explicit legacy-opaque read policy
      pattern: CheckpointProofSystem|CheckpointProof|load_artifact|seal_artifact
    - from: crates/z00z_storage/src/checkpoint/build.rs
      to: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      via: production-approved verifier policy versus opaque-attestation fallback handling
      pattern: TxProofVerifier|SpentIndex|CheckpointDraft|CheckpointProofSystem
---

<objective>
Make checkpoint proof semantics truthful, versioned, and explicit about external verifier trust.

Purpose: close the first blocker from the fused audit by ensuring checkpoint artifacts advertise only the guarantees that `z00z_storage` actually enforces today.
Output: one honest checkpoint statement surface, one explicit legacy-opaque compatibility policy, and one clear trust-boundary contract for checkpoint verification.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/028-crypto-audit-storage/028-FUSION.md
@.planning/phases/028-crypto-audit-storage/FUSION.audit.md
@.planning/phases/028-crypto-audit-storage/028-CONTEXT.md
@crates/z00z_storage/src/checkpoint/artifact.rs
@crates/z00z_storage/src/checkpoint/build.rs
@crates/z00z_storage/src/checkpoint/store.rs
@crates/z00z_storage/src/assets/store_internal/redb_backend.rs
@crates/z00z_storage/tests/test_checkpoint_finalization.rs
@crates/z00z_storage/tests/test_checkpoint_draft_final.rs
@crates/z00z_storage/tests/test_checkpoint_store_api.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce a truthful checkpoint statement and explicit legacy-opaque proof contract</name>
  <files>crates/z00z_storage/src/checkpoint/artifact.rs
crates/z00z_storage/src/checkpoint/store.rs
crates/z00z_storage/tests/test_checkpoint_finalization.rs
crates/z00z_storage/tests/test_checkpoint_draft_final.rs</files>
  <behavior>
    - Test 1: final checkpoint artifacts encode one versioned statement surface that binds the canonical fields listed in `028-CONTEXT.md` instead of treating `CheckpointPubIn` plus opaque bytes as a stronger proof contract than the crate enforces.
    - Test 2: legacy `CheckpointProofSystem::OPAQUE` artifacts remain readable only under one explicit policy: preserved-as-legacy opaque, version-migrated, or rejected with a typed compatibility error.
    - Test 3: any proof-system variant that claims verified semantics is rejected at constructor, load, or seal time unless one validator path actually exists for it.
    - Test 4: docs and test names stop calling opaque payloads “verified proofs” or otherwise implying production finality semantics.
  </behavior>
  <action>Implement the checkpoint-semantics decisions and Gates G-01 plus G-01A from `028-CONTEXT.md`. Refactor `CheckpointProofSystem`, `CheckpointProof`, and related final-artifact helpers so the default storage contract is semantically honest: `OPAQUE` is an attestation or transport class, not a verified finality proof. Introduce one versioned checkpoint statement surface that binds checkpoint version, height, `prev_root`, `new_root`, optional `claim_root`, `spent_delta`, `created_delta`, `prep_snapshot_id`, and `exec_input_id`. Use that statement as the canonical thing an opaque payload refers to. Preserve backward decode only through one explicit legacy policy; do not silently reinterpret existing opaque bytes as verified semantics. Do not add a new proving backend in this task.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_checkpoint_finalization -- --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_draft_final -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-01-PLAN.md current_task="Task 1: Introduce a truthful checkpoint statement and explicit legacy-opaque proof contract"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Checkpoint artifact types, bytes, and tests all describe one truthful opaque-attestation contract plus one explicit legacy compatibility policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the checkpoint verifier trust boundary explicit on build and store paths</name>
  <files>crates/z00z_storage/src/checkpoint/build.rs
crates/z00z_storage/src/checkpoint/store.rs
crates/z00z_storage/src/assets/store_internal/redb_backend.rs
crates/z00z_storage/tests/test_checkpoint_store_api.rs</files>
  <behavior>
    - Test 1: the default production-facing write path cannot claim self-sufficient checkpoint validity when it still delegates tx-proof and spent checks to external hooks.
    - Test 2: permissive verifier or spent-index doubles remain usable only in tests, simulation, or an explicitly named opaque-attestation path.
    - Test 3: persisted checkpoint artifacts created by the RedB path inherit the same honest proof semantics as the in-memory builder and do not bypass the store-facing policy.
  </behavior>
  <action>Implement the trust-hook decisions and Scope Gate from `028-CONTEXT.md` without changing the crate graph or introducing a new proof dependency. Tighten `build_cp_draft(...)`, store APIs, and RedB sealing so the trust boundary is explicit in code and tests. If the store currently emits opaque artifacts from an internal path, require that path to opt into explicit opaque-attestation semantics and document that tx-proof validity still depends on `TxProofVerifier` plus `SpentIndex`. Keep permissive hooks out of the default production write path. Prefer a small storage-owned adapter policy or constructor gate over a broad redesign.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_checkpoint_store_api -- --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_draft_build -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-01-PLAN.md current_task="Task 2: Make the checkpoint verifier trust boundary explicit on build and store paths"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The default checkpoint build and persistence path has one explicit trust contract instead of silently overclaiming self-sufficient verification.</done>
</task>

</tasks>

<verification>
Close this plan only after `z00z_storage` checkpoint artifacts no longer overstate proof semantics and the production write path states exactly where external verifier trust begins and ends.
</verification>

<success_criteria>
Phase 028 starts from an honest checkpoint contract: opaque payloads are clearly legacy or attestation-only, verified semantics are gated behind real validation, and the store path no longer looks stronger than it is.
</success_criteria>

<output>
After completion, create `.planning/phases/028-crypto-audit-storage/028-01-SUMMARY.md`
</output>