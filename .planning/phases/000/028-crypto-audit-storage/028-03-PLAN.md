<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 028-crypto-audit-storage
plan: "03"
type: execute
wave: 3
depends_on:
  - 028-02
files_modified:
  - crates/z00z_storage/src/assets/proof.rs
  - crates/z00z_storage/src/assets/store_internal/proof_help.rs
  - crates/z00z_storage/src/assets/store_internal/test_whitebox_proofs.rs
  - crates/z00z_storage/src/assets/store_internal/test_whitebox_state.rs
  - crates/z00z_storage/tests/test_checkpoint_root_binding.rs
  - crates/z00z_storage/tests/test_claim_source_proof.rs
autonomous: true
requirements:
  - PH28-ROOT-BIND
must_haves:
  truths:
    - `ProofBlob` carries one explicit, versioned commitment that binds the semantic state root to the backend JMT root.
    - `chk_blob(...)` fails closed on any semantic-root and backend-root mismatch before it accepts branch proofs.
    - Existing storage-owned proof exports and whitebox proof tests remain green under the stronger root-binding contract.
  artifacts:
    - path: crates/z00z_storage/src/assets/proof.rs
      provides: Versioned root-binding field and fail-closed verification path.
    - path: crates/z00z_storage/src/assets/store_internal/proof_help.rs
      provides: Proof-blob assembly that emits the new binding field consistently.
    - path: crates/z00z_storage/tests/test_checkpoint_root_binding.rs
      provides: Tamper coverage for semantic/backend-root binding failures.
    - path: crates/z00z_storage/tests/test_claim_source_proof.rs
      provides: Compatibility coverage for storage-owned proof export consumers.
  key_links:
    - from: crates/z00z_storage/src/assets/store_internal/proof_help.rs
      to: crates/z00z_storage/src/assets/proof.rs
      via: shared canonical binding field emitted and then verified by `chk_blob(...)`
      pattern: ProofBlob::new|backend_root|chk_blob
    - from: crates/z00z_storage/src/assets/proof.rs
      to: crates/z00z_storage/tests/test_checkpoint_root_binding.rs
      via: explicit tamper and mismatch regressions for semantic/backend-root commitments
      pattern: chk_blob|RootMix|backend_root
---

<objective>
Add explicit semantic-root and backend-root binding to storage proof blobs and fail closed on cross-root tampering.

Purpose: close the cross-root integrity gap so the semantic root and the JMT backend root are provably tied to the same store epoch.
Output: one versioned root-binding field in `ProofBlob`, one fail-closed verifier path, and one compatibility-safe proof emission update.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/028-crypto-audit-storage/028-FUSION.md
@.planning/phases/028-crypto-audit-storage/028-CONTEXT.md
@crates/z00z_storage/src/assets/proof.rs
@crates/z00z_storage/src/assets/store_internal/proof_help.rs
@crates/z00z_storage/src/assets/store_internal/test_whitebox_proofs.rs
@crates/z00z_storage/src/assets/store_internal/test_whitebox_state.rs
@crates/z00z_storage/tests/test_checkpoint_root_binding.rs
@crates/z00z_storage/tests/test_claim_source_proof.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce a versioned root-binding field in `ProofBlob` and verify it before branch proofs</name>
  <files>crates/z00z_storage/src/assets/proof.rs
crates/z00z_storage/tests/test_checkpoint_root_binding.rs
crates/z00z_storage/src/assets/store_internal/test_whitebox_proofs.rs</files>
  <behavior>
    - Test 1: `chk_blob(...)` rejects any blob where the semantic state root and backend JMT root commitment do not match the expected binding.
    - Test 2: a branch proof that is otherwise valid but paired with the wrong semantic root still fails before leaf or branch acceptance.
    - Test 3: the new binding field is versioned so future proof-format upgrades cannot silently reuse the same domain by accident.
  </behavior>
  <action>Implement the root-binding decisions and Gate G-03 from `028-CONTEXT.md`. Extend `ProofBlob` with one explicit domain-separated binding field that commits to both the semantic root and the backend root. Add one typed verification failure if that binding mismatches. Validate the binding before definition, serial, or asset branch proofs are accepted. Reuse the workspace domain-separation pattern; do not invent a new hash framework or reinterpret `backend_root` as the public semantic root.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_checkpoint_root_binding -- --nocapture
cargo test -p z00z_storage --release --lib test_proof_blob_codec -- --nocapture
cargo test -p z00z_storage --release --lib test_blob_replay_ -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-03-PLAN.md current_task="Task 1: Introduce a versioned root-binding field in ProofBlob and verify it before branch proofs"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`ProofBlob` has one explicit semantic/backend-root binding and `chk_blob(...)` fails closed on any cross-root mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-emit proof blobs with the new root-binding field and keep proof consumers compatible</name>
  <files>crates/z00z_storage/src/assets/store_internal/proof_help.rs
crates/z00z_storage/src/assets/store_internal/test_whitebox_state.rs
crates/z00z_storage/tests/test_claim_source_proof.rs</files>
  <behavior>
    - Test 1: store-generated proof blobs always populate the new root-binding field.
    - Test 2: claim-source proof export remains valid and decodable under the new `ProofBlob` format.
    - Test 3: root-mode parity tests stay green after the proof-format change.
  </behavior>
  <action>Update proof-blob assembly and consumers to emit and validate the new root-binding field consistently. Keep storage-owned claim-source proof export compatible, update whitebox state or proof tests to prove parity, and make the serialized-byte compatibility policy explicit if the new field changes canonical bytes. Do not defer compatibility handling to a later plan; this wave owns the proof-blob format freeze.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_claim_source_proof -- --nocapture
cargo test -p z00z_storage --release --lib test_root_parity_ -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-03-PLAN.md current_task="Task 2: Re-emit proof blobs with the new root-binding field and keep proof consumers compatible"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>All storage-owned proof emitters and proof consumers share the same explicit root-binding contract and compatibility policy.</done>
</task>

</tasks>

<verification>
Close this plan only after cross-root tampering fails deterministically and proof-blob producers plus consumers agree on the new versioned binding field.
</verification>

<success_criteria>
`z00z_storage` no longer relies on an implicit relationship between the semantic root and the backend JMT root; that relationship is now explicit, versioned, and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/028-crypto-audit-storage/028-03-SUMMARY.md`
</output>