<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 028-crypto-audit-storage
plan: "05"
type: execute
wave: 4
depends_on:
  - 028-03
  - 028-04
files_modified:
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_storage/src/assets/mod.rs
  - crates/z00z_storage/src/assets/store_internal/tx_plan.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend.rs
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
  - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
  - crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
  - crates/z00z_storage/tests/test_redb_mutation.rs
autonomous: true
requirements:
  - PH28-NULLIFIER
  - PH28-TRUST-HOOK
must_haves:
  truths:
    - Replay protection is keyed by canonical binary nullifier bytes instead of raw lower-hex text.
    - Storage retains only the nullifier metadata required for replay defense and operational traceability by default.
    - The storage or simulator boundary does not introduce a `z00z_storage -> z00z_wallets` dependency cycle while adopting canonical nullifier bytes.
    - `chain_id` remains consistency metadata or a validation check, not a substitute key for replay scope.
    - Production hardening hazards such as environment-triggered panics or fault-injection hooks are either typed, gated, or removed from the default path.
  artifacts:
    - path: crates/z00z_storage/src/assets/store.rs
      provides: Canonical binary nullifier keying and minimized replay-row contract.
    - path: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      provides: Binary nullifier persistence plus hardened production-default backend behavior.
    - path: crates/z00z_simulator/src/claim_pkg_consumer.rs
      provides: Boundary conversion from upstream claim packages into storage-owned canonical nullifier bytes without a crate-cycle.
    - path: crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
      provides: Replay, reload, and migration-parity regressions for the canonical nullifier path.
  key_links:
    - from: crates/z00z_simulator/src/claim_pkg_consumer.rs
      to: crates/z00z_storage/src/assets/store.rs
      via: canonical nullifier-byte handoff from upstream claim packages into storage-owned replay state
      pattern: ClaimNullTx|claim_null_rec|apply_claim_ops|nullifier
    - from: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      to: crates/z00z_storage/tests/test_redb_rehydrate.rs
      via: binary nullifier-row persistence and reload parity
      pattern: CLAIM_NULL_TABLE|claim_null_key|decode_claim_null_key
---

<objective>
Canonicalize claim-nullifier storage and close the remaining production hardening hazards with parity-safe validation.

Purpose: finish the phase by moving replay protection onto canonical binary nullifier state, minimizing retained correlation metadata, and removing the remaining storage-default hazards that would undermine a truthful release gate.
Output: one binary nullifier storage contract, one parity-safe migration rule, and one hardened production-default backend path.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/028-crypto-audit-storage/028-FUSION.md
@.planning/phases/028-crypto-audit-storage/028-CONTEXT.md
@.planning/phases/000/019-gaps-1/019-01-PLAN.md
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/assets/mod.rs
@crates/z00z_storage/src/assets/store_internal/tx_plan.rs
@crates/z00z_storage/src/assets/store_internal/redb_backend.rs
@crates/z00z_simulator/src/claim_pkg_consumer.rs
@crates/z00z_wallets/src/core/claim/nullifier.rs
@crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
@crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
@crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
@crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
@crates/z00z_storage/tests/test_redb_rehydrate.rs
@crates/z00z_storage/tests/test_redb_mutation.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move replay protection onto canonical binary nullifier state without a crate-cycle</name>
  <files>crates/z00z_storage/src/assets/store.rs
crates/z00z_storage/src/assets/mod.rs
crates/z00z_storage/src/assets/store_internal/tx_plan.rs
crates/z00z_storage/src/assets/store_internal/redb_backend.rs
crates/z00z_simulator/src/claim_pkg_consumer.rs
crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
crates/z00z_storage/tests/test_redb_rehydrate.rs</files>
  <behavior>
    - Test 1: claim replay lookup and persisted keys use canonical binary nullifier bytes, not raw lower-hex strings.
    - Test 2: old replay rows and new binary-key rows are proven equivalent by one explicit parity or migration check before the old key path is removed.
    - Test 3: the simulator or boundary layer converts upstream claim packages into storage-owned canonical bytes without adding a direct `z00z_storage -> z00z_wallets` dependency.
    - Test 4: replay rows retain only the minimum metadata needed for replay defense and operational traceability by default.
  </behavior>
  <action>Implement the nullifier decisions and Gates G-05 plus G-05A from `028-CONTEXT.md`. Replace `nullifier_hex: String` map keys and RedB row keys with a canonical binary nullifier newtype owned by `z00z_storage` in this phase; do not plan an extraction into another crate unless one concrete non-cyclic target is first identified and added as a proposed prerequisite. Use `crates/z00z_wallets/src/core/claim/nullifier.rs` only as the upstream derivation and byte-shape reference, not as a direct dependency target for storage. Update the simulator boundary so it hands storage canonical nullifier bytes rather than normalized strings. Keep `chain_id` only as consistency metadata or a validation check, not as replay-key scope. Minimize retained replay-row metadata; keep only what replay defense and essential auditing need. Define one explicit parity or migration proof for existing persisted rows before deleting the legacy text-key path.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage3_nullifier_store -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_tx_pipeline -- --nocapture
cargo test -p z00z_storage --release --test test_redb_rehydrate -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-05-PLAN.md current_task="Task 1: Move replay protection onto canonical binary nullifier state without a crate-cycle"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Replay protection is keyed by canonical binary nullifier bytes, migration parity is explicit, and no crate-cycle was introduced to achieve it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the remaining production-default hardening hazards and freeze the closeout gates</name>
  <files>crates/z00z_storage/src/assets/store.rs
crates/z00z_storage/src/assets/store_internal/redb_backend.rs
crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
crates/z00z_storage/tests/test_redb_mutation.rs
crates/z00z_storage/tests/test_redb_rehydrate.rs</files>
  <behavior>
    - Test 1: unsupported root-mode configuration returns one typed failure or guarded startup path instead of panicking in production-default flows.
    - Test 2: fault injection for RedB commit failure is gated to tests or an explicit debug-only mode and cannot surprise the default production path.
    - Test 3: the final release-facing validation covers root-mode parity, reload parity, and the binary nullifier migration path together.
    - Test 4: the exact Scenario 1 release-style gates remain green after truthful opaque semantics, binary nullifier migration, and hardened backend defaults land together.
  </behavior>
  <action>Use the final wave to close the local hardening hazards called out in `028-FUSION.md` and `028-CONTEXT.md` without reopening checkpoint semantics from earlier plans. Replace `RootMode::load()` panics with typed, explicit behavior at the store boundary. Gate `Z00Z_STORAGE_REDB_INJ` so fault injection is test-only or behind an explicit debug contract instead of the default backend path. Extend RedB mutation or reload tests so the final release-facing gate proves root-mode parity, replay-row parity, and hardened backend defaults together. If closeout fallout reaches the simulator acceptance surface, update the existing Scenario 1 gates in `test_stage6_checkpoint_final_gate.rs` and `test_scenario1_unified_gate.rs` instead of inventing a second release-only acceptance seam.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_redb_mutation -- --nocapture
cargo test -p z00z_storage --release --test test_redb_rehydrate -- --nocapture
if rg -n 'nullifier_hex:\s*String|claim_null_key\([^)]*&str' crates/z00z_storage/src crates/z00z_storage/tests crates/z00z_simulator/src crates/z00z_simulator/tests; then
  echo "legacy text-key nullifier path remains on canonical replay surfaces"
  exit 1
fi
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage6_checkpoint_final_gate -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_unified_gate -- --nocapture
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-05-PLAN.md current_task="Task 2: Remove the remaining production-default hardening hazards and freeze the closeout gates"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The remaining default-path storage hazards are gated or typed, and the final release-facing validation proves parity after the nullifier migration.</done>
</task>

</tasks>

<verification>
Close the phase only after binary nullifier replay state, migration parity, root-mode behavior, and RedB hardening all pass together under the final release-style gate.
</verification>

<success_criteria>
Phase 028 ends with canonical binary nullifier storage, minimized replay metadata, and no remaining default-path storage hazards that would undermine the audit closeout.
</success_criteria>

<output>
After completion, create `.planning/phases/028-crypto-audit-storage/028-05-SUMMARY.md`
</output>