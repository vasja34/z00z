<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 028-crypto-audit-storage
plan: "04"
type: execute
wave: 3
depends_on:
  - 028-02
files_modified:
  - crates/z00z_storage/src/checkpoint/ids.rs
  - crates/z00z_storage/src/checkpoint/link.rs
  - crates/z00z_storage/src/checkpoint/artifact.rs
  - crates/z00z_storage/src/checkpoint/store.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend.rs
  - crates/z00z_storage/tests/test_checkpoint_ids.rs
  - crates/z00z_storage/tests/test_checkpoint_link_injective.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
autonomous: true
requirements:
  - PH28-ID-BIND
must_haves:
  truths:
    - Draft, final checkpoint, and exec-input IDs are type-separated and domain-separated instead of relying on raw undifferentiated SHA-256 naming.
    - Checkpoint links are tamper-detectable from canonical bytes or an explicit link commitment.
    - `CheckpointAudit` and any other audit-only wrapper data remain outside canonical artifact bytes and outside ID or link commitments.
    - RedB persistence does not permit mixed-era checkpoint IDs and link bytes to coexist without one explicit compatibility rule.
  artifacts:
    - path: crates/z00z_storage/src/checkpoint/ids.rs
      provides: One canonical typed ID derivation helper for draft, checkpoint, and exec artifacts.
    - path: crates/z00z_storage/src/checkpoint/link.rs
      provides: Link commitment or equivalent canonical binding over checkpoint, snapshot, and exec IDs.
    - path: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      provides: Compatibility-safe persistence of the new ID and link semantics.
    - path: crates/z00z_storage/tests/test_checkpoint_ids.rs
      provides: Regression coverage for type-separated artifact IDs.
  key_links:
    - from: crates/z00z_storage/src/checkpoint/ids.rs
      to: crates/z00z_storage/src/checkpoint/artifact.rs
      via: explicit statement bytes and artifact class feed the typed ID derivation path
      pattern: derive_draft_id|derive_checkpoint_id|derive_exec_id|CheckpointPubIn
    - from: crates/z00z_storage/src/checkpoint/link.rs
      to: crates/z00z_storage/src/assets/store_internal/redb_backend.rs
      via: persisted link bytes and canonical tuple binding for checkpoint, snapshot, and exec IDs
      pattern: CheckpointLink|encode_link_bin|link_bytes
---

<objective>
Harden checkpoint IDs and checkpoint links with type-separated, tamper-detectable canonical binding.

Purpose: close the artifact-identity hardening gap so checkpoint IDs and checkpoint links cannot be confused, replayed across artifact classes, or silently mixed across compatibility eras.
Output: one typed ID derivation helper, one explicit checkpoint-link commitment, and one persistence policy that keeps IDs plus links in lockstep.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/028-crypto-audit-storage/028-FUSION.md
@.planning/phases/028-crypto-audit-storage/028-CONTEXT.md
@crates/z00z_storage/src/checkpoint/ids.rs
@crates/z00z_storage/src/checkpoint/link.rs
@crates/z00z_storage/src/checkpoint/artifact.rs
@crates/z00z_storage/src/checkpoint/store.rs
@crates/z00z_storage/src/assets/store_internal/redb_backend.rs
@crates/z00z_storage/tests/test_checkpoint_ids.rs
@crates/z00z_storage/tests/test_checkpoint_link_injective.rs
@crates/z00z_storage/tests/test_redb_rehydrate.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move checkpoint IDs onto one typed, domain-separated derivation helper</name>
  <files>crates/z00z_storage/src/checkpoint/ids.rs
crates/z00z_storage/src/checkpoint/artifact.rs
crates/z00z_storage/tests/test_checkpoint_ids.rs</files>
  <behavior>
    - Test 1: two artifact classes with the same canonical payload bytes still derive different IDs because the artifact class and statement domain are part of the ID input.
    - Test 2: draft, final checkpoint, and exec-input IDs remain deterministic within their own class.
    - Test 3: the public-input or statement fields that are consensus-relevant for checkpoint identity are part of the canonical ID derivation path.
  </behavior>
  <action>Implement the ID-binding decisions and Gate G-04 from `028-CONTEXT.md`. Replace raw undifferentiated SHA-256-over-bytes helpers with one typed, domain-separated ID derivation seam that covers `CheckpointDraftId`, `CheckpointId`, and `CheckpointExecInputId`. Reuse existing workspace hashing and framing patterns. If the final checkpoint identity needs a dedicated statement type beyond the current `CheckpointPubIn`, treat that type as proposed until it is added in code and route ID derivation through its explicit canonical bytes. Keep `CheckpointAudit` and any audit-only metadata out of ID input bytes.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_checkpoint_ids -- --nocapture
cargo test -p z00z_storage --release --test test_checkpoint_draft_final -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-04-PLAN.md current_task="Task 1: Move checkpoint IDs onto one typed, domain-separated derivation helper"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Checkpoint artifact IDs are type-separated and derived from one explicit canonical framing policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind checkpoint links canonically and keep RedB compatibility explicit</name>
  <files>crates/z00z_storage/src/checkpoint/link.rs
crates/z00z_storage/src/checkpoint/store.rs
crates/z00z_storage/src/assets/store_internal/redb_backend.rs
crates/z00z_storage/tests/test_checkpoint_link_injective.rs
crates/z00z_storage/tests/test_redb_rehydrate.rs</files>
  <behavior>
    - Test 1: tampering any one of `checkpoint_id`, `prep_snapshot_id`, or `exec_input_id` invalidates the checkpoint-link contract.
    - Test 2: RedB reload either preserves the same link-commitment semantics for legacy rows or rejects them under one explicit migration rule.
    - Test 3: ID derivation and link binding ship together so the store never accepts mixed old-ID and new-link tuples as if they were canonical.
  </behavior>
  <action>Bind `CheckpointLink` to canonical bytes rather than leaving it as a loose tuple with no cryptographic meaning. Either derive one explicit link commitment over `checkpoint_id + prep_snapshot_id + exec_input_id` or fold those fields into the final checkpoint statement in a way that makes link tampering detectable from canonical bytes. Update the checkpoint store and RedB backend in the same wave so IDs and links do not drift. Keep audit-only wrappers outside the link commitment. If canonical bytes change, define the compatibility rule here rather than punting it to summary-only documentation.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --test test_checkpoint_link_injective -- --nocapture
cargo test -p z00z_storage --release --test test_redb_rehydrate -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/028-crypto-audit-storage/028-04-PLAN.md current_task="Task 2: Bind checkpoint links canonically and keep RedB compatibility explicit"` in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Checkpoint links are canonical tamper-detectable bindings, and RedB compatibility for the new ID-plus-link era is explicit.</done>
</task>

</tasks>

<verification>
Close this plan only after checkpoint IDs and checkpoint links are hardened together and RedB reload proves that mixed-era tuples cannot slip through silently.
</verification>

<success_criteria>
Checkpoint identity in `z00z_storage` now has explicit class separation and explicit tuple binding instead of raw naming that can be over-trusted.
</success_criteria>

<output>
After completion, create `.planning/phases/028-crypto-audit-storage/028-04-SUMMARY.md`
</output>