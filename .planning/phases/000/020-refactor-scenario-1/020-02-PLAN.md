---
phase: 020-refactor-scenario-1
plan: 02
type: execute
wave: 2
depends_on:
  - 020-01
files_modified:
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - crates/z00z_simulator/src/scenario_1/stage_4.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/contracts.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/selection.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/outputs.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/prep.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/verify.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tamper.rs
  - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_simulator/tests/test_stage4_source_shape.rs
  - crates/z00z_simulator/tests/test_stage4_cfg_paths.rs
  - crates/z00z_simulator/tests/test_stage4_chain_path.rs
  - crates/z00z_simulator/tests/test_stage4_tamper.rs
  - crates/z00z_simulator/tests/test_pipeline_genesis_tx.rs
autonomous: true
requirements:
  - SCN1-03
  - SCN1-04
must_haves:
  truths:
    - The tx-prepare lane is split into `tx_plan` and `tx_prepare` as stages 5 and 6 instead of one large Stage 4 body.
    - Stage 4 transport, prep, and report continuity remain machine-readable after the split.
    - The split is owned by the existing `stage_4.rs` module lineage through two explicit tx-lane entrypoints rather than by reusing Stage 3 claim entrypoints.
    - Runner and YAML are updated in the same wave so the new tx-prepare stage surface is explicit and stable.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_4_utils/contracts.rs
      provides: Proposed helper target for typed local contracts in the split tx-prepare lane.
    - path: crates/z00z_simulator/tests/test_stage4_source_shape.rs
      provides: Proposed Wave 0 guardrail coverage for the new Stage 4 stage-split surface.
    - path: crates/z00z_simulator/tests/test_stage4_chain_path.rs
      provides: Canonical continuity coverage for the tx-prepare lane after stage-map expansion.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/runner.rs
      to: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      via: rewritten tx-prepare stage ids and rust_entry bindings
      pattern: stage_4::run_tx_plan|stage_4::run_tx_prepare
    - from: crates/z00z_simulator/src/scenario_1/stage_4.rs
      to: crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs
      via: compact Stage 4 facade delegating into local helpers
      pattern: run_tx_prepare|run_core|stage_4_utils
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Split the current tx-prepare lane into `tx_plan` and `tx_prepare`, and preserve its continuity artifacts.

Purpose: finish the user-selected Stage 3 or 4 priority lane before moving to Stage 5 or 6, while keeping ledger-path, prep, and report semantics stable.
Output: expanded tx-prepare stage wiring, a thinner `stage_4.rs`, deeper `stage_4_utils` ownership, synchronized YAML, and continuity regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/020-refactor-scenario-1/020-CONTEXT.md
@.planning/phases/020-refactor-scenario-1/020-RESEARCH.md
@.planning/phases/020-refactor-scenario-1/020-VALIDATION.md
@.planning/phases/020-refactor-scenario-1/020-01-PLAN.md
@crates/z00z_simulator/src/scenario_1/runner.rs
@crates/z00z_simulator/src/scenario_1/stage_4.rs
@crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs
@crates/z00z_simulator/src/scenario_1/stage_4_utils/paths.rs
@crates/z00z_simulator/src/scenario_1/scenario_config.yaml
@crates/z00z_simulator/src/scenario_1/scenario_design.yaml

<interfaces>
From crates/z00z_simulator/src/scenario_1/stage_4.rs:
```rust
pub(crate) use self::stage_4_utils::resolve_stage4_paths;
pub fn run_tx_prepare(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the tx-prepare stage split and move Stage 4 logic behind explicit helper seams</name>
  <files>crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/src/scenario_1/stage_4.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/contracts.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/selection.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/outputs.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/prep.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/verify.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/tamper.rs, crates/z00z_simulator/src/scenario_1/scenario_config.yaml, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_scenario1_stage_surface.rs, crates/z00z_simulator/tests/test_stage4_source_shape.rs</files>
  <action>Per D-01 through D-04, D-01A, D-08 through D-10, and G-01 through G-03, expand the runner and YAML stage surface again to introduce stage 5 `tx_plan` and stage 6 `tx_prepare`. The runner and YAML must bind these stages to explicit entrypoints under the existing `stage_4.rs` module lineage. The preferred proposed names are `stage_4::run_tx_plan` and `stage_4::run_tx_prepare`; if the implementation uses different names, declare them explicitly in this plan, `020-VALIDATION.md`, and `020-TEST-SPEC.md` in the same wave. At the same time, move the old Stage 4 monolith behind explicit `stage_4_utils` helper seams. Treat filenames such as `contracts.rs`, `selection.rs`, `outputs.rs`, `prep.rs`, `verify.rs`, and `tamper.rs` as proposed targets rather than verified existing files; if the real split fits better into the current `stage_4_utils` layout, keep the implementation aligned with the existing module tree and update this plan plus the validation docs in the same wave. Keep canonical wallet and storage logic in their owning crates and do not turn the split into micro-stages.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_scenario1_stage_surface -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage4_source_shape -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage4_tamper -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_source_shape -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_tamper -- --nocapture</automated>
  </verify>
  <done>The tx-prepare lane is explicitly split as stages 5 and 6 in runner and YAML, and Stage 4 no longer carries the full lane as one monolithic stage body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve Stage 4 continuity artifacts, path remap behavior, and machine-readable ledger fields</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_4.rs, crates/z00z_simulator/src/scenario_1/scenario_config.yaml, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_stage4_cfg_paths.rs, crates/z00z_simulator/tests/test_stage4_chain_path.rs, crates/z00z_simulator/tests/test_pipeline_genesis_tx.rs</files>
  <action>Per D-09, D-12 through D-14, and G-03 through G-05, preserve Stage 4 artifact names, JSON fields, path behavior, and continuity semantics while updating YAML to the new tx-prepare stage surface. Keep the ledger-path evidence machine-readable, keep `resolve_stage4_paths` behavior stable, and ensure the split still feeds the downstream apply and finalization lane without silent field drift.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage4_cfg_paths -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage4_chain_path -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_pipeline_genesis_tx -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_cfg_paths -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_chain_path -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_pipeline_genesis_tx -- --nocapture</automated>
  </verify>
  <done>The tx-prepare split preserves canonical continuity, path remap behavior, and machine-readable ledger-path evidence.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_scenario1_stage_surface -- --nocapture`, `cargo test -p z00z_simulator --test test_stage4_source_shape -- --nocapture`, `cargo test -p z00z_simulator --test test_stage4_cfg_paths -- --nocapture`, `cargo test -p z00z_simulator --test test_stage4_chain_path -- --nocapture`, `cargo test -p z00z_simulator --test test_pipeline_genesis_tx -- --nocapture`, and the matching release commands with `--features test-fast --features wallet_debug_dump`.
</verification>

<success_criteria>
The current Stage 4 lane is no longer one coarse execution stage, and its continuity artifacts remain stable and machine-readable across the new stage layout.
</success_criteria>

<output>
After completion, create `.planning/phases/020-refactor-scenario-1/020-02-SUMMARY.md`
</output>