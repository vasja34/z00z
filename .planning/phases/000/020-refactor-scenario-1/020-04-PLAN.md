---
phase: 020-refactor-scenario-1
plan: 04
type: execute
wave: 4
depends_on:
  - 020-03
files_modified:
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - crates/z00z_simulator/src/scenario_1/mod.rs
  - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_simulator/tests/test_claim_acceptance.rs
  - crates/z00z_simulator/tests/test_stage4_cfg_paths.rs
  - crates/z00z_simulator/tests/test_stage4_chain_path.rs
  - crates/z00z_simulator/tests/test_stage5_receive_bridge.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs
  - crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
  - crates/z00z_simulator/tests/test_stage7_jmt_wallet_scan.rs
  - crates/z00z_simulator/tests/test_stage8_proof_path.rs
  - crates/z00z_simulator/tests/test_pipeline_genesis_tx.rs
  - crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
autonomous: true
requirements:
  - SCN1-03
  - SCN1-04
  - SCN1-05
must_haves:
  truths:
    - The final Scenario 1 stage map is explicit, synchronized, and fixed as the 12-stage target from `genesis_init` through `checkpoint_finalize`.
    - The macro flow claim -> prepare -> receive -> bundle -> apply -> finalize remains stable even if the numeric stage map grew.
    - Release-style simulator gates remain green across the full refactored lane.
  artifacts:
    - path: crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
      provides: Final guardrail for the expanded stage map and macro ordering.
    - path: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      provides: Final synchronized stage definitions and rust_entry wiring.
    - path: crates/z00z_simulator/src/scenario_1/scenario_config.yaml
      provides: Final synchronized path and config structure for the expanded stage map.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/runner.rs
      to: crates/z00z_simulator/src/scenario_1/mod.rs
      via: final module-lineage and stage map wiring
      pattern: run_claim_prepare|run_claim_publish|run_tx_plan|run_tx_prepare|run_transfer_receive|run_transfer_claim|run_bundle_build|run_bundle_publish|run_apply|run_finalize
    - from: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      to: crates/z00z_simulator/src/scenario_1/scenario_config.yaml
      via: synchronized stage definitions, path contracts, and rust_entry bindings
      pattern: rust_entry|paths|stage
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Close Phase 020 on the final 12-stage surface, synchronized YAML, and release-style Scenario 1 acceptance gates.

Purpose: ensure the refactor finishes as a real stage-layout improvement rather than a partial helper shuffle, and prove that the whole Scenario 1 lane still behaves the same.
Output: final runner or module or YAML synchronization, updated stage-surface guardrails, and a green release-style simulator gate set.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/020-refactor-scenario-1/020-CONTEXT.md
@.planning/phases/020-refactor-scenario-1/020-RESEARCH.md
@.planning/phases/020-refactor-scenario-1/020-VALIDATION.md
@.planning/phases/020-refactor-scenario-1/020-03-PLAN.md
@crates/z00z_simulator/src/scenario_1/runner.rs
@crates/z00z_simulator/src/scenario_1/mod.rs
@crates/z00z_simulator/src/scenario_1/scenario_config.yaml
@crates/z00z_simulator/src/scenario_1/scenario_design.yaml

<interfaces>
From crates/z00z_simulator/src/scenario_1/runner.rs:
```rust
pub fn run() -> Result<ScenarioResult, Scenario1Err>;
fn build_stage_map() -> BTreeMap<u32, StageFn>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the final stage surface and YAML end state for the refactored Scenario 1 lane</name>
  <files>crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/src/scenario_1/mod.rs, crates/z00z_simulator/src/scenario_1/scenario_config.yaml, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_scenario1_stage_surface.rs, crates/z00z_simulator/tests/test_stage4_cfg_paths.rs, crates/z00z_simulator/tests/test_stage4_chain_path.rs</files>
  <action>Per D-08 through D-14, D-01A, and G-01 through G-04, make one final synchronization pass across `runner.rs`, `mod.rs`, `scenario_config.yaml`, and `scenario_design.yaml` so the 12-stage map is explicit, monotonic, and free of stale references to the old layout. This pass must also leave one explicit runner-to-module mapping for all final stages 3-12, with no hidden dependence on pre-refactor stage numbering. If any proposed entrypoint names from the earlier plans changed during implementation, normalize the final names here and update `020-TEST-SPEC.md` and `020-VALIDATION.md` in the same wave. Keep the macro flow claim -> prepare -> receive -> bundle -> apply -> finalize stable, keep path resolution and continuity fields machine-readable, and make `test_scenario1_stage_surface` assert the final 12-stage surface rather than the old fixed stage count.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_scenario1_stage_surface -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage4_cfg_paths -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage4_chain_path -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_cfg_paths -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage4_chain_path -- --nocapture</automated>
  </verify>
  <done>The final 12-stage map and YAML surface are synchronized, explicit, and no longer depend on the old fixed-stage assumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the phase on release-style Scenario 1 gates across the full refactored lane</name>
  <files>crates/z00z_simulator/tests/test_claim_acceptance.rs, crates/z00z_simulator/tests/test_stage5_receive_bridge.rs, crates/z00z_simulator/tests/test_stage6_checkpoint_storage_bridge.rs, crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs, crates/z00z_simulator/tests/test_stage7_jmt_wallet_scan.rs, crates/z00z_simulator/tests/test_stage8_proof_path.rs, crates/z00z_simulator/tests/test_pipeline_genesis_tx.rs, crates/z00z_simulator/tests/test_scenario1_unified_gate.rs</files>
  <action>Per G-04 through G-06, run the release-style simulator acceptance set over the refactored lane: claim acceptance, Stage 5 receive continuity, Stage 6 storage bridge continuity, Stage 6 final checkpoint separation, committed-state scan continuity for final stage 11 through the existing `test_stage7_jmt_wallet_scan` module-lineage anchor, proof-path continuity for final stage 12 through the existing `test_stage8_proof_path` module-lineage anchor, pipeline genesis continuity, unified acceptance, the full simulator release test surface, and the Scenario 1 release binary. Treat `test_stage7_jmt_wallet_scan` as continuity for the existing committed-state scan path only and do not overclaim closure of older wallet-runtime backlog items.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_claim_acceptance -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage5_receive_bridge -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage6_checkpoint_storage_bridge -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage7_jmt_wallet_scan -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage8_proof_path -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_pipeline_genesis_tx -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_acceptance -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage5_receive_bridge -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage6_checkpoint_storage_bridge -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage6_checkpoint_final_gate -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage7_jmt_wallet_scan -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_stage8_proof_path -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_pipeline_genesis_tx -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_unified_gate -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump</automated>
  </verify>
  <done>Phase 020 closes with a synchronized expanded stage surface and green release-style Scenario 1 gates across the full refactored lane.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_scenario1_stage_surface -- --nocapture`, the continuity tests for claim, Stage 5, Stage 6, Stage 7, Stage 8, pipeline, and unified acceptance in debug, then the matching release commands, `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`, and `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump`.
</verification>

<success_criteria>
Phase 020 is complete only when the new expanded stage map is explicit and synchronized, the macro flow is preserved, and the full release-style Scenario 1 gate set remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/020-refactor-scenario-1/020-04-SUMMARY.md`
</output>