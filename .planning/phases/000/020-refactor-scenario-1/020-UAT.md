---
status: testing
phase: 020-refactor-scenario-1
source: 020-01-SUMMARY.md, 020-02-SUMMARY.md, 020-03-SUMMARY.md, 020-04-SUMMARY.md
started: 2026-03-26T22:47:57Z
updated: 2026-03-26T22:47:57Z
---

# Phase 020 UAT

## Current Test

number: 1
name: Release Scenario 1 12-Stage Run
expected: |
  Run the release Scenario 1 binary. It should complete one explicit 12-stage flow with
  `stage.done` entries for ids 1 through 12 in this order: genesis_init, wallet_create,
  claim_prepare, claim_publish, tx_plan, tx_prepare, transfer_receive, transfer_claim,
  bundle_build, bundle_publish, checkpoint_apply_storage, checkpoint_finalize.
  Every stage should end with `result=ok`, and the run should finish with a success line.
awaiting: user response

## Tests

### 1. Release Scenario 1 12-Stage Run

expected: Run the release Scenario 1 binary and observe one explicit 12-stage success path from `genesis_init` through `checkpoint_finalize`, with every stage reporting `result=ok` and the run ending successfully.
result: [pending]

### 2. Claim Lane Artifacts

expected: After the run, the claim lane should expose the expected artifacts in the output tree, including the claim package, published claim store, audit log, and wallet debug dumps, without regressing the split between `claim_prepare` and `claim_publish`.
result: [pending]

### 3. Tx Lane Continuity Artifacts

expected: The tx lane should leave the expected machine-readable artifacts in place, including the tx package, `stage_4_snapshot.json`, and the wallet pending/confirmed state files, while preserving the explicit `tx_plan` and `tx_prepare` stage split.
result: [pending]

### 4. Transfer And Bundle Continuity

expected: The flow should preserve the explicit transfer and bundle split, with `transfer_receive`, `transfer_claim`, `bundle_build`, and `bundle_publish` all succeeding and their handoff artifacts remaining present for the downstream checkpoint stages.
result: [pending]

### 5. Final Checkpoint Publication

expected: The final checkpoint lane should produce a successful apply/finalize outcome, with `checkpoint_s7.json` reflecting stage 11, `checkpoint_s8.json` reflecting stage 12, and the final checkpoint artifact or link or audit surfaces present for the release opaque-test path.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

None.
