---
phase: 020-refactor-scenario-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - crates/z00z_simulator/src/scenario_1/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/audit.rs
  - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_simulator/tests/test_claim_acceptance.rs
  - crates/z00z_simulator/tests/test_claim_emit.rs
  - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
  - crates/z00z_simulator/tests/test_claim_snapshot.rs
  - crates/z00z_simulator/tests/test_claim_persist.rs
  - crates/z00z_simulator/tests/test_claim_integration.rs
autonomous: true
requirements:
  - SCN1-04
must_haves:
  truths:
    - Scenario 1 no longer treats the claim lane as one oversized stage, and the new runner or YAML surface makes `claim_prepare` and `claim_publish` explicit as stages 3 and 4.
    - The Stage 3 helper, fault-injector, state, and snapshot API surface remains stable while the claim lane is split.
    - Until the later Stage 4 tx-lane wave lands, both new claim stages must be served from the existing `stage_3.rs` module lineage through two explicit claim-stage entrypoints.
    - The macro flow still enters the claim lane before tx-prepare even if the numeric stage map expands.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/runner.rs
      provides: Expanded claim-lane stage map.
    - path: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      provides: Rewritten claim-lane stage definitions and rust_entry wiring.
    - path: crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
      provides: Proposed Wave 0 guardrail coverage for the new stage map instead of the old eight-stage assumption.
    - path: crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
      provides: Proposed helper target for an extracted claim-package seam behind stable Stage 3 exports.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/runner.rs
      to: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      via: rewritten stage ids and rust_entry bindings
      pattern: stage_3::run_claim_prepare|stage_3::run_claim_publish
    - from: crates/z00z_simulator/src/scenario_1/stage_3.rs
      to: crates/z00z_simulator/src/scenario_1/stage_3_utils/mod.rs
      via: stable Stage 3 helper and snapshot re-exports
      pattern: build_claim_package|build_claim_package_fault|write_claim_bundle|write_claim_bundle_fault|verify_resume_wire|Stage3Snapshot
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Split the current claim lane into `claim_prepare` and `claim_publish`, and align Stage 3 to the new runner and YAML surface.

Purpose: start the phase with the user-selected Stage 3 or 4 priority lane and replace the old coarse claim stage with the explicit stages `claim_prepare` and `claim_publish`.
Output: expanded claim-lane stage wiring in `runner.rs` and YAML, a thinner `stage_3.rs`, extracted claim-package and audit seams, and guardrail coverage for the new stage surface.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/020-refactor-scenario-1/020-CONTEXT.md
@.planning/phases/020-refactor-scenario-1/020-RESEARCH.md
@.planning/phases/020-refactor-scenario-1/020-VALIDATION.md
@crates/z00z_simulator/src/scenario_1/runner.rs
@crates/z00z_simulator/src/scenario_1/mod.rs
@crates/z00z_simulator/src/scenario_1/stage_3.rs
@crates/z00z_simulator/src/scenario_1/stage_3_utils/mod.rs
@crates/z00z_simulator/src/scenario_1/scenario_config.yaml
@crates/z00z_simulator/src/scenario_1/scenario_design.yaml
@crates/z00z_simulator/tests/test_claim_acceptance.rs

<interfaces>
From crates/z00z_simulator/src/scenario_1/runner.rs:
```rust
fn build_stage_map() -> BTreeMap<u32, StageFn>;
```

From crates/z00z_simulator/src/scenario_1/stage_3.rs:
```rust
pub fn build_claim_package(...);
pub fn build_claim_package_fault(...);
pub fn write_claim_bundle(...);
pub fn write_claim_bundle_fault(...);
pub fn run_claim_genesis(ctx: &mut SimContext, stage: &DesignStage) -> StageResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the claim-lane split in runner, YAML, and stage-surface guards</name>
  <files>crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/src/scenario_1/mod.rs, crates/z00z_simulator/src/scenario_1/scenario_config.yaml, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_scenario1_stage_surface.rs</files>
  <action>Per D-01 through D-04, D-01A, D-08, D-09, and G-01 through G-03, expand the Scenario 1 stage map to introduce stage 3 `claim_prepare` and stage 4 `claim_publish`. Update `runner.rs`, `mod.rs`, `scenario_config.yaml`, and `scenario_design.yaml` in one change set. In this wave, the runner and YAML must bind both new claim stages to explicit entrypoints under the existing `stage_3.rs` module lineage. The preferred proposed names are `stage_3::run_claim_prepare` and `stage_3::run_claim_publish`; if implementation uses different names, declare them explicitly in this plan, `020-VALIDATION.md`, and `020-TEST-SPEC.md` in the same wave. Keep stable Stage 3 module entry paths available to the runner and tests, and add `test_scenario1_stage_surface` as a new Wave 0 guardrail so the new 12-stage layout is asserted explicitly rather than inferred from older tests.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_scenario1_stage_surface -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_scenario1_stage_surface -- --nocapture</automated>
  </verify>
  <done>The claim-lane split is explicit as stages 3 and 4 in the runner and YAML, and a dedicated guard locks the new 12-stage surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align Stage 3 internals with the split claim lane while preserving its public helper surface</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/mod.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/audit.rs, crates/z00z_simulator/tests/test_claim_acceptance.rs, crates/z00z_simulator/tests/test_claim_emit.rs, crates/z00z_simulator/tests/test_claim_tx_pipeline.rs, crates/z00z_simulator/tests/test_claim_snapshot.rs, crates/z00z_simulator/tests/test_claim_persist.rs, crates/z00z_simulator/tests/test_claim_integration.rs</files>
  <action>Per D-02, D-03, D-12 through D-14, and the Stage 3 guidance in `020-RESEARCH.md`, move remaining claim-package and audit logic behind `stage_3_utils` while keeping the currently used Stage 3 public surface stable. Treat `stage_3_utils/claim_pkg.rs` and `stage_3_utils/audit.rs` as proposed helper filenames, not verified existing files; if the implementation uses different helper names, update this plan, `020-VALIDATION.md`, and `020-TEST-SPEC.md` in the same wave. Do not change claim artifact meaning, wallet-visible behavior, or canonical publication semantics. The split should make each new claim-lane stage homogeneous rather than merely moving large code blocks into hidden helper files.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_claim_acceptance -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_claim_emit -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_claim_snapshot -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_claim_persist -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_claim_integration -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_acceptance -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_emit -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_tx_pipeline -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_snapshot -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_persist -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_integration -- --nocapture</automated>
  </verify>
  <done>The claim lane is implemented as smaller homogeneous stages, and Stage 3 still exposes the helper and snapshot surface that downstream tests already use.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_scenario1_stage_surface -- --nocapture`, the Stage 3 claim test set in debug, and the matching release commands with `--features test-fast --features wallet_debug_dump`.
</verification>

<success_criteria>
Phase 020 starts with an explicit claim-lane stage split, stable Stage 3 helper exports, and no remaining dependency on the old one-stage claim layout.
</success_criteria>

<output>
After completion, create `.planning/phases/020-refactor-scenario-1/020-01-SUMMARY.md`
</output>