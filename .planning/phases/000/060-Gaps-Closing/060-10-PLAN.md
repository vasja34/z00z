---
phase: 060-Gaps-Closing
plan: 060-10
type: execute
wave: 3
depends_on:
  - 060-05
files_modified:
  - crates/z00z_storage/benches/settlement_benches.md
  - crates/z00z_storage/tests/test_bench_lanes.rs
  - crates/z00z_storage/scripts/run_storage_settlement_bench.py
  - crates/z00z_storage/benches/settlement_shard.rs
  - config/hjmt_runtime/sim_5a7s/manifest.json
  - crates/z00z_simulator/src/scenario_1/runtime_observability.rs
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
requirements:
  - None
autonomous: true
must_haves:
  truths:
    - "Throughput claims must be backed by explicit run-root artifacts that report `durable_root_published_tps`."
    - "The A/B gate must compare `aggregator_owned` and `shard_process` on the same hardware, profile, shard count, operation mix, cache mode, persistence mode, and route generation."
    - "The default stays `aggregator_owned` unless durable throughput and operational recovery both improve or remain neutral."
  artifacts:
    - .planning/phases/060-Gaps-Closing/060-TODO.md
    - .planning/phases/060-Gaps-Closing/060-CONTEXT.md
    - .planning/phases/060-Gaps-Closing/060-TZ1.md
    - reports/z00z-verification-orchestrator-20260618-170025/profiling/hjmt-summary.json
    - crates/z00z_storage/benches/settlement_benches.md
    - crates/z00z_storage/tests/test_bench_lanes.rs
  key_links:
    - "This slice owns `B5` and `B6`, depends on topology and failover closure from `060-05`, and must keep the Phase 058 evidence home as the only canonical bench archive."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 060-10 Plan: HJMT Measurement Lanes And A/B Rerun Packet

<objective>
Close `B5` and `B6` by separating HJMT evidence lanes, rerunning the relevant
measurements on a fair A/B basis, and keeping `aggregator_owned` as the
production default unless the full durable-path gate passes.
</objective>

<context>
@.planning/phases/060-Gaps-Closing/060-TODO.md
@.planning/phases/060-Gaps-Closing/060-CONTEXT.md
@.planning/phases/060-Gaps-Closing/060-TZ1.md
@.planning/phases/060-Gaps-Closing/060-TEST-SPEC.md
@.planning/phases/000/058-HJMT-benchmarks/058-05-PLAN.md
@reports/z00z-verification-orchestrator-20260618-170025/profiling/hjmt-summary.json
@crates/z00z_storage/benches/settlement_benches.md
@crates/z00z_storage/tests/test_bench_lanes.rs
@crates/z00z_storage/scripts/run_storage_settlement_bench.py
@crates/z00z_simulator/src/scenario_1/runtime_observability.rs
</context>

<coverage_contract>
This plan owns `B5` and `B6`. It must separate microbench, whole-command,
scenario-runtime, and throughput evidence, then rerun the HJMT packet without
creating a second benchmark archive or promoting `shard_process` prematurely.
</coverage_contract>

<tasks>
<task type="auto">
<name>Separate HJMT evidence lanes and publish the A/B rerun packet for `aggregator_owned` versus `shard_process`</name>
<read_first>
- `.planning/phases/060-Gaps-Closing/060-TODO.md`
- `.planning/phases/060-Gaps-Closing/060-CONTEXT.md`
- `.planning/phases/060-Gaps-Closing/060-TZ1.md`
- `reports/z00z-verification-orchestrator-20260618-170025/profiling/hjmt-summary.json`
- `crates/z00z_storage/benches/settlement_benches.md`
- `crates/z00z_storage/tests/test_bench_lanes.rs`
- `crates/z00z_storage/scripts/run_storage_settlement_bench.py`
- `crates/z00z_storage/benches/settlement_shard.rs`
- `config/hjmt_runtime/sim_5a7s/manifest.json`
- `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
- `crates/z00z_simulator/src/scenario_1/scenario_design.yaml`
- `crates/z00z_simulator/src/scenario_1/scenario_config.yaml`
</read_first>
<files>
- `crates/z00z_storage/benches/settlement_benches.md`
- `crates/z00z_storage/tests/test_bench_lanes.rs`
- `crates/z00z_storage/scripts/run_storage_settlement_bench.py`
- `crates/z00z_storage/benches/settlement_shard.rs`
- `config/hjmt_runtime/sim_5a7s/manifest.json`
- `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
- `crates/z00z_simulator/src/scenario_1/scenario_design.yaml`
- `crates/z00z_simulator/src/scenario_1/scenario_config.yaml`
</files>
<action>
Implement `B5` and `B6` exactly. Label every HJMT report as one of four lanes:
Criterion closure timing, whole-command `/usr/bin/time -v`, scenario stage
runtime, or user-facing throughput. Keep the existing Phase 058 storage output
home as the only canonical benchmark archive. Update the bench runner and docs
so the selected shard mapping is recorded in run artifacts, then rerun the
relevant HJMT packet for both `aggregator_owned` and `shard_process` on the
same hardware, profile, shard count, operation mix, cache mode, persistence
mode, and route generation. The A/B packet must report
`durable_root_published_tps`, `worker_local_tps`, `hjmt_journal_sync`,
publication latency, blocked time, RSS, CPU utilization, restart time, and
failover recovery time. Reject any default-promotion claim that only improves
worker-local numbers while the durable path or recovery cost regresses. Do not
create a second bench home, and keep the current
`reports/z00z-verification-orchestrator-20260618-170025/profiling/hjmt-summary.json` style "measured=false without
run-root throughput artifact" warning honest until a real throughput packet
exists. Do not treat proof-size or single-proof timing as TPS evidence.
</action>
<acceptance_criteria>
- every HJMT report names its measurement lane explicitly.
- throughput verdicts are backed by `durable_root_published_tps` artifacts.
- the A/B packet compares `aggregator_owned` and `shard_process` fairly on the
  same operational inputs.
- promotion is rejected unless durable throughput and recovery are neutral or
  better.
- `aggregator_owned` remains the default unless the full gate passes.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `cargo test -p z00z_storage --release --test test_bench_lanes -- --nocapture`.
3. Run the existing storage bench script and HJMT scenario reruns for both `aggregator_owned` and `shard_process` on identical hardware, keeping separate artifacts for Criterion timings, `/usr/bin/time -v`, scenario stage-runtime splits, and throughput reports.
4. Inspect the resulting A/B run-root artifacts and confirm they report `durable_root_published_tps`, `worker_local_tps`, `hjmt_journal_sync`, publication latency, blocked time, RSS, CPU utilization, restart time, and failover recovery time for both mappings, and that proof-size or single-proof timing is not used as TPS evidence.
5. Run `cargo test --release`.
6. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
7. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The HJMT evidence packet speaks honestly about measurement class, durable TPS,
and operational recovery, and the production default stays evidence-driven.
</done>
</task>
</tasks>

<threat_model>
- Mixed measurement lanes would let proof timing or local worker throughput
  masquerade as publication throughput.
- A/B runs on different hardware or cache conditions would create fake wins
  that cannot justify a production-default change.
</threat_model>

<verification>
This slice is valid only if the measurement contract becomes explicit and the
default-mapping verdict stays tied to the durable publication path.
</verification>

<success_criteria>
Phase 060 can decide future HJMT performance work from one honest evidence
packet instead of mixed benchmark stories.
</success_criteria>

<output>
Create `.planning/phases/060-Gaps-Closing/060-10-SUMMARY.md` when done.
</output>
