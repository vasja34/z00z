---
phase: 060-Gaps-Closing
plan: 060-06
type: execute
wave: 2
depends_on:
  - 060-01
files_modified:
  - .github/skills/z00z-l4-security-engineering-gate/scripts/audit-supply-chain.sh
  - deny.toml
  - .reviews/reviewed-advisories.toml
  - .reviews/config.toml
  - .reviews/audits.toml
requirements:
  - None
autonomous: true
must_haves:
  truths:
    - "Project-owned advisories must end as upgrades, removals, or reviewed exceptions with explicit owners, reasons, scope, and sunset."
    - "Protected-vendor findings stay on wrapper, pin, upstream, or isolation tracks; direct edits under `crates/z00z_crypto/tari/**` remain forbidden."
    - "Cargo-vet bootstrap exemptions are evidence of missing review work, not mature trust."
    - "The workspace currently has no verified repository-owned advisory review store; `supply-chain/` is the proposed canonical home unless implementation discovers an existing repository-owned equivalent first."
    - "The baseline evidence includes `reports/z00z-verification-orchestrator-20260618-170025/logs/l4-supply-chain.log` with `Vetting Succeeded (776 exempted)`, which must be treated as immature trust rather than closure."
  artifacts:
    - .planning/phases/060-Gaps-Closing/060-TODO.md
    - .planning/phases/060-Gaps-Closing/060-CONTEXT.md
    - .planning/phases/060-Gaps-Closing/060-TZ2.md
    - .planning/phases/060-Gaps-Closing/060-z00z-verification-report.md
    - reports/z00z-verification-orchestrator-20260618-170025/logs/l4-supply-chain.log
    - reports/z00z-verification-orchestrator-20260618-170025/supply-chain/supply-chain-project.md
    - reports/z00z-verification-orchestrator-20260618-170025/.reviews/reviewed-advisories.toml
  key_links:
    - "This slice owns `C2`, depends on the docs posture frozen in `060-01`, and unblocks adversarial closure in `060-09`."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 060-06 Plan: Supply-Chain Review Records And Vet Trust Closure

<objective>
Close `C2` by creating repository-owned advisory review records, wiring the
supply-chain gate to that canonical review store, and moving cargo-vet from
bootstrap-only signaling toward meaningful trust.
</objective>

<context>
@.planning/phases/060-Gaps-Closing/060-TODO.md
@.planning/phases/060-Gaps-Closing/060-CONTEXT.md
@.planning/phases/060-Gaps-Closing/060-TZ2.md
@.planning/phases/060-Gaps-Closing/060-TEST-SPEC.md
@.planning/phases/060-Gaps-Closing/060-z00z-verification-report.md
@.github/skills/z00z-l4-security-engineering-gate/scripts/audit-supply-chain.sh
@deny.toml
@reports/z00z-verification-orchestrator-20260618-170025/logs/l4-supply-chain.log
@reports/z00z-verification-orchestrator-20260618-170025/supply-chain/supply-chain-project.md
@reports/z00z-verification-orchestrator-20260618-170025/.reviews/reviewed-advisories.toml
</context>

<coverage_contract>
This plan owns `C2` only. It must close project-owned advisory review debt and
vet-trust debt without inventing a second supply-chain authority layer or
editing protected vendor code.
</coverage_contract>

<tasks>
<task type="auto">
<name>Create repository-owned advisory review records and a real cargo-vet trust path</name>
<read_first>
- `.planning/phases/060-Gaps-Closing/060-TODO.md`
- `.planning/phases/060-Gaps-Closing/060-CONTEXT.md`
- `.planning/phases/060-Gaps-Closing/060-TZ2.md`
- `.planning/phases/060-Gaps-Closing/060-z00z-verification-report.md`
- `.github/skills/z00z-l4-security-engineering-gate/scripts/audit-supply-chain.sh`
- `deny.toml`
- `reports/z00z-verification-orchestrator-20260618-170025/logs/l4-supply-chain.log`
- `reports/z00z-verification-orchestrator-20260618-170025/supply-chain/supply-chain-project.md`
- `reports/z00z-verification-orchestrator-20260618-170025/.reviews/reviewed-advisories.toml`
</read_first>
<files>
- `.github/skills/z00z-l4-security-engineering-gate/scripts/audit-supply-chain.sh`
- `deny.toml`
- `.reviews/reviewed-advisories.toml`
- `.reviews/config.toml`
- `.reviews/audits.toml`
</files>
<action>
Implement `C2` exactly. Create one repository-owned advisory review file and
one repository-owned cargo-vet store under `supply-chain/`, unless an existing
repository-owned equivalent is discovered first. If such a store is found,
extend it and mark `supply-chain/` as superseded rather than creating a second
authority home. Wire both the L4 gate and orchestrator wrapper to treat the
selected repository-owned files as canonical review inputs while per-run
reports remain snapshots only. Inventory and close the four project-owned
advisories called out in `060-TODO.md`: `bincode 2.0.1 /
RUSTSEC-2025-0141`, `paste 1.0.15 / RUSTSEC-2024-0436`, `derivative 2.2.0 /
RUSTSEC-2024-0388`, and `instant 0.1.13 / RUSTSEC-2024-0384`. For each one,
either remove or replace the dependency, or add a reviewed exception with
owner, reason, scope, sunset, dependency ancestry, code-path criticality,
replacement cost, and temporary-exception conditions. Track protected-vendor
`bincode 1.3.3 / RUSTSEC-2025-0141` separately through wrapper, pin,
upstream, or isolation work rather than mixing it into the project-owned
closure ledger. Treat `bincode 2.0.1 / RUSTSEC-2025-0141` as the highest
priority project-owned advisory because the TODO evidence anchors it in
`z00z_utils` with wide fan-out into `z00z_core` and `z00z_wallets`. Treat
`paste 1.0.15 / RUSTSEC-2024-0436` as a transitive advisory that must confirm
whether the live ancestry still flows through the current `p3-*` chain.
Promote cargo-vet beyond bootstrap-only signaling by shrinking or justifying
the logged `776` exemptions in a repository-owned store. Do not edit protected
vendor code; vendor findings may only be tracked through wrapper, isolation,
pin, or upstream actions.
</action>
<acceptance_criteria>
- no project-owned advisory remains both unresolved and unreviewed.
- the selected repository-owned advisory store is explicit; if no existing
  equivalent is discovered first, the default target is
  `.reviews/reviewed-advisories.toml`.
- the selected repository-owned vet config and companion audit store are
  explicit; if no existing equivalent is discovered first, the default targets
  are `.reviews/config.toml` and `.reviews/audits.toml`.
- the gate and orchestrator wrapper use the selected repository-owned store
  rather than a report-local snapshot.
- each reviewed exception records owner, reason, scope, sunset, dependency
  ancestry, code-path criticality, replacement cost, and temporary-exception
  conditions.
- vendor findings are tracked without protected-vendor source edits.
- cargo-vet trust is no longer represented only by bootstrap exemptions.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `Z00Z_L4_STRICT=1 ./.github/skills/z00z-l4-security-engineering-gate/scripts/audit-supply-chain.sh`.
3. Run `rg -n "RUSTSEC-2025-0141|RUSTSEC-2024-0436|RUSTSEC-2024-0388|RUSTSEC-2024-0384|reviewed =|criteria|owner|reason|scope|sunset|ancestry|criticality|replacement|exception"` against the selected repository-owned advisory and vet-store files. If no existing equivalent was discovered first, the default target set is `.reviews/reviewed-advisories.toml`, `.reviews/config.toml`, and `.reviews/audits.toml`, plus `reports/z00z-verification-orchestrator-20260618-170025/supply-chain/supply-chain-project.md`.
4. Run `cargo vet check` once the selected repository-owned vet config exists and record the bootstrap-exemption delta versus the current report baseline. If no existing equivalent was discovered first, the default config path is `.reviews/config.toml`.
5. Run `cargo test --release` if this slice changes Rust code, Cargo manifests, or test-affecting verification scripts; otherwise record why the broader Rust gate was not required for this slice.
6. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
7. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The supply-chain gate has one repository-owned review ledger, one meaningful
vet-trust path, and no hidden project advisory debt behind bootstrap green-ness.
</done>
</task>
</tasks>

<threat_model>
- Keeping advisory review records only under one generated report root would
  preserve the "no repository decision source" gap.
- Treating cargo-vet bootstrap init as trusted closure would hide the real
  review backlog behind a misleading pass signal.
</threat_model>

<verification>
This slice is valid only if project-owned advisories become repository-owned
decisions and the trusted store moves out of report-local bootstrap status.
</verification>

<success_criteria>
Phase 060 has one supply-chain review source of truth and one honest vet-trust
baseline before adversarial reruns are used as closure evidence.
</success_criteria>

<output>
Create `.planning/phases/060-Gaps-Closing/060-06-SUMMARY.md` when done.
</output>
