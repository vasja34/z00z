---
phase: 060-Gaps-Closing
plan: 060-12
type: execute
wave: 4
depends_on:
  - 060-05
files_modified:
  - crates/z00z_storage/src/backend/roots.rs
  - crates/z00z_storage/src/backend/redb/hjmt.rs
  - crates/z00z_storage/src/settlement/hjmt_batch_proof.rs
  - crates/z00z_storage/src/settlement/hjmt_commit.rs
  - crates/z00z_storage/src/settlement/hjmt_journal.rs
  - crates/z00z_storage/src/settlement/proof_batch.rs
  - crates/z00z_storage/src/settlement/proof_batch_verify.rs
  - crates/z00z_storage/src/settlement/store.rs
  - crates/z00z_storage/src/settlement/README.md
  - crates/z00z_runtime/aggregators/src/recovery.rs
  - crates/z00z_runtime/aggregators/README.md
  - crates/z00z_runtime/aggregators/src/types.rs
  - crates/z00z_runtime/validators/src/checkpoint.rs
  - crates/z00z_runtime/watchers/Cargo.toml
  - crates/z00z_runtime/watchers/src/publication.rs
  - crates/z00z_rollup_node/src/config.rs
  - crates/z00z_storage/tests/test_hjmt_root_generation.rs
  - crates/z00z_storage/tests/test_hjmt_historical_proofs.rs
  - crates/z00z_storage/tests/test_hjmt_import_export.rs
  - crates/z00z_storage/tests/test_hjmt_batch_proof.rs
  - crates/z00z_storage/tests/test_hjmt_batch_proof_negative.rs
  - crates/z00z_storage/src/settlement/test_live_recovery.rs
  - crates/z00z_runtime/aggregators/tests/test_hjmt_failover_same_lineage.rs
  - crates/z00z_runtime/aggregators/tests/test_hjmt_split_brain_fencing.rs
  - crates/z00z_runtime/aggregators/tests/test_hjmt_shard_routing.rs
  - crates/z00z_runtime/aggregators/tests/test_hjmt_publish.rs
  - crates/z00z_rollup_node/tests/test_hjmt_preflight.rs
  - crates/z00z_rollup_node/tests/test_hjmt_topology.rs
  - crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs
  - crates/z00z_runtime/watchers/tests/test_hjmt_publication_contract.rs
  - crates/z00z_storage/tests/fixtures/hjmt_upgrade/shard_root_leaf_v1/manifest.json
  - crates/z00z_storage/tests/fixtures/hjmt_upgrade/checkpoint_publication_v1/manifest.json
  - crates/z00z_simulator/tests/scenario_1/test_claim_integration.rs
  - docs/tech-papers/done/Z00Z-HJMT-Gaps.md
requirements:
  - None
autonomous: true
must_haves:
  truths:
    - "This slice is an audit-driven reopen of three live HJMT storage seams only: live root-of-shard-roots, route-bound per-shard durable journal, and self-contained publication coverage against the committed route table."
    - "This slice does not mint a new canonical Phase 060 task id. It is a subordinate HJMT closeout slice inside the existing Workstream B contract and must reuse the existing topology, failover, publication, and evidence homes."
    - "Do not create a second route-table codec or a second publication checker. One canonical route-table byte contract must be shared across planner, storage, validators, watchers, and rollup-node preflight."
    - "Once root generation 1 is active, `SettlementStateRoot` exported by storage must be the committed root-of-shard-roots, not a parallel monolithic fallback root."
    - "Durable recovery must be able to prove shard identity, routing generation, route digest, and journal lineage from storage-owned state instead of reconstructing them only from runtime placement memory."
    - "If this slice lands after `060-10` starts, all HJMT measurement and final-closeout evidence must be rerun on the post-`060-12` tree before any durable-throughput or full-closeout verdict stays authoritative."
    - "Do not treat this as another codec-only or fixture-only slice. The required closeout is live storage wiring plus downstream shared acceptance."
  artifacts:
    - .planning/phases/060-Gaps-Closing/060-TODO.md
    - .planning/phases/060-Gaps-Closing/060-CONTEXT.md
    - .planning/phases/060-Gaps-Closing/HJMT-RAID -Sharding.md
    - docs/tech-papers/done/Z00Z-HJMT-Design.md
    - docs/tech-papers/done/Z00Z-HJMT-Gaps.md
    - docs/tech-papers/done/Z00Z-HJMT-Threat-Model.md
    - docs/tech-papers/done/Z00Z-HJMT-Upgrade.md
    - crates/z00z_storage/src/backend/roots.rs
    - crates/z00z_storage/src/settlement/hjmt_journal.rs
    - crates/z00z_storage/src/settlement/proof_batch.rs
    - crates/z00z_runtime/aggregators/src/batch_planner.rs
    - crates/z00z_runtime/aggregators/src/recovery.rs
    - crates/z00z_rollup_node/src/config.rs
  key_links:
    - "This slice uses the exact repo audit against `HJMT-RAID -Sharding.md` and the Phase 057/058 HJMT corpus to close wiring gaps without widening into a second authority layer."
    - "This slice sits under the existing Phase 060 HJMT closure lane. It must leave `060-05` lawful-topology work intact and force refreshed evidence for `060-10` and `060-11` afterward."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 060-12 Plan: HJMT Core Storage Shard Truth Closure

<objective>
Close the remaining HJMT core-storage integration seams by making
root-generation-1 root-of-shard-roots live in persisted storage, binding the
durable journal and recovery export to shard route identity, and moving exact
publication coverage against the committed route table into the shared
storage-owned acceptance path.
</objective>

<context>
@.planning/phases/060-Gaps-Closing/060-TODO.md
@.planning/phases/060-Gaps-Closing/060-CONTEXT.md
@.planning/phases/060-Gaps-Closing/HJMT-RAID -Sharding.md
@docs/tech-papers/done/Z00Z-HJMT-Design.md
@docs/tech-papers/done/Z00Z-HJMT-Gaps.md
@docs/tech-papers/done/Z00Z-HJMT-Threat-Model.md
@docs/tech-papers/done/Z00Z-HJMT-Upgrade.md
@crates/z00z_storage/src/backend/roots.rs
@crates/z00z_storage/src/backend/redb/hjmt.rs
@crates/z00z_storage/src/settlement/hjmt_commit.rs
@crates/z00z_storage/src/settlement/hjmt_journal.rs
@crates/z00z_storage/src/settlement/hjmt_batch_proof.rs
@crates/z00z_storage/src/settlement/proof_batch.rs
@crates/z00z_storage/src/settlement/proof_batch_verify.rs
@crates/z00z_storage/src/settlement/store.rs
@crates/z00z_runtime/aggregators/src/batch_planner.rs
@crates/z00z_runtime/aggregators/src/recovery.rs
@crates/z00z_runtime/aggregators/src/types.rs
@crates/z00z_runtime/validators/src/checkpoint.rs
@crates/z00z_rollup_node/src/config.rs
@crates/z00z_storage/tests/test_hjmt_root_generation.rs
@crates/z00z_storage/tests/test_hjmt_historical_proofs.rs
@crates/z00z_storage/src/settlement/test_live_recovery.rs
@crates/z00z_runtime/aggregators/tests/test_hjmt_failover_same_lineage.rs
@crates/z00z_runtime/aggregators/tests/test_hjmt_split_brain_fencing.rs
@crates/z00z_rollup_node/tests/test_hjmt_preflight.rs
@crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs
@crates/z00z_runtime/watchers/tests/test_hjmt_publication_contract.rs
</context>

<coverage_contract>
This plan closes exactly three reopened HJMT seams:

1. live root-of-shard-roots storage and reload semantics;
2. route-bound per-shard durable journal and recovery semantics;
3. self-contained publication coverage against the committed route table.

It must not invent a second route-table byte contract, a second publication
acceptance path, or a second public-root authority plane. It is a corrective
closeout slice under the existing `B1..B6` HJMT lane, not a new parallel
task surface.
</coverage_contract>

<tasks>
<task type="auto">
<name>Make root-generation-1 root-of-shard-roots a live persisted storage root instead of a proof-only side contract</name>
<read_first>
- `.planning/phases/060-Gaps-Closing/HJMT-RAID -Sharding.md`
- `docs/tech-papers/done/Z00Z-HJMT-Upgrade.md`
- `docs/tech-papers/done/Z00Z-HJMT-Gaps.md`
- `crates/z00z_storage/src/backend/roots.rs`
- `crates/z00z_storage/src/backend/redb/hjmt.rs`
- `crates/z00z_storage/src/settlement/proof_batch.rs`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_storage/tests/test_hjmt_root_generation.rs`
- `crates/z00z_storage/tests/test_hjmt_historical_proofs.rs`
- `crates/z00z_storage/src/settlement/test_live_recovery.rs`
</read_first>
<files>
- `crates/z00z_storage/src/backend/roots.rs`
- `crates/z00z_storage/src/backend/redb/hjmt.rs`
- `crates/z00z_storage/src/settlement/proof_batch.rs`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_storage/tests/test_hjmt_root_generation.rs`
- `crates/z00z_storage/tests/test_hjmt_historical_proofs.rs`
- `crates/z00z_storage/src/settlement/test_live_recovery.rs`
- `crates/z00z_storage/tests/fixtures/hjmt_upgrade/shard_root_leaf_v1/manifest.json`
- `crates/z00z_storage/tests/fixtures/hjmt_upgrade/checkpoint_publication_v1/manifest.json`
</files>
<action>
Implement the live root-generation-1 storage path exactly. `ShardRootLeafV1`,
`CheckpointPublicationV1`, and `CheckpointPublicationProofV1` already exist as
proof contracts; this slice must wire them into persisted storage truth.

Add an explicit versioned live root state to `HjmtRoots` and the RedB HJMT
reload path so storage can persist and reload the ordered shard-leaf set or an
equivalent versioned publication state that is sufficient to reconstruct the
same public root without external runtime memory. Route
`SettlementStore::settlement_root(...)`, `settlement_root_for_version(...)`,
historical load, and recovery export through this live state once generation 1
is active. Keep the current monolithic `SettlementV1` root only as generation-0
history. Do not silently keep a parallel monolithic live root after
root-of-shard-roots activation.

Land the exact no-op migration bridge required by the upgrade paper:
`one route table + one shard + one shard leaf + same settlement semantics`.
That bridge must prove that the first generation-1 public root preserves the
same semantic settlement truth as the last generation-0 root, and that
historical proof verification continues to accept both generations under their
correct metadata.
</action>
<acceptance_criteria>
- persisted HJMT state stores enough versioned generation-1 publication data to
  reconstruct the live public root after reload.
- `SettlementStore::settlement_root(...)` and historical root lookup return the
  generation-1 public root when the live state has moved to root-of-shard-roots.
- one-shard no-op migration from generation 0 to generation 1 is covered by a
  semantic continuity test, not only by codec tags.
- historical proofs remain valid under their own root generation and route
  metadata without reinterpretation by current config.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `cargo test -p z00z_storage --release --features test-params-fast --test test_hjmt_root_generation -- --nocapture`.
3. Run `cargo test -p z00z_storage --release --features test-params-fast --test test_hjmt_historical_proofs -- --nocapture`.
4. Run `cargo test -p z00z_storage --release --features test-params-fast test_live_recovery -- --nocapture`.
5. Run `cargo test --release`.
6. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
7. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
Generation-1 root-of-shard-roots becomes real live storage truth instead of a
detached proof-only contract.
</done>
</task>

<task type="auto">
<name>Bind the durable journal and recovery export to shard route identity instead of one bucket-centric store lineage</name>
<read_first>
- `.planning/phases/060-Gaps-Closing/HJMT-RAID -Sharding.md`
- `docs/tech-papers/done/Z00Z-HJMT-Threat-Model.md`
- `docs/tech-papers/done/Z00Z-HJMT-Upgrade.md`
- `crates/z00z_storage/src/settlement/hjmt_journal.rs`
- `crates/z00z_storage/src/settlement/hjmt_commit.rs`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_storage/src/backend/redb/hjmt.rs`
- `crates/z00z_runtime/aggregators/src/recovery.rs`
- `crates/z00z_rollup_node/src/config.rs`
- `crates/z00z_storage/src/settlement/test_live_recovery.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_failover_same_lineage.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_split_brain_fencing.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_preflight.rs`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/hjmt_journal.rs`
- `crates/z00z_storage/src/settlement/hjmt_commit.rs`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_storage/src/backend/redb/hjmt.rs`
- `crates/z00z_runtime/aggregators/src/recovery.rs`
- `crates/z00z_rollup_node/src/config.rs`
- `crates/z00z_storage/src/settlement/test_live_recovery.rs`
- `crates/z00z_storage/tests/test_hjmt_import_export.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_failover_same_lineage.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_split_brain_fencing.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_preflight.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_topology.rs`
</files>
<action>
Turn the current route-bound handoff into a route-bound durable journal.
`SettlementRouteCtx` already carries `batch_id`, `shard_id`,
`routing_generation`, and `route_table_digest`, but the persisted
`HjmtCommitJournalEntry` and exported `SettlementRecoveryState` still collapse
that into one store-version lineage. Close that gap exactly.

Extend the durable journal and the recovery export so storage can prove which
shard-domain produced a committed state. At minimum the durable path must carry
the shard route identity from the admitted batch and the publication-relevant
journal progression that later failover and replay checks depend on. If a new
versioned recovery type is required, keep backward import and historical test
coverage explicit. If the existing recovery type can be extended compatibly,
prefer that path. Do not leave `hjmt_apply_exec_handoff(...)` as a route-aware
API that only emits JSON evidence while persisting no route identity.

RedB recovery, runtime failover resume, and rollup-node startup preflight must
all consume the storage-owned route-bound recovery facts. Wrong shard,
wrong routing generation, wrong route digest, wrong lineage, and stale replay
must reject before a new publication object can be accepted.
</action>
<acceptance_criteria>
- every route-bound execution handoff that mutates live settlement state writes
  shard route identity into the durable journal or a versioned journal companion
  record owned by storage.
- exported recovery state can roundtrip route identity and lineage facts through
  the existing import or export coverage without ambiguity.
- storage-side replay rejects wrong shard, wrong generation, wrong route digest,
  wrong lineage, and stale route-bound resume attempts before publication.
- runtime recovery and startup preflight stop depending on route identity that
  exists only outside storage-owned durable state.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `cargo test -p z00z_storage --release --features test-params-fast test_live_recovery -- --nocapture`.
3. Run `cargo test -p z00z_storage --release --features test-params-fast --test test_hjmt_import_export -- --nocapture`.
4. Run `cargo test -p z00z_aggregators --release --features test-params-fast --test test_hjmt_failover_same_lineage -- --nocapture`.
5. Run `cargo test -p z00z_aggregators --release --features test-params-fast --test test_hjmt_split_brain_fencing -- --nocapture`.
6. Run `cargo test -p z00z_aggregators --release --features test-params-fast --test test_hjmt_migrate -- --nocapture`.
7. Run `cargo test -p z00z_rollup_node --release --features test-params-fast --test test_hjmt_preflight -- --nocapture`.
8. Run `cargo test -p z00z_rollup_node --release --features test-params-fast --test test_hjmt_topology -- --nocapture`.
9. Run `cargo test --release`.
10. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
11. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The durable journal and the recovery boundary become truly shard-aware and
route-bound instead of only route-decorated at the runtime handoff edge.
</done>
</task>

<task type="auto">
<name>Move exact publication coverage against the committed route table into one shared storage-owned acceptance path</name>
<read_first>
- `.planning/phases/060-Gaps-Closing/HJMT-RAID -Sharding.md`
- `docs/tech-papers/done/Z00Z-HJMT-Upgrade.md`
- `docs/tech-papers/done/Z00Z-HJMT-Gaps.md`
- `crates/z00z_runtime/aggregators/src/batch_planner.rs`
- `crates/z00z_storage/src/settlement/hjmt_batch_proof.rs`
- `crates/z00z_storage/src/settlement/proof_batch.rs`
- `crates/z00z_storage/src/settlement/proof_batch_verify.rs`
- `crates/z00z_runtime/aggregators/src/types.rs`
- `crates/z00z_runtime/validators/src/checkpoint.rs`
- `crates/z00z_rollup_node/src/config.rs`
- `crates/z00z_storage/tests/test_hjmt_root_generation.rs`
- `crates/z00z_storage/tests/test_hjmt_historical_proofs.rs`
- `crates/z00z_storage/tests/test_hjmt_batch_proof.rs`
- `crates/z00z_storage/tests/test_hjmt_batch_proof_negative.rs`
- `crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs`
- `crates/z00z_runtime/watchers/tests/test_hjmt_publication_contract.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_preflight.rs`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/hjmt_batch_proof.rs`
- `crates/z00z_storage/src/settlement/proof_batch.rs`
- `crates/z00z_storage/src/settlement/proof_batch_verify.rs`
- `crates/z00z_runtime/aggregators/src/types.rs`
- `crates/z00z_runtime/validators/src/checkpoint.rs`
- `crates/z00z_rollup_node/src/config.rs`
- `crates/z00z_storage/tests/test_hjmt_root_generation.rs`
- `crates/z00z_storage/tests/test_hjmt_historical_proofs.rs`
- `crates/z00z_storage/tests/test_hjmt_batch_proof.rs`
- `crates/z00z_storage/tests/test_hjmt_batch_proof_negative.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_shard_routing.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_publish.rs`
- `crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs`
- `crates/z00z_runtime/watchers/tests/test_hjmt_publication_contract.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_preflight.rs`
- `docs/tech-papers/done/Z00Z-HJMT-Gaps.md`
</files>
<action>
Promote exact shard-set coverage from a preflight-only rule into one shared
storage-owned publication acceptance contract. Today the repository has strong
`CheckpointPublicationV1` ordering and monotonicity checks, but exact
`every active shard exactly once` coverage still lives mostly in rollup-node
preflight and runtime-level `PublicationBinding` surfaces. Close that split.

Introduce one canonical acceptance helper that verifies a publication object or
public proof against the committed route table snapshot. The shared checker must
cover exact active shard membership, strict once-only coverage, carry-forward
byte equality for unchanged shards, activation-checkpoint legality, route
migration monotonicity, and foreign or missing shard rejection. Do not fork a
second route-table codec. Reuse or extract the current canonical route-table
byte contract so planner, storage, validator, watcher, and preflight all bind
the same bytes and the same digest.

Once this shared checker exists, make validator flow, watcher flow, publication
handoff checks, and preflight consume it instead of each keeping a narrower or
parallel shard-coverage story. If `BatchProofBlobV1` path metadata remains
shard-blind for ordinary non-publication proofs, keep that explicit. But the
root-of-shard-roots public proof and publication-acceptance path must stop
depending on a separate runtime-only binding digest when exact shard-set truth
is the real safety boundary.
</action>
<acceptance_criteria>
- a publication with missing, extra, duplicated, foreign, or wrongly carried
  shard leaves rejects against the committed route table snapshot even when the
  local node topology is not used as the only authority.
- `activation_checkpoint > publication_checkpoint`, route-generation drift, and
  route-digest drift reject in the same shared acceptance path instead of being
  split between preflight-only and runtime-only guards.
- validator and watcher publication paths reuse the shared publication
  acceptance contract instead of validating only `PublicationBinding` plus
  `CheckpointPubIn`.
- rollup-node preflight remains a startup and config gate, but it is no longer
  the only place that knows exact shard-set coverage rules.
- HJMT docs and closeout wording stop claiming full core-storage closure unless
  the shared route-table-driven acceptance path is really live.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `cargo test -p z00z_storage --release --features test-params-fast --test test_hjmt_root_generation -- --nocapture`.
3. Run `cargo test -p z00z_storage --release --features test-params-fast --test test_hjmt_historical_proofs -- --nocapture`.
4. Run `cargo test -p z00z_storage --release --features test-params-fast --test test_hjmt_batch_proof -- --nocapture`.
5. Run `cargo test -p z00z_storage --release --features test-params-fast --test test_hjmt_batch_proof_negative -- --nocapture`.
6. Run `cargo test -p z00z_aggregators --release --features test-params-fast --test test_hjmt_shard_routing -- --nocapture`.
7. Run `cargo test -p z00z_aggregators --release --features test-params-fast --test test_hjmt_publish -- --nocapture`.
8. Run `cargo test -p z00z_rollup_node --release --features test-params-fast --test test_hjmt_preflight -- --nocapture`.
9. Run `cargo test -p z00z_validators --release --test test_hjmt_publication_contract -- --nocapture`.
10. Run `cargo test -p z00z_watchers --release --test test_hjmt_publication_contract -- --nocapture`.
11. Run `cargo test -p z00z_simulator --release --features test-params-fast --test scenario_1 test_scenario_settlement -- --nocapture`.
12. Run `cargo test --release`.
13. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
14. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
Exact shard-set publication coverage becomes a real shared acceptance contract
against the committed route table rather than a narrow runtime binding story.
</done>
</task>
</tasks>

<threat_model>
- A proof-only `ShardRootLeafV1` and `CheckpointPublicationV1` surface can make
  the repository look closed while the live public-root reload path still uses a
  different authority model.
- A bucket-centric journal lineage without durable shard route identity can
  accept lawful-looking but foreign replay across shards or generations.
- Exact shard-set coverage enforced only in node preflight creates split
  authority between config-time checks and proof-time acceptance.
- Reusing `PublicationBinding` as the primary publication truth after
  root-of-shard-roots activation would keep runtime glue above the actual
  storage safety boundary.
</threat_model>

<verification>
This slice is valid only if the repository can prove all three claims on the
live tree:

1. storage persists and reloads the generation-1 root-of-shard-roots state;
2. durable journal and recovery export are route-bound per shard;
3. publication acceptance can validate exact shard-set coverage against the
   committed route table through one shared contract path.
</verification>

<success_criteria>
Phase 060 gains one honest integration-closeout slice that turns the existing
HJMT contract packet from mostly codec/runtime/preflight closure into live
storage-root, journal, and publication truth.
</success_criteria>

<output>
Create `.planning/phases/060-Gaps-Closing/060-12-SUMMARY.md` when done.
</output>
