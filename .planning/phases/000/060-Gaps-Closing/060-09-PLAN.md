---
phase: 060-Gaps-Closing
plan: 060-09
type: execute
wave: 3
depends_on:
  - 060-06
files_modified:
  - .github/skills/z00z-verification-orchestrator/scripts/run-security-brainstorm.py
  - crates/z00z_storage/src/checkpoint/audit.rs
  - crates/z00z_storage/src/checkpoint/codec.rs
  - crates/z00z_storage/src/settlement/hjmt_scheduler.rs
  - crates/z00z_storage/src/settlement/timing.rs
  - crates/z00z_storage/tests/test_checkpoint_root_binding.rs
  - crates/z00z_storage/tests/test_async_scheduler.rs
  - crates/z00z_wallets/src/receiver/request/mod.rs
  - crates/z00z_wallets/tests/test_stealth_request.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_flow.rs
requirements:
  - None
autonomous: true
must_haves:
  truths:
    - "The `11` high findings are hypothesis-backed attack surfaces, not auto-proven exploits, and every project-owned high needs a closure artifact."
    - "The first three closures stay the same: checkpoint lineage and delta integrity, PaymentRequest replay or compact-request rebinding, and stealth delivery or inbox notification confusion."
    - "Protected-vendor highs may only end as wrapper, isolation, upstream, or documented exception work; no direct vendor edits are allowed."
    - "The remaining four project-owned highs - crate-level concentration in `crates/z00z_storage`, module-level concentration in `crates/z00z_storage/src/settlement`, and the nondeterministic-source hypotheses in `hjmt_scheduler.rs` and `timing.rs` - must also end in explicit closure artifacts."
    - "The baseline evidence includes `reports/z00z-verification-orchestrator-20260618-170025/logs/l4-adversarial-review.log` with `findings: 392 total, high=11`, plus the current JSON-versus-markdown count mismatch."
  artifacts:
    - .planning/phases/060-Gaps-Closing/060-TODO.md
    - .planning/phases/060-Gaps-Closing/060-CONTEXT.md
    - .planning/phases/060-Gaps-Closing/060-TZ2.md
    - .planning/phases/060-Gaps-Closing/060-z00z-verification-report.md
    - reports/z00z-verification-orchestrator-20260618-170025/logs/l4-adversarial-review.log
    - reports/z00z-verification-orchestrator-20260618-170025/security/adversarial-summary.json
    - reports/z00z-verification-orchestrator-20260618-170025/security/adversarial-review.md
  key_links:
    - "This slice owns `C3`, depends on the supply-chain review baseline from `060-06`, and must feed honest final reruns in `060-11`."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 060-09 Plan: Adversarial High-Finding Closure And Count Reconciliation

<objective>
Close `C3` by converting every current high adversarial finding into a concrete
closure artifact, starting with the three highest-priority project-owned
surfaces, while reconciling the report count across JSON and markdown outputs.
</objective>

<context>
@.planning/phases/060-Gaps-Closing/060-TODO.md
@.planning/phases/060-Gaps-Closing/060-CONTEXT.md
@.planning/phases/060-Gaps-Closing/060-TZ2.md
@.planning/phases/060-Gaps-Closing/060-TEST-SPEC.md
@.planning/phases/060-Gaps-Closing/060-z00z-verification-report.md
@reports/z00z-verification-orchestrator-20260618-170025/logs/l4-adversarial-review.log
@reports/z00z-verification-orchestrator-20260618-170025/security/adversarial-summary.json
@reports/z00z-verification-orchestrator-20260618-170025/security/adversarial-review.md
@crates/z00z_storage/src/checkpoint/audit.rs
@crates/z00z_storage/src/settlement/hjmt_scheduler.rs
@crates/z00z_wallets/tests/test_stealth_request.rs
</context>

<coverage_contract>
This plan owns `C3` only. It must close the current adversarial high-finding
set with tests, proofs, or short closure memos, and it must reconcile the
`high_risk_count` summary with the rendered report shape.
</coverage_contract>

<tasks>
<task type="auto">
<name>Turn every current high adversarial finding into a closure artifact and reconcile the high-count outputs</name>
<read_first>
- `.planning/phases/060-Gaps-Closing/060-TODO.md`
- `.planning/phases/060-Gaps-Closing/060-CONTEXT.md`
- `.planning/phases/060-Gaps-Closing/060-TZ2.md`
- `.planning/phases/060-Gaps-Closing/060-z00z-verification-report.md`
- `reports/z00z-verification-orchestrator-20260618-170025/logs/l4-adversarial-review.log`
- `reports/z00z-verification-orchestrator-20260618-170025/security/adversarial-summary.json`
- `reports/z00z-verification-orchestrator-20260618-170025/security/adversarial-review.md`
- `.github/skills/z00z-verification-orchestrator/scripts/run-security-brainstorm.py`
- `crates/z00z_storage/src/checkpoint/audit.rs`
- `crates/z00z_storage/src/checkpoint/codec.rs`
- `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
- `crates/z00z_storage/src/settlement/timing.rs`
- `crates/z00z_storage/tests/test_checkpoint_root_binding.rs`
- `crates/z00z_storage/tests/test_async_scheduler.rs`
- `crates/z00z_wallets/src/receiver/request/mod.rs`
- `crates/z00z_wallets/tests/test_stealth_request.rs`
- `crates/z00z_wallets/tests/test_stealth_scanner_flow.rs`
</read_first>
<files>
- `.github/skills/z00z-verification-orchestrator/scripts/run-security-brainstorm.py`
- `crates/z00z_storage/src/checkpoint/audit.rs`
- `crates/z00z_storage/src/checkpoint/codec.rs`
- `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
- `crates/z00z_storage/src/settlement/timing.rs`
- `crates/z00z_storage/tests/test_checkpoint_root_binding.rs`
- `crates/z00z_storage/tests/test_async_scheduler.rs`
- `crates/z00z_wallets/src/receiver/request/mod.rs`
- `crates/z00z_wallets/tests/test_stealth_request.rs`
- `crates/z00z_wallets/tests/test_stealth_scanner_flow.rs`
</files>
<action>
Implement `C3` exactly. Normalize the `11` high findings into the `7`
project-owned and `4` protected-vendor buckets already defined in
`060-TODO.md`. Close the first three project-owned highs first:
checkpoint-lineage and delta integrity, PaymentRequest replay or
compact-request rebinding, and stealth-delivery or inbox-notification
confusion. Then explicitly account for the remaining four project-owned highs:
crate-level concentration in `crates/z00z_storage`, module-level concentration
in `crates/z00z_storage/src/settlement`, and the nondeterministic-source
hypotheses in `hjmt_scheduler.rs` and `timing.rs`. For every project-owned
high, produce exactly one closure artifact: a reproducing failing test plus
fix, a negative or property harness that disproves the bypass, or a short
closure memo tied to exact code and test anchors. Every closure artifact must
record finding id, owner crate or module, threat statement, reproduction or
proof strategy, linked tests or proof artifacts, and final status
(`fixed`, `disproved`, or `accepted-risk`). For the concentration findings,
either land concrete hardening or produce artifact-backed closure memos that
justify why the concentration does not remain a live high-risk condition. For
the scheduler or timing nondeterminism findings, prove whether the suspected
nondeterminism is logging-only or verification-relevant. Then rerun the
adversarial review and reconcile `high_risk_count`, JSON `top_findings`, and
markdown high rows so there is no hidden unowned high finding. Use the current
`l4-adversarial-review.log` baseline of `392` total findings and `11` highs to
prove that closure work reduced ambiguity rather than just reformatted the
report. Preserve the exact protected-vendor bucket from `060-TODO.md` during
normalization: crate-level concentration in `crates/z00z_crypto/tari/crypto`,
module-level concentration in `.../ristretto`, proof-adjacent logging in
`ristretto_keys.rs`, and unsafe-block review in `ristretto_keys.rs`. Do not
edit protected vendor code.
</action>
<acceptance_criteria>
- every high finding has an owner and one closure artifact.
- the first three prioritized project-owned highs are artifact-backed first.
- all `7` project-owned highs are accounted for with finding id, owner, threat
  statement, reproduction or proof strategy, linked evidence, and final
  status.
- protected-vendor highs are tracked without source edits under vendor roots.
- report count outputs are reconciled or any generator mismatch is documented
  as a report bug with no hidden unowned high.
- current highs end as fixed, disproved, or accepted-risk with evidence.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `cargo test -p z00z_storage --release --test test_checkpoint_root_binding -- --nocapture`, `cargo test -p z00z_storage --release --test test_async_scheduler -- --nocapture`, `cargo test -p z00z_wallets --release --test test_stealth_request -- --nocapture`, and `cargo test -p z00z_wallets --release --test test_stealth_scanner_flow -- --nocapture`.
3. Run `python3 ./.github/skills/z00z-verification-orchestrator/scripts/run-security-brainstorm.py --root . --scope-kind project --target-root-rel \"\" --verification-root reports/z00z-verification-orchestrator-<stamp>/verification<stamp> --summary-out reports/z00z-verification-orchestrator-<stamp>/security/adversarial-summary.json --report-out reports/z00z-verification-orchestrator-<stamp>/security/adversarial-review.md`.
4. Run a report-consistency check that proves adversarial `high_risk_count`, JSON `top_findings`, and rendered markdown high-finding rows agree, or records any mismatch as a generator bug with no hidden unowned high finding.
5. Run `cargo test --release`.
6. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
7. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The adversarial high set is no longer a prose-only risk register; it becomes a
closed, counted, and artifact-backed security packet.
</done>
</task>
</tasks>

<threat_model>
- Leaving highs as report prose would let future planning confuse suspicion with
  closure.
- Fixing count mismatches by editing reports without proving ownership would
  only hide the unowned-high problem behind cleaner formatting.
</threat_model>

<verification>
This slice is valid only if the high-finding set ends in concrete evidence and
the rerun reports stay internally consistent.
</verification>

<success_criteria>
Phase 060 can speak about adversarial closure in terms of artifacts and tests,
not only risk wording.
</success_criteria>

<output>
Create `.planning/phases/060-Gaps-Closing/060-09-SUMMARY.md` when done.
</output>
