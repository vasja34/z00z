---
phase: 060-Gaps-Closing
plan: 060-02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_core/README.md
  - crates/z00z_core/src/genesis/README.md
  - crates/z00z_core/src/assets/mod.rs
  - crates/z00z_storage/src/settlement/README.md
requirements:
  - None
autonomous: true
must_haves:
  truths:
    - "`z00z_core::genesis` remains the only canonical bootstrap authority for assets, rights, policies, and vouchers."
    - "`GenesisConfig` remains the canonical typed manifest and `assets_config.yaml` cannot remain an implied bootstrap authority."
    - "This slice freezes wording only; it must not invent new config homes or new semantic owners."
    - "Authority wording must explicitly demote the `assets_config.yaml` versus `genesis_config*.yaml` split into one canonical genesis story plus non-authority registry or fixture YAML."
  artifacts:
    - .planning/phases/060-Gaps-Closing/060-TODO.md
    - .planning/phases/060-Gaps-Closing/060-CONTEXT.md
    - .planning/phases/060-Gaps-Closing/060-TZ1.md
    - crates/z00z_core/README.md
    - crates/z00z_core/src/genesis/README.md
    - crates/z00z_core/src/assets/assets_config.yaml
  key_links:
    - "This slice owns `A1` and unblocks the owner-path cleanup in `060-04`."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 060-02 Plan: Canonical Bootstrap Authority Freeze

<objective>
Close `A1` by making the repository say one consistent thing about bootstrap
authority: `z00z_core::genesis` is canonical and every other YAML surface is
registry, fixture, example, or compatibility data.
</objective>

<context>
@.planning/phases/060-Gaps-Closing/060-TODO.md
@.planning/phases/060-Gaps-Closing/060-CONTEXT.md
@.planning/phases/060-Gaps-Closing/060-TZ1.md
@.planning/phases/060-Gaps-Closing/060-TEST-SPEC.md
@crates/z00z_core/README.md
@crates/z00z_core/src/genesis/README.md
@crates/z00z_core/src/assets/mod.rs
@crates/z00z_core/src/assets/assets_config.yaml
@crates/z00z_storage/src/settlement/README.md
</context>

<coverage_contract>
This plan owns `A1` only. It must freeze one canonical bootstrap contract and
remove conflicting wording that still implies `assets_config.yaml` is a second
authority story.
</coverage_contract>

<tasks>
<task type="auto">
<name>Freeze the one-authority bootstrap contract across core and settlement docs</name>
<read_first>
- `.planning/phases/060-Gaps-Closing/060-TODO.md`
- `.planning/phases/060-Gaps-Closing/060-CONTEXT.md`
- `.planning/phases/060-Gaps-Closing/060-TZ1.md`
- `crates/z00z_core/README.md`
- `crates/z00z_core/src/genesis/README.md`
- `crates/z00z_core/src/assets/mod.rs`
- `crates/z00z_core/src/assets/assets_config.yaml`
- `crates/z00z_storage/src/settlement/README.md`
</read_first>
<files>
- `crates/z00z_core/README.md`
- `crates/z00z_core/src/genesis/README.md`
- `crates/z00z_core/src/assets/mod.rs`
- `crates/z00z_storage/src/settlement/README.md`
</files>
<action>
Apply `A1` exactly. Make `z00z_core::genesis` and `GenesisConfig` the only
canonical bootstrap story across repository-facing docs and module comments.
Reclassify `src/assets/assets_config.yaml` as registry, fixture, example, or
compatibility data wherever it is currently presented as bootstrap truth. Do
not reopen the old `assets_config.yaml` versus `genesis_config*.yaml`
authority split, and do not add new config files, new authorities, or
symmetry-only owner surfaces.
</action>
<acceptance_criteria>
- `z00z_core::genesis` is explicitly described as the single bootstrap
  authority.
- `GenesisConfig` is described as the canonical typed manifest.
- `assets_config.yaml` is no longer documented as an equal bootstrap source of
  truth.
- no new config or authority surface is introduced.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `rg -n "single bootstrap authority|GenesisConfig|assets_config.yaml" crates/z00z_core/README.md crates/z00z_core/src/genesis/README.md crates/z00z_core/src/assets/mod.rs crates/z00z_storage/src/settlement/README.md`.
3. Run `cargo test --release` if this slice changes Rust or test files; for docs-only edits, record that the broader Rust gate was not required for this slice.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
5. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The repository tells one bootstrap story and `060-04` can migrate owner paths
without arguing about authority semantics first.
</done>
</task>
</tasks>

<threat_model>
- Contradictory bootstrap docs would let later code changes keep two authority
  stories alive.
- Symmetry-driven config additions would widen the authority surface instead of
  shrinking it.
</threat_model>

<verification>
This slice is valid only if the authority wording converges on one story
without creating a new planning or config layer.
</verification>

<success_criteria>
Phase 060 has one frozen bootstrap authority statement before owner-path and
fixture cleanup begins.
</success_criteria>

<output>
Create `.planning/phases/060-Gaps-Closing/060-02-SUMMARY.md` when done.
</output>
