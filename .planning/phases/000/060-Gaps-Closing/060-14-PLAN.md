---
phase: 060-Gaps-Closing
plan: 060-14
type: execute
wave: 5
depends_on:
  - 060-07
  - 060-08
  - 060-12
  - 060-13
files_modified:
  - crates/z00z_storage/src/settlement/tx_plan_types.rs
  - crates/z00z_storage/src/settlement/test_model.rs
  - crates/z00z_storage/tests/test_store_api.rs
  - crates/z00z_wallets/src/adapters/rpc/types/object.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/object_impl.rs
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_support.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/owned_objects.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs
  - crates/z00z_runtime/validators/src/checkpoint.rs
  - crates/z00z_runtime/validators/src/verdict.rs
  - crates/z00z_runtime/validators/src/engine.rs
  - crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs
  - crates/z00z_runtime/watchers/src/alerts.rs
  - crates/z00z_runtime/watchers/src/engine.rs
  - crates/z00z_runtime/watchers/src/publication.rs
  - crates/z00z_runtime/watchers/tests/test_object_alerts.rs
  - crates/z00z_runtime/watchers/tests/test_hjmt_publication_contract.rs
requirements:
  - None
autonomous: true
must_haves:
  truths:
    - "This slice is a supplemental Phase 059 semantic and runtime closeout only: refund or reject binding, one-plane object issue or create construction, and real validator or watcher incomplete surfaces."
    - "Do not create a second wallet object builder, a new `wallet.object.issue_voucher` or `wallet.object.create_right` namespace, or a parallel publication completeness checker. `wallet.object.preview_package` and `wallet.object.build_package` remain the only package-construction RPCs, and the existing validator or watcher publication path remains the only completeness authority."
    - "Refund and reject outputs must bind to the declared `refund_target_commitment`, and restricted backing must also bind to the declared source context instead of allowing covert restriction stripping into unrelated clean outputs."
    - "Voucher issuance and right creation may widen the existing object-package request shape, but they must not widen `wallet.asset.*` beyond cash-only semantics or force rights into value-bearing accounting."
    - "Rights remain zero-value authority objects. Assets and vouchers require value or source-context checks; rights do not gain asset-balance symmetry just to mirror voucher tests."
    - "`VerdictKind::Incomplete` is reserved for bounded absence or lag states such as retry-pending, missing publication artifact, or publication-gap conditions. Invalid shapes, route drift, binding drift, and malformed object packages remain `Rejected`."
    - "`AlertKind::ValidatorIncomplete` must be emitted through the existing watcher publication contract and evidence path rather than by inventing a second alert mapper or a second binding digest."
    - "`060-14` is the broad reopen packet only. If `060-15` is present in the same Phase 060 packet, do not execute the overlapping MVP subset twice; `060-15` supersedes that exact subset and `060-14` remains broader review context."
    - "If this slice lands after `060-11` was first prepared, rerun the targeted wallet/object, validator, watcher, and simulator closure evidence plus any final closure memo on the post-`060-14` tree before treating the earlier Wave 4 attestation as still authoritative."
  artifacts:
    - .planning/phases/000/059-Core-Upgrade/059-TODO.md
    - .planning/phases/060-Gaps-Closing/060-TODO.md
    - .planning/phases/060-Gaps-Closing/060-CONTEXT.md
    - .planning/phases/060-Gaps-Closing/060-15-PLAN.md
    - docs/tech-papers/done/Z00Z-HJMT-Design.md
    - docs/tech-papers/done/Z00Z-HJMT-Gaps.md
    - docs/tech-papers/done/Z00Z-HJMT-Threat-Model.md
    - docs/tech-papers/done/Z00Z-HJMT-Upgrade.md
    - crates/z00z_storage/src/settlement/record.rs
    - crates/z00z_storage/src/settlement/tx_plan_types.rs
    - crates/z00z_storage/tests/test_store_api.rs
    - crates/z00z_wallets/src/adapters/rpc/types/object.rs
    - crates/z00z_wallets/src/adapters/rpc/methods/object.rs
    - crates/z00z_wallets/src/adapters/rpc/methods/object_impl.rs
    - crates/z00z_wallets/src/wallet/WALLET-GUIDE.md
    - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_support.rs
    - crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs
    - crates/z00z_wallets/src/db/redb_wallet_store/owned_objects.rs
    - crates/z00z_runtime/validators/src/verdict.rs
    - crates/z00z_runtime/validators/src/checkpoint.rs
    - crates/z00z_runtime/validators/src/engine.rs
    - crates/z00z_runtime/watchers/src/alerts.rs
    - crates/z00z_runtime/watchers/src/engine.rs
    - crates/z00z_runtime/watchers/src/publication.rs
    - crates/z00z_runtime/watchers/tests/test_object_alerts.rs
  key_links:
    - "This slice closes late-discovered Phase 059 object-model and publication-runtime seams under the existing `D2`, `D4`, and `D5` wallet contract plus the existing HJMT publication contract. It does not mint a new canonical `A*`, `B*`, `C*`, or `D*` task id."
    - "`060-13` already froze the split-fee and value-bearing object boundaries. `060-14` must keep those boundaries intact and only close the remaining refund/source, issue/create, and incomplete-surface gaps."
    - "`060-12` remains the storage and route-table reopen. `060-14` only finishes the runtime completeness classification on the same publication binding path without creating a second acceptance plane."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 060-14 Plan: Refund Binding, One-Plane Object Issue/Create, And Incomplete Verdict Coverage

<objective>
Close the remaining Phase 059 semantic and runtime gaps by making voucher
reject or refund outputs bind to the declared refund and source context,
extending the existing object-package RPC to build voucher-issue and right-
create packages from source or create context without a parallel API, and
turning `VerdictKind::Incomplete` plus `AlertKind::ValidatorIncomplete` into
live tested runtime behavior on the current publication contract.
</objective>

<context>
@.planning/phases/000/059-Core-Upgrade/059-TODO.md
@.planning/phases/060-Gaps-Closing/060-TODO.md
@.planning/phases/060-Gaps-Closing/060-CONTEXT.md
@.planning/phases/060-Gaps-Closing/060-15-PLAN.md
@docs/tech-papers/done/Z00Z-HJMT-Design.md
@docs/tech-papers/done/Z00Z-HJMT-Gaps.md
@docs/tech-papers/done/Z00Z-HJMT-Threat-Model.md
@docs/tech-papers/done/Z00Z-HJMT-Upgrade.md
@crates/z00z_storage/src/settlement/record.rs
@crates/z00z_storage/src/settlement/tx_plan_types.rs
@crates/z00z_storage/src/backend/query.rs
@crates/z00z_storage/src/settlement/test_model.rs
@crates/z00z_storage/tests/test_store_api.rs
@crates/z00z_wallets/src/adapters/rpc/types/object.rs
@crates/z00z_wallets/src/adapters/rpc/methods/object.rs
@crates/z00z_wallets/src/adapters/rpc/methods/object_impl.rs
@crates/z00z_wallets/src/wallet/WALLET-GUIDE.md
@crates/z00z_wallets/src/services/wallet/store/wallet_service_store_support.rs
@crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs
@crates/z00z_wallets/src/db/redb_wallet_store/owned_objects.rs
@crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs
@crates/z00z_runtime/aggregators/src/types.rs
@crates/z00z_runtime/validators/src/verdict.rs
@crates/z00z_runtime/validators/src/engine.rs
@crates/z00z_runtime/validators/src/checkpoint.rs
@crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs
@crates/z00z_runtime/watchers/src/alerts.rs
@crates/z00z_runtime/watchers/src/engine.rs
@crates/z00z_runtime/watchers/src/publication.rs
@crates/z00z_runtime/watchers/tests/test_object_alerts.rs
@crates/z00z_runtime/watchers/tests/test_hjmt_publication_contract.rs
</context>

<coverage_contract>
This plan closes exactly three supplemental seams:

1. voucher reject or refund outputs must bind to the declared
   `refund_target_commitment`, and restricted backing must also bind to the
   declared source context;
2. `wallet.object.preview_package` and `wallet.object.build_package` must grow
   into the canonical package-construction path for `VoucherAction::Issue` and
   `RightAction::Create` without a parallel RPC surface;
3. validator and watcher runtime surfaces must use real
   `VerdictKind::Incomplete` and `AlertKind::ValidatorIncomplete` semantics for
   unresolved publication states such as retry-pending, missing-artifact, and
   publication-gap cases.

It must not add a new canonical Phase 060 task id, a second wallet authority
plane, a second publication truth path, or a second reject mapper.
</coverage_contract>

<tasks>
<task type="auto">
<name>Bind voucher reject and refund outputs to the declared refund target and restricted source context</name>
<read_first>
- `.planning/phases/000/059-Core-Upgrade/059-TODO.md`
- `.planning/phases/060-Gaps-Closing/060-TODO.md`
- `crates/z00z_storage/src/settlement/record.rs`
- `crates/z00z_storage/src/settlement/tx_plan_types.rs`
- `crates/z00z_storage/src/settlement/test_model.rs`
- `crates/z00z_storage/tests/test_store_api.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/tx_plan_types.rs`
- `crates/z00z_storage/src/settlement/test_model.rs`
- `crates/z00z_storage/tests/test_store_api.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs`
</files>
<action>
Keep `VoucherLeaf::refund_target_commitment` and `VoucherBackingRef` as the one
source of truth for reject or refund routing.

Close the live gap in `ObjectDeltaSetV1` and the store API so that:

- any reject or refund that creates clean asset output must prove that the
  created output is bound to the declared `refund_target_commitment`;
- when the voucher backing is source-restricted, the created refund output must
  also stay bound to the declared consumed-asset or reserve context instead of
  escaping into unrelated clean cash;
- positive paths still succeed through the same delta and store seams;
- negative tests cover both wrong refund target and wrong source-context
  attempts.

Do not add a side validator or a shadow refund planner. Rights remain outside
this value-bearing check because they do not carry assets.
</action>
<acceptance_criteria>
- `ObjectDeltaSetV1` rejects refund or reject outputs that miss the declared
  `refund_target_commitment`.
- `ObjectDeltaSetV1` rejects refund or reject outputs that violate the declared
  restricted source context.
- store API coverage proves positive reject or refund paths still succeed and
  negative target or source-context paths fail closed.
- wallet object-package coverage proves wrong refund target mismatch rejects,
  and it must also reject wrong restricted source-context routing when that
  source context is carried through the existing typed object package path.
- rights do not gain value-bearing refund checks.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `cargo test -p z00z_storage --release --lib test_delta_rejects_refund_target -- --nocapture`.
3. Run `cargo test -p z00z_storage --release --lib test_delta_rejects_refund_ctx -- --nocapture`.
4. Run `cargo test -p z00z_storage --release --lib test_delta_rejects_reserve_ctx -- --nocapture`.
5. Run `cargo test -p z00z_storage --release --test test_store_api test_store_api_voucher_reject_expire_refund -- --nocapture`.
6. Run `cargo test -p z00z_storage --release --test test_store_api test_store_api_voucher_refund_rejects_wrong_target_context -- --nocapture`.
7. Run `cargo test -p z00z_storage --release --test test_store_api test_store_api_voucher_refund_rejects_wrong_source_context -- --nocapture`.
8. Run `cargo test -p z00z_storage --release --test test_store_api test_store_refund_reserve_bad -- --nocapture`.
9. Run `cargo test -p z00z_wallets --release test_build_rejects_refund_target -- --nocapture`.
10. Run `cargo test -p z00z_wallets --release test_build_rejects_refund_ctx -- --nocapture`.
11. Run `cargo test -p z00z_wallets --release test_build_rejects_reserve_ctx -- --nocapture`.
12. Run `cargo test --release`.
13. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
14. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
Refund and reject flows now fail closed against both the declared refund target
and the declared restricted backing context instead of allowing covert
restriction stripping.
</done>
</task>

<task type="auto">
<name>Extend the existing wallet object package RPC to build voucher issue and right create packages from source or create context</name>
<read_first>
- `.planning/phases/000/059-Core-Upgrade/059-TODO.md`
- `.planning/phases/060-Gaps-Closing/060-TODO.md`
- `.planning/phases/060-Gaps-Closing/060-15-PLAN.md`
- `crates/z00z_wallets/src/adapters/rpc/types/object.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/object.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/object_impl.rs`
- `crates/z00z_wallets/src/wallet/WALLET-GUIDE.md`
- `crates/z00z_wallets/src/services/wallet/store/wallet_service_store_support.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store/owned_objects.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs`
</read_first>
<files>
- `crates/z00z_wallets/src/adapters/rpc/types/object.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/object_impl.rs`
- `crates/z00z_wallets/src/services/wallet/store/wallet_service_store_support.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store/owned_objects.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/test_asset_impl.rs`
</files>
<action>
Evolve the existing `RuntimeObjectPackageRequest` into one canonical request
shape that can resolve either:

- an existing non-asset live object for voucher or right lifecycle actions; or
- a source asset or declared reserve or create context for
  `VoucherAction::Issue` and `RightAction::Create`.

The important boundary is architectural:

- no new `wallet.object.issue_voucher` or `wallet.object.create_right` RPC;
- no second builder beside `wallet.object.preview_package` and
  `wallet.object.build_package`;
- no widening of `wallet.asset.*` beyond the existing cash-only surface.

`VoucherAction::Issue` must accept source `Asset` or reserve context through
the same object-package path, and `RightAction::Create` should use the same
request family so callers do not need a fake non-asset target row just to build
create packages.

Keep rights zero-value. Source-context plumbing proves authorization and object
binding, not asset-style value accounting for rights.
</action>
<acceptance_criteria>
- `wallet.object.preview_package` and `wallet.object.build_package` can build a
  voucher issue package from source asset or reserve context without a live
  voucher `stable_id_hex`.
- the same request family can build a right create package without requiring a
  fake existing non-asset object row.
- wrong-family, missing-source, and stale-source paths reject fail closed on
  the canonical object-package surface.
- `wallet.asset.*` stays cash-only and continues rejecting voucher or right
  inventory ids.
- no parallel typed-object RPC namespace or second builder is introduced.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `cargo test -p z00z_wallets --release test_object_issue_asset -- --nocapture`.
3. Run `cargo test -p z00z_wallets --release test_object_issue_reserve -- --nocapture`.
4. Run `cargo test -p z00z_wallets --release test_object_issue_mix -- --nocapture`.
5. Run `cargo test -p z00z_wallets --release test_object_issue_missing -- --nocapture`.
6. Run `cargo test -p z00z_wallets --release test_object_issue_stale -- --nocapture`.
7. Run `cargo test -p z00z_wallets --release test_object_issue_reserve_bad -- --nocapture`.
8. Run `cargo test -p z00z_wallets --release test_object_create_right -- --nocapture`.
9. Run `cargo test -p z00z_wallets --release test_object_create_missing -- --nocapture`.
10. Run `cargo test -p z00z_wallets --release --lib test_tx_build_rejects_voucher -- --nocapture`.
11. Run `cargo test -p z00z_wallets --release --lib test_tx_send_rejects_right -- --nocapture`.
12. Run `cargo test --release`.
13. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
14. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
The existing object-package RPC remains the one canonical construction surface,
now widened just enough to cover voucher issue and right create from truthful
source or create context without a parallel API.
</done>
</task>

<task type="auto">
<name>Make validator incomplete and watcher validator-incomplete live on the current publication contract</name>
<read_first>
- `.planning/phases/000/059-Core-Upgrade/059-TODO.md`
- `.planning/phases/060-Gaps-Closing/060-TODO.md`
- `docs/tech-papers/done/Z00Z-HJMT-Design.md`
- `docs/tech-papers/done/Z00Z-HJMT-Gaps.md`
- `docs/tech-papers/done/Z00Z-HJMT-Threat-Model.md`
- `docs/tech-papers/done/Z00Z-HJMT-Upgrade.md`
- `crates/z00z_runtime/aggregators/src/types.rs`
- `crates/z00z_runtime/validators/src/verdict.rs`
- `crates/z00z_runtime/validators/src/engine.rs`
- `crates/z00z_runtime/validators/src/checkpoint.rs`
- `crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs`
- `crates/z00z_runtime/watchers/src/alerts.rs`
- `crates/z00z_runtime/watchers/src/engine.rs`
- `crates/z00z_runtime/watchers/src/publication.rs`
- `crates/z00z_runtime/watchers/tests/test_hjmt_publication_contract.rs`
</read_first>
<files>
- `crates/z00z_runtime/validators/src/verdict.rs`
- `crates/z00z_runtime/validators/src/checkpoint.rs`
- `crates/z00z_runtime/validators/src/engine.rs`
- `crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs`
- `crates/z00z_runtime/watchers/src/alerts.rs`
- `crates/z00z_runtime/watchers/src/engine.rs`
- `crates/z00z_runtime/watchers/src/publication.rs`
- `crates/z00z_runtime/watchers/tests/test_object_alerts.rs`
- `crates/z00z_runtime/watchers/tests/test_hjmt_publication_contract.rs`
</files>
<action>
Keep `CheckpointFlow` and `PublicationWatch` as the one publication-binding
truth path, but stop collapsing all non-accepted states into hard rejection.

Land real runtime completeness behavior:

- use `VerdictKind::Incomplete` for bounded absence or lag states such as
  retry-pending, missing-artifact, or unresolved publication-gap conditions;
- keep malformed artifacts, route drift, binding drift, and object-package
  failures on the existing `Rejected` path;
- project incomplete states to `AlertKind::ValidatorIncomplete` in watcher
  alerts and evidence so missing verdict, missing binding, pending retry, and
  publication-gap cases stop disappearing into generic snapshots;
- add targeted validator and watcher tests for missing-artifact,
  retry-pending, and publication-gap cases.

Do not create a second publication digest, a second watcher evidence flow, or a
shadow validator checker.
</action>
<acceptance_criteria>
- validator verdicts distinguish accepted, rejected, and incomplete states on
  the live publication path.
- retry-pending and other bounded unresolved states become `Incomplete`
  instead of being overclaimed as accepted or collapsed into hard reject.
- watcher alerts and evidence emit `ValidatorIncomplete` for missing-artifact,
  retry-pending, and publication-gap cases while retaining hard mismatch or
  invalid-batch behavior for true contract violations.
- the same publication binding and route-table digest path remains canonical
  for validator and watcher surfaces.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation.
2. Run `cargo test -p z00z_validators --release --test test_hjmt_publication_contract verdict_retry_incomplete -- --nocapture`.
3. Run `cargo test -p z00z_validators --release --test test_hjmt_publication_contract verdict_blob_gap_incomplete -- --nocapture`.
4. Run `cargo test -p z00z_validators --release --test test_hjmt_publication_contract checkpoint_rejects_pub_drift -- --nocapture`.
5. Run `cargo test -p z00z_watchers --release --test test_hjmt_publication_contract watcher_missing_verdict_incomplete -- --nocapture`.
6. Run `cargo test -p z00z_watchers --release --test test_hjmt_publication_contract watcher_missing_binding_incomplete -- --nocapture`.
7. Run `cargo test -p z00z_watchers --release --test test_hjmt_publication_contract watcher_retry_incomplete -- --nocapture`.
8. Run `cargo test -p z00z_watchers --release --test test_hjmt_publication_contract watcher_gap_incomplete -- --nocapture`.
9. Run `cargo test -p z00z_watchers --release --test test_hjmt_publication_contract watcher_rejects_binding_drift -- --nocapture`.
10. Run `cargo test -p z00z_watchers --release --test test_object_alerts -- --nocapture`.
11. Run `cargo test --release`.
12. If this slice lands after `060-11` was already prepared, rerun the targeted wallet/object, validator, watcher, and simulator closure evidence required by `060-TODO.md` Wave 4 Step 5 on the post-`060-14` tree, and refresh any Phase 060 closeout memo or `060-11` evidence packet that still cites the older tree.
13. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times and fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
14. If this slice needs a git commit after validation passes, use `/z00z-git-versioning` instead of ad hoc git commit commands.
</verify>
<done>
Validator and watcher publication surfaces now express real incomplete states
without forking publication truth away from the existing binding path.
</done>
</task>
</tasks>

<threat_model>
- Unbound refund or reject outputs let a voucher strip its restriction model and
  reappear as unrelated clean value.
- A second object issue or create API would fork typed-object semantics and
  reopen the Phase 060 one-plane wallet authority risk.
- Collapsing unresolved publication states into accepted or rejected hides
  liveness and availability failures that operators and watchers must actually
  see.
- Reusing `Incomplete` for truly invalid artifacts would weaken fail-closed
  rejection and make route or binding drift look retriable when it is not.
</threat_model>

<verification>
- Follow the mandatory verify order in every auto task: bootstrap first,
  targeted release tests second, `cargo test --release` when Rust changes,
  repeated `/GSD-Review-Tasks-Execution` passes after code changes, and
  `/z00z-git-versioning` only if a commit is needed.
- Preserve exact failing or passing evidence for refund-target rejection,
  restricted-source rejection, source-context issue or create package build,
  and incomplete verdict or alert surfacing.
- If any targeted `060-14` work lands after earlier Wave 4 closeout prep, the
  final closure packet must be refreshed on the post-`060-14` tree.
</verification>

<success_criteria>
- refund and reject outputs cannot escape the declared refund target or
  restricted source context.
- `wallet.object.preview_package` and `wallet.object.build_package` remain the
  only typed-object build surface while covering voucher issue and right create.
- rights stay zero-value and outside asset or voucher balance symmetry.
- validator and watcher publication surfaces now expose incomplete states
  without introducing a second publication checker.
- the Phase 060 packet can point to real code and test seams for these three
  reopen items instead of overclaiming that Phase 059 or the earlier 060
  slices already closed them.
</success_criteria>

<output>Create .planning/phases/060-Gaps-Closing/060-14-SUMMARY.md when done.</output>
