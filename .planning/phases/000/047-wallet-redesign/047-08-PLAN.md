---
phase: 047-wallet-redesign
plan: 8
type: execute
wave: 8
depends_on:
  - 047-01
  - 047-02
  - 047-03
  - 047-04
  - 047-05
  - 047-06
  - 047-07
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/src/scenario_1/runner_contract_table.in
  - crates/z00z_simulator/src/scenario_1/runner_verify.rs
  - crates/z00z_simulator/tests/test_scenario1_stage_surface.rs
  - crates/z00z_wallets/src/services/wallet_service_tests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/asset_impl_tests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_pending_body.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_send_body.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/backup_impl/tests.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/tests.rs
  - crates/z00z_wallets/src/backup/backup_importer_impl/tests.rs
  - .planning/phases/047-wallet-redesign/047-wallet-addon-spec.md
autonomous: true
requirements:
  - REQ-019
  - REQ-020
must_haves:
  truths:
    - "Stage 13 report and verifier text stop naming encrypted Snapshot payloads as the live claimed-asset authority."
    - "If Stage 13 claims reopen or `WalletPlusHistory` proof, it actually executes those operations."
    - "Existing wallet and simulator suites do not present Snapshot and `OwnedAssetPayload` as simultaneous canonical authorities."
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs
      provides: rewritten Stage 13 storage truth string and proof notes
    - path: crates/z00z_simulator/src/scenario_1/runner_verify.rs
      provides: fail-closed Stage 13 assertions aligned to owned-asset authority
    - path: .planning/phases/047-wallet-redesign/047-wallet-addon-spec.md
      provides: phase-local Phase 046 spec copy updated so Snapshot is no longer described as the target asset authority
user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Cut the simulator, phase-local docs, and existing tests over to the new storage
truth, then close the validation matrix.

Purpose: prevent the repository from shipping green code plus stale proof text
that still calls Snapshot the live wallet asset authority.
Output: Stage 13 cutover, migrated existing suites, updated phase-local spec
copy, and final validation closeout.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/047-wallet-redesign/047-CONTEXT.md
@.planning/phases/047-wallet-redesign/047-TODO.md

<interfaces>
From `crates/z00z_simulator/src/scenario_1/runner_verify.rs`:

```rust
const STORAGE_AUTHORITY_TEXT: &str =
    "claimed-asset persistence remains in encrypted .wlt snapshot payloads";
```

From `.planning/phases/047-wallet-redesign/047-wallet-redesign-spec.md`:

```text
Replace "claimed-asset persistence remains in encrypted .wlt snapshot payloads"
with language naming `OwnedAssetPayload` objects.
```
</interfaces>
</execution_context>

<context>
@.planning/phases/047-wallet-redesign/047-wallet-redesign-spec.md
@.planning/phases/047-wallet-redesign/047-wallet-addon-spec.md
@.planning/phases/047-wallet-redesign/047-06-PLAN.md
@.planning/phases/047-wallet-redesign/047-07-PLAN.md

This final wave is the honesty wave. The runtime and store can already be right,
but the phase is still incomplete if simulator proof text, existing tests, or
the phase-local 046 spec copy still imply Snapshot is the target authority.
</context>

## Task Spec Reads

- `Task 1`: read `047-wallet-redesign-spec.md` lines `395-427`, `471-490`,
  `1450-1490`, `1718-1778`, `1816-1828`, `1866-1894`, and `1895-1934`.
- `Task 2`: read `047-wallet-redesign-spec.md` lines `1718-1778`,
  `1816-1852`, `1855-1867`, and `1869-1894`.
- `Task 3`: read `047-wallet-redesign-spec.md` lines `421-427`, `1450-1490`,
  `1830-1852`, `1869-1894`, and `1895-1934`.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Stage 13 storage/report text and execute the new proof steps for real</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/src/scenario_1/runner_contract_table.in, crates/z00z_simulator/src/scenario_1/runner_verify.rs</files>
  <action>Re-read the Task 1 spec-read matrix before implementation. Replace the exact stale authority string `claimed-asset persistence remains in encrypted .wlt snapshot payloads` with wording that names `OwnedAssetPayload` objects, keep `prev_root`, `state_root`, and `flat_root` strictly in the storage-root vocabulary, and make Stage 13 actually execute the reopen, restore, tamper, and wallet.tx lifecycle steps it claims to prove. A report flag alone is not evidence.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the Stage 13 truth surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Stage 13 describes and proves the new wallet storage truth honestly, with no leftover Snapshot-authority wording or report-only proof claims.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate existing wallet and simulator suites off Snapshot live-authority assertions</name>
  <files>crates/z00z_wallets/src/services/wallet_service_tests.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/asset_impl_tests.rs, crates/z00z_wallets/src/adapters/rpc/methods/test_tx_pending_body.rs, crates/z00z_wallets/src/adapters/rpc/methods/test_tx_send_body.rs, crates/z00z_wallets/src/adapters/rpc/methods/backup_impl/tests.rs, crates/z00z_wallets/src/db/redb_wallet_store/tests.rs, crates/z00z_wallets/src/backup/backup_importer_impl/tests.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs, crates/z00z_simulator/tests/test_scenario1_stage_surface.rs</files>
  <action>Re-read the Task 2 spec-read matrix before implementation. Upgrade every listed existing suite that currently encodes Snapshot or `claimed_assets` as the live authority so the green repository tells one consistent story: profile objects plus owned-asset objects plus explicit JSONL sidecar. Keep compatibility tests only where they explicitly prove one-shot legacy import/migration; do not leave dual assertions that bless both the old and new authority models at once.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the existing-test migration surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Existing wallet and simulator suites encode one canonical post-cutover authority model and still cover the required reopen/restore/tamper lifecycle behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Update the phase-local Phase 046 spec copy and close the final validation matrix</name>
  <files>.planning/phases/047-wallet-redesign/047-wallet-addon-spec.md, crates/z00z_simulator/src/scenario_1/runner_verify.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs</files>
  <action>Re-read the Task 3 spec-read matrix before implementation. Update the Phase 047-local copy of the Phase 046 spec so Decision 2 no longer implies Snapshot is the target live authority, add the redesign direction captured by Decision 9, remove any stale wording that presents `wallet.asset.send_asset` or adjacent compatibility ops as canonical confirmed spend outside the `wallet.tx.*` plus reconcile lane, and close the final validation matrix with no contradictory strings left in simulator text, docs, or tests. The repository should end this phase with one storage story and one proof story.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the final doc/validation surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>The phase-local doc set, simulator text, and validation matrix all agree that owned assets are first-class encrypted `.wlt` objects and Snapshot is no longer the target authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime truth -> simulator text | A stale Stage 13 string can make honest code look dishonest. |
| existing suites -> canonical authority | Green tests can silently preserve the old architecture if not migrated. |
| phase-local design docs -> execution proof | Contradictory docs would mislead the next agent even if code is correct. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-22 | R | Stage 13 text/reporting | mitigate | Replace the exact stale authority string and require real reopen/restore execution. |
| T-047-23 | T | existing suites | mitigate | Rewrite old-authority assertions in the same patch series, keeping only explicit migration coverage. |
| T-047-24 | I | phase-local spec copy | mitigate | Update the local Phase 046 copy so future planning does not inherit stale storage guidance. |
</threat_model>

<verification>
This wave closes the phase only when the runtime, simulator, docs, and tests all
tell the same owned-asset authority story and the final validation matrix is
green.
</verification>

<success_criteria>
No green artifact in the repository still calls Snapshot the live wallet asset
authority, and Stage 13 proves the new storage truth with real execution.
</success_criteria>

<output>
After completion, create `.planning/phases/047-wallet-redesign/047-08-SUMMARY.md`.
</output>
