---
phase: 047-wallet-redesign
plan: 2
type: execute
wave: 2
depends_on:
  - 047-01
files_modified:
  - crates/z00z_wallets/src/db/redb_wallet_store/objects/mod.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/queries.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/mod.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/tables.rs
  - crates/z00z_wallets/src/db/index_codecs/mod.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/tests.rs
autonomous: true
requirements:
  - REQ-003
  - REQ-004
  - REQ-005
  - REQ-014
must_haves:
  truths:
    - "Asset status transitions update the same `object_id`; they do not allocate a second object for one `asset_id`."
    - "Production code has non-test constructors for validated index updates and values."
    - "Indexed reads return object ids plus cursor metadata using canonical key formats."
  artifacts:
    - path: crates/z00z_wallets/src/db/redb_wallet_store/objects/mod.rs
      provides: production `write_object_by_id(...)` and shared index-manifest replacement path
    - path: crates/z00z_wallets/src/db/redb_wallet_store/queries.rs
      provides: indexed page reads for future owned-asset queries
    - path: crates/z00z_wallets/src/db/index_codecs/mod.rs
      provides: production-safe key/value wrappers used outside tests
user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Land the low-level write and query primitives required for `OwnedAssetPayload`
before any live asset authority cutover.

Purpose: make in-place object updates and indexed reads safe, canonical, and
production-usable.
Output: `write_object_by_id(...)`, production index builders, indexed page
queries, and regression tests proving stale index rows are replaced.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/047-wallet-redesign/047-CONTEXT.md
@.planning/phases/047-wallet-redesign/047-TODO.md

<interfaces>
From `crates/z00z_wallets/src/db/redb_wallet_store/objects/mod.rs`:

```rust
pub fn write_object<R: SecureRngProvider>(
    session: &WalletSession,
    kind_id: u8,
    payload_version: u16,
    payload_bytes: Vec<u8>,
    index_updates: &[IndexUpdate],
    rng_provider: R,
) -> WalletResult<u128>;
```

From `crates/z00z_wallets/src/db/redb_wallet_store/tables.rs`:

```rust
impl IndexUpdate {
    #[cfg(test)]
    pub(crate) fn new(
        table: IndexTable,
        semantic_key: Vec<u8>,
        value: Vec<u8>,
    ) -> WalletResult<Self>;
}
```

From `.planning/phases/047-wallet-redesign/047-wallet-redesign-spec.md`:

```rust
pub fn write_object_by_id<R: SecureRngProvider>(...) -> WalletResult<u64>;
pub fn read_objects_by_index(...) -> WalletResult<IndexedObjectPage>;
```
</interfaces>
</execution_context>

<context>
@.planning/phases/047-wallet-redesign/047-wallet-redesign-spec.md
@.planning/phases/047-wallet-redesign/047-01-PLAN.md

The current object writer always allocates a new `object_id`. That is correct
for inserts and wrong for asset status transitions, profile rewrites, and any
future singleton update. This wave adds the missing production API without
duplicating the existing index-manifest replacement logic.
</context>

## Task Spec Reads

- `Task 1`: read `047-wallet-redesign-spec.md` lines `117-135`, `471-485`,
  `1200-1235`, `1291-1327`, `1527-1553`, and `1779-1788`.
- `Task 2`: read `047-wallet-redesign-spec.md` lines `1168-1235`,
  `1308-1327`, `1333-1448`, `1527-1553`, and `1783-1789`.
- `Task 3`: read `047-wallet-redesign-spec.md` lines `471-490`,
  `1200-1235`, `1317-1327`, `1779-1790`, `1830-1845`, and `1895-1934`.

<tasks>

<task type="auto">
  <name>Task 1: Add production object-by-id writes and non-test index builders</name>
  <files>crates/z00z_wallets/src/db/redb_wallet_store/objects/mod.rs, crates/z00z_wallets/src/db/redb_wallet_store/mod.rs, crates/z00z_wallets/src/db/redb_wallet_store/tables.rs, crates/z00z_wallets/src/db/index_codecs/mod.rs</files>
  <action>Re-read the Task 1 spec-read matrix before implementation. Add `write_object_by_id(...)` as a production path that validates supported versions, encrypts with the existing object id, reuses the same index-manifest replacement logic behind `write_object_with_indexes(...)`, and commits object bytes, index updates, save-sequence bump, and integrity update in one write transaction. At the same time, expose non-test constructors/builders for validated index updates and values so later owned-asset code does not depend on `#[cfg(test)]` helpers.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the low-level writer/index-builder surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Production code can rewrite an existing object id safely, and no future owned-asset implementation needs to lean on test-only index constructors.</done>
</task>

<task type="auto">
  <name>Task 2: Add indexed object-page reads with canonical cursor semantics</name>
  <files>crates/z00z_wallets/src/db/redb_wallet_store/queries.rs, crates/z00z_wallets/src/db/index_codecs/mod.rs, crates/z00z_wallets/src/db/redb_wallet_store/mod.rs</files>
  <action>Re-read the Task 2 spec-read matrix before implementation. Add `read_objects_by_index(...)` and any supporting exact-key or prefix-query helpers needed for owned-asset lookups, but keep the cursor and semantic-prefix handling bound to the existing canonical key format in `crate::db::index_codecs`. The API must return object ids plus cursor metadata; payloads are loaded by id afterward, and large data stays in objects rather than index values.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and fix the indexed-read contract before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Wallet-store callers can query object ids by exact owned-asset index semantics without treating index rows as a second truth plane.</done>
</task>

<task type="auto">
  <name>Task 3: Prove object-id stability and stale-index replacement with store tests</name>
  <files>crates/z00z_wallets/src/db/redb_wallet_store/tests.rs, crates/z00z_wallets/src/db/redb_wallet_store/objects/mod.rs, crates/z00z_wallets/src/db/redb_wallet_store/queries.rs</files>
  <action>Re-read the Task 3 spec-read matrix before implementation. Add regression coverage proving that updating one object from status A to status B keeps the same `object_id`, removes stale index entries through `index_manifest`, detects stale/missing index rows during validation, and never requires production paths to call a test-only constructor. These tests are the guardrail that later owned-asset waves will lean on for duplicate-id and in-place status-update guarantees.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the store-level regression surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Store tests prove one-asset/one-object-id semantics and stale-index replacement before any live wallet authority cutover begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| object writer -> index manifest | A bad rewrite path could leave stale indexes pointing at the wrong object state. |
| index helpers -> production callers | Test-only constructors would make the live owned-asset store impossible to implement honestly. |
| indexed page reads -> caller filters | Non-canonical cursor semantics could create silent pagination drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-04 | T | `write_object_by_id(...)` | mitigate | Reuse the existing manifest replacement path instead of cloning logic. |
| T-047-05 | D | indexed reads | mitigate | Keep cursor semantics tied to canonical key formats and small index values. |
| T-047-06 | R | store tests | mitigate | Add explicit same-`object_id` and stale-index replacement coverage now. |
</threat_model>

<verification>
Phase 047 stays safe when later waves can update objects in place, query them by
canonical index semantics, and trust tests to detect stale index drift.
</verification>

<success_criteria>
The low-level store can rewrite existing object ids safely and expose owned-asset
query primitives without introducing a second truth plane.
</success_criteria>

<output>
After completion, create `.planning/phases/047-wallet-redesign/047-02-SUMMARY.md`.
</output>
