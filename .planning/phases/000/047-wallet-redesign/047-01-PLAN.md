---
phase: 047-wallet-redesign
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_wallets/src/db/redb_wallet_store/tables.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/mod.rs
  - crates/z00z_wallets/src/db/redb/schema/redb-schema.yaml
  - crates/z00z_wallets/src/db/schema_keys.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/debug/debug_types.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/debug/debug_export.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/tests.rs
autonomous: true
requirements:
  - REQ-012
  - REQ-013
  - REQ-014
  - REQ-019
  - REQ-020
must_haves:
  truths:
    - "Rust `ObjectKindId`, payload-version gates, `IndexTable`, and `redb-schema.yaml` stay numerically aligned in one patch."
    - "Snapshot remains a transitional object kind only; this wave must not yet change wallet create/open/backup runtime behavior."
    - "Debug dump output behind `wallet_debug_dump` can decode every new object kind and any new owned-asset index tables."
  artifacts:
    - path: crates/z00z_wallets/src/db/redb_wallet_store/tables.rs
      provides: new object kinds, payload versions, and payload-module exports
    - path: crates/z00z_wallets/src/db/schema_keys.rs
      provides: canonical `IndexTable` additions for owned-asset indexes
    - path: crates/z00z_wallets/src/db/redb/schema/redb-schema.yaml
      provides: schema ids and index table entries matching Rust exactly
    - path: crates/z00z_wallets/src/db/redb_wallet_store/debug/debug_types.rs
      provides: decode support for new payload kinds under debug dump
user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Add the new `.wlt` vocabulary for wallet profile, owned assets, tx records, tx
events, backup manifest, and owned-asset indexes without changing runtime wallet
behavior yet.

Purpose: lock the numeric schema contract before any live authority cutover.
Output: Rust/YAML/debug schema parity for all new object kinds and index tables.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/047-wallet-redesign/047-CONTEXT.md
@.planning/phases/047-wallet-redesign/047-TODO.md

<interfaces>
From `crates/z00z_wallets/src/db/redb_wallet_store/tables.rs`:

```rust
#[repr(u8)]
pub enum ObjectKindId {
    WalletRoot = 1,
    Account = 2,
    DerivationState = 7,
    ScanState = 8,
    App = 15,
    Chain = 16,
    Keys = 17,
    StealthMeta = 18,
    TofuPins = 19,
    Snapshot = 250,
}
```

From `crates/z00z_wallets/src/db/redb_wallet_store/mod.rs`:

```rust
fn is_supported_payload_version(kind_id: u8, payload_version: u16) -> bool;
```

From `.planning/phases/047-wallet-redesign/047-wallet-redesign-spec.md`:

```text
WalletProfile = 20
OwnedAsset = 21
WalletTx = 22
WalletTxEvent = 23
BackupManifest = 24
```
</interfaces>
</execution_context>

<context>
@.planning/phases/047-wallet-redesign/047-wallet-redesign-spec.md
@.planning/phases/047-wallet-redesign/047-TODO.md

This wave is intentionally schema-first. It fixes existing Rust/YAML drift
around `StealthMeta`, `TofuPins`, and `Snapshot`, introduces the new object and
index vocabulary, and extends debug tooling, while explicitly deferring live
behavior changes to later waves.
</context>

## Task Spec Reads

- `Task 1`: read `047-wallet-redesign-spec.md` lines `48-135`, `467-490`,
  `1164-1235`, `1236-1289`, `1496-1525`, `1779-1788`, `1796-1800`, `1855-1857`,
  `1866-1867`, and `1869-1894`.
- `Task 2`: read `047-wallet-redesign-spec.md` lines `1210-1235`,
  `1275-1289`, `1496-1525`, `1816-1845`, `1866-1867`, and `1869-1894`.

<tasks>

<task type="auto">
  <name>Task 1: Align Rust object kinds, payload versions, index tables, and schema YAML</name>
  <files>crates/z00z_wallets/src/db/redb_wallet_store/tables.rs, crates/z00z_wallets/src/db/redb_wallet_store/mod.rs, crates/z00z_wallets/src/db/redb/schema/redb-schema.yaml, crates/z00z_wallets/src/db/schema_keys.rs</files>
  <action>Re-read the Task 1 spec-read matrix before touching code. Add `WalletProfile = 20`, `OwnedAsset = 21`, `WalletTx = 22`, `WalletTxEvent = 23`, and `BackupManifest = 24`; add payload-version constants for each new kind; add explicit owned-asset index-table variants instead of overloading old asset-output semantics; fix the existing YAML drift for `StealthMeta = 18`, `TofuPins = 19`, and `Snapshot = 250`; and extend `is_supported_payload_version(...)` so every new kind/version pair is accepted while unsupported drift is rejected. Keep this wave behavior-neutral: do not rewire wallet create/open/backup/receive runtime paths yet.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the schema/vocabulary drift before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Rust object ids, payload versions, owned-asset index-table ids, and `redb-schema.yaml` match exactly with no leftover `18/19/250/254` drift.</done>
</task>

<task type="auto">
  <name>Task 2: Extend debug decode support and schema-guard regression tests</name>
  <files>crates/z00z_wallets/src/db/redb_wallet_store/debug/debug_types.rs, crates/z00z_wallets/src/db/redb_wallet_store/debug/debug_export.rs, crates/z00z_wallets/src/db/redb_wallet_store/tests.rs</files>
  <action>Re-read the Task 2 spec-read matrix before implementation. Extend the `wallet_debug_dump` decode/export path so every new payload kind and any new owned-asset index tables can be inspected, then add store-level regression coverage that proves the supported-version matcher rejects drift and the documented Rust/YAML ids stay locked together. The tests in this wave must preserve the truth that runtime behavior is still unchanged while schema vocabulary is widened.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and fix the debug/schema guard surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Debug dump can render the new object kinds, and regression coverage locks the schema ids/version gates without overstating any runtime cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust ids -> YAML ids | Numeric drift would corrupt future `.wlt` reads and misdocument live data. |
| payload-version gates -> object writes | Missing gates would let unsupported versions enter production stores. |
| debug dump -> forensic validation | Missing decode support would hide schema mistakes during later waves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-01 | T | `tables.rs` / `redb-schema.yaml` | mitigate | Keep Rust and YAML ids in one patch and add drift tests. |
| T-047-02 | I | `debug_*` | mitigate | Add decode support now so later waves can inspect stores honestly. |
| T-047-03 | R | version gates | mitigate | Extend `is_supported_payload_version(...)` and add reject-path tests. |
</threat_model>

<verification>
Phase 047 stays on track when the `.wlt` schema vocabulary is widened cleanly,
the old 18/19/250 drift is removed, and no live wallet behavior changes have
landed yet.
</verification>

<success_criteria>
The repository has one exact schema truth for the new wallet-redesign object and
index vocabulary, and later waves can build on it without numeric ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/047-wallet-redesign/047-01-SUMMARY.md`.
</output>
