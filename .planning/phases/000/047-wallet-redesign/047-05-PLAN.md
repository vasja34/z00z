---
phase: 047-wallet-redesign
plan: 5
type: execute
wave: 5
depends_on:
  - 047-03
  - 047-04
files_modified:
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/mod.rs
  - crates/z00z_wallets/src/services/wallet_service_tests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/asset_impl_tests.rs
autonomous: true
requirements:
  - REQ-010
  - REQ-011
  - REQ-019
  - REQ-020
must_haves:
  truths:
    - "`recv_range(...)` remains the wallet-side ownership authority and persists through `OwnedAssetPayload` objects."
    - "Scan-hit insert plus scan cursor update either commit atomically or are explicitly idempotent on replay with fault-injection coverage."
    - "`wallet.asset.receive_asset` remains compatibility-only and does not become a second persistence authority."
  artifacts:
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
      provides: canonical receive/scan cutover onto owned assets plus scan-state persistence
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs
      provides: `recv_route(..., ReceiveNext::PersistClaim)` backed by owned-asset authority
    - path: crates/z00z_wallets/src/services/wallet_service_tests.rs
      provides: restart/resume and replay-safety regression coverage
user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Rewire the canonical receive and scan boundary so scan hits persist as owned
asset objects and stay coupled to scan-state progress.

Purpose: keep wallet-side ownership detection intact while removing Snapshot
from the receive persistence path.
Output: owned-asset-backed `recv_range(...)`, robust scan cursor updates, and
compatibility-only receive helpers that no longer imply a second authority.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/047-wallet-redesign/047-CONTEXT.md
@.planning/phases/047-wallet-redesign/047-TODO.md

<interfaces>
From `crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs`:

```rust
pub async fn recv_range(
    &self,
    wallet_id: &PersistWalletId,
    chunks: &[ScanChunk],
    requests: &[PaymentRequest],
    max_ckpt: Option<usize>,
) -> WalletResult<ScanRangeOut>;
```

From `crates/z00z_wallets/src/db/redb_wallet_store/mod.rs`:

```rust
pub fn read_scan_state(session: &WalletSession) -> WalletResult<Option<ScanStatePayload>>;
pub fn upsert_scan_state<R: SecureRngProvider>(
    session: &WalletSession,
    payload: &ScanStatePayload,
    rng_provider: R,
) -> WalletResult<u128>;
```
</interfaces>
</execution_context>

<context>
@.planning/phases/047-wallet-redesign/047-wallet-redesign-spec.md
@.planning/phases/047-wallet-redesign/047-04-PLAN.md

The receive path already has the right ownership detector and scan-state seam.
This wave changes only the persistence authority and failure handling around
that detector.
</context>

## Task Spec Reads

- `Task 1`: read `047-wallet-redesign-spec.md` lines `471-481`, `902-927`,
  `1041-1062`, `1625-1648`, `1783-1791`, and `1856-1858`.
- `Task 2`: read `047-wallet-redesign-spec.md` lines `480-481`, `923-927`,
  `1049-1062`, `1636-1639`, `1788-1791`, and `1856-1858`.
- `Task 3`: read `047-wallet-redesign-spec.md` lines `433-454`,
  `1629-1648`, `1809-1814`, `1830-1845`, and `1895-1934`.

<tasks>

<task type="auto">
  <name>Task 1: Rewire `recv_range(...)` and `recv_route(...)` to persist owned assets</name>
  <files>crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs, crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs</files>
  <action>Re-read the Task 1 spec-read matrix before implementation. Keep `StealthOutputScanner`, request registration, and the existing receive orchestration, but replace the `ReceiveNext::PersistClaim` internals so scan hits persist through `OwnedAssetPayload` rather than the Snapshot claimed-asset vector. Preserve the wallet-side authority boundary: remote data supplies chunks and proofs only, while ownership remains decided locally by receiver keys plus `recv_range(...)`.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the receive authority cutover before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>`recv_range(...)` persists found assets through the owned-asset store while keeping wallet-side scan authority intact.</done>
</task>

<task type="auto">
  <name>Task 2: Couple owned-asset writes and scan-state updates with replay-safe failure handling</name>
  <files>crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs, crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs, crates/z00z_wallets/src/services/wallet_service_tests.rs</files>
  <action>Re-read the Task 2 spec-read matrix before implementation. Prefer one RedB transaction that inserts or idempotently confirms the owned asset, refreshes indexes, and advances `ScanStatePayload`. If the first patch must keep a temporary two-step path, document that failure boundary explicitly and add fault-injection coverage proving replay does not create a second asset object or drift the cursor.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the scan-state coupling before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Replay after a mid-path failure remains idempotent, and scan-state persistence cannot silently drift away from the owned-asset set.</done>
</task>

<task type="auto">
  <name>Task 3: Keep `wallet.asset.receive_asset` compatibility-only and migrate receive-path tests</name>
  <files>crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_impl/asset_impl_tests.rs, crates/z00z_wallets/src/services/wallet_service_tests.rs</files>
  <action>Re-read the Task 3 spec-read matrix before implementation. Keep `wallet.asset.receive_asset` as a compatibility/reporting helper that may report ownership but does not become a second persistence authority unless it routes through the same owned-asset store. Update existing receive-path and restart/resume tests so they assert one canonical owned-asset authority and reject any wording or behavior that implies JMT-side ownership detection.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the compatibility/test migration surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>The receive compatibility helper no longer implies a second persistence authority, and migrated tests prove one wallet-side ownership boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan detector -> owned-asset persistence | A bad cutover could turn detection into a second hidden authority. |
| asset insert -> cursor update | Split writes could drift the wallet's asset set and resume position. |
| compatibility receive helper -> docs/tests | Compatibility wording could reintroduce JMT or duplicate-authority confusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-13 | T | `recv_route(...)` | mitigate | Route persistence only through the owned-asset store. |
| T-047-14 | D | cursor coupling | mitigate | Prefer one transaction and add replay/fault-injection coverage. |
| T-047-15 | R | compatibility receive helper | mitigate | Keep it report-only or store-routed and migrate existing tests accordingly. |
</threat_model>

<verification>
This wave is correct when receive remains wallet-side, scan hits land in owned
asset objects, and restart/replay cannot create asset/cursor drift.
</verification>

<success_criteria>
`recv_range(...)` becomes an owned-asset persistence path without changing who
decides ownership or creating a second receive authority.
</success_criteria>

<output>
After completion, create `.planning/phases/047-wallet-redesign/047-05-SUMMARY.md`.
</output>
