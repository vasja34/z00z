---
phase: 047-wallet-redesign
plan: 10
type: execute
wave: 10
depends_on:
  - 047-08
files_modified:
  - crates/z00z_wallets/src/services/wallet/session/wallet_service_session_rotation.rs
  - crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation_recovery.rs
  - crates/z00z_wallets/src/services/wallet/session/wallet_service_session_runtime_limits.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_admin.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/tests.rs
  - crates/z00z_wallets/src/adapters/rpc/types/key.rs
  - crates/z00z_wallets/src/adapters/rpc/types/key/tests.rs
  - crates/z00z_wallets/tests/test_phase047_truth.rs
  - crates/z00z_wallets/tests/test_rpc_types_serialization.rs
  - crates/z00z_wallets/src/services/wallet_service_types_core.rs
  - crates/z00z_wallets/src/wallet/entity/wallet_entity_key_api.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/mod.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/tables.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/crypto_ops/mod.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/crypto_ops/seed_reveal.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/session.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/meta.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/profile.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs
autonomous: true
requirements:
  - REQ-015
  - REQ-019
  - REQ-020
  - FOLLOWUP-047-10-DURABLE-ROTATION
must_haves:
  truths:
    - "`wallet.key.rotate_master_key` becomes a real durable persisted rotation and no longer claims success on top of in-memory rederive only."
    - "Secrets remain in the `secrets` table and any persisted rotation rewrites encrypted state atomically with rollback or replay safety."
    - "Audit, rate-limit, restart, and response semantics prove the same durable storage truth as the implementation."
  artifacts:
    - path: crates/z00z_wallets/src/services/wallet/session/wallet_service_session_rotation.rs
      provides: durable rotation orchestration instead of memory-only placeholder behavior
    - path: crates/z00z_wallets/src/adapters/rpc/methods/key.rs
      provides: public RPC contract updated from Phase 1 memory-only semantics to durable rotation semantics
    - path: crates/z00z_wallets/src/wallet/entity/wallet_entity_key_api.rs
      provides: reconciled key-governance contract so later phases do not contradict the rotation surface
    - path: crates/z00z_wallets/src/adapters/rpc/methods/key_impl/tests.rs
      provides: positive and negative persisted-rotation coverage
user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Turn the current memory-only master-key rotation placeholder into an honest,
durable, restart-safe persisted rotation flow.

Purpose: `047-wallet-addon-spec2.md` still lists durable persisted seed/master
key rotation as future work, while the live code only re-derives cached keys in
memory. This follow-up wave reopens the surface explicitly and lands a real
persisted rotation contract instead of the current misleading placeholder.
Output: one explicit rotation contract, atomic persisted rewrite or recovery
marker, and tests proving durable behavior across restart and failure paths.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/047-wallet-redesign/047-CONTEXT.md
@.planning/phases/047-wallet-redesign/047-wallet-addon-spec2.md

<interfaces>
From `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs`:

```rust
let keys_rederived = service
    .rotate_master_key_in_memory(wallet_id)
    .await?;
```

From `crates/z00z_wallets/src/wallet/entity/wallet_entity_key_api.rs`:

```rust
/// Phase 044: Key API governance layer.
/// Keys are NOT managed by the wallet; the wallet accepts pre-derived keys only.
pub fn rotate_master_key(
    &self,
    _wallet_id: &WalletId,
    _password: String,
    _confirmation: String,
) -> RuntimeRotateKeyResponse
```

From `crates/z00z_wallets/src/db/redb_wallet_store/profile.rs` and the
canonical object tables:

- persisted rotation must rewrite the canonical wallet profile, owned assets,
  scan state, keys, meta, secrets, and integrity rows under one restart-safe
  contract.
</interfaces>
</execution_context>

<context>
@.planning/phases/047-wallet-redesign/047-FULL-AUDIT.md
@.planning/phases/047-wallet-redesign/047-08-PLAN.md

Phase 047 is already closed. This plan exists only because the operator asked
for post-closeout follow-up plans from `047-wallet-addon-spec2.md`. It must
resolve the mismatch between the current memory-only placeholder and the target
durable persisted contract explicitly instead of smuggling a rewrite behind the
Phase 1 placeholder surface.
</context>

## Task Spec Reads

- `Task 1`: read `047-wallet-addon-spec2.md` lines `231-256`; read
  `047-wallet-redesign-spec.md` lines `485-490`.
- `Task 2`: read `047-wallet-addon-spec2.md` lines `231-256`; read
  `047-wallet-redesign-spec.md` lines `485-490`.
- `Task 3`: read `047-wallet-addon-spec2.md` lines `231-256`; read
  `047-wallet-redesign-spec.md` lines `485-490`.

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the rotation API, naming, and governance boundary before touching storage</name>
  <files>crates/z00z_wallets/src/wallet/entity/wallet_entity_key_api.rs, crates/z00z_wallets/src/adapters/rpc/methods/key.rs, crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_admin.rs, crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs, crates/z00z_wallets/src/adapters/rpc/types/key.rs</files>
  <action>Re-read the Task 1 spec-read matrix before implementation. Replace the ambiguous Phase 1 placeholder semantics with one honest contract for `wallet.key.rotate_master_key`, and reconcile the later key-governance guard so the public trait docs, wallet entity surface, RPC server path, and response types all explicitly permit durable persisted rotation. Define the method as an operator-confirmed durable storage rewrite with session validation, rate limit, audit invariants, and restart verification. Choose the outward contract now: replace the Phase 1 `keys_rederived` success metric with a durable rewrite metric such as `records_rewrapped`, and update the public docs, server response, and audit wording accordingly in the same patch series.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the rotation API honesty surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>The public trait docs, wallet entity layer, RPC server surface, and audit naming all describe one honest persisted rotation contract instead of a memory-only placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Implement atomic persisted rewrite and restart recovery for the rotated root</name>
  <files>crates/z00z_wallets/src/services/wallet/session/wallet_service_session_rotation.rs, crates/z00z_wallets/src/services/wallet/session/wallet_service_session_derivation_recovery.rs, crates/z00z_wallets/src/services/wallet/session/wallet_service_session_runtime_limits.rs, crates/z00z_wallets/src/db/redb_wallet_store/mod.rs, crates/z00z_wallets/src/db/redb_wallet_store/tables.rs, crates/z00z_wallets/src/db/redb_wallet_store/crypto_ops/mod.rs, crates/z00z_wallets/src/db/redb_wallet_store/crypto_ops/seed_reveal.rs, crates/z00z_wallets/src/db/redb_wallet_store/session.rs, crates/z00z_wallets/src/db/redb_wallet_store/meta.rs, crates/z00z_wallets/src/db/redb_wallet_store/profile.rs, crates/z00z_wallets/src/db/redb_wallet_store/owned_assets.rs</files>
  <action>Re-read the Task 2 spec-read matrix before implementation. The rewrite set is fixed in this plan and must not be rediscovered ad hoc by the executor: rewrite `secrets.master_key`, every other `SECRETS_TABLE` row sealed under that root, every `OBJECTS_TABLE` record sealed under `derived_keys.data_key` including WalletProfile, OwnedAsset, ScanState, StealthMeta, TofuPins, and Keys, plus the `META_TABLE` marker and integrity rows required to reopen under the new key. Add a dedicated `rotation_in_progress` meta marker, rewrap secret rows first, then object rows and integrity metadata, clear the marker only after successful reopen verification under the new root, and make `rotate_master_key_in_memory` a post-commit cache rebuild step only.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the durable rewrite path before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>The wallet can reopen only on the new persisted root after rotation, and interrupted rotation cannot leave ambiguous or silently downgraded state behind.</done>
</task>

<task type="auto">
  <name>Task 3: Prove durable rotation with adversarial tests, restart checks, and audit evidence</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/key_impl/tests.rs, crates/z00z_wallets/src/services/wallet_service_tests.rs, crates/z00z_wallets/src/adapters/rpc/types/key.rs, crates/z00z_wallets/src/adapters/rpc/types/key/tests.rs, crates/z00z_wallets/tests/test_phase047_truth.rs, crates/z00z_wallets/tests/test_rpc_types_serialization.rs, crates/z00z_wallets/src/services/wallet_service_types_core.rs</files>
  <action>Re-read the Task 3 spec-read matrix before implementation. Add positive coverage for successful durable rotation and negative coverage for wrong password, wrong confirmation, rate-limit exhaustion, double-submit, restart-after-rotate, and injected partial-write recovery. Update the outward contract everywhere it is enforced: runtime type docs, serialization tests, phase truth tests, and `wallet_service_types_core` comments must all shift from the Phase 1 `keys_rederived` story to the chosen durable rewrite metric and restart-safe semantics. No doc, comment, test, or log line may keep the old memory-only contract alive after this slice lands.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the durable-rotation test surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Tests, audit logs, type docs, and truth fixtures all prove durable persisted rotation and no longer overstate an in-memory cache rebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-visible RPC -> actual storage behavior | A durable-looking RPC can mislead operators if the implementation only mutates memory. |
| root-secret rewrite -> persisted wallet state | Partial rewrite can strand secrets, profiles, or owned assets behind mismatched encryption state. |
| later governance text -> reopened rotation scope | Unreconciled policy drift can make the next phase silently re-disable or contradict rotation behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-28 | R | rotation RPC and docs | mitigate | Rewrite naming and API contract so durable claims match the implemented persistence semantics. |
| T-047-29 | T | persisted rewrite path | mitigate | Add atomic marker, rollback or replay safety, and restart verification before reporting success. |
| T-047-30 | I | later key-governance surface | mitigate | Reconcile `wallet_entity_key_api` and runtime RPC semantics in the same patch series. |
</threat_model>

<verification>
This wave passes only when `wallet.key.rotate_master_key` proves durable
persisted behavior across restart and failure paths and the full public
contract is updated to match that behavior.
</verification>

<success_criteria>
No code path or test still claims durable master-key rotation while only
calling the memory-only rederive placeholder, and the final rotation contract is
crash-safe, restart-safe, and audit-honest.
</success_criteria>

<output>
After completion, create `.planning/phases/047-wallet-redesign/047-10-SUMMARY.md`.
</output>