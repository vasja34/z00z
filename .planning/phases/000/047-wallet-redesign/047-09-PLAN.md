---
phase: 047-wallet-redesign
plan: 9
type: execute
wave: 9
depends_on:
  - 047-08
files_modified:
  - crates/z00z_wallets/src/wallet/snapshot/snapshot_types.rs
  - crates/z00z_wallets/src/wallet/snapshot/snapshot_impl.rs
  - crates/z00z_wallets/src/backup/backup_exporter_impl.rs
  - crates/z00z_wallets/src/backup/export/backup_exporter_verify.rs
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack_snapshot.rs
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_load_restore.rs
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_transfer_import.rs
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_create_unlock_open.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
  - crates/z00z_wallets/src/db/wallet_store.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/backup.rs
  - crates/z00z_wallets/src/db/redb_wallet_store/debug/debug_export.rs
  - crates/z00z_wallets/src/backup/backup_importer_impl/mod.rs
  - crates/z00z_wallets/src/backup/backup_importer_impl/tests.rs
  - crates/z00z_wallets/src/services/wallet_service_tests.rs
  - crates/z00z_wallets/src/wallet/WALLET-GUIDE.md
autonomous: true
requirements:
  - REQ-001
  - REQ-002
  - REQ-009
  - REQ-015
  - REQ-019
  - REQ-020
must_haves:
  truths:
    - "Normal create/open/save/export/restore flows no longer serialize or depend on a full `WalletPersistenceState` payload."
    - "Normal create/open/save/export/restore flows no longer depend on compatibility bytes or a second live authority plane."
    - "Wallet reopen and backup truth stays profile-first plus owned-asset objects plus scan state plus explicit JSONL tx-history sidecar."
  artifacts:
    - path: crates/z00z_wallets/src/services/wallet/store/wallet_service_store_load_restore.rs
      provides: live reopen and restore separation with no hidden compatibility fallback on normal paths
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
      provides: export and restore flow that no longer treats compatibility bytes as a normal persistence plane
    - path: crates/z00z_wallets/src/services/wallet/store/wallet_service_store_transfer_import.rs
      provides: import decode path with no silent compatibility fallback on the canonical transfer route
    - path: crates/z00z_wallets/src/backup/export/backup_exporter_verify.rs
      provides: backup export verification that no longer blesses compatibility bytes as a normal wallet-state bridge
    - path: crates/z00z_wallets/src/wallet/snapshot/snapshot_types.rs
      provides: no dual-authority export shape on the normal pack surface
user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Delete obsolete compatibility state from all normal live wallet flows and leave
one canonical profile/owned-asset/scan-state/JSONL path.

Purpose: Phase 047 already shipped with profile-first plus owned-asset truth,
but residual compatibility state still exists in reopen, export, debug, and
test surfaces. This follow-up wave removes that residual compatibility from the
normal path without reopening a second authority model.
Output: one canonical reopen/export/restore shape, no normal-path compatibility
payload dependency, and migrated tests/docs that describe only the canonical
path.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/047-wallet-redesign/047-CONTEXT.md
@.planning/phases/047-wallet-redesign/047-wallet-addon-spec2.md

<interfaces>
From `crates/z00z_wallets/src/services/wallet/store/wallet_service_store_load_restore.rs`:

- normal reopen and restore must use the canonical profile plus owned-asset
  objects plus scan state plus JSONL history path only.

From `crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs`:

- normal export and restore must emit and consume one canonical pack shape only.

From `crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs`:

- tx history naming remains the explicit `wallet_<stem>_tx_history.jsonl` plane.
</interfaces>
</execution_context>

<context>
@.planning/phases/047-wallet-redesign/047-FULL-AUDIT.md
@.planning/phases/047-wallet-redesign/047-07-PLAN.md
@.planning/phases/047-wallet-redesign/047-08-PLAN.md

Phase 047 is already closed. This plan exists only because the operator
explicitly requested post-closeout follow-up plans for the residual future
slices listed in `047-wallet-addon-spec2.md`. Execute it only as an intentional
extension of the shipped Phase 047 packet, not as a claim that 047-01 through
047-08 were incomplete.
</context>

## Task Spec Reads

- `Task 1`: read `047-wallet-addon-spec2.md` lines `170-214` and `248-252`;
  read `047-wallet-redesign-spec.md` lines `471-479` and `489-490`.
- `Task 2`: read `047-wallet-addon-spec2.md` lines `170-214` and `248-252`;
  read `047-wallet-redesign-spec.md` lines `471-472`, `479`, `485`, and
  `489-490`.
- `Task 3`: read `047-wallet-addon-spec2.md` lines `170-214` and `248-252`;
  read `047-wallet-redesign-spec.md` lines `471-472` and `489-490`.

<tasks>

<task type="auto">
  <name>Task 1: Fence normal compatibility callers and delete the obsolete path</name>
  <files>crates/z00z_wallets/src/services/wallet/store/wallet_service_store_load_restore.rs, crates/z00z_wallets/src/services/wallet/store/wallet_service_store_create_unlock_open.rs, crates/z00z_wallets/src/db/wallet_store.rs, crates/z00z_wallets/src/db/redb_wallet_store/backup.rs, crates/z00z_wallets/src/db/redb_wallet_store/debug/debug_export.rs</files>
  <action>Re-read the Task 1 spec-read matrix before implementation. Inventory every use of `read_wallet_snapshot`, `create_snapshot`, `profile_from_snapshot`, and snapshot-based claimed-asset migration, then remove that compatibility path from normal reopen/save/export/restore flows entirely. Debug or export helpers must not silently preserve obsolete compatibility bytes as live truth, and no fallback path may reintroduce a second authority plane.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the snapshot-caller separation before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Only one canonical reopen/save/export/restore path remains on the normal live flows, and no normal code path depends on obsolete compatibility bytes or a second authority plane.</done>
</task>

<task type="auto">
  <name>Task 2: Remove compatibility material from the canonical export, restore, and pack shape</name>
  <files>crates/z00z_wallets/src/wallet/snapshot/snapshot_types.rs, crates/z00z_wallets/src/wallet/snapshot/snapshot_impl.rs, crates/z00z_wallets/src/backup/backup_exporter_impl.rs, crates/z00z_wallets/src/backup/export/backup_exporter_verify.rs, crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs, crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack_snapshot.rs, crates/z00z_wallets/src/services/wallet/store/wallet_service_store_transfer_import.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs, crates/z00z_wallets/src/backup/backup_importer_impl/mod.rs, crates/z00z_wallets/src/backup/backup_importer_impl/tests.rs</files>
  <action>Re-read the Task 2 spec-read matrix before implementation. Stop building full compatibility bundles on the normal save/export path, remove any compatibility field from the canonical export shape, and stop treating `WalletPersistenceState.claimed_assets` as a normal restore input. Carry canonical restore state through `wallet_profile`, `owned_assets`, `scan_state`, `keys`, and the explicit JSONL history plane only. Traverse the real export, decode, and verify seams in the backup exporter, backup export verifier, canonical pack loader, transfer import route, and backup importer so no fallback survives on either the normal export or normal import path.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the normal export/restore pack shape before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>Export and restore surfaces expose one canonical pack shape, including WalletOnly restore of canonical backups without requiring a published tx-history sidecar, and no normal-path compatibility field or compatibility-derived claimed-asset path.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate tests and docs so only the canonical path remains</name>
  <files>crates/z00z_wallets/src/backup/backup_importer_impl/tests.rs, crates/z00z_wallets/src/services/wallet_service_tests.rs, crates/z00z_wallets/src/wallet/WALLET-GUIDE.md, crates/z00z_wallets/src/db/redb_wallet_store/debug/debug_export.rs</files>
  <action>Re-read the Task 3 spec-read matrix before implementation. Rewrite existing tests and docs that still describe obsolete compatibility state or `claimed_assets` as a normal reopen/export contract. Keep coverage only where it proves the canonical path. Remove dead roundtrip coverage that would otherwise bless a second authority model, and ensure canonical reopen/export round-trips succeed without compatibility bytes on the normal path.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the test/doc truth surface before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then run `/.github/prompts/gsd-review-tasks-execution.prompt.md` through `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode and continue until at least 2 consecutive runs show no significant issues.
4. If this task needs a commit, use `/z00z-git-versioning` before creating it.</verify>
  <done>The repository describes only the canonical reopen/export/test story, and no normal path implies a second authority plane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical `.wlt` truth -> obsolete compatibility residue | A silent fallback can reintroduce a second asset-authority plane after phase closeout. |
| normal export/restore -> compatibility decoding | Hidden compatibility fields can keep old behavior reachable even when docs claim profile-first authority. |
| docs/tests/debug helpers -> live implementation | Stale helper coverage can preserve the wrong storage story after runtime cutover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-25 | R | reopen and restore routing | mitigate | Remove compatibility bytes from all normal callers and fail closed on any obsolete fallback. |
| T-047-26 | R | debug/export helpers | mitigate | Prevent debug surfaces from silently decoding or blessing obsolete compatibility bytes as live truth. |
| T-047-27 | I | docs and tests | mitigate | Rewrite old-authority assertions so the repository tells only one post-closeout storage story. |
</threat_model>

<verification>
This wave passes only when the normal wallet reopen/export/restore story works
without obsolete compatibility bytes and the repository tells one canonical
storage story.
</verification>

<success_criteria>
No normal Phase 047 wallet flow still depends on `WalletPersistenceState` or
`claimed_assets` as a live authority plane, and the repository describes only
the canonical profile plus owned-asset plus scan-state plus JSONL history path.
</success_criteria>

<output>
After completion, create `.planning/phases/047-wallet-redesign/047-09-SUMMARY.md`.
</output>