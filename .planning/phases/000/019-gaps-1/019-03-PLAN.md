---
phase: 019-gaps-1
plan: 03
type: execute
wave: 3
depends_on:
  - 019-02
files_modified:
  - crates/z00z_wallets/src/core/backup/backup_exporter.rs
  - crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
  - crates/z00z_wallets/src/core/backup/backup_importer.rs
  - crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
  - crates/z00z_wallets/src/core/backup/wallet_backup.rs
  - crates/z00z_wallets/src/core/wallet/snapshot.rs
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/backup.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/types/backup.rs
  - crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs
autonomous: true
requirements:
  - PH19-BACKUP
must_haves:
  truths:
    - The active public backup contract converges on `WalletExportPack` and explicitly guarantees restore of root secret material, wallet snapshot state, and versioned restore metadata for the active format.
    - `BackupPayloadV1` remains readable as a legacy format and does not break restore for existing backup files.
    - The phase closes only after wallet and simulator validation gates are green for nullifier ownership, receive taxonomy migration, and backup convergence.
  artifacts:
    - path: crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
      provides: Public backup V2 generation backed by `WalletExportPack`.
    - path: crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
      provides: V2 restore plus V1 legacy fallback read path.
    - path: crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs
      provides: Public RPC semantics aligned to the guaranteed restore set and legacy compatibility.
    - path: crates/z00z_wallets/src/adapters/rpc/methods/backup.rs
      provides: Public JSON-RPC contract text aligned to the guaranteed restore set and legacy V1 input semantics.
    - path: crates/z00z_wallets/src/adapters/rpc/types/backup.rs
      provides: Public backup response types and serialization tests aligned to the converged active format semantics.
    - path: crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs
      provides: Dispatcher-bound JSON-RPC roundtrip coverage so backup create and restore semantics remain correct on the public transport path, not only in direct impl tests.
    - path: crates/z00z_wallets/src/services/wallet_service.rs
      provides: Public backup and restore boundary aligned to the converged wallet export/import contract.
  key_links:
    - from: crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
      to: crates/z00z_wallets/src/services/wallet_service.rs
      via: public backup V2 creation on top of `WalletExportPack`
      pattern: create_backup|export_wallet_payload|WalletExportPack
    - from: crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
      to: crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs
      via: legacy V1 fallback read and public restore semantics
      pattern: restore_backup|import_wallet_payload|BackupPayloadV1
    - from: crates/z00z_wallets/src/adapters/rpc/methods/backup.rs
      to: crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs
      via: public JSON-RPC method registration and transport-visible backup contract
      pattern: wallet.backup.create_backup|wallet.backup.restore_backup|register_backup_methods
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Converge the public backup contract onto `WalletExportPack` while preserving V1 readability and phase gates.

Purpose: close the backup contract gap so public backup create and restore define one explicit guaranteed restore set and reuse the strongest existing wallet export/import path.
Output: one `WalletExportPack`-backed public backup format, legacy V1 read compatibility, explicit guaranteed restore semantics for the active format, and final phase gate validation across wallet and simulator surfaces.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/019-gaps-1/019-CONTEXT.md
@.planning/phases/019-gaps-1/019-RESEARCH.md
@.planning/phases/019-gaps-1/019-VALIDATION.md
@.planning/phases/019-gaps-1/019-02-PLAN.md
@crates/z00z_wallets/src/core/backup/backup_exporter.rs
@crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
@crates/z00z_wallets/src/core/backup/backup_importer.rs
@crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
@crates/z00z_wallets/src/core/backup/wallet_backup.rs
@crates/z00z_wallets/src/core/wallet/snapshot.rs
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_wallets/src/adapters/rpc/methods/backup.rs
@crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs
@crates/z00z_wallets/src/adapters/rpc/types/backup.rs
@crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs
@crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce a WalletExportPack-backed public backup V2 with V1 fallback read path</name>
  <files>crates/z00z_wallets/src/core/backup/backup_exporter.rs, crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs, crates/z00z_wallets/src/core/backup/backup_importer.rs, crates/z00z_wallets/src/core/backup/backup_importer_impl.rs, crates/z00z_wallets/src/core/backup/wallet_backup.rs, crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/src/adapters/rpc/methods/backup.rs, crates/z00z_wallets/src/adapters/rpc/types/backup.rs</files>
  <behavior>
    - Test 1: the active public backup format carries the guaranteed restore set on top of `WalletExportPack`: root secret material, wallet snapshot state, and versioned restore metadata.
    - Test 2: legacy `BackupPayloadV1` files remain readable and do not break restore compatibility during migration.
  </behavior>
  <action>Per D-08 through D-10, D-16, and G-04 through G-05, converge public backup create and restore onto the existing encrypted wallet export/import path instead of evolving a second schema. Introduce the V2 wrapper only as transport around the existing full-state contract and keep V1 readable as legacy input until the migration is complete. Keep the public promise explicit: the active format must guarantee root secret material, wallet snapshot state, and versioned restore metadata; any omitted journals, caches, or history must be described as post-restore rescan territory rather than implied as part of the guarantee.</action>
  <verify>
    <automated>cargo test -p z00z_wallets test_export_import_wallet_payload -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets backup_impl::tests::test_backup_create_list_restore -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets test_runtime_restore_backup_response -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets test_export_import_wallet_payload -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets backup_impl::tests::test_backup_create_list_restore -- --nocapture</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/019-gaps-1/019-03-PLAN.md current_task="Task 1: Introduce a WalletExportPack-backed public backup V2 with V1 fallback read path"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>The public backup format converges on `WalletExportPack`, legacy V1 restore input remains readable, and the public RPC contract does not over-promise beyond the guaranteed restore set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate backup semantics, legacy compatibility, and final phase gates</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/backup.rs, crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs, crates/z00z_wallets/src/adapters/rpc/types/backup.rs, crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs, crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/src/core/wallet/snapshot.rs, crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs</files>
  <behavior>
    - Test 1: the public backup and restore boundary is explicit about the guaranteed restore set and legacy compatibility.
    - Test 2: dispatcher-bound JSON-RPC create and restore roundtrips preserve the same guaranteed restore semantics and legacy readability as the direct backup RPC implementation path.
    - Test 3: the phase closes only after wallet and simulator gates are green for nullifier ownership, receive migration, and backup convergence, and no public RPC contract text or response surface implies unsupported restore breadth.
  </behavior>
  <action>Per D-14 through D-16 and the validation gates in `019-CONTEXT.md`, finish the backup migration by aligning service logic, RPC method contract text, RPC response semantics, and dispatcher-bound JSON-RPC reachability, then run the phase-level gate set rather than only local backup tests. If V1 readability and the V2 guaranteed restore set cannot coexist safely, keep the old public contract unchanged and mark V2 incomplete instead of shipping partial migration behavior. Do not advertise restore of caches, journals, or transaction-history extras unless the implementation actually restores them.</action>
  <verify>
    <automated>cargo test -p z00z_wallets test_export_import_wallet_payload -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets backup_impl::tests::test_backup_create_list_restore -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets test_runtime_restore_backup_response -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --test test_rpc_dispatcher_roundtrip -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --test test_e2e_runtime_parity -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage3_nullifier_store -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets && cargo test -p z00z_simulator</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets test_export_import_wallet_payload -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets backup_impl::tests::test_backup_create_list_restore -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_rpc_dispatcher_roundtrip -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_e2e_runtime_parity -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage3_nullifier_store -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/019-gaps-1/019-03-PLAN.md current_task="Task 2: Validate backup semantics, legacy compatibility, and final phase gates"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>The backup migration is explicit, legacy-compatible, dispatcher-safe on the public JSON-RPC path, and the phase closes only with the wallet and simulator validation gates green.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_wallets test_export_import_wallet_payload -- --nocapture`, `cargo test -p z00z_wallets backup_impl::tests::test_backup_create_list_restore -- --nocapture`, `cargo test -p z00z_wallets test_runtime_restore_backup_response -- --nocapture`, `cargo test -p z00z_wallets --test test_rpc_dispatcher_roundtrip -- --nocapture`, `cargo test -p z00z_wallets --test test_e2e_runtime_parity -- --nocapture`, `cargo test -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture`, `cargo test -p z00z_simulator --test test_stage3_nullifier_store -- --nocapture`, `cargo test -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture`, and the matching `--release --features test-fast --features wallet_debug_dump` commands for wallet backup, receive, and nullifier gates, plus `cargo test -p z00z_wallets && cargo test -p z00z_simulator`. For each auto task, run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue fixing until 2 consecutive runs report no significant issues.
</verification>

<success_criteria>
Phase 019 converges public backup create and restore onto `WalletExportPack`, keeps V1 readable, explicitly guarantees restore of root secret material plus wallet snapshot state plus versioned restore metadata for the active format, and only closes after nullifier, receive, and backup validation gates are all green.
</success_criteria>

<output>
After completion, create `.planning/phases/019-gaps-1/019-03-SUMMARY.md`
</output>