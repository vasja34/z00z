---
phase: 019-gaps-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_simulator/src/claim_pkg_consumer.rs
  - crates/z00z_simulator/src/claim_pkg_store.rs
  - crates/z00z_wallets/src/core/claim/nullifier_store.rs
  - crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
  - crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
autonomous: true
requirements:
  - PH19-NULL
must_haves:
  truths:
    - Claim replay protection becomes storage-owned canonical state instead of wallet-local or simulator-local process state.
    - Claim publication advances asset state and nullifier state together under one canonical storage transition and fails closed on partial publish.
    - Reserve, rollback, reload, and finalize semantics remain explicit and deterministic across claim publish failure paths.
  artifacts:
    - path: crates/z00z_storage/src/assets/store.rs
      provides: Canonical storage-owned asset-plus-nullifier transition boundary.
    - path: crates/z00z_simulator/src/claim_pkg_consumer.rs
      provides: Claim publish flow bound to storage-owned nullifier commit, rollback, and finalize order.
    - path: crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
      provides: Regression coverage for reserve, rollback, reload, and finalize semantics on the canonical path.
    - path: crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
      provides: Regression coverage for publish-time replay rejection, reservation preconditions, and spent-state persistence on the canonical claim publish path.
  key_links:
    - from: crates/z00z_simulator/src/claim_pkg_consumer.rs
      to: crates/z00z_storage/src/assets/store.rs
      via: canonical asset-plus-nullifier publish transition
      pattern: apply_ops|finalize|nullifier|rollback
    - from: crates/z00z_simulator/src/claim_pkg_store.rs
      to: crates/z00z_wallets/src/core/claim/nullifier_store.rs
      via: claim reservation, reload, and finalize orchestration
      pattern: reserve|reload|finalize|lease
    - from: crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
      to: crates/z00z_simulator/src/claim_pkg_consumer.rs
      via: canonical publish-path regression coverage for replay rejection and spent-state durability
      pattern: publish_claims_store|nullifier|replay|Spent
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Define one storage-owned nullifier transition and bind claim publish to it.

Purpose: close the replay-protection ownership gap so claim publication no longer depends on wallet-local or simulator-local nullifier state outside the canonical storage transition.
Output: one canonical asset-plus-nullifier commit seam with fail-closed reserve, rollback, reload, and finalize behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/019-gaps-1/019-CONTEXT.md
@.planning/phases/019-gaps-1/019-RESEARCH.md
@.planning/phases/019-gaps-1/019-VALIDATION.md
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_simulator/src/claim_pkg_consumer.rs
@crates/z00z_simulator/src/claim_pkg_store.rs
@crates/z00z_wallets/src/core/claim/nullifier_store.rs
@crates/z00z_wallets/src/core/tx/claim_tx.rs
@crates/z00z_simulator/tests/test_stage3_nullifier_store.rs
@crates/z00z_simulator/tests/test_claim_tx_pipeline.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the canonical asset-plus-nullifier storage transition</name>
  <files>crates/z00z_storage/src/assets/store.rs, crates/z00z_wallets/src/core/claim/nullifier_store.rs, crates/z00z_simulator/tests/test_stage3_nullifier_store.rs, crates/z00z_simulator/tests/test_claim_tx_pipeline.rs</files>
  <behavior>
    - Test 1: canonical claim publish advances asset state and nullifier state together and fails closed on partial write or verification failure.
    - Test 2: JMT asset absence remains separate from replay-protection state and cannot masquerade as the spent marker.
  </behavior>
  <action>Per D-01 through D-03 and G-01, extend the existing storage-owned write boundary so the nullifier contract is anchored to canonical storage rather than local process state. Keep `NullifierClaim` as identity data and `NullifierLease` as the transaction-scoped reservation handle, but make finalization depend on the storage-owned transition instead of a second persistence seam. Reuse the existing canonical commit seam if it can prove atomic asset-plus-nullifier advancement; do not force a same-root redesign or invent a parallel storage engine just to satisfy the ownership goal. Treat the direct `publish_claims_store(...)` path as part of the canonical contract, not as test-only plumbing, so replay rejection and spent-state persistence are proven on the same path that calls `AssetStore::apply_ops(...)`.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage3_nullifier_store -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage3_nullifier_store -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/019-gaps-1/019-01-PLAN.md current_task="Task 1: Define the canonical asset-plus-nullifier storage transition"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>The codebase has one storage-owned canonical transition for claim replay protection and asset publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind claim publish reserve, rollback, and finalize to the storage-owned path</name>
  <files>crates/z00z_simulator/src/claim_pkg_consumer.rs, crates/z00z_simulator/src/claim_pkg_store.rs, crates/z00z_simulator/tests/test_stage3_nullifier_store.rs, crates/z00z_simulator/tests/test_claim_tx_pipeline.rs</files>
  <behavior>
    - Test 1: reserve, rollback, reload, and finalize still work across bundle write, verification, and apply failure paths.
    - Test 2: claim publish does not leave mixed asset and nullifier state after interrupted or rejected publication, including direct `publish_claims_store(...)` replay and missing-reservation paths.
  </behavior>
  <action>Per D-02, D-14, and the blocker rules in `019-CONTEXT.md`, refactor the simulator claim publish flow so reservation, rollback, reload, and finalize remain explicit but all successful finalization is tied to the canonical storage transition. Do not invent a second nullifier engine, do not bypass `z00z_storage`, and do not widen this track into a broader JMT redesign unless the existing commit seam cannot prove atomic advancement.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage3_nullifier_store -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage3_nullifier_store -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/019-gaps-1/019-01-PLAN.md current_task="Task 2: Bind claim publish reserve, rollback, and finalize to the storage-owned path"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>Claim publish reserve, rollback, reload, and finalize are all consistent with the storage-owned nullifier contract and partial publish fails closed.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_stage3_nullifier_store -- --nocapture`, `cargo test -p z00z_simulator --test test_claim_tx_pipeline -- --nocapture`, `cargo test -p z00z_simulator -- --nocapture`, and the matching `--release --features test-fast --features wallet_debug_dump` commands for both nullifier suites. For each auto task, run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue fixing until 2 consecutive runs report no significant issues.
</verification>

<success_criteria>
Phase 019 has one storage-owned claim replay-protection contract where canonical asset publication and canonical nullifier state advance together, direct canonical publish-path replay is rejected, and partial publish outcomes fail closed automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/019-gaps-1/019-01-SUMMARY.md`
</output>