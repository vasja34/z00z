---
phase: 019-gaps-1
plan: 02
type: execute
wave: 2
depends_on:
  - 019-01
files_modified:
  - crates/z00z_wallets/src/core/address/stealth_scanner.rs
  - crates/z00z_wallets/src/core/address/stealth_scanner/types.rs
  - crates/z00z_wallets/src/core/address/leaf_scan.rs
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs
  - crates/z00z_wallets/tests/test_e2e_runtime_parity.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
  - crates/z00z_simulator/src/scenario_1/stage_5.rs
  - crates/z00z_simulator/examples/simulator_interop.rs
autonomous: true
requirements:
  - PH19-SCAN
must_haves:
  truths:
    - The authoritative public runtime receive contract is report-first and classifies malformed runtime input as `InvalidInput`.
    - Canonical pack-return helpers remain helpers only and do not remain the authoritative taxonomy surface.
    - Direct adapters and high-value callers in scope are migrated in the same plan so the phase does not leave mixed receive semantics behind.
  artifacts:
    - path: crates/z00z_wallets/src/core/address/stealth_scanner.rs
      provides: Runtime report-first receive surface with explicit invalid-input handling.
    - path: crates/z00z_wallets/src/services/wallet_service.rs
      provides: Public receive boundary aligned to the authoritative runtime report contract.
    - path: crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs
      provides: Direct RPC tx verification path aligned with taxonomy-safe owned-output receive handling or explicitly marked compatibility-only.
    - path: crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
      provides: Regression coverage for malformed runtime input and report taxonomy.
    - path: crates/z00z_wallets/tests/test_e2e_runtime_parity.rs
      provides: Canonical and runtime receive parity coverage after taxonomy hardening.
  key_links:
    - from: crates/z00z_wallets/src/core/address/stealth_scanner.rs
      to: crates/z00z_wallets/src/services/wallet_service.rs
      via: runtime report-first receive boundary and invalid-input classification
      pattern: scan|report|InvalidInput|recv_one
    - from: crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs
      to: crates/z00z_wallets/src/core/address/leaf_scan.rs
      via: direct public caller migration away from taxonomy-loss entry points
      pattern: receiver_scan_leaf|receiver_scan_report|scan_leaf
    - from: crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs
      to: crates/z00z_wallets/src/core/address/stealth_scanner.rs
      via: direct tx-output receive classification must not rely on ambiguous raw scan semantics
      pattern: scan_leaf|InvalidInput|InvalidProof|NotMine
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Harden the public receive taxonomy and migrate direct callers in scope.

Purpose: close the validation-layering gap so malformed runtime inputs and proof failures cannot silently degrade into `NotMine`, `MaybeMine`, or `None` on authoritative public paths.
Output: one report-first runtime receive contract, explicit invalid-input handling, and migration of directly coupled adapters and high-value callers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/019-gaps-1/019-CONTEXT.md
@.planning/phases/019-gaps-1/019-RESEARCH.md
@.planning/phases/019-gaps-1/019-VALIDATION.md
@.planning/phases/019-gaps-1/019-01-PLAN.md
@crates/z00z_wallets/src/core/address/leaf_scan.rs
@crates/z00z_wallets/src/core/address/stealth_scanner.rs
@crates/z00z_wallets/src/core/address/stealth_scanner/types.rs
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs
@crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs
@crates/z00z_wallets/tests/test_e2e_runtime_parity.rs
@crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
@crates/z00z_simulator/src/scenario_1/stage_5.rs
@crates/z00z_simulator/examples/simulator_interop.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden authoritative runtime receive report semantics</name>
  <files>crates/z00z_wallets/src/core/address/stealth_scanner.rs, crates/z00z_wallets/src/core/address/stealth_scanner/types.rs, crates/z00z_wallets/src/core/address/leaf_scan.rs, crates/z00z_wallets/src/services/wallet_service.rs, crates/z00z_wallets/tests/test_e2e_runtime_parity.rs, crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs</files>
  <behavior>
    - Test 1: malformed runtime assets surface as `InvalidInput` on the authoritative public receive path.
    - Test 2: proof failures remain explicit and no authoritative public path silently downgrades them to `NotMine`, `MaybeMine`, or `None`.
  </behavior>
  <action>Per D-04 through D-07 and G-02, make the runtime receive contract report-first and explicit. Keep service-level `validate_stealth_consistency()` as defense in depth, but also make the scanner-level report surface taxonomy-safe so direct callers cannot lose invalid-input or invalid-proof classification by bypassing the service helper.</action>
  <verify>
    <automated>cargo test -p z00z_wallets tx_verify_skips_partial_stealth -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --test test_e2e_runtime_parity -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_e2e_runtime_parity -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/019-gaps-1/019-02-PLAN.md current_task="Task 1: Harden authoritative runtime receive report semantics"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>The public runtime receive contract is report-first, explicit, and taxonomy-safe for malformed runtime input and proof failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate direct adapters and high-value receive callers to taxonomy-safe entry points</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs, crates/z00z_simulator/src/scenario_1/stage_5.rs, crates/z00z_simulator/examples/simulator_interop.rs, crates/z00z_wallets/tests/test_e2e_runtime_parity.rs, crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs</files>
  <behavior>
    - Test 1: directly coupled RPC, simulator, and high-value example flows no longer depend on taxonomy-loss entry points for authoritative behavior.
    - Test 2: migration leaves no mixed receive semantics across the public runtime boundary and the highest-value direct callers in scope, including tx-output verification adapters that currently call `scan_leaf()` directly.
  </behavior>
  <action>Per D-15 and G-03, migrate the directly coupled adapters and high-value callers in the same plan track rather than leaving a half-migrated taxonomy split. Include both asset receive RPC surfaces and tx-output verification adapters that currently call `scan_leaf()` directly. Keep compatibility-only helpers internal or clearly secondary, and if a direct caller must remain on `ScanResult` for a local reason, label it explicitly as compatibility-only with a bounded rationale instead of leaving the plan silent.</action>
  <verify>
    <automated>cargo test -p z00z_wallets tx_verify_skips_partial_stealth -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --test test_e2e_runtime_parity -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_e2e_runtime_parity -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/019-gaps-1/019-02-PLAN.md current_task="Task 2: Migrate direct adapters and high-value receive callers to taxonomy-safe entry points"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>The runtime receive migration in scope is complete enough that direct public callers no longer contradict the hardened report-first contract.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_wallets tx_verify_skips_partial_stealth -- --nocapture`, `cargo test -p z00z_wallets --test test_e2e_runtime_parity -- --nocapture`, `cargo test -p z00z_wallets --test test_stealth_scanner_prefilter -- --nocapture`, `cargo test -p z00z_wallets -- --nocapture`, and the matching `--release --features test-fast --features wallet_debug_dump` commands for the runtime-parity and prefilter suites. For each auto task, run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue fixing until 2 consecutive runs report no significant issues.
</verification>

<success_criteria>
Phase 019 has one authoritative report-first public receive contract, malformed runtime input is explicit `InvalidInput`, and the directly coupled callers in scope, including tx-output verification adapters, no longer preserve contradictory receive taxonomy.
</success_criteria>

<output>
After completion, create `.planning/phases/019-gaps-1/019-02-SUMMARY.md`
</output>