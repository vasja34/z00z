---
status: validated
phase: 019-gaps-1
source:
  - 019-01-SUMMARY.md
  - 019-02-SUMMARY.md
  - 019-03-SUMMARY.md
started: 2026-03-25T18:40:42Z
updated: 2026-03-25T23:59:00Z
---

# Phase 019 UAT

## Current Test

number: 5
name: Legacy Backup Readability Stays Narrow and Explicit
expected: |
  Legacy `BackupPayloadV1` input remains readable on restore, but operator-facing
  backup semantics clearly distinguish V1 readability from the full-restore
  guarantees of the active `WalletExportPack`-backed format.
awaiting: phase closeout after unrelated workspace compile blocker is cleared

## Tests

### 1. Claim Replay Protection Fails Closed

expected: When the same claim package is reserved, published twice, or resumed after restart, the second publish path is rejected and the store never ends up in mixed asset/nullifier state.
result: passed

### 2. Runtime Receive Classification Is Report-First

expected: Public receive and direct verification paths classify owned, foreign, and malformed runtime input through explicit `ReceiveReport` semantics, and malformed input surfaces as `InvalidInput` rather than degrading to `NotMine` or `MaybeMine`.
result: passed

### 3. Direct Callers Stay Aligned With Receive Taxonomy

expected: Asset RPC, tx verification, parity coverage, and the wallet reload example all behave consistently with the report-first receive contract instead of assuming raw `ScanResult` reconstruction on public boundaries.
result: passed

### 4. Active Backup Roundtrip Restores Full Wallet State

expected: Creating a backup and restoring it through the active format brings back root secret material, wallet snapshot state, and versioned restore metadata without drifting from dispatcher-visible JSON-RPC behavior.
result: passed

### 5. Legacy Backup Readability Stays Narrow and Explicit

expected: Legacy `BackupPayloadV1` input remains readable on restore, but operator-facing backup semantics clearly distinguish V1 readability from the full-restore guarantees of the active `WalletExportPack`-backed format.
result: passed

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- The historical unrelated wallet compile blocker is resolved on the current branch state: `cargo test -p z00z_wallets --no-run` is green after the integration-test helper stopped importing the feature-gated `sign_claim_auth` re-export.
