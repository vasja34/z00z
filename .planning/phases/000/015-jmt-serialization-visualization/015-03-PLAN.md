---
phase: 015-jmt-serialization-visualization
plan: 03
type: execute
wave: 3
depends_on:
  - 015-02
files_modified:
  - crates/z00z_storage/src/serialization/restore.rs
  - crates/z00z_storage/src/serialization/view.rs
  - crates/z00z_storage/src/serialization/mod.rs
  - crates/z00z_storage/src/lib.rs
  - crates/z00z_storage/src/assets/README.MD
  - crates/z00z_storage/tests/serialization_restore.rs
  - crates/z00z_storage/tests/serialization_visualization.rs
autonomous: true
requirements:
  - STSER-02
  - STSER-03
  - STSER-04
must_haves:
  truths:
    - Saved canonical artifacts can be reconstructed into an inspection-grade structure that preserves namespace layout and root equivalence checks.
    - Human-readable export is deterministic and derived from the typed artifact, not from direct raw live jmt nodes.
    - Storage documentation and crate wiring make the phase 015 inspection boundary explicit.
  artifacts:
    - path: crates/z00z_storage/src/serialization/restore.rs
      provides: Inspection-grade reconstruction helpers.
    - path: crates/z00z_storage/src/serialization/view.rs
      provides: Deterministic DOT and plain-text rendering.
    - path: crates/z00z_storage/tests/serialization_restore.rs
      provides: Reconstruction regression coverage.
    - path: crates/z00z_storage/tests/serialization_visualization.rs
      provides: Visualization ordering and content regression coverage.
    - path: crates/z00z_storage/src/assets/README.MD
      provides: Boundary note explaining inspection-only scope.
  key_links:
    - from: crates/z00z_storage/src/serialization/restore.rs
      to: crates/z00z_storage/src/serialization/artifact.rs
      via: typed reconstruction from persisted artifact
      pattern: restore|rebuild|artifact
    - from: crates/z00z_storage/src/serialization/view.rs
      to: crates/z00z_storage/src/serialization/restore.rs
      via: renderer consumes reconstructed or typed artifact state
      pattern: render_jmt_view|JmtViewFmt
    - from: crates/z00z_storage/src/assets/README.MD
      to: crates/z00z_storage/src/serialization/mod.rs
      via: documented storage-owned boundary
      pattern: serialization|inspection
---

    <!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Add reconstruction, deterministic visualization, and boundary documentation for phase 015.

Purpose: Complete the inspection-grade half of phase 015 while preserving the existing semantic-root and storage ownership boundaries.
Output: Restore and view modules, restore and visualization regression tests, and explicit documentation of the new inspection-only surface.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/015-jmt-serialization-visualization/015-CONTEXT.md
@.planning/phases/015-jmt-serialization-visualization/015-02-PLAN.md
@crates/z00z_storage/src/assets/README.MD
@crates/z00z_storage/src/serialization/artifact.rs
@crates/z00z_storage/src/serialization/codec.rs
@crates/z00z_storage/src/serialization/build.rs

<interfaces>
From crates/z00z_storage/src/serialization/mod.rs:
```rust
pub use self::artifact::{JmtSerArtifact, JmtSerEdge, JmtSerNode, JmtSerTreeId, JmtSerVersion};
```

From phase 015 requirements:
```text
STSER-02: Support restoring or reconstructing tree-relevant serialized data for inspection, replay, or equivalence validation.
STSER-03: Add a deterministic human-readable visualization/export layer for node layout, links, hashes, namespaces, and canonical paths.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inspection-grade reconstruction from artifacts</name>
  <files>crates/z00z_storage/src/serialization/restore.rs, crates/z00z_storage/tests/serialization_restore.rs</files>
  <behavior>
    - Test 1: Restoring from a canonical artifact preserves node count, edge count, namespace grouping, and root-equivalence metadata.
    - Test 2: Reconstruction failures return typed mismatch errors when required structure is missing or inconsistent.
  </behavior>
  <action>Create restore tests first, then implement reconstruction helpers that rebuild inspection-grade relationships from `JmtSerArtifact` per D-03. Keep this path read-only and inspection-oriented; do not mutate the live `AssetStore` write path or introduce a second mutable store implementation.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test serialization_restore -- --nocapture</automated>
  </verify>
  <done>Canonical artifacts can be reconstructed into a typed inspection model that validates structural integrity and root equivalence without touching live store mutation paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic DOT and plain-text visualization</name>
  <files>crates/z00z_storage/src/serialization/view.rs, crates/z00z_storage/tests/serialization_visualization.rs</files>
  <behavior>
    - Test 1: DOT output contains stable nodes, edges, namespace labels, and ordering for unchanged artifacts.
    - Test 2: Plain-text output contains stable node summaries, key hashes, canonical path tuples, and tree namespace labels.
  </behavior>
  <action>Create visualization tests first, then implement `JmtView` and `JmtViewFmt` so rendering consumes only the typed artifact or reconstructed inspection model per STSER-03. Use deterministic ordering and explicit formatting. Do not introduce a new graph dependency in this phase.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test serialization_visualization -- --nocapture</automated>
  </verify>
  <done>Phase 015 artifacts can be rendered deterministically into human-readable DOT and plain-text views suitable for tests and regression diffs.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize facade wiring and boundary documentation</name>
  <files>crates/z00z_storage/src/serialization/mod.rs, crates/z00z_storage/src/lib.rs, crates/z00z_storage/src/assets/README.MD</files>
  <action>Finalize the public or crate-visible exports for restore and view helpers, then document the inspection-only boundary in `assets/README.MD` per STSER-04. Explicitly state that phase 015 artifacts do not replace semantic public roots or widen ownership into other crates. Preserve existing crate organization and avoid unrelated README edits.</action>
  <verify>
    <automated>cargo test -p z00z_storage --tests -- --nocapture</automated>
  </verify>
  <done>The crate surface and storage documentation describe the new module accurately, and all storage tests remain green with the completed phase 015 feature set.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_storage --test serialization_restore --test serialization_visualization -- --nocapture` and `cargo test -p z00z_storage --tests -- --nocapture`.
</verification>

<success_criteria>
Phase 015 fully supports deterministic inspection artifacts: build, persist, restore, visualize, and document, without leaking raw jmt internals or changing existing storage semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/015-jmt-serialization-visualization/015-03-SUMMARY.md`
</output>
