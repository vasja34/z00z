---
phase: 015-jmt-serialization-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - 015-01
files_modified:
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_storage/src/assets/store_internal/tree_store.rs
  - crates/z00z_storage/src/serialization/build.rs
  - crates/z00z_storage/src/serialization/codec.rs
  - crates/z00z_storage/src/serialization/store.rs
  - crates/z00z_storage/tests/serialization_roundtrip.rs
  - crates/z00z_storage/tests/serialization_determinism.rs
autonomous: true
requirements:
  - STSER-01
  - STSER-02
must_haves:
  truths:
    - The live storage state can be converted into one deterministic canonical artifact using storage-owned traversal code.
    - Canonical artifact bytes can be encoded, decoded, persisted, and reloaded without losing typed structure.
    - Re-encoding unchanged state yields stable bytes and a stable derived artifact identity.
  artifacts:
    - path: crates/z00z_storage/src/serialization/build.rs
      provides: Builder from live asset store state to typed artifact.
    - path: crates/z00z_storage/src/serialization/codec.rs
      provides: Version-gated deterministic encoding and decoding helpers.
    - path: crates/z00z_storage/src/serialization/store.rs
      provides: File persistence facade for artifacts.
    - path: crates/z00z_storage/tests/serialization_roundtrip.rs
      provides: Roundtrip regression coverage.
    - path: crates/z00z_storage/tests/serialization_determinism.rs
      provides: Stable byte and identity regression coverage.
  key_links:
    - from: crates/z00z_storage/src/serialization/build.rs
      to: crates/z00z_storage/src/assets/store.rs
      via: read-only extraction seam over asset store state
      pattern: impl AssetStore|pub\(crate\)|pub\(super\)
    - from: crates/z00z_storage/src/serialization/codec.rs
      to: crates/z00z_storage/src/serialization/artifact.rs
      via: version-gated BincodeCodec serialization
      pattern: BincodeCodec|serialize|deserialize
    - from: crates/z00z_storage/src/serialization/store.rs
      to: crates/z00z_storage/src/serialization/codec.rs
      via: save/load facade over canonical bytes
      pattern: save_artifact|load_artifact|derive_artifact_id
---

    <!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Implement deterministic artifact building, canonical encoding, and persistence for phase 015.

Purpose: Produce the machine-readable storage-owned serialization path that satisfies STSER-01 and the roundtrip half of STSER-02.
Output: Build, codec, and file store modules plus regression tests for roundtrip and determinism.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/015-jmt-serialization-visualization/015-CONTEXT.md
@.planning/phases/015-jmt-serialization-visualization/015-01-PLAN.md
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/assets/store_internal/tree_store.rs
@crates/z00z_storage/src/snapshot/codec.rs
@crates/z00z_storage/src/snapshot/store.rs

<interfaces>
From crates/z00z_storage/src/assets/store.rs:
```rust
#[derive(Default)]
pub struct MemTreeStore {
    inner: RefCell<MemTreeInner>,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub enum StoreOp {
    Put(Box<StoreItem>),
    Delete(AssetPath),
}
```

From crates/z00z_storage/src/assets/store_internal/tree_store.rs:
```rust
pub(super) struct TreeStore<'a> {
    inner: &'a MemTreeStore,
}

impl<'a> TreeStore<'a> {
    pub(super) fn get_value(
        &self,
        tree_id: TreeId,
        key: KeyHash,
        version: Version,
    ) -> Result<Option<Vec<u8>>, AssetStoreError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic build and codec path</name>
  <files>crates/z00z_storage/src/serialization/build.rs, crates/z00z_storage/src/serialization/codec.rs, crates/z00z_storage/tests/serialization_roundtrip.rs, crates/z00z_storage/tests/serialization_determinism.rs</files>
  <behavior>
    - Test 1: Building an artifact from unchanged logical state twice yields byte-identical canonical bytes.
    - Test 2: Encoding and decoding a canonical artifact preserves typed nodes, edges, namespaces, and roots.
    - Test 3: Unsupported version or malformed bytes return typed serialization errors.
  </behavior>
  <action>Create roundtrip and determinism tests first, then implement `build.rs` and `codec.rs` per STSER-01 and STSER-02. Builder logic must traverse only through storage-owned seams, normalize namespace and node data into the phase 015 artifact, and sort deterministically. Codec logic must follow the snapshot version-gated bincode pattern and derive a stable artifact id from canonical bytes only.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test serialization_roundtrip --test serialization_determinism -- --nocapture</automated>
  </verify>
  <done>Phase 015 can build, encode, and decode canonical artifacts deterministically, with regression tests proving roundtrip stability and typed failure behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add persistence facade and read-only store hooks</name>
  <files>crates/z00z_storage/src/assets/store.rs, crates/z00z_storage/src/assets/store_internal/tree_store.rs, crates/z00z_storage/src/serialization/store.rs, crates/z00z_storage/tests/serialization_roundtrip.rs</files>
  <behavior>
    - Test 1: Persisted artifacts load back to the same typed structure and derived identity.
    - Test 2: Read-only hooks expose enough namespace-aware state for serialization without widening the mutable public AssetStore API.
  </behavior>
  <action>Add the minimum read-only extraction seams required to collect namespace-aware JMT state, keeping them crate-private or narrowly scoped per D-04 safety boundary. Implement `serialization/store.rs` with `save_artifact`, `load_artifact`, and `derive_artifact_id` using `z00z_utils::io` only. Avoid exposing mutable internals or raw jmt types through public APIs.</action>
  <verify>
    <automated>cargo test -p z00z_storage --test serialization_roundtrip -- --nocapture</automated>
  </verify>
  <done>Artifacts can be persisted and reloaded through a narrow storage-owned facade, and the live store exposes only the internal read-only seams needed by the builder.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_storage --test serialization_roundtrip --test serialization_determinism -- --nocapture` and `cargo test -p z00z_storage --lib -- --nocapture`.
</verification>

<success_criteria>
The machine-readable phase 015 path exists end to end: live state to artifact, artifact to bytes, bytes to artifact, and artifact to persisted file storage, all deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/015-jmt-serialization-visualization/015-02-SUMMARY.md`
</output>
