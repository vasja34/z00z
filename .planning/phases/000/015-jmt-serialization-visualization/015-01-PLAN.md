---
phase: 015-jmt-serialization-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_storage/src/lib.rs
  - crates/z00z_storage/src/error.rs
  - crates/z00z_storage/src/serialization/mod.rs
  - crates/z00z_storage/src/serialization/artifact.rs
autonomous: true
requirements:
  - STSER-01
  - STSER-04
must_haves:
  truths:
    - User-facing storage APIs expose a storage-owned serialization contract rather than raw jmt internals.
    - Phase 015 has a versioned artifact model that can carry deterministic tree data without changing existing checkpoint or snapshot behavior.
    - Downstream implementation work has explicit type contracts for nodes, edges, namespaces, metadata, and errors.
  artifacts:
    - path: crates/z00z_storage/src/serialization/mod.rs
      provides: Public serialization facade for phase 015.
    - path: crates/z00z_storage/src/serialization/artifact.rs
      provides: Versioned artifact, typed node and edge records, and namespace identifiers.
    - path: crates/z00z_storage/src/error.rs
      provides: Typed serialization error variants or enum used by later plans.
  key_links:
    - from: crates/z00z_storage/src/lib.rs
      to: crates/z00z_storage/src/serialization/mod.rs
      via: module declaration or re-export boundary
      pattern: pub mod serialization|pub use .*serialization
    - from: crates/z00z_storage/src/serialization/mod.rs
      to: crates/z00z_storage/src/serialization/artifact.rs
      via: explicit typed re-exports
      pattern: pub use self::artifact
---

    <!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Define the storage-owned phase 015 contract boundary for deterministic JMT serialization.

Purpose: Establish the exact artifact types, versioning, and typed error surface before any live-store traversal or rendering code is added.
Output: A compile-ready serialization module facade, artifact contracts, and error surface that later plans can implement against.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/015-jmt-serialization-visualization/015-CONTEXT.md
@specs/015-z00z-storage-addons/storage-serialization-plan.md
@crates/z00z_storage/src/lib.rs
@crates/z00z_storage/src/error.rs
@crates/z00z_storage/src/assets/README.MD

<interfaces>
From crates/z00z_storage/src/lib.rs:
```rust
pub mod assets;
pub mod checkpoint;
pub mod error;
pub mod snapshot;

pub use self::error::{CheckResult, CheckpointError};
```

From crates/z00z_storage/src/error.rs:
```rust
pub type CheckResult<T> = Result<T, CheckpointError>;

#[derive(Debug, Error)]
pub enum CheckpointError {
    VersionMix,
    Codec(#[from] CodecError),
    Backend(String),
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the serialization module contract</name>
  <files>crates/z00z_storage/src/serialization/mod.rs, crates/z00z_storage/src/serialization/artifact.rs</files>
  <action>Create the new serialization module boundary per D-01 storage ownership and D-02 artifact format. Define a versioned artifact model with storage-owned enums and structs for namespace identifiers, nodes, edges, artifact metadata, and canonical roots. Keep the public contract explicit and small. Do not export raw `jmt::storage::Node`, `NodeKey`, `TreeUpdateBatch`, or proof types. Match existing crate naming and typed facade conventions.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib serialization:: -- --nocapture</automated>
  </verify>
  <done>`serialization/mod.rs` and `serialization/artifact.rs` exist, compile, and expose only storage-owned types needed by later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the storage error surface for serialization</name>
  <files>crates/z00z_storage/src/error.rs</files>
  <action>Add precise typed error support for phase 015 per STSER-04. This may be a new serialization-specific enum and result alias or targeted variants on the existing storage error surface, but it must support version mismatch, codec failure, unsupported node kind, reconstruction mismatch, and visualization mismatch. Keep error text concise and stable for tests.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib error -- --nocapture</automated>
  </verify>
  <done>The crate has a typed error path that later serialization, restore, and view modules can use without relying on stringly typed failures.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the module into the crate facade without expanding scope</name>
  <files>crates/z00z_storage/src/lib.rs</files>
  <action>Expose the new serialization module only to the extent needed for downstream plans, per STSER-04 and the phase boundary. Preserve current checkpoint and snapshot exports unchanged. Do not add unrelated crate reorganization.</action>
  <verify>
    <automated>cargo test -p z00z_storage --lib -- --nocapture</automated>
  </verify>
  <done>`z00z_storage` compiles with the new module boundary, and existing public exports remain intact.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_storage --lib -- --nocapture`.
</verification>

<success_criteria>
Phase 015 has a stable contract layer that later plans can build against without re-deciding public types, error semantics, or ownership boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/015-jmt-serialization-visualization/015-01-SUMMARY.md`
</output>
