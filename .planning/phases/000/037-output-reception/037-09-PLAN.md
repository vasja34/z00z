---
phase: 037-output-reception
plan: "09"
type: execute
wave: 9
depends_on:
  - "037-08"
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - .planning/phases/037-output-reception/037-TEST-SPEC.md
  - .planning/phases/037-output-reception/037-TESTS-TASKS.md
  - .planning/phases/037-output-reception/037-TEST-PLAN.md
  - crates/z00z_wallets/src/core/address/stealth_scanner/types_receive.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
  - crates/z00z_wallets/src/core/chain/scan_engine.rs
  - crates/z00z_wallets/src/core/chain/scan_engine_impl.rs
  - crates/z00z_wallets/src/core/storage/scan_storage.rs
  - crates/z00z_wallets/src/services/test_wallet_service_suite.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_asset_impl_suite.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Only actionable receive failures are observable."
    - "Progress and partial-success semantics stay on one implemented contract."
    - "Gap-only Phase 037 tests run only after the later decision-gated tasks settle."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - ".planning/phases/037-output-reception/037-TEST-SPEC.md"
    - "crates/z00z_wallets/src/core/address/stealth_scanner/types_receive.rs"
    - "crates/z00z_wallets/src/core/storage/scan_storage.rs"
  key_links:
    - "ReceiveReject/ReceiveReport -> stable logs/metrics only for actionable failures"
    - "ScanStatePayload -> recv_range(...) -> ScanRangeOut/ScanRangeStat progress contract"
    - "Task 9 coverage -> approved 037 test-planning package after Tasks 11/12/13/17"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Finish the observability and progress contract around the live receive surface, then close the deferred gap-only test task after all remaining decision gates are resolved.

Purpose: keep operator visibility useful while preserving one implemented progress/callback story rooted in the current service boundary, and only then expand tests for the real residual Phase 037 gaps.
Output: non-alerting `NotMine`, stable actionable failure hooks, a progress narrative anchored to `ScanStatePayload`, `ScanRangeOut`, and `ScanRangeStat`, plus a correctly sequenced Task 9 test delta.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
</execution_context>

<context>
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@.planning/phases/037-output-reception/037-TEST-SPEC.md
@.planning/phases/037-output-reception/037-TESTS-TASKS.md
@.planning/phases/037-output-reception/037-TEST-PLAN.md
@crates/z00z_wallets/src/core/address/stealth_scanner/types_receive.rs
@crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
@crates/z00z_wallets/src/core/storage/scan_storage.rs
@crates/z00z_wallets/src/core/chain/scan_engine.rs
@crates/z00z_wallets/src/core/chain/scan_engine_impl.rs
</context>

<tasks>

<task type="auto">
  <name>Task 16. Add receive-path observability only for actionable rejections</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/address/stealth_scanner/types_receive.rs
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
  </files>
  <action>Current live RPC receive still emits an operator-facing warning on `ReceiveStatus::NotMine`; remove or downgrade that signal first, then keep `NotMine` non-alerting and map only actionable receive failures onto stable logs, counters, or both using the repository-standard surfaces already present in the service and RPC layers. Rebase the metrics/observability section in `037-ARCHITECTURE.md` onto the current `ReceiveReject` and `ReceiveReport` vocabulary, and add operator severity guidance for `InvalidInput`, `InvalidProof`, and `RuntimeFail` without inventing a richer `DetectionError` tree.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 16. Add receive-path observability only for actionable rejections"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>No operator-facing warning or equivalent alert hook remains on `ReceiveStatus::NotMine`, actionable failures have stable observable hooks, and the docs do not promise richer observability than the code exposes.</done>
</task>

<task type="auto">
  <name>Task 17. Finish the canonical progress, partial-success, and callback contract</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/core/chain/scan_engine.rs
    crates/z00z_wallets/src/core/chain/scan_engine_impl.rs
    crates/z00z_wallets/src/core/storage/scan_storage.rs
    crates/z00z_wallets/src/services/test_wallet_service_suite.rs
  </files>
  <action>Select the live-service branch explicitly. Keep resume persistence anchored to `ScanStatePayload`, keep current `ScanRangeOut` and `ScanRangeStat` as the only implemented progress surface, and keep `ScanEngine`, `ScanEngineImpl`, or callback facade ideas proposed-only unless they can be added as a thin wrapper without inventing a second event pipeline. Rebase the Phase 037 docs onto the live service contract and add or extend tests only if code changes are needed to preserve monotonic cursor, reported progress, or partial-success behavior.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 17. Finish the canonical progress, partial-success, and callback contract"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>The phase docs point to one implemented progress/callback contract rooted in `ScanStatePayload`, `ScanRangeOut`, and `ScanRangeStat`, with no proposed-only API presented as live truth.</done>
</task>

<task type="auto">
  <name>Task 9. Add only the missing tests that cover unresolved Phase 037 gaps</name>
  <files>
    .planning/phases/037-output-reception/037-TEST-SPEC.md
    .planning/phases/037-output-reception/037-TESTS-TASKS.md
    .planning/phases/037-output-reception/037-TEST-PLAN.md
    crates/z00z_wallets/src/services/test_wallet_service_suite.rs
    crates/z00z_wallets/src/adapters/rpc/methods/test_asset_impl_suite.rs
    crates/z00z_wallets/tests/test_phase037_output_reception.rs
  </files>
  <action>Diff the post-Task-17 codebase against `037-TEST-SPEC.md`, `037-TESTS-TASKS.md`, and `037-TEST-PLAN.md`, then add only the missing tests that remain unresolved. Reuse existing restart, persistence, route-gate, exact-`asset_id`, status-mapping, and adapter/service parity coverage. Prefer extending the existing wallet-service and RPC suites; create `crates/z00z_wallets/tests/test_phase037_output_reception.rs` only when a genuinely new entry point is introduced and the existing suites cannot host the scenario. Add new tests only for new inbox paths, scan-engine parity, or promoted canonical parallel scanning that still survive after Tasks 11, 12, 13, and 17. Do not duplicate restart/resume, claimed-restart, `PersistClaim`, or request-binding tests that already exist.</action>
  <verify>
    <automated>Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump`. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 9. Add only the missing tests that cover unresolved Phase 037 gaps"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>The live test suite covers only the remaining unresolved Phase 037 gaps after the later decision gates settle, and the approved test-planning package stays in sync with what was actually added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| receive results -> operator visibility | Over-alerting or under-alerting would misclassify receive failures |
| persisted cursor -> progress reporting | Drift between saved cursor and reported progress would break restart semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-25 | D | observability surface | mitigate | Remove the current `NotMine` warning path first, then expose only actionable failures through stable hooks |
| T-037-26 | T | progress contract | mitigate | Anchor progress and partial-success docs to `ScanStatePayload`, `ScanRangeOut`, and `ScanRangeStat` only |
| T-037-27 | R | operator guidance | mitigate | Add severity mapping for stable receive reject classes without inventing extra error trees |

</threat_model>

<verification>
Check the updated observability text and any touched tests against the live `ReceiveReject`, `ScanStatePayload`, and approved Phase 037 test-planning contracts before closing the plan.
</verification>

<success_criteria>
Operator-facing receive observability is stable and bounded, the progress contract is described and enforced on one implemented surface only, and Task 9 expands coverage only after the dependent decision gates are closed.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-09-SUMMARY.md`.
</output>
