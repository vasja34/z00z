---
phase: 037-output-reception
plan: "06"
type: execute
wave: 6
depends_on:
  - "037-05"
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/core/address/stealth_scanner.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
  - crates/z00z_wallets/src/core/storage/asset_storage.rs
  - crates/z00z_wallets/src/core/storage/asset_storage_impl.rs
  - crates/z00z_wallets/src/services/wallet_service_store_load_restore.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every new receive path still obeys existing crypto guardrails."
    - "Phase 037 keeps one canonical persistence target instead of adding a third layer."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - "crates/z00z_wallets/src/core/stealth/output.rs"
    - "crates/z00z_wallets/src/core/storage/asset_storage.rs"
  key_links:
    - "detector prefilters -> downstream proof verification -> recv_route persistence"
    - "wallet-native claimed persistence remains the current target"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Reinforce crypto and persistence invariants while resolving the storage-model drift in favor of the live wallet-native baseline.

Purpose: keep Phase 037 aligned with current security boundaries and stop the docs from inventing a third receive persistence layer.
Output: explicit guardrail language in code/docs plus a storage section that names wallet-native claimed persistence as the current canonical target.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
</execution_context>

<context>
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@crates/z00z_wallets/src/core/stealth/output.rs
@crates/z00z_wallets/src/core/storage/asset_storage.rs
@crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
</context>

<tasks>

<task type="auto">
  <name>Task 10. Enforce crypto and security guardrails on every new receive path</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/stealth/output.rs
    crates/z00z_wallets/src/core/address/stealth_scanner.rs
    crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
  </files>
  <action>Audit the receive-path changes from Plans 01-05 against the existing guardrails and harden any touched comments or code so they preserve constant-time tag comparison, tag16/inbox-as-prefilter semantics, downstream proof verification, explicit `ReceiveNext::PersistClaim` gating, and reuse of the same canonical detection core. Do not introduce alternative ownership logic, relaxed tag checks, or proof-verification shortcuts.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 10. Enforce crypto and security guardrails on every new receive path"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>Every touched receive path still enforces the existing crypto guardrails, and no alternate ownership logic or persistence shortcut was introduced.</done>
</task>

<task type="auto">
  <name>Task 11. Resolve the Section 6.4 storage-model drift without creating a third persistence layer</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/storage/asset_storage.rs
    crates/z00z_wallets/src/core/storage/asset_storage_impl.rs
    crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
    crates/z00z_wallets/src/services/wallet_service_store_load_restore.rs
  </files>
  <action>Select the wallet-native persistence branch explicitly for Phase 037. Reclassify Section 6.4 in `037-ARCHITECTURE.md` into implemented, future-unification, and stale sections; name wallet-native claimed-asset persistence as the current canonical target; keep `AssetStorage` unification future-only; and preserve the existing compatibility scrub semantics unless a later phase deliberately replaces them with tested code.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 11. Resolve the Section 6.4 storage-model drift without creating a third persistence layer"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>Phase 037 names one canonical receive persistence target, does not promise a third persistence layer, and preserves the compatibility scrub contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prefilter and tag checks -> ownership logic | Relaxed crypto guardrails would create false ownership or timing leaks |
| receive state -> persisted claimed assets | A second persistence layer would drift storage truth and replay behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-16 | I | receive crypto checks | mitigate | Preserve constant-time tag comparison and downstream proof verification only |
| T-037-17 | T | persistence model | mitigate | Keep wallet-native claimed persistence as the current canonical target |
| T-037-18 | D | storage docs | mitigate | Reclassify Section 6.4 explicitly and quarantine future-unification ideas |

</threat_model>

<verification>
Cross-check the updated storage section in `037-ARCHITECTURE.md` against the actual persistence boundary in `wallet_service_actions_reachability.rs` and the restore seam.
</verification>

<success_criteria>
Receive guardrails and the storage target are explicit, aligned to live code, and no third persistence layer is implied.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-06-SUMMARY.md`.
</output>
