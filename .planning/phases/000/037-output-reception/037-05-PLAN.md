---
phase: 037-output-reception
plan: "05"
type: execute
wave: 5
depends_on:
  - "037-04"
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - crates/z00z_wallets/src/core/address/optimized_scanner.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "OptimizedScanner remains optional unless parity is fully proven."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - "crates/z00z_wallets/src/core/address/optimized_scanner.rs"
  key_links:
    - "OptimizedScanner -> canonical detector parity only"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Resolve the `OptimizedScanner` ambiguity without pulling the dependent gap-only test task ahead of the remaining decision gates.

Purpose: keep parallel scanning subordinate to the canonical detector and preserve the Phase 037 sequencing rule that Task 9 runs only after the later decision-gated tasks have settled.
Output: explicit optional-status documentation for `OptimizedScanner` plus one canonical parity anchor in `optimized_scanner.rs` for optional-wrapper checks.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
</execution_context>

<context>
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@crates/z00z_wallets/src/core/address/optimized_scanner.rs

<interfaces>
From optimized_scanner.rs:
```rust
pub fn new(keys: &ReceiverKeys, batch_size: usize) -> Self;
pub fn add_request(&mut self, request: &PaymentRequest);
pub fn scan_batch(&self, leaves: &[Asset]) -> Vec<ScanResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 8. Decide whether `OptimizedScanner` stays optional or becomes canonical</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/address/optimized_scanner.rs
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  </files>
  <action>Select the optional branch explicitly: keep `OptimizedScanner` as a batching wrapper over the same canonical detector, document it as non-canonical in `037-ARCHITECTURE.md`, and add or adjust parity tests only inside `optimized_scanner.rs` far enough to prove that optional batching does not create a second ownership or persistence implementation. Do not route the canonical receive lane through `OptimizedScanner` in this phase.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 8. Decide whether \`OptimizedScanner\` stays optional or becomes canonical"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>`OptimizedScanner` is explicitly documented and tested as optional wrapper behavior, not the canonical receive path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional batch wrapper -> canonical detector | Wrapper behavior must not become a silent second implementation authority |
| test backlog -> live tests | Coverage must expand only where real gaps remain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-13 | T | OptimizedScanner parity | mitigate | Keep the optional branch explicit and back it with parity-style tests only |
| T-037-14 | D | test suite scope | mitigate | Anchor Task 9 to the approved 037 test-planning package and forbid duplicate scenarios |
| T-037-15 | I | test evidence | mitigate | Prefer existing suites and add a dedicated phase integration test only for new scenarios |

</threat_model>

<verification>
Before closing the plan, confirm the optional-wrapper decision stays aligned with the `optimized_scanner.rs` parity anchor and that Task 9 remains deferred until the later decision-gated tasks complete.
</verification>

<success_criteria>
Parallel scanning remains optional and bounded, and the dependent gap-only test task is not pulled ahead of the later decision gates.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-05-SUMMARY.md`.
</output>
