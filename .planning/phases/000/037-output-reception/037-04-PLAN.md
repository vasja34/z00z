---
phase: 037-output-reception
plan: "04"
type: execute
wave: 4
depends_on:
  - "037-03"
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - crates/z00z_wallets/src/services/wallet_service_actions.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/core/chain/scan_engine.rs
  - crates/z00z_wallets/src/core/chain/scan_engine_impl.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Inbox hints, if added, remain metadata and never a second ownership authority."
    - "ScanEngineImpl is resolved explicitly instead of being left half-claimed."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - "crates/z00z_wallets/src/services/wallet_service_actions_receive.rs"
    - "crates/z00z_wallets/src/core/chain/scan_engine_impl.rs"
  key_links:
    - "inbox hint path -> recv_range(...)"
    - "ScanEngineImpl -> explicit de-scope or thin-delegate note, not ambiguous parity"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Resolve the Task 6 branch safely and resolve the ScanEngine seam explicitly.

Purpose: close two conditional seams without duplicating ownership detection, persistence, or scanner logic.
Output: either an explicitly deferred inbox-hint branch or a bounded inbox-hint module if the gates close, plus an explicit ScanEngineImpl outcome recorded in code and phase docs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
</execution_context>

<context>
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
@crates/z00z_wallets/src/core/chain/scan_engine.rs
@crates/z00z_wallets/src/core/chain/scan_engine_impl.rs

<interfaces>
From scan_engine.rs:
```rust
pub trait ScanEngine {
    fn scan(&mut self) -> ScanEngineResult<()>;
    fn scan_range(&mut self, start_height: u64, end_height: u64) -> ScanEngineResult<()>;
    fn get_progress(&self) -> (u64, u64);
    fn set_progress_callback(&mut self, callback: ScanEngineProgressCallback);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6. Implement inbox-assisted receive only at the service or adapter boundary</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/services/wallet_service_actions.rs
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/services/wallet_service_actions_receive_inbox.rs
    crates/z00z_wallets/src/services/test_wallet_service_suite.rs
  </files>
    <action>Only take the implementation branch if a concrete live inbox or hint source can be verified in the codebase and Task 15 has already closed deterministic ordered non-expired candidate selection. In that case, implement the inbox-assisted receive path as a new sibling service module named `wallet_service_actions_receive_inbox.rs`, wire it through the existing `wallet_service_actions.rs` include stack, and keep it request-bound metadata only. The module may ingest inbox hints and translate them into candidate-selection inputs for the existing `recv_range(...)` orchestration, but it must not create a second scan/detect/store pipeline, stable receiver registry, or ownership authority. If the implementation branch opens, add or extend service-boundary tests for hint hit, hint miss, false-positive hints, the guarantee that inbox hints never bypass canonical ownership detection, and proof that helper routing remains request-bound instead of depending on stable receiver identity. If no live hint source exists yet, keep inbox-assisted receive future-only in `037-ARCHITECTURE.md`, do not create a speculative receive-inbox module in Phase 037, and do not add Task 6 hint tests in this phase. If Task 15 later changes candidate-ordering internals after the implementation branch opens, rerun the same Task 6 proof as a regression check instead of treating it as a deferred requirement.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 6. Implement inbox-assisted receive only at the service or adapter boundary"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>A live hint source is either wired through the canonical `recv_range(...)` lane with tests proving hints remain notify-only metadata and helper routing stays request-bound, or the inbox-assisted branch is explicitly deferred without creating a speculative `wallet_service_actions_receive_inbox.rs` module. If Task 15 changes candidate-ordering internals later, that same Task 6 proof is rerun as regression coverage.</done>
</task>

<task type="auto">
  <name>Task 7. Resolve `ScanEngineImpl` by either de-scoping it or making it a thin delegate</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/chain/scan_engine.rs
    crates/z00z_wallets/src/core/chain/scan_engine_impl.rs
  </files>
  <action>Take the de-scope branch explicitly for Phase 037: keep `ScanEngineImpl` stub-only, remove parity-sounding documentation, and mark the scan engine surface proposed-only until a future phase can implement a thin delegate over the canonical receive lane without duplicating detection, rejection mapping, cursor persistence, or claimed persistence. Do not land a new scan engine implementation in this plan.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 7. Resolve \`ScanEngineImpl\` by either de-scoping it or making it a thin delegate"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>`ScanEngineImpl` no longer sits in a half-present state: Phase 037 docs and touched code treat it as explicitly de-scoped proposed-only functionality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbox hints -> canonical detector | Hints must not be elevated into ownership authority |
| scan-engine trait -> receive lane | Stub scan-engine docs must not imply live parity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-10 | S | inbox-assisted receive | mitigate | Keep hint plumbing in a sibling service module and route final confirmation through `recv_range(...)` only |
| T-037-11 | T | scan-engine docs | mitigate | Choose the de-scope branch explicitly and remove parity-sounding documentation |
| T-037-12 | E | receive orchestration | mitigate | Forbid a second scan/detect/store pipeline and require service-boundary tests to prove detector reuse |

</threat_model>

<verification>
Confirm Task 6 is either explicitly deferred without a speculative inbox module or, if the gates close later, the new inbox module composes into the service layer, and that `ScanEngineImpl` remains explicitly out of scope rather than ambiguously implied.
</verification>

<success_criteria>
Inbox-assisted receive is either explicitly deferred because no live source exists yet or exists only as request-bound metadata plumbing, and the scan-engine seam is resolved without duplicating the canonical detector.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-04-SUMMARY.md`.
</output>
