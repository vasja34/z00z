---
phase: 037-output-reception
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receiver.rs
  - crates/z00z_wallets/src/core/chain/scan_engine_impl.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 037 baseline is recorded as implemented, not greenfield."
    - "WalletService::recv_range(...) is the canonical receive lane."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - "crates/z00z_wallets/src/services/wallet_service_actions_receive.rs"
    - "crates/z00z_wallets/src/core/chain/scan_engine_impl.rs"
  key_links:
    - "037-TODO.md -> recv_range(...) -> live receiver keys -> StealthOutputScanner"
    - "Public single-asset receive remains compatibility-only while ScanEngineImpl stays non-parity"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Freeze the implemented Phase 037 baseline and make the canonical receive lane explicit before any conditional extensions are attempted.

Purpose: honor D-01 through D-08 by turning the live receive truth into the starting constraint for every later plan.
Output: a canonical Phase 037 receive architecture ledger plus rebased service comments that point to the live receive seams.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
@crates/z00z_wallets/src/services/wallet_service_actions_receiver.rs
@crates/z00z_wallets/src/core/chain/scan_engine_impl.rs

<interfaces>
From wallet_service_actions_receive.rs:
```rust
pub async fn scan_asset_report(
    &self,
    wallet_id: &PersistWalletId,
    asset: &Asset,
) -> Result<ReceiveReport, ReceiveReject>;

pub async fn recv_range(
    &self,
    wallet_id: &PersistWalletId,
    chunks: &[ScanChunk],
    requests: &[PaymentRequest],
    max_ckpt: Option<usize>,
) -> WalletResult<ScanRangeOut>;

pub fn receive_asset(
    &self,
    wallet_id: &PersistWalletId,
    asset_id: AssetId,
) -> RuntimeReceiveAssetResponse;
```

From wallet_service_actions_receiver.rs:
```rust
pub async fn receiver_keys(&self, wallet_id: &PersistWalletId) -> WalletResult<ReceiverKeys>;
pub(crate) async fn live_receiver_keys(
    &self,
    wallet_id: &PersistWalletId,
) -> WalletResult<ReceiverKeys>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0. Freeze the implemented Phase 037 baseline before extending it</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/services/wallet_service_actions_receiver.rs
  </files>
  <action>Create `.planning/phases/037-output-reception/037-ARCHITECTURE.md` as the canonical receive-document ledger for Phase 037 and record the implemented baseline from Task 0 exactly as live code truth: `recv_range(...)`, `StealthOutputScanner`, `recv_route(...)`, wallet-native claimed persistence, `ScanStatePayload`, request-aware helpers, and `receiver_keys(...)`. Rebase any misleading service-level comments in the touched Rust files so they reinforce the implemented baseline instead of reopening it as greenfield work. Keep the wording aligned to D-01, D-02, D-03, D-06, D-07, and D-08.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 0. Freeze the implemented Phase 037 baseline before extending it"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>`037-ARCHITECTURE.md` exists, explicitly marks the Task 0 baseline as implemented, and no touched comment or phase doc language reopens those seams as missing work.</done>
</task>

<task type="auto">
  <name>Task 1. Keep `WalletService::recv_range(...)` as the canonical receive path</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/core/chain/scan_engine_impl.rs
  </files>
  <action>Use the new architecture ledger plus the touched Rust docs to make `WalletService::recv_range(...)` the canonical implemented request-aware receive lane. Mark `WalletService::receive_asset(...)` and outward `wallet.asset.receive_asset` as compatibility-only single-asset lanes, and record that `ScanEngineImpl` remains non-parity because it still returns not-implemented errors. Do not implement `ScanEngineImpl` in this plan; only remove ambiguity and overclaiming.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 1. Keep \`WalletService::recv_range(...)\` as the canonical receive path"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>The Phase 037 receive documentation and touched Rust docs name `recv_range(...)` as canonical and do not claim current `ScanEngineImpl` parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase docs -> executor behavior | Incorrect documentation would cause later plans to mutate the wrong receive seam |
| service docs -> public API expectations | Overclaiming parity could misroute implementation onto compatibility lanes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-01 | T | 037-ARCHITECTURE.md | mitigate | Freeze the implemented baseline in one ledger and cross-link it to live files only |
| T-037-02 | I | wallet_service_actions_receive.rs docs | mitigate | Mark `receive_asset(...)` compatibility-only and keep `recv_range(...)` as the sole canonical lane |
| T-037-03 | E | scan_engine_impl.rs docs | mitigate | Record stub-only status explicitly so no consumer treats ScanEngineImpl as already authorized parity |

</threat_model>

<verification>
Re-read `037-ARCHITECTURE.md`, `wallet_service_actions_receive.rs`, and `scan_engine_impl.rs` together and confirm they all tell the same canonical-lane story.
</verification>

<success_criteria>
Task 0 and Task 1 are closed without introducing new receive architecture, and every later plan can treat the canonical baseline as frozen.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-01-SUMMARY.md`.
</output>
