---
phase: 037-output-reception
plan: "03"
type: execute
wave: 3
depends_on:
  - "037-02"
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/services/wallet_service_store_support.rs
  - crates/z00z_wallets/src/services/test_wallet_service_suite.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Receive persistence remains an explicit gate."
    - "Phase 037 documentation resolves to live receive files only."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - "crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs"
    - "crates/z00z_wallets/src/services/test_wallet_service_suite.rs"
  key_links:
    - "recv_range(...) -> recv_route(...) -> claimed persistence"
    - "Phase docs -> live module paths only"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Lock the explicit persistence gate and rebase Phase 037 architecture prose onto the live receive module tree.

Purpose: ensure every later receive extension still flows through the existing persist/no-persist split and one live documentation surface.
Output: stable `ReceiveNext::PersistClaim` contract evidence plus a fully rebased Phase 037 architecture ledger.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
@crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
@crates/z00z_wallets/src/services/wallet_service_store_support.rs
@crates/z00z_wallets/src/services/test_wallet_service_suite.rs

<interfaces>
From wallet_service_actions_reachability.rs:
```rust
pub async fn recv_route(
    &self,
    wallet_id: &PersistWalletId,
    asset: Asset,
    next: ReceiveNext,
) -> WalletResult<bool>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4. Preserve explicit `ReceiveNext::PersistClaim` gating</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/services/wallet_service_store_support.rs
    crates/z00z_wallets/src/services/test_wallet_service_suite.rs
  </files>
  <action>Keep `recv_route(...)` as the frozen receive-to-persist gate and make that explicit in docs, code comments, and service-level regression coverage. Preserve `ReportOnly` versus `PersistClaim`, preserve the existing compatibility scrub at `recv_claim_asset(...)`, and ensure any touched receive flow still routes persistence through the same gate instead of mutating claimed storage directly.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 4. Preserve explicit \`ReceiveNext::PersistClaim\` gating"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>Detection-only paths remain non-mutating, persistence stays behind `recv_route(...)`, and the compatibility scrub rule is still documented and covered.</done>
</task>

<task type="auto">
  <name>Task 5. Re-baseline Phase 037 architecture documentation to live code</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/core/address/stealth_scanner.rs
    crates/z00z_wallets/src/core/chain/scan_engine_impl.rs
  </files>
  <action>Use `037-ARCHITECTURE.md` as the single Phase 037 receive document and rebase all module-path tables, flow descriptions, and ownership notes onto the live code tree: `WalletService::recv_range(...)`, `core::address::leaf_scan`, and `core::address::StealthOutputScanner`. Mark trait-based scanner variants as superseded or future-only unless code is added later in the phase.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 5. Re-baseline Phase 037 architecture documentation to live code"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>The Phase 037 architecture document no longer presents stale module trees as current implementation, and every named implementation path resolves to a live file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detection result -> persistence | Persisting detection results without the gate would corrupt claimed state semantics |
| architecture doc -> later plans | Stale module maps would send later work into the wrong files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-07 | T | recv_route gating | mitigate | Preserve the explicit `ReceiveNext::PersistClaim` gate and back it with service-boundary tests |
| T-037-08 | I | 037-ARCHITECTURE.md | mitigate | Remove stale trait-stack claims and keep only live file-path references |
| T-037-09 | E | claimed persistence boundary | mitigate | Preserve compatibility scrub behavior and forbid direct claimed-state writes from detection-only paths |

</threat_model>

<verification>
Re-read the updated gate tests together with `037-ARCHITECTURE.md` and confirm the same `ReportOnly` versus `PersistClaim` contract is described everywhere.
</verification>

<success_criteria>
The persistence boundary is explicit, tested, and documented, and the phase architecture narrative matches the live code tree.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-03-SUMMARY.md`.
</output>
