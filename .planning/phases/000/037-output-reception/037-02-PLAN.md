---
phase: 037-output-reception
plan: "02"
type: execute
wave: 2
depends_on:
  - "037-01"
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - crates/z00z_wallets/src/core/address/stealth_scanner.rs
  - crates/z00z_wallets/src/core/address/stealth_scanner/types.rs
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Strict tag-only receive requires concrete Tag16Context registration."
    - "Ownership detection stays separate from downstream proof validation."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - "crates/z00z_wallets/src/core/address/stealth_scanner.rs"
    - "crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs"
  key_links:
    - "add_request(...) does not by itself authorize strict tag-only ownership claims"
    - "receive detection -> downstream validation boundary remains explicit"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Materialize the request-bound tag-context contract and keep the canonical detector focused on ownership classification rather than proof verification.

Purpose: close the first live detector drift items without widening the detector into a second validation engine.
Output: explicit Tag16Context contract, negative coverage for context-free strict tag scans, and receive docs that separate detection from proof verification.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@crates/z00z_wallets/src/core/address/stealth_scanner.rs
@crates/z00z_wallets/src/core/stealth/output.rs

<interfaces>
From types_tag_cache.rs:
```rust
pub struct Tag16Context {
    pub k_dh: [u8; 32],
    pub req_id: Option<[u8; 32]>,
}

pub fn add_request(&mut self, request: &PaymentRequest);
```

From stealth_scan_support.rs:
```rust
fn scan_dh(
    owner_handle: &[u8; 32],
    input: &ScanInput<'_>,
    dh: &[u8; 32],
    req_ids: &[[u8; 32]],
) -> DetectState;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2. Materialize request-bound `Tag16Context` explicitly</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/address/stealth_scanner.rs
    crates/z00z_wallets/src/core/address/stealth_scanner/types.rs
    crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
  </files>
  <action>Make the strict tag-only contract explicit in code comments, phase docs, and regression coverage: `add_request(...)` remains request-liveness metadata only, while strict tag-only receive stays gated on concrete `add_tag_context(...)` registration. Strengthen or extend the existing prefilter regression coverage so future tag-only entry points must reject request-bound flows that lack `Tag16Context` values.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 2. Materialize request-bound \`Tag16Context\` explicitly"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>Strict tag-only receive is documented and tested as requiring explicit tag-context registration, and `add_request(...)` alone is no longer ambiguous.</done>
</task>

<task type="auto">
  <name>Task 3. Keep proof verification downstream of ownership detection</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/address/stealth_scanner.rs
    crates/z00z_wallets/src/core/stealth/output.rs
  </files>
  <action>Rebase code comments and Phase 037 documentation so `scan_report(...)`, `scan_leaf(...)`, and range scanning remain ownership-detection plus receive-classification surfaces only. Do not add inline range-proof verification. Route any proof-verification language or future logic to downstream import, tx-validation, or explicit verification boundaries instead.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 3. Keep proof verification downstream of ownership detection"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>No touched receive-path code verifies range proofs inline, and the docs clearly separate ownership detection from downstream proof validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tag prefilter -> ownership claim | Prefilter metadata must not become proof of ownership |
| detector -> verifier | Detection must not silently absorb downstream proof validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-04 | S | Tag16 strict path | mitigate | Require explicit `Tag16Context` before strict tag-only paths are allowed to claim ownership |
| T-037-05 | T | detector boundary | mitigate | Keep proof validation downstream and document `Detected` as ownership only |
| T-037-06 | D | prefilter tests | mitigate | Extend prefilter tests so regressions fail fast if contextless strict tag-only claims reappear |

</threat_model>

<verification>
Confirm the prefilter regression suite and the updated docs tell the same story about `Tag16Context` and downstream proof boundaries.
</verification>

<success_criteria>
The request-bound tag-context contract and the detector-vs-verifier split are both explicit, tested, and aligned in Phase 037 docs.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-02-SUMMARY.md`.
</output>
