---
phase: 037-output-reception
plan: "08"
type: execute
wave: 8
depends_on:
  - "037-07"
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - crates/z00z_wallets/src/core/address/stealth_scanner/types_tag_cache.rs
  - crates/z00z_wallets/src/core/address/stealth_scan_support.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/core/address/test_stealth_scan_support_suite.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Historical ECC ideas are translated into live repository terms only."
    - "Request-aware candidate iteration is deterministic and expiry-aware."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - "crates/z00z_wallets/src/core/address/stealth_scanner/types_tag_cache.rs"
    - "crates/z00z_wallets/src/core/address/test_stealth_scan_support_suite.rs"
  key_links:
    - "historical receive ideas -> live file and type names only"
    - "active request cache -> ordered non-expired candidates -> fallback last"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Translate retained historical receive ideas into live terms and remove the hidden nondeterminism from request-candidate iteration.

Purpose: eliminate stale naming drift and make the request-bound scan path predictable, reviewable, and testable.
Output: sanitized Phase 037 terminology plus ordered, expiry-aware request candidate iteration with focused regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
</execution_context>

<context>
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@crates/z00z_wallets/src/core/address/stealth_scanner/types_tag_cache.rs
@crates/z00z_wallets/src/core/address/stealth_scan_support.rs
@crates/z00z_wallets/src/services/wallet_service_actions_receive.rs

<interfaces>
From types_tag_cache.rs:
```rust
pub fn add_request(&mut self, request: &PaymentRequest);
pub(crate) fn active_requests(&self) -> impl Iterator<Item = &[u8; 32]>;
```

From stealth_scan_support.rs:
```rust
fn scan_dh(
    owner_handle: &[u8; 32],
    input: &ScanInput<'_>,
    dh: &[u8; 32],
    req_ids: &[[u8; 32]],
) -> DetectState;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 14. Translate surviving ECC ideas into live surfaces and quarantine stale names</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  </files>
  <action>Walk the remaining historical receive ideas and keep only the pieces that translate cleanly into the live repository surfaces. Rewrite the Phase 037 architecture ledger so DA, inbox, and request-bound helper concepts are expressed in current checkpoint/request/router terminology only. Explicitly quarantine stale names such as `Receiver`, `ReceptionResult`, `receiver::scanner`, `receiver::storage`, `OutputScanner`, `FullScanner`, `HybridScanner`, `ScanConfig`, and `DoSMitigationConfig` unless later plans add code for them.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 14. Translate surviving ECC ideas into live surfaces and quarantine stale names"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>Every retained historical idea is described in live repository terms, and stale ECC names are removed or explicitly marked proposed-only.</done>
</task>

<task type="auto">
  <name>Task 15. Make request-candidate ordering deterministic and expiry-aware</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/address/stealth_scanner/types_tag_cache.rs
    crates/z00z_wallets/src/core/address/stealth_scan_support.rs
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/core/address/test_stealth_scan_support_suite.rs
  </files>
  <action>Make request-candidate policy deterministic at the request-aware orchestration boundary, not by treating `Tag16Cache` membership as a complete request source. Build the ordered candidate list from full `PaymentRequest` records carrying at least `req_id` and expiry semantics, or prune expired requests before `req_id` registration if the existing cache remains id-only. Do not claim expiry enforcement from `HashSet` reordering alone. Preserve current low-level detector semantics unless an explicit parity proof justifies changing them, keep the `req_id = None` fallback explicit and last at the ordered-candidate-policy level, and terminate on the first successful candidate. Update the architecture ledger to describe the implemented ordering policy, and add regression tests for deterministic iteration order, expiry pruning, fallback-last behavior, and first-success short-circuiting.</action>
  <verify>
    <automated>Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump`. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 15. Make request-candidate ordering deterministic and expiry-aware"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>The request-aware scan path no longer depends on `HashSet` iteration order, expired requests are pruned before scanning, the fallback candidate is last, and the docs describe the same policy the code enforces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical design language -> live implementation | Stale names can reintroduce false architectural claims |
| active request cache -> request candidate selection | Unordered iteration can create irreproducible request-priority and expiry handling in request-aware scans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-22 | T | phase terminology | mitigate | Translate every retained idea into live terms and quarantine stale names explicitly |
| T-037-23 | D | request candidate iteration | mitigate | Introduce stable ordered request policy, explicit expiry handling, fallback-last semantics, and first-success tests without promoting cache membership into a full request authority |
| T-037-24 | R | receive docs | mitigate | Rebase the documented ordering policy onto the exact implemented code path |

</threat_model>

<verification>
Read the updated ordering tests next to the cache and scan-support code, then confirm `037-ARCHITECTURE.md` describes the same ordered candidate policy.
</verification>

<success_criteria>
Phase 037 terminology is live-code accurate and the request-aware candidate policy is deterministic, expiry-aware, reproducible, and regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-08-SUMMARY.md`.
</output>
