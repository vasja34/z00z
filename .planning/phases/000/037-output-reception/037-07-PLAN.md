---
phase: 037-output-reception
plan: "07"
type: execute
wave: 7
depends_on:
  - "037-06"
files_modified:
  - .planning/phases/037-output-reception/037-ARCHITECTURE.md
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_asset_impl_suite.rs
  - crates/z00z_wallets/src/core/address/optimized_scanner.rs
  - crates/z00z_wallets/src/core/address/stealth_scanner/types_range.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 037 does not invent a second reception API stack."
    - "Scanner configuration language matches live knobs only."
  artifacts:
    - ".planning/phases/037-output-reception/037-ARCHITECTURE.md"
    - "crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs"
    - "crates/z00z_wallets/src/core/address/stealth_scanner/types_range.rs"
  key_links:
    - "thin-facade ideas -> live service and RPC seams only"
    - "DoS/scanner config docs -> implemented knobs, not proposed stacks"
---
<!-- markdownlint-disable MD033 MD041 MD022 MD031 MD040 MD060 -->

<objective>
Resolve the reception-API and scanner-config drift by rebasing both onto the live service and scanner seams.

Purpose: prevent Phase 037 from describing an orchestration or configuration stack that the repository does not actually implement.
Output: one receive API story rooted in live service/RPC boundaries and one scanner-configuration story rooted in real implemented knobs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
</execution_context>

<context>
@.planning/phases/037-output-reception/037-CONTEXT.md
@.planning/phases/037-output-reception/037-TODO.md
@crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
@crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
@crates/z00z_wallets/src/core/address/optimized_scanner.rs
</context>

<tasks>

<task type="auto">
  <name>Task 12. Resolve the Section 6.5 reception-API drift with a thin facade or documentation rebase</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
    crates/z00z_wallets/src/services/wallet_service_actions_reachability.rs
    crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server.rs
    crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
    crates/z00z_wallets/src/adapters/rpc/methods/test_asset_impl_suite.rs
  </files>
  <action>Select the documentation-rebase branch. Keep `Receiver`, `ReceptionConfig`, `ReceptionResult`, and callback/event API proposed-only in `037-ARCHITECTURE.md`; rebase the receive API story onto the live `recv_range(...)`, `scan_asset_report(...)`, outward `wallet.asset.receive_asset`, and claimed-asset query seams; and avoid implementing a second scan/detect/store orchestration stack in this phase.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 12. Resolve the Section 6.5 reception-API drift with a thin facade or documentation rebase"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>The phase docs no longer describe a second orchestration API as if it already exists, and all live receive API statements map to real service/RPC seams.</done>
</task>

<task type="auto">
  <name>Task 13. Resolve the scanner-config and DoS-policy drift against the live scanner surface</name>
  <files>
    .planning/phases/037-output-reception/037-ARCHITECTURE.md
    crates/z00z_wallets/src/core/address/optimized_scanner.rs
    crates/z00z_wallets/src/core/address/stealth_scanner/types_range.rs
    crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  </files>
  <action>Select the live-knobs branch. Rebase Phase 037 documentation onto the current scanner knobs (`max_ckpt`, `DoSMitigation`, explicit request registration, explicit `Tag16Context` materialization for any strict tag-only request-bound path, and existing background strategy hooks), keep richer `ScanConfig` or `DoSMitigationConfig` vocabulary future-only, and document tag16 optimization, inbox hints, and parallel scanning as strategy inputs over the same canonical detector. Make the request/tag invariant explicit: `add_request(...)` stays metadata/liveness only, while strict tag-only request-bound scanning requires concrete `Tag16Context` values.</action>
  <verify>
    <automated>If this task changes Rust or tests, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the issues and rerun it before broader validation. Then run `cargo test --release --features test-fast --features wallet_debug_dump` when Rust or test files changed. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/037-output-reception/037-TODO.md current_task="Task 13. Resolve the scanner-config and DoS-policy drift against the live scanner surface"` in YOLO mode at least 3 times, and stop only after at least 2 consecutive runs report no significant issues. For docs-only deltas, confirm markdown diagnostics are clean and every referenced file path resolves.</automated>
  </verify>
  <done>Phase 037 names one explicit DoS/scanner surface and no longer presents unimplemented configuration stacks as current behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase docs -> API consumers | Overstated API/facade claims would mislead later implementation and review |
| config docs -> scanner behavior | Unimplemented config types would create false operator expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-19 | T | reception API docs | mitigate | Rebase the API narrative onto live service and RPC seams only |
| T-037-20 | I | scanner-config docs | mitigate | Keep only implemented knobs as current behavior and mark richer configs future-only |
| T-037-21 | D | receive orchestration | mitigate | Forbid a second orchestration stack and preserve the single canonical receive lane |

</threat_model>

<verification>
Re-read the updated API and config sections in `037-ARCHITECTURE.md` next to the touched service/RPC/scanner files and confirm every described knob or facade exists today.
</verification>

<success_criteria>
Phase 037 presents one real receive API story and one real scanner-config story, both grounded in the live codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/037-output-reception/037-07-SUMMARY.md`.
</output>
