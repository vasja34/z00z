<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 025-crypto-audit-crypto
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/src/domains.rs
  - crates/z00z_crypto/src/kdf.rs
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_crypto/src/stealth_bind.rs
  - crates/z00z_crypto/tests/test_stealth_bind_vectors.rs
  - crates/z00z_crypto/tests/test_spec_golden_vectors.rs
  - crates/z00z_crypto/tests/test_hash_policy.rs
  - crates/z00z_crypto/tests/domain_separation_tests.rs
  - crates/z00z_wallets/src/core/stealth/tag.rs
  - crates/z00z_wallets/src/core/address/stealth_scan_support.rs
  - crates/z00z_wallets/src/core/tx/builder.rs
  - crates/z00z_wallets/src/core/tx/output_flow.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_prefilter.rs
  - crates/z00z_wallets/tests/test_stealth_scanner_flow.rs
autonomous: true
requirements:
  - PH25-STEALTH-BIND
must_haves:
  truths:
    - `tag16` and `leaf_ad` formulas are owned by `z00z_crypto` rather than duplicated or wallet-local.
    - `range_ctx_hash` exists as an explicit crypto binding contract for later claim or tx digests.
    - Frozen vectors guard the canonical stealth and binding formulas against silent drift.
    - Existing scan-prefilter behavior remains covered after the ownership move, rather than assuming vector tests alone are sufficient.
    - DST and Poseidon-sensitive framing gets frozen enough that this phase does not imply circuit compatibility without evidence.
  artifacts:
    - path: crates/z00z_crypto/src/stealth_bind.rs
      provides: One crypto-owned `tag16`, `leaf_ad`, and `range_ctx_hash` API surface.
    - path: crates/z00z_crypto/tests/test_stealth_bind_vectors.rs
      provides: Frozen vectors for stealth and range-binding formulas.
    - path: crates/z00z_wallets/src/core/stealth/tag.rs
      provides: Consumer migration away from wallet-owned canonical formulas.
  key_links:
    - from: crates/z00z_crypto/src/stealth_bind.rs
      to: crates/z00z_wallets/src/core/address/stealth_scan_support.rs
      via: shared `tag16` and `leaf_ad` formulas
      pattern: compute_tag16|compute_leaf_ad
    - from: crates/z00z_crypto/src/claim/v2.rs
      to: crates/z00z_crypto/src/stealth_bind.rs
      via: `range_ctx_hash` inclusion in canonical statement framing
      pattern: range_ctx_hash
---

<objective>
Promote crypto-owned stealth or range binding APIs and freeze their vectors.

Purpose: stop drift between wallet-local stealth formulas and crypto-layer domain separation, and add one explicit binding contract for future claim or tx digest work.
Output: one crypto-owned stealth-binding module, wallet consumer migration, and frozen vector tests for `tag16`, `leaf_ad`, and `range_ctx_hash`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/025-crypto-audit-crypto/025-CONTEXT.md
@.planning/phases/025-crypto-audit-crypto/025-FUSION.md
@crates/z00z_crypto/src/domains.rs
@crates/z00z_crypto/src/kdf.rs
@crates/z00z_wallets/src/core/stealth/tag.rs
@crates/z00z_wallets/src/core/address/stealth_scan_support.rs
@crates/z00z_wallets/src/core/tx/builder.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move canonical `tag16` and `leaf_ad` ownership into `z00z_crypto`</name>
  <files>crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/src/domains.rs, crates/z00z_crypto/src/kdf.rs, crates/z00z_crypto/src/stealth_bind.rs, crates/z00z_crypto/tests/test_stealth_bind_vectors.rs, crates/z00z_wallets/src/core/stealth/tag.rs, crates/z00z_wallets/src/core/address/stealth_scan_support.rs</files>
  <read_first>crates/z00z_wallets/src/core/stealth/tag.rs, crates/z00z_wallets/src/core/address/stealth_scan_support.rs, crates/z00z_crypto/src/domains.rs, crates/z00z_crypto/src/kdf.rs, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - Crypto exports the canonical `compute_tag16` and `compute_leaf_ad` formulas.
    - Wallet code consumes the crypto-owned API instead of defining the canonical formulas locally.
    - Frozen vectors prove the formulas did not change during the ownership move.
    - Existing wallet prefilter and scan-path tests stay green after the ownership move.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_crypto/src/stealth_bind.rs` contains `pub fn compute_tag16`.
    - `crates/z00z_crypto/src/stealth_bind.rs` contains `pub fn compute_leaf_ad`.
    - `crates/z00z_wallets/src/core/stealth/tag.rs` no longer contains the canonical formula body for both functions.
    - `crates/z00z_crypto/tests/test_stealth_bind_vectors.rs` contains vector checks for `tag16` and `leaf_ad`.
    - Existing wallet scan tests such as `test_stealth_scanner_prefilter.rs` or `test_stealth_scanner_flow.rs` remain the acceptance guard for prefilter behavior.
  </acceptance_criteria>
  <action>Implement D08, D10, D13, and D15 from `025-FUSION.md` by creating one crypto-owned stealth binding module, for example `stealth_bind.rs`, and re-export it through the root facade. Preserve the existing wallet formulas exactly: `compute_tag16(k_dh, leaf_ad)` using `Z00Z/TAG16`, and `compute_leaf_ad(asset_id, serial_id, r_pub, owner_tag, c_amount)` using the current 132-byte preimage discipline. Then migrate `z00z_wallets` scan and tx consumer code to call the crypto-owned helpers instead of keeping canonical formulas under wallet ownership. Add frozen vectors to prove the formulas did not drift, and keep existing scan-prefilter tests green so the move also preserves live scan semantics.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --features test-fast --test test_stealth_bind_vectors -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump --test test_kdf -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump --test test_stealth_scanner_prefilter -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>One crypto-owned API now defines `tag16` and `leaf_ad`, wallet consumers call it, and frozen vectors prove exact formula preservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `range_ctx_hash` and freeze binding vectors for later `claim_v2` use</name>
  <files>crates/z00z_crypto/src/claim/v2.rs, crates/z00z_crypto/src/stealth_bind.rs, crates/z00z_crypto/tests/test_stealth_bind_vectors.rs, crates/z00z_crypto/tests/test_spec_golden_vectors.rs, crates/z00z_wallets/src/core/tx/builder.rs, crates/z00z_wallets/src/core/tx/output_flow.rs</files>
  <read_first>crates/z00z_crypto/src/claim/v2.rs, crates/z00z_wallets/src/core/tx/builder.rs, crates/z00z_wallets/src/core/tx/output_flow.rs, crates/z00z_crypto/tests/test_spec_golden_vectors.rs, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - A dedicated `range_ctx_hash` helper exists before `claim_v2` migration work lands.
    - Vectors prove `range_ctx_hash` changes when any bound context field changes.
    - Wallet tx code has a stable import path for the new helper, even if full claim wiring lands later.
    - Domain-sensitive framing for the new helper is frozen well enough that later phases cannot silently drift labels or DST bytes.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_crypto/src/stealth_bind.rs` contains `pub fn range_ctx_hash`.
    - `crates/z00z_crypto/tests/test_stealth_bind_vectors.rs` contains `range_ctx_hash` vector coverage.
    - `crates/z00z_crypto/tests/test_spec_golden_vectors.rs`, `test_hash_policy.rs`, or `domain_separation_tests.rs` freezes the relevant DST or domain-sensitive framing for the new helper.
    - `crates/z00z_wallets/src/core/tx/builder.rs`, `crates/z00z_wallets/src/core/tx/output_flow.rs`, or the adjacent live claim digest path references `range_ctx_hash`.
  </acceptance_criteria>
  <action>Add one explicit `range_ctx_hash` helper that binds `asset_id`, `chain_id`, root or proof versions, policy version, commitment bytes, and proof bytes into one 32-byte digest ready for `claim_v2` and tx-digest framing. Export it from the new crypto-owned binding module, add vectors that mutate one field at a time and prove the hash changes, and thread the import path into one live wallet tx or claim digest path so later claim migration does not need to rediscover the helper location. Freeze the associated domain or DST framing in existing hash-policy or domain-separation tests. If circuit-side Poseidon compatibility still cannot be proven in this phase, record that remaining evidence gap explicitly instead of claiming native-to-circuit equivalence.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --features test-fast --test test_spec_golden_vectors -- --nocapture
cargo test -p z00z_crypto --release --features test-fast --test test_hash_policy -- --nocapture
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump --test test_golden_vectors -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>`range_ctx_hash` exists, frozen vectors guard it, and downstream claim or tx migration has one stable crypto import path for the binding helper.</done>
</task>

</tasks>

<verification>
Verify that wallet-local stealth formulas no longer define the canonical behavior, that `range_ctx_hash` is a crypto-owned helper with frozen vectors, and that release validation still starts with the bootstrap gate.
</verification>

<success_criteria>
`tag16`, `leaf_ad`, and `range_ctx_hash` are owned by `z00z_crypto`, backed by vectors, and available for live wallet or simulator migration without duplicated formulas.
</success_criteria>

<output>
After completion, create `.planning/phases/025-crypto-audit-crypto/025-03-SUMMARY.md`
</output>