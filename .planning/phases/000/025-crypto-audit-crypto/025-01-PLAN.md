<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 025-crypto-audit-crypto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_crypto/src/claim/mod.rs
  - crates/z00z_crypto/src/claim/proof.rs
  - crates/z00z_crypto/src/claim/statement.rs
  - crates/z00z_crypto/src/claim/verifier.rs
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_storage/src/assets/types.rs
  - crates/z00z_storage/src/assets/proof.rs
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_storage/src/checkpoint/artifact.rs
  - crates/z00z_storage/src/checkpoint/build.rs
  - crates/z00z_core/src/assets/snapshot.rs
  - crates/z00z_crypto/tests/test_claim_v2_contract.rs
  - crates/z00z_storage/tests/test_claim_source_proof.rs
autonomous: true
requirements:
  - PH25-CLAIM-V2
  - PH25-SOURCE-PROOF
must_haves:
  truths:
    - `claim_v2` has explicit versioned contracts for canonical claim statements, authority signatures, and source proofs before wallet or simulator migration starts.
    - Storage owns authoritative claim-source roots and inclusion-proof APIs instead of letting wallets or simulator code invent root semantics.
    - The repository has one typed error surface for unsupported claim proof or root versions rather than implicit placeholder semantics.
    - `025-01` must not expand default production reachability of `claim_v1`; any interim compatibility needed before consumer cutover stays explicit and is staged for final facade cleanup in `025-05`.
  artifacts:
    - path: crates/z00z_crypto/src/claim/v2.rs
      provides: Typed `claim_v2` contracts and verification helpers.
    - path: crates/z00z_storage/src/assets/types.rs
      provides: Typed authoritative root contracts for claim-source verification.
    - path: crates/z00z_storage/src/assets/store.rs
      provides: Storage-owned claim-source root and proof retrieval API seam.
  key_links:
    - from: crates/z00z_crypto/src/claim/v2.rs
      to: crates/z00z_storage/src/assets/types.rs
      via: shared `ClaimSourceProof` or root-version contract names
      pattern: ClaimSourceProof|ClaimSourceRoot|ClaimRootVer
    - from: crates/z00z_storage/src/checkpoint/artifact.rs
      to: crates/z00z_core/src/assets/snapshot.rs
      via: authoritative root publication metadata
      pattern: claim_source_root|claim_root_ver|claim_auth
---

<objective>
Define `claim_v2` and authoritative source-proof contracts before any consumer migration.

Purpose: prevent wallet, simulator, and storage work from inventing incompatible claim-root or signature interfaces during remediation.
Output: typed `claim_v2` contracts in `z00z_crypto`, storage-owned authoritative root or source-proof contracts, and focused regression tests proving the new interfaces compile and serialize.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/025-crypto-audit-crypto/025-CONTEXT.md
@.planning/phases/025-crypto-audit-crypto/025-FUSION.md
@crates/z00z_crypto/src/claim/mod.rs
@crates/z00z_crypto/src/claim/proof.rs
@crates/z00z_crypto/src/claim/statement.rs
@crates/z00z_storage/src/assets/types.rs
@crates/z00z_storage/src/assets/proof.rs
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/checkpoint/artifact.rs
@crates/z00z_core/src/assets/snapshot.rs

<interfaces>
`claim_v2` contract target:

```rust
pub struct ClaimStmtV2 {
    pub chain_id: u32,
    pub root_ver: u16,
    pub source_proof_ver: u16,
    pub tx_ver: u16,
    pub range_ctx_hash: [u8; 32],
}

pub struct ClaimAuthoritySigV2 {
    pub sig_bytes: Vec<u8>,
}

pub enum ClaimSourceProof {
    MerkleInclusionV1 { root: [u8; 32], proof: Vec<u8> },
    GenesisAllocV1 { root: [u8; 32], record: Vec<u8> },
}
```

Storage seam target:

```rust
pub trait ClaimSourceApi {
    fn claim_source_root(&self) -> ClaimSourceRoot;
    fn claim_source_proof(&self, path: &AssetPath) -> Result<ClaimSourceProof, AssetStoreError>;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `claim_v2` crypto contracts and canonical statement framing</name>
  <files>crates/z00z_crypto/src/claim/mod.rs, crates/z00z_crypto/src/claim/proof.rs, crates/z00z_crypto/src/claim/statement.rs, crates/z00z_crypto/src/claim/verifier.rs, crates/z00z_crypto/src/claim/v2.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/tests/test_claim_v2_contract.rs</files>
  <read_first>crates/z00z_crypto/src/claim/mod.rs, crates/z00z_crypto/src/claim/proof.rs, crates/z00z_crypto/src/claim/statement.rs, crates/z00z_crypto/src/claim/verifier.rs, crates/z00z_crypto/src/lib.rs, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - `claim_v2` defines a canonical statement shape that includes `chain_id`, root version, source-proof version, tx version, and `range_ctx_hash`.
    - `ClaimAuthoritySigV2` is defined as a real signature container shape, not as a digest-derived placeholder.
    - Unsupported proof or root versions fail through typed errors rather than implicit fallbacks.
    - Canonical statement framing is frozen early enough that later wallet and simulator migration does not rediscover message bytes or signature scope.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_crypto/src/claim/v2.rs` contains `pub struct ClaimStmtV2`.
    - `crates/z00z_crypto/src/claim/v2.rs` contains `pub struct ClaimAuthoritySigV2`.
    - `crates/z00z_crypto/src/claim/v2.rs` contains `pub enum ClaimSourceProof`.
    - `crates/z00z_crypto/src/claim/v2.rs` contains `pub enum ClaimV2Err`.
    - `crates/z00z_crypto/src/lib.rs` re-exports `ClaimStmtV2` or `ClaimAuthoritySigV2`.
    - `crates/z00z_crypto/tests/test_claim_v2_contract.rs` proves canonical framing roundtrip plus version-rejection behavior for the new types.
  </acceptance_criteria>
  <action>Implement the contract layer described in D14.1 through D14.3 of `025-FUSION.md`. Add a new `crates/z00z_crypto/src/claim/v2.rs` module and wire it from `claim/mod.rs` plus the root facade. Define `ClaimStmtV2` with canonical bytes that cover `chain_id`, `root_ver`, `source_proof_ver`, `tx_ver`, `range_ctx_hash`, `claim_id`, source asset id, source commitment, recipient binding, nullifier, owner-bind digest, and output leaf hashes. Define `ClaimAuthoritySigV2` as the canonical container for Tari-backed Schnorr bytes, plus `ClaimSourceProof`, `ClaimRootVer`, and `ClaimProofVer` enums or newtypes. Add explicit error variants for unsupported claim version, unsupported root version, unsupported source-proof version, malformed bytes, and mismatched `range_ctx_hash`. Freeze at least one canonical framing vector in `test_claim_v2_contract.rs` so later phases cannot reinterpret signed bytes. Keep `claim_v1` compiling only as compatibility scaffolding needed for staged migration, but do not extend it further and do not add new default-facade re-exports for it.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --features test-fast
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>`claim_v2` contracts exist in `z00z_crypto`, compile under the release crypto gate after the bootstrap gate, and expose explicit version or serialization errors rather than placeholder semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add storage-owned authoritative root and source-proof interfaces</name>
  <files>crates/z00z_storage/src/assets/types.rs, crates/z00z_storage/src/assets/proof.rs, crates/z00z_storage/src/assets/store.rs, crates/z00z_storage/src/checkpoint/artifact.rs, crates/z00z_storage/src/checkpoint/build.rs, crates/z00z_core/src/assets/snapshot.rs, crates/z00z_storage/tests/test_claim_source_proof.rs</files>
  <read_first>crates/z00z_storage/src/assets/types.rs, crates/z00z_storage/src/assets/proof.rs, crates/z00z_storage/src/assets/store.rs, crates/z00z_storage/src/checkpoint/artifact.rs, crates/z00z_storage/src/checkpoint/build.rs, crates/z00z_core/src/assets/snapshot.rs, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - Storage exposes an authoritative claim-source root with version metadata.
    - Storage exposes a typed source-proof retrieval API that can feed `ClaimSourceProof` consumers.
    - Checkpoint or snapshot artifacts can publish the trusted claim-source root or authority metadata downstream.
    - The storage seam stays storage-owned even if the final adapter into `ClaimSourceProof` lives at a crate boundary rather than directly inside `z00z_storage`.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_storage/src/assets/types.rs` contains `ClaimSourceRoot`.
    - `crates/z00z_storage/src/assets/store.rs` contains `claim_source_root` or a clearly named authoritative-root getter.
    - `crates/z00z_storage/src/assets/store.rs` contains `claim_source_proof` or a clearly named typed proof retrieval seam.
    - `crates/z00z_storage/src/checkpoint/artifact.rs` contains `claim_source_root`, `claim_root_ver`, or equivalent trusted root publication metadata.
    - `crates/z00z_storage/tests/test_claim_source_proof.rs` verifies versioned source-proof or root roundtrip behavior.
  </acceptance_criteria>
  <action>Implement the storage-facing half of D14.1, D14.4, and D15 from `025-FUSION.md`. Extend `assets/types.rs` with typed `ClaimSourceRoot`, `ClaimRootVer`, and any request or view types needed for wallets or simulator code to ask storage for a trusted claim source. Extend `assets/proof.rs`, `checkpoint/build.rs`, and `checkpoint/artifact.rs` so claim-source proofs stay storage-owned and can be attached to checkpoint or snapshot outputs without becoming opaque blobs. Extend `assets/store.rs` with explicit methods for returning the authoritative root and one typed proof retrieval result for a canonical asset path or source asset id. If directly returning `z00z_crypto::ClaimSourceProof` from storage would create the wrong layer coupling, keep the storage proof type storage-owned and add only a narrow adapter at the consumer boundary. Extend `z00z_core/src/assets/snapshot.rs` only where necessary to publish trusted authority or root metadata to consumers.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_storage --release --features test-fast
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>The authoritative claim-source root and proof seam is storage-owned, versioned, and available for downstream `claim_v2` migration after the bootstrap gate and release tests.</done>
</task>

</tasks>

<verification>
Confirm that `claim_v2` contracts exist before wallet or simulator migration begins, that storage owns authoritative source-proof APIs, and that release validation runs with the bootstrap gate first and the full workspace release gate second.
</verification>

<success_criteria>
The repository has one concrete `claim_v2` contract vocabulary and one storage-owned source-proof seam, so later plans can migrate live claim flows without interface discovery or placeholder ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/025-crypto-audit-crypto/025-01-SUMMARY.md`
</output>