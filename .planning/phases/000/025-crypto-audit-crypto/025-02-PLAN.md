<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 025-crypto-audit-crypto
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_crypto/src/hash.rs
  - crates/z00z_crypto/src/kdf.rs
  - crates/z00z_crypto/src/types.rs
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/src/aead.rs
  - crates/z00z_crypto/src/zkpack.rs
  - crates/z00z_crypto/tests/test_fail_closed.rs
  - crates/z00z_crypto/tests/corrupted_data_handling.rs
  - crates/z00z_wallets/src/core/stealth/facade_zkpack.rs
  - crates/z00z_wallets/tests/test_zkpack.rs
autonomous: true
requirements:
  - PH25-ZKPACK
  - PH25-FAILCLOSED
must_haves:
  truths:
    - Failures in scalar derivation, HMAC setup, and random-scalar generation stop the operation instead of producing constant outputs.
    - The custom crypto `aead_zkpack` path is no longer a default blessed production surface.
    - `ZkPackEncrypted` parsing reports explicit decode or version failures.
    - This wave introduces the early experimental or compatibility boundary required by D14.5 Phase 0, even if the final default-facade cutover closes in `025-05` after consumer migration.
  artifacts:
    - path: crates/z00z_crypto/tests/test_fail_closed.rs
      provides: Regression coverage for fail-closed helper behavior.
    - path: crates/z00z_crypto/src/zkpack.rs
      provides: Explicit `ZkPack` wire error surface.
    - path: crates/z00z_wallets/src/core/stealth/facade_zkpack.rs
      provides: The blessed production pack path with vectors that remain green.
  key_links:
    - from: crates/z00z_crypto/src/lib.rs
      to: crates/z00z_wallets/src/core/stealth/facade_zkpack.rs
      via: blessed production `ZkPack` surface selection
      pattern: zkpack|experimental|facade
---

<objective>
Make derivation helpers fail-closed and narrow production `ZkPack` to the standard AEAD path.

Purpose: remove the remaining fail-open cryptographic helper behavior and stop the custom crypto zkpack path from looking equivalent to the wallet AEAD production path.
Output: typed fail-closed helpers, explicit zkpack wire errors, and a clear production or experimental boundary for pack encryption.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/025-crypto-audit-crypto/025-CONTEXT.md
@.planning/phases/025-crypto-audit-crypto/025-FUSION.md
@crates/z00z_crypto/src/hash.rs
@crates/z00z_crypto/src/kdf.rs
@crates/z00z_crypto/src/types.rs
@crates/z00z_crypto/src/lib.rs
@crates/z00z_crypto/src/aead.rs
@crates/z00z_crypto/src/zkpack.rs
@crates/z00z_wallets/src/core/stealth/facade_zkpack.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove fail-open helper behavior from the stable crypto surface</name>
  <files>crates/z00z_crypto/src/hash.rs, crates/z00z_crypto/src/kdf.rs, crates/z00z_crypto/src/types.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/tests/test_fail_closed.rs, crates/z00z_crypto/tests/corrupted_data_handling.rs</files>
  <read_first>crates/z00z_crypto/src/hash.rs, crates/z00z_crypto/src/kdf.rs, crates/z00z_crypto/src/types.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/tests/corrupted_data_handling.rs, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - Stable helper APIs return typed errors instead of `Z00ZScalar::one()` or `[0u8; 32]` fallback values.
    - Random-scalar generation does not contain an unbounded recovery loop in production helper code.
    - Regression tests prove helper failures do not silently succeed.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_crypto/src/hash.rs` or `crates/z00z_crypto/src/kdf.rs` contains `try_hmac_sha256` or an equivalent fail-closed helper.
    - `crates/z00z_crypto/src/hash.rs` or `crates/z00z_crypto/src/kdf.rs` contains `try_hash_to_scalar_domain` or equivalent fail-closed helper.
    - `crates/z00z_crypto/src/types.rs` contains `try_from_hash` or an equivalent fail-closed scalar constructor.
    - `crates/z00z_crypto/src/types.rs` no longer contains `Err(_) => loop` inside `pub fn random`.
    - `crates/z00z_crypto/tests/test_fail_closed.rs` contains a test for HMAC or scalar fallback rejection.
  </acceptance_criteria>
  <action>Implement D07 and D15 from `025-FUSION.md` against the current code instead of the older audit snapshots. Keep `hash_to_scalar_zk()` unchanged because it is already fail-closed, but retire fail-open compatibility behavior in `hash::hash_to_scalar_domain`, `kdf::hash_to_scalar_domain`, `Z00ZScalar::from_hash`, `hmac_sha256`, `hmac_sha256_raw`, and `Z00ZScalar::random`. Add stable `try_*` helpers returning typed errors, migrate the root facade to bless the fail-closed names, and keep compatibility wrappers only if they cannot be removed in this phase. Any compatibility wrapper that survives must be clearly marked non-production or deprecated and must not silently substitute a constant value. Reuse `corrupted_data_handling.rs` for explicit malformed-input rejection, and extend an existing fuzz target only if a stable harness already exists in this crate during the phase.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --features test-fast --test test_fail_closed -- --nocapture
cargo test -p z00z_crypto --release --features test-fast
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>The stable crypto helper surface is fail-closed, regression tests prove no silent fallback remains, and the release gate runs after the mandatory bootstrap gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the wallet AEAD facade the only blessed production `ZkPack` path</name>
  <files>crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/src/aead.rs, crates/z00z_crypto/src/zkpack.rs, crates/z00z_wallets/src/core/stealth/facade_zkpack.rs, crates/z00z_wallets/tests/test_zkpack.rs</files>
  <read_first>crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/src/aead.rs, crates/z00z_crypto/src/zkpack.rs, crates/z00z_wallets/src/core/stealth/facade_zkpack.rs, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - The custom crypto zkpack module is clearly experimental and non-default.
    - `ZkPackEncrypted` parsing returns explicit version or decode errors.
    - Wallet vector tests remain the canonical proof that the production pack path works.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_crypto/src/lib.rs` does not contain `pub mod aead_zkpack` as a default production export.
    - `crates/z00z_crypto/src/lib.rs` contains `experimental` or a feature-gated equivalent for the custom zkpack path.
    - `crates/z00z_crypto/src/zkpack.rs` contains `enum ZkPackWireError` or equivalent typed error.
    - `crates/z00z_wallets/src/core/stealth/facade_zkpack.rs` still contains `ChaCha20Poly1305`.
    - `crates/z00z_wallets/tests/test_zkpack.rs` or wallet zkpack tests validate the production path after the change.
  </acceptance_criteria>
  <action>Implement D06, D13, D14.1, and D15 from `025-FUSION.md` for the pack-encryption boundary. Keep `ZkPackEncrypted` as the shared wire type, but change `zkpack.rs` to return typed decode or version errors instead of `Option`. Reclassify `z00z_crypto::aead_zkpack` under an explicit experimental or non-default feature path in this wave, while leaving the final default-facade cleanup and legacy-export removal to `025-05` after claim and wallet consumers finish migrating. Make the wallet ChaCha20-Poly1305 facade in `z00z_wallets/src/core/stealth/facade_zkpack.rs` the only blessed production reference path in docs, exports, and tests. Preserve the existing vector coverage and extend it if the wire type or parse errors change.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump --test test_zkpack -- --nocapture
cargo test -p z00z_crypto --release --features test-fast
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>The repository presents one blessed production `ZkPack` path, the custom crypto path is visibly experimental, and explicit wire errors replace ambiguous parsing behavior.</done>
</task>

</tasks>

<verification>
Verify the bootstrap gate runs first, the fail-closed helpers do not silently succeed, and the wallet AEAD facade remains the only blessed production `ZkPack` path before broader release validation runs.
</verification>

<success_criteria>
Fail-open helper behavior is removed from the stable surface, `ZkPackEncrypted` parsing is explicit, and production pack guidance points to the wallet AEAD facade only.
</success_criteria>

<output>
After completion, create `.planning/phases/025-crypto-audit-crypto/025-02-SUMMARY.md`
</output>