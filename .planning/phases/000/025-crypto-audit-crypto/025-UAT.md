---
status: completed
phase: 025-crypto-audit-crypto
source: 025-01-SUMMARY.md, 025-02-SUMMARY.md, 025-03-SUMMARY.md, 025-04-SUMMARY.md, 025-05-SUMMARY.md
started: 2026-03-27T21:52:50Z
updated: 2026-03-28T02:50:00Z
---

# Phase 025 UAT

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 5
name: Final Release Validation
expected: |
  The bootstrap gate, targeted Phase 025 regressions, and the full workspace release validation should pass in that order. The wallet claim verifier should accept only claim_v2 packages with authoritative non-zero source roots and valid proof blobs, and the default crypto facade should keep legacy claim_v1 and custom zkpack helpers behind explicit feature gates.
awaiting: none

## Tests

### 1. Claim V2 Contracts And Storage-Owned Source Proof

expected: Running `cargo test -p z00z_crypto --release --features test-fast --test test_claim_v2_contract -- --nocapture` and `cargo test -p z00z_storage --release --features test-fast --test test_claim_source_proof -- --nocapture` should pass, with canonical claim_v2 framing and storage-owned source-proof root matching visible in the assertions.
result: [passed]

### 2. Fail-Closed Crypto Helpers And Production ZkPack Boundary

expected: Running `cargo test -p z00z_crypto --release --features test-fast --test test_fail_closed -- --nocapture` should pass. The stable helper surface should return deterministic non-zero results without silent fallback behavior, and the default production guidance should point to the wallet ChaCha20-Poly1305 facade while custom crypto zkpack helpers remain explicitly experimental.
result: [passed]

### 3. Crypto-Owned Stealth Binding Compatibility

expected: Running `cargo test -p z00z_crypto --release --features test-fast --test test_stealth_bind_vectors -- --nocapture` should pass. The frozen `tag16`, `leaf_ad`, preimage-layout, and `range_ctx_hash` vectors should hold exactly, showing that wallet-visible stealth outputs stayed stable after ownership moved into `z00z_crypto`.
result: [passed]

### 4. Live Claim V2 Wallet And Simulator Flow

expected: Running `cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump test_zero_root_rejected -- --nocapture` and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_pkg_crypto_support -- --nocapture` should pass. The live claim path should use `claim_source`, authoritative non-zero source roots, explicit zero-root rejection, and direct storage-owned proof retrieval instead of placeholder proof blobs or a wallet-owned proof-construction shortcut.
result: [passed]

### 5. Default Crypto Facade Cleanup

expected: Running `cargo test -p z00z_crypto --release --features test-fast --test test_public_surface -- --nocapture` should pass. The default `z00z_crypto` facade and README should advertise production `claim_v2` plus the stable `ZkPackEncrypted` wire type, while `legacy-claim-v1` and custom zkpack seal/open helpers require explicit feature gates.
result: [passed]

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None.
