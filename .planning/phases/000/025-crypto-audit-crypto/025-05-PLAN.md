<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 025-crypto-audit-crypto
plan: 05
type: execute
wave: 3
depends_on:
  - 025-01
  - 025-02
  - 025-04
files_modified:
  - crates/z00z_crypto/Cargo.toml
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/src/claim/mod.rs
  - crates/z00z_crypto/src/README.md
  - crates/z00z_crypto/tests/test_public_surface.rs
  - crates/z00z_wallets/src/core/tx/claim_tx.rs
  - crates/z00z_wallets/src/core/stealth/facade_zkpack.rs
autonomous: true
requirements:
  - PH25-CLAIM-GATE
  - PH25-ZKPACK
must_haves:
  truths:
    - The default `z00z_crypto` facade no longer exports placeholder claim_v1 or custom zkpack production surfaces.
    - Any remaining legacy claim or custom zkpack surface is explicitly feature-gated for tests or compatibility only.
    - Release-style regression gates and repeated YOLO review passes close the phase after the surface cleanup.
    - This phase completes a staged cutover that should already have introduced compatibility or experimental boundaries earlier in `025-01` and `025-02`, rather than discovering those boundaries for the first time at closeout.
  artifacts:
    - path: crates/z00z_crypto/tests/test_public_surface.rs
      provides: Guardrail that the default facade does not leak legacy claim or zkpack surfaces.
    - path: crates/z00z_crypto/Cargo.toml
      provides: Feature gates for any remaining legacy compatibility surface.
    - path: crates/z00z_crypto/src/README.md
      provides: Updated production guidance for claim and zkpack surfaces.
  key_links:
    - from: crates/z00z_crypto/Cargo.toml
      to: crates/z00z_crypto/src/lib.rs
      via: feature-gated legacy or experimental modules
      pattern: legacy-claim-v1|experimental-zkpack
---

<objective>
Gate legacy `claim_v1` and custom zkpack surfaces off the default production facade and close the phase.

Purpose: finish the audit remediation by making the safe surfaces unmistakable in the root crypto API and validating them under release-style regression gates.
Output: default production facade cleanup, compatibility feature gates for any remaining legacy code, updated docs, and final public-surface regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/025-crypto-audit-crypto/025-CONTEXT.md
@.planning/phases/025-crypto-audit-crypto/025-02-PLAN.md
@.planning/phases/025-crypto-audit-crypto/025-04-PLAN.md
@.planning/phases/025-crypto-audit-crypto/025-FUSION.md
@crates/z00z_crypto/Cargo.toml
@crates/z00z_crypto/src/lib.rs
@crates/z00z_crypto/src/claim/mod.rs
@crates/z00z_crypto/src/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove legacy claim and custom zkpack surfaces from the default crypto facade</name>
  <files>crates/z00z_crypto/Cargo.toml, crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/src/claim/mod.rs, crates/z00z_crypto/tests/test_public_surface.rs</files>
  <read_first>crates/z00z_crypto/Cargo.toml, crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/src/claim/mod.rs, crates/z00z_crypto/tests/test_public_surface.rs, .planning/phases/025-crypto-audit-crypto/025-02-PLAN.md, .planning/phases/025-crypto-audit-crypto/025-04-PLAN.md</read_first>
  <behavior>
    - The default `z00z_crypto` facade exports only the production claim_v2 and production pack surfaces.
    - Legacy claim_v1 and custom zkpack paths remain accessible only through explicit compatibility or experimental features if they still exist.
    - A regression test fails if the default facade leaks legacy names again.
    - The regression guard checks the default facade specifically, not a compatibility-feature build.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_crypto/Cargo.toml` contains `legacy-claim-v1` or a clearly named equivalent feature gate.
    - `crates/z00z_crypto/Cargo.toml` contains `experimental-zkpack` or a clearly named equivalent feature gate.
    - `crates/z00z_crypto/src/lib.rs` does not contain a default `pub use` for `GenesisClaimProof`.
    - `crates/z00z_crypto/src/lib.rs` does not contain a default `pub mod aead_zkpack`.
    - `crates/z00z_crypto/tests/test_public_surface.rs` asserts the default facade does not expose legacy claim or zkpack items.
  </acceptance_criteria>
  <action>Implement D05, D06, D14.1, and D15 final-surface cleanup. Finalize the explicit feature gates that were staged earlier in `025-01` and `025-02` for any remaining legacy claim or custom zkpack compatibility code, and remove those items from the default root facade. Keep `claim_v2`, the standard AEAD surface, `ZkPackEncrypted`, and the crypto-owned stealth binding APIs as the stable production exports. Add one regression test or source-inspection test that fails if the default facade leaks `GenesisClaimProof`, `prove_genesis_claim`, `verify_genesis_claim`, `ClaimAuthoritySig::from_statement`, or `aead_zkpack` again.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --features test-fast --test test_public_surface -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>The default `z00z_crypto` facade no longer advertises placeholder claim or custom zkpack surfaces, and a regression guard prevents them from reappearing silently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh docs and close on release-style regression gates</name>
  <files>crates/z00z_crypto/src/README.md, crates/z00z_wallets/src/core/tx/claim_tx.rs, crates/z00z_wallets/src/core/stealth/facade_zkpack.rs</files>
  <read_first>crates/z00z_crypto/src/README.md, crates/z00z_wallets/src/core/tx/claim_tx.rs, crates/z00z_wallets/src/core/stealth/facade_zkpack.rs, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - Docs describe `claim_v2`, the production `ZkPack` path, and any remaining compatibility features accurately.
    - Final release-style validation runs after the bootstrap gate.
    - Repeated review passes close only after two consecutive clean outputs.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_crypto/src/README.md` contains `claim_v2`.
    - `crates/z00z_crypto/src/README.md` contains `experimental` or `legacy-claim-v1` guidance.
    - `crates/z00z_crypto/src/README.md` contains `ChaCha20-Poly1305` or `wallet facade` guidance for production `ZkPack`.
  </acceptance_criteria>
  <action>Update `z00z_crypto` crate documentation so the production guidance matches the new surface: `claim_v2` is the only production claim path, authoritative roots or proofs are storage-owned, and the wallet ChaCha20-Poly1305 facade is the blessed production `ZkPack` path. Then close the phase on the required regression stack: bootstrap gate first, targeted public-surface or claim tests, broader release cargo tests, and repeated `/GSD-Review-Tasks-Execution` YOLO runs until two consecutive passes show no significant issues.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_crypto --release --features test-fast
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump test_claim_tx -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>The docs match the real production surface, the bootstrap gate and release gates were both executed in order, and final review passes are clean on two consecutive runs.</done>
</task>

</tasks>

<verification>
Verify the default root crypto facade advertises only the production claim and pack surfaces, the docs say the same thing, and the phase closes only after bootstrap-first release validation and repeated clean review passes.
</verification>

<success_criteria>
The default production surface is unmistakable, legacy compatibility is explicitly gated, and the phase has one final regression or review closure point instead of lingering ambiguous exports.
</success_criteria>

<output>
After completion, create `.planning/phases/025-crypto-audit-crypto/025-05-SUMMARY.md`
</output>