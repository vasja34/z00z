<!-- markdownlint-disable MD001 MD007 MD022 MD032 MD033 MD041 MD047 -->
---
phase: 025-crypto-audit-crypto
plan: 04
type: execute
wave: 2
depends_on:
  - 025-01
  - 025-03
files_modified:
  - crates/z00z_wallets/src/core/tx/claim_tx.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/src/core/tx/test_claim_tx.rs
  - crates/z00z_wallets/tests/support/test_s5_sender_examples_support.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
  - crates/z00z_simulator/tests/test_claim_emit.rs
  - crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
  - crates/z00z_storage/src/assets/store.rs
  - crates/z00z_storage/tests/test_claim_source_proof.rs
autonomous: true
requirements:
  - PH25-CLAIM-V2
  - PH25-SOURCE-PROOF
must_haves:
  truths:
    - Production wallet and simulator claim flows stop generating or verifying placeholder proof or signature blobs.
    - Claim builders or verifiers reject zero-root or unsupported root-version states.
    - Live claim flows consume the storage-owned root or proof API instead of hard-coding `ZERO_ROOT`.
    - Trusted claim-source root or proof material reaches wallet and simulator code through an explicit trusted seam, not by trusting attacker-controlled tx payload fields.
  artifacts:
    - path: crates/z00z_wallets/src/core/tx/claim_tx.rs
      provides: Live claim_v2 verifier path.
    - path: crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
      provides: Live claim_v2 builder path using authoritative roots.
    - path: crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
      provides: End-to-end coverage that claim packages use the new contracts.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
      to: crates/z00z_storage/src/assets/store.rs
      via: authoritative root or source-proof retrieval
      pattern: claim_source_root|claim_source_proof
    - from: crates/z00z_wallets/src/core/tx/claim_tx.rs
      to: crates/z00z_crypto/src/claim/v2.rs
      via: `ClaimStmtV2` and `ClaimAuthoritySigV2` verification
      pattern: ClaimStmtV2|ClaimAuthoritySigV2|ClaimSourceProof
    - from: crates/z00z_wallets/src/core/tx/tx_verifier.rs
      to: crates/z00z_storage/src/assets/store.rs
      via: trusted verifier input seam for authoritative roots or proofs
      pattern: claim_source_root|claim_source_proof|ClaimSourceRoot
---

<objective>
Migrate live wallet and simulator claim flows to authoritative `claim_v2` signatures and source proofs.

Purpose: replace the currently reachable placeholder claim security boundary in real consumer flows with authoritative root and signature verification.
Output: wallet and simulator claim_v2 migration, zero-root rejection, and end-to-end tests tied to storage-owned source-proof APIs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/025-crypto-audit-crypto/025-CONTEXT.md
@.planning/phases/025-crypto-audit-crypto/025-01-PLAN.md
@.planning/phases/025-crypto-audit-crypto/025-03-PLAN.md
@.planning/phases/025-crypto-audit-crypto/025-FUSION.md
@crates/z00z_wallets/src/core/tx/claim_tx.rs
@crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs
@crates/z00z_storage/src/assets/store.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace wallet claim verification with `claim_v2` authority and source-proof checks</name>
  <files>crates/z00z_wallets/src/core/tx/claim_tx.rs, crates/z00z_wallets/src/core/tx/tx_verifier.rs, crates/z00z_wallets/src/core/tx/test_claim_tx.rs, crates/z00z_wallets/tests/support/test_s5_sender_examples_support.rs, crates/z00z_storage/src/assets/store.rs, crates/z00z_storage/tests/test_claim_source_proof.rs</files>
  <read_first>crates/z00z_wallets/src/core/tx/claim_tx.rs, crates/z00z_wallets/src/core/tx/tx_verifier.rs, crates/z00z_wallets/src/core/tx/test_claim_tx.rs, crates/z00z_wallets/tests/support/test_s5_sender_examples_support.rs, crates/z00z_storage/src/assets/store.rs, .planning/phases/025-crypto-audit-crypto/025-01-PLAN.md, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - Wallet claim verification constructs `ClaimStmtV2` and verifies `ClaimAuthoritySigV2`.
    - Wallet claim verification consumes `ClaimSourceProof` from storage-owned APIs or from a trusted verifier input seam fed by those APIs.
    - Zero-root, unsupported root-version, and source-proof mismatch states are explicit errors.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_wallets/src/core/tx/claim_tx.rs` or `crates/z00z_wallets/src/core/tx/tx_verifier.rs` contains `ClaimStmtV2`.
    - `crates/z00z_wallets/src/core/tx/claim_tx.rs` or `crates/z00z_wallets/src/core/tx/tx_verifier.rs` contains `ClaimAuthoritySigV2`.
    - `crates/z00z_wallets/src/core/tx/claim_tx.rs` or `crates/z00z_wallets/src/core/tx/tx_verifier.rs` contains `ClaimSourceProof`.
    - `crates/z00z_wallets/src/core/tx/claim_tx.rs` or `crates/z00z_wallets/src/core/tx/tx_verifier.rs` contains an explicit zero-root rejection path.
    - `crates/z00z_wallets/src/core/tx/test_claim_tx.rs` contains a test for zero-root or source-proof rejection.
  </acceptance_criteria>
  <action>Migrate wallet claim verification from the placeholder claim API to the contracts introduced in `025-01`. Build `ClaimStmtV2` from canonical wallet-visible fields, include `range_ctx_hash`, decode `ClaimSourceProof`, and verify a real `ClaimAuthoritySigV2` against a trusted authority key loaded from storage-owned state or registry metadata instead of from attacker-controlled tx payload. If `claim_tx.rs` is not the right place to reach storage directly, extend `tx_verifier.rs` or an adjacent trusted verifier input seam so the authoritative root or proof enters verification from trusted caller context rather than from package bytes. Add explicit wallet-side errors for zero-root rejection, unsupported root version, unsupported source-proof version, and source-proof mismatch. Update support builders and focused tests in the same wave.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump test_zero_root_rejected -- --nocapture
cargo test -p z00z_storage --release --features test-fast --test test_claim_source_proof -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>Wallet claim verification depends on `claim_v2` contracts, rejects zero-root or unsupported-version states explicitly, and no longer trusts placeholder proof or signature blobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace simulator claim package generation with authoritative roots and `claim_v2` artifacts</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs, crates/z00z_simulator/tests/test_claim_emit.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs</files>
  <read_first>crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs, crates/z00z_simulator/tests/test_claim_emit.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs, crates/z00z_wallets/src/core/tx/claim_tx.rs, .planning/phases/025-crypto-audit-crypto/025-01-PLAN.md, .planning/phases/025-crypto-audit-crypto/025-FUSION.md</read_first>
  <behavior>
    - Simulator no longer hard-codes `ZERO_ROOT` into emitted claim packages.
    - Simulator emits `claim_v2` proof and authority fields compatible with wallet verification.
    - End-to-end simulator tests prove authoritative root or proof retrieval is wired through a trusted scenario seam, even if stage-3 helpers do not hold an `AssetStore` directly.
  </behavior>
  <acceptance_criteria>
    - `crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs` does not contain `genesis_root: super::ZERO_ROOT` in the live path.
    - `crates/z00z_simulator/src/scenario_1/stage_3_utils/claim_pkg.rs` contains `ClaimStmtV2`.
    - `crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs` contains a test for authoritative-root claim emission.
    - `crates/z00z_simulator/tests/test_claim_emit.rs` contains a v2 claim decode or verify assertion.
  </acceptance_criteria>
  <action>Migrate simulator stage 3 claim-package generation from placeholder proof or signature construction to `claim_v2`. Replace the hard-coded `ZERO_ROOT` live path with storage-owned authoritative root retrieval or with an explicit trusted scenario input that is populated from storage-owned APIs earlier in the flow, emit versioned `ClaimSourceProof`, sign canonical `ClaimStmtV2` bytes with the trusted claim authority key used by the scenario, and update claim-package tests so they decode and verify the new v2 artifacts. Preserve fault-injection coverage only where it still models the real boundary.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_emit -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_pkg_crypto_support -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues. Fix all issues and warnings before closing the task.</automated>
  </verify>
  <done>The simulator emits authoritative-root claim_v2 packages, wallet-side verification accepts them, and placeholder zero-root emission is removed from the live path.</done>
</task>

</tasks>

<verification>
Verify that both wallet and simulator claim flows consume `claim_v2`, that zero-root live paths are gone, and that release validation still follows the bootstrap gate first and the full workspace release gate second.
</verification>

<success_criteria>
The current production-reachable claim boundary no longer depends on placeholder artifacts, and simulator plus wallet flows agree on authoritative root and signature semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/025-crypto-audit-crypto/025-04-SUMMARY.md`
</output>