---
phase: 062-Gaps-Closing-2
plan: PLAN-062-G26
type: execute
wave: 9
depends_on: 
  - PLAN-062-G07
files_modified:
  - crates/z00z_wallets/src/chain/scan_engine.rs
  - crates/z00z_wallets/src/chain/local_node_sim.rs
  - crates/z00z_wallets/src/chain/scan_engine_impl.rs
  - crates/z00z_wallets/src/services/wallet_actions_receive.rs
  - crates/z00z_wallets/tests/test_remote_scan_worker.rs
requirements:
  - TASK-124
autonomous: true
must_haves:
  truths:
    - "Remote scan is a helper seam only."
    - "Phase 062 requires local implementation of the remote-worker seam."
    - "Remote worker is implemented against local chain/node simulation with trust-boundary, no-mutation, restart, and malicious/stale worker tests."
  artifacts:
    - crates/z00z_wallets/src/chain/scan_engine.rs
    - crates/z00z_wallets/src/chain/local_node_sim.rs
    - crates/z00z_wallets/src/chain/scan_engine_impl.rs
    - crates/z00z_wallets/src/services/wallet_actions_receive.rs
    - crates/z00z_wallets/tests/test_remote_scan_worker.rs
  key_links:
    - "Group scope: Local remote scan worker simulation and trust-boundary implementation."
    - "Dependencies: PLAN-062-G07"
    - "Acceptance: Remote worker is implemented against local chain/node simulation with trust-boundary, no-mutation, restart, and malicious/stale worker tests."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-062-G26 Plan: Remote Scan Worker Local Simulation

<objective>
Deliver the grouped closure slice for TASK-124: Local remote scan worker simulation and trust-boundary implementation. The
implementation must preserve the exact task inventory, acceptance contract, and
evidence format from `062-TODO.md` while extending the current code or doc
owners named below.
</objective>

<context>
@.planning/phases/062-Gaps-Closing-2/062-TODO.md
@.planning/phases/062-Gaps-Closing-2/062-CONTEXT.md
@crates/z00z_wallets/src/chain/scan_engine.rs
@crates/z00z_wallets/src/chain/local_node_sim.rs
@crates/z00z_wallets/src/chain/scan_engine_impl.rs
@crates/z00z_wallets/src/services/wallet_actions_receive.rs
@crates/z00z_wallets/tests/test_remote_scan_worker.rs
@crates/z00z_wallets/src/services/test_wallet_service.rs
</context>

<codebase_fit_note>
Keep the remote worker on the existing wallet receive path. The current-tree
authorities are `scan_engine.rs`, `local_node_sim.rs`, `scan_engine_impl.rs`,
`wallet_actions_receive.rs`, `test_remote_scan_worker.rs`, and the existing
worker negative tests in `test_wallet_service.rs`.
</codebase_fit_note>

<coverage_contract>
This grouped plan covers TASK-124. It depends on PLAN-062-G07.
Acceptance for the group is: Remote worker is implemented against local chain/node simulation with trust-boundary, no-mutation, restart, and malicious/stale worker tests.
No included task may close on placeholder logic, compile-only proof, or
docs-only proof for runtime behavior.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-062-G26`
task_ids:
- `TASK-124`
copied_task_rows:
- "| TASK-124 | PLAN-062-T124 | [local full-system simulation register](#local-full-system-simulation-closure-register) | Implement remote scan worker against local chain/node simulation. | TASK-026 through TASK-031, TASK-121 | Remote worker has trust-boundary/no-mutation/restart/stale/malicious-worker tests and cannot replace authoritative local receive verification. |"
source_refs:
- `TASK-124`: [local full-system simulation register](#local-full-system-simulation-closure-register)
inputs:
- `.planning/phases/062-Gaps-Closing-2/062-TODO.md` canonical task rows and group contract
- `crates/z00z_wallets/src/chain/scan_engine.rs`
- `crates/z00z_wallets/src/chain/local_node_sim.rs`
- `crates/z00z_wallets/src/chain/scan_engine_impl.rs`
- `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- `crates/z00z_wallets/tests/test_remote_scan_worker.rs`
- `crates/z00z_wallets/src/services/test_wallet_service.rs`
- dependency output from `PLAN-062-G07`
outputs:
- `.planning/phases/062-Gaps-Closing-2/062-26-SUMMARY.md` summary with completion/blocker evidence for `PLAN-062-G26`
- changed or created `crates/z00z_wallets/src/chain/scan_engine.rs`
- changed or created `crates/z00z_wallets/src/chain/local_node_sim.rs`
- changed or created `crates/z00z_wallets/src/chain/scan_engine_impl.rs`
- changed or created `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- changed or created `crates/z00z_wallets/tests/test_remote_scan_worker.rs`
dependencies:
- `PLAN-062-G07`
acceptance_tests:
- `cargo test --release -p z00z_wallets --test test_remote_scan_worker`
- `cargo test --release -p z00z_wallets test_remote_worker_ -- --nocapture`
- `cargo test --release -p z00z_wallets test_worker_ -- --nocapture`
- `cargo test --release -p z00z_wallets`
- `rg -n "RemoteScanWorker|recv_range_from_worker|recv_range_with_worker|no-mutation|stale|malicious" crates/z00z_wallets/src/chain crates/z00z_wallets/src/services`
simulation_gate:
- Mandatory local chain/node simulation over the real wallet receive path.
negative_tests:
- Remote worker must never bypass local receive verification.
- Restart/replay/no-mutation violations leave wallet state unchanged.
</plan_matrix>

<plan_artifacts>
- artifact_id: `scan-engine-contract`
  path_or_api: `crates/z00z_wallets/src/chain/scan_engine.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-124]
- artifact_id: `local-node-sim`
  path_or_api: `crates/z00z_wallets/src/chain/local_node_sim.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-124]
- artifact_id: `scan-engine-impl`
  path_or_api: `crates/z00z_wallets/src/chain/scan_engine_impl.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-124]
- artifact_id: `wallet-actions-receive`
  path_or_api: `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-124]
- artifact_id: `test-remote-scan-worker`
  path_or_api: `crates/z00z_wallets/tests/test_remote_scan_worker.rs`
  kind: `test`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-124]
- artifact_id: `plan-062-g26-summary`
  path_or_api: `.planning/phases/062-Gaps-Closing-2/062-26-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-124]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_wallets --test test_remote_scan_worker`
- `cargo test --release -p z00z_wallets test_remote_worker_ -- --nocapture`
- `cargo test --release -p z00z_wallets test_worker_ -- --nocapture`
- `cargo test --release -p z00z_wallets`
- `rg -n "RemoteScanWorker|recv_range_from_worker|recv_range_with_worker|no-mutation|stale|malicious" crates/z00z_wallets/src/chain crates/z00z_wallets/src/services`
</plan_tests>

<plan_results>
- `PLAN-062-G26` produces `.planning/phases/062-Gaps-Closing-2/062-26-SUMMARY.md` with exact `TASK-124` completion or blocker fields using the `TASK-075` evidence template.
- Group acceptance is proven only when the commands in `plan_tests` and the mandatory review loop complete cleanly.
- Evidence cites changed files, source refs, task ids, and any remaining adapter-only exclusions explicitly.
</plan_results>

<task_artifacts>
- task_id: `TASK-124`
  implementation_depth: `simulated-full`
  artifacts:
    - `crates/z00z_wallets/src/chain/scan_engine.rs`
    - `crates/z00z_wallets/src/chain/local_node_sim.rs`
    - `crates/z00z_wallets/src/chain/scan_engine_impl.rs`
    - `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
    - `crates/z00z_wallets/tests/test_remote_scan_worker.rs`
</task_artifacts>

<task_tests>
- task_id: `TASK-124`
  commands:
    - `cargo test --release -p z00z_wallets --test test_remote_scan_worker`
    - `cargo test --release -p z00z_wallets test_remote_worker_ -- --nocapture`
    - `cargo test --release -p z00z_wallets test_worker_ -- --nocapture`
    - `cargo test --release -p z00z_wallets`
    - `rg -n "RemoteScanWorker|recv_range_from_worker|recv_range_with_worker|no-mutation|stale|malicious" crates/z00z_wallets/src/chain crates/z00z_wallets/src/services`
  scenarios:
    - `Implement remote scan worker against local chain/node simulation.`
    - `restart/replay/stale-state cases fail closed`
</task_tests>

<task_results>
- task_id: `TASK-124`
  expected_results:
    - `Remote worker has trust-boundary/no-mutation/restart/stale/malicious-worker tests and cannot replace authoritative local receive verification.`
    - `anti-placeholder evidence includes changed files plus passing or fail-closed tests; compile-only proof is invalid.`
</task_results>

<anti_placeholder_gate>
- Every included task must change or create the code, docs, config, tests, or simulator artifacts named in `task_artifacts`, or explicitly remove a live claim when `implementation_depth` is `live-claim-removed`.
- `TODO`, `panic!`, string-only errors, empty wrappers, compile-only proof, or doc-only claims do not satisfy this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_wallets/src/chain/scan_engine.rs`
- `crates/z00z_wallets/src/chain/local_node_sim.rs`
- `crates/z00z_wallets/src/chain/scan_engine_impl.rs`
- `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- `crates/z00z_wallets/tests/test_remote_scan_worker.rs`
- `crates/z00z_wallets/src/services/test_wallet_service.rs`
</current_code_refs>

<blockers>
- Use the exact TASK-075 blocker template when a command or local simulator proof cannot close a task.
- Only external transport, remote deployment, or third-party network adapters may remain adapter-only after local deterministic simulation is implemented.
- Any missing task mapping, missing evidence field, or placeholder-only closure is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence cites the exact task ids, changed files, test commands, simulator outputs when relevant, and remaining adapter-only exclusions.
- If a task cannot be closed locally, record a blocker with the exact TASK-075 fields and treat the group as phase-failing until resolved or explicitly classified as external-adapter-only.
</evidence_gate>

<not_recommendation_gate>
- This plan is executable scope, not a recommendation list.
- Any item that is still future-only after execution must be justified by the normative task row as `live-claim-removed` or as an external-adapter-only exclusion after local simulator closure.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Remote Scan Worker Local Simulation</name>
<read_first>
- `.planning/phases/062-Gaps-Closing-2/062-TODO.md`
- `.planning/phases/062-Gaps-Closing-2/062-CONTEXT.md`
- `crates/z00z_wallets/src/chain/scan_engine.rs`
- `crates/z00z_wallets/src/chain/local_node_sim.rs`
- `crates/z00z_wallets/src/chain/scan_engine_impl.rs`
- `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- `crates/z00z_wallets/tests/test_remote_scan_worker.rs`
- `crates/z00z_wallets/src/services/test_wallet_service.rs`
</read_first>
<files>
- `crates/z00z_wallets/src/chain/scan_engine.rs`
- `crates/z00z_wallets/src/chain/local_node_sim.rs`
- `crates/z00z_wallets/src/chain/scan_engine_impl.rs`
- `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- `crates/z00z_wallets/tests/test_remote_scan_worker.rs`
</files>
<action>
Execute `PLAN-062-G26` as the grouped closure packet for Local remote scan worker simulation and trust-boundary implementation.
Keep every included task row visible in execution artifacts, extend only the
live code/doc owners named in `current_code_refs`, and use local deterministic
simulation wherever the task contract permits `simulated-full` closure.
This plan must not create recommendation-only work, a second authority plane,
or a placeholder implementation lane.
</action>
<acceptance_criteria>
- `TASK-124` closes only when Remote worker has trust-boundary/no-mutation/restart/stale/malicious-worker tests and cannot replace authoritative local receive verification.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_wallets --test test_remote_scan_worker`
   - `cargo test --release -p z00z_wallets test_remote_worker_ -- --nocapture`
   - `cargo test --release -p z00z_wallets test_worker_ -- --nocapture`
   - `cargo test --release -p z00z_wallets`
   - `rg -n "RemoteScanWorker|recv_range_from_worker|recv_range_with_worker|no-mutation|stale|malicious" crates/z00z_wallets/src/chain crates/z00z_wallets/src/services`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
Remote worker is implemented against local chain/node simulation with trust-boundary, no-mutation, restart, and malicious/stale worker tests.
</done>
</task>
</tasks>

<threat_model>
- Authority drift could accidentally create a second truth plane while trying to close the named gap.
- Placeholder drift could close with DTO, docs, or string changes that do not modify real behavior.
- Evidence drift could claim closure without local negative tests or simulator proof where the task explicitly requires it.
</threat_model>

<verification>
This slice is valid only if the listed commands, negative cases, evidence gate, and anti-placeholder gate all pass together.
</verification>

<success_criteria>
- Remote worker is implemented against local chain/node simulation with trust-boundary, no-mutation, restart, and malicious/stale worker tests.
- All included task rows retain traceability to their source refs and current code anchors.
- Any remaining non-live boundary is explicitly justified as external-adapter-only.
</success_criteria>

<output>
Create `.planning/phases/062-Gaps-Closing-2/062-26-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/062-Gaps-Closing-2/062-26-SUMMARY.md`
- Group-level completion or blocker evidence for `PLAN-062-G26`
- Updated code, docs, config, tests, or simulator artifacts listed in `plan_artifacts`

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
|---|---|---|---|---|---|---|---|---|
| `TASK-124` | `PLAN-062-G26` | [local full-system simulation register](#local-full-system-simulation-closure-register) | crates/z00z_wallets/src/chain/scan_engine.rs; crates/z00z_wallets/src/chain/local_node_sim.rs; crates/z00z_wallets/src/chain/scan_engine_impl.rs; crates/z00z_wallets/src/services/wallet_actions_receive.rs; crates/z00z_wallets/tests/test_remote_scan_worker.rs | `cargo test --release -p z00z_wallets --test test_remote_scan_worker`; `cargo test --release -p z00z_wallets test_remote_worker_ -- --nocapture`; `cargo test --release -p z00z_wallets test_worker_ -- --nocapture`; `cargo test --release -p z00z_wallets` | Remote worker has trust-boundary/no-mutation/restart/stale/malicious-worker tests and cannot replace authoritative local receive verification. | Mandatory local chain/node simulation over the real wallet receive path. | changed code/docs/tests or explicit live-claim removal with negative proof | `Completed` |
