---
phase: 062-Gaps-Closing-2
plan: PLAN-062-G24
type: execute
wave: 9
depends_on: 
  - PLAN-062-G23
files_modified:
  - crates/z00z_wallets/src/chain/broadcast_impl.rs
  - crates/z00z_wallets/src/persistence/tx_storage.rs
  - crates/z00z_wallets/src/persistence/tx_storage_impl.rs
  - crates/z00z_wallets/src/rpc/tx_runtime_state.rs
  - crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs
  - crates/z00z_wallets/tests/test_chain_broadcast_retry.rs
requirements:
  - TASK-122
autonomous: true
must_haves:
  truths:
    - "Broadcast integrates with durable tx history."
    - "Retry timing may be simulated, storage semantics stay real."
    - "Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths."
  artifacts:
    - crates/z00z_wallets/src/chain/broadcast_impl.rs
    - crates/z00z_wallets/src/persistence/tx_storage.rs
    - crates/z00z_wallets/src/persistence/tx_storage_impl.rs
    - crates/z00z_wallets/src/rpc/tx_runtime_state.rs
    - crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs
    - crates/z00z_wallets/tests/test_chain_broadcast_retry.rs
  key_links:
    - "Group scope: Broadcast submission, retry, confirmation polling, and tx-store lifecycle integration."
    - "Dependencies: PLAN-062-G23"
    - "Acceptance: Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-062-G24 Plan: Broadcast Retry And Confirmation Persistence

<objective>
Deliver the grouped closure slice for TASK-122: Broadcast submission, retry, confirmation polling, and tx-store lifecycle integration. The
implementation must preserve the exact task inventory, acceptance contract, and
evidence format from `062-TODO.md` while extending the current code or doc
owners named below.
</objective>

<context>
@.planning/phases/062-Gaps-Closing-2/062-TODO.md
@.planning/phases/062-Gaps-Closing-2/062-CONTEXT.md
@crates/z00z_wallets/src/chain/broadcast_impl.rs
@crates/z00z_wallets/src/persistence/tx_storage.rs
@crates/z00z_wallets/src/persistence/tx_storage_impl.rs
@crates/z00z_wallets/src/rpc/tx_runtime_state.rs
@crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs
@crates/z00z_wallets/tests/test_chain_broadcast_retry.rs
</context>

<codebase_fit_note>
`crates/z00z_wallets/tests/test_chain_broadcast_retry.rs` is a live workspace
artifact on the canonical wallet broadcast closeout path. Keep the task
evidence anchored on the existing broadcast, tx-storage, and broadcast-suite
seams instead of introducing a second test lane.
</codebase_fit_note>

<coverage_contract>
This grouped plan covers TASK-122. It depends on PLAN-062-G23.
Acceptance for the group is: Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths.
No included task may close on placeholder logic, compile-only proof, or
docs-only proof for runtime behavior.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-062-G24`
task_ids:
- `TASK-122`
copied_task_rows:
- "| TASK-122 | PLAN-062-T122 | [local full-system simulation register](#local-full-system-simulation-closure-register) | Implement broadcast submission, retry, confirmation polling, and tx-store integration. | TASK-121 | Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths. |"
source_refs:
- `TASK-122`: [local full-system simulation register](#local-full-system-simulation-closure-register)
inputs:
- `.planning/phases/062-Gaps-Closing-2/062-TODO.md` canonical task rows and group contract
- `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- `crates/z00z_wallets/src/persistence/tx_storage.rs`
- `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
- `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- `crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs`
- `crates/z00z_wallets/tests/test_chain_broadcast_retry.rs`
- dependency output from `PLAN-062-G23`
outputs:
- `.planning/phases/062-Gaps-Closing-2/062-24-SUMMARY.md` summary with completion/blocker evidence for `PLAN-062-G24`
- changed or created `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- changed or created `crates/z00z_wallets/src/persistence/tx_storage.rs`
- changed or created `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
- changed or created `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- changed or created `crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs`
- changed or created `crates/z00z_wallets/tests/test_chain_broadcast_retry.rs`
dependencies:
- `PLAN-062-G23`
acceptance_tests:
- `cargo test --release -p z00z_wallets broadcast_retry -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry`
- `cargo test --release -p z00z_wallets --test test_tx_store_integration`
- `rg -n "broadcast|confirmation|retry|timeout|duplicate|reorg|replacement" crates/z00z_wallets/src/chain crates/z00z_wallets/src/rpc`
simulation_gate:
- Mandatory local node/mempool simulation around real wallet broadcast and tx-store primitives.
negative_tests:
- Timeout, reject, duplicate, reorg, and replacement paths must persist lifecycle updates.
- String-only or timeout-only broadcast closure is invalid.
</plan_matrix>

<plan_artifacts>
- artifact_id: `broadcast-impl`
  path_or_api: `crates/z00z_wallets/src/chain/broadcast_impl.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-122]
- artifact_id: `tx-storage`
  path_or_api: `crates/z00z_wallets/src/persistence/tx_storage.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-122]
- artifact_id: `tx-storage-impl`
  path_or_api: `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-122]
- artifact_id: `tx-runtime-state`
  path_or_api: `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-122]
- artifact_id: `test-tx-broadcast-suite`
  path_or_api: `crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs`
  kind: `test`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-122]
- artifact_id: `test-chain-broadcast-retry`
  path_or_api: `crates/z00z_wallets/tests/test_chain_broadcast_retry.rs`
  kind: `test`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-122]
- artifact_id: `plan-062-g24-summary`
  path_or_api: `.planning/phases/062-Gaps-Closing-2/062-24-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-122]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_wallets broadcast_retry -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry`
- `cargo test --release -p z00z_wallets --test test_tx_store_integration`
- `rg -n "broadcast|confirmation|retry|timeout|duplicate|reorg|replacement" crates/z00z_wallets/src/chain crates/z00z_wallets/src/rpc`
</plan_tests>

<plan_results>
- `PLAN-062-G24` produces `.planning/phases/062-Gaps-Closing-2/062-24-SUMMARY.md` with exact `TASK-122` completion or blocker fields using the `TASK-075` evidence template.
- Group acceptance is proven only when the commands in `plan_tests` and the mandatory review loop complete cleanly.
- Evidence cites changed files, source refs, task ids, and any remaining adapter-only exclusions explicitly.
</plan_results>

<task_artifacts>
- task_id: `TASK-122`
  implementation_depth: `simulated-full`
  artifacts:
    - `crates/z00z_wallets/src/chain/broadcast_impl.rs`
    - `crates/z00z_wallets/src/persistence/tx_storage.rs`
    - `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
    - `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
</task_artifacts>

<task_tests>
- task_id: `TASK-122`
  commands:
    - `cargo test --release -p z00z_wallets broadcast_retry -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry`
    - `cargo test --release -p z00z_wallets --test test_tx_store_integration`
    - `rg -n "broadcast|confirmation|retry|timeout|duplicate|reorg|replacement" crates/z00z_wallets/src/chain crates/z00z_wallets/src/rpc`
  scenarios:
    - `Implement broadcast submission, retry, confirmation polling, and tx-store integration.`
    - `typed rejection paths are deterministic`
</task_tests>

<task_results>
- task_id: `TASK-122`
  expected_results:
    - `Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths.`
    - `anti-placeholder evidence includes changed files plus passing or fail-closed tests; compile-only proof is invalid.`
</task_results>

<anti_placeholder_gate>
- Every included task must change or create the code, docs, config, tests, or simulator artifacts named in `task_artifacts`, or explicitly remove a live claim when `implementation_depth` is `live-claim-removed`.
- `TODO`, `panic!`, string-only errors, empty wrappers, compile-only proof, or doc-only claims do not satisfy this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- `crates/z00z_wallets/src/persistence/tx_storage.rs`
- `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
- `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- `crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs`
- `crates/z00z_wallets/tests/test_chain_broadcast_retry.rs`
</current_code_refs>

<blockers>
- Use the exact TASK-075 blocker template when a command or local simulator proof cannot close a task.
- Only external transport, remote deployment, or third-party network adapters may remain adapter-only after local deterministic simulation is implemented.
- Any missing task mapping, missing evidence field, or placeholder-only closure is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence cites the exact task ids, changed files, test commands, simulator outputs when relevant, and remaining adapter-only exclusions.
- If a task cannot be closed locally, record a blocker with the exact TASK-075 fields and treat the group as phase-failing until resolved or explicitly classified as external-adapter-only.
</evidence_gate>

<not_recommendation_gate>
- This plan is executable scope, not a recommendation list.
- Any item that is still future-only after execution must be justified by the normative task row as `live-claim-removed` or as an external-adapter-only exclusion after local simulator closure.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Broadcast Retry And Confirmation Persistence</name>
<read_first>
- `.planning/phases/062-Gaps-Closing-2/062-TODO.md`
- `.planning/phases/062-Gaps-Closing-2/062-CONTEXT.md`
- `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- `crates/z00z_wallets/src/persistence/tx_storage.rs`
- `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
- `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- `crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs`
- `crates/z00z_wallets/tests/test_chain_broadcast_retry.rs`
</read_first>
<files>
- `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- `crates/z00z_wallets/src/persistence/tx_storage.rs`
- `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
- `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- `crates/z00z_wallets/src/rpc/test_tx_broadcast_suite.rs`
- `crates/z00z_wallets/tests/test_chain_broadcast_retry.rs`
</files>
<action>
Execute `PLAN-062-G24` as the grouped closure packet for Broadcast submission, retry, confirmation polling, and tx-store lifecycle integration.
Keep every included task row visible in execution artifacts, extend only the
live code/doc owners named in `current_code_refs`, and use local deterministic
simulation wherever the task contract permits `simulated-full` closure.
This plan must not create recommendation-only work, a second authority plane,
or a placeholder implementation lane.
</action>
<acceptance_criteria>
- `TASK-122` closes only when Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_wallets broadcast_retry -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry`
   - `cargo test --release -p z00z_wallets --test test_tx_store_integration`
   - `rg -n "broadcast|confirmation|retry|timeout|duplicate|reorg|replacement" crates/z00z_wallets/src/chain crates/z00z_wallets/src/rpc`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths.
</done>
</task>
</tasks>

<threat_model>
- Authority drift could accidentally create a second truth plane while trying to close the named gap.
- Placeholder drift could close with DTO, docs, or string changes that do not modify real behavior.
- Evidence drift could claim closure without local negative tests or simulator proof where the task explicitly requires it.
</threat_model>

<verification>
This slice is valid only if the listed commands, negative cases, evidence gate, and anti-placeholder gate all pass together.
</verification>

<success_criteria>
- Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths.
- All included task rows retain traceability to their source refs and current code anchors.
- Any remaining non-live boundary is explicitly justified as external-adapter-only.
</success_criteria>

<output>
Create `.planning/phases/062-Gaps-Closing-2/062-24-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/062-Gaps-Closing-2/062-24-SUMMARY.md`
- Group-level completion or blocker evidence for `PLAN-062-G24`
- Updated code, docs, config, tests, or simulator artifacts listed in `plan_artifacts`

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
|---|---|---|---|---|---|---|---|---|
| `TASK-122` | `PLAN-062-G24` | [local full-system simulation register](#local-full-system-simulation-closure-register) | crates/z00z_wallets/src/chain/broadcast_impl.rs; crates/z00z_wallets/src/persistence/tx_storage.rs; crates/z00z_wallets/src/persistence/tx_storage_impl.rs; crates/z00z_wallets/src/rpc/tx_runtime_state.rs | `cargo test --release -p z00z_wallets broadcast_retry -- --nocapture`; `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry` | Broadcast retry/polling persists lifecycle updates and handles timeout, reject, duplicate, reorg/replacement, and confirmation paths. | Mandatory local node/mempool simulation around real wallet broadcast and tx-store primitives. | changed code/docs/tests or explicit live-claim removal with negative proof | `Completed` |
