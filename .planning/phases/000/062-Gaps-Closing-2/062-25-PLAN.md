---
phase: 062-Gaps-Closing-2
plan: PLAN-062-G25
type: execute
wave: 9
depends_on: 
  - PLAN-062-G23
files_modified:
  - crates/z00z_wallets/src/tx/fee_estimator.rs
  - crates/z00z_wallets/src/tx/test_fee_estimator.rs
  - crates/z00z_wallets/tests/test_fee_rate_source.rs
requirements:
  - TASK-123
autonomous: true
must_haves:
  truths:
    - "Estimator needs a simulated live source path."
    - "Transaction-weight logic remains real."
    - "Fee estimator uses local simulated source with cache/fallback/stale/zero/spike tests and adapter seam for real network source."
  artifacts:
    - crates/z00z_wallets/src/tx/fee_estimator.rs
    - crates/z00z_wallets/src/tx/test_fee_estimator.rs
    - crates/z00z_wallets/tests/test_fee_rate_source.rs
  key_links:
    - "Group scope: Simulated-live network fee-rate source through the existing FeeRateSource seam."
    - "Dependencies: PLAN-062-G23"
    - "Acceptance: Fee estimator uses local simulated source with cache/fallback/stale/zero/spike tests and adapter seam for real network source."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-062-G25 Plan: Simulated Live Fee-Rate Source

<objective>
Deliver the grouped closure slice for TASK-123: Simulated-live network fee-rate source. The
implementation must preserve the exact task inventory, acceptance contract, and
evidence format from `062-TODO.md` while extending the current code or doc
owners named below.
</objective>

<context>
@.planning/phases/062-Gaps-Closing-2/062-TODO.md
@.planning/phases/062-Gaps-Closing-2/062-CONTEXT.md
@crates/z00z_wallets/src/tx/fee_estimator.rs
@crates/z00z_wallets/src/tx/test_fee_estimator.rs
@crates/z00z_wallets/tests/test_chain_client_sim.rs
@crates/z00z_wallets/tests/test_fee_rate_source.rs
</context>

<codebase_fit_note>
`crates/z00z_wallets/tests/test_fee_rate_source.rs` is a live workspace
artifact on the canonical wallet fee-source closeout path. Keep the task
evidence anchored on the existing fee-estimator, fee-estimator-test, and
local-node simulation seams instead of introducing a second fee-source test
lane.
</codebase_fit_note>

<coverage_contract>
This grouped plan covers TASK-123. It depends on PLAN-062-G23.
Acceptance for the group is: Fee estimator uses local simulated source with cache/fallback/stale/zero/spike tests and adapter seam for real network source.
No included task may close on placeholder logic, compile-only proof, or
docs-only proof for runtime behavior.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-062-G25`
task_ids:
- `TASK-123`
copied_task_rows:
- "| TASK-123 | PLAN-062-T123 | [local full-system simulation register](#local-full-system-simulation-closure-register) | Wire fee-rate source into fee estimation through local node/network simulation. | TASK-121 | Fee estimator uses simulated live source with cache/fallback/stale/zero/spike tests; external network source remains adapter-only. |"
source_refs:
- `TASK-123`: [local full-system simulation register](#local-full-system-simulation-closure-register)
inputs:
- `.planning/phases/062-Gaps-Closing-2/062-TODO.md` canonical task rows and group contract
- `crates/z00z_wallets/src/tx/fee_estimator.rs`
- `crates/z00z_wallets/src/tx/test_fee_estimator.rs`
- `crates/z00z_wallets/tests/test_chain_client_sim.rs`
- `crates/z00z_wallets/tests/test_fee_rate_source.rs`
- dependency output from `PLAN-062-G23`
outputs:
- `.planning/phases/062-Gaps-Closing-2/062-25-SUMMARY.md` summary with completion/blocker evidence for `PLAN-062-G25`
- changed or created `crates/z00z_wallets/src/tx/fee_estimator.rs`
- changed or created `crates/z00z_wallets/src/tx/test_fee_estimator.rs`
- changed or created `crates/z00z_wallets/tests/test_fee_rate_source.rs`
dependencies:
- `PLAN-062-G23`
acceptance_tests:
- `cargo test --release -p z00z_wallets test_rates_ -- --nocapture`
- `cargo test --release -p z00z_wallets spike_rate -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_fee_rate_source`
- `cargo test --release -p z00z_wallets --test test_chain_client_sim`
- `rg -n "fee source|cache_ttl|stale|zero|spike|fallback" crates/z00z_wallets/src/tx crates/z00z_wallets/src/chain`
simulation_gate:
- Mandatory local fee-source simulation over the real fee estimator.
negative_tests:
- Zero, stale, or spiking fee data must fail safe.
- Static-only estimator cannot survive if live-fee claims remain.
</plan_matrix>

<plan_artifacts>
- artifact_id: `fee-estimator`
  path_or_api: `crates/z00z_wallets/src/tx/fee_estimator.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-123]
- artifact_id: `test-fee-estimator`
  path_or_api: `crates/z00z_wallets/src/tx/test_fee_estimator.rs`
  kind: `test`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-123]
- artifact_id: `test-fee-rate-source`
  path_or_api: `crates/z00z_wallets/tests/test_fee_rate_source.rs`
  kind: `test`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-123]
- artifact_id: `plan-062-g25-summary`
  path_or_api: `.planning/phases/062-Gaps-Closing-2/062-25-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-123]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_wallets test_rates_ -- --nocapture`
- `cargo test --release -p z00z_wallets spike_rate -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_fee_rate_source`
- `cargo test --release -p z00z_wallets --test test_chain_client_sim`
- `rg -n "fee source|cache_ttl|stale|zero|spike|fallback" crates/z00z_wallets/src/tx crates/z00z_wallets/src/chain`
</plan_tests>

<plan_results>
- `PLAN-062-G25` produces `.planning/phases/062-Gaps-Closing-2/062-25-SUMMARY.md` with exact `TASK-123` completion or blocker fields using the `TASK-075` evidence template.
- Group acceptance is proven only when the commands in `plan_tests` and the mandatory review loop complete cleanly.
- Evidence cites changed files, source refs, task ids, and any remaining adapter-only exclusions explicitly.
</plan_results>

<task_artifacts>
- task_id: `TASK-123`
  implementation_depth: `simulated-full`
  artifacts:
    - `crates/z00z_wallets/src/tx/fee_estimator.rs`
    - `crates/z00z_wallets/src/tx/test_fee_estimator.rs`
    - `crates/z00z_wallets/tests/test_fee_rate_source.rs`
</task_artifacts>

<task_tests>
- task_id: `TASK-123`
  commands:
    - `cargo test --release -p z00z_wallets test_rates_ -- --nocapture`
    - `cargo test --release -p z00z_wallets spike_rate -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_fee_rate_source`
    - `cargo test --release -p z00z_wallets --test test_chain_client_sim`
    - `rg -n "fee source|cache_ttl|stale|zero|spike|fallback" crates/z00z_wallets/src/tx crates/z00z_wallets/src/chain`
  scenarios:
    - `Wire fee-rate source into fee estimation through local node/network simulation.`
    - `simulated faults cannot bypass authoritative local verification`
</task_tests>

<task_results>
- task_id: `TASK-123`
  expected_results:
    - `Fee estimator uses simulated live source with cache/fallback/stale/zero/spike tests; external network source remains adapter-only.`
    - `anti-placeholder evidence includes changed files plus passing or fail-closed tests; compile-only proof is invalid.`
</task_results>

<anti_placeholder_gate>
- Every included task must change or create the code, docs, config, tests, or simulator artifacts named in `task_artifacts`, or explicitly remove a live claim when `implementation_depth` is `live-claim-removed`.
- `TODO`, `panic!`, string-only errors, empty wrappers, compile-only proof, or doc-only claims do not satisfy this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_wallets/src/tx/fee_estimator.rs`
- `crates/z00z_wallets/src/tx/test_fee_estimator.rs`
- `crates/z00z_wallets/tests/test_chain_client_sim.rs`
- `crates/z00z_wallets/tests/test_fee_rate_source.rs`
</current_code_refs>

<blockers>
- Use the exact TASK-075 blocker template when a command or local simulator proof cannot close a task.
- Only external transport, remote deployment, or third-party network adapters may remain adapter-only after local deterministic simulation is implemented.
- Any missing task mapping, missing evidence field, or placeholder-only closure is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence cites the exact task ids, changed files, test commands, simulator outputs when relevant, and remaining adapter-only exclusions.
- If a task cannot be closed locally, record a blocker with the exact TASK-075 fields and treat the group as phase-failing until resolved or explicitly classified as external-adapter-only.
</evidence_gate>

<not_recommendation_gate>
- This plan is executable scope, not a recommendation list.
- Any item that is still future-only after execution must be justified by the normative task row as `live-claim-removed` or as an external-adapter-only exclusion after local simulator closure.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Simulated Live Fee-Rate Source</name>
<read_first>
- `.planning/phases/062-Gaps-Closing-2/062-TODO.md`
- `.planning/phases/062-Gaps-Closing-2/062-CONTEXT.md`
- `crates/z00z_wallets/src/tx/fee_estimator.rs`
- `crates/z00z_wallets/src/tx/test_fee_estimator.rs`
- `crates/z00z_wallets/tests/test_chain_client_sim.rs`
- `crates/z00z_wallets/tests/test_fee_rate_source.rs`
</read_first>
<files>
- `crates/z00z_wallets/src/tx/fee_estimator.rs`
- `crates/z00z_wallets/src/tx/test_fee_estimator.rs`
- `crates/z00z_wallets/tests/test_fee_rate_source.rs`
</files>
<action>
Execute `PLAN-062-G25` as the grouped closure packet for Simulated-live network fee-rate source.
Keep every included task row visible in execution artifacts, extend only the
live code/doc owners named in `current_code_refs`, and use local deterministic
simulation wherever the task contract permits `simulated-full` closure.
This plan must not create recommendation-only work, a second authority plane,
or a placeholder implementation lane.
</action>
<acceptance_criteria>
- `TASK-123` closes only when Fee estimator uses simulated live source with cache/fallback/stale/zero/spike tests; external network source remains adapter-only.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_wallets test_rates_ -- --nocapture`
   - `cargo test --release -p z00z_wallets spike_rate -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_fee_rate_source`
   - `cargo test --release -p z00z_wallets --test test_chain_client_sim`
   - `rg -n "fee source|cache_ttl|stale|zero|spike|fallback" crates/z00z_wallets/src/tx crates/z00z_wallets/src/chain`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
Fee estimator uses local simulated source with cache/fallback/stale/zero/spike tests and adapter seam for real network source.
</done>
</task>
</tasks>

<threat_model>
- Authority drift could accidentally create a second truth plane while trying to close the named gap.
- Placeholder drift could close with DTO, docs, or string changes that do not modify real behavior.
- Evidence drift could claim closure without local negative tests or simulator proof where the task explicitly requires it.
</threat_model>

<verification>
This slice is valid only if the listed commands, negative cases, evidence gate, and anti-placeholder gate all pass together.
</verification>

<success_criteria>
- Fee estimator uses local simulated source with cache/fallback/stale/zero/spike tests and adapter seam for real network source.
- All included task rows retain traceability to their source refs and current code anchors.
- Any remaining non-live boundary is explicitly justified as external-adapter-only.
</success_criteria>

<output>
Create `.planning/phases/062-Gaps-Closing-2/062-25-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/062-Gaps-Closing-2/062-25-SUMMARY.md`
- Group-level completion or blocker evidence for `PLAN-062-G25`
- Updated code, docs, config, tests, or simulator artifacts listed in `plan_artifacts`

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
|---|---|---|---|---|---|---|---|---|
| `TASK-123` | `PLAN-062-G25` | [local full-system simulation register](#local-full-system-simulation-closure-register) | crates/z00z_wallets/src/tx/fee_estimator.rs; crates/z00z_wallets/src/tx/test_fee_estimator.rs; crates/z00z_wallets/tests/test_fee_rate_source.rs | `cargo test --release -p z00z_wallets test_rates_ -- --nocapture`; `cargo test --release -p z00z_wallets spike_rate -- --nocapture`; `cargo test --release -p z00z_wallets --test test_fee_rate_source` | Fee estimator uses simulated live source with cache/fallback/stale/zero/spike tests; external network source remains adapter-only. | Mandatory local fee-source simulation over the real fee estimator. | changed code/docs/tests or explicit live-claim removal with negative proof | `Completed` |
