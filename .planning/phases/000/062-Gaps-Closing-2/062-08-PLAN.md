---
phase: 062-Gaps-Closing-2
plan: PLAN-062-G08
type: execute
wave: 2
depends_on: 
  - PLAN-062-G07
files_modified:
  - crates/z00z_wallets/src/receiver/request.rs
  - crates/z00z_wallets/src/services/wallet_actions_receive.rs
  - crates/z00z_wallets/src/receiver/request_inbox.rs
  - crates/z00z_wallets/tests/test_e2e_req_flow.rs
  - crates/z00z_wallets/tests/test_stealth_request.rs
requirements:
  - TASK-030
  - TASK-031
autonomous: true
must_haves:
  truths:
    - "Inbox feeds metadata only."
    - "Wallet-local request semantics are not transport privacy claims."
    - "Inbox consumes request validation, is implemented as non-authoritative/off-consensus, and cannot replace receive scan authority."
  artifacts:
    - crates/z00z_wallets/src/receiver/request.rs
    - crates/z00z_wallets/src/services/wallet_actions_receive.rs
    - crates/z00z_wallets/src/receiver/request_inbox.rs
    - crates/z00z_wallets/tests/test_e2e_req_flow.rs
    - crates/z00z_wallets/tests/test_stealth_request.rs
  key_links:
    - "Group scope: Request-bound inbox helper and inbox no-mutation/order behavior."
    - "Dependencies: PLAN-062-G07"
    - "Acceptance: Inbox consumes request validation, is implemented as non-authoritative/off-consensus, and cannot replace receive scan authority."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-062-G08 Plan: Request-Bound Inbox Helper

<objective>
Deliver the grouped closure slice for TASK-030, TASK-031: Request-bound inbox helper and inbox no-mutation/order behavior. The
implementation must preserve the exact task inventory, acceptance contract, and
evidence format from `062-TODO.md` while extending the current code or doc
owners named below.
</objective>

<context>
@.planning/phases/062-Gaps-Closing-2/062-TODO.md
@.planning/phases/062-Gaps-Closing-2/062-CONTEXT.md
@.planning/phases/062-Gaps-Closing-2/GAPS.md
@crates/z00z_wallets/src/receiver/request.rs
@crates/z00z_wallets/src/services/wallet_actions_receive.rs
@crates/z00z_wallets/src/receiver/request_inbox.rs
@crates/z00z_wallets/tests/test_e2e_req_flow.rs
@crates/z00z_wallets/tests/test_stealth_request.rs
</context>

<codebase_fit_note>
`crates/z00z_wallets/src/receiver/request_inbox.rs` and
`crates/z00z_wallets/tests/test_stealth_request.rs` are live workspace
artifacts. Keep them on the canonical request-validation plus wallet receive
path for `TASK-030` and `TASK-031`; do not create a second inbox seam.
</codebase_fit_note>

<coverage_contract>
This grouped plan covers TASK-030, TASK-031. It depends on PLAN-062-G07.
Acceptance for the group is: Inbox consumes request validation, is implemented as non-authoritative/off-consensus, and cannot replace receive scan authority.
No included task may close on placeholder logic, compile-only proof, or
docs-only proof for runtime behavior.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-062-G08`
task_ids:
- `TASK-030`
- `TASK-031`
copied_task_rows:
- "| TASK-030 | PLAN-062-T030 | [body](./GAPS.md#L1103), [reading map](./GAPS.md#L2478) | Request-bound inbox helper implementation/decision. | TASK-029 | Inbox helper consumes payment-request validation and remains non-authoritative/off-consensus. |"
- "| TASK-031 | PLAN-062-T031 | [body](./GAPS.md#L1138), [reading map](./GAPS.md#L2479) | Inbox ordering and no-mutation tests. | TASK-030 | Inbox ordering cannot replace scan authority or persistence. |"
source_refs:
- `TASK-030`: [body](./GAPS.md#L1103), [reading map](./GAPS.md#L2478)
- `TASK-031`: [body](./GAPS.md#L1138), [reading map](./GAPS.md#L2479)
inputs:
- `.planning/phases/062-Gaps-Closing-2/062-TODO.md` canonical task rows and group contract
- `.planning/phases/062-Gaps-Closing-2/GAPS.md`
- `crates/z00z_wallets/src/receiver/request.rs`
- `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- `crates/z00z_wallets/src/receiver/request_inbox.rs`
- `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
- `crates/z00z_wallets/tests/test_stealth_request.rs`
- dependency output from `PLAN-062-G07`
outputs:
- `.planning/phases/062-Gaps-Closing-2/062-08-SUMMARY.md` summary with completion/blocker evidence for `PLAN-062-G08`
- changed or created `crates/z00z_wallets/src/receiver/request.rs`
- changed or created `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- changed or created `crates/z00z_wallets/src/receiver/request_inbox.rs`
- changed or created `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
- changed or created `crates/z00z_wallets/tests/test_stealth_request.rs`
dependencies:
- `PLAN-062-G07`
acceptance_tests:
- `cargo test --release -p z00z_wallets --test test_stealth_request`
- `cargo test --release -p z00z_wallets --test test_e2e_req_flow`
- `cargo test --release -p z00z_wallets --test test_view_key_contract`
- `rg -n "request-bound|inbox|authoritative|off-consensus" crates/z00z_wallets/src/receiver crates/z00z_wallets/src/services`
simulation_gate:
- No external simulation; prove through live request and receive primitives.
negative_tests:
- Inbox hints must not replace scan authority.
- Expired or replayed requests must not mutate receive results.
</plan_matrix>

<plan_artifacts>
- artifact_id: `request`
  path_or_api: `crates/z00z_wallets/src/receiver/request.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-030, TASK-031]
- artifact_id: `wallet-actions-receive`
  path_or_api: `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-030, TASK-031]
- artifact_id: `request-inbox`
  path_or_api: `crates/z00z_wallets/src/receiver/request_inbox.rs`
  kind: `rust`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-030, TASK-031]
- artifact_id: `test-e2e-req-flow`
  path_or_api: `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
  kind: `test`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-030, TASK-031]
- artifact_id: `test-stealth-request`
  path_or_api: `crates/z00z_wallets/tests/test_stealth_request.rs`
  kind: `test`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-030, TASK-031]
- artifact_id: `plan-062-g08-summary`
  path_or_api: `.planning/phases/062-Gaps-Closing-2/062-08-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `modify-or-create`
  source_task_ids: [TASK-030, TASK-031]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_wallets --test test_stealth_request`
- `cargo test --release -p z00z_wallets --test test_e2e_req_flow`
- `cargo test --release -p z00z_wallets --test test_view_key_contract`
- `rg -n "request-bound|inbox|authoritative|off-consensus" crates/z00z_wallets/src/receiver crates/z00z_wallets/src/services`
</plan_tests>

<plan_results>
- `PLAN-062-G08` produces `.planning/phases/062-Gaps-Closing-2/062-08-SUMMARY.md` with exact TASK-075 completion or blocker fields.
- Group acceptance is proven only when the commands in `plan_tests` and the mandatory review loop complete cleanly.
- Evidence cites changed files, source refs, task ids, and any remaining adapter-only exclusions explicitly.
</plan_results>

<task_artifacts>
- task_id: `TASK-030`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_wallets/src/receiver/request.rs`
    - `crates/z00z_wallets/src/receiver/request_inbox.rs`
    - `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
    - `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
- task_id: `TASK-031`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
    - `crates/z00z_wallets/tests/test_stealth_request.rs`
    - `crates/z00z_wallets/src/receiver/request.rs`
    - `crates/z00z_wallets/src/receiver/request_inbox.rs`
</task_artifacts>

<task_tests>
- task_id: `TASK-030`
  commands:
    - `cargo test --release -p z00z_wallets --test test_stealth_request`
    - `cargo test --release -p z00z_wallets --test test_e2e_req_flow`
    - `cargo test --release -p z00z_wallets --test test_view_key_contract`
    - `rg -n "request-bound|inbox|authoritative|off-consensus" crates/z00z_wallets/src/receiver crates/z00z_wallets/src/services`
  scenarios:
    - `Request-bound inbox helper implementation/decision.`
    - `negative paths are proven with explicit fail-closed tests`
- task_id: `TASK-031`
  commands:
    - `cargo test --release -p z00z_wallets --test test_stealth_request`
    - `cargo test --release -p z00z_wallets --test test_e2e_req_flow`
    - `cargo test --release -p z00z_wallets --test test_view_key_contract`
    - `rg -n "request-bound|inbox|authoritative|off-consensus" crates/z00z_wallets/src/receiver crates/z00z_wallets/src/services`
  scenarios:
    - `Inbox ordering and no-mutation tests.`
    - `negative paths are proven with explicit fail-closed tests`
</task_tests>

<task_results>
- task_id: `TASK-030`
  expected_results:
    - `Inbox helper consumes payment-request validation and remains non-authoritative/off-consensus.`
    - `anti-placeholder evidence includes changed files plus passing or fail-closed tests; compile-only proof is invalid.`
- task_id: `TASK-031`
  expected_results:
    - `Inbox ordering cannot replace scan authority or persistence.`
    - `anti-placeholder evidence includes changed files plus passing or fail-closed tests; compile-only proof is invalid.`
</task_results>

<anti_placeholder_gate>
- Every included task must change or create the code, docs, config, tests, or simulator artifacts named in `task_artifacts`, or explicitly remove a live claim when `implementation_depth` is `live-claim-removed`.
- `TODO`, `panic!`, string-only errors, empty wrappers, compile-only proof, or doc-only claims do not satisfy this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_wallets/src/receiver/request.rs`
- `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- `crates/z00z_wallets/src/receiver/request_inbox.rs`
- `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
- `crates/z00z_wallets/tests/test_stealth_request.rs`
</current_code_refs>

<blockers>
- Use the exact TASK-075 blocker template when a command or local simulator proof cannot close a task.
- Only external transport, remote deployment, or third-party network adapters may remain adapter-only after local deterministic simulation is implemented.
- Any missing task mapping, missing evidence field, or placeholder-only closure is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence cites the exact task ids, changed files, test commands, simulator outputs when relevant, and remaining adapter-only exclusions.
- If a task cannot be closed locally, record a blocker with the exact TASK-075 fields and treat the group as phase-failing until resolved or explicitly classified as external-adapter-only.
</evidence_gate>

<not_recommendation_gate>
- This plan is executable scope, not a recommendation list.
- Any item that is still future-only after execution must be justified by the normative task row as `live-claim-removed` or as an external-adapter-only exclusion after local simulator closure.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Request-Bound Inbox Helper</name>
<read_first>
- `.planning/phases/062-Gaps-Closing-2/062-TODO.md`
- `.planning/phases/062-Gaps-Closing-2/062-CONTEXT.md`
- `.planning/phases/062-Gaps-Closing-2/GAPS.md`
- `crates/z00z_wallets/src/receiver/request.rs`
- `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- `crates/z00z_wallets/src/receiver/request_inbox.rs`
- `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
- `crates/z00z_wallets/tests/test_stealth_request.rs`
</read_first>
<files>
- `crates/z00z_wallets/src/receiver/request.rs`
- `crates/z00z_wallets/src/services/wallet_actions_receive.rs`
- `crates/z00z_wallets/src/receiver/request_inbox.rs`
- `crates/z00z_wallets/tests/test_e2e_req_flow.rs`
- `crates/z00z_wallets/tests/test_stealth_request.rs`
</files>
<action>
Execute `PLAN-062-G08` as the grouped closure packet for Request-bound inbox helper and inbox no-mutation/order behavior.
Keep every included task row visible in execution artifacts, extend only the
live code/doc owners named in `current_code_refs`, and use local deterministic
simulation wherever the task contract permits `simulated-full` closure.
This plan must not create recommendation-only work, a second authority plane,
or a placeholder implementation lane.
</action>
<acceptance_criteria>
- `TASK-030` closes only when Inbox helper consumes payment-request validation and remains non-authoritative/off-consensus.
- `TASK-031` closes only when Inbox ordering cannot replace scan authority or persistence.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_wallets --test test_stealth_request`
   - `cargo test --release -p z00z_wallets --test test_e2e_req_flow`
   - `cargo test --release -p z00z_wallets --test test_view_key_contract`
   - `rg -n "request-bound|inbox|authoritative|off-consensus" crates/z00z_wallets/src/receiver crates/z00z_wallets/src/services`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
Inbox consumes request validation, is implemented as non-authoritative/off-consensus, and cannot replace receive scan authority.
</done>
</task>
</tasks>

<threat_model>
- Authority drift could accidentally create a second truth plane while trying to close the named gap.
- Placeholder drift could close with DTO, docs, or string changes that do not modify real behavior.
- Evidence drift could claim closure without local negative tests or simulator proof where the task explicitly requires it.
</threat_model>

<verification>
This slice is valid only if the listed commands, negative cases, evidence gate, and anti-placeholder gate all pass together.
</verification>

<success_criteria>
- Inbox consumes request validation, is implemented as non-authoritative/off-consensus, and cannot replace receive scan authority.
- All included task rows retain traceability to their source refs and current code anchors.
- Any remaining non-live boundary is explicitly justified as external-adapter-only.
</success_criteria>

<output>
Create `.planning/phases/062-Gaps-Closing-2/062-08-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/062-Gaps-Closing-2/062-08-SUMMARY.md`
- Group-level completion or blocker evidence for `PLAN-062-G08`
- Updated code, docs, config, tests, or simulator artifacts listed in `plan_artifacts`

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
|---|---|---|---|---|---|---|---|---|
| `TASK-030` | `PLAN-062-G08` | [body](./GAPS.md#L1103), [reading map](./GAPS.md#L2478) | crates/z00z_wallets/src/receiver/request.rs; crates/z00z_wallets/src/receiver/request_inbox.rs; crates/z00z_wallets/src/services/wallet_actions_receive.rs; crates/z00z_wallets/tests/test_e2e_req_flow.rs | `cargo test --release -p z00z_wallets --test test_stealth_request`; `cargo test --release -p z00z_wallets --test test_e2e_req_flow` | Inbox helper consumes payment-request validation and remains non-authoritative/off-consensus. | No external simulation; prove through live request and receive primitives. | changed code/docs/tests or explicit live-claim removal with negative proof | `Completed` |
| `TASK-031` | `PLAN-062-G08` | [body](./GAPS.md#L1138), [reading map](./GAPS.md#L2479) | crates/z00z_wallets/tests/test_e2e_req_flow.rs; crates/z00z_wallets/tests/test_stealth_request.rs; crates/z00z_wallets/src/receiver/request.rs; crates/z00z_wallets/src/receiver/request_inbox.rs | `cargo test --release -p z00z_wallets --test test_stealth_request`; `cargo test --release -p z00z_wallets --test test_e2e_req_flow` | Inbox ordering cannot replace scan authority or persistence. | No external simulation; prove through live request and receive primitives. | changed code/docs/tests or explicit live-claim removal with negative proof | `Completed` |
