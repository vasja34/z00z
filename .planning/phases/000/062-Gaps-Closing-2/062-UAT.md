---
status: testing
phase: 062-Gaps-Closing-2
source:
  - 062-TODO.md
  - 062-CONTEXT.md
  - 062-VALIDATION.md
  - 062-TEST-SPEC.md
  - 062-TESTS-TASKS.md
  - 062-05-SUMMARY.md
  - 062-07-SUMMARY.md
  - 062-08-SUMMARY.md
  - 062-11-SUMMARY.md
  - 062-12-SUMMARY.md
  - 062-16-SUMMARY.md
  - 062-17-SUMMARY.md
  - 062-18-SUMMARY.md
  - 062-19-SUMMARY.md
  - 062-20-SUMMARY.md
  - 062-21-SUMMARY.md
  - 062-23-SUMMARY.md
  - 062-24-SUMMARY.md
  - 062-25-SUMMARY.md
  - 062-26-SUMMARY.md
  - 062-27-SUMMARY.md
started: 2026-06-27T07:43:37+03:00
updated: 2026-06-27T07:43:37+03:00
---

# Phase 062 UAT

This UAT session covers the live-scope closeout for Phase 062 on the current
workspace tree.

The checklist is intentionally release-oriented: canonical packet truth,
wallet lifecycle and receive authority, object and rights boundaries, HJMT and
thin-mode closeout, and the local simulated node-facing wallet behavior must
all hold on one canonical path with no future-only escape hatches left as live
truth.

## Current Test

number: 1
name: Phase Packet And Canonical Path Truth
expected: |
  Inspect the Phase 062 packet or run the packet-truth checks directly.

  `rg -o "TASK-[0-9]{3}" .planning/phases/062-Gaps-Closing-2/062-TODO.md | sort -u | wc -l`
  should still report `125`, and
  `rg -o "PLAN-062-G[0-9]{2}" .planning/phases/062-Gaps-Closing-2/062-TODO.md | sort -u | wc -l`
  should still report `27`.

  Then run the canonical-path grep from the packet:
  `rg -n "request_inbox.rs|manifest_ref_loader.rs|dist_sim.rs|consensus_adapter.rs|dist_dispatch.rs|dist_scheduler.rs|thin_types.rs|thin_snapshot.rs|thin_cache.rs|thin_builder.rs|local_node_sim.rs|test_wallet_policy.rs|RuntimeReceiveScanOutcome" .planning/phases/062-Gaps-Closing-2/062-TEST-SPEC.md .planning/phases/062-Gaps-Closing-2/062-TESTS-TASKS.md`

  The packet should still point at the live module and test names, and no
  stale alternate path, missing string, or future-only alias should remain as
  live truth for Phase 062.
awaiting: user response

## Tests

### 1. Phase Packet And Canonical Path Truth

expected: Inspect the Phase 062 packet or run the packet-truth checks directly. `062-TODO.md` should still report `125` unique `TASK-NNN` ids and `27` grouped `PLAN-062-GNN` ids, and the canonical-path grep in `062-TEST-SPEC.md` plus `062-TESTS-TASKS.md` should still point at the live module names such as `request_inbox.rs`, `manifest_ref_loader.rs`, `dist_sim.rs`, `thin_types.rs`, `local_node_sim.rs`, `test_wallet_policy.rs`, and `RuntimeReceiveScanOutcome`. No stale alternate path, missing string, or future-only alias should remain as live truth.
result: [pending]

### 2. Wallet Lifecycle And Durable History

expected: Run `cargo test --release -p z00z_wallets --test test_tx_store_integration`, `cargo test --release -p z00z_wallets test_tx_impl_suite:: -- --nocapture`, or inspect a real wallet send/import flow. Lifecycle and status fields should project from durable persisted state, history should survive restart, and the current view should preserve row ordering, fold semantics, tombstones, and final status instead of collapsing back to an in-memory pending list.
result: [pending]

### 3. Typed Verify, Import, And Unsupported-Version Failures

expected: Run `cargo test --release -p z00z_wallets --test test_import_error_taxonomy` and the related verify/import negative paths, or inspect invalid package handling manually. Unsupported versions, parse failures, verify-only failures, and import conflicts should return stable typed failures and should not mutate wallet state before admission succeeds.
result: [pending]

### 4. Receive Scan Outcome And Remote Worker Boundaries

expected: Run `cargo test --release -p z00z_wallets --test test_remote_scan_worker` or inspect a real receive scan path. `RuntimeReceiveScanOutcome` and `last_receive_outcome` should report truthful receive status, scan cursor persistence should remain restart-safe, and invalid, stale, or malicious worker evidence must not mutate assets, scan state, or final receive results before full local validation.
result: [pending]

### 5. Request Inbox Helper Stays Metadata Only

expected: Run `cargo test --release -p z00z_wallets --test test_e2e_req_flow` or inspect a request-driven receive flow. Request-bound inbox metadata should be useful for ordering and UX, but it must remain non-authoritative, off-consensus, and unable to replace or mutate the authoritative receive scan result on its own.
result: [pending]

### 6. Privacy, Reveal, Backup, And Logging Hygiene

expected: Run `cargo test --release -p z00z_wallets --test test_rpc_logging_risk_policy`, `cargo test --release -p z00z_wallets --test test_backup_metadata_policy`, and `cargo test --release -p z00z_wallets --test test_view_key_contract`, or inspect emitted logs and backup metadata. Sensitive values should stay redacted, reveal claims should remain bounded to wallet-local surfaces, and no backup, export, request, or logging path should overclaim transport anonymity or leak secret material.
result: [pending]

### 7. Cash, Object, Voucher, And Right Boundaries

expected: Run `cargo test --release -p z00z_validators --test test_object_policy_verdicts` and `cargo test --release -p z00z_wallets test_asset_impl -- --nocapture`, or inspect a real object-wallet flow. Cash inventory should stay on `wallet.asset.*`, object, voucher, and right inventory should stay on `wallet.object.*`, vouchers or rights must not become spendable cash by accident, and missing-right or unknown-policy paths must fail closed.
result: [pending]

### 8. Local HJMT And Distributed Simulator Truth

expected: Run `cargo test --release -p z00z_simulator --test scenario_1 test_hjmt_e2e:: -- --nocapture`, `cargo test --release -p z00z_aggregators --test test_hjmt_dist_journal`, and `cargo test --release -p z00z_aggregators --test test_hjmt_dispatch`, or inspect the local simulator evidence. Route, proof, journal, publication, quorum, standby catch-up, rollout, and dispatch behavior should execute on real project primitives inside the local simulator, and no future-only wording should be required to explain the live behavior.
result: [pending]

### 9. Thin Signed-Index Helper And Safe Fallback

expected: Run `cargo test --release -p z00z_wallets --test test_thin_index`, `cargo test --release -p z00z_wallets --test test_thin_cache`, and `cargo test --release -p z00z_wallets --test test_thin_fallback`, or inspect a thin-package flow. Thin snapshots should stay authenticated helper data bound to canonical roots and packages, expand before runtime admission, and fail closed or fall back to thick mode safely on stale, wrong, expired, withheld, or equivocated helper data.
result: [pending]

### 10. Local Node ChainClient, Broadcast, Fee Source, And Spend Policy

expected: Run `cargo test --release -p z00z_wallets --test test_chain_client_sim`, `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry`, `cargo test --release -p z00z_wallets --test test_fee_rate_source`, and `cargo test --release -p z00z_wallets --test test_wallet_policy`, or inspect the local node-sim wallet flow. Tip, block, header, submit, status, and network-info paths should work through the local node simulation, broadcast retries and confirmations should persist into durable tx history, fee sourcing should cache and fail closed on bad data, and daily-spend plus confirmation policy enforcement should stay restart-safe and reject overspend or premature spend attempts.
result: [pending]

## Summary

total: 10
passed: 0
issues: 0
pending: 10
skipped: 0
blocked: 0

## Gaps

None yet.
