---
phase: 018-a-b-c
plan: 02
type: execute
wave: 2
depends_on:
  - 018-01
files_modified:
  - proposed: crates/z00z_simulator/src/scenario_1/jmt_wallet_scan.rs
  - crates/z00z_simulator/src/scenario_1/stage_7.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - proposed: crates/z00z_simulator/tests/test_stage7_jmt_wallet_scan.rs
  - crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
autonomous: true
requirements:
  - SCN1-03
  - SCN1-04
must_haves:
  truths:
    - Wallet-side discovery after Stage 7 is driven by proof-validated committed-state enumeration, not detached leaf-only scanning.
    - Charlie wallet runtime is updated only after canonical post-apply JMT verification succeeds.
    - Lifecycle evidence is refreshed from the committed post-apply state so `pending -> confirmed` rows can be tied to the JMT scan path.
    - Refreshed wallet evidence is checked by one explicit wallet-balance invariant gate so post-apply totals are proven coherent at scenario level.
    - Acceptance output must include one machine-readable report that makes the `leaf scan` versus `JMT scan` distinction explicit for this phase rather than leaving it implicit in code or logs.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/jmt_wallet_scan.rs
      status: proposed
      provides: Post-apply proof-validated JMT wallet scan helper.
    - path: crates/z00z_simulator/src/scenario_1/stage_7.rs
      provides: Charlie runtime update, committed-state wallet evidence refresh, and scenario-level wallet invariant gate after Stage 7 apply.
    - path: crates/z00z_simulator/tests/test_stage7_jmt_wallet_scan.rs
      status: proposed
      provides: Regression coverage for proof-validated JMT scan and Charlie path completion.
    - path: proposed outputs/transactions/charlie_jmt_scan.json
      status: proposed
      provides: Machine-readable Charlie scan evidence that ties committed-row proof verification, ownership detection, and the `leaf scan` versus `JMT scan` distinction into one acceptance artifact.
  key_links:
    - from: proposed crates/z00z_simulator/src/scenario_1/jmt_wallet_scan.rs
      to: crates/z00z_storage/src/assets/store.rs
      via: committed row enumeration and proof validation
      pattern: list|proof_blob|chk_blob
    - from: crates/z00z_simulator/src/scenario_1/stage_7.rs
      to: proposed crates/z00z_simulator/src/scenario_1/jmt_wallet_scan.rs
      via: post-apply Charlie receive and wallet evidence refresh
      pattern: export_post_tx_view|charlie|wallets_state_diff
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Add one proof-validated JMT wallet scan path and complete the Charlie runtime story.

Purpose: close the acceptance gap where `scenario_1` proves only detached ownership hints or bridge fragments, but not wallet-side inclusion of committed post-apply state for Charlie.
Output: one reusable JMT wallet scan helper, real Charlie runtime update after Stage 7 apply, and refreshed wallet evidence artifacts driven by committed-state verification.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/018-a-b-c/018-CONTEXT.md
@.planning/phases/018-a-b-c/018-RESEARCH.md
@.planning/phases/018-a-b-c/018-01-PLAN.md
@crates/z00z_simulator/src/scenario_1/stage_5.rs
@crates/z00z_simulator/src/scenario_1/stage_7.rs
@crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/assets/proof.rs
@crates/z00z_storage/src/assets/store_internal/proof_help.rs
@crates/z00z_wallets/src/core/tx/state_update.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement one proof-validated JMT wallet scan helper over committed post-tx state</name>
  <files>crates/z00z_simulator/src/scenario_1/jmt_wallet_scan.rs, crates/z00z_simulator/tests/test_stage7_jmt_wallet_scan.rs</files>
  <behavior>
    - Test 1: the helper loads committed post-apply rows, builds and validates inclusion proofs, and only then performs ownership detection.
    - Test 2: detached leaf-only ownership without committed-state proof is rejected as insufficient for this path.
  </behavior>
  <action>Per D-04 through D-06 and D-18, add one simulator-local helper that loads the persisted `post_tx` store, enumerates canonical rows, validates each candidate with `proof_blob(...)` and `chk_blob(...)`, and then runs stealth ownership detection on proof-validated rows only. Do not parse observational JMT export files directly and do not create actor-specific scan implementations.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage7_jmt_wallet_scan -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage7_jmt_wallet_scan -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/018-a-b-c/018-02-PLAN.md current_task="Task 1: Implement one proof-validated JMT wallet scan helper over committed post-tx state"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>The phase has one committed-state JMT wallet scan helper that proves inclusion before ownership detection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Charlie runtime update and lifecycle evidence refresh into Stage 7</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_7.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_stage7_jmt_wallet_scan.rs, crates/z00z_simulator/tests/test_scenario1_unified_gate.rs</files>
  <behavior>
    - Test 1: after canonical Stage 7 apply, Charlie wallet runtime changes through the proof-validated JMT scan path and is visible in the standard wallet evidence reports.
    - Test 2: refreshed `pending -> confirmed` evidence can be traced to the committed post-apply scan rather than only to Stage 4 or Stage 5 overlays.
    - Test 3: refreshed wallet before/after or confirmed evidence passes one explicit wallet-balance invariant gate after the committed JMT-driven update.
    - Test 4: one machine-readable acceptance artifact explains that this path used committed-store proof verification before ownership detection and therefore is not equivalent to the detached leaf-scan path from Stage 5.
  </behavior>
  <action>Per D-07 through D-09 and D-16 through D-19, call the JMT scan helper after Stage 7 canonical apply, update Charlie runtime state only from proof-validated rows, refresh the standard before/after/diff/report artifacts so the acceptance path shows the full `Alice -> Bob -> Charlie` story on artifacts, add one scenario-level wallet-balance invariant gate over the refreshed evidence, and write one machine-readable scan artifact such as `outputs/transactions/charlie_jmt_scan.json` that explicitly states the committed-row proof verification step and the `leaf scan` versus `JMT scan` distinction. Reuse the existing Stage 4 balance and wallet diff/report helpers where possible. Keep Stage 6 as a bridge and do not move Charlie update logic backward.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage7_jmt_wallet_scan -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage7_jmt_wallet_scan -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/018-a-b-c/018-02-PLAN.md current_task="Task 2: Wire Charlie runtime update and lifecycle evidence refresh into Stage 7"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>Charlie runtime and wallet evidence are refreshed from committed post-apply JMT state, one explicit wallet-balance invariant gate proves coherent post-apply totals, and one machine-readable scan artifact makes the proof-validated JMT path distinct from detached leaf scanning in the artifact-backed full-path story.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_stage7_jmt_wallet_scan -- --nocapture`, `cargo test -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture`, the matching `--release --features test-fast --features wallet_debug_dump` commands, and `cargo test --release --features test-fast --features wallet_debug_dump`. For each auto task, run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue fixing until 2 consecutive runs report no significant issues.
</verification>

<success_criteria>
Phase 018 proves wallet-side inclusion and ownership for committed post-apply rows, shows Charlie runtime change in the standard Scenario 1 artifact surface, enforces one scenario-level wallet-balance invariant over the refreshed evidence, and makes the `leaf scan` versus `JMT scan` distinction explicit in acceptance output.
</success_criteria>

<output>
After completion, create `.planning/phases/018-a-b-c/018-02-SUMMARY.md`
</output>