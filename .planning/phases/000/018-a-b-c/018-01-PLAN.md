---
phase: 018-a-b-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_4.rs
  - crates/z00z_simulator/src/scenario_1/storage_view.rs
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - proposed: crates/z00z_simulator/tests/test_stage4_chain_path.rs
  - crates/z00z_simulator/tests/test_stage4_root_support.rs
autonomous: true
requirements:
  - SCN1-03
  - SCN1-04
must_haves:
  truths:
    - Stage 4 pre-state continuity is derived from the full claim-backed live store rather than a selected-input subset store.
    - The phase emits one canonical ledger continuity artifact that ties claim, prep, and post-apply roots into one machine-readable path.
    - Observational exports remain inspection-only and do not become the source of truth for canonical chain continuity.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_4.rs
      provides: Canonical Stage 4 continuity built from the full claim-backed store.
    - path: crates/z00z_simulator/src/scenario_1/storage_view.rs
      provides: Explicit ledger continuity artifact export surface.
    - path: proposed crates/z00z_simulator/src/scenario_1/outputs/storage/ledger_path.json
      status: proposed
      provides: Canonical machine-readable claim-to-prep-to-post-apply continuity artifact.
    - path: crates/z00z_simulator/tests/test_stage4_chain_path.rs
      status: proposed
      provides: Regression coverage for claim-to-prep continuity and ledger-path output.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_4.rs
      to: crates/z00z_simulator/src/claim_pkg_consumer.rs
      via: full claim-backed store root continuity
      pattern: publish_claims_store|PrepSnapshot|prev_root_hex
    - from: crates/z00z_simulator/src/scenario_1/storage_view.rs
      to: crates/z00z_simulator/src/scenario_1/stage_7.rs
      via: claim root to post-apply root continuity artifact
      pattern: claim_post|pre_tx|post_tx|ledger_path
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Repair Stage 4 root continuity and emit one canonical ledger-path artifact.

Purpose: close the structural gap where `claim_post` and `pre_tx` are generated from different root stories, preventing the simulator from proving one continuous canonical chain path.
Output: Stage 4 continuity rebased on the full claim-backed store plus one machine-readable ledger-path artifact tying claim, prep, and post-apply roots together.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/018-a-b-c/018-CONTEXT.md
@.planning/phases/018-a-b-c/018-RESEARCH.md
@.planning/phases/017-scenario-1/017-CONTEXT.md
@crates/z00z_simulator/src/scenario_1/stage_4.rs
@crates/z00z_simulator/src/scenario_1/storage_view.rs
@crates/z00z_simulator/src/claim_pkg_consumer.rs
@crates/z00z_storage/src/assets/store.rs
@crates/z00z_storage/src/snapshot/store.rs

<interfaces>
From crates/z00z_simulator/src/scenario_1/stage_4.rs:
```rust
fn build_canon_snapshot(prep: &PrepFile) -> Result<(PrepSnapshot, PrepSnapshotId), String>;
```

From crates/z00z_storage/src/assets/store.rs:
```rust
pub fn root(&self) -> Result<AssetStateRoot, AssetStoreError>;
pub fn proof_blob(&self, path: &AssetPath) -> Result<ProofBlob, AssetStoreError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebase Stage 4 continuity on the full claim-backed store</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_4.rs, crates/z00z_simulator/tests/test_stage4_root_support.rs, crates/z00z_simulator/tests/test_stage4_chain_path.rs</files>
  <behavior>
    - Test 1: Stage 4 derives `prep.prev_root_hex` and selected witness rows from the full claim-backed store instead of a selected-input subset store.
    - Test 2: root continuity fails closed when `claim_post` and Stage 4 prep roots drift.
  </behavior>
  <action>Per D-01 through D-03, remove the subset-root masquerading path from Stage 4 and derive continuity from the same full claim-backed live store that underlies claim publication. Keep the selected rows as execution inputs, but make the root story canonical and global. Extend current Stage 4 support tests instead of adding a second prep pipeline.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage4_root_support -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_stage4_chain_path -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage4_root_support -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage4_chain_path -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/018-a-b-c/018-01-PLAN.md current_task="Task 1: Rebase Stage 4 continuity on the full claim-backed store"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>Stage 4 proves continuity from the full claim-backed store and rejects continuity drift automatically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit one canonical ledger-path artifact for claim to post-apply continuity</name>
  <files>crates/z00z_simulator/src/scenario_1/storage_view.rs, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_stage4_chain_path.rs</files>
  <behavior>
    - Test 1: one machine-readable artifact such as proposed `outputs/storage/ledger_path.json` records claim root, prep root, post-apply root, draft id, exec input id, and checkpoint id when present.
    - Test 2: observational exports remain unchanged as inspection surfaces and are not treated as the canonical continuity artifact.
  </behavior>
  <action>Per D-02, D-03, and D-15, add a deterministic continuity export at one explicit path such as proposed `crates/z00z_simulator/src/scenario_1/outputs/storage/ledger_path.json` that records the chain continuity explicitly instead of forcing acceptance logic to infer it from separate `claim_post`, `pre_tx`, and `post_tx` summaries. Keep the new artifact separate from existing observational view summaries and treat the exact filename as proposed until implemented.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage4_chain_path -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage4_chain_path -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/018-a-b-c/018-01-PLAN.md current_task="Task 2: Emit one canonical ledger-path artifact for claim to post-apply continuity"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>The scenario exports one canonical ledger-path artifact that downstream acceptance and finalization logic can reference directly.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_stage4_root_support -- --nocapture`, `cargo test -p z00z_simulator --test test_stage4_chain_path -- --nocapture`, the matching `--release --features test-fast --features wallet_debug_dump` commands, and `cargo test --release --features test-fast --features wallet_debug_dump`. For each auto task, run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue fixing until 2 consecutive runs report no significant issues.
</verification>

<success_criteria>
Phase 018 has one explicit claim-to-prep-to-post-apply continuity path rooted in the full claim-backed store rather than inferred from separate inspection artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/018-a-b-c/018-01-SUMMARY.md`
</output>