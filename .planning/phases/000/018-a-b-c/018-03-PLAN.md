---
phase: 018-a-b-c
plan: 03
type: execute
wave: 3
depends_on:
  - 018-01
  - 018-02
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_8.rs
  - crates/z00z_simulator/src/scenario_1/stage_6.rs
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
  - crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
  - proposed: crates/z00z_simulator/tests/test_stage8_proof_path.rs
autonomous: true
requirements:
  - SCN1-04
  - SCN1-05
must_haves:
  truths:
    - Stage 8 acceptance includes one finalized `OpaqueTest` lane with concrete artifact, link, and audit surfaces.
    - The final checkpoint summary records `checkpoint_id_hex` plus proposed sealed artifact, link, and audit path fields when finalization succeeds.
    - Proof-bearing transaction flow is covered explicitly across Stage 6 exec input, Stage 7 draft apply, and Stage 8 final publication.
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_8.rs
      provides: Finalized checkpoint path reporting with artifact, link, and audit surfaces.
    - path: crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
      provides: Final gate coverage for draft-only versus finalized checkpoint output behavior.
    - path: crates/z00z_simulator/tests/test_stage8_proof_path.rs
      status: proposed
      provides: Explicit propagation and binding coverage for Stage 6 to Stage 8 proof path.
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_6.rs
      to: crates/z00z_simulator/src/scenario_1/stage_8.rs
      via: exec-input proof path into finalized checkpoint publication
      pattern: build_cp_proof|exec_input_id_hex|checkpoint_id_hex
    - from: crates/z00z_simulator/src/scenario_1/stage_8.rs
      to: crates/z00z_storage/src/checkpoint/store.rs
      via: artifact, link, and audit persistence
      pattern: seal_artifact|save_link|save_audit
---

<!-- markdownlint-disable MD033 MD041 MD031 MD040 MD018 MD046 MD047 -->

<objective>
Finalize the checkpoint proof path and release one full acceptance lane.

Purpose: close the remaining proof and artifact gaps by turning Stage 8 from `draft_only` evidence into one finalized acceptance path with explicit artifact, link, audit, and checkpoint id surfaces.
Output: finalized Stage 8 summary reporting, explicit proof propagation coverage, and one end-to-end acceptance lane that proves the phase on artifacts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/018-a-b-c/018-CONTEXT.md
@.planning/phases/018-a-b-c/018-RESEARCH.md
@.planning/phases/018-a-b-c/018-01-PLAN.md
@.planning/phases/018-a-b-c/018-02-PLAN.md
@crates/z00z_simulator/src/scenario_1/stage_6.rs
@crates/z00z_simulator/src/scenario_1/stage_8.rs
@crates/z00z_storage/src/checkpoint/store.rs
@crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs
@crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose finalized Stage 8 artifact, link, and audit surfaces in summary outputs</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_8.rs, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/tests/test_stage6_checkpoint_final_gate.rs</files>
  <behavior>
    - Test 1: Stage 8 keeps `draft_only` behavior for non-finalizing lanes, but finalized `OpaqueTest` lanes expose `checkpoint_id_hex` plus proposed summary fields such as `artifact_path`, `link_path`, and `audit_path`.
    - Test 2: final and draft outputs remain distinct and cannot be confused as interchangeable sources of truth.
  </behavior>
  <action>Per D-13 through D-15, extend `Stage8Summary` in `crates/z00z_simulator/src/scenario_1/stage_8.rs` so a successful finalized lane records `checkpoint_id_hex` plus proposed concrete path fields for the sealed artifact, link, and audit outputs. Keep draft and final surfaces separate and preserve `draft_only` for negative or bridge gates. If the exact field names differ during implementation, they must remain explicit and machine-readable in the Stage 8 summary contract.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/018-a-b-c/018-03-PLAN.md current_task="Task 1: Expose finalized Stage 8 artifact, link, and audit surfaces in summary outputs"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>Stage 8 can prove a finalized checkpoint path on artifacts instead of stopping at `draft_only`.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit proof-path and end-to-end acceptance coverage</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_6.rs, crates/z00z_simulator/tests/test_stage8_proof_path.rs, crates/z00z_simulator/tests/test_scenario1_unified_gate.rs</files>
  <behavior>
    - Test 1: proof-bearing transaction material is propagated and bound consistently across Stage 6 exec input, Stage 7 draft apply, and Stage 8 final artifact publication.
    - Test 2: the unified end-to-end gate proves claim continuity, Charlie JMT wallet scan, wallet-balance invariant success, and finalized checkpoint publication together.
  </behavior>
  <action>Per D-10 through D-12 and D-19, add explicit proof propagation and binding coverage rather than widening cryptographic scope. Reuse the existing checkpoint proof path and make the unified gate prove the complete `claim -> prep -> apply -> Charlie JMT scan -> wallet invariant -> finalize` lane on one acceptance surface.</action>
  <verify>
    <automated>cargo test -p z00z_simulator --test test_stage8_proof_path -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_stage8_proof_path -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/018-a-b-c/018-03-PLAN.md current_task="Task 2: Add explicit proof-path and end-to-end acceptance coverage"` in YOLO mode at least 3 times, fix all issues and warnings after each pass, and stop only after at least 2 consecutive review runs report no significant issues.
  </verify>
  <done>The phase has one explicit proof-path regression suite and one unified end-to-end acceptance lane that proves the full artifact story, including the wallet-balance invariant gate required by Phase 018 acceptance.</done>
</task>

</tasks>

<verification>
Run `cargo test -p z00z_simulator --test test_stage6_checkpoint_final_gate -- --nocapture`, `cargo test -p z00z_simulator --test test_stage8_proof_path -- --nocapture`, `cargo test -p z00z_simulator --test test_scenario1_unified_gate -- --nocapture`, the matching `--release --features test-fast --features wallet_debug_dump` commands, and `cargo test --release --features test-fast --features wallet_debug_dump`. For each auto task, run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times and continue fixing until 2 consecutive runs report no significant issues.
</verification>

<success_criteria>
Phase 018 proves one finalized checkpoint lane with explicit proof-path binding and a complete end-to-end artifact story from claim continuity through Charlie JMT wallet scan, wallet-balance invariant success, and sealed checkpoint publication.
</success_criteria>

<output>
After completion, create `.planning/phases/018-a-b-c/018-03-SUMMARY.md`
</output>