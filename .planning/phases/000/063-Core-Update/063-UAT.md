---
status: verified
phase: 063-Core-Update
source: [063-VALIDATION.md, 063-SECURITY.md, 063-01-SUMMARY.md, 063-02-SUMMARY.md, 063-03-SUMMARY.md, 063-04-SUMMARY.md, 063-05-SUMMARY.md, 063-06-SUMMARY.md, 063-07-SUMMARY.md, 063-08-SUMMARY.md, 063-09-SUMMARY.md, 063-10-SUMMARY.md, 063-11-SUMMARY.md, 063-12-SUMMARY.md, 063-13-SUMMARY.md]
started: 2026-06-29T00:00:00Z
updated: 2026-06-29T15:07:29+03:00
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 9
name: UAT Complete
expected: |
  The Phase 063 live packet is proven by release-mode tests, scans, docs, and
  support-surface build gates with no pending UAT gaps.
awaiting: none

## Tests

### 1. Full Genesis Bootstrap Contract
expected: Starting from the canonical Phase 063 configuration roots, one full bootstrap path completes through the public genesis surface, keeps the documented threading and export controls live, and emits the typed artifacts and manifest outputs that the current docs describe.
result: [passed]
evidence: `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`; `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`; `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_full_bootstrap_matches_legacy_run -- --nocapture`

### 2. Canonical Genesis Public Surface
expected: Public callers use one canonical genesis module path with no duplicate nested `genesis::genesis` ownership path, and downstream crates compile against that same surface.
result: [passed]
evidence: `cargo test --release -p z00z_core --test test_live_guardrails -- --nocapture`; `rg -n "include!\\(|z00z_core::genesis::genesis::" crates/z00z_core/src/genesis crates/z00z_core/tests crates/z00z_core/docs crates/z00z_core/README.md`

### 3. Docs And Registry Truth
expected: Core README and docs describe only the current asset and genesis surfaces, and live drift guards catch stale names, stale file paths, and unsupported capability claims.
result: [passed]
evidence: `cargo test --release -p z00z_core --test test_live_guardrails -- --nocapture`; `cargo test --release -p z00z_core --test assets_tests config_integration -- --nocapture`

### 4. Single Asset Bootstrap Authority
expected: Asset definitions load through one canonical YAML-to-registry path, runtime callers observe the same catalog, and docs and examples point to that same live path.
result: [passed]
evidence: `cargo test --release -p z00z_core --test assets_tests config_integration -- --nocapture`; `cargo test --release -p z00z_core --test assets_tests rights_config::test_rights_config_loads -- --nocapture`

### 5. Selective Genesis Lanes
expected: Full bootstrap remains the only end-to-end path, while assets-only, rights-only, and vouchers-only lane runs each stay scoped to their own outputs and still enforce the documented safety checks.
result: [passed]
evidence: `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_partial_run_does_not_emit_full_settlement_manifest -- --nocapture`; `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_selected_lanes_preserve_terminal_collision_checks -- --nocapture`

### 6. Voucher And Object-Family Semantics
expected: `vouchers` is the only live voucher namespace, wallet object APIs keep cash, rights, vouchers, and capabilities in their correct families, and bootstrap-only voucher rows do not leak into runtime cash inventory.
result: [passed]
evidence: `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`; `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`; `rg -n "\\bvauchers\\b" crates/z00z_core crates/z00z_wallets crates/z00z_storage crates/z00z_simulator crates/z00z_core/docs crates/z00z_core/README.md`

### 7. Scenario 1 Object Flow Matrix
expected: Local scenario coverage exercises rights, vouchers, validator mandates, agentic rights, and machine capabilities, while wrong-family and expired-right flows are rejected instead of silently succeeding.
result: [passed]
evidence: `cargo test --release -p z00z_wallets --test test_asset_import_security test_rejects_cash_inventory_write -- --nocapture`; `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_1 test_scenario1_object_flows_matrix_contract -- --nocapture`; `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_1 test_rights_business_entitlement_lifecycle_local -- --nocapture`; `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_1 test_agentic_right_lifecycle_local -- --nocapture`; `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_1 test_machine_capability_lifecycle_local -- --nocapture`

### 8. Canonical Config Root And Flat Test Surface
expected: Live YAML for `z00z_core` resolves only from `crates/z00z_core/z00z_config/`, the owned core integration surface is flat and canonically named, and the active doc tree is ASCII-normalized.
result: [passed]
evidence: `find crates/z00z_core -path '*/target' -prune -o \\( -name '*.yaml' -o -name '*.yml' \\) -type f | sort`; `find crates/z00z_core/tests -mindepth 2 -type f ! -path '*/tests/fixtures/*'`; `rg -n "examples/.+/.+|benches/.+/.+|bin/.+/.+|src/assets/.*\\.ya?ml|src/genesis/.*\\.ya?ml|crates/z00z_core/configs|tests/vectors|[А-Яа-я]" crates/z00z_core/docs crates/z00z_core/README.md crates/z00z_core/benches crates/z00z_core/bin crates/z00z_core/examples crates/z00z_core/tests -g '*.md'`

### 9. Explicit Benches, Binaries, And Examples
expected: Every live bench, bin, and example used by `z00z_core` is explicitly declared in `Cargo.toml`, builds in release mode, and no nested support-tree placeholders remain.
result: [passed]
evidence: `cargo doc --release -p z00z_core --no-deps`; `cargo bench -p z00z_core --no-run`; `cargo build --release -p z00z_core --bins`; `cargo test --release -p z00z_core --examples`; `find crates/z00z_core/benches crates/z00z_core/bin crates/z00z_core/examples -mindepth 2 -type f`

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[]
