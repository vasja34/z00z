---
phase: 063-Core-Update
plan: PLAN-063-G05
type: execute
wave: 2
depends_on:
  - PLAN-063-G01
  - PLAN-063-G04
files_modified:
  - crates/z00z_core/src/genesis/genesis_run.rs
  - crates/z00z_core/src/genesis/genesis_config_validate.rs
  - crates/z00z_core/src/genesis/genesis_derivation.rs
  - crates/z00z_core/src/genesis/genesis_rights.rs
  - crates/z00z_core/src/genesis/genesis_vouchers.rs
  - crates/z00z_core/src/genesis/mod.rs
  - crates/z00z_core/tests/test_genesis_manifest_refs.rs
  - crates/z00z_core/tests/test_genesis_manifest.rs
requirements:
  - REC-063-P1-02
autonomous: true
must_haves:
  truths:
    - "Generation lanes may decouple without splitting bootstrap authority."
    - "Validation must become plan-aware instead of globally unconditional."
    - "Partial generation outputs must not masquerade as full settlement manifests."
    - "The canonical full settlement manifest filename stays reserved for full bootstrap only."
  artifacts:
    - crates/z00z_core/src/genesis/genesis_run.rs
    - crates/z00z_core/src/genesis/genesis_config_validate.rs
    - crates/z00z_core/src/genesis/genesis_derivation.rs
    - crates/z00z_core/src/genesis/genesis_rights.rs
    - crates/z00z_core/src/genesis/genesis_vouchers.rs
  key_links:
    - "Scope: explicit generation-plan API and mode-aware validation."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G05 Plan: Object-Family Generation Lanes Without Split Authority

<objective>
Close `REC-063-P1-02` by adding explicit generation-plan types, mode-aware
validation, partial lane execution, and lane receipts while preserving
`run_genesis()` as the full-bootstrap wrapper.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@.planning/phases/063-Core-Update/063-core-examples.md
@crates/z00z_core/src/genesis/mod.rs
@crates/z00z_core/src/genesis/genesis_run.rs
@crates/z00z_core/src/genesis/genesis_config_validate.rs
@crates/z00z_core/src/genesis/genesis_derivation.rs
@crates/z00z_core/src/genesis/genesis_rights.rs
@crates/z00z_core/src/genesis/genesis_vouchers.rs
</context>

<coverage_contract>
This plan covers `REC-063-P1-02` only. It closes only when selected-family
generation is explicit, auditable, and still subordinate to one canonical
`GenesisConfig`.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G05`
task_ids:
- `REC-063-P1-02`
copied_task_rows:
- "| REC-063-P1-02 | P1. Decouple object-family generation lanes without splitting bootstrap authority | Keep one canonical `GenesisConfig` authority, add explicit generation lanes and mode-aware validation, keep `run_genesis()` as the full-bootstrap default, and use lane receipts instead of full manifest names for partial runs. |"
source_refs:
- `./063-TODO.md#p1-decouple-object-family-generation-lanes-without-splitting-bootstrap-authority`
- `./063-core-examples.md`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `crates/z00z_core/src/genesis/genesis_derivation.rs`
- `crates/z00z_core/src/genesis/genesis_rights.rs`
- `crates/z00z_core/src/genesis/genesis_vouchers.rs`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- `063-core-examples.md`
- live genesis execution and validation helpers
outputs:
- `.planning/phases/063-Core-Update/063-05-SUMMARY.md`
- `GenesisLane`, `GenesisSelection`, and `GenesisGenerationPlan`-style API
- mode-aware validation and partial generation receipts
- explicit shared-prerequisite, family-lane, and full-bootstrap aggregation boundaries
dependencies:
- `PLAN-063-G01`
- `PLAN-063-G04`
acceptance_tests:
- `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
- `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_rights -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_vouchers -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_full_bootstrap_matches_legacy_run -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_assets_only_skips_rights_validation -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_rights_only_requires_policy_resolution_when_needed -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_vouchers_only_rejects_non_voucher_policy -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_partial_run_does_not_emit_full_settlement_manifest -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_selected_lanes_preserve_terminal_collision_checks -- --nocapture`
- `rg -n "FullBootstrap|Selected|lane_manifest|generation_receipt|validate_genesis_config_for" crates/z00z_core/src/genesis crates/z00z_core/tests`
simulation_gate:
- No fake crypto or fake policy resolution. Partial generation must still use real loaders, real derivation, and real collision rules.
negative_tests:
- partial runs must not emit `genesis_settlement_manifest.json`
- unrelated config sections must not fail validation for a lane that does not need them
- vouchers-only runs must still reject wrong policy-family bindings
- full-bootstrap digest parity, manifest byte-stability, and simulator stage-1 parity must not regress under the new lane plan
- the canonical full settlement manifest filename remains reserved for full bootstrap only
</plan_matrix>

<plan_artifacts>
- `plan-types`: `crates/z00z_core/src/genesis/mod.rs`
- `plan-runner`: `crates/z00z_core/src/genesis/genesis_run.rs`
- `plan-validation`: `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `lane-executors`: `crates/z00z_core/src/genesis/genesis_derivation.rs`, `genesis_rights.rs`, `genesis_vouchers.rs`
- `lane-tests`: `crates/z00z_core/tests/test_genesis_manifest.rs`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
- `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_rights -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_vouchers -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_full_bootstrap_matches_legacy_run -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_assets_only_skips_rights_validation -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_rights_only_requires_policy_resolution_when_needed -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_vouchers_only_rejects_non_voucher_policy -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_partial_run_does_not_emit_full_settlement_manifest -- --nocapture`
- `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_selected_lanes_preserve_terminal_collision_checks -- --nocapture`
</plan_tests>

<plan_results>
- `PLAN-063-G05` produces `063-05-SUMMARY.md` with lane-selection, validation, partial-export, and full-bootstrap parity evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P1-02`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/src/genesis/mod.rs`
    - `crates/z00z_core/src/genesis/genesis_run.rs`
    - `crates/z00z_core/src/genesis/genesis_config_validate.rs`
    - `crates/z00z_core/src/genesis/genesis_derivation.rs`
    - `crates/z00z_core/src/genesis/genesis_rights.rs`
    - `crates/z00z_core/src/genesis/genesis_vouchers.rs`
    - `crates/z00z_core/tests/test_genesis_manifest.rs`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P1-02`
  commands:
    - `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
    - `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
    - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_rights -- --nocapture`
    - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_vouchers -- --nocapture`
    - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_full_bootstrap_matches_legacy_run -- --nocapture`
    - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_assets_only_skips_rights_validation -- --nocapture`
    - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_rights_only_requires_policy_resolution_when_needed -- --nocapture`
    - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_vouchers_only_rejects_non_voucher_policy -- --nocapture`
    - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_partial_run_does_not_emit_full_settlement_manifest -- --nocapture`
    - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_selected_lanes_preserve_terminal_collision_checks -- --nocapture`
    - `rg -n "FullBootstrap|Selected|lane_manifest|generation_receipt|validate_genesis_config_for" crates/z00z_core/src/genesis crates/z00z_core/tests`
  scenarios:
    - shared prerequisites, family lanes, and full-bootstrap aggregation stay structurally distinct under one `GenesisConfig` authority
    - policies remain a shared prerequisite lane and dependency lane for rights and vouchers when semantics require it
    - partial receipts stay lane-scoped through names such as `genesis_generation_receipt.json` rather than the full settlement manifest
    - full bootstrap stays byte-stable and keeps simulator stage-1 parity
    - `assets_only`, `rights_only`, and `vouchers_only` stay explicit and audited
</task_tests>

<task_results>
- task_id: `REC-063-P1-02`
  expected_results:
    - `selected-family generation is explicit and mode-aware`
    - `anti-placeholder evidence includes partial-run negative tests and manifest naming guards`
</task_results>

<anti_placeholder_gate>
- Adding plan types without wiring real validation and real export behavior does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `crates/z00z_core/src/genesis/genesis_derivation.rs`
- `crates/z00z_core/src/genesis/genesis_rights.rs`
- `crates/z00z_core/src/genesis/genesis_vouchers.rs`
</current_code_refs>

<blockers>
- Any partial lane that can emit files looking canonical without explicit receipt semantics is phase-failing for this slice.
- Any divergence between full-bootstrap and partial-lane policy resolution semantics is phase-failing for this slice.
</blockers>

<evidence_gate>
- Summary evidence must show one full-bootstrap wrapper plus explicit partial-lane receipts, digest and manifest parity evidence, and simulator stage-1 full-bootstrap parity.
</evidence_gate>

<not_recommendation_gate>
- This slice must land real generation and validation behavior, not a future design placeholder.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Add explicit generation-plan lanes under one bootstrap authority</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `.planning/phases/063-Core-Update/063-core-examples.md`
- `crates/z00z_core/src/genesis/mod.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `crates/z00z_core/src/genesis/genesis_derivation.rs`
- `crates/z00z_core/src/genesis/genesis_rights.rs`
- `crates/z00z_core/src/genesis/genesis_vouchers.rs`
</read_first>
<files>
- `crates/z00z_core/src/genesis/mod.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `crates/z00z_core/src/genesis/genesis_derivation.rs`
- `crates/z00z_core/src/genesis/genesis_rights.rs`
- `crates/z00z_core/src/genesis/genesis_vouchers.rs`
- `crates/z00z_core/tests/test_genesis_manifest.rs`
</files>
<action>
Introduce explicit generation-plan contracts, split common validation from
lane-specific validation, preserve `run_genesis()` as the full-bootstrap
wrapper, and ensure partial runs emit lane receipts rather than pretending to
be the full settlement manifest.
</action>
<acceptance_criteria>
- Full bootstrap remains the default and preserves current manifest semantics.
- Partial lane selection is explicit, typed, and auditable.
- Validation is mode-aware and rejects only semantically relevant omissions.
- Partial runs never emit the canonical full settlement manifest filename.
- The canonical full settlement manifest filename is reserved for full bootstrap only.
- Shared prerequisites, family lanes, and full-bootstrap aggregation are separated without creating a second bootstrap authority.
- Policies remain a shared prerequisite lane and dependency lane rather than a parallel authority bucket.
- Full-bootstrap digest parity, manifest byte-stability, and simulator stage-1 parity remain intact.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
   - `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
   - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_rights -- --nocapture`
   - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_vouchers -- --nocapture`
   - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_full_bootstrap_matches_legacy_run -- --nocapture`
   - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_assets_only_skips_rights_validation -- --nocapture`
   - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_rights_only_requires_policy_resolution_when_needed -- --nocapture`
   - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_plan_vouchers_only_rejects_non_voucher_policy -- --nocapture`
   - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_partial_run_does_not_emit_full_settlement_manifest -- --nocapture`
   - `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests test_genesis_selected_lanes_preserve_terminal_collision_checks -- --nocapture`
   - `rg -n "FullBootstrap|Selected|lane_manifest|generation_receipt|validate_genesis_config_for" crates/z00z_core/src/genesis crates/z00z_core/tests`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
Selected-family generation is explicit, validated, and still subordinate to one bootstrap authority.
</done>
</task>
</tasks>

<threat_model>
- Partial generation can accidentally weaken full-bootstrap invariants.
- Partial outputs can be mistaken for canonical full manifests if receipt semantics are weak.
- Split policy resolution semantics can make partial exports disagree with the consensus-safe full-bootstrap path.
</threat_model>

<verification>
This slice is valid only if full-bootstrap parity stays intact and partial lanes remain explicit, bounded, and negative-tested.
</verification>

<success_criteria>
- One canonical bootstrap wrapper remains.
- Partial generation is explicit, mode-aware, and receipt-scoped.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-05-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-05-SUMMARY.md`
- generation-plan API, mode-aware validation, and lane receipt coverage

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P1-02` | `PLAN-063-G05` | `063-TODO.md#p1-decouple-object-family-generation-lanes-without-splitting-bootstrap-authority`; `063-core-examples.md`; live genesis execution anchors | `063-TODO.md`; `063-CONTEXT.md`; `063-core-examples.md`; genesis execution files | generation-plan types, validation, lane executors, tests | `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_rights -- --nocapture`; `cargo test --release -p z00z_core --features test-params-fast --test genesis_tests genesis_vouchers -- --nocapture` | explicit partial generation under one canonical bootstrap authority | real loaders/crypto only | changed generation code plus partial-manifest negative tests | `Complete` |
