---
phase: 063-Core-Update
plan: PLAN-063-G08
type: execute
wave: 2
depends_on:
  - PLAN-063-G05
  - PLAN-063-G07
files_modified:
  - crates/z00z_core/src/genesis/README.md
  - crates/z00z_core/README.md
  - crates/z00z_core/docs/**
  - crates/z00z_storage/src/settlement/tx_plan_types.rs
  - crates/z00z_wallets/src/rpc/object_rpc_impl.rs
requirements:
  - REC-063-P1-05
autonomous: true
must_haves:
  truths:
    - "Every object family gets one authoritative lifecycle and semantics matrix."
    - "Voucher bootstrap shape vs runtime shape is explicit."
    - "Asset mintability/runtime support is either documented precisely or narrowed."
  artifacts:
    - crates/z00z_core/src/genesis/README.md
    - crates/z00z_core/README.md
    - crates/z00z_core/docs/**
  key_links:
    - "Scope: object-family semantics matrix and canonical lifecycle narrative."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G08 Plan: Authoritative Object-Family Semantics Matrix

<objective>
Close `REC-063-P1-05` by publishing one canonical object-family semantics
matrix for assets, rights, policies, and vouchers.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@.planning/phases/063-Core-Update/063-core-examples.md
@crates/z00z_core/src/genesis/README.md
@crates/z00z_core/README.md
@crates/z00z_wallets/src/rpc/object_rpc_impl.rs
@crates/z00z_storage/src/settlement/tx_plan_types.rs
</context>

<coverage_contract>
This plan covers `REC-063-P1-05` only. It closes only when one canonical matrix
states lifecycle, value, policy-binding, backing, bootstrap-shape, runtime
shape, and caller-path semantics for every object family.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G08`
task_ids:
- `REC-063-P1-05`
copied_task_rows:
- "| REC-063-P1-05 | P1. Publish an authoritative object-family semantics matrix | Add one canonical matrix for whether genesis can create each family, whether runtime can create or mutate it, whether bootstrap shape differs from runtime shape, whether it carries value, what policy binding is required, whether backing is required, and what the caller path is. |"
source_refs:
- `./063-TODO.md#p1-publish-an-authoritative-object-family-semantics-matrix`
- `./063-core-examples.md`
- `crates/z00z_core/src/genesis/README.md`
- `crates/z00z_wallets/src/rpc/object_rpc_impl.rs`
- `crates/z00z_storage/src/settlement/tx_plan_types.rs`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- `063-core-examples.md`
- live object-family docs and runtime anchors
outputs:
- `.planning/phases/063-Core-Update/063-08-SUMMARY.md`
- canonical semantics matrix in core docs
- clarified asset/voucher/right lifecycle wording
dependencies:
- `PLAN-063-G05`
- `PLAN-063-G07`
acceptance_tests:
- `cargo test --release -p z00z_core --doc`
- `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`
- `rg -n "VoucherBootstrapEntryV1|mintable|value|backing|required_signatures|primary_family" crates/z00z_core crates/z00z_wallets crates/z00z_storage`
simulation_gate:
- No simulation. The matrix must be anchored in live code and docs.
negative_tests:
- vouchers must not be described as merely asset plus right
- rights must not be described as value-carrying
- unsupported asset runtime minting claims must not survive
</plan_matrix>

<plan_artifacts>
- `genesis-doc`: `crates/z00z_core/src/genesis/README.md`
- `crate-doc`: `crates/z00z_core/README.md`
- `core-docs`: `crates/z00z_core/docs/**`
- `runtime-anchors`: `crates/z00z_wallets/src/rpc/object_rpc_impl.rs`, `crates/z00z_storage/src/settlement/tx_plan_types.rs`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core --doc`
- `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`
- `rg -n "VoucherBootstrapEntryV1|mintable|value|backing|required_signatures|primary_family" crates/z00z_core crates/z00z_wallets crates/z00z_storage`
</plan_tests>

<plan_results>
- `PLAN-063-G08` produces `063-08-SUMMARY.md` with the published matrix and linked runtime anchors.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P1-05`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/src/genesis/README.md`
    - `crates/z00z_core/README.md`
    - `crates/z00z_core/docs/**`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P1-05`
  commands:
    - `cargo test --release -p z00z_core --doc`
    - `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`
    - `rg -n "VoucherBootstrapEntryV1|mintable|value|backing|required_signatures|primary_family" crates/z00z_core crates/z00z_wallets crates/z00z_storage`
  scenarios:
    - `canonical matrix names each family's bootstrap and runtime semantics`
    - `stale or vague lifecycle claims are removed`
</task_tests>

<task_results>
- task_id: `REC-063-P1-05`
  expected_results:
    - `one canonical semantics matrix governs docs`
    - `anti-placeholder evidence includes linked runtime anchors and doc guards`
</task_results>

<anti_placeholder_gate>
- A matrix without linked live runtime anchors does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/src/genesis/README.md`
- `crates/z00z_core/README.md`
- `crates/z00z_wallets/src/rpc/object_rpc_impl.rs`
- `crates/z00z_storage/src/settlement/tx_plan_types.rs`
</current_code_refs>

<blockers>
- If asset runtime minting remains ambiguous, the phase must either document its exact contract or explicitly narrow the public wording in this same slice.
</blockers>

<evidence_gate>
- Summary evidence must point from each matrix row to at least one live code or test anchor.
</evidence_gate>

<not_recommendation_gate>
- This slice must publish live semantics, not a speculative whitepaper layer.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Publish one canonical object-family semantics matrix</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `.planning/phases/063-Core-Update/063-core-examples.md`
- `crates/z00z_core/src/genesis/README.md`
- `crates/z00z_core/README.md`
- `crates/z00z_wallets/src/rpc/object_rpc_impl.rs`
- `crates/z00z_storage/src/settlement/tx_plan_types.rs`
</read_first>
<files>
- `crates/z00z_core/src/genesis/README.md`
- `crates/z00z_core/README.md`
- `crates/z00z_core/docs/**`
</files>
<action>
Write one authoritative semantics matrix that ties bootstrap, runtime, policy,
value, backing, and caller-path behavior to the live object model, then remove
or narrow any wording that leaves asset, right, or voucher behavior ambiguous.
</action>
<acceptance_criteria>
- The matrix is canonical and anchored in live code/doc paths.
- Vouchers are described as value/backing-bearing bootstrap/runtime objects, not as asset-plus-right shorthand.
- Rights are described as zero-value authority objects.
- Asset runtime lifecycle claims are either explicit or narrowed.
- `VoucherBootstrapEntryV1` is explicitly documented as a bootstrap input rather than the runtime voucher object.
- `mintable` wording is either proven by runtime code/tests or narrowed so it does not imply unsupported post-genesis asset minting.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core --doc`
   - `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`
   - `rg -n "VoucherBootstrapEntryV1|mintable|value|backing|required_signatures|primary_family" crates/z00z_core crates/z00z_wallets crates/z00z_storage`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
One canonical object-family semantics matrix governs the live Phase 063 contract.
</done>
</task>
</tasks>

<threat_model>
- Divergent semantics stories across docs, wallets, and storage create future contract drift.
</threat_model>

<verification>
This slice is valid only if the matrix is singular, live-anchored, and removes ambiguous lifecycle claims.
</verification>

<success_criteria>
- One authoritative semantics matrix exists.
- Each family's lifecycle and value semantics are explicit.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-08-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-08-SUMMARY.md`
- canonical object-family semantics matrix and linked doc cleanup

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P1-05` | `PLAN-063-G08` | `063-TODO.md#p1-publish-an-authoritative-object-family-semantics-matrix`; `063-core-examples.md`; live runtime anchors | `063-TODO.md`; `063-CONTEXT.md`; `063-core-examples.md`; core/runtime docs | core docs and semantics matrix | `cargo test --release -p z00z_core --doc`; `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture` | one authoritative matrix with live anchors | real docs/runtime only | changed matrix plus linked runtime anchors | `Complete` |
