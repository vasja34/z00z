---
phase: 063-Core-Update
plan: PLAN-063-G06
type: execute
wave: 2
depends_on:
  - PLAN-063-G04
  - PLAN-063-G05
files_modified:
  - crates/z00z_core/src/assets/registry.rs
  - crates/z00z_core/src/assets/registry_snapshot.rs
  - crates/z00z_core/src/genesis/genesis_run.rs
  - crates/z00z_simulator/src/scenario_1/stage_1/mod.rs
  - crates/z00z_core/tests/test_assets_registry_integration.rs
requirements:
  - REC-063-P1-03
autonomous: true
must_haves:
  truths:
    - "`GLOBAL_ASSET_REGISTRY` may remain read-mostly fallback, not a co-equal write owner."
    - "Generation and simulator flows must prefer explicit registry ownership."
    - "Test-only reset helpers stay test-only."
  artifacts:
    - crates/z00z_core/src/assets/registry.rs
    - crates/z00z_core/src/genesis/genesis_run.rs
    - crates/z00z_simulator/src/scenario_1/stage_1/mod.rs
  key_links:
    - "Scope: global-registry write ownership narrowing."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G06 Plan: GLOBAL_ASSET_REGISTRY Ownership Narrowing

<objective>
Close `REC-063-P1-03` by reducing dual-authority writes and pushing primary
ownership toward explicit `AssetDefinitionRegistry` instances.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
@crates/z00z_core/src/assets/registry.rs
@crates/z00z_core/src/assets/registry_snapshot.rs
@crates/z00z_core/src/genesis/genesis_run.rs
@crates/z00z_simulator/src/scenario_1/stage_1/mod.rs
</context>

<coverage_contract>
This plan covers `REC-063-P1-03` only. It closes only when core flows stop
looking like two registries are both authoritative writers.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G06`
task_ids:
- `REC-063-P1-03`
copied_task_rows:
- "| REC-063-P1-03 | P1. Reduce dual-authority writes around GLOBAL_ASSET_REGISTRY | Treat the global registry as a process-wide read-mostly fallback, narrow global writes to adapter boundaries, and push primary ownership toward explicit registry instances. |"
source_refs:
- `./063-TODO.md#p1-reduce-dual-authority-writes-around-global_asset_registry`
- `.github/requirements/Z00Z_DESIGN_FOUNDATION.md`
- `crates/z00z_core/src/assets/registry.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_simulator/src/scenario_1/stage_1/mod.rs`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- live registry/global-write call sites
outputs:
- `.planning/phases/063-Core-Update/063-06-SUMMARY.md`
- explicit registry-first generation and simulator flows
- narrowed global-write adapter boundary
dependencies:
- `PLAN-063-G04`
- `PLAN-063-G05`
acceptance_tests:
- `cargo test --release -p z00z_core --test assets_tests registry_integration -- --nocapture`
- `cargo test --release -p z00z_simulator --test scenario_1 test_scenario1_stage_surface::test_scenario1_stage_surface -- --nocapture`
- `rg -n "GLOBAL_ASSET_REGISTRY|set_global|register_global|ctx\\.registry" crates/z00z_core crates/z00z_simulator`
simulation_gate:
- No fake registry semantics. Use live generation and simulator flows with real registry instances.
negative_tests:
- same-flow dual writes must be removed or explicitly bounded
- test-only reset helpers must not leak into production paths
</plan_matrix>

<plan_artifacts>
- `registry-core`: `crates/z00z_core/src/assets/registry.rs`
- `registry-runner`: `crates/z00z_core/src/genesis/genesis_run.rs`
- `sim-stage1`: `crates/z00z_simulator/src/scenario_1/stage_1/mod.rs`
- `registry-tests`: `crates/z00z_core/tests/test_assets_registry_integration.rs`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core --test assets_tests registry_integration -- --nocapture`
- `cargo test --release -p z00z_simulator --test scenario_1 test_scenario1_stage_surface::test_scenario1_stage_surface -- --nocapture`
- `rg -n "GLOBAL_ASSET_REGISTRY|ctx\\.registry" crates/z00z_core crates/z00z_simulator`
</plan_tests>

<plan_results>
- `PLAN-063-G06` produces `063-06-SUMMARY.md` with explicit-vs-global ownership evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P1-03`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/src/assets/registry.rs`
    - `crates/z00z_core/src/genesis/genesis_run.rs`
    - `crates/z00z_simulator/src/scenario_1/stage_1/mod.rs`
    - `crates/z00z_core/tests/test_assets_registry_integration.rs`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P1-03`
  commands:
    - `cargo test --release -p z00z_core --test assets_tests registry_integration -- --nocapture`
    - `cargo test --release -p z00z_simulator --test scenario_1 test_scenario1_stage_surface::test_scenario1_stage_surface -- --nocapture`
    - `rg -n "GLOBAL_ASSET_REGISTRY|ctx\\.registry" crates/z00z_core crates/z00z_simulator`
  scenarios:
    - `generation prefers explicit registry ownership`
    - `simulator stage does not present two authoritative write lanes`
</task_tests>

<task_results>
- task_id: `REC-063-P1-03`
  expected_results:
    - `global registry is bounded to the fallback or adapter role`
    - `anti-placeholder evidence includes call-site narrowing and tests`
</task_results>

<anti_placeholder_gate>
- Comments about preferred ownership without code-path changes do not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/src/assets/registry.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_simulator/src/scenario_1/stage_1/mod.rs`
</current_code_refs>

<blockers>
- Any surviving same-flow dual-write that still makes both registries look authoritative is phase-failing for this slice.
</blockers>

<evidence_gate>
- Summary evidence must show the exact narrowed global-write boundary and the exact explicit-owner path.
</evidence_gate>

<not_recommendation_gate>
- This slice must land real ownership narrowing in live code, not aspirational comments.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Narrow global registry writes to explicit adapter boundaries</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `crates/z00z_core/src/assets/registry.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_simulator/src/scenario_1/stage_1/mod.rs`
</read_first>
<files>
- `crates/z00z_core/src/assets/registry.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_simulator/src/scenario_1/stage_1/mod.rs`
- `crates/z00z_core/tests/test_assets_registry_integration.rs`
</files>
<action>
Move primary asset-definition ownership to explicitly passed registries in
generation and simulator flows, keep the singleton only as a clearly bounded
fallback or adapter seam, and preserve test-only reset helpers under test cfg.
</action>
<acceptance_criteria>
- Explicit registries own core write flows.
- The singleton no longer looks like a co-equal write authority in the same flow.
- Tests prove the narrowed ownership model.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core --test assets_tests registry_integration -- --nocapture`
   - `cargo test --release -p z00z_simulator --test scenario_1 test_scenario1_stage_surface::test_scenario1_stage_surface -- --nocapture`
   - `rg -n "GLOBAL_ASSET_REGISTRY|ctx\\.registry" crates/z00z_core crates/z00z_simulator`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
Registry ownership is explicit and the singleton is bounded.
</done>
</task>
</tasks>

<threat_model>
- Dual registry writes weaken reproducibility and test isolation.
</threat_model>

<verification>
This slice is valid only if global writes stop acting as a second core authority path.
</verification>

<success_criteria>
- Explicit registries own primary writes.
- Global registry writes are narrow and reviewable.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-06-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-06-SUMMARY.md`
- explicit registry-ownership cleanup across core and simulator flows

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P1-03` | `PLAN-063-G06` | `063-TODO.md#p1-reduce-dual-authority-writes-around-global_asset_registry`; registry and simulator anchors | `063-TODO.md`; `063-CONTEXT.md`; registry call sites | registry core, genesis run, simulator stage, tests | `cargo test --release -p z00z_core --test assets_tests registry_integration -- --nocapture`; `cargo test --release -p z00z_simulator --test scenario_1 test_scenario1_stage_surface::test_scenario1_stage_surface -- --nocapture` | singleton no longer acts as co-equal writer | real registries only | changed write paths plus call-site scans | `Complete` |
