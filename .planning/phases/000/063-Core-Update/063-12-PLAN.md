---
phase: 063-Core-Update
plan: PLAN-063-G12
type: execute
wave: 4
depends_on:
  - PLAN-063-G03
  - PLAN-063-G08
  - PLAN-063-G10
  - PLAN-063-G11
files_modified:
  - crates/z00z_core/docs/**
  - crates/z00z_core/README.md
  - crates/z00z_core/bin/**/*.md
  - crates/z00z_core/benches/**/*.md
  - crates/z00z_core/examples/**/*.md
  - crates/z00z_core/tests/**/*.md
requirements:
  - REC-063-P2-02
autonomous: true
must_haves:
  truths:
    - "`z00z_core/docs` must describe live APIs, live config paths, and live support surfaces only."
    - "ASCII English filenames replace non-ASCII documentation filenames."
    - "Support-surface Markdown must not reintroduce stale nested paths."
  artifacts:
    - crates/z00z_core/docs/**
    - crates/z00z_core/README.md
    - support-surface Markdown under bin, benches, examples, tests
  key_links:
    - "Scope: docs truth restoration after config and test layout normalization."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G12 Plan: `z00z_core/docs` Truth Restoration

<objective>
Close `REC-063-P2-02` by auditing `z00z_core/docs` and support-surface Markdown
against the live code, `z00z_config` layout, and canonical Phase 063 contracts.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@crates/z00z_core/docs
@crates/z00z_core/README.md
@crates/z00z_core/bin
@crates/z00z_core/benches
@crates/z00z_core/examples
@crates/z00z_core/tests
</context>

<coverage_contract>
This plan covers `REC-063-P2-02` only. It closes only when `z00z_core/docs`
and adjacent Markdown surfaces describe only live code and live paths.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G12`
task_ids:
- `REC-063-P2-02`
copied_task_rows:
- "| REC-063-P2-02 | P2. Restore z00z_core/docs against live code | Audit `z00z_core/docs` and support-surface Markdown against live code and `z00z_config`, remove stale paths and examples, and rename non-ASCII docs to English ASCII filenames. |"
source_refs:
- `./063-TODO.md (P2. Restore z00z_core/docs against live code)`
- `crates/z00z_core/docs/**`
- `crates/z00z_core/README.md`
- support-surface Markdown under `bin`, `benches`, `examples`, and `tests`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- live docs and support-surface Markdown
outputs:
- `.planning/phases/063-Core-Update/063-12-SUMMARY.md`
- truthful docs and ASCII filenames
- zero stale path references to old YAML or nested support paths
dependencies:
- `PLAN-063-G03`
- `PLAN-063-G08`
- `PLAN-063-G10`
- `PLAN-063-G11`
acceptance_tests:
- `cargo doc --release -p z00z_core --no-deps`
- `rg -n "src/assets/.*\\.ya?ml|src/genesis/.*\\.ya?ml|config/genesis|assets_config\\.yaml|examples/(assets|genesis)|benches/(assets|genesis)|bin/(assets|genesis)" crates/z00z_core/docs crates/z00z_core/README.md`
- `rg -n "src/assets/.*\\.ya?ml|src/genesis/.*\\.ya?ml|config/genesis|assets_config\\.yaml|examples/(assets|genesis)|benches/(assets|genesis)|bin/(assets|genesis)|[А-Яа-я]" crates/z00z_core/bin crates/z00z_core/benches crates/z00z_core/examples crates/z00z_core/tests -g '*.md'`
simulation_gate:
- No simulation. Documentation must be validated against live code and live paths directly.
negative_tests:
- no non-ASCII live doc filenames remain
- no stale pre-`z00z_config` path survives in docs
</plan_matrix>

<plan_artifacts>
- `docs-root`: `crates/z00z_core/docs/**`
- `readme`: `crates/z00z_core/README.md`
- `support-docs`: `crates/z00z_core/bin/**/*.md`, `crates/z00z_core/benches/**/*.md`, `crates/z00z_core/examples/**/*.md`, `crates/z00z_core/tests/**/*.md`
</plan_artifacts>

<plan_tests>
- `cargo doc --release -p z00z_core --no-deps`
- stale-path scans across docs and support-surface Markdown
</plan_tests>

<plan_results>
- `PLAN-063-G12` produces `063-12-SUMMARY.md` with docs audit and stale-path scan evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P2-02`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/docs/**`
    - `crates/z00z_core/README.md`
    - support-surface Markdown under `bin`, `benches`, `examples`, `tests`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P2-02`
  commands:
    - `cargo doc --release -p z00z_core --no-deps`
    - `rg -n "src/assets/.*\\.ya?ml|src/genesis/.*\\.ya?ml|config/genesis|assets_config\\.yaml|examples/(assets|genesis)|benches/(assets|genesis)|bin/(assets|genesis)" crates/z00z_core/docs crates/z00z_core/README.md`
    - `rg -n "src/assets/.*\\.ya?ml|src/genesis/.*\\.ya?ml|config/genesis|assets_config\\.yaml|examples/(assets|genesis)|benches/(assets|genesis)|bin/(assets|genesis)|[А-Яа-я]" crates/z00z_core/bin crates/z00z_core/benches crates/z00z_core/examples crates/z00z_core/tests -g '*.md'`
  scenarios:
    - `docs reference only live code and live z00z_config paths`
    - `support-surface Markdown cannot reintroduce drift`
</task_tests>

<task_results>
- task_id: `REC-063-P2-02`
  expected_results:
    - `core docs are truthful and ASCII-normalized`
    - `anti-placeholder evidence includes zero-stale-path and zero-non-ascii scans`
</task_results>

<anti_placeholder_gate>
- Renaming files without rewriting stale content does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/docs/**`
- `crates/z00z_core/README.md`
- support-surface Markdown under `bin`, `benches`, `examples`, `tests`
</current_code_refs>

<blockers>
- Any support-surface Markdown that still points to old nested paths after the layout wave is phase-failing for this slice.
</blockers>

<evidence_gate>
- Summary evidence must show the final docs filename list and the final zero-stale-path scan outputs.
</evidence_gate>

<not_recommendation_gate>
- This slice must rewrite the live docs now, not defer cleanup to a later editorial pass.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Audit core docs and support-surface Markdown to live code</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `crates/z00z_core/docs/**`
- `crates/z00z_core/README.md`
- support-surface Markdown under `bin`, `benches`, `examples`, `tests`
</read_first>
<files>
- `crates/z00z_core/docs/**`
- `crates/z00z_core/README.md`
- `crates/z00z_core/bin/**/*.md`
- `crates/z00z_core/benches/**/*.md`
- `crates/z00z_core/examples/**/*.md`
- `crates/z00z_core/tests/**/*.md`
</files>
<action>
Rewrite docs and support-surface Markdown to the live APIs, live `z00z_config`
paths, and live flattened support layout; remove stale examples and old file
names; and replace non-ASCII documentation filenames with English ASCII names.
</action>
<acceptance_criteria>
- Docs reference only live code and live config paths.
- Support-surface Markdown no longer points to stale nested layouts.
- Non-ASCII doc filenames are eliminated from the live tree.
- `cargo doc --release -p z00z_core --no-deps` is green after doc changes.
- Unsupported or non-authoritative ideas are removed from live docs and moved back to planning artifacts.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo doc --release -p z00z_core --no-deps`
   - `rg -n "src/assets/.*\\.ya?ml|src/genesis/.*\\.ya?ml|config/genesis|assets_config\\.yaml|examples/(assets|genesis)|benches/(assets|genesis)|bin/(assets|genesis)" crates/z00z_core/docs crates/z00z_core/README.md`
   - `rg -n "src/assets/.*\\.ya?ml|src/genesis/.*\\.ya?ml|config/genesis|assets_config\\.yaml|examples/(assets|genesis)|benches/(assets|genesis)|bin/(assets|genesis)|[А-Яа-я]" crates/z00z_core/bin crates/z00z_core/benches crates/z00z_core/examples crates/z00z_core/tests -g '*.md'`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`z00z_core` docs are truthful, ASCII-normalized, and aligned to the live code.
</done>
</task>
</tasks>

<threat_model>
- Docs and support Markdown can silently reintroduce stale paths after code migrations.
</threat_model>

<verification>
This slice is valid only if docs, filenames, and support-surface references all match the live tree.
</verification>

<success_criteria>
- Live docs are truthful and ASCII-only.
- Stale path references are absent across core docs and support-surface Markdown.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-12-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-12-SUMMARY.md`
- truthful core docs and support-surface Markdown

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P2-02` | `PLAN-063-G12` | `063-TODO.md (P2. Restore z00z_core/docs against live code)`; docs and support Markdown anchors | `063-TODO.md`; `063-CONTEXT.md`; docs tree | docs, README, support Markdown | `cargo doc --release -p z00z_core --no-deps`; stale-path scans | truthful docs with ASCII filenames and live paths only | real docs only | zero-stale-path and zero-non-ascii scans | `Complete` |
