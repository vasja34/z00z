---
phase: 063-Core-Update
plan: PLAN-063-G10
type: execute
wave: 3
depends_on:
  - PLAN-063-G04
  - PLAN-063-G05
  - PLAN-063-G07
  - PLAN-063-G08
files_modified:
  - crates/z00z_core/z00z_config/**
  - crates/z00z_core/configs/**
  - crates/z00z_core/src/assets/assets_config.yaml
  - crates/z00z_core/src/genesis/genesis_config_devnet*.yaml
  - crates/z00z_core/examples/genesis/genesis_config_devnet_test.yaml
  - crates/z00z_core/tests/vectors/*.yaml
  - crates/z00z_core/src/genesis/genesis_config.rs
  - crates/z00z_core/src/genesis/manifest_ref_loader.rs
requirements:
  - REC-063-P1-07
autonomous: true
must_haves:
  truths:
    - "`z00z_config/` becomes the only core YAML root."
    - "One canonical `GenesisConfig` authority survives the move."
    - "`063-core-examples.md` fields become live parser-owned scope when parsed and validated, even before every field is consumed by runtime materialization."
  artifacts:
    - crates/z00z_core/z00z_config/**
    - crates/z00z_core/src/genesis/genesis_config.rs
    - crates/z00z_core/src/genesis/manifest_ref_loader.rs
    - crates/z00z_core/tests/**
  key_links:
    - "Scope: core YAML root migration and example materialization."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G10 Plan: Canonical `z00z_config` YAML Root

<objective>
Close `REC-063-P1-07` by moving all core-owned YAML into
`crates/z00z_core/z00z_config/`, preserving one canonical `GenesisConfig`
authority, and materializing only the `063-core-examples.md` fields that live
code truly supports.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@.planning/phases/063-Core-Update/063-core-examples.md
@crates/z00z_core/Cargo.toml
@crates/z00z_core/src/genesis/genesis_config.rs
@crates/z00z_core/src/genesis/manifest_ref_loader.rs
@crates/z00z_core/configs
@crates/z00z_core/src/genesis
@crates/z00z_core/src/assets
</context>

<coverage_contract>
This plan covers `REC-063-P1-07` only. It closes only when `z00z_config` is
the sole live YAML root and live config fields match real parser and runtime
support.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G10`
task_ids:
- `REC-063-P1-07`
copied_task_rows:
- "| REC-063-P1-07 | P1. Make z00z_core/z00z_config the only core YAML authority | Create `z00z_config/` as the only core YAML root, move all core-owned YAML there, preserve one `GenesisConfig` authority, replace legacy `genesis_config_devnet_small.yaml` with `devnet_genesis_config.yaml`, and make every `063-core-examples.md` section a typed live parser-owned config surface. |"
source_refs:
- `./063-TODO.md (P1. Make z00z_core/z00z_config the only core YAML authority)`
- `./063-core-examples.md`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/genesis/manifest_ref_loader.rs`
- `crates/z00z_core/configs/*.yaml`
- `crates/z00z_core/src/genesis/*.yaml`
- `crates/z00z_core/src/assets/assets_config.yaml`
- `crates/z00z_core/examples/genesis/genesis_config_devnet_test.yaml`
- `crates/z00z_core/tests/vectors/*.yaml`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- `063-core-examples.md`
- live YAML files and parser loaders
outputs:
- `.planning/phases/063-Core-Update/063-10-SUMMARY.md`
- `crates/z00z_core/z00z_config/**` as the sole live YAML root
- parser, validation, and tests aligned to live example fields
dependencies:
- `PLAN-063-G04`
- `PLAN-063-G05`
- `PLAN-063-G07`
- `PLAN-063-G08`
acceptance_tests:
- `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
- `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
- `cargo test --release -p z00z_core --test test_rights_config -- --nocapture`
- `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`
- `find crates/z00z_core -path '*/target' -prune -o \\( -name '*.yaml' -o -name '*.yml' \\) -type f | sort`
- `perl -ne 'print if /domain_name:/ && !/domain_name: z00z\\.core\\.[a-z_]+\\.[a-z0-9_]+\\.devnet\\.v1$/' .planning/phases/063-Core-Update/063-core-examples.md`
- `rg -n "src/(assets|genesis)/.*\\.ya?ml|examples/.+\\.ya?ml|tests/vectors|crates/z00z_core/configs|genesis_config_devnet_small|devnet_actions_config" crates/z00z_core wiki`
- `rg -n "src/genesis/genesis_config_devnet|genesis_config_devnet_small|crates/z00z_core/configs|devnet_actions_config" wiki crates/z00z_core/docs crates/z00z_core/README.md crates/z00z_core`
simulation_gate:
- No simulation. Use the live parser, live manifest refs, and live release tests.
negative_tests:
- no second live YAML root may survive
- no second live `devnet_small_*_config.yaml` family may survive unless later code adds an explicit non-small devnet profile
- no `wallet_profiles[]` or `policy_profiles[]` field may reach live genesis without typed parser and validation support
- no `devnet_actions_config.yaml` may survive as live config without parser/runtime support
- `manifest_refs` must not fan out root-owned `chain`, `outputs`, or `performance`
- archived tarballs or config snapshots must not remain as a second live YAML authority
</plan_matrix>

<plan_artifacts>
- `yaml-root`: `crates/z00z_core/z00z_config/**`
- `yaml-loaders`: `crates/z00z_core/src/genesis/genesis_config.rs`, `crates/z00z_core/src/genesis/manifest_ref_loader.rs`
- `legacy-yaml-moves`: `crates/z00z_core/configs/**`, `crates/z00z_core/src/genesis/*.yaml`, `crates/z00z_core/src/assets/assets_config.yaml`, `crates/z00z_core/examples/genesis/genesis_config_devnet_test.yaml`, `crates/z00z_core/tests/vectors/*.yaml`
- `yaml-tests`: `crates/z00z_core/tests/**`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
- `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
- `cargo test --release -p z00z_core --test test_rights_config -- --nocapture`
- `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`
- `find crates/z00z_core -path '*/target' -prune -o \( -name '*.yaml' -o -name '*.yml' \) -type f | sort`
- `perl -ne 'print if /domain_name:/ && !/domain_name: z00z\.core\.[a-z_]+\.[a-z0-9_]+\.devnet\.v1$/' .planning/phases/063-Core-Update/063-core-examples.md`
- stale target-state reference scans across wiki, docs, README, and `crates/z00z_core`
</plan_tests>

<plan_results>
- `PLAN-063-G10` produces `063-10-SUMMARY.md` with YAML-root migration and parser/materialization evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P1-07`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/z00z_config/**`
    - `crates/z00z_core/src/genesis/genesis_config.rs`
    - `crates/z00z_core/src/genesis/manifest_ref_loader.rs`
    - `crates/z00z_core/tests/**`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P1-07`
  commands:
    - `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
    - `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
    - `cargo test --release -p z00z_core --test test_rights_config -- --nocapture`
    - `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`
    - `find crates/z00z_core -path '*/target' -prune -o \\( -name '*.yaml' -o -name '*.yml' \\) -type f | sort`
    - `perl -ne 'print if /domain_name:/ && !/domain_name: z00z\\.core\\.[a-z_]+\\.[a-z0-9_]+\\.devnet\\.v1$/' .planning/phases/063-Core-Update/063-core-examples.md`
    - `rg -n "src/genesis/genesis_config_devnet|genesis_config_devnet_small|crates/z00z_core/configs|devnet_actions_config" wiki crates/z00z_core/docs crates/z00z_core/README.md crates/z00z_core`
  scenarios:
    - `all core-owned live YAML lives under z00z_config`
    - `live config fields match actual parser and runtime support`
    - `catalog _config.yaml names stay canonical while legacy small-devnet aliases disappear`
    - `actions stay nested under policies rather than reviving a standalone actions lane`
    - `manifest_refs stay limited to assets, rights, policies, and vouchers while chain, outputs, and performance stay root-owned`
</task_tests>

<task_results>
- task_id: `REC-063-P1-07`
  expected_results:
    - `one canonical core YAML root exists`
    - `anti-placeholder evidence includes parser, manifest-ref, and field-materialization tests`
</task_results>

<anti_placeholder_gate>
- Moving files without updating loaders, callers, and tests in the same wave does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/configs/**`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/genesis/manifest_ref_loader.rs`
- `crates/z00z_core/src/genesis/*.yaml`
- `crates/z00z_core/src/assets/assets_config.yaml`
</current_code_refs>

<blockers>
- Any surviving live caller of `genesis_config_devnet_small.yaml` or `crates/z00z_core/configs/**` is phase-failing for this slice.
- This migration must stay scoped to `crates/z00z_core` unless a later phase explicitly widens the authority move.
</blockers>

<evidence_gate>
- Summary evidence must show the post-migration YAML file list, the domain-name pattern check, the manifest-root boundary proof, and zero stale target-state reference scans.
</evidence_gate>

<not_recommendation_gate>
- This slice must land the real YAML move, loader rewrites, and release tests, not a target-state note.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Move all core-owned live YAML under z00z_config</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `.planning/phases/063-Core-Update/063-core-examples.md`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/genesis/manifest_ref_loader.rs`
- every live YAML owner under `crates/z00z_core`
</read_first>
<files>
- `crates/z00z_core/z00z_config/**`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/genesis/manifest_ref_loader.rs`
- `crates/z00z_core/configs/**`
- `crates/z00z_core/src/genesis/*.yaml`
- `crates/z00z_core/src/assets/assets_config.yaml`
- `crates/z00z_core/examples/genesis/genesis_config_devnet_test.yaml`
- `crates/z00z_core/tests/vectors/*.yaml`
- `crates/z00z_core/tests/**`
</files>
<action>
Move every live core-owned YAML file under `crates/z00z_core/z00z_config/`,
rewrite every loader, doc, example, test, and CLI reference in the same wave,
preserve manifest-ref root ownership semantics, and ensure every
`063-core-examples.md` field is either consumed by runtime materialization or
kept as an explicit typed parser-owned field with live validation.
</action>
<acceptance_criteria>
- `z00z_config/` is the only live YAML root under `crates/z00z_core`.
- `GenesisConfig` remains the sole bootstrap authority after the move.
- The catalog keeps the `_config.yaml` family naming under `z00z_config`, including `devnet_genesis_config.yaml`, `devnet_assets_config.yaml`, `devnet_rights_config.yaml`, `devnet_policies_config.yaml`, and `devnet_vouchers_config.yaml`.
- `devnet_genesis_config.yaml` becomes the canonical small devnet root filename.
- No second live `devnet_small_*_config.yaml` family survives unless a later wave adds explicit non-small devnet code support.
- `wallet_profiles[]` and `policy_profiles[]` are live typed parser-owned sections in genesis YAML and are validated even when some fields remain pre-materialization scope.
- Actions remain nested inside `policies[]`; no standalone live actions lane or `devnet_actions_config.yaml` survives without parser, validation, and runtime support added in the same wave.
- `manifest_refs` may fan out only `assets`, `rights`, `policies`, and `vouchers`; `chain`, `outputs`, and `performance` remain root-owned.
- Live asset and right `domain_name` fields follow `z00z.core.<family>.<name>.devnet.v1`.
- Archived tarballs or config snapshots are migration inputs only and do not remain as a second live authority under `crates/z00z_core`.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
   - `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
   - `cargo test --release -p z00z_core --test test_rights_config -- --nocapture`
   - `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`
   - `find crates/z00z_core -path '*/target' -prune -o \\( -name '*.yaml' -o -name '*.yml' \\) -type f | sort`
   - `perl -ne 'print if /domain_name:/ && !/domain_name: z00z\\.core\\.[a-z_]+\\.[a-z0-9_]+\\.devnet\\.v1$/' .planning/phases/063-Core-Update/063-core-examples.md`
   - `rg -n "src/(assets|genesis)/.*\\.ya?ml|examples/.+\\.ya?ml|tests/vectors|crates/z00z_core/configs|genesis_config_devnet_small|devnet_actions_config" crates/z00z_core wiki`
   - `rg -n "src/genesis/genesis_config_devnet|genesis_config_devnet_small|crates/z00z_core/configs|devnet_actions_config" wiki crates/z00z_core/docs crates/z00z_core/README.md crates/z00z_core`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`z00z_config` is the only live core YAML root and live fields match real code support.
</done>
</task>
</tasks>

<threat_model>
- Partial YAML migration can strand callers on dead paths or create a second authority root.
- Dead config fields can create silent drift between examples and runtime behavior.
</threat_model>

<verification>
This slice is valid only if loaders, YAML locations, field support, and release tests all converge on one canonical root.
</verification>

<success_criteria>
- One canonical YAML root exists under `z00z_core`.
- Live example fields are parsed and validated; fields not yet consumed by runtime materialization remain explicit parser-owned scope rather than dead YAML.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-10-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-10-SUMMARY.md`
- canonical `z00z_config` YAML root and parser/materialization coverage

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P1-07` | `PLAN-063-G10` | `063-TODO.md (P1. Make z00z_core/z00z_config the only core YAML authority)`; `063-core-examples.md`; live YAML owners and loaders | `063-TODO.md`; `063-CONTEXT.md`; `063-core-examples.md`; all core YAML files | `z00z_config/**`, loaders, tests, migrated YAML owners | `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`; YAML-root scans | one canonical core YAML root with live field support only | real parser/loader only | changed YAML paths plus loader/test rewrites | `Complete` |
