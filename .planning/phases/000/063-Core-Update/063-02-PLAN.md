---
phase: 063-Core-Update
plan: PLAN-063-G02
type: execute
wave: 1
depends_on:
  - PLAN-063-G01
files_modified:
  - crates/z00z_core/src/genesis/mod.rs
  - crates/z00z_core/src/genesis/genesis.rs
  - crates/z00z_core/src/genesis/validator.rs
  - crates/z00z_core/src/genesis/genesis_output.rs
  - crates/z00z_core/src/genesis/genesis_output_support.rs
  - crates/z00z_core/src/lib.rs
requirements:
  - REC-063-P0-02
autonomous: true
must_haves:
  truths:
    - "`z00z_core::genesis::*` remains the only canonical caller path."
    - "Boundary-defining modules must not be assembled by `include!`."
    - "No deep public owner path survives as a second bootstrap facade."
  artifacts:
    - crates/z00z_core/src/genesis/mod.rs
    - crates/z00z_core/src/genesis/genesis.rs
    - crates/z00z_core/src/genesis/validator.rs
    - crates/z00z_core/src/lib.rs
  key_links:
    - "Scope: public bootstrap ownership normalization."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G02 Plan: Canonical Genesis Public Owner Path

<objective>
Close `REC-063-P0-02` by collapsing `genesis` to one explicit public owner path
and removing `include!`-assembled boundary layers.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
@crates/z00z_core/src/lib.rs
@crates/z00z_core/src/genesis/mod.rs
@crates/z00z_core/src/genesis/genesis.rs
@crates/z00z_core/src/genesis/validator.rs
@crates/z00z_core/src/genesis/genesis_output.rs
</context>

<coverage_contract>
This plan covers `REC-063-P0-02` only. It closes only when caller-visible
bootstrap ownership is singular and boundary modules are explicit submodules.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G02`
task_ids:
- `REC-063-P0-02`
copied_task_rows:
- "| REC-063-P0-02 | P0. Collapse genesis to one canonical public owner path | Make `z00z_core::genesis::*` the only canonical caller path, convert deep implementation from `include!` assembly to explicit internal submodules, and internalize extra public owner layers. |"
source_refs:
- `./063-TODO.md#p0-collapse-genesis-to-one-canonical-public-owner-path`
- `.github/requirements/Z00Z_DESIGN_FOUNDATION.md`
- `crates/z00z_core/src/genesis/mod.rs`
- `crates/z00z_core/src/genesis/genesis.rs`
- `crates/z00z_core/src/genesis/validator.rs`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- design-foundation split rules
- live genesis module tree
outputs:
- `.planning/phases/063-Core-Update/063-02-SUMMARY.md`
- explicit genesis and validator internal submodules
- one canonical public bootstrap owner path
- downstream import inventory for every removed deep owner-path caller
- `genesis_output.rs` merged into its owner or demoted from boundary-defining status
dependencies:
- `PLAN-063-G01`
acceptance_tests:
- `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
- `cargo test --release -p z00z_core --lib genesis -- --nocapture`
- `rg -n "include!|pub mod genesis;|pub use .*genesis::genesis" crates/z00z_core/src/genesis crates/z00z_core/src/lib.rs`
simulation_gate:
- No simulation. Use the real module tree and public API surface.
negative_tests:
- no second public bootstrap path survives
- no boundary `include!` assembly survives in the canonical genesis surface
- no downstream caller may keep depending on the removed deep owner path after the same-wave rewrite
</plan_matrix>

<plan_artifacts>
- `genesis-mod`: `crates/z00z_core/src/genesis/mod.rs`
- `genesis-boundary`: `crates/z00z_core/src/genesis/genesis.rs`
- `validator-boundary`: `crates/z00z_core/src/genesis/validator.rs`
- `crate-root`: `crates/z00z_core/src/lib.rs`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
- `rg -n "include!|z00z_core::genesis::genesis" crates/z00z_core/src crates/z00z_core/tests crates/z00z_core/docs`
</plan_tests>

<plan_results>
- `PLAN-063-G02` produces `063-02-SUMMARY.md` with caller-path inventory, downstream import audit, and boundary cleanup evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P0-02`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/src/lib.rs`
    - `crates/z00z_core/src/genesis/mod.rs`
    - `crates/z00z_core/src/genesis/genesis.rs`
    - `crates/z00z_core/src/genesis/validator.rs`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P0-02`
  commands:
    - `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
    - `rg -n "include!|z00z_core::genesis::genesis" crates/z00z_core/src crates/z00z_core/tests crates/z00z_core/docs`
  scenarios:
    - `shallow public bootstrap facade remains canonical`
    - `deep public owner path and include-assembly are removed`
    - `downstream imports are inventoried before any deep-path removal lands`
</task_tests>

<task_results>
- task_id: `REC-063-P0-02`
  expected_results:
    - `genesis boundary modules are explicit and reviewable`
    - `anti-placeholder evidence includes real module-tree changes and import scans`
</task_results>

<anti_placeholder_gate>
- Re-export churn without removing `include!` boundary assembly does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/src/genesis/mod.rs`
- `crates/z00z_core/src/genesis/genesis.rs`
- `crates/z00z_core/src/genesis/validator.rs`
- `crates/z00z_core/src/lib.rs`
</current_code_refs>

<blockers>
- Any unresolved downstream import still requiring `z00z_core::genesis::genesis::*` must be inventoried and rewritten in the same wave or recorded as a phase-failing blocker.
- Caller-visible path changes must not land before the downstream import inventory is complete.
</blockers>

<evidence_gate>
- Summary evidence must show zero remaining canonical callers on the deep public owner path plus the final `genesis_output.rs` boundary decision.
</evidence_gate>

<not_recommendation_gate>
- This slice must ship structural ownership cleanup, not a doc note about future cleanup.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Collapse genesis to one public owner path</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `.github/requirements/Z00Z_DESIGN_FOUNDATION.md`
- `crates/z00z_core/src/lib.rs`
- `crates/z00z_core/src/genesis/mod.rs`
- `crates/z00z_core/src/genesis/genesis.rs`
- `crates/z00z_core/src/genesis/validator.rs`
</read_first>
<files>
- `crates/z00z_core/src/lib.rs`
- `crates/z00z_core/src/genesis/mod.rs`
- `crates/z00z_core/src/genesis/genesis.rs`
- `crates/z00z_core/src/genesis/validator.rs`
- `crates/z00z_core/src/genesis/genesis_output.rs`
</files>
<action>
Replace boundary `include!` assembly with explicit internal submodules, keep the
shallow `z00z_core::genesis::*` caller path canonical, and eliminate deep
public-owner duplication before broader namespace or config migrations land.
</action>
<acceptance_criteria>
- `z00z_core::genesis::*` is the only canonical public bootstrap owner path.
- `genesis.rs` and `validator.rs` are no longer assembled from `include!` fragments.
- Public re-exports are narrow and explicit.
- Downstream deep-path callers are inventoried before removal and rewritten in the same wave.
- `genesis_output.rs` is merged into its real owner module or explicitly demoted from boundary status.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
   - `rg -n "include!|z00z_core::genesis::genesis|pub use .*genesis::genesis" crates/z00z_core/src crates/z00z_core/tests crates/z00z_core/docs`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
Genesis public ownership is singular, explicit, and reviewable.
</done>
</task>
</tasks>

<threat_model>
- Multiple public owner paths create concept drift and import instability.
- `include!` boundary assembly hides ownership and review surface.
</threat_model>

<verification>
This slice is valid only if public imports and module assembly both converge on the canonical shallow facade.
</verification>

<success_criteria>
- One canonical bootstrap caller path remains.
- Boundary modules are explicit and no longer composed through `include!`.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-02-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-02-SUMMARY.md`
- updated genesis boundary modules and caller-path cleanup

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P0-02` | `PLAN-063-G02` | `063-TODO.md#p0-collapse-genesis-to-one-canonical-public-owner-path`; design foundation; live genesis module tree | `063-TODO.md`; `063-CONTEXT.md`; genesis boundary files | `lib.rs`; `genesis/mod.rs`; `genesis.rs`; `validator.rs` | `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`; import scans | one canonical public bootstrap owner path | real module tree only | changed module structure plus zero stale-path scan | `Complete` |
