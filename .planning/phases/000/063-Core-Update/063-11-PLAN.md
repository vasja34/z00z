---
phase: 063-Core-Update
plan: PLAN-063-G11
type: execute
wave: 3
depends_on:
  - PLAN-063-G07
  - PLAN-063-G10
files_modified:
  - crates/z00z_core/Cargo.toml
  - crates/z00z_core/tests/**
  - crates/z00z_core/src/assets/test_*.rs
  - crates/z00z_core/src/genesis/test_*.rs
  - crates/z00z_core/src/policies/mod.rs
  - crates/z00z_core/src/vouchers/mod.rs
requirements:
  - REC-063-P2-01
autonomous: true
must_haves:
  truths:
    - "Integration tests flatten under `crates/z00z_core/tests/`."
    - "`*_suite.rs` naming is removed."
    - "Rights and vouchers get explicit object-family test ownership under the canonical root."
  artifacts:
    - crates/z00z_core/Cargo.toml
    - crates/z00z_core/tests/**
    - crates/z00z_core/src/*/test_*.rs
  key_links:
    - "Scope: test layout, naming, and object-family ownership normalization."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G11 Plan: Test Ownership And Naming Flattening

<objective>
Close `REC-063-P2-01` by flattening `z00z_core` test ownership, removing
`*_suite.rs` naming, and making rights/vouchers coverage explicit under the
canonical test root.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@crates/z00z_core/Cargo.toml
@crates/z00z_core/tests
@crates/z00z_core/src/policies/mod.rs
@crates/z00z_core/src/vouchers/mod.rs
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<coverage_contract>
This plan covers `REC-063-P2-01` only. It closes only when the integration test
tree is flattened, names follow `test_*.rs`, and rights/vouchers test coverage
is owned from the canonical root.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G11`
task_ids:
- `REC-063-P2-01`
copied_task_rows:
- "| REC-063-P2-01 | P2. Flatten test ownership and align names with the repository standard | Flatten `crates/z00z_core/tests`, keep only `tests/fixtures` as a subdirectory, rename `*_suite.rs`, move vectors to `z00z_config`, remove cross-owner `#[path = ...]` test wiring, and add explicit rights/vouchers test files. |"
source_refs:
- `./063-TODO.md#p2-flatten-test-ownership-and-align-names-with-the-repository-standard`
- `.github/requirements/Z00Z_DESIGN_FOUNDATION.md`
- `crates/z00z_core/Cargo.toml`
- `crates/z00z_core/tests/**`
- `crates/z00z_core/src/policies/mod.rs`
- `crates/z00z_core/src/vouchers/mod.rs`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- live test tree and manifest paths
outputs:
- `.planning/phases/063-Core-Update/063-11-SUMMARY.md`
- flattened integration test root
- canonical `test_*.rs` naming and object-family ownership
dependencies:
- `PLAN-063-G07`
- `PLAN-063-G10`
acceptance_tests:
- `cargo test --release -p z00z_core --tests`
- `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
- `find crates/z00z_core/tests -mindepth 2 -type f ! -path '*/tests/fixtures/*'`
- `rg -n "test_integration_assets_test|_suite\\.rs|tests/(assets|genesis|rights|vauchers|vectors)|#\\[path = \"\\.\\./assets" crates/z00z_core/Cargo.toml crates/z00z_core/tests crates/z00z_core/src`
simulation_gate:
- No simulation. The live test tree and manifest entries must be rewritten.
negative_tests:
- no nested owned-test directories survive outside `tests/fixtures`
- no legacy `vauchers` test file names survive
- numeric test filenames must not survive when the file has a clear behavior theme that can be named directly
</plan_matrix>

<plan_artifacts>
- `manifest-tests`: `crates/z00z_core/Cargo.toml`
- `integration-tests`: `crates/z00z_core/tests/**`
- `source-tests`: `crates/z00z_core/src/assets/test_*.rs`, `crates/z00z_core/src/genesis/test_*.rs`
- `owner-mods`: `crates/z00z_core/src/policies/mod.rs`, `crates/z00z_core/src/vouchers/mod.rs`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core --tests`
- `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
- `find crates/z00z_core/tests -mindepth 2 -type f ! -path '*/tests/fixtures/*'`
- `rg -n "test_integration_assets_test|_suite\\.rs|tests/(assets|genesis|rights|vauchers|vectors)" crates/z00z_core/Cargo.toml crates/z00z_core/tests crates/z00z_core/src`
</plan_tests>

<plan_results>
- `PLAN-063-G11` produces `063-11-SUMMARY.md` with flattened-test-tree and rename-guard evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P2-01`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/Cargo.toml`
    - `crates/z00z_core/tests/**`
    - `crates/z00z_core/src/policies/mod.rs`
    - `crates/z00z_core/src/vouchers/mod.rs`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P2-01`
  commands:
    - `cargo test --release -p z00z_core --tests`
    - `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
    - `find crates/z00z_core/tests -mindepth 2 -type f ! -path '*/tests/fixtures/*'`
    - `rg -n "test_integration_assets_test|_suite\\.rs|tests/(assets|genesis|rights|vauchers|vectors)" crates/z00z_core/Cargo.toml crates/z00z_core/tests crates/z00z_core/src`
  scenarios:
    - `integration tests are flat and canonically named`
    - `rights and vouchers have explicit owned coverage`
</task_tests>

<task_results>
- task_id: `REC-063-P2-01`
  expected_results:
    - `owned integration tests are visible from one root`
    - `anti-placeholder evidence includes zero-stale-layout scans and passing release tests`
</task_results>

<anti_placeholder_gate>
- Renaming only `Cargo.toml` entries without moving the actual test files does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/Cargo.toml`
- `crates/z00z_core/tests/**`
- `crates/z00z_core/src/policies/mod.rs`
- `crates/z00z_core/src/vouchers/mod.rs`
</current_code_refs>

<blockers>
- Any intentional rename-guard updates must land in the same wave as file moves.
</blockers>

<evidence_gate>
- Summary evidence must include the post-move test file list and zero stale-path scan output.
</evidence_gate>

<not_recommendation_gate>
- This slice must produce the real flattened tree, not a follow-up note to do it later.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Flatten test ownership and canonicalize test names</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `crates/z00z_core/Cargo.toml`
- `crates/z00z_core/tests/**`
- `crates/z00z_core/src/policies/mod.rs`
- `crates/z00z_core/src/vouchers/mod.rs`
</read_first>
<files>
- `crates/z00z_core/Cargo.toml`
- `crates/z00z_core/tests/**`
- `crates/z00z_core/src/assets/test_*.rs`
- `crates/z00z_core/src/genesis/test_*.rs`
- `crates/z00z_core/src/policies/mod.rs`
- `crates/z00z_core/src/vouchers/mod.rs`
</files>
<action>
Flatten the integration test tree, rename suite-style files to canonical
`test_*.rs` names, remove cross-owner path wiring, move vector-backed tests to
the canonical config root or Rust fixtures, and add explicit rights/vouchers
owned coverage under the flat test root.
</action>
<acceptance_criteria>
- `crates/z00z_core/tests/` is flat except for `tests/fixtures`.
- `tests/fixtures` is the only allowed subdirectory under `crates/z00z_core/tests/`.
- No `*_suite.rs` names remain.
- YAML vectors leave `tests/vectors` and are loaded from `z00z_config` or Rust fixtures instead.
- Rights and vouchers coverage is visible under the canonical test root.
- Release integration tests pass after the move.
- Rights tests cover config parsing, terminal-id determinism, policy binding, lifecycle windows, payload commitments, and zero-value authority semantics.
- Voucher tests cover bootstrap materialization, policy binding, replay nonce derivation, lifecycle state, backing, refund target, partial redemption, and expired voucher rejection.
- Asset tests keep the existing coverage under clearer flattened names.
- Genesis tests keep full-bootstrap parity and manifest golden coverage after the path changes.
- Prefer meaningful names when the file has a clear behavior theme, instead of keeping numeric-only filenames.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core --tests`
   - `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
   - `find crates/z00z_core/tests -mindepth 2 -type f ! -path '*/tests/fixtures/*'`
   - `rg -n "test_integration_assets_test|_suite\\.rs|tests/(assets|genesis|rights|vauchers|vectors)|#\\[path = \"\\.\\./assets" crates/z00z_core/Cargo.toml crates/z00z_core/tests crates/z00z_core/src`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`z00z_core` test ownership is flat, canonical, and release-validated.
</done>
</task>
</tasks>

<threat_model>
- Mixed test ownership hides coverage gaps and increases rename fragility.
</threat_model>

<verification>
This slice is valid only if the test tree, manifest, and naming all converge on the repository standard.
</verification>

<success_criteria>
- Flat canonical test root is visible.
- Rights and vouchers coverage is explicit.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-11-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-11-SUMMARY.md`
- flattened canonical test layout and naming

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P2-01` | `PLAN-063-G11` | `063-TODO.md#p2-flatten-test-ownership-and-align-names-with-the-repository-standard`; live test tree and manifest | `063-TODO.md`; `063-CONTEXT.md`; `Cargo.toml`; tests tree | manifest, tests tree, owner modules | `cargo test --release -p z00z_core --tests`; zero-stale-layout scans | flat canonical test ownership | real test tree only | changed layout plus passing release tests | `Complete` |
