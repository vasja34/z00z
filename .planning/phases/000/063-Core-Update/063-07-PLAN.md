---
phase: 063-Core-Update
plan: PLAN-063-G07
type: execute
wave: 2
depends_on:
  - PLAN-063-G02
  - PLAN-063-G03
files_modified:
  - crates/z00z_core/src/lib.rs
  - crates/z00z_core/src/vouchers/mod.rs
  - crates/z00z_storage/src/**
  - crates/z00z_wallets/src/**
  - crates/z00z_simulator/src/**
  - crates/z00z_simulator/tests/**
  - crates/z00z_core/README.md
requirements:
  - REC-063-P1-04
autonomous: true
must_haves:
  truths:
    - "`z00z_core::vouchers` becomes the only canonical path."
    - "All callers are rewritten in the same wave."
    - "Voucher bootstrap semantics stay intact while the misspelling is removed."
  artifacts:
    - crates/z00z_core/src/lib.rs
    - crates/z00z_core/src/vouchers/mod.rs
    - downstream caller inventory and rewrites
  key_links:
    - "Scope: namespace normalization without semantic drift."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G07 Plan: Vouchers Namespace Normalization

<objective>
Close `REC-063-P1-04` by rewriting the misspelled `vauchers` surface to the
canonical `vouchers` path in one coordinated patch set.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@crates/z00z_core/src/lib.rs
@crates/z00z_core/src/vouchers/mod.rs
@crates/z00z_core/README.md
@crates/z00z_storage
@crates/z00z_wallets
@crates/z00z_simulator
</context>

<coverage_contract>
This plan covers `REC-063-P1-04` only. It closes only when the misspelled
public lane is fully removed and all live callers use `vouchers`.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G07`
task_ids:
- `REC-063-P1-04`
copied_task_rows:
- "| REC-063-P1-04 | P1. Fix the public namespace debt around vauchers | Introduce `z00z_core::vouchers` as the canonical path, rewrite every caller in the same wave, and remove `vauchers` without a compatibility alias. |"
source_refs:
- `./063-TODO.md#p1-fix-the-public-namespace-debt-around-vauchers`
- `crates/z00z_core/src/lib.rs`
- `crates/z00z_core/src/vouchers/mod.rs`
- `crates/z00z_core/README.md`
- downstream imports across `z00z_storage`, `z00z_wallets`, and `z00z_simulator`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- live namespace imports across the workspace
outputs:
- `.planning/phases/063-Core-Update/063-07-SUMMARY.md`
- canonical `vouchers` module path across the workspace
- full caller inventory for storage, wallets, simulator, tests, docs, and examples
- clarified voucher bootstrap semantics in docs
dependencies:
- `PLAN-063-G02`
- `PLAN-063-G03`
acceptance_tests:
- `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`
- `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`
- `rg -n "VoucherBootstrapEntryV1" crates/z00z_core crates/z00z_wallets crates/z00z_storage crates/z00z_simulator`
- `rg -n "\\bvauchers\\b" crates/z00z_core crates/z00z_storage crates/z00z_wallets crates/z00z_simulator`
simulation_gate:
- No simulation. This is a real namespace and caller rewrite.
negative_tests:
- no compatibility alias or shim may survive
- voucher bootstrap semantics must not be renamed into a different concept
- `VoucherBootstrapEntryV1` must not be renamed away or reframed as a runtime voucher object
</plan_matrix>

<plan_artifacts>
- `core-root`: `crates/z00z_core/src/lib.rs`
- `voucher-owner`: `crates/z00z_core/src/vouchers/mod.rs`
- `downstream-callers`: `crates/z00z_storage/src/**`, `crates/z00z_wallets/src/**`, `crates/z00z_simulator/src/**`
- `voucher-docs`: `crates/z00z_core/README.md`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`
- `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`
- `rg -n "VoucherBootstrapEntryV1" crates/z00z_core crates/z00z_wallets crates/z00z_storage crates/z00z_simulator`
- `rg -n "\\bvauchers\\b" crates/z00z_core crates/z00z_storage crates/z00z_wallets crates/z00z_simulator`
</plan_tests>

<plan_results>
- `PLAN-063-G07` produces `063-07-SUMMARY.md` with full caller inventory and zero-legacy-path evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P1-04`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/src/lib.rs`
    - `crates/z00z_core/src/vouchers/mod.rs`
    - `crates/z00z_storage/src/**`
    - `crates/z00z_wallets/src/**`
    - `crates/z00z_simulator/src/**`
    - `crates/z00z_core/README.md`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P1-04`
  commands:
    - `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`
    - `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`
    - `rg -n "VoucherBootstrapEntryV1" crates/z00z_core crates/z00z_wallets crates/z00z_storage crates/z00z_simulator`
    - `rg -n "\\bvauchers\\b" crates/z00z_core crates/z00z_storage crates/z00z_wallets crates/z00z_simulator`
  scenarios:
    - `all live callers use the canonical vouchers namespace`
    - `voucher bootstrap semantics remain unchanged and caller inventory is complete before removal`
</task_tests>

<task_results>
- task_id: `REC-063-P1-04`
  expected_results:
    - `legacy misspelling is absent from live code and docs`
    - `anti-placeholder evidence includes zero-legacy-path scans and passing voucher flows`
</task_results>

<anti_placeholder_gate>
- Adding `vouchers` while leaving `vauchers` as a live alias does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/src/lib.rs`
- `crates/z00z_core/src/vouchers/mod.rs`
- downstream caller imports across storage, wallets, and simulator
</current_code_refs>

<blockers>
- Any surviving downstream caller or doc using `vauchers` is phase-failing for this slice.
</blockers>

<evidence_gate>
- Summary evidence must include the full caller inventory, zero remaining `vauchers` hits in the live workspace, and the preserved `VoucherBootstrapEntryV1` semantic boundary.
</evidence_gate>

<not_recommendation_gate>
- This slice must remove the legacy path in code, not merely document the preferred spelling.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Normalize the vouchers namespace in one wave</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `crates/z00z_core/src/lib.rs`
- `crates/z00z_core/src/vouchers/mod.rs`
- workspace caller inventory under `crates/z00z_storage`, `crates/z00z_wallets`, and `crates/z00z_simulator`
</read_first>
<files>
- `crates/z00z_core/src/lib.rs`
- `crates/z00z_core/src/vouchers/mod.rs`
- `crates/z00z_storage/src/**`
- `crates/z00z_wallets/src/**`
- `crates/z00z_simulator/src/**`
- `crates/z00z_core/README.md`
</files>
<action>
Rewrite the core and downstream namespace to `vouchers`, preserve
`VoucherBootstrapEntryV1` semantics, and remove the misspelled public lane in
the same wave with no alias or shim left behind.
</action>
<acceptance_criteria>
- `z00z_core::vouchers` is the only canonical voucher owner path.
- All internal and downstream callers are updated in the same wave.
- Voucher bootstrap semantics are clarified without type-concept drift.
- `VoucherBootstrapEntryV1` remains the bootstrap-manifest concept rather than being renamed into a different runtime shape.
- Vouchers are not documented or coded as reducible to `asset + right`.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`
   - `cargo test --release -p z00z_wallets rpc::methods::asset_rpc_impl::test_asset_impl::test_object_rpc_lists_inventory -- --nocapture`
   - `rg -n "\\bvauchers\\b" crates/z00z_core crates/z00z_storage crates/z00z_wallets crates/z00z_simulator`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
The canonical vouchers namespace is normalized across the live workspace.
</done>
</task>
</tasks>

<threat_model>
- Carrying both spellings would preserve namespace debt and future concept drift.
</threat_model>

<verification>
This slice is valid only if the misspelled lane disappears from all live callers.
</verification>

<success_criteria>
- `vouchers` is canonical everywhere live.
- Voucher semantics stay intact while the misspelling disappears.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-07-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-07-SUMMARY.md`
- canonical vouchers namespace across the core and downstream crates

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P1-04` | `PLAN-063-G07` | `063-TODO.md#p1-fix-the-public-namespace-debt-around-vauchers`; core and downstream imports | `063-TODO.md`; `063-CONTEXT.md`; workspace caller inventory | core namespace files, downstream callers, docs | `cargo test --release -p z00z_core test_voucher_config_validates_shape -- --nocapture`; `rg -n "\\bvauchers\\b" ...` | canonical `vouchers` path with no live legacy alias | real workspace rewrite only | zero-legacy-path scan plus passing voucher tests | `Complete` |
