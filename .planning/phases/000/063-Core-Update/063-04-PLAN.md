---
phase: 063-Core-Update
plan: PLAN-063-G04
type: execute
wave: 2
depends_on:
  - PLAN-063-G02
  - PLAN-063-G03
files_modified:
  - crates/z00z_core/src/assets/registry_config.rs
  - crates/z00z_core/src/assets/registry_catalog.rs
  - crates/z00z_core/src/assets/registry_catalog_load.rs
  - crates/z00z_core/src/assets/mod.rs
  - crates/z00z_core/src/assets/registry.rs
  - crates/z00z_core/src/genesis/genesis_config.rs
  - crates/z00z_core/README.md
  - crates/z00z_core/src/genesis/README.md
  - crates/z00z_core/tests/test_live_guardrails.rs
requirements:
  - REC-063-P1-01
autonomous: true
must_haves:
  truths:
    - "`GenesisConfig` stays the only bootstrap authority."
    - "Secondary registry YAML vocabulary must look secondary in code shape, not only in prose."
    - "Shared parsing logic may survive without preserving authority ambiguity."
  artifacts:
    - crates/z00z_core/src/assets/registry_config.rs
    - crates/z00z_core/src/genesis/genesis_config.rs
    - crates/z00z_core/src/assets/mod.rs
  key_links:
    - "Scope: bootstrap-authority code-shape enforcement."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G04 Plan: Single Bootstrap Authority Code-Shape Enforcement

<objective>
Close `REC-063-P1-01` by making the secondary registry YAML lane visibly
secondary in type names, paths, and docs while preserving one canonical
`GenesisConfig` bootstrap authority.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@crates/z00z_core/README.md
@crates/z00z_core/src/genesis/README.md
@crates/z00z_core/src/assets/mod.rs
@crates/z00z_core/src/assets/registry_config.rs
@crates/z00z_core/src/assets/registry_catalog.rs
@crates/z00z_core/src/assets/registry_catalog_load.rs
@crates/z00z_core/src/genesis/genesis_config.rs
</context>

<coverage_contract>
This plan covers `REC-063-P1-01` only. It closes only when the secondary YAML
lane can no longer be mistaken for a competing bootstrap authority.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G04`
task_ids:
- `REC-063-P1-01`
copied_task_rows:
- "| REC-063-P1-01 | P1. Enforce the single bootstrap authority in code shape, not only in prose | Keep shared parsing logic, but separate authority vocabulary from registry-data vocabulary and preserve `GenesisConfig` as the only typed authority for bootstrap. |"
source_refs:
- `./063-TODO.md#p1-enforce-the-single-bootstrap-authority-in-code-shape-not-only-in-prose`
- `crates/z00z_core/README.md`
- `crates/z00z_core/src/assets/registry_config.rs`
- `crates/z00z_core/src/genesis/genesis_config.rs`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- live registry-config and genesis-config loaders
outputs:
- `.planning/phases/063-Core-Update/063-04-SUMMARY.md`
- renamed or relocated secondary-lane vocabulary
- docs and API names that clearly separate bootstrap authority from registry data
dependencies:
- `PLAN-063-G02`
- `PLAN-063-G03`
acceptance_tests:
- `cargo test --release -p z00z_core --test assets_tests test_config -- --nocapture`
- `cargo test --release -p z00z_core --test assets_tests -- --nocapture`
- `cargo test --release -p z00z_core --test test_live_guardrails -- --nocapture`
- `rg -n "AssetDefinitionRegistry::load_from_config|load_registry_from_yaml|assets_config::|mod assets_config|assets_config_load|src/assets/assets_config\\.rs|GLOBAL_ASSET_REGISTRY\\.load_catalog_from_yaml|load_from_file\\(" crates/z00z_core`
simulation_gate:
- No simulation. The loader and docs must express the real authority model directly.
negative_tests:
- secondary registry lane must not read like a second bootstrap path
- shared parsing must not create duplicate authoritative types
</plan_matrix>

<plan_artifacts>
- `registry-loader`: `crates/z00z_core/src/assets/registry_config.rs`
- `registry-catalog`: `crates/z00z_core/src/assets/registry_catalog.rs`
- `registry-catalog-load`: `crates/z00z_core/src/assets/registry_catalog_load.rs`
- `genesis-loader`: `crates/z00z_core/src/genesis/genesis_config.rs`
- `assets-doc`: `crates/z00z_core/src/assets/mod.rs`
- `authority-docs`: `crates/z00z_core/README.md`, `crates/z00z_core/src/genesis/README.md`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core --test assets_tests test_config -- --nocapture`
- `cargo test --release -p z00z_core --test assets_tests -- --nocapture`
- `cargo test --release -p z00z_core --test test_live_guardrails -- --nocapture`
- `rg -n "AssetDefinitionRegistry::load_from_config|load_registry_from_yaml|assets_config::|mod assets_config|assets_config_load|src/assets/assets_config\\.rs|GLOBAL_ASSET_REGISTRY\\.load_catalog_from_yaml|load_from_file\\(" crates/z00z_core`
</plan_tests>

<plan_results>
- `PLAN-063-G04` produces `063-04-SUMMARY.md` with code-shape and vocabulary evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P1-01`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/src/assets/registry_config.rs`
    - `crates/z00z_core/src/assets/registry_catalog.rs`
    - `crates/z00z_core/src/assets/registry_catalog_load.rs`
    - `crates/z00z_core/src/assets/registry.rs`
    - `crates/z00z_core/src/genesis/genesis_config.rs`
    - `crates/z00z_core/src/assets/mod.rs`
    - `crates/z00z_core/README.md`
    - `crates/z00z_core/src/genesis/README.md`
    - `crates/z00z_core/tests/test_live_guardrails.rs`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P1-01`
  commands:
    - `cargo test --release -p z00z_core --test assets_tests test_config -- --nocapture`
    - `cargo test --release -p z00z_core --test assets_tests -- --nocapture`
    - `cargo test --release -p z00z_core --test test_live_guardrails -- --nocapture`
    - `rg -n "AssetDefinitionRegistry::load_from_config|load_registry_from_yaml|assets_config::|mod assets_config|assets_config_load|src/assets/assets_config\\.rs|GLOBAL_ASSET_REGISTRY\\.load_catalog_from_yaml|load_from_file\\(" crates/z00z_core`
  scenarios:
    - `bootstrap lane stays singular`
    - `secondary registry lane is explicitly secondary in naming and docs`
</task_tests>

<task_results>
- task_id: `REC-063-P1-01`
  expected_results:
    - `registry loader names and docs no longer mimic bootstrap ownership`
    - `anti-placeholder evidence includes real API or path naming cleanup`
</task_results>

<anti_placeholder_gate>
- Prose-only disclaimers without API or path vocabulary cleanup do not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/src/assets/registry_config.rs`
- `crates/z00z_core/src/assets/registry_catalog.rs`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/README.md`
</current_code_refs>

<blockers>
- If a public secondary YAML API must remain for compatibility, its secondary role must be visible in the type/function names and docs of the same patch.
</blockers>

<evidence_gate>
- Summary evidence must show that only `GenesisConfig` remains the canonical bootstrap type.
</evidence_gate>

<not_recommendation_gate>
- This slice must change the live code shape, not only surrounding prose.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Make bootstrap authority singular in code shape</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `crates/z00z_core/README.md`
- `crates/z00z_core/src/genesis/README.md`
- `crates/z00z_core/src/assets/registry_config.rs`
- `crates/z00z_core/src/genesis/genesis_config.rs`
</read_first>
<files>
- `crates/z00z_core/src/assets/registry_config.rs`
- `crates/z00z_core/src/assets/registry_catalog.rs`
- `crates/z00z_core/src/assets/registry_catalog_load.rs`
- `crates/z00z_core/src/assets/registry.rs`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/assets/mod.rs`
- `crates/z00z_core/README.md`
- `crates/z00z_core/src/genesis/README.md`
- `crates/z00z_core/tests/test_live_guardrails.rs`
</files>
<action>
Preserve shared parsing logic where it reduces duplication, but rename or
relocate the registry-data lane so the code itself communicates that it is
secondary to canonical bootstrap loading.
</action>
<acceptance_criteria>
- `GenesisConfig` remains the only canonical bootstrap type.
- Secondary registry YAML loading reads as registry-data input, not as a second genesis path.
- Tests and docs reflect the same authority model.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core --test assets_tests test_config -- --nocapture`
   - `cargo test --release -p z00z_core --test assets_tests -- --nocapture`
   - `cargo test --release -p z00z_core --test test_live_guardrails -- --nocapture`
   - `rg -n "AssetDefinitionRegistry::load_from_config|load_registry_from_yaml|assets_config::|mod assets_config|assets_config_load|src/assets/assets_config\\.rs|GLOBAL_ASSET_REGISTRY\\.load_catalog_from_yaml|load_from_file\\(" crates/z00z_core`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
The live code shape makes `GenesisConfig` the only bootstrap authority.
</done>
</task>
</tasks>

<threat_model>
- Secondary-loader symmetry can create operator confusion and future code drift.
</threat_model>

<verification>
This slice is valid only if code, tests, and docs all point to one authoritative bootstrap lane.
</verification>

<success_criteria>
- The secondary YAML lane is visibly secondary in naming and docs.
- `GenesisConfig` remains singular and canonical.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-04-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-04-SUMMARY.md`
- authority-vocabulary cleanup across bootstrap and registry loaders

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P1-01` | `PLAN-063-G04` | `063-TODO.md#p1-enforce-the-single-bootstrap-authority-in-code-shape-not-only-in-prose`; registry and genesis loaders | `063-TODO.md`; `063-CONTEXT.md`; loader/docs anchors | registry loader, genesis loader, docs | `cargo test --release -p z00z_core --test assets_tests test_config -- --nocapture`; `cargo test --release -p z00z_core --test assets_tests -- --nocapture`; `cargo test --release -p z00z_core --test test_live_guardrails -- --nocapture` | code shape enforces one bootstrap authority | real loaders only | renamed or relocated secondary lane plus tests/docs | `Complete` |
