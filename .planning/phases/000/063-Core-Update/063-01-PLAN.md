---
phase: 063-Core-Update
plan: PLAN-063-G01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_core/src/genesis/genesis_config.rs
  - crates/z00z_core/src/genesis/genesis_config_validate.rs
  - crates/z00z_core/src/genesis/genesis_run.rs
  - crates/z00z_core/src/genesis/genesis_output_support.rs
  - crates/z00z_core/src/genesis/genesis_settlement_manifest.rs
  - crates/z00z_core/tests/test_genesis_manifest_refs.rs
  - crates/z00z_core/tests/test_genesis_validation.rs
requirements:
  - REC-063-P0-01
autonomous: true
must_haves:
  truths:
    - "`performance.num_threads` must become a real local execution contract or be removed."
    - "`snapshot_export_path` must have one exact live meaning."
    - "Regression tests must prove thread-count and artifact-path behavior."
  artifacts:
    - crates/z00z_core/src/genesis/genesis_run.rs
    - crates/z00z_core/src/genesis/genesis_config*.rs
    - crates/z00z_core/src/genesis/genesis_output_support.rs
    - crates/z00z_core/tests/test_genesis_manifest_refs.rs
  key_links:
    - "Scope: genesis runtime contract integrity."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G01 Plan: Genesis Execution Contract Restoration

<objective>
Close `REC-063-P0-01` by making the parsed genesis execution controls truthful,
deterministic, and test-backed.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@crates/z00z_core/src/genesis/genesis_config.rs
@crates/z00z_core/src/genesis/genesis_config_validate.rs
@crates/z00z_core/src/genesis/genesis_run.rs
@crates/z00z_core/src/genesis/genesis_output_support.rs
@crates/z00z_core/src/genesis/genesis_settlement_manifest.rs
</context>

<coverage_contract>
This plan covers `REC-063-P0-01` only. It closes only when thread-count and
artifact-path semantics match the live config contract and are proven by tests.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G01`
task_ids:
- `REC-063-P0-01`
copied_task_rows:
- "| REC-063-P0-01 | P0. Fix the genesis execution contract drift | Apply `performance.num_threads` through a dedicated local Rayon pool, define one exact meaning for `snapshot_export_path`, and add regression tests for thread-config and artifact-path behavior. |"
source_refs:
- `./063-TODO.md#p0-fix-the-genesis-execution-contract-drift`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_core/src/genesis/genesis_output_support.rs`
- `crates/z00z_core/src/genesis/genesis_settlement_manifest.rs`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- live genesis config and output helpers
outputs:
- `.planning/phases/063-Core-Update/063-01-SUMMARY.md`
- truthful local-thread execution behavior
- one canonical snapshot export path contract
- regression tests for thread-config and artifact-path behavior
dependencies: []
acceptance_tests:
- `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
- `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
- `cargo test --release -p z00z_core --test genesis_tests test_validation -- --nocapture`
- `rg -n "num_threads|snapshot_export_path|assets_export_path" crates/z00z_core/src/genesis crates/z00z_core/tests`
simulation_gate:
- No simulator substitution is allowed; use the real genesis runtime, a local Rayon pool, and the real output writer path.
negative_tests:
- configured thread count must not be silently ignored
- partial or split artifact-path semantics must fail tests
- local execution controls must not mutate process-global Rayon behavior seen by `z00z_simulator` or unrelated tests
- `build_global()` must not be introduced unless the repository explicitly chooses process-global Rayon behavior
</plan_matrix>

<plan_artifacts>
- `genesis-run`: `crates/z00z_core/src/genesis/genesis_run.rs`
- `genesis-config`: `crates/z00z_core/src/genesis/genesis_config.rs`
- `genesis-validate`: `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `genesis-output`: `crates/z00z_core/src/genesis/genesis_output_support.rs`
- `genesis-tests`: `crates/z00z_core/tests/test_genesis_manifest_refs.rs`, `crates/z00z_core/tests/test_genesis_validation.rs`
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
- `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
- `cargo test --release -p z00z_core --test genesis_tests test_validation -- --nocapture`
</plan_tests>

<plan_results>
- `PLAN-063-G01` produces `063-01-SUMMARY.md` with thread-contract and artifact-path evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P0-01`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/src/genesis/genesis_run.rs`
    - `crates/z00z_core/src/genesis/genesis_config.rs`
    - `crates/z00z_core/src/genesis/genesis_config_validate.rs`
    - `crates/z00z_core/src/genesis/genesis_output_support.rs`
    - `crates/z00z_core/tests/test_genesis_manifest_refs.rs`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P0-01`
  commands:
    - `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
    - `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
    - `cargo test --release -p z00z_core --test genesis_tests test_validation -- --nocapture`
  scenarios:
    - `configured local thread count is honored or removed from the live contract`
    - `snapshot outputs land exactly on the documented path contract`
</task_tests>

<task_results>
- task_id: `REC-063-P0-01`
  expected_results:
    - `parsed runtime fields match live generation behavior`
    - `anti-placeholder evidence includes path-sensitive and config-sensitive tests`
</task_results>

<anti_placeholder_gate>
- Logging a thread count without applying it does not close this plan.
- Merely rewriting docs without runtime and tests does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/genesis/genesis_output_support.rs`
</current_code_refs>

<blockers>
- Any unresolved ambiguity between `snapshot_export_path` and `assets_export_path` is phase-failing for this slice.
</blockers>

<evidence_gate>
- Summary evidence must name the exact chosen output-path contract and the exact tests proving it.
</evidence_gate>

<not_recommendation_gate>
- This slice must land a real runtime contract and tests, not a recommendation note.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Restore truthful genesis execution controls</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_core/src/genesis/genesis_output_support.rs`
</read_first>
<files>
- `crates/z00z_core/src/genesis/genesis_config.rs`
- `crates/z00z_core/src/genesis/genesis_config_validate.rs`
- `crates/z00z_core/src/genesis/genesis_run.rs`
- `crates/z00z_core/src/genesis/genesis_output_support.rs`
- `crates/z00z_core/tests/test_genesis_manifest_refs.rs`
- `crates/z00z_core/tests/test_genesis_validation.rs`
</files>
<action>
Install the real local execution contract for thread-count behavior, collapse
artifact-path semantics to one truthful contract, and lock both through release
tests before later boundary or config migrations begin.
</action>
<acceptance_criteria>
- `performance.num_threads` is either honored through a local pool or removed from the live config contract.
- `snapshot_export_path` and `assets_export_path` no longer describe conflicting live outputs.
- any surviving thread control remains local to genesis execution and does not surprise simulator or unrelated test flows
- `build_global()` is not used unless the repository intentionally adopts process-global Rayon behavior.
- Release tests prove the chosen behavior and reject stale semantics.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`
   - `cargo test --release -p z00z_core --test test_genesis_manifest_goldens -- --nocapture`
   - `cargo test --release -p z00z_core --test genesis_tests test_validation -- --nocapture`
   - `rg -n "num_threads|snapshot_export_path|assets_export_path" crates/z00z_core/src/genesis crates/z00z_core/tests`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
Genesis execution controls are truthful, deterministic, and release-tested.
</done>
</task>
</tasks>

<threat_model>
- Silent config drift can mislead operators into trusting fields that do nothing.
- Mixed output-path semantics can strand artifacts in the wrong directory.
- Process-global Rayon mutation can create non-deterministic cross-test or simulator behavior outside the genesis slice.
</threat_model>

<verification>
This slice is valid only if runtime behavior and release tests agree on one execution contract.
</verification>

<success_criteria>
- One truthful local execution contract for thread count and snapshot output exists.
- Stale semantics are removed from code and tests.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-01-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-01-SUMMARY.md`
- updated genesis runtime and tests for thread-count and artifact-path behavior

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P0-01` | `PLAN-063-G01` | `063-TODO.md#p0-fix-the-genesis-execution-contract-drift`; genesis runtime and config anchors | `063-TODO.md`; `063-CONTEXT.md`; `genesis_run.rs`; `genesis_config*.rs` | runtime/config/output files plus release tests | `cargo test --release -p z00z_core --test test_genesis_manifest_refs -- --nocapture`; `cargo test --release -p z00z_core --test genesis_tests test_validation -- --nocapture` | parsed execution controls match live behavior | real runtime only | changed runtime plus path-sensitive tests | `Complete` |
