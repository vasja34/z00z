---
phase: 063-Core-Update
plan: PLAN-063-G13
type: execute
wave: 4
depends_on:
  - PLAN-063-G10
  - PLAN-063-G11
  - PLAN-063-G12
files_modified:
  - crates/z00z_core/Cargo.toml
  - crates/z00z_core/benches/**
  - crates/z00z_core/bin/**
  - crates/z00z_core/examples/**
  - crates/z00z_core/docs/**
requirements:
  - REC-063-P2-03
autonomous: true
must_haves:
  truths:
    - "Bench, bin, and example files flatten to direct owner paths."
    - "Example-local YAML disappears in favor of `z00z_config`."
    - "Feature and crate responsibility wording match the real support surface."
  artifacts:
    - crates/z00z_core/Cargo.toml
    - crates/z00z_core/benches/**
    - crates/z00z_core/bin/**
    - crates/z00z_core/examples/**
  key_links:
    - "Scope: support-surface flattening and crate-boundary hygiene."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-063-G13 Plan: Benches, Binaries, Examples, And Crate Responsibility

<objective>
Close `REC-063-P2-03` by flattening `benches`, `bin`, and `examples`,
removing example-local YAML, and tightening `z00z_core` support-surface
responsibility.
</objective>

<context>
@.planning/phases/063-Core-Update/063-TODO.md
@.planning/phases/063-Core-Update/063-CONTEXT.md
@crates/z00z_core/Cargo.toml
@crates/z00z_core/benches
@crates/z00z_core/bin
@crates/z00z_core/examples
@crates/z00z_core/docs
</context>

<coverage_contract>
This plan covers `REC-063-P2-03` only. It closes only when support-surface file
paths are flat, example-local YAML is removed, and `Cargo.toml` reflects the
new direct ownership layout.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-063-G13`
task_ids:
- `REC-063-P2-03`
copied_task_rows:
- "| REC-063-P2-03 | P2. Flatten benches, binaries, examples, and crate responsibility | Flatten `benches`, `bin`, and `examples`, remove example-local YAML in favor of `z00z_config`, update `Cargo.toml` paths, and tighten feature and crate responsibility wording. |"
source_refs:
- `./063-TODO.md#p2-flatten-benches-binaries-examples-and-crate-responsibility`
- `crates/z00z_core/Cargo.toml`
- `crates/z00z_core/benches/**`
- `crates/z00z_core/bin/**`
- `crates/z00z_core/examples/**`
- `crates/z00z_core/docs/**`
inputs:
- `063-TODO.md`
- `063-CONTEXT.md`
- live support-surface tree and manifest entries
outputs:
- `.planning/phases/063-Core-Update/063-13-SUMMARY.md`
- flat bench/bin/example paths
- updated support-surface docs and manifest entries
- explicit decision on whether operator-facing CLIs and export-heavy features remain in `z00z_core` or move behind stricter feature gates
dependencies:
- `PLAN-063-G10`
- `PLAN-063-G11`
- `PLAN-063-G12`
acceptance_tests:
- `cargo bench -p z00z_core --no-run` (bench profile is release-equivalent)
- `cargo build --release -p z00z_core --bins`
- `cargo test --release -p z00z_core --examples`
- `find crates/z00z_core/benches -mindepth 2 -type f`
- `find crates/z00z_core/bin -mindepth 2 -type f`
- `find crates/z00z_core/examples -mindepth 2 -type f`
- `find crates/z00z_core/benches crates/z00z_core/bin crates/z00z_core/examples -mindepth 2 -name 'README.md'`
- `rg -n "path = \"(benches|bin|examples)/.*/" crates/z00z_core/Cargo.toml`
simulation_gate:
- No simulation. The support-surface tree and manifest entries must be rewritten directly.
negative_tests:
- no nested support paths survive
- no example-local YAML survives as live support input
- default library features must not keep unnecessary operator-facing CLI or export burden without an explicit boundary rationale
</plan_matrix>

<plan_artifacts>
- `manifest-support`: `crates/z00z_core/Cargo.toml`
- `benches-flat`: `crates/z00z_core/benches/**`
- `bin-flat`: `crates/z00z_core/bin/**`
- `examples-flat`: `crates/z00z_core/examples/**`
- `support-docs`: `crates/z00z_core/docs/**`
</plan_artifacts>

<plan_tests>
- `cargo bench -p z00z_core --no-run` (bench profile is release-equivalent)
- `cargo build --release -p z00z_core --bins`
- `cargo test --release -p z00z_core --examples`
- nested-path scans across `benches`, `bin`, `examples`, and `Cargo.toml`
- feature-gating scan across `crates/z00z_core/Cargo.toml`
- nested README scans across flattened support surfaces
</plan_tests>

<plan_results>
- `PLAN-063-G13` produces `063-13-SUMMARY.md` with flat-support-tree and manifest evidence.
</plan_results>

<task_artifacts>
- task_id: `REC-063-P2-03`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_core/Cargo.toml`
    - `crates/z00z_core/benches/**`
    - `crates/z00z_core/bin/**`
    - `crates/z00z_core/examples/**`
    - `crates/z00z_core/docs/**`
</task_artifacts>

<task_tests>
- task_id: `REC-063-P2-03`
  commands:
    - `cargo bench -p z00z_core --no-run` (bench profile is release-equivalent)
    - `cargo build --release -p z00z_core --bins`
    - `cargo test --release -p z00z_core --examples`
    - `find crates/z00z_core/benches -mindepth 2 -type f`
    - `find crates/z00z_core/bin -mindepth 2 -type f`
    - `find crates/z00z_core/examples -mindepth 2 -type f`
    - `find crates/z00z_core/benches crates/z00z_core/bin crates/z00z_core/examples -mindepth 2 -name 'README.md'`
    - `rg -n "path = \"(benches|bin|examples)/.*/" crates/z00z_core/Cargo.toml`
    - `rg -n "default =|clap|zip|export" crates/z00z_core/Cargo.toml`
  scenarios:
    - `support-surface entrypoints are flat and manifest-backed`
    - `examples load config from z00z_config, not local YAML`
    - bench names stay subject-first and `[[bench]]`, `[[bin]]`, and `[[example]]` paths are updated explicitly in `Cargo.toml`
    - one README per support surface may remain when useful, but nested READMEs do not preserve old owner folders
    - operator-facing CLI and export-heavy features have an explicit `z00z_core` boundary or stricter feature gate
</task_tests>

<task_results>
- task_id: `REC-063-P2-03`
  expected_results:
    - `support-surface paths are flat and truthful`
    - `anti-placeholder evidence includes green bench/bin/example build gates and zero-nested-path scans`
</task_results>

<anti_placeholder_gate>
- Updating docs without moving the support tree and manifest entries does not close this plan.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_core/Cargo.toml`
- `crates/z00z_core/benches/**`
- `crates/z00z_core/bin/**`
- `crates/z00z_core/examples/**`
</current_code_refs>

<blockers>
- Any surviving nested support path in `Cargo.toml` or the file tree is phase-failing for this slice.
- If `clap` or export-heavy tooling remains in default library features, the slice must justify that boundary explicitly or tighten the feature gating in the same wave.
</blockers>

<evidence_gate>
- Summary evidence must include the final support-surface file lists, the zero-nested-path and zero-nested-README scans, subject-first bench/bin/example naming proof, and the final feature-boundary decision for CLI and export-heavy support.
</evidence_gate>

<not_recommendation_gate>
- This slice must land the real support-tree flattening and manifest rewrites now.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Flatten benches, bins, and examples to direct owner paths</name>
<read_first>
- `.planning/phases/063-Core-Update/063-TODO.md`
- `.planning/phases/063-Core-Update/063-CONTEXT.md`
- `crates/z00z_core/Cargo.toml`
- `crates/z00z_core/benches/**`
- `crates/z00z_core/bin/**`
- `crates/z00z_core/examples/**`
</read_first>
<files>
- `crates/z00z_core/Cargo.toml`
- `crates/z00z_core/benches/**`
- `crates/z00z_core/bin/**`
- `crates/z00z_core/examples/**`
- `crates/z00z_core/docs/**`
</files>
<action>
Flatten bench, bin, and example entrypoints to direct owner paths, remove
example-local YAML in favor of `z00z_config`, update every `Cargo.toml`
support-surface path, and tighten crate-boundary wording so the support surface
matches the actual code layout.
</action>
<acceptance_criteria>
- `benches`, `bin`, and `examples` have no nested file paths.
- Bench names remain subject-first and `Cargo.toml` updates every `[[bench]]`, `[[bin]]`, and `[[example]]` path explicitly.
- `Cargo.toml` points only to flat support paths.
- Example-local YAML is gone from the live support surface.
- One README per support surface may remain when useful, but nested README files do not preserve historical owner folders.
- Bench, bin, and example release gates pass after the move.
- Operator-facing CLIs and heavyweight export packaging have an explicit `z00z_core` boundary decision, including whether they should move toward a dedicated tooling crate.
- Default library features, including `clap` if still present, are revisited and either justified or moved behind stricter feature gating.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo bench -p z00z_core --no-run` (bench profile is release-equivalent)
   - `cargo build --release -p z00z_core --bins`
   - `cargo test --release -p z00z_core --examples`
   - `find crates/z00z_core/benches -mindepth 2 -type f`
   - `find crates/z00z_core/bin -mindepth 2 -type f`
   - `find crates/z00z_core/examples -mindepth 2 -type f`
   - `find crates/z00z_core/benches crates/z00z_core/bin crates/z00z_core/examples -mindepth 2 -name 'README.md'`
   - `rg -n "path = \"(benches|bin|examples)/.*/" crates/z00z_core/Cargo.toml`
   - `rg -n "default =|clap|zip|export" crates/z00z_core/Cargo.toml`
3. When Rust or test-affecting changes are relevant, run `cargo test --release`.
4. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
5. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
6. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
The `z00z_core` support surface is flat, truthful, and release-validated.
</done>
</task>
</tasks>

<threat_model>
- Nested support trees hide stale ownership assumptions and reintroduce path drift.
</threat_model>

<verification>
This slice is valid only if support-surface files and manifest entries flatten together.
</verification>

<success_criteria>
- Support-surface paths are flat.
- Bench, bin, and example release gates are green on the flat tree.
</success_criteria>

<output>
Create `.planning/phases/063-Core-Update/063-13-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/063-Core-Update/063-13-SUMMARY.md`
- flat support-surface tree and manifest entries

## 🔎 Coverage Appendix

| TASK-NNN / Requirement | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REC-063-P2-03` | `PLAN-063-G13` | `063-TODO.md#p2-flatten-benches-binaries-examples-and-crate-responsibility`; support tree and manifest entries | `063-TODO.md`; `063-CONTEXT.md`; `Cargo.toml`; support tree | manifest, benches, bins, examples, docs | `cargo bench -p z00z_core --no-run` (bench profile is release-equivalent); `cargo build --release -p z00z_core --bins`; `cargo test --release -p z00z_core --examples` | flat support surface with no local YAML drift | real support tree only | zero-nested-path scans plus green release support gates | `Complete` |
