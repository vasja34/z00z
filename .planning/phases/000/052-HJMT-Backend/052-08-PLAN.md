---
phase: 052-HJMT-Backend
plan: 052-08
type: execute
wave: 8
depends_on:
  - 052-07
files_modified:
  - .planning/phases/052-HJMT-Backend/052-TODO.md
  - .planning/phases/052-HJMT-Backend/052-CONTEXT.md
  - .planning/phases/052-HJMT-Backend/052-TEST-SPEC.md
  - .planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md
requirements:
  - PH52-ADAPTIVE-BUCKETS-FOLLOWUP
autonomous: true
must_haves:
  - Adaptive bucket work stays future-only until fixed-bucket evidence is green.
  - Split, merge, migration, historical-proof, and recovery semantics are planned together.
  - No adaptive runtime lane or migration-proof placeholder lands in Phase 052.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 052-08 Plan: Adaptive Buckets And Migration Proofs Candidate

<objective>
Prepare the future `HJMT-Adaptive-Buckets-And-Migration-Proofs` phase
candidate after fixed-bucket forest evidence exists, without implementing
adaptive buckets in Phase 052.
</objective>

<execution_context>
Read `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`,
`.planning/phases/052-HJMT-Backend/052-TODO.md`,
`.planning/phases/052-HJMT-Backend/052-07-PLAN.md`, and
`docs/Z00Z-JMT-Design.md` sections 4.4, 6.3, 7.3, 8.1, 9.2, 13.4, and 14
before editing.
</execution_context>

<context>
Adaptive buckets are attractive only after fixed-bucket workload numbers are
known. They add split proofs, merge proofs, bucket epochs, old or new policy
compatibility, migration replay, historical proof rules, and privacy review.
Those concerns are bigger than a backend swap and need a dedicated phase.
</context>

<tasks>
<task type="auto">
<name>Define adaptive bucket phase entry conditions and protocol surface</name>
<read_first>
- `docs/Z00Z-JMT-Design.md` sections 4.4, 6.3, 8.1, 9.2, and 14
- `.planning/phases/052-HJMT-Backend/052-07-PLAN.md`
- `.planning/phases/052-HJMT-Backend/052-SUMMARY.md` when it exists
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TODO.md`
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
</files>
<action>
Write the future phase candidate requirements for adaptive bucket split,
merge, and migration proof machinery. Include entry conditions from fixed
bucket benchmarks, proof-size evidence, hot-bucket thresholds, recovery
evidence, and privacy review. Define required protocol objects such as bucket
epochs, migration records, old-policy and new-policy bindings, historical
proof compatibility, and deterministic replay across bucket policy changes.
</action>
<acceptance_criteria>
- The candidate cannot start until fixed-bucket forest implementation,
  benchmarks, recovery, and proof evidence are green.
- The candidate names split proof, merge proof, migration proof, bucket epoch,
  historical proof compatibility, and replay requirements.
- The candidate forbids runtime adaptive behavior without repository-backed
  design updates and fail-closed verifier semantics.
- The candidate records rollback and policy downgrade hazards.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- For docs-only planning edits, run `git diff --check -- .planning/phases/052-HJMT-Backend`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-08-PLAN.md current_task="Define adaptive bucket phase entry conditions and protocol surface"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the planning packet.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
Adaptive bucket migration has a future phase candidate with hard entry
conditions and no live Phase 052 runtime effect.
</done>
</task>

<task type="auto">
<name>Define adaptive migration test and simulator obligations</name>
<read_first>
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
- `docs/Z00Z-JMT-Design.md` sections 13.2, 13.3, and 13.4
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
</files>
<action>
Define the future test matrix for adaptive buckets: split proof success and
tamper rejection, merge proof success and tamper rejection, migration proof
success and tamper rejection, old-root and new-root proof verification,
replay across bucket epochs, crash recovery during split or merge, benchmark
comparison against fixed buckets, and simulator `scenario_1` root continuity
through storage-owned APIs.
</action>
<acceptance_criteria>
- Future tests cover success, rejection, recovery, replay, benchmark, and
  simulator paths.
- Tests explicitly prove old proofs do not validate under new roots unless
  migration rules authorize them.
- Tests keep adaptive policy metadata storage-owned and proof-verifier-owned,
  not downstream-owned.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- For docs-only planning edits, run `git diff --check -- .planning/phases/052-HJMT-Backend`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-08-PLAN.md current_task="Define adaptive migration test and simulator obligations"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the planning packet.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
Adaptive bucket migration has future test obligations before promotion.
</done>
</task>
</tasks>

<threat_model>
- Adaptive split timing can leak activity; require privacy review before
  proof-visible policy changes.
- Migration proofs can validate stale or wrong roots; require old/new root and
  epoch binding.
- Crash during split or merge can fork roots; require journal and replay tests
  before any live rollout.
</threat_model>

<verification>
This plan is planning-only unless a later phase promotion happens. Use docs
diff checks now, and bootstrap-first validation if any Rust or test seam is
changed while preparing the future phase.
</verification>

<success_criteria>
- Adaptive bucket work is not forgotten.
- Adaptive bucket work is not smuggled into fixed-bucket Phase 052 runtime.
- Future phase entry conditions are evidence-based.
</success_criteria>

<output>
Update planning and test-spec artifacts only; do not add adaptive bucket code.
</output>
