---
phase: 052-HJMT-Backend
plan: 052-06
type: execute
wave: 6
depends_on:
  - 052-05
files_modified:
  - crates/z00z_storage/src/assets/store_internal/forest_config.rs
  - crates/z00z_storage/benches/assets/assets_benches.md
  - crates/z00z_storage/benches/assets/nested.rs
  - crates/z00z_storage/benches/assets/shard.rs
  - crates/z00z_storage/Cargo.toml
  - .planning/phases/052-HJMT-Backend/052-TODO.md
  - .planning/phases/052-HJMT-Backend/052-06-SUMMARY.md
  - .planning/phases/052-HJMT-Backend/052-SUMMARY.md
  - .planning/STATE.md
requirements:
  - PH52-ROLLOUT-BENCHMARKS
  - PH52-CLOSEOUT
autonomous: true
must_haves:
  - Compatibility remains the default backend until all forest gates are green.
  - Benchmark evidence covers async multi-insert or multi-delete plus proof-size duties.
  - Scenario 1 passes through compatibility, forest, and dual-verify modes once those modes exist.
  - Backend closeout records exact commands, review loops, and honest deferred follow-ups.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 052-06 Plan: Rollout Gating, Benchmarks, And Closeout

<objective>
Keep compatibility as the safe default while extending benchmark evidence,
running the backend verification order, and recording Phase 052 backend
closeout evidence honestly before the `052-07` green-state audit.
</objective>

<execution_context>
Read `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`,
`.planning/phases/052-HJMT-Backend/052-TODO.md`,
`.planning/phases/052-HJMT-Backend/052-05-PLAN.md`,
`crates/z00z_storage/benches/assets/shard.rs`,
`crates/z00z_storage/benches/assets/nested.rs`, and
`docs/Z00Z-JMT-Design.md` sections 6.3, 6.4, 9.1, 9.2, 9.3, 13.4, and 15
before editing.
</execution_context>

<context>
This backend closeout plan does not invent new semantics. It controls rollout,
proves the performance claims the design is allowed to make, and records
executed validation evidence before `052-07` performs the independent
green-state audit and deferred follow-up ledger. The benchmark harness already
exists in source, so this plan extends it instead of building a disconnected
measurement surface.

Any `forest_*`, `test_phase052_*`, or `052-*SUMMARY.md` file named below is a
proposed file home when it does not yet exist in the current codebase. If
earlier execution slices or the active GSD workflow land a better-fitting
extension point first, extend that landed module or artifact path instead of
creating a duplicate logic lane.

`crates/z00z_storage/benches/assets/assets_benches.md` is the preferred
benchmark-evidence file when present; create it only if the active checkout
does not already have an equivalent evidence home.
</context>

<tasks>
<task type="auto">
<name>Gate rollout and extend benchmark evidence</name>
<read_first>
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `crates/z00z_storage/src/assets/store_internal/forest_config.rs`
- `crates/z00z_storage/benches/assets/shard.rs`
- `crates/z00z_storage/benches/assets/nested.rs`
- `crates/z00z_storage/benches/assets/assets_benches.md` when present
</read_first>
<files>
- `crates/z00z_storage/src/assets/store_internal/forest_config.rs`
- `crates/z00z_storage/benches/assets/shard.rs`
- `crates/z00z_storage/benches/assets/nested.rs`
- `crates/z00z_storage/benches/assets/assets_benches.md` as the benchmark
  evidence file, or the active workflow's equivalent evidence home if absent
- `crates/z00z_storage/Cargo.toml`
</files>
<action>
Keep compatibility as the default backend until equivalence, guardrails,
checkpoint, reload, and proof gates are green, while enabling explicit forest
and dual-verify modes for local validation and CI. Extend the existing bench
harnesses to measure random, broad, hot-definition, hot-serial, delete-heavy,
proof-heavy, and recovery workloads, including multithread async
`multi-insert` and `multi-delete` execution. Compare compatibility baseline,
bucket-width variants, and forest execution under those workloads. Record
separate planning time, child commit time, parent commit time, journal time,
proof time, proof size, reload time, inclusion prove and verify time,
shared-parent transaction proof-size evidence, non-existence prove and verify
time, non-existence serialized proof size, and absence aggregation throughput.
If a definition-sharded-only comparison lane is helpful, keep it benchmark-only
rather than a production backend.
</action>
<acceptance_criteria>
- Compatibility remains the default backend mode until the Phase 052 gates are
  green.
- Forest and dual-verify modes are explicit and configuration-gated.
- Benchmark evidence exists for random, broad, hot-definition, hot-serial,
  delete-heavy, proof-heavy, recovery, async multi-insert, async multi-delete,
  inclusion proof-size, and non-existence proof-size workloads.
- Benchmark reporting compares compatibility baseline, bucket-width variants,
  and forest execution, while keeping any definition-sharded-only lane
  benchmark-only rather than a production backend.
- Measured evidence is recorded from the landed bench harness and does not rely
  on undocumented manual spreadsheets or one-off ad hoc scripts.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation. Treat it as the early regression detector, not a replacement for broader cargo validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-06-PLAN.md current_task="Gate rollout and extend benchmark evidence"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
Phase 052 has a safe rollout contract and source-backed benchmark evidence for
the design's performance and proof-size claims.
</done>
</task>

<task type="auto">
<name>Run the backend verification gate and record closeout evidence</name>
<read_first>
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `.planning/phases/052-HJMT-Backend/052-TODO.md`
- `.planning/phases/052-HJMT-Backend/052-05-PLAN.md`
- `crates/z00z_simulator/src/scenario_1/runner_verify.rs`
- `crates/z00z_simulator/src/scenario_1/scenario_config.yaml`
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TODO.md`
- `.planning/phases/052-HJMT-Backend/052-06-SUMMARY.md`
- `.planning/phases/052-HJMT-Backend/052-SUMMARY.md`
- `.planning/STATE.md`
- Additional phase closeout artifacts chosen by the active GSD workflow
</files>
<action>
Run the backend verification gate for Phase 052 and record closeout evidence
honestly. This includes the mandatory bootstrap-first workflow, relevant
focused storage tests, broad cargo validation when forest mode, proof format,
checkpoint behavior, or downstream guardrails changed, and the required
repeated `/GSD-Review-Tasks-Execution` loop. Run simulator `scenario_1` in
compatibility, forest, and dual-verify modes once those modes exist, record
the exact commands and backend modes used, and capture any deferred proof
family, benchmark, or protocol migration as an explicit follow-up rather than
silently implying it shipped.
</action>
<acceptance_criteria>
- Closeout notes record exact validation commands, backend modes, benchmark
  evidence, and review-loop outcomes.
- `scenario_1` passes through compatibility, forest, and dual-verify modes
  once those modes exist.
- Any deferred item is recorded explicitly as a follow-up with repository-
  backed context rather than hidden inside a green summary.
- Phase state only moves to complete when the forest backend, equivalence
  corpus, proof gates, reload or checkpoint gates, benchmark duties, and
  scenario gates all have executed evidence.
</acceptance_criteria>
<verify>
- Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before any broader validation. Treat `bootstrap_tests.sh` as the early regression detector, not as a replacement for the broader cargo test command.
- Then, when relevant, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-06-PLAN.md current_task="Run the backend verification gate and record closeout evidence"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the code.
- Run focused release tests for the landed storage homes, then run `cargo test -p z00z_storage --release --features test-fast` and `cargo test -p z00z_simulator --release --features wallet_debug_dump scenario_1` when the touched code makes them relevant.
- When broad validation is relevant, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- When backend modes exist, run `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump` in compatibility, forest, and dual-verify configurations and record the exact mode-selection inputs used.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
Phase 052 backend closeout is supported by executed validation evidence, not by
planning intent or benchmark promises, and is ready for the `052-07`
green-state audit.
</done>
</task>
</tasks>

<threat_model>
- A non-default forest rollout could ship without full evidence; keep
  compatibility default until all gates are green.
- Benchmark claims could overstate the real system if they come from ad hoc
  scripts or partial workloads; extend the landed harness and record exact
  modes.
- Closeout drift could hide deferred work behind a green summary; require
  explicit follow-up recording.
</threat_model>

<verification>
This plan changes Rust implementation, tests, benchmark harnesses, and phase
closeout artifacts. Run the task-level verification order exactly, including
bootstrap first, broad cargo validation when relevant, scenario validation
after backend modes land, and the required repeated
`/GSD-Review-Tasks-Execution` YOLO loop.
</verification>

<success_criteria>
- Compatibility stays the safe default while forest and dual-verify rollout is
  proven.
- Benchmark evidence exists for async batch workloads and proof-size duties.
- Backend closeout records executed storage, simulator, and review evidence
  honestly.
</success_criteria>

<output>
Update rollout configuration, benchmark harnesses, and phase closeout
artifacts only after the preceding implementation plans are green.
</output>
