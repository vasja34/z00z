---
phase: 052-HJMT-Backend
plan: 052-11
type: execute
wave: 11
depends_on:
  - 052-10
files_modified:
  - .planning/phases/052-HJMT-Backend/052-TODO.md
  - .planning/phases/052-HJMT-Backend/052-CONTEXT.md
  - .planning/phases/052-HJMT-Backend/052-TEST-SPEC.md
  - .planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md
requirements:
  - PH52-RIGHTLEAF-FEEENVELOPE-FOLLOWUP
autonomous: true
must_haves:
  - RightLeaf remains future-only until generalized-rights runtime semantics exist.
  - FeeEnvelope remains separate from terminal right semantics.
  - Future protocol tests cover schema, proof, checkpoint, wallet, validator, and simulator impact.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 052-11 Plan: RightLeaf And FeeEnvelope Protocol Candidate

<objective>
Prepare the future `RightLeaf-FeeEnvelope-Protocol` phase candidate without
exporting `RightLeaf` or `FeeEnvelope` from Phase 052 live storage APIs.
</objective>

<execution_context>
Read `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`,
`.planning/phases/052-HJMT-Backend/052-TODO.md`,
`.planning/phases/052-HJMT-Backend/052-10-PLAN.md`, and
`docs/Z00Z-JMT-Design.md` sections 1.2, 2.4, 2.4.1, 5.1, 5.2, 11, 12, and
14 before editing.
</execution_context>

<context>
`RightLeaf` is a future generalized terminal object. `FeeEnvelope` is a
separate processing-guarantee primitive. Adding either during Phase 052 would
turn a backend implementation into rights runtime, proof vocabulary, fee
model, and legal-architecture work. This plan keeps them first-class but
future-only.
</context>

<tasks>
<task type="auto">
<name>Define RightLeaf terminal-family candidate without live export</name>
<read_first>
- `docs/Z00Z-JMT-Design.md` sections 1.2, 2.4, 2.4.1, 5.1, 11, and 12
- `.planning/phases/052-HJMT-Backend/052-10-PLAN.md`
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TODO.md`
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
</files>
<action>
Define future `RightLeaf` candidate scope: terminal family marker, bounded
right type, issuer or provider scope, holder or capability binding, expiry or
one-time-use semantics, revocation or transition semantics, selective
disclosure, proof envelope family, checkpoint interaction, and compatibility
with `AssetLeaf` under a generalized root generation. Keep all of this out of
Phase 052 live exports.
</action>
<acceptance_criteria>
- Phase 052 source and public docs still do not export live `RightLeaf`.
- The future candidate names required schema, proof, checkpoint, wallet,
  validator, simulator, and legal-review surfaces.
- The candidate keeps `RightLeaf` narrow and typed, not a dumping ground for
  fee state, workflow prose, or off-chain business semantics.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- For docs-only planning edits, run `git diff --check -- .planning/phases/052-HJMT-Backend`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-11-PLAN.md current_task="Define RightLeaf terminal-family candidate without live export"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the planning packet.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
`RightLeaf` is first-class future protocol work, not live Phase 052 storage
scope.
</done>
</task>

<task type="auto">
<name>Define FeeEnvelope separation and protocol test duties</name>
<read_first>
- `docs/Z00Z-JMT-Design.md` sections 2.4, 2.4.1, 11, 12, and 14
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TODO.md`
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
</files>
<action>
Define future `FeeEnvelope` candidate scope separately from `RightLeaf`.
Specify payer or sponsor binding, processing guarantee, verification or relay
budget, expiry, replay protection, fee-credit or reserve interaction, and
failure modes. Define tests that prove fees do not change terminal right
meaning, invalid fee envelopes reject before right transition, and wallets or
validators cannot treat fee support as proof of right ownership.
</action>
<acceptance_criteria>
- `FeeEnvelope` remains separate from terminal right semantics.
- Future tests cover fee tamper, replay, expired support, insufficient support,
  wrong sponsor, wrong right binding, and state preservation after rejection.
- The candidate states that `RightLeaf` may reference or pair with fee support
  only through explicit bounded protocol rules.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- For docs-only planning edits, run `git diff --check -- .planning/phases/052-HJMT-Backend`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-11-PLAN.md current_task="Define FeeEnvelope separation and protocol test duties"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the planning packet.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
`FeeEnvelope` has a future protocol candidate that stays separate from
terminal right semantics.
</done>
</task>
</tasks>

<threat_model>
- `RightLeaf` can become an overbroad schema if fee, workflow, legal prose, and
  off-chain state are mixed into one leaf.
- `FeeEnvelope` can be mistaken for ownership or right validity; keep it as
  processing support only.
- Future rights runtime can widen proof and checkpoint semantics; require a
  dedicated protocol phase after generalized root rules are defined.
</threat_model>

<verification>
This plan is planning-only unless a future phase is promoted. Use docs diff
checks now, and bootstrap-first validation if tests or Rust code are changed.
</verification>

<success_criteria>
- `RightLeaf` and `FeeEnvelope` remain future-only in Phase 052.
- Future protocol candidate preserves separation between right semantics and
  fee or processing support.
- Test duties exist before any live export can be promoted.
</success_criteria>

<output>
Update planning and test-spec artifacts only; do not export `RightLeaf` or
`FeeEnvelope` as live storage contracts.
</output>
