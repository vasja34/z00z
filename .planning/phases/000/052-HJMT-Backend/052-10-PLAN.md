---
phase: 052-HJMT-Backend
plan: 052-10
type: execute
wave: 10
depends_on:
  - 052-07
files_modified:
  - .planning/phases/052-HJMT-Backend/052-TODO.md
  - .planning/phases/052-HJMT-Backend/052-CONTEXT.md
  - .planning/phases/052-HJMT-Backend/052-TEST-SPEC.md
  - .planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md
requirements:
  - PH52-GENERALIZED-ROOT-FOLLOWUP
autonomous: true
must_haves:
  - AssetStateRoot remains live through Phase 052.
  - SettlementStateRoot requires a later protocol migration with generation rules.
  - The compatibility oracle is preserved until the root vocabulary migration has its own oracle.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 052-10 Plan: Generalized Settlement Root Model Candidate

<objective>
Prepare the future `Generalized-Rights-Root-Model` phase candidate for a
possible `AssetStateRoot` to `SettlementStateRoot` migration without replacing
the Phase 052 oracle.
</objective>

<execution_context>
Read `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`,
`.planning/phases/052-HJMT-Backend/052-TODO.md`,
`.planning/phases/052-HJMT-Backend/052-07-PLAN.md`, and
`docs/Z00Z-JMT-Design.md` sections 1.2, 2.4, 5.2, 8.3, 12, 14, and Appendix A
before editing.
</execution_context>

<context>
Replacing `AssetStateRoot` during the backend swap would destroy the main
equivalence oracle. A generalized root migration needs its own phase, own
generation metadata, own checkpoint compatibility story, and own rollback
rules after fixed-bucket forest behavior is stable.
</context>

<tasks>
<task type="auto">
<name>Define settlement-root migration model and entry conditions</name>
<read_first>
- `docs/Z00Z-JMT-Design.md` sections 1.2, 5.2, 8.3, 12, and 14
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `.planning/phases/052-HJMT-Backend/052-07-PLAN.md`
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TODO.md`
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
</files>
<action>
Define a future migration model for introducing `SettlementStateRoot` only
after a mixed asset or right runtime is canonical. Include root-generation
metadata, compatibility adapters, checkpoint statement updates, proof-envelope
versioning, wallet and validator migration impact, old-root and new-root
coexistence rules, rollback rules, and golden corpus requirements.
</action>
<acceptance_criteria>
- Phase 052 keeps `AssetStateRoot` live and does not export a generalized root.
- The future candidate requires its own oracle rather than reusing Phase 052
  backend equivalence as proof of root-vocabulary correctness.
- Checkpoint, proof, wallet, validator, simulator, and reload impacts are
  listed as explicit migration surfaces.
- Old/new root confusion and downgrade risks have fail-closed test duties.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- For docs-only planning edits, run `git diff --check -- .planning/phases/052-HJMT-Backend`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-10-PLAN.md current_task="Define settlement-root migration model and entry conditions"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the planning packet.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
The generalized settlement root has a future migration model without changing
Phase 052 live root vocabulary.
</done>
</task>

<task type="auto">
<name>Define settlement-root migration tests and scenario coverage</name>
<read_first>
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
- `docs/Z00Z-JMT-Design.md` section 13
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
</files>
<action>
Define future tests for root vocabulary migration: old `AssetStateRoot`
acceptance, new `SettlementStateRoot` acceptance, mixed-generation rejection,
checkpoint reload across generation boundaries, proof envelope versioning,
wallet and validator downgrade rejection, simulator `scenario_1` migration
mode, and state preservation on rejected root-generation inputs.
</action>
<acceptance_criteria>
- Future tests cover success and reject paths for old, new, mixed, and
  downgraded root generations.
- Checkpoint and proof tests remain storage-owned.
- `scenario_1` remains a consumer of semantic roots, not a root-vocabulary
  authority.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- For docs-only planning edits, run `git diff --check -- .planning/phases/052-HJMT-Backend`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-10-PLAN.md current_task="Define settlement-root migration tests and scenario coverage"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the planning packet.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
The future generalized-root migration has explicit test and simulator duties.
</done>
</task>
</tasks>

<threat_model>
- Root vocabulary migration can mask backend bugs; require fixed-bucket forest
  green state first.
- Old/new root confusion can validate wrong checkpoint evidence; require
  generation binding.
- A generalized name can become a universal root by accident; constrain it to
  storage-family settlement state only.
</threat_model>

<verification>
This plan is planning-only unless a future phase is promoted. Use docs diff
checks now, and bootstrap-first validation if tests or Rust code are changed.
</verification>

<success_criteria>
- `AssetStateRoot` remains live in Phase 052.
- `SettlementStateRoot` has a future migration candidate with oracle, proof,
  checkpoint, rollback, and simulator obligations.
- No generalized root ships without a repository-backed protocol migration.
</success_criteria>

<output>
Update planning and test-spec artifacts only; do not export
`SettlementStateRoot` as a live root.
</output>
