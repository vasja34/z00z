---
phase: 052-HJMT-Backend
plan: 052-09
type: execute
wave: 9
depends_on:
  - 052-07
files_modified:
  - .planning/phases/052-HJMT-Backend/052-TODO.md
  - .planning/phases/052-HJMT-Backend/052-CONTEXT.md
  - .planning/phases/052-HJMT-Backend/052-TEST-SPEC.md
  - .planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md
requirements:
  - PH52-OCCUPANCY-METADATA-FOLLOWUP
autonomous: true
must_haves:
  - Proof-visible bucket occupancy counters remain future-only.
  - Local operational metrics stay distinct from committed proof metadata.
  - Any future counter policy requires design update, privacy review, and reject tests.
user_setup: none
---

<!-- markdownlint-disable MD032 MD033 MD060 -->

# 052-09 Plan: Bucket Occupancy Metadata Privacy Candidate

<objective>
Prepare the future privacy and protocol review for proof-visible bucket
occupancy metadata without committing counters in Phase 052.
</objective>

<execution_context>
Read `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`,
`.planning/phases/052-HJMT-Backend/052-TODO.md`,
`.planning/phases/052-HJMT-Backend/052-07-PLAN.md`, and
`docs/Z00Z-JMT-Design.md` sections 4.1, 4.3, 8.1, 12, 14, and Appendix A
before editing.
</execution_context>

<context>
`leaf_count` and similar counters may look useful for monitoring or adaptive
policy, but proof-visible counters become public settlement metadata. Phase
052 can keep operational metrics local, but must not expose bucket occupancy
inside the verifier-visible proof contract unless the design source changes.
</context>

<tasks>
<task type="auto">
<name>Define occupancy metadata privacy review gate</name>
<read_first>
- `docs/Z00Z-JMT-Design.md` sections 4.1, 8.1, 14, and Appendix A
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TODO.md`
- `.planning/phases/052-HJMT-Backend/052-CONTEXT.md`
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
</files>
<action>
Record a future review gate for proof-visible bucket occupancy metadata. The
gate must distinguish private diagnostics from committed proof fields, define
which counter forms are forbidden until design update, and require privacy
analysis for scale leakage, activity leakage, policy-change leakage, and
cross-proof correlation.
</action>
<acceptance_criteria>
- Proof-visible occupancy counters remain excluded from Phase 052.
- Local metrics are allowed only as non-authoritative diagnostics.
- Any future committed counter needs `docs/Z00Z-JMT-Design.md` update first.
- Privacy review covers exact counts, ranges, thresholds, sparse-bucket hints,
  policy generation changes, and proof correlation.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- For docs-only planning edits, run `git diff --check -- .planning/phases/052-HJMT-Backend`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-09-PLAN.md current_task="Define occupancy metadata privacy review gate"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the planning packet.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
Bucket occupancy metadata has a future privacy gate and no live Phase 052
proof-visible counter.
</done>
</task>

<task type="auto">
<name>Define occupancy metadata guardrail and reject tests</name>
<read_first>
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
- `crates/z00z_storage/tests/test_phase051_guardrails.rs`
</read_first>
<files>
- `.planning/phases/052-HJMT-Backend/052-TEST-SPEC.md`
- `.planning/phases/052-HJMT-Backend/052-TESTS-TASKS.md`
</files>
<action>
Define future tests that fail if proof-visible `leaf_count`,
`bucket_occupancy`, or equivalent counters are added before the design update.
If a later phase explicitly commits counters, require tamper rejection, root
binding, policy-generation binding, reload validation, and downstream
non-authority guardrails.
</action>
<acceptance_criteria>
- Current Phase 052 tests know counters are not live proof metadata.
- Future counter tests require explicit design update and proof-version bump.
- Downstream crates remain unable to treat occupancy metadata as business or
  settlement authority.
</acceptance_criteria>
<verify>
- For any Rust or test-affecting change in this task, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, and rerun it before broader validation.
- Then, when relevant to this task, run `cargo test --release --features test-fast --features wallet_debug_dump`.
- For docs-only planning edits, run `git diff --check -- .planning/phases/052-HJMT-Backend`.
- Run `.github/prompts/gsd-review-tasks-execution.prompt.md` via `/GSD-Review-Tasks-Execution current_spec=.planning/phases/052-HJMT-Backend/052-09-PLAN.md current_task="Define occupancy metadata guardrail and reject tests"` in YOLO mode at least 3 times. Fix all issues and warnings. Stop only after at least 2 consecutive runs show no significant issues in the planning packet.
- If committing this task is needed, use `/z00z-git-versioning`; do not use raw git commit or push commands.
</verify>
<done>
Future occupancy metadata has guardrails before it can become proof-visible.
</done>
</task>
</tasks>

<threat_model>
- Proof-visible counters can leak activity, scale, hot buckets, or timing.
- Counter drift can become a new proof-verifier input without versioning.
- Downstream code could treat occupancy as business meaning; guardrails must
  keep it diagnostic unless a later protocol phase says otherwise.
</threat_model>

<verification>
This plan is planning-only unless a future phase promotes counters. Use docs
diff checks now, and bootstrap-first validation if tests or Rust guardrails are
changed.
</verification>

<success_criteria>
- Proof-visible bucket occupancy remains blocked in Phase 052.
- Future promotion needs design update, privacy review, and fail-closed tests.
- Local metrics do not become public authority.
</success_criteria>

<output>
Update planning and test-spec artifacts only; do not add proof-visible bucket
occupancy counters.
</output>
