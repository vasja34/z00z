---
phase: 044-wallet-assets
plan: 04
type: execute
wave: 4
depends_on: ["044-03"]
files_modified:
  - crates/z00z_wallets/src/chain/client/chain_client.rs
  - crates/z00z_wallets/src/chain/client/chain_client_impl.rs
  - crates/z00z_wallets/src/chain/broadcast/broadcast.rs
  - crates/z00z_wallets/src/chain/broadcast/broadcast_impl.rs
  - crates/z00z_wallets/src/tx/state/state_update.rs
  - crates/z00z_wallets/src/persistence/scans/storage.rs
  - crates/z00z_wallets/src/persistence/scans/storage_impl.rs
  - crates/z00z_storage/src/checkpoint/build.rs
  - crates/z00z_storage/src/checkpoint/exec_input.rs
  - crates/z00z_storage/src/checkpoint/artifact_types.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_storage/src/assets/store_internal/store_rows.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs
  - crates/z00z_wallets/src/receiver/scan/types_receive.rs
  - crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_broadcast_body.rs
  - crates/z00z_wallets/src/tx/state/test_state_update_suite.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_tests.rs
autonomous: true
requirements:
  - PH44-ADMIT
  - PH44-RECONCILE
  - PH44-RECEIVE
  - PH44-HISTORY
must_haves:
  truths:
    - Simulated admission is explicit and trait-backed, not RPC-local fake success.
    - Storage-backed reconciliation is the only path from pending to final wallet state.
    - Report-only receive remains non-persistent.
    - Final receiver ownership still routes through `recv_route(..., ReceiveNext::PersistClaim)`.
  artifacts:
    - crates/z00z_wallets/src/chain/broadcast/broadcast_impl.rs
    - crates/z00z_wallets/src/persistence/scans/storage_impl.rs
    - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
    - crates/z00z_storage/src/checkpoint/build.rs
  key_links:
    - admission trait -> typed receipt -> checkpoint evidence
    - storage evidence -> reconciliation -> final status transitions
    - report-only detection -> pending receive -> canonical persist-claim path
---

<objective>
Land explicit simulated admission, storage-backed reconciliation, and receiver
finalization semantics so pending wallet state becomes final only when typed
evidence matches the journal.

Purpose: remove fake broadcast success and make receiver handling truthful
across report-only, pending, and final paths.
Output: explicit admission traits, reconciliation logic, and receiver
finalization semantics tied to storage evidence.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/044-wallet-assets/044-CONTEXT.md
@.planning/phases/044-wallet-assets/044-TODO.md
@.planning/phases/044-wallet-assets/044-wallets-assets-spec.md
@.planning/phases/044-wallet-assets/044-wallets-patch.md
</execution_context>

<context>
@.planning/phases/044-wallet-assets/044-TODO.md
@.planning/phases/044-wallet-assets/044-wallets-assets-spec.md
@.planning/phases/044-wallet-assets/044-wallets-patch.md
@crates/z00z_wallets/src/chain/client/chain_client.rs
@crates/z00z_wallets/src/chain/client/chain_client_impl.rs
@crates/z00z_wallets/src/chain/broadcast/broadcast.rs
@crates/z00z_wallets/src/chain/broadcast/broadcast_impl.rs
@crates/z00z_wallets/src/persistence/scans/storage.rs
@crates/z00z_wallets/src/persistence/scans/storage_impl.rs
@crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
@crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs
@crates/z00z_storage/src/checkpoint/build.rs
@crates/z00z_storage/src/checkpoint/exec_input.rs
@crates/z00z_storage/src/checkpoint/artifact_types.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 044-06 Admission And Confirmation Boundary</name>
  <files>crates/z00z_wallets/src/chain/client/chain_client.rs, crates/z00z_wallets/src/chain/client/chain_client_impl.rs, crates/z00z_wallets/src/chain/broadcast/broadcast.rs, crates/z00z_wallets/src/chain/broadcast/broadcast_impl.rs, crates/z00z_wallets/src/tx/state/state_update.rs, crates/z00z_wallets/src/persistence/scans/storage.rs, crates/z00z_wallets/src/persistence/scans/storage_impl.rs, crates/z00z_storage/src/checkpoint/build.rs, crates/z00z_storage/src/checkpoint/exec_input.rs, crates/z00z_storage/src/checkpoint/artifact_types.rs, crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_storage/src/assets/store_internal/store_rows.rs</files>
  <action>Introduce one explicit trait-backed simulated admission adapter, have
  broadcast call that adapter instead of returning RPC-local fake success,
  store admission receipts in the tx journal, and have confirmation apply
  typed checkpoint evidence before moving wallet state forward; keep the real
  chain client and broadcast traits as later implementations without changing
  wallet lifecycle semantics.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test test_tx_broadcast_body -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Broadcast can no longer report success without admission trait evidence.
    - Admission receipts and checkpoint evidence are recorded for later reconciliation.
    - Simulated confirmation binds selected inputs and created outputs to typed evidence.
  </acceptance_criteria>
  <done>Admission is explicit, confirmation is evidence-backed, and fake broadcast success is gone.</done>
</task>

<task type="auto">
  <name>Task 2: 044-07 Storage-Backed Wallet Reconciliation</name>
  <files>crates/z00z_wallets/src/persistence/tx/tx_storage.rs, crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs, crates/z00z_wallets/src/persistence/assets/asset_storage.rs, crates/z00z_wallets/src/persistence/assets/asset_storage_impl.rs, crates/z00z_wallets/src/persistence/scans/storage.rs, crates/z00z_wallets/src/persistence/scans/storage_impl.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs, crates/z00z_wallets/src/tx/state/lifecycle.rs, crates/z00z_storage/src/checkpoint/build.rs, crates/z00z_storage/src/checkpoint/exec_input.rs, crates/z00z_storage/src/checkpoint/artifact_types.rs, crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_storage/src/assets/store_internal/store_rows.rs</files>
  <action>Add a narrow wallet reconciler that scans pending and admitted journal
  rows, queries storage/checkpoint adapters for typed tx-effect evidence, and
  transitions wallet rows to `Spent`, `Available`, and `Confirmed` only when
  `SpentEnt` and `CreatedEnt` coverage matches journal expectations; leave
  rows pending when evidence is absent, fail closed or quarantine on conflict,
  and keep reconciliation resumable and idempotent.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test test_state_update_suite -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Pending rows remain pending when evidence is missing.
    - Matching sender spend/change evidence finalizes exactly once.
    - Wrong-root or missing-output evidence fails closed without raising available balance.
  </acceptance_criteria>
  <done>Storage-backed reconciliation can finalize wallet state only when typed evidence matches the journal.</done>
</task>

<task type="auto">
  <name>Task 3: 044-08 Receiver Pending, Report-Only, And Finalization Behavior</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_catalog.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs, crates/z00z_wallets/src/receiver/scan/types_receive.rs, crates/z00z_wallets/src/receiver/scan/stealth_scanner.rs</files>
  <action>Keep `receive_asset_impl(...)` report-only and non-persistent; add
  receiver pending rows for verified packages that are not yet spendable;
  finalize receiver rows only through `recv_route(..., ReceiveNext::PersistClaim)`;
  record claim reservation, claim finalization, quarantine, and release
  evidence; prevent duplicate finalization by output id, serial id, claim
  nullifier, and tx output row; and keep import origin and confirmation source
  honest.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test asset_impl_tests -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Report-only detection does not mutate claims or balances.
    - Pending receiver rows exist but are not spendable until confirmation.
    - Duplicate finalization fails closed instead of double-counting balance.
  </acceptance_criteria>
  <done>Receiver behavior stays split between preview, pending, and canonical persist-claim finalization.</done>
</task>

</tasks>

<verification>
Before declaring Plan 044-04 complete:
- [ ] Broadcast requires explicit admission evidence and no longer fakes success.
- [ ] Reconciliation moves state only when storage evidence matches journal expectations.
- [ ] Report-only receive stays non-persistent and finalization remains canonical.
</verification>

<success_criteria>

- Admission and confirmation are explicit and evidence-backed.
- Reconciliation is resumable, idempotent, and storage-authoritative.
- Receiver behavior remains truthful across report-only, pending, and final paths.

</success_criteria>

<output>
After completion, create `.planning/phases/044-wallet-assets/044-04-SUMMARY.md`
</output>
