---
phase: 044-wallet-assets
plan: 03
type: execute
wave: 3
depends_on: ["044-02"]
files_modified:
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
  - crates/z00z_wallets/src/backup/crypto/backup_wire.rs
  - crates/z00z_wallets/src/backup/export/backup_exporter_impl.rs
  - crates/z00z_wallets/src/backup/import/backup_importer_impl.rs
  - crates/z00z_wallets/src/backup/import/backup_importer.rs
  - crates/z00z_wallets/src/persistence/tx/tx_storage.rs
  - crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_send.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_lifecycle.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs
  - crates/z00z_wallets/src/chain/client/chain_client.rs
  - crates/z00z_wallets/src/chain/client/chain_client_impl.rs
  - crates/z00z_wallets/src/chain/broadcast/broadcast.rs
  - crates/z00z_wallets/src/chain/broadcast/broadcast_impl.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
  - crates/z00z_wallets/tests/test_tx_parity.rs
autonomous: true
requirements:
  - PH44-OFFLINE
  - PH44-HISTORY
  - PH44-DRIFT
  - P-044-010
  - P-044-011
  - P-044-012
must_haves:
  truths:
    - Backup and restore preserve the live JSONL tx-history bytes and manifest.
    - Legacy per-tx JSON directories are migration input only.
    - Portable package export/import keeps the canonical tx bytes and tx hash stable.
    - Sender and receiver submission stay role-neutral by tx hash.
  artifacts:
    - crates/z00z_wallets/src/backup/export/backup_exporter_impl.rs
    - crates/z00z_wallets/src/backup/import/backup_importer_impl.rs
    - crates/z00z_wallets/src/backup/import/backup_importer.rs
    - crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
  key_links:
    - live JSONL file -> forensic backup payload -> restore path
    - imported portable package -> same admission path -> same tx hash
    - legacy directory -> migration rows -> JSONL authority
---

<objective>
Add backup, restore, migration, and portable submission semantics that preserve
exact live JSONL bytes and exact tx package bytes across history and transport
flows.

Purpose: make forensic replay, restore, and role-neutral submission rely on the
same canonical bytes that the live tx-history store already records.
Output: exact-byte-preserving backup/restore logic, explicit legacy migration
guards, and portable package import/export that keep tx hashes stable.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/044-wallet-assets/044-CONTEXT.md
@.planning/phases/044-wallet-assets/044-TODO.md
@.planning/phases/044-wallet-assets/044-wallets-assets-spec.md
@.planning/phases/044-wallet-assets/044-wallets-patch.md
</execution_context>

<context>
@.planning/phases/044-wallet-assets/044-TODO.md
@.planning/phases/044-wallet-assets/044-wallets-assets-spec.md
@.planning/phases/044-wallet-assets/044-wallets-patch.md
@crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
@crates/z00z_wallets/src/backup/crypto/backup_wire.rs
@crates/z00z_wallets/src/backup/export/backup_exporter_impl.rs
@crates/z00z_wallets/src/backup/import/backup_importer_impl.rs
@crates/z00z_wallets/src/backup/import/backup_importer.rs
@crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
@crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_send.rs
@crates/z00z_wallets/src/chain/client/chain_client.rs
@crates/z00z_wallets/src/chain/broadcast/broadcast.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 044-04C Backup, Restore, And Forensic JSONL Preservation</name>
  <files>crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs, crates/z00z_wallets/src/backup/crypto/backup_wire.rs, crates/z00z_wallets/src/backup/export/backup_exporter_impl.rs, crates/z00z_wallets/src/backup/import/backup_importer_impl.rs, crates/z00z_wallets/src/backup/import/backup_importer.rs, crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs, crates/z00z_wallets/src/backup/import/test_backup_importer_suite.rs</files>
  <action>Change backup creation to read the live `wallet_<stem>_tx_history.jsonl`
  bytes instead of collecting history from per-tx JSON files; validate rows,
  hashes, wallet identity, and tx package hashes; store exact JSONL bytes plus
  manifest in the encrypted forensic payload; make restore write JSONL bytes
  back to the live wallet-stem path; and keep `Vec<TxRecord>` as a derived
  compatibility view only.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test test_backup_importer_suite -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Backup stores exact live JSONL bytes plus manifest and does not rebuild from per-tx JSON.
    - Restore writes JSONL bytes back to the wallet stem or forensic-only output without inventing rows.
    - Tampering with JSONL bytes, manifest, hashes, or identity data fails closed.
  </acceptance_criteria>
  <done>Backup and restore preserve the forensic JSONL history exactly and do not synthesize it from private implementation files.</done>
</task>

<task type="auto">
  <name>Task 2: 044-04D Legacy Per-Tx JSON Migration And Guards</name>
  <files>crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs, crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs, crates/z00z_wallets/tests/test_tx_store_integration.rs</files>
  <action>Add explicit source-shape guards for existing `wallet_<stem>_tx_history/`
  directories; treat `wallet_<stem>_tx_history.jsonl` as authoritative when
  both exist; preserve exact tx bytes in any fixture or compatibility reader
  used by tests; never delete the legacy directory automatically; and add
  source-shape guards that reject reintroduction of the old live-store model.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>rg -n "wallet_tx_history_dir|collect_tx_history_records|format!\\(\\\"\\{tx_hash\\}\\.json\\\"\\)|wallet_\\{wallet_stem\\}_tx_history" crates/z00z_wallets/src</automated>
  </verify>
  <acceptance_criteria>
    - Legacy directories remain compatibility-shape evidence only.
    - New writes go only to JSONL.
    - Source-shape guards fail if the old live-store model returns.
  </acceptance_criteria>
  <done>Legacy per-tx JSON is only a rejected compatibility shape, never a live authority.</done>
</task>

<task type="auto">
  <name>Task 3: 044-05 Portable Package Export, Import, And Role-Neutral Submission</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_send.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_lifecycle.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_server_transfer.rs, crates/z00z_wallets/src/chain/client/chain_client.rs, crates/z00z_wallets/src/chain/client/chain_client_impl.rs, crates/z00z_wallets/src/chain/broadcast/broadcast.rs, crates/z00z_wallets/src/chain/broadcast/broadcast_impl.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs, crates/z00z_wallets/src/persistence/tx/tx_storage.rs, crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs</files>
  <action>Make verified tx packages exportable as portable canonical tx bytes;
  keep sender and receiver submission role-neutral by tx hash; have receiver
  import decode, verify, validate chain/version, and write only pending
  receiver state before confirmation; preserve canonical package bytes and
  hashes in the live JSONL store; and keep broadcast/admission honest rather
  than faking chain success inside RPC handlers.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test test_tx_parity -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Sender and receiver submit the same tx bytes through one canonical admission path.
    - Exported package bytes stay exact and role-neutral.
    - Receiver import never skips validation or writes finalized state before confirmation.
  </acceptance_criteria>
  <done>Portable tx submission reuses the same canonical tx bytes and admission path for both wallet roles.</done>
</task>

</tasks>

<verification>
Before declaring Plan 044-03 complete:
- [ ] Backup and restore preserve exact live JSONL bytes plus manifest.
- [ ] Legacy per-tx JSON directories are migration input only and never the live store.
- [ ] Portable export/import preserves canonical tx bytes and a single admission path.
</verification>

<success_criteria>

- Forensic backup and restore preserve the live tx-history JSONL exactly.
- Legacy migration is explicit, deterministic, and non-destructive.
- Portable package export/import remains role-neutral and byte-stable.

</success_criteria>

<output>
After completion, create `.planning/phases/044-wallet-assets/044-03-SUMMARY.md`
</output>
