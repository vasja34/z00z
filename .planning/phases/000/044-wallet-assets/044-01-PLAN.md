---
phase: 044-wallet-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/044-wallet-assets/044-coverage.md
  - crates/z00z_wallets/src/persistence/assets/asset_storage.rs
  - crates/z00z_wallets/src/persistence/assets/asset_storage_impl.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs
  - crates/z00z_wallets/src/db/redb/store/redb_wallet_store_objects.rs
  - crates/z00z_wallets/src/wallet/snapshot/snapshot_types.rs
  - crates/z00z_wallets/src/tx/selection/asset_selector.rs
  - crates/z00z_wallets/src/tx/verify/tx_wire_types.rs
  - crates/z00z_wallets/src/tx/verify/tx_verifier.rs
  - crates/z00z_wallets/src/tx/verify/tx_verifier_helpers.rs
  - crates/z00z_wallets/src/tx/state/state_resolved_input.rs
  - crates/z00z_wallets/src/tx/spend/spend_verification.rs
  - crates/z00z_wallets/src/tx/tx_assembler.rs
  - crates/z00z_wallets/src/tx/output/output_flow.rs
  - crates/z00z_wallets/src/stealth/output/output_build.rs
  - crates/z00z_wallets/src/stealth/output/output_validator.rs
  - crates/z00z_wallets/src/tx/proof/spend_proof_backend.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_send.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_lifecycle.rs
  - crates/z00z_wallets/src/persistence/assets/test_asset_storage_impl_suite.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_send_body.rs
autonomous: true
requirements:
  - PH44-LEDGER
  - PH44-SEND
  - PH44-HISTORY
  - PH44-CANCEL
must_haves:
  truths:
    - Wallet assets are spendable only in `Available` state.
    - Sender build reserves inputs before tx bytes are exposed and releases reservations on failure.
    - Sender build and send reuse the canonical selector, assembler, proof backend, and verifier seams.
    - Tx details, pending lists, and history remain journal-backed and do not fabricate stub rows.
  artifacts:
    - .planning/phases/044-wallet-assets/044-coverage.md
    - crates/z00z_wallets/src/persistence/assets/asset_storage.rs
    - crates/z00z_wallets/src/tx/tx_assembler.rs
    - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_send.rs
  key_links:
    - `AssetSelectorImpl` -> reservation -> `TxAssemblerImpl` -> `verify_full_tx_package(...)`
    - `TxRecord` -> journal rows -> history and details responses
    - reservation failure -> release path -> typed send failure
---

<objective>
Create the Phase 044 coverage ledger and the wallet-ledger/send lifecycle
foundation so every later task can rely on explicit ownership, reservation,
and verification seams.

Purpose: establish the execution map, wallet lifecycle state, and canonical
send path before any JSONL storage or backup work starts.
Output: `044-coverage.md` plus the wallet lifecycle and sender build/send
implementation seams required by later plans.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/044-wallet-assets/044-CONTEXT.md
@.planning/phases/044-wallet-assets/044-TODO.md
@.planning/phases/044-wallet-assets/044-wallets-assets-spec.md
@.planning/phases/044-wallet-assets/044-wallets-patch.md
</execution_context>

<context>
@.planning/phases/044-wallet-assets/044-TODO.md
@.planning/phases/044-wallet-assets/044-wallets-assets-spec.md
@.planning/phases/044-wallet-assets/044-wallets-patch.md
@crates/z00z_wallets/src/persistence/assets/asset_storage.rs
@crates/z00z_wallets/src/persistence/assets/asset_storage_impl.rs
@crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs
@crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs
@crates/z00z_wallets/src/tx/selection/asset_selector.rs
@crates/z00z_wallets/src/tx/verify/tx_verifier.rs
@crates/z00z_wallets/src/tx/tx_assembler.rs
@crates/z00z_wallets/src/tx/proof/spend_proof_backend.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 044-01 Embedded Coverage And No-Duplicate Lock</name>
  <files>.planning/phases/044-wallet-assets/044-coverage.md</files>
  <action>Create the Phase 044 coverage ledger before any code edits, map every
  `EV-044-*`, `D-044-*`, `PH44-*`, `AC-044-*`, `T-044-*`, and `PT-044-*`
  identifier to an owner file, a test home, and an evidence slot, record the
  Phase 043 prerequisite status for each dependency seam, and prove that the
  execution plan does not touch `crates/z00z_crypto/tari/**`.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task touches Rust/test-affecting files, run `cargo test --release --features test-fast --features wallet_debug_dump` after bootstrap passes.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>rg -n "EV-044-|D-044-|PH44-|AC-044-|T-044-|PT-044-" .planning/phases/044-wallet-assets/044-coverage.md</automated>
  </verify>
  <acceptance_criteria>
    - `044-coverage.md` exists and covers every Phase 044 identifier with owner, test, and evidence slots.
    - Phase 043 prerequisite status is recorded without creating a duplicate authority path.
    - The coverage ledger and phase plan remain free of `crates/z00z_crypto/tari/**` edits.
  </acceptance_criteria>
  <done>Phase 044 has a coverage ledger that makes the later plans auditable and no duplicate authority path is introduced.</done>
</task>

<task type="auto">
  <name>Task 2: 044-02 Wallet Asset Ledger And Reservation Layer</name>
  <files>crates/z00z_wallets/src/persistence/assets/asset_storage.rs, crates/z00z_wallets/src/persistence/assets/asset_storage_impl.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs, crates/z00z_wallets/src/db/redb/store/redb_wallet_store_objects.rs, crates/z00z_wallets/src/wallet/snapshot/snapshot_types.rs, crates/z00z_wallets/src/tx/selection/asset_selector.rs</files>
  <action>Introduce wallet-owned lifecycle rows for `Available`, `Reserved`,
  `Exported`, `ClaimPending`, `PendingSpend`, `Spent`, `PendingChange`,
  `PendingReceive`, `ReorgPending`, `Quarantined`, and `Dropped`; keep
  `wallet_claimed_assets` and `put_claimed_asset(...)` as the ownership
  authority; add atomic reserve and release operations; fail closed when any
  selected input is not `Available`; keep final spendability on `Available`
  rather than inventing `Validated`; and add redaction rules for sensitive
  lifecycle data in debug exports.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test test_asset_storage_impl_suite -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Two simultaneous build attempts against the same wallet-owned input cannot both reserve it.
    - Reservation and release are atomic and do not masquerade as final spend.
    - Existing claimed assets are represented truthfully without changing the live ownership authority.
  </acceptance_criteria>
  <done>Wallet asset lifecycle state is explicit, reservation is safe, and final spendability remains `Available` only after confirmation.</done>
</task>

<task type="auto">
  <name>Task 3: 044-03 Sender Build And Send Lifecycle</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_send.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_lifecycle.rs, crates/z00z_wallets/src/tx/selection/asset_selector.rs, crates/z00z_wallets/src/tx/verify/tx_wire_types.rs, crates/z00z_wallets/src/tx/verify/tx_verifier.rs, crates/z00z_wallets/src/tx/verify/tx_verifier_helpers.rs, crates/z00z_wallets/src/tx/state/state_resolved_input.rs, crates/z00z_wallets/src/tx/spend/spend_verification.rs, crates/z00z_wallets/src/tx/tx_assembler.rs, crates/z00z_wallets/src/tx/output/output_flow.rs, crates/z00z_wallets/src/stealth/output/output_build.rs, crates/z00z_wallets/src/stealth/output/output_validator.rs, crates/z00z_wallets/src/tx/proof/spend_proof_backend.rs</files>
  <action>Replace `BuiltTxStub` with canonical tx package bytes and journal
  metadata; query only available wallet assets; reserve selected inputs before
  output construction; build real recipient, change, and fee outputs through
  the approved builder paths; assemble and prove through the canonical Phase
  043 seams; call `verify_full_tx_package(...)` before persistence advances;
  and release reservations plus record a typed failure on every build,
  verification, output, proof, or reservation error.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test test_tx_send_body -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Sender build emits real selected inputs, role-tagged outputs, and verifier-approved tx bytes.
    - Failure paths release reservations and do not leave `BuiltTxStub` behavior behind.
    - Sender and receiver submission still converge on the same tx hash and admission path.
  </acceptance_criteria>
  <done>Sender build/send is canonical, reserved inputs never leak early, and output construction reuses the approved wallet seams.</done>
</task>

</tasks>

<verification>
Before declaring Plan 044-01 complete:
- [ ] `044-coverage.md` maps the full Phase 044 identifier set and records Phase 043 prerequisite status.
- [ ] Asset reservation and sender build/send no longer rely on stub or fake-success behavior.
- [ ] The bootstrap gate, relevant release gate, and review loop are part of every task verify block.
</verification>

<success_criteria>

- The coverage ledger exists and is comprehensive.
- Wallet asset lifecycle state is explicit and reservation-safe.
- Sender build/send uses canonical selection, proof, assembly, and verification seams.
- The plan keeps the phase boundary truthful and does not create duplicate authorities.

</success_criteria>

<output>
After completion, create `.planning/phases/044-wallet-assets/044-01-SUMMARY.md`
</output>
