---
phase: 044-wallet-assets
plan: 06
type: execute
wave: 6
depends_on: ["044-05"]
files_modified:
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_send.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_send_body.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_pending_body.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_history_receipt_sort.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_history_cursor_filters.rs
  - .planning/phases/044-wallet-assets/044-PH44-TRACE-MATRIX.md
autonomous: true
requirements:
  - PH44-SEND
  - PH44-ADMIT
  - PH44-RECONCILE
  - PH44-OFFLINE
must_haves:
  truths:
    - send_transaction must execute canonical build plus admit flow and persist canonical package bytes.
    - canonical compact recipient inputs are required by send and build fixtures.
    - tx-focused history and pending tests must stay green without legacy recipient aliases.
  artifacts:
    - .planning/phases/044-wallet-assets/044-PH44-TRACE-MATRIX.md
  key_links:
    - PH44-SEND -> send_transaction_impl -> tx build plus admission path
    - PH44-ADMIT -> broadcast/admission tests and receipt-aware views
    - PH44-RECONCILE -> history and pending projections over journal-backed lifecycle state
---

# 044-06 Execution Slice

## Objective

Finalize PH44 send/admit/reconcile convergence by removing remaining test drift and
enforcing canonical recipient and package flow in live send execution.

## Tasks

1. Replace metadata-only `send_transaction_impl` flow with canonical `build_transaction_impl` plus `broadcast_transaction_impl` execution.
2. Persist canonical tx package bytes in tx journal storage for send-path records.
3. Update tx test fixtures that used non-canonical recipient aliases (`alice`, `bob`, `carol`) to compact receiver cards.
4. Seed spendable wallet assets in affected tests so canonical build and admission flow remains realistic.
5. Re-run tx-focused suite under release plus `test-fast` and capture evidence.

## Verification

- Command:
  - `cargo test -p z00z_wallets --release --features test-fast test_tx_ -- --nocapture`
- Expected:
  - `test result: ok. 34 passed; 0 failed` for tx-focused unit set in `src/lib.rs`.

## Acceptance Criteria

- Send path executes canonical build plus admit flow.
- No tx-focused tests fail due to legacy recipient aliases.
- PH44 traceability artifact exists and maps send/admit/reconcile/offline requirements to concrete tests.

## Output

- Updated send implementation and tx fixtures.
- New traceability matrix document for PH44 requirement closure evidence.
