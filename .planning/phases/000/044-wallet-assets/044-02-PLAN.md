---
phase: 044-wallet-assets
plan: 02
type: execute
wave: 2
depends_on: ["044-01"]
files_modified:
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
  - crates/z00z_wallets/src/persistence/tx/tx_storage.rs
  - crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/types/tx.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_history.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_lifecycle.rs
  - crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
  - crates/z00z_wallets/src/backup/crypto/backup_wire.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_history_body.rs
autonomous: true
requirements:
  - PH44-HISTORY
  - PH44-DRIFT
  - P-044-001
  - P-044-002
  - P-044-003
  - P-044-004
  - P-044-005
  - P-044-006
  - P-044-007
  - P-044-008
  - P-044-009
must_haves:
  truths:
    - Tx journal rows carry real selected-input, output-role, reservation, receipt, and status data.
    - `wallet_<stem>_tx_history.jsonl` is the canonical live tx-history file.
    - `TxStorageImpl` folds JSONL rows by `tx_hash` and preserves exact canonical package bytes.
    - No new per-tx JSON live store or new broad tx-package database appears.
  artifacts:
    - crates/z00z_wallets/src/persistence/tx/tx_storage.rs
    - crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
    - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_history.rs
    - crates/z00z_wallets/src/adapters/rpc/types/tx.rs
  key_links:
    - `TxRecord` -> journal rows -> history/detail responses
    - `wallet_<stem>.wlt` -> sibling `wallet_<stem>_tx_history.jsonl`
    - JSONL canonical file -> by-hash reads -> latest status view
---

<objective>
Define the tx journal and lock the live JSONL authority layer so later backup,
restore, migration, and portable submission tasks can use one canonical history
source.

Purpose: make tx details, pending lists, and history truthful before backup or
restore starts depending on them.
Output: a versioned tx journal shape, canonical JSONL path contract, and a
single-file JSONL `TxStorageImpl` authority.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/044-wallet-assets/044-CONTEXT.md
@.planning/phases/044-wallet-assets/044-TODO.md
@.planning/phases/044-wallet-assets/044-wallets-assets-spec.md
@.planning/phases/044-wallet-assets/044-wallets-patch.md
</execution_context>

<context>
@.planning/phases/044-wallet-assets/044-TODO.md
@.planning/phases/044-wallet-assets/044-wallets-assets-spec.md
@.planning/phases/044-wallet-assets/044-wallets-patch.md
@crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
@crates/z00z_wallets/src/persistence/tx/tx_storage.rs
@crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs
@crates/z00z_wallets/src/adapters/rpc/types/tx.rs
@crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_history.rs
@crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_lifecycle.rs
@crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs
@crates/z00z_wallets/src/backup/crypto/backup_wire.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 044-04 Tx Journal, Details, Pending Lists, And History</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs, crates/z00z_wallets/src/persistence/tx/tx_storage.rs, crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs, crates/z00z_wallets/src/adapters/rpc/types/tx.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_history.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl_server_lifecycle.rs</files>
  <action>Extend the tx journal so it stores selected input refs, resolved-input
  metadata hashes, output refs, output roles, amount, fee, origin, submitter
  role, reservation ids, lifecycle status, and canonical tx package bytes when
  they exist; then make tx details, pending lists, and history responses read
  from that journal instead of stub metadata or empty arrays.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test test_tx_history_body -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Built, submitted, failed, cancelled, and confirmed txs all expose truthful journal-backed details.
    - Empty input/output detail arrays are no longer fabricated for live txs.
    - Pending/history views preserve pagination and filter behavior while using journal data.
  </acceptance_criteria>
  <done>Tx details and history are no longer stubbed; they are journal-backed and package-aware.</done>
</task>

<task type="auto">
  <name>Task 2: 044-04A Canonical JSONL Live Tx-History Authority And Path Contract</name>
  <files>crates/z00z_wallets/src/services/wallet/store/wallet_service_store_persistence_pack.rs, crates/z00z_wallets/src/persistence/tx/tx_storage.rs, crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs, crates/z00z_wallets/src/services/wallet/tests/test_wallet_service_suite.rs</files>
  <action>Replace any live-store assumption that `wallet_<stem>_tx_history/` is
  authoritative with `wallet_<stem>_tx_history.jsonl`; keep `.wlt` as wallet
  snapshot and restore state only; restrict any directory helper to legacy
  migration paths; ensure new writes cannot create a per-tx JSON live store;
  and reject attempts to widen Phase 044 into a broad tx-package database.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>rg -n "wallet_\\{wallet_stem\\}_tx_history|wallet_tx_history_dir|format!\\(\\\"\\{tx_hash\\}\\.json\\\"\\)" crates/z00z_wallets/src</automated>
  </verify>
  <acceptance_criteria>
    - `wallet_<stem>_tx_history.jsonl` is the only live tx-history path.
    - `.wlt` remains wallet-state-only.
    - New writes cannot accidentally revive the per-tx JSON directory model.
  </acceptance_criteria>
  <done>Wallet tx-history authority is locked to the sibling JSONL file and not to a directory or backup sidecar.</done>
</task>

<task type="auto">
  <name>Task 3: 044-04B JSONL-Backed TxStorageImpl And Folded Reads</name>
  <files>crates/z00z_wallets/src/persistence/tx/tx_storage.rs, crates/z00z_wallets/src/persistence/tx/tx_storage_impl.rs, crates/z00z_wallets/src/backup/crypto/backup_wire.rs, crates/z00z_wallets/tests/test_tx_store_integration.rs</files>
  <action>Refactor `TxStorageImpl` into a single JSONL-file store with
  deterministic read/modify/write updates, folded reads keyed by `tx_hash`,
  exact package-byte retention, and fail-closed handling for malformed input;
  keep the canonical live path at `wallet_<stem>_tx_history.jsonl` and reject
  any attempt to revive a per-tx JSON live store.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>When this task changes Rust code, run `cargo test --release --features test-fast --features wallet_debug_dump` after the bootstrap gate.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` at least 3 times in YOLO mode, fix all issues and warnings, and stop only after 2 consecutive runs show no significant issues.</automated>
    <automated>cargo test -p z00z_wallets --features test-fast --test test_tx_store_integration -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Put/update/delete-style operations preserve exact tx package bytes.
    - Folded reads return the latest visible record without mutating prior rows.
    - Malformed rows or hash mismatches fail closed.
  </acceptance_criteria>
  <done>Tx storage is tamper-evident JSONL with deterministic rewrite semantics and no live per-tx JSON writes.</done>
</task>

</tasks>

<verification>
Before declaring Plan 044-02 complete:
- [ ] Journal-backed details and history no longer rely on stub metadata.
- [ ] `wallet_<stem>_tx_history.jsonl` is the canonical live path.
- [ ] `TxStorageImpl` rewrites the canonical JSONL file without losing package bytes.
</verification>

<success_criteria>

- The tx journal is rich enough for history, pending, and details views.
- The live tx-history authority is a single JSONL file per wallet stem.
- Storage callers remain compatible while the backing authority becomes foldable JSONL.

</success_criteria>

<output>
After completion, create `.planning/phases/044-wallet-assets/044-02-SUMMARY.md`
</output>
