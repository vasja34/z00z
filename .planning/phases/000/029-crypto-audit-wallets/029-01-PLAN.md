<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 029-crypto-audit-wallets
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
autonomous: true
requirements:
  - PH29-RECON
must_haves:
  truths:
    - Every fused wallet finding is classified against the current tree as `still_open`, `partially_closed`, `validation_only`, or `stale_in_current_tree` before code changes start.
    - Every relevant provision from `029-FUSION.md` and `029-CONTEXT.md`, including lower-severity naming, feature-gate, mnemonic-boundary, and open-question items, is mapped either to one execution wave, one explicit open decision, or one explicit out-of-phase downgrade.
    - Because `029-RESEARCH.md` does not exist in the phase directory, no claim may be treated as research-backed unless `029-RECONCILIATION.md` restates it as current-tree evidence or labels it as source ambiguity.
    - Runtime panic claims, key-manager TTL status, deterministic seed-salt usage, backup KDF drift, and digest-builder ownership are anchored to exact current files instead of copied from stale prose.
    - Downstream plans consume one frozen execution target inventory instead of re-litigating scope during implementation.
  artifacts:
    - path: .planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
      provides: Current-tree finding matrix, runtime-versus-test panic inventory, and exact file-to-requirement execution map.
  key_links:
    - from: .planning/phases/029-crypto-audit-wallets/029-FUSION.md
      to: .planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
      via: finding-by-finding status mapping against live wallet code
      pattern: FUS-05|FUS-06|FUS-07|FUS-08|FUS-09|FUS-10|FUS-11
    - from: .planning/phases/029-crypto-audit-wallets/029-CONTEXT.md
      to: .planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
      via: Gate 0 and phase-wave execution order
      pattern: Gate 0|Wave 0|still_open|validation_only|open_questions|feature-gate
---

<objective>
Freeze the exact live scope of the wallet audit before any implementation wave starts.

Purpose: prevent Phase 029 from executing stale or partially-landed findings as if they were still open, and give every later plan one canonical status matrix to follow.
Output: one reconciliation artifact that maps the fused audit to the current tree, classifies runtime versus test-only panic sites, records how missing `029-RESEARCH.md` affects evidence quality, and locks the exact execution target inventory.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/029-crypto-audit-wallets/029-FUSION.md
@.planning/phases/029-crypto-audit-wallets/029-CONTEXT.md
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_wallets/src/services/chain_service.rs
@crates/z00z_wallets/src/core/key/key_manager.rs
@crates/z00z_wallets/src/core/key/stealth_keys.rs
@crates/z00z_wallets/src/core/security/encryption.rs
@crates/z00z_wallets/src/core/backup/wallet_backup.rs
@crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
@crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
@crates/z00z_wallets/src/db/redb_wallet_store.rs
@crates/z00z_wallets/src/core/hashing.rs
@crates/z00z_wallets/src/core/tx/tx_verifier.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the current-tree reconciliation matrix for every fused finding cluster</name>
  <files>.planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md</files>
  <action>Create `029-RECONCILIATION.md` as the canonical Gate 0 artifact. Reconcile every in-scope finding cluster from `029-FUSION.md`, with explicit coverage for blocker, secondary, naming, feature-gate, mnemonic-boundary, and open-question items, not only the headline blockers. For each cluster, classify the current-tree status as `still_open`, `partially_closed`, `validation_only`, `stale_in_current_tree`, `explicit_open_decision`, or `out_of_phase_followup`. The matrix must at minimum reconcile the `chain_service` crash claim, wallet-service `expect()` or `unwrap()` sites, key-manager TTL enforcement, gap-limit math, deterministic `compute_seed_salt(...)` usage, backup KDF self-description, `FileKeyStore` password ownership, `WalletExportPack` plaintext seed-phrase boundary, entropy-warning surfacing, `build_tx_package_digest()` framing ownership, `derive_s_out` or `generate_identity_keypair()` facade hardening notes, and feature-gated debug or verbose exposure. If a claim would otherwise rely on a nonexistent `029-RESEARCH.md`, label it explicitly as `current_tree_evidence_only` or `source_ambiguity` instead of treating it as research-backed. Anchor each row to exact Rust files and note whether the finding stays blocker-grade, downgrades to validation-only, becomes an explicit open decision, or moves to a later wave.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test --release --features test-fast --features wallet_debug_dump
rg -n "expect\(|unwrap\(|derive_view_secret_key|derive_view_key_versioned|DERIVED_KEY_TTL_SECONDS|saturating_sub\(|compute_seed_salt|WalletBackupCrypto|build_tx_package_digest|WalletExportPack|seed_phrase|RuntimeValidationResult|generate_identity_keypair|derive_s_out|SafePassword|feature" crates/z00z_wallets/src</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`029-RECONCILIATION.md` exists, every fused finding cluster is mapped to one live status with file-accurate evidence, and any claim lacking a phase-local research source is labeled explicitly instead of being silently upgraded to fact.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the execution target inventory and requirement-to-file map for later waves</name>
  <files>.planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md</files>
  <action>Extend `029-RECONCILIATION.md` with one requirement-to-file execution map keyed by `PH29-*` IDs, one runtime-versus-test panic inventory, and one explicit `covered in later plan` versus `open decision` versus `out_of_phase` table for any lower-severity fusion/context provisions that do not become direct code waves. For each requirement, list the minimum Rust modules and tests that downstream plans must touch, plus any optional files that may expand only if execution uncovers a required integration seam. Where a downstream test target does not exist yet, mark it as a proposed new file rather than as an existing file. For panic findings, distinguish operator-reachable service flows from test helpers, fixtures, reachability stubs, and bench-only code. The result must be specific enough that later plans can inherit file ownership without reopening scoping debates.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test --release --features test-fast --features wallet_debug_dump
rg -n "wallet_service|chain_service|stealth_keys|redb_wallet_store|wallet_backup|tx_verifier|file_key_store|seed_phrase|snapshot|RuntimeValidationResult|generate_identity_keypair|derive_s_out|open decision|source_ambiguity|proposed new file" .planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The reconciliation artifact contains one frozen file inventory, one runtime-versus-test panic classification table, and one explicit mapping for every in-scope fusion/context provision that later waves can consume directly without guessing.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate 0 is satisfied: Phase 029 has one canonical finding-status matrix, one exact target inventory grounded in the live wallet tree, and one explicit record of how the missing `029-RESEARCH.md` affects evidence confidence.
</verification>

<success_criteria>
The wallet audit scope is frozen against the current tree, stale findings are downgraded explicitly, and later waves have one authoritative execution target map.
</success_criteria>

<output>
After completion, create `.planning/phases/029-crypto-audit-wallets/029-01-SUMMARY.md`
</output>