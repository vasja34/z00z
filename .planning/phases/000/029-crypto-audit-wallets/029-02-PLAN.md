<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 029-crypto-audit-wallets
plan: "02"
type: execute
wave: 2
depends_on:
  - 029-01
files_modified:
  - crates/z00z_wallets/src/core/key/stealth_keys.rs
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/core/tx/spending.rs
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/tests/test_e2e_send_scan.rs
  - crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs
  - crates/z00z_wallets/tests/test_view_key_contract.rs
autonomous: true
requirements:
  - PH29-VIEWKEY
must_haves:
  truths:
    - Sender, scanner, and spend verification all use one canonical live view-key derivation path for the same `ReceiverSecret`.
    - Historical or rotated view-key derivation remains available only through an explicit non-default surface that cannot drift back into hot paths silently.
    - Regression coverage fails if `derive_rotated_view_secret_key(...)` re-enters live scan or spend flows.
  artifacts:
    - path: crates/z00z_wallets/src/core/key/stealth_keys.rs
      provides: Canonical live-versus-historical view-key contract.
    - path: crates/z00z_wallets/src/services/wallet_service.rs
      provides: Explicit receiver-key rotation or retrieval behavior that honors the same contract.
    - path: crates/z00z_wallets/tests/test_e2e_send_scan.rs
      provides: Existing send-plus-scan end-to-end coverage kept aligned with the canonical live derivation path.
    - path: crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs
      provides: Existing public derivation roundtrip coverage kept aligned with the same live-versus-historical contract.
    - path: crates/z00z_wallets/tests/test_view_key_contract.rs
      provides: Lock-step sender, scanner, spend, and rotation regression coverage.
  key_links:
    - from: crates/z00z_wallets/src/core/key/stealth_keys.rs
      to: crates/z00z_wallets/src/core/stealth/output.rs
      via: scan and ownership checks consume the canonical live derivation path
      pattern: derive_view_secret_key|scan|verify
    - from: crates/z00z_wallets/src/core/key/stealth_keys.rs
      to: crates/z00z_wallets/src/core/tx/spending.rs
      via: spend-rule verification shares the same live view-key contract
      pattern: derive_view_secret_key|derive_rotated_view_secret_key|view_key
---

<objective>
Canonicalize the live wallet view-key path and stop versioned derivation drift from breaking recoverability.

Purpose: close the highest-priority protocol finding below the runtime panic blockers by ensuring sender, scanner, and spend flows stay in lock-step for the same receiver secret.
Output: one explicit live derivation path, one explicit historical or rotation-only path, and regression coverage that blocks future cross-use.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/029-crypto-audit-wallets/029-FUSION.md
@.planning/phases/029-crypto-audit-wallets/029-CONTEXT.md
@.planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
@crates/z00z_wallets/src/core/key/stealth_keys.rs
@crates/z00z_wallets/src/core/stealth/output.rs
@crates/z00z_wallets/src/core/tx/spending.rs
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_wallets/tests/test_e2e_send_scan.rs
@crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confine the live protocol path to `derive_view_secret_key(...)` and quarantine versioned derivation</name>
  <files>crates/z00z_wallets/src/core/key/stealth_keys.rs
crates/z00z_wallets/src/core/stealth/output.rs
crates/z00z_wallets/src/core/tx/spending.rs
crates/z00z_wallets/src/services/wallet_service.rs</files>
  <behavior>
    - Test 1: the default sender, scanner, and spend paths derive the same live view key from the same `ReceiverSecret`.
    - Test 2: `derive_rotated_view_secret_key(...)` cannot be called from live scan or spend hot paths after this change.
    - Test 3: rotation or historical-recovery code paths remain explicit and do not change the default live derivation result.
  </behavior>
  <action>Implement the canonical view-key decisions from `029-CONTEXT.md` without inventing a third derivation family. Treat `derive_view_secret_key(...)` as the live protocol path and move `derive_rotated_view_secret_key(...)` behind an explicit historical or rotation-only contract through naming, placement, visibility, or wrapper APIs. Update scan, spend, and receiver-key retrieval call sites so they all consume the live path directly. Keep rotation behavior available only where the reconciliation matrix still marks it as legitimate historical state. Avoid broad API churn outside wallet-owned code.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_view_key_contract -- --nocapture
! rg -n "derive_rotated_view_secret_key" crates/z00z_wallets/src/core/stealth/output.rs crates/z00z_wallets/src/core/tx/spending.rs
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The live wallet protocol uses one explicit view-key derivation path and the versioned helper is confined to explicit historical or rotation-only behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lock-step regression coverage for sender, scanner, spend, and rotation flows</name>
  <files>crates/z00z_wallets/tests/test_e2e_send_scan.rs
crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs
crates/z00z_wallets/tests/test_view_key_contract.rs</files>
  <behavior>
    - Test 1: sender output creation and receiver scanning agree on the same live view key for one receiver secret.
    - Test 2: spend verification uses the same live view-key result and fails if a versioned helper is substituted.
    - Test 3: rotation or historical access remains explicit and testable without mutating the live default path.
    - Test 4: the pre-existing end-to-end send-scan and RPC derivation anchors remain aligned with the same live-path contract instead of silently drifting onto separate helper behavior.
  </behavior>
  <action>Create focused regression coverage in `test_view_key_contract.rs` that proves the live-path contract from end to end, and extend `test_e2e_send_scan.rs` plus `test_rpc_key_derive_e2e.rs` only where those existing anchors need one explicit assertion to remain truthful under the new contract. Prefer direct calls into existing wallet-owned APIs over fixture-heavy reachability stubs. Include at least one negative test that would have passed only if a versioned helper leaked back into a live path. If useful, add a source guard assertion that checks the hot-path files no longer reference the versioned helper directly.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_view_key_contract -- --nocapture
cargo test -p z00z_wallets --release --test test_e2e_send_scan -- --nocapture
cargo test -p z00z_wallets --release --test test_rpc_key_derive_e2e -- --nocapture --test-threads=1
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Regression tests prove that live sender, scanner, spend, and explicit rotation behavior remain in lock-step and cannot silently drift, and the pre-existing end-to-end anchors remain aligned with the same contract.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate A is satisfied: live view-key derivation is singular and tests prove that historical helpers cannot re-enter hot paths silently.
</verification>

<success_criteria>
The wallet no longer risks silent `NotMine` drift from mixed view-key derivation families, and the allowed historical path is explicit in both code and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/029-crypto-audit-wallets/029-02-SUMMARY.md`
</output>