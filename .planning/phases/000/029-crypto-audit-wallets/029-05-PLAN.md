<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 029-crypto-audit-wallets
plan: "05"
type: execute
wave: 4
depends_on:
  - 029-03
files_modified:
  - crates/z00z_wallets/src/core/key/key_manager.rs
  - crates/z00z_wallets/src/core/key/stealth_keys.rs
  - crates/z00z_wallets/src/core/storage/file_key_store.rs
  - crates/z00z_wallets/tests/test_key_manager.rs
  - crates/z00z_wallets/tests/test_receiver_secret_validation.rs
  - crates/z00z_wallets/tests/test_file_key_store.rs
autonomous: true
requirements:
  - PH29-KEYMGR
  - PH29-SECRET
must_haves:
  truths:
    - Impossible BIP-44 counter order fails loudly instead of being hidden by `saturating_sub(...)`.
    - `ReceiverSecret` instances cannot survive create, load, or decrypt boundaries unless they can derive the canonical live view key successfully.
    - `FileKeyStore` no longer stores cloneable plaintext password bytes or convenience traits on secret-bearing state.
  artifacts:
    - path: crates/z00z_wallets/src/core/key/key_manager.rs
      provides: Loud gap-limit invariant handling and explicit cache-validation behavior.
    - path: crates/z00z_wallets/src/core/key/stealth_keys.rs
      provides: `ReceiverSecret::validate_usable()` and constructor/load enforcement.
    - path: crates/z00z_wallets/src/core/storage/file_key_store.rs
      provides: Zeroizing password wrapper ownership for receiver-secret persistence.
    - path: crates/z00z_wallets/tests/test_file_key_store.rs
      provides: Regression coverage for the new secret-wrapper persistence boundary.
  key_links:
    - from: crates/z00z_wallets/src/core/key/key_manager.rs
      to: crates/z00z_wallets/tests/test_key_manager.rs
      via: explicit gap-limit failure semantics and cache behavior coverage
      pattern: checked_sub|StateCorrupted|gap_limit|ttl
    - from: crates/z00z_wallets/src/core/storage/file_key_store.rs
      to: crates/z00z_wallets/src/core/key/stealth_keys.rs
      via: `SafePassword` or equivalent zeroizing wrapper is the only password-bearing persistence boundary
      pattern: EncryptionScheme|SafePassword|to_encrypted|from_encrypted
---

<objective>
Close the remaining key-manager and receiver-secret boundary gaps with loud invariants and zeroizing wrappers.

Purpose: prevent silent key-allocation corruption and plaintext password copying from surviving inside wallet-owned secret persistence paths.
Output: loud gap-limit math, enforced receiver-secret usability checks, and a `FileKeyStore` password boundary aligned with the rest of the wallet's zeroizing secret handling.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/029-crypto-audit-wallets/029-FUSION.md
@.planning/phases/029-crypto-audit-wallets/029-CONTEXT.md
@.planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
@crates/z00z_wallets/src/core/key/key_manager.rs
@crates/z00z_wallets/src/core/key/stealth_keys.rs
@crates/z00z_wallets/src/core/storage/file_key_store.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace silent key-manager invariant masking and enforce usable receiver-secret construction</name>
  <files>crates/z00z_wallets/src/core/key/key_manager.rs
crates/z00z_wallets/src/core/key/stealth_keys.rs
crates/z00z_wallets/tests/test_key_manager.rs
crates/z00z_wallets/tests/test_receiver_secret_validation.rs</files>
  <behavior>
    - Test 1: impossible `last_used_plus1 > next_index` state returns a typed corruption or gap-limit error instead of silently producing zero gap.
    - Test 2: `ReceiverSecret::from_bytes(...)`, load, and decrypt paths reject unusable receiver secrets before an object escapes the constructor boundary.
    - Test 3: existing TTL behavior remains validated and documented rather than accidentally regressed while gap-limit logic changes.
  </behavior>
  <action>Implement the still-open key-manager and receiver-secret actions from `029-CONTEXT.md`. Replace `saturating_sub(...)` with loud checked invariant handling in the BIP-44 allocation path, keeping the error surface typed and wallet-owned. Add `ReceiverSecret::validate_usable()` using the canonical live view-key derivation path and call it from creation, load, and decrypt constructors so invalid receiver secrets cannot survive object creation. Keep TTL work limited to validation or test proof if the reconciliation matrix marked it as already landed.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_key_manager -- --nocapture
cargo test -p z00z_wallets --release --test test_receiver_secret_validation -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Key-manager counter invariants fail loudly and receiver secrets are validated at their creation and load boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace cloneable password bytes in `FileKeyStore` with zeroizing wrappers</name>
  <files>crates/z00z_wallets/src/core/storage/file_key_store.rs
crates/z00z_wallets/src/core/key/stealth_keys.rs
crates/z00z_wallets/tests/test_file_key_store.rs</files>
  <behavior>
    - Test 1: `FileKeyStore` persists receiver secrets using `SafePassword` or an equivalent zeroizing wrapper instead of `Vec<u8>` password copies.
    - Test 2: secret-bearing enums or structs no longer derive convenience traits that clone or expose password material implicitly.
    - Test 3: receiver-secret encrypt or decrypt helpers accept the new password wrapper cleanly at the persistence boundary.
  </behavior>
  <action>Align `FileKeyStore` with the rest of the wallet's password-handling policy. Replace `EncryptionScheme::Password(Vec<u8>)` with `SafePassword` or an equivalent zeroizing wrapper, update the receiver-secret persistence helpers to accept that wrapper directly, and remove convenience derives from secret-bearing state where they would imply copying or logging password material. Keep the conversion boundary at the outer API edge instead of sprinkling raw byte slices deeper into persistence code.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_file_key_store -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`FileKeyStore` no longer keeps cloneable plaintext password bytes and uses the same zeroizing secret-wrapper policy as the stronger wallet paths.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate D is satisfied for the still-open key-manager and secret-wrapper findings: invariant failures are loud and password-bearing persistence boundaries are zeroizing.
</verification>

<success_criteria>
Wallet key allocation can no longer hide impossible state transitions, and receiver-secret persistence no longer relies on cloneable plaintext password buffers.
</success_criteria>

<output>
After completion, create `.planning/phases/029-crypto-audit-wallets/029-05-SUMMARY.md`
</output>