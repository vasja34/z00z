<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 029-crypto-audit-wallets
plan: "06"
type: execute
wave: 5
depends_on:
  - 029-04
  - 029-05
files_modified:
  - crates/z00z_wallets/src/core/hashing.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/src/core/key/seed.rs
  - crates/z00z_wallets/src/core/security/password.rs
  - crates/z00z_wallets/src/core/wallet/snapshot.rs
  - crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/types/common.rs
  - crates/z00z_wallets/Cargo.toml
  - crates/z00z_wallets/README.md
  - crates/z00z_wallets/tests/test_tx_tamper.rs
  - crates/z00z_wallets/tests/test_wlt_validator.rs
  - crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
  - crates/z00z_wallets/tests/test_tx_digest_framing.rs
  - crates/z00z_wallets/tests/test_runtime_validation_result.rs
  - crates/z00z_wallets/tests/test_backup_metadata_policy.rs
  - crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
autonomous: true
requirements:
  - PH29-DIGEST
  - PH29-VALIDATION
  - PH29-BACKUP
  - PH29-SECRET
must_haves:
  truths:
    - Transaction package digests distinguish ambiguous variable-length string combinations through explicit framing.
    - Wallet validation surfaces can expose warnings explicitly where the reconciled scope still requires them, instead of collapsing everything to binary valid or invalid state.
    - Backup metadata visibility and debug-feature privacy tradeoffs are explicit in code and docs, not silent defaults.
    - The mnemonic-bearing `WalletExportPack` boundary is explicit: plaintext seed text is either confined to an intentional outer export seam or replaced with a stronger wallet-owned boundary, but it is not left as an undocumented implicit default.
  artifacts:
    - path: crates/z00z_wallets/src/core/tx/tx_verifier.rs
      provides: Explicitly framed `build_tx_package_digest()` semantics.
    - path: crates/z00z_wallets/src/adapters/rpc/types/common.rs
      provides: Runtime validation DTO surface aligned with the accepted warning policy.
    - path: crates/z00z_wallets/tests/test_tx_tamper.rs
      provides: Existing typed tamper-rejection anchor kept aligned with the canonical digest builder.
    - path: crates/z00z_wallets/tests/test_wlt_validator.rs
      provides: Existing validator anchor kept aligned with the accepted final validation and metadata policy.
    - path: crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
      provides: Existing encrypted seed-reveal anchor kept aligned with the final mnemonic-boundary policy.
    - path: crates/z00z_wallets/tests/test_tx_digest_framing.rs
      provides: Regression coverage for ambiguous digest inputs.
    - path: crates/z00z_wallets/tests/test_backup_metadata_policy.rs
      provides: Regression coverage for the chosen public-header versus encrypted-metadata policy.
    - path: crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs
      provides: Regression coverage for the chosen mnemonic export-boundary policy.
  key_links:
    - from: crates/z00z_wallets/src/core/hashing.rs
      to: crates/z00z_wallets/src/core/tx/tx_verifier.rs
      via: existing `frame_*` helpers become the only digest-framing rule
      pattern: frame_str|frame_bytes|frame_u32_le|build_tx_package_digest
    - from: crates/z00z_wallets/src/core/security/password.rs
      to: crates/z00z_wallets/src/adapters/rpc/types/common.rs
      via: validation warnings or binary status semantics must match across core and RPC DTO boundaries
      pattern: RuntimeValidationResult|warning|valid|error
---

<objective>
Close the remaining wallet-audit ambiguity around digest framing, validation surfaces, and metadata or debug disclosure policy.

Purpose: finish Phase 029 only after the remaining non-primitive ambiguity surfaces are made explicit, tested, and documented.
Output: framed tx digest semantics, an explicit validation-result contract, and documented backup-metadata or feature-gate policy with regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/029-crypto-audit-wallets/029-FUSION.md
@.planning/phases/029-crypto-audit-wallets/029-CONTEXT.md
@.planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
@crates/z00z_wallets/src/core/hashing.rs
@crates/z00z_wallets/src/core/tx/tx_verifier.rs
@crates/z00z_wallets/src/core/key/seed.rs
@crates/z00z_wallets/src/core/security/password.rs
@crates/z00z_wallets/src/core/wallet/snapshot.rs
@crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
@crates/z00z_wallets/src/adapters/rpc/types/common.rs
@crates/z00z_wallets/src/services/seed_phrase.rs
@crates/z00z_wallets/Cargo.toml
@crates/z00z_wallets/README.md
@crates/z00z_wallets/tests/test_tx_tamper.rs
@crates/z00z_wallets/tests/test_wlt_validator.rs
@crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reframe `build_tx_package_digest()` with the existing wallet hashing helpers</name>
  <files>crates/z00z_wallets/src/core/hashing.rs
crates/z00z_wallets/src/core/tx/tx_verifier.rs
crates/z00z_wallets/tests/test_tx_tamper.rs
crates/z00z_wallets/tests/test_tx_digest_framing.rs</files>
  <behavior>
    - Test 1: ambiguous tuples such as `(\"AB\", \"C\")` and `(\"A\", \"BC\")` produce different digests.
    - Test 2: the digest builder uses the existing `frame_*` helpers instead of local ad hoc concatenation.
    - Test 3: existing digest consumers remain on one canonical builder rather than forking a new helper.
    - Test 4: the pre-existing typed tamper-rejection anchor remains aligned with the canonical digest builder and does not silently validate a divergent hash path.
  </behavior>
  <action>Implement the digest-framing fix directly in `build_tx_package_digest()` and keep ownership of the digest builder in `tx_verifier.rs`. Use the existing `frame_str`, `frame_bytes`, and `frame_u32_le` helpers from `core/hashing.rs` so transaction digest semantics follow the same one-source-of-truth framing policy as the rest of the wallet. Do not create a second digest helper elsewhere just to satisfy the tests. Extend `test_tx_tamper.rs` only where needed so the pre-existing tamper-rejection anchor continues to prove the same canonical digest and typed failure surface.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_tx_digest_framing -- --nocapture
cargo test -p z00z_wallets --release --test test_tx_tamper -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Wallet transaction package digests use explicit framing and reject the previous ambiguous concatenation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize warning-capable validation and backup-metadata disclosure policy</name>
  <files>crates/z00z_wallets/src/core/key/seed.rs
crates/z00z_wallets/src/core/security/password.rs
crates/z00z_wallets/src/core/wallet/snapshot.rs
crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
crates/z00z_wallets/src/adapters/rpc/types/common.rs
crates/z00z_wallets/Cargo.toml
crates/z00z_wallets/README.md
crates/z00z_wallets/tests/test_wlt_validator.rs
crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
crates/z00z_wallets/tests/test_runtime_validation_result.rs
crates/z00z_wallets/tests/test_backup_metadata_policy.rs
crates/z00z_wallets/tests/test_wallet_export_pack_boundary.rs</files>
  <behavior>
    - Test 1: the accepted runtime validation surface can carry warnings explicitly if the reconciled scope still requires entropy or validation warnings beyond binary valid or invalid state.
    - Test 2: backup metadata follows one explicit policy: minimal public header or encrypted private metadata, with tests matching that choice.
    - Test 3: the chosen `WalletExportPack` or mnemonic boundary is explicit, bounded, and regression-tested against the documented outer service boundary.
    - Test 4: privacy-sensitive debug or verbose feature gates are documented consistently in code and README.
    - Test 5: the pre-existing validator and encrypted seed-reveal anchors remain aligned with the accepted validation, metadata, and mnemonic-boundary contract instead of preserving superseded assumptions.
  </behavior>
  <action>Use the reconciliation artifact to decide whether warning-capable validation is still required in Phase 029. If it is, extend the core and RPC `RuntimeValidationResult` surface in one aligned change instead of adding a second ad hoc warning channel; if reconciliation downgrades it, record that downgrade explicitly instead of leaving the DTO question implicit. Finalize the backup metadata policy explicitly: either keep only a minimal public header or move privacy-sensitive metadata into the encrypted payload. In the same wave, close the mnemonic-boundary finding from `029-FUSION.md` and `029-CONTEXT.md`: either keep `WalletExportPack { seed_phrase: String }` only as an explicit outer export seam aligned with `services/seed_phrase.rs`, with tightly bounded plaintext lifetime and tests, or move the export path to a stronger wallet-owned seed boundary that the current codebase already supports. Do not leave `WalletExportPack` as an undocumented implicit plaintext boundary. If `CipherSeedContainer` wrapper governance still cannot be resolved from the current tree, label it explicitly as an open decision in docs or summary instead of silently inventing a new format. Extend `test_wlt_validator.rs` and `test_show_seed_phrase_plaintext.rs` where needed so the existing validator and encrypted seed-reveal anchors stay aligned with the accepted final contract. Make the chosen metadata, mnemonic-boundary, validation, and feature-gate policy observable in code, tests, and crate docs.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_runtime_validation_result -- --nocapture
cargo test -p z00z_wallets --release --test test_backup_metadata_policy -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_export_pack_boundary -- --nocapture
cargo test -p z00z_wallets --release --test test_wlt_validator -- --nocapture
cargo test -p z00z_wallets --release --test test_show_seed_phrase_plaintext -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Digest, validation, metadata, mnemonic-boundary, and debug-surface ambiguity is closed by one explicit code-and-doc policy with regression coverage or an explicitly recorded open decision where the current tree cannot support a safe closure.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate E is satisfied: digest framing is explicit, validation and metadata policy is explicit, the mnemonic export boundary is explicit, and the remaining wallet-audit ambiguity surfaces are covered by tests, docs, or one explicit open decision.
</verification>

<success_criteria>
Phase 029 closes with no remaining silent ambiguity around tx digests, runtime validation results, backup metadata disclosure, mnemonic export boundaries, or privacy-sensitive debug surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/029-crypto-audit-wallets/029-06-SUMMARY.md`
</output>