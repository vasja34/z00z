<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 029-crypto-audit-wallets
plan: "04"
type: execute
wave: 4
depends_on:
  - 029-03
files_modified:
  - .planning/phases/029-crypto-audit-wallets/029-PANIC-INVENTORY.md
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_wallets/src/services/chain_service.rs
  - crates/z00z_wallets/src/core/hashing.rs
  - crates/z00z_wallets/src/core/wallet/snapshot.rs
  - crates/z00z_wallets/src/db/redb_wallet_store.rs
  - crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
  - crates/z00z_wallets/tests/test_wallet_service_errors.rs
  - crates/z00z_wallets/tests/test_seed_salt_policy.rs
autonomous: true
requirements:
  - PH29-PANIC
  - PH29-SEEDSALT
must_haves:
  truths:
    - Operator-reachable wallet flows return typed errors instead of `expect()` or `unwrap()` panics.
    - Test-only or fixture-only panic sites remain explicitly classified and are not silently treated as production blockers.
    - New wallet writes no longer derive seed salt deterministically from `wallet_id`, while legacy compatibility remains bounded.
  artifacts:
    - path: .planning/phases/029-crypto-audit-wallets/029-PANIC-INVENTORY.md
      provides: Runtime-versus-test panic inventory and closure status for service-layer crash findings.
    - path: crates/z00z_wallets/src/services/wallet_service.rs
      provides: Typed runtime error propagation for live wallet flows plus random-seed-salt rollout points.
    - path: crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
      provides: Existing encrypted seed-reveal anchor kept aligned with the new persisted wallet-owned seed-salt contract.
    - path: crates/z00z_wallets/tests/test_seed_salt_policy.rs
      provides: Regression coverage for random persisted seed-salt behavior and legacy fallback bounds.
  key_links:
    - from: .planning/phases/029-crypto-audit-wallets/029-PANIC-INVENTORY.md
      to: crates/z00z_wallets/src/services/wallet_service.rs
      via: classified runtime panic sites are the only ones eligible for blocker-grade rewrites
      pattern: expect|unwrap|runtime|test-only
    - from: crates/z00z_wallets/src/core/hashing.rs
      to: crates/z00z_wallets/src/db/redb_wallet_store.rs
      via: random wallet-owned seed-salt metadata replaces deterministic new-write salt derivation
      pattern: compute_seed_salt|seed_salt|wallet metadata
---

<objective>
Remove runtime wallet crash paths and roll new writes off deterministic seed salt.

Purpose: close the operational blocker cluster that still mixes service-layer panics with legacy salt derivation in operator-reachable flows.
Output: one panic inventory artifact, typed error propagation for runtime wallet flows, and a random wallet-owned seed-salt contract for new writes with bounded legacy fallback.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/029-crypto-audit-wallets/029-FUSION.md
@.planning/phases/029-crypto-audit-wallets/029-CONTEXT.md
@.planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_wallets/src/services/chain_service.rs
@crates/z00z_wallets/src/core/hashing.rs
@crates/z00z_wallets/src/core/wallet/snapshot.rs
@crates/z00z_wallets/src/db/redb_wallet_store.rs
@crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify and replace runtime `expect()` or `unwrap()` sites with typed wallet errors</name>
  <files>.planning/phases/029-crypto-audit-wallets/029-PANIC-INVENTORY.md
crates/z00z_wallets/src/services/wallet_service.rs
crates/z00z_wallets/src/services/chain_service.rs
crates/z00z_wallets/tests/test_wallet_service_errors.rs</files>
  <behavior>
    - Test 1: runtime service failures return `WalletError` or `WalletResult<T>` instead of terminating the process.
    - Test 2: the panic inventory distinguishes runtime, fixture-only, test-only, and reachability-stub sites explicitly.
    - Test 3: any stale `chain_service` crash claim is either downgraded with evidence or fixed in code, never left ambiguous.
  </behavior>
  <action>Create `029-PANIC-INVENTORY.md` and use it to drive code changes. Inventory every `expect()` or `unwrap()` in `wallet_service.rs` and `chain_service.rs`, classify each site as runtime-reachable or non-runtime, and rewrite only the blocker-grade runtime ones to typed `WalletError` propagation. Keep fixture or test invariants explicit when they are genuinely test-only. Do not let reachability stubs or bench-only helpers dominate the runtime risk classification. Any downgraded `chain_service` claim must cite current-tree evidence in the inventory.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_wallet_service_errors -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Runtime panic sites are either removed in code or downgraded explicitly in the inventory with file-accurate evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move new writes to random wallet-owned seed salt and keep deterministic salt as legacy-only fallback</name>
  <files>crates/z00z_wallets/src/core/hashing.rs
crates/z00z_wallets/src/core/wallet/snapshot.rs
crates/z00z_wallets/src/db/redb_wallet_store.rs
crates/z00z_wallets/src/services/wallet_service.rs
crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
crates/z00z_wallets/tests/test_seed_salt_policy.rs</files>
  <behavior>
    - Test 1: newly created or rewritten wallet state persists a random wallet-owned seed salt instead of deriving it from `wallet_id`.
    - Test 2: legacy wallets still read through bounded fallback logic until migration completes.
    - Test 3: seed-reveal and export flows consume the persisted wallet-owned salt rather than reconstructing new-write salt from `compute_seed_salt(...)`.
    - Test 4: the pre-existing encrypted seed-reveal anchor remains truthful under the new seed-salt contract instead of hard-coding the superseded deterministic path.
  </behavior>
  <action>Implement the deterministic-seed-salt policy from `029-CONTEXT.md`. Keep `compute_seed_salt(...)` available only for bounded legacy read or migration flows, then move new wallet creation, rewrite, seed-reveal, and export paths onto a persisted random 16-byte wallet-owned salt carried in wallet metadata or the accepted wallet-owned persistence seam. Do not introduce a second metadata source of truth. If a compatibility rewrite is needed during wallet open or save, keep it in the same wallet-owned persistence boundary used by the RedB migration flow. Update `test_show_seed_phrase_plaintext.rs` where needed so the existing encrypted seed-reveal proof remains aligned with the accepted new-write salt contract instead of hard-coding a legacy deterministic derivation rule.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_seed_salt_policy -- --nocapture
cargo test -p z00z_wallets --release --test test_show_seed_phrase_plaintext -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>New wallet writes use random persisted seed salt, while deterministic salt survives only as an explicit legacy compatibility path.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate C is satisfied for runtime panic classification and the new-write seed-salt contract is random and wallet-owned.
</verification>

<success_criteria>
Operator-reachable wallet flows fail closed instead of crashing, and deterministic seed salt no longer defines the new-write security boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/029-crypto-audit-wallets/029-04-SUMMARY.md`
</output>