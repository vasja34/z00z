<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 029-crypto-audit-wallets
plan: "03"
type: execute
wave: 3
depends_on:
  - 029-02
files_modified:
  - crates/z00z_wallets/src/core/security/encryption.rs
  - crates/z00z_wallets/src/core/backup/wallet_backup.rs
  - crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
  - crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto.rs
  - crates/z00z_wallets/src/db/redb_wallet_store.rs
  - crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  - crates/z00z_wallets/tests/test_wlt_validator.rs
  - crates/z00z_wallets/tests/test_backup_kdf_contract.rs
  - crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
autonomous: true
requirements:
  - PH29-KDF
  - PH29-BACKUP
must_haves:
  truths:
    - New wallet and backup encryption semantics are modeled on one explicit self-describing KDF contract instead of parallel implicit behavior.
    - Unknown backup or wallet KDF versions are rejected before expensive derivation.
    - Successful V1 wallet unlock rewrites persisted state to the canonical V2 contract and proves reopen from the rewritten `.wlt`.
  artifacts:
    - path: crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
      provides: Versioned backup header with explicit KDF description and compatibility handling.
    - path: crates/z00z_wallets/src/db/redb_wallet_store.rs
      provides: Persisted V1-to-V2 rewrite during accepted unlock or migration.
    - path: crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs
      provides: Existing create-save-load-export-import roundtrip coverage kept aligned with the new backup and wallet KDF contract.
    - path: crates/z00z_wallets/tests/test_redb_wlt_open.rs
      provides: Existing `.wlt` open-path regression coverage kept aligned with the persisted V1-to-V2 rewrite contract.
    - path: crates/z00z_wallets/tests/test_backup_kdf_contract.rs
      provides: Regression coverage for self-describing backup KDF behavior.
    - path: crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
      provides: Legacy-read plus persisted-rewrite plus reopen coverage for `.wlt` migration.
  key_links:
    - from: crates/z00z_wallets/src/db/redb_wallet_crypto.rs
      to: crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
      via: RedB V2 `KdfParams` shape becomes the model for backup KDF governance
      pattern: KdfParams|version|algorithm|salt|memory_cost|time_cost|parallelism
    - from: crates/z00z_wallets/src/db/redb_wallet_store.rs
      to: crates/z00z_wallets/src/core/security/encryption.rs
      via: legacy wallet-encryption read compatibility yields one persisted V2 rewrite path
      pattern: migrate_kdf_if_needed|migrate_kdf_v1_to_v2|WalletEncryption
---

<objective>
Converge wallet and backup KDF governance onto the RedB V2 baseline and make migration persistent.

Purpose: remove the strongest persistence-contract drift in `z00z_wallets` so wallet files and backups no longer depend on implicit KDF semantics or soft upgrade side effects.
Output: one self-describing backup KDF header, one explicit unknown-version rejection policy, and one persisted V1-to-V2 `.wlt` migration contract with reopen proof.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/029-crypto-audit-wallets/029-FUSION.md
@.planning/phases/029-crypto-audit-wallets/029-CONTEXT.md
@.planning/phases/029-crypto-audit-wallets/029-RECONCILIATION.md
@crates/z00z_wallets/src/core/security/encryption.rs
@crates/z00z_wallets/src/core/backup/wallet_backup.rs
@crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
@crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
@crates/z00z_wallets/src/db/redb_wallet_crypto.rs
@crates/z00z_wallets/src/db/redb_wallet_store.rs
@crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs
@crates/z00z_wallets/tests/test_redb_wlt_open.rs
@crates/z00z_wallets/tests/test_wlt_validator.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make backup KDF semantics self-describing and aligned with the RedB V2 contract</name>
  <files>crates/z00z_wallets/src/core/backup/wallet_backup.rs
crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs
crates/z00z_wallets/tests/test_wlt_validator.rs
crates/z00z_wallets/tests/test_backup_kdf_contract.rs</files>
  <behavior>
    - Test 1: backup export persists one versioned KDF description with explicit algorithm, salt, memory cost, time cost, parallelism, and salt-padding semantics.
    - Test 2: backup import rejects unknown KDF versions before expensive derivation or decrypt work starts.
    - Test 3: legacy backup reads remain available only through one explicit compatibility path.
    - Test 4: the pre-existing wallet persistence and validator anchors remain aligned with the new self-describing backup contract instead of silently preserving implicit behavior.
  </behavior>
  <action>Model the backup KDF contract on RedB V2 `KdfParams` instead of inventing a parallel schema. Evolve the backup header and AAD compatibility code so new backup writes persist explicit KDF metadata, including the salt-normalization rule, while old readers remain bounded behind one explicit compatibility helper. Reject unknown KDF versions eagerly. Do not keep the old repeat-the-16-byte-salt behavior as the default write contract for new backups. Extend `test_wallet_persistence_backup_service.rs` and `test_wlt_validator.rs` where needed so the existing roundtrip and malformed-container anchors continue to prove the accepted contract instead of a superseded implicit one.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_backup_kdf_contract -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_persistence_backup_service -- --nocapture
cargo test -p z00z_wallets --release --test test_wlt_validator -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Backup KDF behavior is self-describing, versioned, and bounded by one explicit compatibility policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn V1 wallet migration into a persisted rewrite with reopen proof</name>
  <files>crates/z00z_wallets/src/core/security/encryption.rs
crates/z00z_wallets/src/db/redb_wallet_crypto.rs
crates/z00z_wallets/src/db/redb_wallet_store.rs
crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs
crates/z00z_wallets/tests/test_redb_wlt_open.rs
crates/z00z_wallets/tests/test_wallet_kdf_migration.rs</files>
  <behavior>
    - Test 1: legacy V1 wallet data can still be opened through explicit compatibility handling.
    - Test 2: an accepted unlock rewrites the wallet to the canonical V2 KDF contract instead of leaving migration as a best-effort side effect.
    - Test 3: the rewritten `.wlt` reopens cleanly under the V2 contract and no longer depends on repetition-padded salt semantics for new writes.
    - Test 4: the pre-existing wallet persistence and `.wlt` open anchors remain aligned with the new rewrite-on-accept contract.
  </behavior>
  <action>Keep the RedB V2 path as the canonical baseline and finish the migration governance in `redb_wallet_store.rs`. Successful unlock of legacy wallet state must trigger a persisted rewrite to the V2 contract and prove reopen from the rewritten container. Keep legacy-read support explicit and bounded. If `WalletEncryption` still participates in compatibility reads, keep that surface backward-compatible only; do not let it define the new-write policy. Extend `test_wallet_persistence_backup_service.rs` and `test_redb_wlt_open.rs` where needed so the pre-existing persistence anchors remain truthful after migration becomes a persisted accept path instead of a soft side effect.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_wallets --release --test test_wallet_kdf_migration -- --nocapture
cargo test -p z00z_wallets --release --test test_wallet_persistence_backup_service -- --nocapture
cargo test -p z00z_wallets --release --test test_redb_wlt_open -- --nocapture
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Legacy wallet unlock is compatible but bounded, and accepted migration now proves one persisted rewrite plus one successful reopen on the canonical V2 contract.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate B is satisfied for KDF governance: backup headers are self-describing and V1 `.wlt` unlock no longer leaves migration as a soft side effect.
</verification>

<success_criteria>
Wallet files and backups now derive encryption keys under one explicit governed contract, and legacy compatibility is preserved only through bounded migration paths.
</success_criteria>

<output>
After completion, create `.planning/phases/029-crypto-audit-wallets/029-03-SUMMARY.md`
</output>