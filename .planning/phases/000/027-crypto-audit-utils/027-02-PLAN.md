<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 027-crypto-audit-utils
plan: "02"
type: execute
wave: 2
depends_on:
  - 027-01
files_modified:
  - crates/z00z_utils/src/config/yaml.rs
  - crates/z00z_utils/src/config/layered.rs
  - crates/z00z_utils/src/config/mod.rs
  - crates/z00z_utils/src/config/test_config.rs
  - crates/z00z_utils/tests/test_config_integration.rs
  - crates/z00z_utils/examples/config_demo.rs
  - crates/z00z_utils/Z00Z_UTILS_MODULE_MAP.md
  - crates/z00z_utils/Z00Z_UTILS_QUICK_REFERENCE.md
autonomous: true
requirements:
  - PH27-CONFIG
must_haves:
  truths:
    - YAML config reads are bounded and malformed or unreadable YAML no longer collapses into a silent default path.
    - The default layered-config constructor fails closed except for an explicit missing-file downgrade.
    - Docs, examples, and tests all point at the same constructor policy instead of preserving the old fail-open default by accident.
  artifacts:
    - path: crates/z00z_utils/src/config/yaml.rs
      provides: Bounded YAML file loading, one named YAML size-limit constant, and explicit error classification.
    - path: crates/z00z_utils/src/config/layered.rs
      provides: Explicit fail-closed versus optional layered-config constructors.
    - path: crates/z00z_utils/tests/test_config_integration.rs
      provides: Oversized, malformed, permission-denied, and missing-file regression coverage.
    - path: crates/z00z_utils/Z00Z_UTILS_QUICK_REFERENCE.md
      provides: User-facing constructor guidance aligned to the chosen layered-config policy.
  key_links:
    - from: crates/z00z_utils/src/config/yaml.rs
      to: crates/z00z_utils/src/config/layered.rs
      via: YAML load result classification in the default constructor path
      pattern: from_file|with_yaml|new|default
---

<objective>
Bound YAML input and remove implicit fail-open behavior from the default layered-config path.

Purpose: close the clearest post-blocker configuration risk cluster from `027-FUSION.md` before time or RNG migration decisions amplify it downstream.
Output: one bounded YAML load path, one explicit default constructor policy, and a complete error-matrix regression suite.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/027-crypto-audit-utils/027-FUSION.md
@.planning/phases/027-crypto-audit-utils/027-CONTEXT.md
@.planning/phases/027-crypto-audit-utils/027-RESEARCH.md
@crates/z00z_utils/src/config/mod.rs
@crates/z00z_utils/src/config/yaml.rs
@crates/z00z_utils/src/config/layered.rs
@crates/z00z_utils/src/config/test_config.rs
@crates/z00z_utils/tests/test_config_integration.rs
@crates/z00z_utils/examples/config_demo.rs
@crates/z00z_utils/Z00Z_UTILS_MODULE_MAP.md
@crates/z00z_utils/Z00Z_UTILS_QUICK_REFERENCE.md
@crates/z00z_utils/src/io/fs.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route YAML file loading through a bounded read and explicit error surface</name>
  <files>crates/z00z_utils/src/config/yaml.rs
crates/z00z_utils/src/config/mod.rs
crates/z00z_utils/src/config/test_config.rs
crates/z00z_utils/tests/test_config_integration.rs</files>
  <behavior>
    - Test 1: Missing YAML still returns a typed not-found error from `YamlConfig::from_file`.
    - Test 2: Malformed YAML, oversized input, and permission-denied input all surface explicit errors.
    - Test 3: Successful bounded YAML loads still preserve the existing dotted-key lookup behavior.
    - Test 4: Oversized input is proven against the chosen bound with a fixture that exceeds the named limit by at least one byte.
  </behavior>
  <action>Implement Decision "YAML and Layered Config Policy" from `027-CONTEXT.md`. Add exactly one config-owned YAML size-limit constant in `crates/z00z_utils/src/config/yaml.rs` and route `YamlConfig::from_file` through the existing bounded I/O helpers in `crates/z00z_utils/src/io/fs.rs` before UTF-8 and YAML parsing. Keep the public `YamlConfig` shape stable where possible, but classify oversized, malformed, permission-denied, and not-found cases explicitly enough that `LayeredConfig` can distinguish them. The regression suite for this task must prove all four cases separately rather than collapsing them into one generic failure assertion. Preserve dotted key lookup semantics. Do not add a new YAML dependency. If a checkpoint commit becomes useful before the constructor split, use `/z00z-git-versioning` rather than raw git commands.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release --test test_config_integration
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>`YamlConfig::from_file` is bounded and produces an error surface rich enough to distinguish missing-file fallback from real configuration failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split the layered-config constructors so the permissive path is explicit</name>
  <files>crates/z00z_utils/src/config/layered.rs
crates/z00z_utils/src/config/mod.rs
crates/z00z_utils/src/config/test_config.rs
crates/z00z_utils/tests/test_config_integration.rs
crates/z00z_utils/examples/config_demo.rs
crates/z00z_utils/Z00Z_UTILS_MODULE_MAP.md
crates/z00z_utils/Z00Z_UTILS_QUICK_REFERENCE.md</files>
  <behavior>
    - Test 1: The default constructor no longer swallows malformed or permission-denied YAML errors.
    - Test 2: A clearly named optional or permissive constructor treats only missing YAML as an allowed downgrade.
    - Test 3: Rustdoc examples, internal module-map docs, and quick-reference docs all reflect the same constructor vocabulary.
  </behavior>
  <action>Refactor `LayeredConfig` so the fail-open behavior is no longer implicit. Keep one fail-closed default path such as `new`, `try_new`, or `default`, and introduce a clearly named optional YAML constructor for the missing-file-only downgrade. Update crate docs and examples to use the chosen policy explicitly, and update tests so `NotFound` is the only downgrade that can remove YAML from the default layered flow. Also update the existing `Z00Z_UTILS_MODULE_MAP.md` and `Z00Z_UTILS_QUICK_REFERENCE.md` guidance if the constructor names or recommended call shape change; do not leave the old fail-open vocabulary behind in internal reference docs. Preserve `EnvConfig` priority over YAML values. If downstream examples break, fix them in this task instead of deferring doc drift.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release --test test_config_integration
cargo check -p z00z_utils --release --example config_demo
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The default layered-config path fails closed, the permissive path is explicit, and the YAML error matrix from Gate B is covered by tests and examples.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate B from `027-CONTEXT.md` is satisfied for not-found, malformed, oversized, and permission-denied YAML inputs.
</verification>

<success_criteria>
`YamlConfig` and `LayeredConfig` expose one bounded, explicit policy surface instead of a mix of unbounded reads and silent YAML suppression.
</success_criteria>

<output>
After completion, create `.planning/phases/027-crypto-audit-utils/027-02-SUMMARY.md`
</output>