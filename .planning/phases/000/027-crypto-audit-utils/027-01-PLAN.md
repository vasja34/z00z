<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 027-crypto-audit-utils
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_utils/src/os_hardening.rs
  - crates/z00z_utils/tests/test_os_hardening_integration.rs
autonomous: true
requirements:
  - PH27-MEMLOCK
must_haves:
  truths:
    - Safe callers can no longer produce a dangling `LockedBytes` drop path through the public `lock_bytes_best_effort` API.
    - `LockedBytes` still zeroizes the locked buffer before the unlock call, but it no longer leaks raw addresses through `Debug` output.
    - The lifetime and drop contract is proven with executable validation rather than comments alone.
  artifacts:
    - path: crates/z00z_utils/src/os_hardening.rs
      provides: Lifetime-bound `LockedBytes` guard and best-effort lock API.
    - path: crates/z00z_utils/tests/test_os_hardening_integration.rs
      provides: Guard-shape and cross-platform integration coverage.
  key_links:
    - from: crates/z00z_utils/src/os_hardening.rs
      to: crates/z00z_utils/tests/test_os_hardening_integration.rs
      via: public `lock_bytes_best_effort` and guard drop behavior
      pattern: lock_bytes_best_effort|LockedBytes
---

<objective>
Make the Phase 027 release blocker lifetime-safe and prove the `LockedBytes` contract.

Purpose: remove the only confirmed sign-off blocker from `027-FUSION.md` before any lower-priority hardening work starts.
Output: a lifetime-bound `LockedBytes<'a>` guard, sanitized debug output, and executable validation covering type-level safety plus best-effort runtime behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/027-crypto-audit-utils/027-FUSION.md
@.planning/phases/027-crypto-audit-utils/027-CONTEXT.md
@.planning/phases/027-crypto-audit-utils/027-RESEARCH.md
@crates/z00z_utils/src/os_hardening.rs
@crates/z00z_utils/tests/test_os_hardening_integration.rs

<interfaces>
From `crates/z00z_utils/src/os_hardening.rs`:

```rust
pub fn lock_bytes_best_effort(bytes: &mut [u8]) -> Option<LockedBytes>;
pub struct LockedBytes;
```

Target direction for this plan:

```rust
pub fn lock_bytes_best_effort<'a>(bytes: &'a mut [u8]) -> Option<LockedBytes<'a>>;
pub struct LockedBytes<'a> { /* lifetime-bound guard */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the address-only guard with a lifetime-bound `LockedBytes<'a>` contract</name>
  <files>crates/z00z_utils/src/os_hardening.rs</files>
  <behavior>
    - Test 1: `lock_bytes_best_effort` returns a guard whose lifetime is bound to the borrowed slice.
    - Test 2: `LockedBytes` zeroizes before unlock without keeping raw integer addresses in the public debug surface.
    - Test 3: Empty-slice behavior remains best-effort and non-panicking.
  </behavior>
  <action>Implement Decision "Secret-Memory Boundary" from `027-CONTEXT.md`. Refactor `LockedBytes` to `LockedBytes<'a>` backed by `NonNull<u8>` plus `PhantomData<&'a mut [u8]>` instead of a plain address integer. Keep the zeroize-before-unlock order, preserve best-effort Unix and Windows behavior, and remove raw backing addresses from `Debug`. Do not widen the public API beyond the existing `lock_bytes_best_effort` entrypoint. If a small helper is needed for platform-specific unlock logic, keep it private to `os_hardening.rs`. If a checkpoint commit becomes useful before the unsafe refactor, use `/z00z-git-versioning` instead of ad hoc git commands.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo +nightly miri test -p z00z_utils --test test_os_hardening_integration</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The public lock guard is lifetime-bound, preserves zeroize-before-unlock semantics, and no longer relies on comments to prevent a dangling drop path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add guard-shape regression coverage and Miri-oriented validation hooks</name>
  <files>crates/z00z_utils/tests/test_os_hardening_integration.rs</files>
  <behavior>
    - Test 1: The integration surface still accepts empty slices and best-effort platform failures without panicking.
    - Test 2: The guard remains debug-printable without leaking raw memory addresses.
    - Test 3: The public API shape used by downstream callers compiles against the new lifetime-bound contract.
  </behavior>
  <action>Upgrade the integration test seam so the new guard contract is exercised through the public API rather than private internals. Keep the tests cross-platform and non-destructive, but make the Miri command meaningful by ensuring the integration target actually instantiates the guard through safe code. Record any platform-specific Miri limitation in the phase summary instead of weakening the type-level fix. If additional compile-only assertions are needed to pin the public API contract, keep them inside this integration target.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release --test test_os_hardening_integration
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo +nightly miri test -p z00z_utils --test test_os_hardening_integration</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The blocker fix is backed by executable integration coverage and a repeatable Miri-oriented validation command.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate A from `027-CONTEXT.md` is satisfied: lifetime-bound guard shape, unit or integration coverage, and Miri-oriented validation all pass.
</verification>

<success_criteria>
`LockedBytes` is no longer an address-only guard, safe callers cannot outlive the borrowed slice through the public API, and the blocker is closed with executable proof instead of comments.
</success_criteria>

<output>
After completion, create `.planning/phases/027-crypto-audit-utils/027-01-SUMMARY.md`
</output>