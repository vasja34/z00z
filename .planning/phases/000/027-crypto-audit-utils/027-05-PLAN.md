<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 027-crypto-audit-utils
plan: "05"
type: execute
wave: 5
depends_on:
  - 027-04
files_modified:
  - crates/z00z_utils/src/logger/mod.rs
  - crates/z00z_utils/src/logger/file_logger.rs
  - crates/z00z_utils/src/logger/rotating_file_logger.rs
  - crates/z00z_utils/src/logger/structured.rs
  - crates/z00z_utils/src/logger/test_logger.rs
  - crates/z00z_utils/tests/test_logger_integration.rs
  - crates/z00z_utils/src/io/fs.rs
  - crates/z00z_utils/tests/test_io_integration.rs
autonomous: true
requirements:
  - PH27-LOGGER
  - PH27-IO
must_haves:
  truths:
    - Persisted log output strips ANSI and control-byte injection and keeps the log level visible on disk.
    - Structured logging fallback does not collapse serialization failures into an uninformative normal-looking event; it keeps the stable explicit `logger.serialize_error` sentinel contract for this wave.
    - Generic write helpers no longer swallow permission-copy failures.
    - The durability and secrecy difference between generic and private write paths is explicit in code, tests, or docs.
  artifacts:
    - path: crates/z00z_utils/src/logger/mod.rs
      provides: Shared message sanitization policy.
    - path: crates/z00z_utils/src/logger/rotating_file_logger.rs
      provides: Rotating persisted log format with severity prefix coverage.
    - path: crates/z00z_utils/src/logger/structured.rs
      provides: Explicit structured-event failure contract with the stable `logger.serialize_error` sentinel shape instead of low-signal fallback drift.
    - path: crates/z00z_utils/src/io/fs.rs
      provides: Explicit generic-versus-private write semantics and permission propagation.
  key_links:
    - from: crates/z00z_utils/src/logger/mod.rs
      to: crates/z00z_utils/src/logger/rotating_file_logger.rs
      via: shared sanitize_message behavior and persisted line format
      pattern: sanitize_message|write_log
    - from: crates/z00z_utils/src/logger/structured.rs
      to: crates/z00z_utils/tests/test_logger_integration.rs
      via: structured-event serialization failure regression coverage
      pattern: StructuredLogger|log_event|logger.serialize_error
    - from: crates/z00z_utils/src/io/fs.rs
      to: crates/z00z_utils/tests/test_io_integration.rs
      via: permission-copy failure and durability contract coverage
      pattern: write_file|atomic_write_file_private|atomic_write_file_streaming
---

<objective>
Harden persisted logging plus generic write semantics for sanitization, severity fidelity, and durability policy.

Purpose: close the remaining high-value misuse-resistance gaps after the blocker, config, time, and RNG contract waves have frozen the public direction.
Output: one stronger persisted log sanitizer, one restored rotating-log severity format, and explicit generic write semantics for permissions and durability.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/027-crypto-audit-utils/027-FUSION.md
@.planning/phases/027-crypto-audit-utils/027-CONTEXT.md
@.planning/phases/027-crypto-audit-utils/027-RESEARCH.md
@crates/z00z_utils/src/logger/mod.rs
@crates/z00z_utils/src/logger/file_logger.rs
@crates/z00z_utils/src/logger/rotating_file_logger.rs
@crates/z00z_utils/src/logger/structured.rs
@crates/z00z_utils/src/logger/test_logger.rs
@crates/z00z_utils/tests/test_logger_integration.rs
@crates/z00z_utils/src/io/fs.rs
@crates/z00z_utils/tests/test_io_integration.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend persisted log sanitization and restore rotating severity prefixes</name>
  <files>crates/z00z_utils/src/logger/mod.rs
crates/z00z_utils/src/logger/file_logger.rs
crates/z00z_utils/src/logger/rotating_file_logger.rs
crates/z00z_utils/src/logger/structured.rs
crates/z00z_utils/src/logger/test_logger.rs
crates/z00z_utils/tests/test_logger_integration.rs</files>
  <behavior>
    - Test 1: ANSI escape sequences and broader control-byte injection are sanitized before persisted writes.
    - Test 2: Rotating log output keeps the severity prefix in persisted lines.
    - Test 3: Structured logging serialization failure remains explicit and testable instead of degrading to a low-signal fallback event.
    - Test 4: A regression test in `crates/z00z_utils/tests/test_logger_integration.rs` forces structured-event serialization failure and asserts that the emitted payload contains the explicit `logger.serialize_error` marker.
    - Test 5: Existing best-effort symlink protection remains intact and explicitly documented where parent-directory trust still applies.
  </behavior>
  <action>Implement the logger portion of `027-FUSION.md` and Gate F from `027-CONTEXT.md`. Strengthen `sanitize_message` to neutralize ANSI escapes and other non-printable control-byte injection beyond the existing newline, carriage-return, and NUL handling. Ensure both `FileLogger` and `RotatingFileLogger` inherit the shared sanitizer behavior. Restore the log-level prefix in rotating output so persisted log lines preserve operational severity. Also tighten `StructuredLogger` failure handling so serialization errors stay explicit and diagnosable instead of collapsing to a low-signal normal-looking event. For this wave, the observable failure contract is fixed: preserve the explicit `logger.serialize_error` marker on serialization failure and do not rename it. Pin that behavior with one concrete regression in `crates/z00z_utils/tests/test_logger_integration.rs` that forces a serialization failure and asserts the emitted payload contains `logger.serialize_error`. Keep the existing final-component symlink rejection unless a tighter existing-dependency path is trivial; otherwise document the trusted-parent assumption instead of overclaiming a TOCTOU fix. Stay in-tree unless a new dependency is explicitly justified.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release --test test_logger_integration
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Persisted loggers sanitize injected escape or control bytes consistently and no longer drop severity prefixes during rotation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make generic write semantics explicit and stop swallowing permission-copy failures</name>
  <files>crates/z00z_utils/src/io/fs.rs
crates/z00z_utils/tests/test_io_integration.rs</files>
  <behavior>
    - Test 1: `write_file` propagates permission-copy failures instead of silently ignoring them.
    - Test 2: Generic versus private write durability semantics are explicit in the code and tested where practical.
    - Test 3: Private atomic write behavior for secret material remains the strongest path.
    - Test 4: The permission-copy failure proof is deterministic in CI, either through a reproducible filesystem setup or through one explicitly documented crate-private test seam for permission propagation.
  </behavior>
  <action>Implement the file-I/O portion of `027-FUSION.md` and Gate G from `027-CONTEXT.md`. Refactor `write_file` so permission-copy failures are surfaced instead of swallowed. Keep `atomic_write_file_private` as the canonical secret path, and document or encode the durability distinction between `write_file`, `atomic_write_file_private`, and `atomic_write_file_streaming` across Unix and non-Unix paths. Add regression coverage for overwrite-with-permissions scenarios and any platform-gated durability expectations that can be tested without destructive tricks. The permission-copy failure proof for this wave must be deterministic in CI: either reproduce the failure through a stable platform-gated filesystem setup, or, if host permissions cannot trigger the failure reliably with tempdir-only test fixtures, add one explicitly documented crate-private permission-propagation seam inside `crates/z00z_utils/src/io/fs.rs` and drive the failure path through `crates/z00z_utils/tests/test_io_integration.rs` without widening the public API. Record the chosen proof strategy in the phase summary so reviewers know whether the proof is host-driven or seam-driven. If a stronger generic durable variant is introduced, keep it clearly named rather than silently changing the meaning of the existing helper.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release --test test_io_integration
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Generic write helpers stop hiding permission-copy failures and clearly advertise their durability or secrecy contract relative to the private atomic path.</done>
</task>

</tasks>

<verification>
Close this plan only after Gates F and G from `027-CONTEXT.md` are satisfied: sanitizer coverage proves escape handling and severity fidelity, and I/O coverage proves explicit permission and durability semantics.
</verification>

<success_criteria>
`z00z_utils` persisted logging and write helpers have explicit, test-backed guarantees instead of best-effort behavior that looks stronger than it is.
</success_criteria>

<output>
After completion, create `.planning/phases/027-crypto-audit-utils/027-05-SUMMARY.md`
</output>