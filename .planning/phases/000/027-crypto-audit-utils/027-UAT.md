---
status: testing
phase: 027-crypto-audit-utils
source:
  - 027-01-SUMMARY.md
  - 027-02-SUMMARY.md
  - 027-03-SUMMARY.md
  - 027-04-SUMMARY.md
  - 027-05-SUMMARY.md
  - 027-06-SUMMARY.md
started: 2026-03-29T19:02:10Z
updated: 2026-03-29T19:02:10Z
---

# Phase 027 UAT

## Current Test

number: 1
name: Cold Start Smoke Test
expected: |
  Stop any running Rust process that could mask startup problems, then run the
  Phase 027 release-style verification from a fresh shell. The workspace should
  build and execute without relying on warm caches, stale RPC logs, or stale
  session state. A clean run of the final release gate should complete green
  without reintroducing the earlier `z00z_wallets` fallout.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test

expected: Stop any running Rust process, then run the Phase 027 release-style verification from a fresh shell. The workspace should complete the final release gate green without stale-cache or stale-artifact dependence.
result: [pending]

### 2. Secret Memory Hardening

expected: The memlock surface should behave as a safe public contract: borrowed lock guards stay lifetime-bound, debug output does not expose raw addresses, long-lived wallet secrets use owned locked storage, and the memlock integration seam remains clean under release and Miri validation.
result: [pending]

### 3. Config Fail-Closed Behavior

expected: Present-but-invalid config input should fail explicitly. Oversized YAML, malformed YAML, permission-denied YAML, non-UTF-8 env overrides, and YAML null or non-scalar leaves should surface typed errors, while missing YAML should downgrade only through the explicit optional path.
result: [pending]

### 4. Time Policy Fail-Closed Behavior

expected: Security-sensitive time consumers should fail closed on clock problems. Wallet session expiry, seed-view throttling, and related time-gated checks should no longer depend on ambiguous lossy helpers, and the production-perimeter time scan should stay clean.
result: [pending]

### 5. Deterministic RNG Guardrails

expected: Approved reproducibility domains in genesis and simulator flows should remain stable, but deterministic RNG should no longer appear as ambient production entropy outside the explicit allowlist.
result: [pending]

### 6. Persisted Logger And File I/O Hardening

expected: Persisted log files should neutralize ANSI or control-byte injection, preserve severity prefixes on disk, keep `logger.serialize_error` explicit on structured failures, and surface permission-copy failures in generic write helpers instead of silently hiding them.
result: [pending]

### 7. JSON Boundary Ownership

expected: Structured logger macros should build payloads through the owned `z00z_utils::codec` JSON surface rather than direct `serde_json` macro drift, and the full release gate should remain green with that narrow compatibility exception documented explicitly.
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 0

## Gaps

none
