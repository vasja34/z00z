<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 027-crypto-audit-utils
plan: "03"
type: execute
wave: 3
depends_on:
  - 027-02
files_modified:
  - crates/z00z_utils/src/time/traits.rs
  - crates/z00z_utils/src/time/mod.rs
  - crates/z00z_utils/src/time/system.rs
  - crates/z00z_utils/src/time/test_time.rs
  - crates/z00z_utils/tests/test_time_policy_micros.rs
  - crates/z00z_utils/examples/time_provider_demo.rs
  - crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
  - crates/z00z_wallets/tests/test_key_manager.rs
  - crates/z00z_wallets/tests/test_stealth_request.rs
  - crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
  - crates/z00z_core/bin/assets/assets_generation_cli.rs
  - crates/z00z_core/src/genesis/genesis.rs
  - crates/z00z_core/src/assets/registry.rs
  - crates/z00z_core/src/assets/snapshot.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend.rs
  - crates/z00z_wallets/src/core/address/stealth_trust.rs
  - crates/z00z_wallets/src/core/key/key_manager.rs
  - crates/z00z_wallets/src/core/wallet/policy.rs
  - crates/z00z_wallets/src/services/chain_service.rs
  - crates/z00z_wallets/src/services/wallet_service.rs
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/wallet_flow.rs
autonomous: true
requirements:
  - PH27-TIME
must_haves:
  truths:
    - The fallible `try_unix_timestamp*` surface is the blessed production contract for security-sensitive paths.
    - Lossy zero-fallback helpers remain only as explicit compatibility paths, not as the default or shortest production guidance.
    - No verified security-critical downstream `.unix_timestamp*()` caller closes the phase still depending on ambiguous lossy wrappers, including any additional direct callers found during the required repository re-scan.
  artifacts:
    - path: crates/z00z_utils/src/time/traits.rs
      provides: Explicit fallible-versus-compatibility timestamp contract.
    - path: crates/z00z_utils/tests/test_time_policy_micros.rs
      provides: Repository guardrail for forbidden or deprecated time patterns.
    - path: crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
      provides: Wallet rate-limit regression coverage for fail-closed timing behavior.
    - path: crates/z00z_wallets/tests/test_stealth_request.rs
      provides: Expiry-path coverage for security-sensitive timestamp handling.
    - path: crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
      provides: Release-style simulator proof that approved non-security compatibility paths remain green.
    - path: crates/z00z_wallets/src/services/wallet_service.rs
      provides: Representative downstream consumer migration away from ambiguous lossy helpers where required.
    - path: crates/z00z_wallets/src/core/wallet/policy.rs
      provides: Security-sensitive wallet policy timing classification in the rollout set.
  key_links:
    - from: crates/z00z_utils/src/time/traits.rs
      to: crates/z00z_core/src/genesis/genesis.rs
      via: chosen helper naming and fallible timestamp contract
      pattern: try_unix_timestamp|unix_timestamp
    - from: crates/z00z_utils/tests/test_time_policy_micros.rs
      to: crates/z00z_wallets/src/services/wallet_service.rs
      via: repository time-policy enforcement
      pattern: unix_timestamp|unix_timestamp_millis|unix_timestamp_micros
    - from: crates/z00z_utils/tests/test_time_policy_micros.rs
      to: crates/z00z_wallets/src/core/wallet/policy.rs
      via: repository time-policy enforcement for policy-gated runtime checks
      pattern: unix_timestamp|unix_timestamp_millis|unix_timestamp_micros
---

<objective>
Freeze the fail-closed time-provider contract and migrate verified downstream consumers off ambiguous lossy wrappers.

Purpose: satisfy Gates C and D from `027-CONTEXT.md` before deterministic RNG or logger hardening locks in broader utility semantics.
Output: one explicit time-policy surface in `z00z_utils`, updated downstream call sites across the verified consumer list, and regression coverage that prevents security-sensitive drift back to lossy helpers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/027-crypto-audit-utils/027-FUSION.md
@.planning/phases/027-crypto-audit-utils/027-CONTEXT.md
@.planning/phases/027-crypto-audit-utils/027-RESEARCH.md
@crates/z00z_utils/src/time/traits.rs
@crates/z00z_utils/tests/test_time_policy_micros.rs
@crates/z00z_core/src/assets/nonce.rs
@crates/z00z_core/bin/assets/assets_generation_cli.rs
@crates/z00z_core/src/genesis/genesis.rs
@crates/z00z_core/src/assets/registry.rs
@crates/z00z_core/src/assets/snapshot.rs
@crates/z00z_storage/src/assets/store_internal/redb_backend.rs
@crates/z00z_wallets/src/core/address/stealth_trust.rs
@crates/z00z_wallets/src/core/key/key_manager.rs
@crates/z00z_wallets/src/core/wallet/policy.rs
@crates/z00z_wallets/src/services/chain_service.rs
@crates/z00z_wallets/src/services/wallet_service.rs
@crates/z00z_simulator/src/scenario_1/stage_3.rs
@crates/z00z_simulator/src/scenario_1/stage_3_utils/wallet_flow.rs

<assumptions>
- The file list in `027-CONTEXT.md` and `027-RESEARCH.md` is the minimum mandatory rollout set, not proof that no other first-party production callers exist.
- The front-matter `files_modified` inventory is the known seed set at planning time, not the full closure set; if the mandatory production-perimeter scan finds additional direct callers that require in-wave migration, execution must extend the touched-file set and the phase summary instead of pretending the original inventory was exhaustive.
- Before closing this wave, execution must re-scan all first-party production Rust sources for additional `.unix_timestamp*()` consumers and either migrate them, classify them as explicit non-security compatibility users, or record them as a blocker or defer with justification in the phase summary.
- Before closing this wave, execution must run one recorded repository re-scan command for the declared production perimeter and preserve that exact command in the phase summary so reviewers can reproduce the closure set instead of re-interpreting the scan rules from prose.
- For this plan, `first-party production Rust sources` means workspace `crates/**/src/**/*.rs` and `crates/**/bin/**/*.rs` files, excluding `crates/z00z_crypto/tari/**`, `crates/z00z_utils/src/time/**`, tests, examples, benches, and fuzz-only files.
</assumptions>

<interfaces>
From `crates/z00z_utils/src/time/traits.rs`:

```rust
fn try_unix_timestamp(&self) -> Result<u64, TimeError>;
fn try_unix_timestamp_ms(&self) -> Result<u64, TimeError>;
fn try_unix_timestamp_micros(&self) -> Result<u64, TimeError>;
fn unix_timestamp(&self) -> u64;
fn unix_timestamp_millis(&self) -> u64;
fn unix_timestamp_micros(&self) -> u64;
```

Verified canonical pattern already in scope:

```rust
time_provider.try_unix_timestamp_micros()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the lossy time helpers an explicit compatibility surface in `z00z_utils`</name>
  <files>crates/z00z_utils/src/time/traits.rs
crates/z00z_utils/src/time/mod.rs
crates/z00z_utils/src/time/system.rs
crates/z00z_utils/src/time/test_time.rs
crates/z00z_utils/tests/test_time_policy_micros.rs
crates/z00z_utils/examples/time_provider_demo.rs</files>
  <behavior>
    - Test 1: The fallible `try_unix_timestamp*` methods remain the canonical production path.
    - Test 2: Lossy helpers are either explicitly renamed, deprecated, or wrapped behind compatibility wording that cannot be mistaken for the blessed production contract.
    - Test 3: Repository time-policy tests reject forbidden rounded-micros or direct epoch-micros patterns and cover any new compatibility naming.
  </behavior>
  <action>Implement Decision "Time Provider Policy" from `027-CONTEXT.md`. Keep the existing fallible capability and make the lossy zero-fallback path explicit by either renaming it, deprecating it, or introducing clearly named compatibility wrappers while preserving a workable migration path. Update time docs, examples, and tests so the shortest production guidance always points at `try_unix_timestamp*`. Extend the repository guard test if needed so it can distinguish approved compatibility helpers from forbidden rounded-micros or direct `SystemTime` epoch-micros patterns. Do not change the blessed `z00z_core/src/assets/nonce.rs` fail-closed pattern away from `try_unix_timestamp_micros`.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release --test test_time_policy_micros
cargo check -p z00z_utils --release --example time_provider_demo
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The `z00z_utils` time surface makes fallible production behavior explicit and treats lossy wrappers as compatibility-only rather than as the default policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify and migrate the verified downstream time consumers</name>
  <files>crates/z00z_core/src/genesis/genesis.rs
crates/z00z_core/bin/assets/assets_generation_cli.rs
crates/z00z_core/src/assets/registry.rs
crates/z00z_core/src/assets/snapshot.rs
crates/z00z_storage/src/assets/store_internal/redb_backend.rs
crates/z00z_wallets/src/core/address/stealth_trust.rs
crates/z00z_wallets/src/core/key/key_manager.rs
crates/z00z_wallets/src/core/wallet/policy.rs
crates/z00z_wallets/src/services/chain_service.rs
crates/z00z_wallets/src/services/wallet_service.rs
crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs
crates/z00z_wallets/tests/test_key_manager.rs
crates/z00z_wallets/tests/test_stealth_request.rs
crates/z00z_simulator/src/scenario_1/stage_3.rs
crates/z00z_simulator/src/scenario_1/stage_3_utils/wallet_flow.rs
crates/z00z_simulator/tests/test_scenario1_unified_gate.rs</files>
  <behavior>
    - Test 1: Security-sensitive consumers move to `try_unix_timestamp*` and typed error handling.
    - Test 2: Operational or demo-only consumers keep compatibility helpers only when their non-security role is explicit in code or summary notes.
    - Test 3: The simulator release-style gate still passes with the exact user-preferred command after the migration.
    - Test 4: Wallet rate-limit, key-manager, and request-expiry anchors prove that fail-closed time handling survives real downstream workflows instead of only source scans.
    - Test 5: The repository-wide re-scan closes with no unclassified first-party production direct `.unix_timestamp*()` caller left behind.
  </behavior>
  <action>Use the verified consumer list from `027-CONTEXT.md` and `027-RESEARCH.md` as the minimum mandatory rollout scope for this plan, not as a claim that no other production consumers exist. Classify each listed call site as security-critical, operational, or demo/test-only and migrate every security-critical site away from lossy wrappers. During execution, re-scan all first-party production Rust sources for additional direct `.unix_timestamp(`, `.unix_timestamp_millis(`, and `.unix_timestamp_micros(` call sites before closing the wave. The closure scan for this wave must be recorded as an exact command in the phase summary and must, at minimum, cover both the direct helper call pattern and the forbidden direct epoch-micros pattern already guarded by `crates/z00z_utils/tests/test_time_policy_micros.rs`; the recommended command form is `rg -n '\.unix_timestamp(_millis|_micros)?\(|duration_since\(SystemTime::UNIX_EPOCH\).*as_micros\(' crates --glob 'crates/**/src/**/*.rs' --glob 'crates/**/bin/**/*.rs' --glob '!crates/z00z_crypto/tari/**' --glob '!crates/z00z_utils/src/time/**' --glob '!**/tests/**' --glob '!**/examples/**' --glob '!**/benches/**' --glob '!**/fuzz/**'`. At review time, the already observed extra live production callers extend beyond the shorter planning-time list into `crates/z00z_utils/src/logger/file_logger.rs`, `crates/z00z_wallets/src/services/app_service.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/tx_impl.rs`, `crates/z00z_wallets/src/db/redb_wallet_store.rs`, `crates/z00z_wallets/src/core/address/address_manager.rs`, `crates/z00z_wallets/src/core/address/stealth_request.rs`, `crates/z00z_wallets/src/core/tx/fee_estimator.rs`, `crates/z00z_wallets/src/core/storage/secret_store_impl.rs`, and `crates/z00z_simulator/src/scenario_1/stage_2_utils/flow.rs`; do not close the wave by classifying only the shorter planning-time list or by assuming the top-level `files_modified` inventory was near-exhaustive. Closure rule: no newly discovered first-party production direct `.unix_timestamp*()` caller within the declared scan perimeter may remain unclassified. Each additional direct caller found by the re-scan must end in exactly one of these states: migrated to `try_unix_timestamp*`, retained on an explicitly named compatibility helper with a documented non-security rationale, or recorded as a blocker or defer with file path and justification in the phase summary. Extend the downstream proof to the existing wallet anchors `crates/z00z_wallets/tests/test_addr_rate_limit_integration.rs`, `crates/z00z_wallets/tests/test_key_manager.rs`, and `crates/z00z_wallets/tests/test_stealth_request.rs`; if those anchors cannot absorb the remaining assertions cleanly, add one focused `crates/z00z_wallets/tests/test_time_policy_phase027.rs` seam and keep the ownership split explicit in the summary. Preserve compatibility only for non-security simulator, artifact, or operational timestamps that genuinely do not gate nonce, expiry, anti-replay, policy enforcement, or canonical ordering. Keep the full classification table in the phase summary for Gate C traceability, and back the approved non-security simulator path with the exact user-required release commands rather than a substitute fast-only check. If a checkpoint commit becomes useful during the cross-crate rollout, use `/z00z-git-versioning` instead of raw git commands.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release
cargo test -p z00z_wallets --release --test test_addr_rate_limit_integration -- --nocapture
cargo test -p z00z_wallets --release --test test_key_manager -- --nocapture
cargo test -p z00z_wallets --release --test test_stealth_request -- --nocapture
cargo test -p z00z_core --test genesis_tests -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump
rg -n '\.unix_timestamp(_millis|_micros)?\(|duration_since\(SystemTime::UNIX_EPOCH\).*as_micros\(' crates --glob 'crates/**/src/**/*.rs' --glob 'crates/**/bin/**/*.rs' --glob '!crates/z00z_crypto/tari/**' --glob '!crates/z00z_utils/src/time/**' --glob '!**/tests/**' --glob '!**/examples/**' --glob '!**/benches/**' --glob '!**/fuzz/**'
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>All verified security-critical downstream time consumers are fail closed, the required repository re-scan is completed, and any remaining lossy usage is explicit, justified, and non-security in scope.</done>
</task>

</tasks>

<verification>
Close this plan only after Gates C and D from `027-CONTEXT.md` are satisfied: every verified consumer is classified, and no security-critical consumer still depends on ambiguous lossy helpers.
</verification>

<success_criteria>
The time-provider contract is fail closed by default, downstream critical callers are migrated after a repository-wide production-consumer classification pass, and any remaining compatibility helpers are explicit transitional or demo-only paths.
</success_criteria>

<output>
After completion, create `.planning/phases/027-crypto-audit-utils/027-03-SUMMARY.md`
</output>