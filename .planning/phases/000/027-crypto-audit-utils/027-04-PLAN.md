<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 027-crypto-audit-utils
plan: "04"
type: execute
wave: 4
depends_on:
  - 027-03
files_modified:
  - crates/z00z_utils/Cargo.toml
  - crates/z00z_utils/src/rng/traits.rs
  - crates/z00z_utils/src/rng/deterministic.rs
  - crates/z00z_utils/src/rng/mock.rs
  - crates/z00z_utils/src/rng/mod.rs
  - crates/z00z_utils/src/rng/test_rng.rs
  - crates/z00z_utils/src/lib.rs
  - crates/z00z_core/Cargo.toml
  - crates/z00z_simulator/Cargo.toml
  - crates/z00z_crypto/src/types.rs
  - crates/z00z_core/bin/assets/assets_generation_cli.rs
  - crates/z00z_core/src/genesis/asset_std.rs
  - crates/z00z_core/src/genesis/genesis.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs
  - crates/z00z_core/tests/genesis/test_reproducibility.rs
  - crates/z00z_core/tests/genesis/test_genesis.rs
  - crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
autonomous: true
requirements:
  - PH27-RNG
must_haves:
  truths:
    - Deterministic RNG remains available for the approved genesis and simulator reproducibility domains, including the verified asset-generation tooling.
    - Unapproved production code cannot treat deterministic RNG as the blessed source of unpredictable entropy as easily as before.
    - The deterministic trait surface no longer carries approval-sounding semantics without an explicit justification.
    - No non-allowlisted direct or aliased deterministic-RNG consumer closes this wave without an explicit phase-summary decision.
  artifacts:
    - path: crates/z00z_utils/src/rng/traits.rs
      provides: Explicit deterministic-versus-secure RNG trait semantics.
    - path: crates/z00z_utils/Cargo.toml
      provides: Feature or build guardrail for deterministic RNG availability.
    - path: crates/z00z_core/Cargo.toml
      provides: Approved genesis-domain opt-in for deterministic RNG if gating is added.
    - path: crates/z00z_core/tests/genesis/test_reproducibility.rs
      provides: Deterministic genesis proof that approved seeds remain reproducible under the new guardrail.
    - path: crates/z00z_simulator/tests/test_scenario1_unified_gate.rs
      provides: Release-style simulator proof that approved deterministic reproducibility survives the guardrail.
  key_links:
    - from: crates/z00z_utils/src/rng/mod.rs
      to: crates/z00z_core/src/genesis/asset_std.rs
      via: approved deterministic export surface
      pattern: DeterministicRngProvider|DeterministicRng
    - from: crates/z00z_utils/src/rng/traits.rs
      to: crates/z00z_core/bin/assets/assets_generation_cli.rs
      via: approved genesis tooling contract
      pattern: DeterministicRngProvider|SecureRngProvider
    - from: crates/z00z_utils/src/rng/traits.rs
      to: crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs
      via: reproducibility-only consumer contract
      pattern: DeterministicRngProvider|SecureRngProvider
---

<objective>
Fence deterministic RNG to approved reproducibility domains and replace approval-sounding trait semantics.

Purpose: satisfy Gates E and E1 from `027-CONTEXT.md` without breaking verified genesis or simulator flows.
Output: a guarded deterministic RNG surface in `z00z_utils`, explicit deterministic trait semantics, and approved opt-in wiring for the known reproducibility domains.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/027-crypto-audit-utils/027-FUSION.md
@.planning/phases/027-crypto-audit-utils/027-CONTEXT.md
@.planning/phases/027-crypto-audit-utils/027-RESEARCH.md
@crates/z00z_utils/Cargo.toml
@crates/z00z_utils/src/rng/traits.rs
@crates/z00z_utils/src/rng/deterministic.rs
@crates/z00z_utils/src/rng/mock.rs
@crates/z00z_utils/src/rng/mod.rs
@crates/z00z_utils/src/rng/test_rng.rs
@crates/z00z_core/Cargo.toml
@crates/z00z_simulator/Cargo.toml
@crates/z00z_crypto/src/types.rs
@crates/z00z_core/bin/assets/assets_generation_cli.rs
@crates/z00z_core/src/genesis/asset_std.rs
@crates/z00z_core/src/genesis/genesis.rs
@crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace approval-sounding deterministic trait semantics and add an explicit guardrail</name>
  <files>crates/z00z_utils/Cargo.toml
crates/z00z_utils/src/rng/traits.rs
crates/z00z_utils/src/rng/deterministic.rs
crates/z00z_utils/src/rng/mock.rs
crates/z00z_utils/src/rng/mod.rs
crates/z00z_utils/src/rng/test_rng.rs
crates/z00z_utils/src/lib.rs</files>
  <behavior>
    - Test 1: The deterministic trait no longer requires or implies the same approval semantics as `SecureRngProvider`.
    - Test 2: Deterministic provider availability is guarded behind an explicit test or genesis capability, a compile-time guard, or both.
    - Test 3: Existing deterministic vectors remain reproducible under the approved opt-in path.
  </behavior>
  <action>Implement Decision "Deterministic RNG Guardrails" from `027-CONTEXT.md`. Remove or replace the `CryptoRng`-bound semantics from the deterministic trait so the public contract cannot be mistaken for blessed unpredictable entropy. Add one explicit availability guardrail in `z00z_utils`, preferably through a dedicated feature plus any compile-time assertion pattern reused from `MockRngProvider`, while keeping deterministic vectors stable. Update docs, exports, and tests to reflect the new approved-domain vocabulary. Do not break the existing secure RNG surface or introduce a new RNG crate.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The deterministic RNG surface is explicitly reproducibility-only, guarded, and no longer approval-sounding by trait contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Opt approved genesis and simulator domains into the new deterministic guardrail</name>
  <files>crates/z00z_core/Cargo.toml
crates/z00z_simulator/Cargo.toml
crates/z00z_crypto/src/types.rs
crates/z00z_core/bin/assets/assets_generation_cli.rs
crates/z00z_core/src/genesis/asset_std.rs
crates/z00z_core/src/genesis/genesis.rs
crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs
crates/z00z_core/tests/genesis/test_reproducibility.rs
crates/z00z_core/tests/genesis/test_genesis.rs
crates/z00z_simulator/tests/test_scenario1_unified_gate.rs</files>
  <behavior>
    - Test 1: Approved genesis code and verified asset-generation tooling still compile and use the deterministic surface explicitly.
    - Test 2: Approved simulator code still compiles and passes the user-preferred release test command.
    - Test 3: No additional production domain is implicitly opted into deterministic RNG availability.
    - Test 4: Genesis reproducibility tests still prove same-seed stability and different-seed divergence under the guardrail.
    - Test 5: After a repository re-scan, no non-allowlisted direct or aliased deterministic-RNG consumer remains unclassified.
  </behavior>
  <action>Wire only the codebase-verified allowlist consumers into the guardrail introduced in Task 1. The approved set for this phase is `crates/z00z_core/src/genesis/genesis.rs`, `crates/z00z_core/src/genesis/asset_std.rs`, `crates/z00z_core/bin/assets/assets_generation_cli.rs`, and `crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_impl.rs`. If a dedicated feature is added, enable it explicitly in `z00z_core` for genesis reproducibility and asset generation, and in `z00z_simulator` for scenario reproducibility. Detection rule for this wave: a `direct or aliased deterministic-RNG consumer` is a first-party production Rust source file that either directly names `DeterministicRngProvider` or `DeterministicRng`, or imports or re-exports those symbols under an alias from `z00z_utils::rng`. At review time, an additional non-allowlisted direct production deterministic surface is already visible in `crates/z00z_crypto/src/types.rs` via `random_deterministic(provider: &impl z00z_utils::rng::DeterministicRng)`; classify that seam in the same pass instead of assuming only genesis and simulator callers exist. Anchor the approved-domain proof in the existing genesis reproducibility suite `crates/z00z_core/tests/genesis/test_reproducibility.rs` and the release-style simulator gate `crates/z00z_simulator/tests/test_scenario1_unified_gate.rs` so same-seed stability, different-seed divergence, and approved simulator reproducibility are executable rather than summary-only claims. Do not treat `stage_3.rs` as a direct deterministic-provider consumer unless repository evidence shows one of those direct or aliased source-level forms, or a guardrail-induced compile failure that requires an explicit local allowlist decision. Before closing the wave, re-scan first-party production Rust sources for those direct names plus aliased `use` or `pub use` forms rooted at `z00z_utils::rng`. Closure rule: every non-allowlisted direct or aliased consumer found by that scan must end in exactly one state: removed or migrated away from deterministic RNG, added to the allowlist by an explicit phase-summary decision with file path and rationale, or recorded as a blocker with file path and reason. Keep the code references narrow so other crates do not inherit deterministic RNG availability by accident. If the guardrail would break approved consumers, roll back to the documented allowlist plus stronger docs only after recording the reason in the phase summary, per the rollback rule in `027-CONTEXT.md`. If a checkpoint commit becomes useful before touching both Cargo manifests, use `/z00z-git-versioning` rather than raw git commands.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release
cargo check -p z00z_core --release --bin assets_generation_cli
cargo test -p z00z_core --test genesis_tests -- --nocapture
cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump
cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>Approved deterministic domains stay green, the deterministic allowlist closes with no unclassified direct or aliased consumer, and no broader production surface gets deterministic RNG access by implication.</done>
</task>

</tasks>

<verification>
Close this plan only after Gates E and E1 from `027-CONTEXT.md` are satisfied: approved domains stay intact, and the deterministic trait surface no longer sounds like a secure entropy approval.
</verification>

<success_criteria>
`z00z_utils` exposes deterministic RNG as an explicitly guarded reproducibility tool, not as a drop-in production entropy source, and the approved consumers, including the verified asset-generation path, continue to work under release-style gates.
</success_criteria>

<output>
After completion, create `.planning/phases/027-crypto-audit-utils/027-04-SUMMARY.md`
</output>