<!-- markdownlint-disable MD007 MD031 MD032 MD033 MD041 MD047 -->
---
phase: 027-crypto-audit-utils
plan: "06"
type: execute
wave: 6
depends_on:
  - 027-05
files_modified:
  - crates/z00z_utils/src/codec/mod.rs
  - crates/z00z_utils/src/codec/test_codec.rs
  - crates/z00z_utils/src/logger/macros.rs
  - crates/z00z_utils/src/lib.rs
  - crates/z00z_utils/tests/test_codec_integration.rs
  - crates/z00z_utils/tests/test_logger_integration.rs
autonomous: true
requirements:
  - PH27-JSON
must_haves:
  truths:
    - The repository has an explicit `z00z_utils` policy for JSON macro and `Value` usage instead of undocumented drift.
    - `z00z_utils` logger macros no longer reach directly into `::serde_json::json!()` as an undocumented exception.
    - The chosen policy preserves current consumer compatibility because the verified `Value` blast radius reaches other crates beyond `z00z_utils` in this phase.
    - The narrow Wave 6 execution boundary is exact: only the owned `z00z_utils` compatibility exports in `codec/mod.rs` and the logger-macro path routed through that surface are modified and verified by this wave.
  artifacts:
    - path: crates/z00z_utils/src/codec/mod.rs
      provides: Explicit JSON boundary policy and sanctioned compatibility surface.
    - path: crates/z00z_utils/src/logger/macros.rs
      provides: Logger macros that route through the sanctioned JSON boundary.
    - path: crates/z00z_utils/tests/test_logger_integration.rs
      provides: Concrete regression seam for macro-to-codec boundary coverage.
  key_links:
    - from: crates/z00z_utils/src/logger/macros.rs
      to: crates/z00z_utils/src/codec/mod.rs
      via: sanctioned JSON construction helper or documented alias
      pattern: json!|Value
    - from: crates/z00z_utils/src/logger/macros.rs
      to: crates/z00z_utils/tests/test_logger_integration.rs
      via: macro regression coverage on the sanctioned JSON boundary
      pattern: log_info!|log_warn!|log_error!|log_debug!|log_trace!
---

<objective>
Record and enforce the explicit JSON boundary policy inside `z00z_utils` without a wide consumer rewrite.

Purpose: satisfy Gate H from `027-CONTEXT.md` by choosing a concrete architecture direction that matches the verified blast radius.
Output: an explicit sanctioned JSON compatibility surface in `z00z_utils`, logger macros routed through that surface, and regression coverage that keeps the boundary decision intentional.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/027-crypto-audit-utils/027-FUSION.md
@.planning/phases/027-crypto-audit-utils/027-CONTEXT.md
@.planning/phases/027-crypto-audit-utils/027-RESEARCH.md
@crates/z00z_utils/src/codec/mod.rs
@crates/z00z_utils/src/logger/macros.rs
@crates/z00z_utils/src/lib.rs
@crates/z00z_utils/src/codec/test_codec.rs
@crates/z00z_utils/tests/test_codec_integration.rs
@crates/z00z_utils/tests/test_logger_integration.rs

<interfaces>
Current public compatibility surface:

```rust
pub use serde_json::{json, Value};
```

Allowed Wave 6 execution boundary:

```rust
pub use serde_json::{json, Value};
```

and logger macros may use only that `z00z_utils`-owned compatibility surface or a helper defined in `crates/z00z_utils/src/codec/mod.rs`.

Verified internal drift point:

```rust
let payload = ::serde_json::json!({ ... });
```

Verified blast-radius constraint for this phase: `Value` and JSON helpers are still consumed outside `z00z_utils`, including live paths in `z00z_core`, `z00z_wallets`, `z00z_simulator`, and `z00z_networks/rpc`, so a wide removal is out of scope for Wave 6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Turn the JSON exception into an explicit `z00z_utils` compatibility policy</name>
  <files>crates/z00z_utils/src/codec/mod.rs
crates/z00z_utils/src/codec/test_codec.rs
crates/z00z_utils/src/lib.rs
crates/z00z_utils/tests/test_codec_integration.rs</files>
  <behavior>
    - Test 1: The crate-level JSON compatibility surface is documented as intentional rather than accidental.
    - Test 2: Existing `Value` or `json!` compatibility exports keep working unless the plan deliberately records a migration.
    - Test 3: Codec tests cover the sanctioned compatibility path so future drift is visible.
  </behavior>
  <action>Use the verified blast radius from `027-RESEARCH.md` to choose the narrow-exception path instead of a wide `Value` removal in this phase. The recorded justification for this wave is that live `Value` consumers extend beyond `z00z_utils` into `z00z_core`, `z00z_wallets`, `z00z_simulator`, and `z00z_networks/rpc`, so wide removal would exceed the approved rollout boundary. Keep the public compatibility alias surface in `codec/mod.rs`, but rewrite its docs and any relevant `lib.rs` notes so it is an explicit repository policy exception owned by `z00z_utils`, not an accidental raw `serde_json` leak. Add or update tests to pin the compatibility contract deliberately. Exact Wave 6 boundary rule: do not expand the execution scope beyond the current `json` and `Value` exports in `codec/mod.rs` plus the logger-macro route that is refactored in Task 2, and do not claim repository-wide `serde_json` elimination in this wave.</action>
  <verify>
    <automated>set -euo pipefail
./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
cargo test -p z00z_utils --release --test test_codec_integration
cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The JSON boundary is explicit and documented as a narrow `z00z_utils` compatibility exception instead of undocumented drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route logger macros through the sanctioned JSON boundary instead of direct `serde_json` calls</name>
  <files>crates/z00z_utils/src/logger/macros.rs
crates/z00z_utils/src/codec/mod.rs
crates/z00z_utils/src/codec/test_codec.rs
crates/z00z_utils/tests/test_codec_integration.rs
crates/z00z_utils/tests/test_logger_integration.rs</files>
  <behavior>
    - Test 1: Logger macros build structured payloads through the sanctioned `z00z_utils` JSON surface.
    - Test 2: No direct `::serde_json::json!()` use remains inside `z00z_utils` logger macros.
    - Test 3: Regression coverage lives in a real logger test seam, not only in codec-only tests or docs.
    - Test 4: The change does not require a cross-workspace consumer rewrite in this phase.
    - Test 5: A final source scan proves no direct `serde_json` JSON-macro route remains in `crates/z00z_utils/src/logger/macros.rs`, including absolute, relative, or directly imported forms.
  </behavior>
  <action>Refactor `crates/z00z_utils/src/logger/macros.rs` so the structured logging macros use the sanctioned JSON boundary chosen in Task 1 rather than direct `::serde_json::json!()` calls. Keep the public macro call shape stable for downstream callers. Exact boundary rule for this task: after the refactor, logger macros may depend only on the owned `z00z_utils` compatibility surface in `crates/z00z_utils/src/codec/mod.rs` or a helper defined there; they must not call `::serde_json::json!()` directly and must not widen the public exception surface beyond `json` and `Value`. Add focused regression coverage in `crates/z00z_utils/tests/test_logger_integration.rs` so the macro path is pinned through a real logger-facing seam rather than only through codec tests. If a checkpoint commit becomes useful before touching the macro export surface, use `/z00z-git-versioning` instead of raw git commands.</action>
  <verify>
    <automated>set -euo pipefail
  ./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh
  cargo test -p z00z_utils --release --test test_logger_integration
  rg '(^|[^[:alnum:]_])(::)?serde_json::json!|use serde_json::json|use serde_json::\{[^}]*json' crates/z00z_utils/src/logger/macros.rs && exit 1 || true
  cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <review>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Continue until at least 2 consecutive runs show no significant issues, and fix every issue or warning before closing the task.</review>
  </verify>
  <done>The internal logger macro path honors the explicit `z00z_utils` JSON policy and no longer depends on undocumented direct `serde_json` drift.</done>
</task>

</tasks>

<verification>
Close this plan only after Gate H from `027-CONTEXT.md` is satisfied: the JSON boundary choice is explicit, documented, and reflected in the live logger macro path.
</verification>

<success_criteria>
`z00z_utils` owns an explicit narrow JSON exception, and its internal structured logging helpers follow that policy instead of bypassing it directly.
</success_criteria>

<output>
After completion, create `.planning/phases/027-crypto-audit-utils/027-06-SUMMARY.md`
</output>