---
phase: 046-wallet-addons
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs
  - crates/z00z_simulator/src/scenario_1/storage_view.rs
autonomous: true
requirements:
  - 046-02
must_haves:
  truths:
    - "Stage 13 uses one canonical wallet.tx flow for build, broadcast, cancel, reconcile, and history reporting."
    - "The report layer preserves distinct `prev_root`, `state_root`, and `flat_root` meanings."
    - "Stage 13 storage helpers reuse existing storage-view roots and do not duplicate root derivation logic."
    - "Stage 13 drives the logged wallet RPC transport through build, pending, cancel, verify, broadcast, reconcile, details, history, export, and import without simulator-only tx-history writes."
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs
      provides: Stage 13 wallet.tx module wiring
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs
      provides: Canonical live wallet.tx lifecycle flow
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs
      provides: Stage 13 report and history summary emission
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs
      provides: Root binding and storage bridge helpers
    - path: crates/z00z_simulator/src/scenario_1/storage_view.rs
      provides: Reused storage-view snapshot helpers
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs
      to: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs
      via: logged wallet RPC transport
      pattern: 'build_logged_transport_with_wallet|unlock_wallet|get_receiver_card|build_transaction'
    - from: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs
      to: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs
      via: tx lifecycle root binding
      pattern: 'claimed asset|prev_root|flat_root'
    - from: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs
      to: crates/z00z_simulator/src/scenario_1/storage_view.rs
      via: storage-view reuse
      pattern: 'export_post_tx_view|export_pre_tx_view'
    - from: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs
      to: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/{flow,report,storage}.rs
      via: module wiring
      pattern: 'mod flow|mod report|mod storage'

user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Implement the live stage 13 wallet.tx lifecycle so simulator evidence follows the production wallet boundary instead of approximating it with a separate tx lane.

Purpose: make wallet.tx build/broadcast/cancel/reconcile and the associated report/store roots share one canonical stage 13 flow.
Output: stage 13 flow, report, and storage helpers wired through the module root.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/046-wallet-addons/046-CONTEXT.md

<interfaces>
From `crates/z00z_simulator/src/scenario_1/storage_view.rs`:

```rust
pub(crate) fn export_pre_tx_view(out: &Path, snapshot_id: PrepSnapshotId, snapshot: &PrepSnapshot) -> Result<PathBuf, String>;
pub(crate) fn export_post_tx_view(out: &Path, snapshot_id: PrepSnapshotId, snapshot: &PrepSnapshot, exec: &CheckpointExecInput, draft: &CheckpointDraft) -> Result<PathBuf, String>;
pub(crate) fn export_post_tx_final_view(out: &Path, artifact: &CheckpointArtifact, link: &CheckpointLink, audit: &CheckpointAudit) -> Result<PathBuf, String>;
```

From `.planning/phases/046-wallet-addons/046-wallet-addon-spec.md`:

```text
Stage 13 must preserve distinct prev_root, state_root, and flat_root semantics.
```
</interfaces>
</execution_context>

<context>
@.planning/phases/046-wallet-addons/046-wallet-addon-spec.md
@.planning/phases/046-wallet-addons/046-storage-explain.md
@.planning/phases/046-wallet-addons/046-wallet-misses.md

The storage bridge should reuse the existing storage-view helpers and keep the stage report faithful to the canonical wallet.tx lifecycle rather than inventing a new history model.
Do not couple receiver import continuation or lifecycle proof to full Phase 044 closure; Phase 046 must stand on the current canonical wallet.tx surfaces.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the canonical stage 13 wallet.tx lifecycle flow</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs</files>
  <action>Build the live wallet.tx lifecycle around the scenario 13 stage so `wallet.session.unlock_wallet`, `wallet.key.get_receiver_card`, `wallet.tx.build_transaction`, `wallet.tx.list_pending_transactions`, `wallet.tx.cancel_transaction`, `wallet.tx.verify_transaction_package`, `wallet.tx.broadcast_transaction`, `wallet.tx.reconcile_transaction`, `wallet.tx.get_transaction_details`, and `wallet.tx.get_transaction_history` all use one logged transport flow. The first build must reserve inputs through pending history, cancellation must expose live `cancelled` status, the second build must succeed after cancel, and broadcast-plus-reconcile must remove spent claimed inputs, append wallet-owned outputs, and persist the changed claimed asset set through the canonical wallet boundary. Keep claimed-asset selection and package validation aligned with the production wallet boundary; do not add a second lane that bypasses the canonical wallet.tx entrypoints.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and fix the flow before any broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Stage 13 has one wallet.tx lifecycle flow that can drive build, broadcast, cancel, and reconcile without falling back to a separate simulation-only lane.</done>
</task>

<task type="auto">
  <name>Task 2: Bind stage 13 reporting and storage to the canonical roots</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs, crates/z00z_simulator/src/scenario_1/storage_view.rs</files>
  <action>Implement the report and storage bridge helpers so the stage keeps `prev_root`, `state_root`, and `flat_root` separate, widens only the narrow `sync_snapshot_store(...)` and `sync_post_store(...)` helpers needed for reuse, and proves the storage contract through `CheckpointPackageProofVerifier` in the spec order: `verify_pkg_contract(...)`, `expected_prev_root(...)`, `from_stage7(...)`, replayed pre-store root, then replayed post-store root. Because the current live `storage_view.rs` export surface does not yet expose `flat_root`, the implementation must either reuse an already persisted metadata/artifact field when one is reachable through the existing helper chain or add one explicit accessor/export extension on the canonical storage/report path; it must not read REDB internals ad hoc and must not duplicate root derivation math inside the stage layer.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the storage bridge before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Stage 13 reports and storage exports preserve the canonical root vocabulary and reuse the existing storage-view helpers instead of reimplementing them.</done>
</task>

<task type="auto">
  <name>Task 3: Carry wallet.tx status markers through the stage 13 history summaries</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs</files>
  <action>Make the stage history output preserve the canonical wallet.tx RPC status set of pending, cancelled, and confirmed, while reporting imported/exported lifecycle evidence as separate persisted markers rather than as extra RPC status enums. The history summary must also preserve the live cursor/filter/sort traversal contract of `wallet.tx.get_transaction_history`, so later verification can distinguish a successful lifecycle from a partially applied one without narrowing the public history surface. Imported/exported markers must remain provable through canonical JSONL row kinds plus persisted `record_imported(...)` and `record_exported(...)` evidence instead of simulator-only aliases or invented DTO status values.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the status summary before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>The stage 13 history summary carries the wallet.tx status markers needed for later tamper and regression tests.</done>
</task>

<task type="auto">
  <name>Task 4: Prove receiver export/import continuation on the same canonical tx lane</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs</files>
  <action>Extend the stage 13 lifecycle so sender export and receiver import use `wallet.tx.export_transaction` and `wallet.tx.import_transaction`, the receiver detects owned outputs through the live import path, receiver pending history surfaces the imported tx id, and receiver-side `wallet.tx.broadcast_transaction` plus `wallet.tx.reconcile_transaction` continue the same tx id without inventing a receiver-only lane. Receiver-side broadcast must prove the live `TxSubmitterRole::Receiver` path. If raw portable bytes are required for broadcast, derive them from the same live export result or add one explicit canonical non-test helper on the runtime export/broadcast path; do not depend on test-only helpers and do not change the RPC contract.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the import continuation before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Stage 13 proves sender export and receiver import/broadcast/reconcile on the same canonical wallet.tx lane with receiver-owned-output evidence preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wallet.tx flow → storage bridge | Tampered package or root math drift must not reach persisted stage output. |
| report layer → verification tooling | The report must keep `prev_root`, `state_root`, and `flat_root` distinguishable for downstream checks. |
| storage-view reuse → stage helper layer | Reused helpers must stay narrow so stage 13 does not gain a second storage implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-046-04 | T | `flow.rs` / `storage.rs` | mitigate | Keep lifecycle validation and root binding in one canonical path so tampered tx packages cannot be accepted through a side channel. |
| T-046-05 | I | `report.rs` | mitigate | Preserve separate root fields and history statuses so downstream diagnostics do not collapse distinct states into one blob. |
| T-046-06 | R | `storage_view.rs` | mitigate | Reuse the existing export helpers and retain explicit storage summaries so the stage output can be audited later. |
</threat_model>

<verification>
The plan is correct when the stage 13 wallet.tx flow can emit canonical report/store evidence and the storage bridge still reuses the simulator’s existing export surface.
</verification>

<success_criteria>
Stage 13 wallet.tx evidence is produced through one canonical logged RPC flow, receiver import continuation stays on that same lane, and the output remains auditable because the root fields and history markers stay distinct.
</success_criteria>

<output>
After completion, create `.planning/phases/046-wallet-addons/046-02-SUMMARY.md`.
</output>