---
phase: 046-wallet-addons
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_simulator/src/config.rs
  - crates/z00z_simulator/src/scenario_1/scenario_config.yaml
  - crates/z00z_simulator/src/scenario_1/scenario_design.yaml
  - crates/z00z_simulator/src/scenario_1/runner_contract_table.in
  - crates/z00z_simulator/src/scenario_1/runner_contract.rs
  - crates/z00z_simulator/src/scenario_1/runner_verify.rs
  - crates/z00z_simulator/src/scenario_1/mod.rs
  - crates/z00z_simulator/src/scenario_1/runner.rs
  - crates/z00z_simulator/src/scenario_1/stage_13.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tamper.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs
autonomous: true
requirements:
  - 046-01
must_haves:
  truths:
    - "Scenario 1 exposes a canonical stage 13 wallet.tx lifecycle entry."
    - "The runner contract, design, and verification dispatch agree on 13 stages."
    - "Stage 13 is reachable from the scenario runner through the public facade, not via ad hoc helper code."
    - "`ScenarioCfg` exposes a typed `stage13_wallet_tx_rpc_lifecycle` config boundary and the contract carries exact `S13-1` through `S13-15` step ids."
  artifacts:
    - path: crates/z00z_simulator/src/config.rs
      provides: `Stage13WalletTxCfg` and `ScenarioCfg` stage 13 field
    - path: crates/z00z_simulator/src/scenario_1/scenario_config.yaml
      provides: Stage 13 config entry and label
    - path: crates/z00z_simulator/src/scenario_1/scenario_design.yaml
      provides: Scenario 1 design surface with a 13th stage
    - path: crates/z00z_simulator/src/scenario_1/runner_contract_table.in
      provides: Canonical stage contract table entry for stage 13
    - path: crates/z00z_simulator/src/scenario_1/runner_contract.rs
      provides: Design validation against the 13-stage canonical contract
    - path: crates/z00z_simulator/src/scenario_1/runner_verify.rs
      provides: Stage coverage and post-condition verification dispatch for stage 13
    - path: crates/z00z_simulator/src/scenario_1/runner.rs
      provides: Runtime routing into the new stage
    - path: crates/z00z_simulator/src/scenario_1/stage_13.rs
      provides: Thin facade for the new stage entry
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs
      provides: Stage 13 wallet.tx module root
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs
      provides: Stage 13 lifecycle scaffold file
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs
      provides: Stage 13 report scaffold file
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs
      provides: Stage 13 storage scaffold file
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs
      provides: Stage 13 scan scaffold file
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tamper.rs
      provides: Stage 13 tamper scaffold file
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs
      provides: Stage 13 regression scaffold file
  key_links:
    - from: crates/z00z_simulator/src/config.rs
      to: crates/z00z_simulator/src/scenario_1/scenario_config.yaml
      via: stage 13 typed config
      pattern: 'stage13_wallet_tx_rpc_lifecycle|Stage13WalletTxCfg'
    - from: crates/z00z_simulator/src/scenario_1/runner_contract_table.in
      to: crates/z00z_simulator/src/scenario_1/runner_contract.rs
      via: canonical stage table include
      pattern: 'CANONICAL_STAGE_SPECS.*13|stage 13'
    - from: crates/z00z_simulator/src/scenario_1/runner.rs
      to: crates/z00z_simulator/src/scenario_1/stage_13.rs
      via: stage dispatcher
      pattern: 'stage_13|stage == 13'
    - from: crates/z00z_simulator/src/scenario_1/runner_verify.rs
      to: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs
      via: stage coverage dispatch
      pattern: 'unsupported stage for coverage|stage 13'

user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Make Scenario 1 recognize stage 13 as a first-class wallet.tx lifecycle stage so later plans can implement the live wallet transaction flow without inventing a parallel simulator path.

Purpose: establish the canonical stage entry and contract before the wallet.tx flow work starts.
Output: updated Scenario 1 contract, runner wiring, and the stage 13 module scaffold.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/046-wallet-addons/046-CONTEXT.md

<interfaces>
From `crates/z00z_simulator/src/scenario_1/runner_contract.rs`:

```rust
const CANONICAL_STAGE_SPECS: [CanonicalStageSpec; 12] = include!(concat!(
    env!("CARGO_MANIFEST_DIR"),
    "/src/scenario_1/runner_contract_table.in"
));
```

From `crates/z00z_simulator/src/scenario_1/runner.rs`:

```rust
pub(super) fn run_scenario_1(config: &Path, design: &Path) -> Result<(), Scenario1Err>;
```

From `crates/z00z_simulator/src/scenario_1/runner_verify.rs`:

```rust
pub(super) fn check_stage(ctx: &SimContext, stage: &DesignStage) -> StageResult;
```

From `.planning/phases/046-wallet-addons/046-wallet-addon-spec.md`:

```text
Scenario 1 stage 13 becomes the wallet.tx lifecycle lane.
```
</interfaces>
</execution_context>

<context>
@.planning/phases/046-wallet-addons/046-wallet-addon-spec.md
@.planning/phases/046-wallet-addons/046-storage-explain.md
@.planning/phases/046-wallet-addons/046-wallet-misses.md

The stage contract must stay aligned with the locked decisions in the Phase 046 spec: live wallet.tx is canonical, claimed assets are the ownership plane, and the simulator only mirrors the production boundary.
</context>

## Exact Stage 13 Contract Steps

- `S13-1`: Prepare RPC lifecycle dirs and logged transport.
- `S13-2`: Unlock sender and receiver wallets.
- `S13-3`: Build tx through `wallet.tx.build_transaction`.
- `S13-4`: Verify pending reservation with `wallet.tx.list_pending_transactions`.
- `S13-5`: Cancel first tx and verify cancellation status.
- `S13-6`: Rebuild tx and verify package with `wallet.tx.verify_transaction_package`.
- `S13-7`: Broadcast tx with `wallet.tx.broadcast_transaction`.
- `S13-8`: Reconcile tx with `wallet.tx.reconcile_transaction`.
- `S13-9`: Verify details/history through `wallet.tx.get_transaction_details` and `wallet.tx.get_transaction_history`.
- `S13-10`: Export/import portable tx and assert receiver owned-output behavior.
- `S13-11`: Run tamper fail-closed checks.
- `S13-12`: Run backup restore plus tx-history checks.
- `S13-13`: Run TOFU/payment request checks.
- `S13-14`: Run session hardening checks.
- `S13-15`: Write lifecycle report and logs.

## Exact Stage 13 Config Schema

- `enabled: bool`
- `sender_actor: String`
- `receiver_actor: String`
- `change_actor: String`
- `amount: u64`
- `asset_id: Option<String>`
- `memo: String`
- `tx_exports_dir: String`
- `report_file: String`
- `logger_file: String`
- `require_cancel_release: bool`
- `require_import_owned_output: bool`
- `require_restore_history: bool`
- `require_tofu_negative: bool`
- `require_session_negative: bool`

<tasks>

<task type="auto">
  <name>Task 1: Extend the canonical Scenario 1 contract to stage 13</name>
  <files>crates/z00z_simulator/src/config.rs, crates/z00z_simulator/src/scenario_1/scenario_config.yaml, crates/z00z_simulator/src/scenario_1/scenario_design.yaml, crates/z00z_simulator/src/scenario_1/runner_contract_table.in, crates/z00z_simulator/src/scenario_1/runner_contract.rs</files>
  <action>Add the new stage 13 contract entry, keep the stage count consistent at 13 everywhere, introduce a typed `Stage13WalletTxCfg` plus optional `ScenarioCfg.stage13_wallet_tx_rpc_lifecycle` field, and update the design surface so the scenario stops advertising a 12-stage world. The stage table must carry the exact `S13-1` through `S13-15` step ids from the spec. Do not add a second wallet flow or change the existing stage ordering; the stage 13 entry must extend the canonical Scenario 1 contract, not fork it.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the stage contract before any broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>The canonical stage table and design both acknowledge a 13th stage and the contract count no longer disagrees with the scenario design.</done>
</task>

<task type="auto">
  <name>Task 2: Wire runner dispatch and verification for stage 13</name>
  <files>crates/z00z_simulator/src/scenario_1/runner_verify.rs, crates/z00z_simulator/src/scenario_1/runner.rs, crates/z00z_simulator/src/scenario_1/mod.rs, crates/z00z_simulator/src/scenario_1/stage_13.rs</files>
  <action>Route Scenario 1 into the new stage 13 facade and make the verification dispatcher aware of the new stage instead of failing it as unsupported. Keep the existing stage 1-12 coverage behavior intact; the change here is additive and must preserve fail-closed validation for all pre-existing stages.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, fix the dispatcher or facade wiring before any broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Scenario 1 can dispatch and verify stage 13 without regressions in the existing stage coverage checks.</done>
</task>

<task type="auto">
  <name>Task 3: Create the stage 13 wallet.tx module scaffold</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/mod.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/flow.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/report.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/storage.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tamper.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs</files>
  <action>Create the stage 13 module root and the full `flow`, `report`, `storage`, `scan`, `tamper`, and `tests` file skeleton under `stage_13_wallet_tx/` so later plans can fill the wallet.tx lifecycle, storage bridge, and scan/report helpers without reopening the public module layout. Keep the facades thin and avoid putting business logic into the scaffold step.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the module tree before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>The stage 13 wallet.tx module tree exists, is imported from the Scenario 1 facade, and downstream plans can add implementation files under it without changing the public stage contract again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design contract → runtime dispatcher | A stale contract can route the wrong stage or hide a missing stage. |
| simulator verification → stage coverage logs | Verification must fail closed when a stage is unsupported or uncovered. |
| public stage facade → internal helper tree | Facades must not leak direct internal-only helpers into the public surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-046-01 | T | `runner_contract.rs` / `runner_verify.rs` | mitigate | Keep the 13-stage contract and coverage dispatcher in sync so unsupported stages fail closed instead of being silently skipped. |
| T-046-02 | I | `stage_13.rs` | mitigate | Keep the facade thin and avoid embedding wallet secrets or serialized sensitive state in the stage wrapper. |
| T-046-03 | R | stage contract files | mitigate | Preserve explicit stage entries and deterministic labels so later verification can prove what was implemented. |
</threat_model>

<verification>
The phase remains on track when Scenario 1 recognizes stage 13, the verification dispatcher accepts the new stage path, and the scaffolding compiles without forcing a parallel wallet flow.
</verification>

<success_criteria>
The scenario contract, runtime dispatch, and verification surface all agree that stage 13 exists and belongs to the canonical Scenario 1 flow.
</success_criteria>

<output>
After completion, create `.planning/phases/046-wallet-addons/046-01-SUMMARY.md`.
</output>