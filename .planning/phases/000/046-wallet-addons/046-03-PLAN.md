---
phase: 046-wallet-addons
plan: 3
type: execute
wave: 3
depends_on:
  - 2
files_modified:
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
  - crates/z00z_wallets/src/services/wallet_service_tests.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs
autonomous: true
requirements:
  - 046-03
  - 046-04
must_haves:
  truths:
    - "WalletPlusHistory restore preserves both claimed assets and canonical tx-history JSONL."
    - "Stage 13 scan state resumes from persisted wallet-side authority instead of a separate scanner lane."
    - "Wrong-password backup restore rejects without mutating the existing wallet state."
    - "The encrypted `.wlt` snapshot remains the canonical claimed-asset persistence plane, while `ScanStorageImpl` persists separate local scan-state only."
  artifacts:
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
      provides: Backup and restore flow hooks
    - path: crates/z00z_wallets/src/services/wallet_service_tests.rs
      provides: Restore and scan regression coverage
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs
      provides: Stage 13 scan-state and claim-persistence helper
  key_links:
    - from: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs
      to: crates/z00z_wallets/src/services/wallet_service_tests.rs
      via: restore parity checks
      pattern: 'WalletPlusHistory|wrong password|claimed assets'
    - from: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs
      to: crates/z00z_wallets/src/services/wallet_service_tests.rs
      via: wallet-side scan resume coverage
      pattern: 'recv_range|ScanStatePayload|PersistClaim'
    - from: crates/z00z_wallets/src/services/wallet_service_tests.rs
      to: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
      via: live scan cursor and claim persistence
      pattern: 'read_scan_state|upsert_scan_state|ScanStorageImpl|test_recv_range_restart'

user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Prove that wallet backup/restore and scan resume stay on the wallet-owned persistence boundary and continue to carry claimed assets plus history through a restart.

Purpose: close the persistence gap without inventing a second restore or scan authority.
Output: backup/restore wiring and tests that preserve claimed assets, history JSONL, and wallet-side scan state.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/046-wallet-addons/046-CONTEXT.md

<interfaces>
From `crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs`:

```text
WalletPlusHistory restore must rebuild both .wlt claimed assets and transaction history.
```

From `.planning/phases/046-wallet-addons/046-wallet-addon-spec.md`:

```text
Receive resume authority stays wallet-side through recv_range(...), ScanStatePayload, and PersistClaim.
```
</interfaces>
</execution_context>

<context>
@.planning/phases/046-wallet-addons/046-wallet-addon-spec.md
@.planning/phases/046-wallet-addons/046-storage-explain.md
@.planning/phases/046-wallet-addons/046-wallet-misses.md

Restore and scan replay must preserve the canonical claim set and restart cursor; they should not rewrite the wallet into a new scanner model.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make WalletPlusHistory restore preserve the wallet-owned claim set</name>
  <files>crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup.rs</files>
  <action>Wire the backup restore path so `restore_backup_with_mode(..., WalletPlusHistory)` reconstructs both the `.wlt` claimed assets and the canonical transaction history JSONL, preserves restored wallet-id semantics, proves the restored `wallet_history_jsonl_path(...)` exists, enforces all-or-nothing restore semantics, and keeps the encrypted snapshot object inside `.wlt` as the canonical claimed-asset persistence plane. Wrong-password, snapshot-decode, tx-history-decode, JSONL-replay, and staged-write failures must all reject before any live wallet mutation or durable commit, must preserve the public `WalletError::InvalidPassword` mapping where applicable, and must discard staged restore outputs instead of leaving half-restored `.wlt` or history artifacts. Compare decoded source and restored JSONL histories through the live decode helper, and do not add a new wallet asset store.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the restore path before any broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Backup restore can replay WalletPlusHistory, and any restore failure path leaves the existing wallet state unchanged with no partially committed `.wlt` or history sidecar.</done>
</task>

<task type="auto">
  <name>Task 2: Add wallet-side scan resume and claim persistence coverage</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/scan.rs</files>
  <action>Implement the stage 13 scan helper so persisted scan state resumes through the wallet-side `recv_range(...)` authority, `read_scan_state(...)` and `upsert_scan_state(...)` remain the live resume-cursor path, and every discovered hit still flows through `PersistClaim`. Keep `ScanStorageImpl` described as separate local scan-state persistence only, and if remote aggregator or JMT data is referenced, keep it as a read-adapter input rather than a new ownership detector.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the scan helper before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Stage 13 can prove wallet-side scan resumption and claim persistence without introducing a second scan authority.</done>
</task>

<task type="auto">
  <name>Task 3: Extend wallet service regression tests for restore and restart parity</name>
  <files>crates/z00z_wallets/src/services/wallet_service_tests.rs</files>
  <action>Add regression tests that prove the restored wallet retains claimed assets and tx history, that a restart resumes scanning from the persisted cursor rather than re-scanning from scratch, and that the existing `test_recv_range_restart` remains the canonical proof for wallet-side receive restart. Keep the tests aligned with the canonical wallet-owned state so they fail if restore or resume leaks into a new persistence model.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the regression tests before broader validation.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Wallet service tests prove the backup/restore and scan-restart path preserves history and claim ownership across a restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup archive → live wallet state | A bad restore must never mutate the live wallet before password and payload checks pass. |
| scan cursor → claim persistence | Resume logic must not skip or duplicate persisted claims. |
| persisted history → regression tests | Tests must observe the canonical JSONL history, not a synthetic secondary log. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-046-07 | T | `wallet_service_actions_backup.rs` | mitigate | Validate restore credentials and wallet state before replaying history or claim assets. |
| T-046-08 | D | `scan.rs` | mitigate | Keep scan resume and claim persistence tied to persisted cursor state so rescan loops and missed claims do not arise. |
| T-046-09 | R | `wallet_service_tests.rs` | mitigate | Assert the observable restart behavior so the phase cannot later claim a restore path that the tests do not prove. |
</threat_model>

<verification>
The plan is correct when restore parity and scan resume are both covered by wallet-service tests and the stage 13 helper follows the wallet-side persistence boundary.
</verification>

<success_criteria>
Backup restore and scan resume both preserve claimed ownership and transaction history across a restart without introducing a second persistence plane.
</success_criteria>

<output>
After completion, create `.planning/phases/046-wallet-addons/046-03-SUMMARY.md`.
</output>