---
phase: 046-wallet-addons
plan: 6
type: execute
wave: 5
depends_on:
  - 2
  - 3
  - 4
  - 5
files_modified:
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tamper.rs
  - crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_impl/tests/mod.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/test_tx_pending_body.rs
  - crates/z00z_wallets/src/services/wallet_service_tests.rs
autonomous: true
status: paused_pending_rewrite
requirements:
  - 046-08
  - 046-09
must_haves:
  truths:
    - "Tamper and mismatch cases fail closed and leave claimed assets unchanged."
    - "History markers cover pending, cancelled, confirmed, imported, and exported wallet.tx evidence."
    - "The exact release simulator and wallet gates prove the stage 13 path with the expected wallet_debug_dump flags."
    - "The focused wallet, wallet-service, and simulator test inventory stays explicit so Phase 046 cannot close with missing regression categories."
  artifacts:
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tamper.rs
      provides: Stage 13 package and portable-tx tamper helper
    - path: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs
      provides: Stage 13 tamper and history regression coverage
    - path: crates/z00z_wallets/src/adapters/rpc/methods/tx_impl/tests/mod.rs
      provides: Shared wallet.tx RPC test fixtures
    - path: crates/z00z_wallets/src/adapters/rpc/methods/test_tx_pending_body.rs
      provides: Pending-body and reconcile regression checks
    - path: crates/z00z_wallets/src/services/wallet_service_tests.rs
      provides: Backup/restore and replay service-level regression checks
  key_links:
    - from: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs
      to: crates/z00z_wallets/src/adapters/rpc/methods/tx_impl/tests/mod.rs
      via: shared wallet.tx fixtures
      pattern: 'tamper|history|claim'
    - from: crates/z00z_wallets/src/adapters/rpc/methods/test_tx_pending_body.rs
      to: crates/z00z_wallets/src/services/wallet_service_tests.rs
      via: restore and reconcile parity
      pattern: 'pending|cancelled|confirmed|imported|exported'
    - from: crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs
      to: Scenario 1 release smoke commands
      via: release validation gate
      pattern: 'cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump'

user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Prove the Phase 046 wallet.tx story with tamper-closed tests and the exact release smoke gates the simulator expects.

Purpose: turn the earlier implementation work into hard evidence that the stage 13 flow, history markers, and restore/replay paths behave as specified.
Output: regression tests plus the exact release commands to prove the phase end to end.
</objective>

> [!IMPORTANT]
> This plan is paused pending rewrite. Do not execute `046-06` as written until the wallet `.wlt` redesign direction is reconciled with the current regression inventory and any redundant coverage already landed in `046-04`. The rewrite must preserve the fail-closed tamper and release-smoke intent, but avoid baking in obsolete Snapshot/claimed-assets assumptions.

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/046-wallet-addons/046-CONTEXT.md

<interfaces>
From `crates/z00z_wallets/src/adapters/rpc/methods/tx_impl/tests/mod.rs`:

```text
seed_spendable_stealth_coin seeds enough wallet inputs to exercise repeated-send and reconcile paths.
```

From `crates/z00z_wallets/src/adapters/rpc/methods/test_tx_pending_body.rs`:

```text
The pending-body fixture proves wallet.tx pending, cancel, and reconcile behavior.
```
</interfaces>
</execution_context>

<context>
@.planning/phases/046-wallet-addons/046-wallet-addon-spec.md
@.planning/phases/046-wallet-addons/046-storage-explain.md
@.planning/phases/046-wallet-addons/046-wallet-misses.md

These tests should prove the behavior already planned by the earlier plans, not create a second interpretation of wallet.tx history.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stage 13 tamper-closed and history-marker regression tests</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tamper.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx_impl/tests/mod.rs</files>
  <action>Add regressions that prove malformed tx packages, root mismatches, or history tampering fail closed and leave claimed assets unchanged. Cover `tx_id` mismatch, `tx_hash` mismatch, wrong `chain_id`, bad `verified_block_height == 0`, bad `checkpoint_hex` or `confirmed_root_hex`, and wrong spent or created asset ids. Every negative case must capture claimed assets before and after, assert an error result, and, when the storage proof helper is involved, keep the reported pre/post root pair unchanged and emit no fresh post-commit root artifact. Include the wallet.tx history markers needed to distinguish pending, cancelled, and confirmed paths so the tests fail if the stage starts accepting a blurred status model.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the tests before broader checks.
2. Then run `cargo test -p z00z_wallets tx_impl --features test-fast`.
3. Then run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump stage13`.
4. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Stage 13 tests prove tamper cases fail closed and history markers stay precise.</done>
</task>

<task type="auto">
  <name>Task 2: Extend wallet replay tests for restore, pending-body, and imported/exported evidence</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/test_tx_pending_body.rs, crates/z00z_wallets/src/services/wallet_service_tests.rs, crates/z00z_simulator/src/scenario_1/stage_13_wallet_tx/tests.rs</files>
  <action>Extend the wallet-side regression coverage so WalletPlusHistory restore, pending-body reconciliation, imported/exported history evidence, receiver-owned-output import detection, and the existing `test_recv_range_restart` proof all converge on the same canonical wallet.tx path. The one-run history assertions must require one `pending` row from the first build, one `cancelled` row from the first cancellation, one `pending` row from the second build, one `confirmed` row after broadcast/reconcile, one imported marker or persisted `imported == true` record on receiver import, and one exported marker through `record_exported(...)`. The regressions must also prove the live `wallet.tx.get_transaction_history` cursor, filter, and sort traversal contract rather than only status presence. Keep the tests aligned with the live service boundary and the exact stage 13 release smoke expectations, and preserve the existing Stage 4 and Stage 6 package-byte, history-path, and tamper fail-closed checks.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and repair the replay tests before broader checks.
2. Then run `cargo test -p z00z_wallets wallet_service_tests --features test-fast`.
3. Then run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump stage13`.
4. Then run `cargo run --release -p z00z_simulator --bin scenario_1 --features wallet_debug_dump`.
5. Then run `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump`.
6. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>Wallet-side replay tests and the exact Scenario 1 release smoke commands prove the phase 13 evidence path end to end.</done>
</task>

</tasks>

## Required Regression Inventory

- `test_tx_build_reserves_then_cancel_releases`
- `test_tx_reconcile_updates_claimed_assets`
- `test_tx_reconcile_rejects_bad_evidence_without_asset_mutation`
- `test_tx_import_rejects_wrong_chain_without_history_mutation`
- `test_tx_history_filters_pending_cancelled_confirmed`
- `test_tx_history_cursor_filter_sort_roundtrip`
- `test_tx_export_import_detects_receiver_owned_outputs`
- `test_claimed_assets_restore_from_wlt_snapshot`
- `test_claimed_assets_checksum_tamper_rejected`
- `test_wallet_plus_history_restore_keeps_tx_jsonl`
- `test_restore_wrong_password_rejects_without_wallet_mutation`
- `test_stage13_storage_contract_matches_prev_root_and_post_store`
- `test_stage13_runs_wallet_tx_rpc_lifecycle`
- `test_stage13_receiver_import_continuation_uses_receiver_path`
- `test_stage13_rejects_tampered_tx_without_claimed_mutation`
- `test_stage13_backup_restore_compares_claimed_assets_and_history`
- `test_stage13_payment_request_negative_paths`
- `test_stage13_session_hardening_negative_paths`
- `test_stage13_rotate_master_key_auth_rate_limit_and_redaction`
- `test_stage13_history_covers_statuses_and_import_export_markers`

## Final Closeout Gate

```bash
cargo fmt
cargo clippy --all-targets --all-features
cargo test --all
```

If public Rust API docs change:

```bash
cargo doc --no-deps
```

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tamper test fixture → wallet.tx accept path | Regression fixtures must prove the flow fails closed when the package or root is wrong. |
| wallet replay tests → canonical history model | Tests must observe the same history vocabulary as the implementation, not a simplified alias. |
| release smoke command → final evidence claim | The phase should only close after the exact release commands prove the scenario path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-046-16 | T | `tests.rs` | mitigate | Assert fail-closed behavior on malformed or mismatched tx evidence. |
| T-046-17 | I | `test_tx_pending_body.rs` | mitigate | Keep the pending/cancel/reconcile markers precise so the history model cannot drift. |
| T-046-18 | R | release smoke commands | mitigate | Run the exact release commands and keep their output aligned with the planned stage 13 boundary. |
</threat_model>

<verification>
The plan is correct when the regression tests and the exact release smoke commands both prove the canonical wallet.tx history model without gaps.
</verification>

<success_criteria>
Phase 046 closes only when tamper-closed tests pass and the requested release simulator and wallet commands prove the stage 13 story end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/046-wallet-addons/046-06-SUMMARY.md`.
</output>
