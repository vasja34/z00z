---
phase: 046-wallet-addons
plan: 5
type: execute
wave: 4
depends_on:
  - 3
  - 4
files_modified:
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_rpc.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs
  - crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup_rpc.rs
  - crates/z00z_wallets/src/chain/scan_engine_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs
autonomous: true
requirements:
  - 046-07
must_haves:
  truths:
    - "wallet.asset methods are described as compatibility or UX surfaces, not as ledger-authority paths."
    - "receive, reachability, and scanner comments use exact wallet-side authority wording."
    - "No stale stub, placeholder, Phase 1, or residue wording remains in the touched wallet RPC surfaces."
    - "Claim-auth wording stays on the live `ClaimSourceProof` / `verify_claim_authority(...)` / `claim_auth_pk()` boundary and not on JWT language."
  artifacts:
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_rpc.rs
      provides: rotate-master-key compatibility wording target
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs
      provides: Wallet.asset comment and wording cleanup target
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
      provides: Receive authority wording cleanup target
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs
      provides: Claim mutation wording cleanup target
    - path: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup_rpc.rs
      provides: Backup RPC wording cleanup target
    - path: crates/z00z_wallets/src/chain/scan_engine_impl.rs
      provides: Scanner seam wording cleanup target
    - path: crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs
      provides: Asset RPC label cleanup target
    - path: crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs
      provides: Backup RPC label cleanup target
  key_links:
    - from: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs
      to: crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs
      via: compatibility wording consistency
      pattern: 'wallet.asset|compatibility|UX'
    - from: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs
      to: crates/z00z_wallets/src/chain/scan_engine_impl.rs
      via: scanner authority wording
      pattern: 'recv_range|StealthOutputScanner|ScanEngineImpl'
    - from: crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup_rpc.rs
      to: crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs
      via: backup wording consistency
      pattern: 'stub|placeholder|Phase 1|residue'

user_setup: []
---

<!-- markdownlint-disable-file MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Remove stale wording from the wallet RPC and scanner surfaces so the comments and labels match the actual wallet-side authority boundaries established by the earlier plans.

Purpose: keep docs and labels from overstating compatibility helpers as canonical ledger authority.
Output: cleaned wording on the wallet.asset, receive, reachability, backup, and scanner surfaces.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/046-wallet-addons/046-CONTEXT.md
</execution_context>

<context>
@.planning/phases/046-wallet-addons/046-wallet-addon-spec.md
@.planning/phases/046-wallet-addons/046-storage-explain.md
@.planning/phases/046-wallet-addons/046-wallet-misses.md

This is a wording-only cleanup pass. Do not widen the scope to new logic or new persistence behavior.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stale wallet.asset and receive/reachability comments</name>
  <files>crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_assets.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_receive.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_reachability.rs</files>
  <action>Replace wording that treats these surfaces as stub, placeholder, or Phase 1 implementation notes with the exact compatibility or wallet-side authority language used by the live boundary. The pass must explicitly clarify the asset list/merge/metadata/receive/send/split/staking/swap/unstaking helper family, including `wallet.asset.list_assets`, `wallet.asset.get_asset_balance`, `wallet.asset.import_asset`, `wallet.asset.receive_asset`, and `wallet.asset.send_asset`; it must say that `scan_asset_report(...)` is compatibility-only, `recv_range(...)` is the canonical receive authority, remote aggregator or JMT data stays read-adapter only, and claim-auth wording uses the live `ClaimSourceProof` plus `verify_claim_authority(...)` / `claim_auth_pk()` boundary instead of JWT phrasing. The goal is clarity only; do not alter behavior while cleaning the comments.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and fix the wording pass before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>The touched wallet.asset, receive, and reachability surfaces no longer describe themselves with stale placeholder language.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize backup and scanner labels to the live boundary language</name>
  <files>crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_rpc.rs, crates/z00z_wallets/src/services/wallet/actions/wallet_service_actions_backup_rpc.rs, crates/z00z_wallets/src/chain/scan_engine_impl.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_impl.rs, crates/z00z_wallets/src/adapters/rpc/methods/backup_impl.rs</files>
  <action>Update the scanner, backup, and rotate-master-key compatibility labels so they describe the live wallet-side authority and report-only compatibility surfaces with no ambiguity. Remove the exact stale-comment targets named by the spec, including the `reachability placeholder path` wording for `rotate_master_key(...)`, generic `Phase 1 reachability stub/placeholder` wording in reachability helpers, and the `Phase 030 residue placeholder pending deletion or replacement` header if the assets file remains built. Ensure any Stage 13 log or README labels name the exact boundary: compatibility asset surface, canonical `wallet.tx.*` lifecycle, or in-memory key rederive.</action>
  <verify>1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first; if it fails, stop and fix the wording pass before broader checks.
2. Then run `cargo test --release --features test-fast --features wallet_debug_dump`.
3. Then invoke `/GSD-Review-Tasks-Execution` in YOLO mode three times and stop only after two consecutive clean passes.</verify>
  <done>The backup and scanner surfaces now use the canonical live-state and compatibility wording only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation text → operator interpretation | Misleading wording can cause the wrong boundary to be trusted during later debugging. |
| scanner labels → wallet ownership model | The comments must not imply a remote or JMT-side scanner owns claim detection. |
| backup RPC labels → recovery expectations | The wording must not overstate what the backup path persists or mutates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-046-13 | R | `wallet_service_actions_assets.rs` | mitigate | Rewrite the wording so compatibility surfaces are labeled as such and cannot be cited as canonical authority. |
| T-046-14 | I | `scan_engine_impl.rs` | mitigate | Use wallet-side scanner wording only, so the docs do not disclose or imply a second ownership model. |
| T-046-15 | S | `backup_impl.rs` | mitigate | Align the RPC labels with the actual recovery behavior so users do not trust an unsupported backup assumption. |
</threat_model>

<verification>
The plan is correct when the touched comments and labels all say the same thing as the locked phase context and do not introduce new behavior.
</verification>

<success_criteria>
The wallet RPC and scanner surfaces read as canonical, exact, and non-overstated, with no leftover placeholder or Phase 1 wording and no false JWT or ledger-authority claims.
</success_criteria>

<output>
After completion, create `.planning/phases/046-wallet-addons/046-05-SUMMARY.md`.
</output>