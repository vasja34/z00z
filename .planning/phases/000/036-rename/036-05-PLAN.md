---
phase: 036-rename
plan: 5
type: execute
wave: 2
depends_on: [036-04]
files_modified:
  - crates/z00z_wallets/src/core/tx/spending.rs
  - crates/z00z_wallets/src/core/tx/mod.rs
  - crates/z00z_storage/src/assets/proof.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend_state.rs
  - crates/z00z_storage/src/assets/store_internal/redb_backend_helpers.rs
  - crates/z00z_wallets/src/core/tx/asset_selector.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Step 1 compatibility rows remain explicitly blocked on retirement proof.
    - No Step 1 shim, legacy record, or compatibility decode lane is renamed or deleted prematurely.
    - The backlog still emits zero rename tasks for Step 1 rows.
  artifacts:
    - path: crates/z00z_wallets/src/core/tx/spending.rs
      provides: compatibility shim modules `spend_v1` and `events_v1` held in place.
    - path: crates/z00z_storage/src/assets/proof.rs
      provides: compatibility-owned `ProofBlobV0` decode surface.
    - path: crates/z00z_wallets/src/core/tx/asset_selector.rs
      provides: compatibility-owned `multi_v1` selector lane.
  key_links:
    - from: crates/z00z_storage/src/assets/store_internal/redb_backend_state.rs
      to: crates/z00z_storage/tests/test_redb_rehydrate.rs
      via: legacy `ClaimNullRecV0` compatibility coverage
      pattern: Step 1 rows stay tied to explicit rehydrate compatibility proof
    - from: crates/z00z_wallets/src/core/tx/spending.rs
      to: crates/z00z_wallets/src/core/tx/mod.rs
      via: public shim re-export path
      pattern: no rename-now emission for spend_v1 or events_v1
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-05 PLAN

<objective>
Execute the Step 1 compatibility-preservation slice from the canonical Phase 036 backlog.

Purpose: keep compatibility shims, legacy decode lanes, and import-read surfaces explicit until code-backed retirement proof exists, without letting naming cleanup collapse them early.
Output: `036-02 Preserve Compatibility Shims And Compatibility Read-Import Lanes` is closed with zero rename-now work and explicit retirement preconditions recorded for every Step 1 row.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-2.md
@.planning/phases/036-rename/036-a1-versioning-spec.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Preserve the exact task wording from `036-TODO-2.md`; do not rename the task, split its raw-row ownership, or import meaning from historical TODO1 artifacts.
- Honor D-01 through D-20 from `036-CONTEXT.md`, especially D-03, D-06, D-11, D-12, D-15, D-16, and D-17.
- The raw inventory is the only task-generation surface. If execution finds a missed compatibility caller or producer, update `036-a1-versioning-spec.md` first, then `036-TODO-2.md`, and only then resume code edits.
- Treat the `Compact Patch-Plan By Disputed Row Group` as a grouping aid only; if it ever disagrees with a raw row, the raw row wins.
- Before any edit, complete `read_first` and revalidate that the task-local `files`, `required_test_surfaces`, `acceptance_criteria`, `verify`, and `done` blocks still match the canonical raw-row ownership in `036-a1-versioning-spec.md` and `036-TODO-2.md`.
- If execution edits `036-a1-versioning-spec.md` or `036-TODO-2.md`, stop the current wave immediately and rerun review plus planning before any further code edits, closure checks, or summary claims.
- In every auto-task verify block, run validation in this order: bootstrap gate first, then `cargo test --release --features test-fast --features wallet_debug_dump`, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun the deterministic gates after each fix cycle, and do not stop the review loop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-02 Preserve Compatibility Shims And Compatibility Read-Import Lanes</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-01 through D-20 and the `Task Transfer Matrix` row for `036-02`.
    - `.planning/phases/036-rename/036-TODO-2.md` — the full `036-02` section, `Row-Step Coverage Ledger`, and `Completion Gate`.
    - `.planning/phases/036-rename/036-a1-versioning-spec.md` — `Execution Order By Row Range`, `Raw Inventory Appendix -> Non-Test Signatures`, and `Execution Step Completion Rules`.
  </read_first>
  <files>
    crates/z00z_wallets/src/core/tx/spending.rs
    crates/z00z_wallets/src/core/tx/mod.rs
    crates/z00z_storage/src/assets/proof.rs
    crates/z00z_storage/src/assets/store_internal/redb_backend_state.rs
    crates/z00z_storage/src/assets/store_internal/redb_backend_helpers.rs
    crates/z00z_wallets/src/core/tx/asset_selector.rs
  </files>
  <action>Execute `036-02 Preserve Compatibility Shims And Compatibility Read-Import Lanes` exactly as written in `036-TODO-2.md`. Keep non-test rows `20-21`, `24-25`, and `27` as compatibility-owned rows with no rename-now or delete-now emission. Preserve `spend_v1`, `events_v1`, `ProofBlobV0`, `ClaimNullRecV0`, and `multi_v1` at their current spellings while linking each row to its concrete compatibility window: public shim path, legacy proof decode, legacy nullifier rehydrate, or compatibility selector import lane. Record missing retirement proof as future follow-up rather than smuggling that work into this cleanup backlog. Do not rename or delete any Step 1 row, do not invent unsuffixed replacements, and do not let internal cleanup drift into compatibility-lane retirement.</action>
  <required_test_surfaces>
    crates/z00z_wallets/src/core/tx/spending.rs
    crates/z00z_wallets/src/core/tx/asset_selector_tests.rs
    crates/z00z_storage/src/assets/store_internal/test_whitebox_proofs.rs
    crates/z00z_storage/tests/test_redb_rehydrate.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - Every Step 1 row is explicitly classified as compatibility-owned with a concrete retirement precondition.
    - The phase backlog still emits zero rename or delete work for Step 1 rows.
    - No compatibility shim or legacy read-import lane is collapsed under naming-cleanup pressure.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_storage --release --features test-fast --test test_redb_rehydrate</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast asset_selector_tests</automated>
    <automated>rg -n "spend_v1|events_v1|ProofBlobV0|ClaimNullRecV0|multi_v1" crates/z00z_wallets/src crates/z00z_storage/src crates/z00z_storage/tests</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified by deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic gates; continue additional review or fix cycles until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Step 1 compatibility rows remain explicit, untouched, and backed by concrete retirement conditions, with no premature rename or delete work emitted in this wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compatibility lanes -> cleanup pass | Premature retirement would strand legacy read or import paths without proof. |
| shim modules -> public callers | Renaming or deleting shims too early would break published or semi-published call surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-05-01 | T | compatibility rows 20-21, 24-25, 27 | mitigate | Keep every Step 1 row unchanged until explicit retirement proof exists in the source spec and adjacent tests. |
| T-036-05-02 | D | proof or rehydrate compatibility paths | mitigate | Preserve `ProofBlobV0` and `ClaimNullRecV0` until `test_whitebox_proofs` and `test_redb_rehydrate` prove retirement conditions are satisfied in a later wave. |
| T-036-05-03 | E | public shim surface | mitigate | Keep `spend_v1`, `events_v1`, and `multi_v1` explicit with no rename-now emission in this plan. |
</threat_model>

<verification>
The executor must finish this plan with every Step 1 row still tied to a concrete compatibility justification and no premature rename or delete work emitted.
</verification>

<success_criteria>
`036-02 Preserve Compatibility Shims And Compatibility Read-Import Lanes` is closed exactly on the Step 1 raw-row set, with every row held by explicit compatibility proof requirements and no cleanup drift into retirement work.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-05-SUMMARY.md`.
</output>