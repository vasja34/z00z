---
phase: 036-rename
plan: 22
type: execute
wave: 11
depends_on: [036-20, 036-21]
files_modified:
  - crates/z00z_crypto/src/domains.rs
  - crates/z00z_crypto/src/kdf.rs
  - crates/z00z_crypto/src/ecdh.rs
  - crates/z00z_wallets/src/core/stealth/ecdh.rs
  - crates/z00z_wallets/src/core/address/stealth_request.rs
  - crates/z00z_wallets/src/core/address/stealth_card.rs
  - crates/z00z_crypto/src/hash_domains.rs
  - crates/z00z_crypto/src/hash_policy.rs
  - crates/z00z_crypto/tests/zkpack_domain_verification.rs
  - crates/z00z_crypto/tests/test_hash_policy.rs
  - .planning/phases/036-rename/036-a5_hashdomain-spec.md
  - .planning/phases/036-rename/036-22-SUMMARY.md
autonomous: true
requirements: [None]
must_haves:
  truths:
    - "All 27 rows in the embedded hash-domain normalization ledger are executed exactly: 24 non-Tari `hash_domain!` declarations use dotted lower-case namespace strings, 3 adjacent KDF salts use full-word identifiers plus dotted lower-case byte labels, and the `hash_domain!(TypeName, \"domain\", version)` call shape still remains unchanged."
    - "The only symbol renames introduced by this continuation are the ledger-owned domain type and salt names (`KdhDomain` -> `DhKeyDomain`, `ReqDomain` -> `PaymentRequestDomain`, `RcardDomain` -> `ReceiverCardDomain`, and the three KDF salt constant expansions), and every live consumer of those names still compiles and passes targeted crypto or wallet validation."
    - "The closeout summary proves the row-owned normalization sweep and records any broad-scan residual `Z00Z/` mirrors that stay outside the 27-row ledger without falsely claiming that the separate partial `036-20` shim-removal boundary is closed."
  artifacts:
    - path: .planning/phases/036-rename/036-22-PLAN.md
      provides: Self-contained execution contract for the hash-domain canonicalization continuation rooted in 036-a5_hashdomain-spec.md.
    - path: .planning/phases/036-rename/036-22-SUMMARY.md
      provides: Truth-based execution summary covering the 27-row normalization ledger, row-owned scans, and any out-of-scope broad-scan residuals.
    - path: .planning/phases/036-rename/036-a5_hashdomain-spec.md
      provides: Canonical source ledger for every domain and KDF salt normalization owned by this continuation.
  key_links:
    - from: .planning/phases/036-rename/036-a5_hashdomain-spec.md
      to: crates/z00z_crypto/src/domains.rs
      via: exact owner-file normalization of rows 1 through 24 plus in-file registry tests
      pattern: preserve `hash_domain!(TypeName, "domain", version)` shape while changing only identifier and literal fields
    - from: .planning/phases/036-rename/036-a5_hashdomain-spec.md
      to: crates/z00z_crypto/src/kdf.rs
      via: exact owner-file normalization of rows 25 through 27 plus downstream HKDF call-site rewrites
      pattern: expand abbreviated salt constant names and switch slash-separated byte labels to dotted lower-case namespaces
    - from: .planning/phases/036-rename/036-a5_hashdomain-spec.md
      to: crates/z00z_crypto/src/ecdh.rs
      via: `KdhDomain` -> `DhKeyDomain` symbol rename ripple into crypto-owned and wallet-owned ECDH helpers
      pattern: keep the same hash flow and version while changing only the domain type name and explanatory comments
---

# 036-22 PLAN

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Execute `036-22 Canonicalize The Non-Tari Hash-Domain And KDF Salt Surface Without Semantic Drift`.

Purpose: use `.planning/phases/036-rename/036-a5_hashdomain-spec.md` as the authoritative, self-contained source for the next Phase 036 continuation so the remaining noncanonical `hash_domain!` declarations and adjacent KDF salts move onto one dotted lower-case namespace style without changing version numbers, Tari vendor code, or unrelated contract surfaces.
Output: normalized domain and salt owners in `z00z_crypto`, reconciled direct symbol consumers and crypto-owned validation mirrors, and a truthful `036-22-SUMMARY.md` that closes the 27-row ledger without overstating the still-open `036-20` boundary.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
@.github/skills/smart-tests-bootstrap/SKILL.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-20-SUMMARY.md
@.planning/phases/036-rename/036-21-SUMMARY.md
@.planning/phases/036-rename/036-a5_hashdomain-spec.md
@crates/z00z_crypto/src/domains.rs
@crates/z00z_crypto/src/kdf.rs
@crates/z00z_crypto/src/ecdh.rs
@crates/z00z_wallets/src/core/stealth/ecdh.rs
@crates/z00z_crypto/src/hash_domains.rs
@crates/z00z_crypto/tests/zkpack_domain_verification.rs
@crates/z00z_crypto/tests/test_hash_policy.rs

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Keep Phase 036 self-contained under `.planning/phases/036-rename/` while adding the next explicit continuation instead of a duplicate phase folder. | 22 | COVERED | `036-22-PLAN.md` stays inside the existing Phase 036 directory and is registered in `STATE.md` and `ROADMAP.md`. |
| SPEC | REQ-001 | All non-Tari `hash_domain!` literals use dotted lower-case `z00z.<scope>.<purpose>.v<version>` form. | 22 | COVERED | Task A updates all 24 ledger-owned declarations and in-file registry assertions in `domains.rs`. |
| SPEC | REQ-002 | Opaque domain identifiers expand to descriptive `PascalCase` names where the ledger requires it. | 22 | COVERED | Task A/B rename `KdhDomain`, `ReqDomain`, and `RcardDomain` plus all direct symbol consumers. |
| SPEC | REQ-003 | Adjacent KDF salts use dotted lower-case namespaces and full-word constant names. | 22 | COVERED | Task A updates the 3 `kdf.rs` salt constants and their local call sites. |
| SPEC | REQ-004 | Version numbers and cryptographic family stay unchanged. | 22 | COVERED | Global gates prohibit version or algorithm changes; tasks normalize identifiers and literals only. |
| SPEC | CON-001 | Tari vendor files remain untouched. | 22 | COVERED | Files modified exclude `crates/z00z_crypto/tari/**`, and verification guards keep Tari out of scope. |
| SPEC | CON-002 | Preserve `hash_domain!(TypeName, "domain", version)` call shape. | 22 | COVERED | Task A modifies only type identifiers and literal strings in the owner declarations. |
| SPEC | GUD-001 | Keep one naming lane per domain family and avoid introducing a second dialect. | 22 | COVERED | Task B updates crypto-owned registry/tests that mirror the row-owned domains instead of leaving a competing internal registry dialect. |
| CONTEXT | D-11 | Do not rename literal-backed/public contracts unless the source spec explicitly upgrades them. | 22 | COVERED | Global gates confine edits to the 27 ledger rows and their direct compile/test consumers; unrelated manual `Z00Z/...` strings require a spec update before widening scope. |
| CONTEXT | D-12 | One raw row, one owner; do not collapse multiple files into synthetic shared ownership. | 22 | COVERED | Task A owns declaration/salt producers, Task B owns direct consumer fallout, and the summary records any broader residuals instead of silently absorbing them. |
| CONTEXT | D-15 | If a missed caller/producer appears, update the source spec first and then resume. | 22 | COVERED | Task B and Task C require fail-closed residual recording in `036-22-SUMMARY.md` rather than speculative widening. |

## Live Consumer Inventory

- `crates/z00z_crypto/src/ecdh.rs` and `crates/z00z_wallets/src/core/stealth/ecdh.rs` are the two live symbol consumers of `KdhDomain` and will not compile after the row-4 rename unless updated in the same continuation.
- `crates/z00z_wallets/src/core/address/stealth_request.rs` and `crates/z00z_wallets/src/core/address/stealth_card.rs` carry the live signing contexts for rows 23 and 24. Leaving those files on `Z00Z/REQv1` and `Z00Z/RCARD` would make the declaration rename declaration-only instead of a real contract migration.
- `crates/z00z_crypto/src/hash_domains.rs`, `crates/z00z_crypto/tests/zkpack_domain_verification.rs`, and `crates/z00z_crypto/tests/test_hash_policy.rs` are crypto-owned mirrors that currently hardcode old ledger literals. They must be reconciled or recorded explicitly if the row-owned normalization proves they still sit outside the authoritative surface.
- `036-20-SUMMARY.md` remains the authoritative open boundary for the shim-removal continuation. `036-22` is independent and must not claim that `036-20` closed.

## Embedded Hash-Domain Normalization Ledger

The ledger below is embedded from `.planning/phases/036-rename/036-a5_hashdomain-spec.md` so this plan remains self-sufficient.

The `Comments` column records `036-22` execution verification state.

### Table Legend

| Value in `Change Type` | Meaning |
| --- | --- |
| `literal normalization` | Keep the type name, change only the domain literal. |
| `identifier + literal normalization` | Expand or rename the type/constant identifier and normalize the literal. |

| # | Kind | Current Surface | File | Line | Canonical Target | Change Type | Notes | Comments |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | hash_domain | `hash_domain!(EphemeralScalarDomain, "Z00Z/R", 1);` | `crates/z00z_crypto/src/domains.rs` | 158 | `hash_domain!(EphemeralScalarDomain, "z00z.consensus.ephemeral_scalar.v1", 1);` | literal normalization | Hedged ephemeral scalar. | ✅ |
| 2 | hash_domain | `hash_domain!(ReceiverIdDomain, "Z00Z/RID", 1);` | `crates/z00z_crypto/src/domains.rs` | 159 | `hash_domain!(ReceiverIdDomain, "z00z.consensus.receiver_id.v1", 1);` | literal normalization | Owner-handle derivation for the receiver identity lane. | ✅ |
| 3 | hash_domain | `hash_domain!(ViewKeyDomain, "Z00Z/VIEW", 1);` | `crates/z00z_crypto/src/domains.rs` | 160 | `hash_domain!(ViewKeyDomain, "z00z.consensus.view_key.v1", 1);` | literal normalization | View-secret derivation lane. | ✅ |
| 4 | hash_domain | `hash_domain!(KdhDomain, "Z00Z/DH", 1);` | `crates/z00z_crypto/src/domains.rs` | 163 | `hash_domain!(DhKeyDomain, "z00z.consensus.dh_key.v1", 1);` | identifier + literal normalization | ECDH-to-symmetric-key lane. | ✅ |
| 5 | hash_domain | `hash_domain!(OwnerTagDomain, "Z00Z/TAG", 1);` | `crates/z00z_crypto/src/domains.rs` | 166 | `hash_domain!(OwnerTagDomain, "z00z.consensus.owner_tag.v1", 1);` | literal normalization | Owner-tag derivation lane. | ✅ |
| 6 | hash_domain | `hash_domain!(Tag16Domain, "Z00Z/TAG16", 1);` | `crates/z00z_crypto/src/domains.rs` | 167 | `hash_domain!(Tag16Domain, "z00z.consensus.tag16.v1", 1);` | literal normalization | Scan-accelerator tag lane. | ✅ |
| 7 | hash_domain | `hash_domain!(AssetIdDomain, "Z00Z/ASSET", 1);` | `crates/z00z_crypto/src/domains.rs` | 170 | `hash_domain!(AssetIdDomain, "z00z.consensus.asset_id.v1", 1);` | literal normalization | Asset identifier derivation lane. | ✅ |
| 8 | hash_domain | `hash_domain!(LeafAdDomain, "Z00Z/LEAFAD", 1);` | `crates/z00z_crypto/src/domains.rs` | 171 | `hash_domain!(LeafAdDomain, "z00z.consensus.leaf_ad.v1", 1);` | literal normalization | Leaf associated-data lane. | ✅ |
| 9 | hash_domain | `hash_domain!(LeafHashDomain, "Z00Z/LEAFHASH", 1);` | `crates/z00z_crypto/src/domains.rs` | 172 | `hash_domain!(LeafHashDomain, "z00z.consensus.leaf_hash.v1", 1);` | literal normalization | Leaf hash lane for the JMT path. | ✅ |
| 10 | hash_domain | `hash_domain!(ZkPackDomain, "Z00Z/ZKPACK", 1);` | `crates/z00z_crypto/src/domains.rs` | 175 | `hash_domain!(ZkPackDomain, "z00z.consensus.zkpack.v1", 1);` | literal normalization | ZkPack sponge initialization lane. | ✅ |
| 11 | hash_domain | `hash_domain!(PackKeyDomain, "Z00Z/PACKKEY", 1);` | `crates/z00z_crypto/src/domains.rs` | 176 | `hash_domain!(PackKeyDomain, "z00z.consensus.pack_key.v1", 1);` | literal normalization | Pack encryption key derivation lane. | ✅ |
| 12 | hash_domain | `hash_domain!(PackNonceDomain, "Z00Z/PACKNONCE", 1);` | `crates/z00z_crypto/src/domains.rs` | 177 | `hash_domain!(PackNonceDomain, "z00z.consensus.pack_nonce.v1", 1);` | literal normalization | Pack nonce derivation lane. | ✅ |
| 13 | hash_domain | `hash_domain!(PackFlowDomain, "Z00Z/PKFLOW", 1);` | `crates/z00z_crypto/src/domains.rs` | 178 | `hash_domain!(PackFlowDomain, "z00z.consensus.pack_flow.v1", 1);` | literal normalization | Older pack flow lane. | ✅ |
| 14 | hash_domain | `hash_domain!(PackMacDomain, "Z00Z/PKMAC", 1);` | `crates/z00z_crypto/src/domains.rs` | 179 | `hash_domain!(PackMacDomain, "z00z.consensus.pack_mac.v1", 1);` | literal normalization | Older pack MAC lane. | ✅ |
| 15 | hash_domain | `hash_domain!(XofBlockDomain, "Z00Z/XOFBLK", 1);` | `crates/z00z_crypto/src/domains.rs` | 180 | `hash_domain!(XofBlockDomain, "z00z.consensus.xof_block.v1", 1);` | literal normalization | XOF block generation lane. | ✅ |
| 16 | hash_domain | `hash_domain!(TxDigestDomain, "Z00Z/TXDIGEST", 1);` | `crates/z00z_crypto/src/domains.rs` | 183 | `hash_domain!(TxDigestDomain, "z00z.consensus.tx_digest.v1", 1);` | literal normalization | Transaction digest lane. | ✅ |
| 17 | hash_domain | `hash_domain!(SpendNullifierDomain, "Z00Z/NULLIFIER", 1);` | `crates/z00z_crypto/src/domains.rs` | 184 | `hash_domain!(SpendNullifierDomain, "z00z.consensus.nullifier.v1", 1);` | literal normalization | Spend-nullifier lane. | ✅ |
| 18 | hash_domain | `hash_domain!(TxOutputNonceDomain, "Z00Z/TXOUTNONCE", 1);` | `crates/z00z_crypto/src/domains.rs` | 185 | `hash_domain!(TxOutputNonceDomain, "z00z.consensus.tx_output_nonce.v1", 1);` | literal normalization | Tx output nonce lane. | ✅ |
| 19 | hash_domain | `hash_domain!(RangeCtxDomain, "Z00Z/RANGECTX", 1);` | `crates/z00z_crypto/src/domains.rs` | 186 | `hash_domain!(RangeCtxDomain, "z00z.consensus.range_ctx.v1", 1);` | literal normalization | Range-proof context lane. | ✅ |
| 20 | hash_domain | `hash_domain!(Stage4OutSeedDomain, "Z00Z/STAGE4/OUTSEED", 1);` | `crates/z00z_crypto/src/domains.rs` | 187 | `hash_domain!(Stage4OutSeedDomain, "z00z.consensus.stage4_out_seed.v1", 1);` | literal normalization | Stage-4 deterministic output seed lane. | ✅ |
| 21 | hash_domain | `hash_domain!(TxProofDomain, "Z00Z/TXPROOF/V1", 1);` | `crates/z00z_crypto/src/domains.rs` | 188 | `hash_domain!(TxProofDomain, "z00z.consensus.tx_proof.v1", 1);` | literal normalization | TxProof Fiat-Shamir challenge lane. | ✅ |
| 22 | hash_domain | `hash_domain!(CheckpointDomain, "Z00Z/CHECKPOINT/V1", 1);` | `crates/z00z_crypto/src/domains.rs` | 189 | `hash_domain!(CheckpointDomain, "z00z.consensus.checkpoint.v1", 1);` | literal normalization | Checkpoint-proof lane. | ✅ |
| 23 | hash_domain | `hash_domain!(ReqDomain, "Z00Z/REQv1", 1);` | `crates/z00z_crypto/src/domains.rs` | 192 | `hash_domain!(PaymentRequestDomain, "z00z.payment.request.v1", 1);` | identifier + literal normalization | Expands the abbreviated request domain name. | ✅ |
| 24 | hash_domain | `hash_domain!(RcardDomain, "Z00Z/RCARD", 1);` | `crates/z00z_crypto/src/domains.rs` | 193 | `hash_domain!(ReceiverCardDomain, "z00z.receiver.card.v1", 1);` | identifier + literal normalization | Expands the abbreviated receiver-card domain name. | ✅ |
| 25 | const | `KDF_CONS_SALT: &[u8] = b"z00z/consensus/kdf/v1";` | `crates/z00z_crypto/src/kdf.rs` | 150 | `KDF_CONSENSUS_SALT: &[u8] = b"z00z.consensus.kdf.v1";` | identifier + literal normalization | Expands the consensus abbreviation and aligns the salt with the dotted namespace convention. | ✅ |
| 26 | const | `KDF_WLT_SALT: &[u8] = b"z00z/wallet/kdf/v1";` | `crates/z00z_crypto/src/kdf.rs` | 151 | `KDF_WALLET_SALT: &[u8] = b"z00z.wallet.kdf.v1";` | identifier + literal normalization | Expands the wallet abbreviation and removes the slash-separated label. | ✅ |
| 27 | const | `KDF_WLT_VAR_SALT: &[u8] = b"z00z/wallet/kdf_var/v1";` | `crates/z00z_crypto/src/kdf.rs` | 152 | `KDF_WALLET_VARIABLE_SALT: &[u8] = b"z00z.wallet.kdf.variable.v1";` | identifier + literal normalization | Replaces the shorthand `VAR` with the full word `VARIABLE` for clarity. | ✅ |

## Broad-Scan Truth Guard

`036-a5_hashdomain-spec.md` proposes a broad discovery scan that includes raw `Z00Z/` tokens across `crates/**/*.rs`. Live repository reads already show that this broad pattern catches additional non-ledger constants and tests outside the 27-row owner set. Execution must therefore use a two-layer validation model:

1. **Row-owned proof**: zero old identifiers/literals remain in the owner and direct-consumer files named by this plan.
2. **Broad discovery scan**: rerun the spec scan, then record any surviving non-ledger hits in `036-22-SUMMARY.md` as out-of-scope residuals unless the source spec is upgraded first.

Do not silently widen `036-22` to absorb unrelated `Z00Z/...` strings just to force a false zero-hit result.
</context>

<global_execution_gates>

- Treat `.planning/phases/036-rename/036-a5_hashdomain-spec.md` as the sole owner of this continuation. Every ledger row must map to an explicit code or summary outcome.
- Preserve version numbers, cryptographic family, and `hash_domain!(TypeName, "domain", version)` call shape exactly. Only identifier and literal normalization is allowed for the owner declarations and salts.
- Do not touch `crates/z00z_crypto/tari/**`.
- Do not widen into unrelated wallet/request/signing domain constants or broad historical test vectors unless a deterministic failure maps them back to a ledger row and the source spec is updated first.
- The row-4, row-23, and row-24 symbol renames are compile-affecting. Update every live direct symbol consumer in the same execution wave.
- `036-20-SUMMARY.md` remains the authoritative partial shim-removal truth. `036-22` must not claim that the shim-removal boundary is closed.
- If execution needs a git commit, use `.github/skills/z00z-git-versioning/scripts/version-manager.sh` through the `/z00z-git-versioning` workflow instead of raw git commands.
</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-22.A Normalize the hash-domain owner declarations and adjacent KDF salt owners exactly per the 27-row ledger</name>
  <files>crates/z00z_crypto/src/domains.rs, crates/z00z_crypto/src/kdf.rs</files>
  <action>Apply rows 1 through 27 from the embedded ledger exactly in the owner files. In `domains.rs`, normalize all 24 non-Tari consensus-domain declarations to the ledger-provided dotted lower-case strings, rename only the three ledger-owned identifiers (`KdhDomain` -> `DhKeyDomain`, `ReqDomain` -> `PaymentRequestDomain`, `RcardDomain` -> `ReceiverCardDomain`), and update the in-file uniqueness/count/spec-string tests so they assert the new canonical values without changing the version number or macro call shape. In `kdf.rs`, rename the three salt constants to their full-word forms and replace the slash-separated salt byte labels with the dotted lower-case canonical targets while keeping the existing HKDF flow and output semantics unchanged. Do not touch Tari code, do not invent extra domain families, and do not widen into manual domain-like constants outside these owner files.</action>
  <verify>
    <automated>cargo test -p z00z_crypto --release --features test-fast --lib</automated>
    <automated>rg -n 'hash_domain!\(.*"Z00Z/|\bKDF_CONS_SALT\b|\bKDF_WLT_SALT\b|\bKDF_WLT_VAR_SALT\b' crates/z00z_crypto/src/domains.rs crates/z00z_crypto/src/kdf.rs</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>The owner files reflect all 27 ledger rows exactly, the three expanded identifiers are live in code, the old KDF constant names are gone from `kdf.rs`, and the owner-file unit tests assert only the new canonical dotted-lowercase strings.</done>
</task>

<task type="auto">
  <name>036-22.B Reconcile direct symbol consumers and crypto-owned registry/test mirrors without widening ledger ownership</name>
  <files>crates/z00z_crypto/src/ecdh.rs, crates/z00z_wallets/src/core/stealth/ecdh.rs, crates/z00z_wallets/src/core/address/stealth_request.rs, crates/z00z_wallets/src/core/address/stealth_card.rs, crates/z00z_crypto/src/hash_domains.rs, crates/z00z_crypto/src/hash_policy.rs, crates/z00z_crypto/tests/zkpack_domain_verification.rs, crates/z00z_crypto/tests/test_hash_policy.rs</files>
  <action>Update the direct compile/test fallout from Task A and only that fallout. Rename the `KdhDomain` imports/usages in `z00z_crypto/src/ecdh.rs` and `z00z_wallets/src/core/stealth/ecdh.rs` to `DhKeyDomain`, updating surrounding comments so they describe the canonical `dh_key` lane instead of the old abbreviation. Update the live request/card signature contexts in `z00z_wallets/src/core/address/stealth_request.rs` and `z00z_wallets/src/core/address/stealth_card.rs` so rows 23 and 24 migrate the real signing contract instead of becoming declaration-only renames. In `hash_domains.rs`, `hash_policy.rs`, `zkpack_domain_verification.rs`, and `test_hash_policy.rs`, replace the row-owned legacy strings with the new canonical dotted-lowercase domain labels wherever those files intentionally mirror or gate the normalized consensus-domain registry. Keep the same test intent and registry cardinality. If a broader scan finds additional non-ledger `Z00Z/...` strings outside these files, do not silently expand ownership; record them for Task C unless a deterministic compile/test failure proves they must be reclassified first.</action>
  <verify>
    <automated>cargo test -p z00z_crypto --release --features test-fast --test zkpack_domain_verification</automated>
    <automated>cargo test -p z00z_crypto --release --features test-fast --test test_hash_policy</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --test test_ecdh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --lib stealth_request</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --lib stealth_card</automated>
    <automated>rg -n '\bKdhDomain\b|\bReqDomain\b|\bRcardDomain\b|Z00Z/(R|RID|VIEW|DH|TAG|TAG16|ASSET|LEAFAD|LEAFHASH|ZKPACK|PACKKEY|PACKNONCE|PKFLOW|PKMAC|XOFBLK|TXDIGEST|NULLIFIER|TXOUTNONCE|RANGECTX|STAGE4/OUTSEED|TXPROOF/V1|CHECKPOINT/V1|REQv1|RCARD)' crates/z00z_crypto/src/ecdh.rs crates/z00z_wallets/src/core/stealth/ecdh.rs crates/z00z_wallets/src/core/address/stealth_request.rs crates/z00z_wallets/src/core/address/stealth_card.rs crates/z00z_crypto/src/hash_domains.rs crates/z00z_crypto/src/hash_policy.rs crates/z00z_crypto/tests/zkpack_domain_verification.rs crates/z00z_crypto/tests/test_hash_policy.rs</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>The direct symbol consumers compile against the renamed domain types, the crypto-owned registry/test mirrors now assert the new canonical dotted-lowercase values for the row-owned domains, and no old ledger identifiers or old ledger literals remain in the files owned by this task.</done>
</task>

<task type="auto">
  <name>036-22.C Write a truthful closeout summary with row-owned proof and explicit broad-scan residual accounting</name>
  <files>.planning/phases/036-rename/036-22-SUMMARY.md</files>
  <action>Write `036-22-SUMMARY.md` as a row-exact closeout for the embedded ledger. Enumerate the declaration/salt owner edits, the direct consumer fallout, the exact targeted tests run, and the zero-hit row-owned scans. Rerun the broad spec discovery scan `rg -n 'hash_domain!\(|KDF_CONS_SALT|KDF_WLT_SALT|KDF_WLT_VAR_SALT|Z00Z/' crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'`, then classify any surviving non-ledger hits explicitly as out-of-scope residuals unless the spec was upgraded first. State plainly that `036-20-SUMMARY.md` remains the authoritative partial shim-removal boundary and that `036-22` closes only the hash-domain continuation.</action>
  <verify>
    <automated>rg -n 'REQ-001|REQ-002|REQ-003|REQ-004|AC-001|AC-002|AC-003|AC-004|036-20-SUMMARY|out-of-scope residual|row-owned scan|broad spec discovery scan' .planning/phases/036-rename/036-22-SUMMARY.md</automated>
    <automated>rg -n 'hash_domain!\(|KDF_CONS_SALT|KDF_WLT_SALT|KDF_WLT_VAR_SALT|Z00Z/' crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>`036-22-SUMMARY.md` proves the 27-row normalization sweep with repository-backed evidence, records any surviving non-ledger broad-scan hits honestly, and does not over-claim closure for the separate partial `036-20` continuation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hash-domain owner declarations -> cryptographic behavior | Changing a domain literal or symbol in the wrong place could silently alter key derivation, proof challenges, or signature context beyond the row-owned contract. |
| owner-file renames -> downstream consumer compile surfaces | The three identifier expansions are compile-affecting and can leave wallet/crypto consumers or registry tests behind if they are not updated in lockstep. |
| summary closeout -> phase truth | A false zero-hit interpretation of the broad `Z00Z/` scan could overstate the ledger closure and hide remaining non-ledger residuals or the still-open `036-20` boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-22-01 | T | `crates/z00z_crypto/src/domains.rs` | mitigate | Restrict edits to the 24 ledger-owned declarations, preserve macro shape/version, and keep the owner-file tests aligned with the new canonical strings. |
| T-036-22-02 | T | `crates/z00z_crypto/src/kdf.rs` | mitigate | Rename only the three listed salt identifiers/literals, keep HKDF behavior unchanged, and prove old salt names are absent from the owner file after the rewrite. |
| T-036-22-03 | D | `crates/z00z_crypto/src/ecdh.rs`, `crates/z00z_wallets/src/core/stealth/ecdh.rs` | mitigate | Rename the `DhKeyDomain` consumers in the same wave and run targeted crypto/wallet tests so compile or runtime drift cannot linger. |
| T-036-22-04 | R | `036-22-SUMMARY.md` closeout narrative | mitigate | Require both row-owned scans and the broad spec discovery scan in the summary, explicitly classify out-of-scope residuals, and restate that `036-20-SUMMARY.md` remains the open partial boundary. |
</threat_model>

<verification>
This continuation closes only when the 27-row embedded ledger is reconciled exactly, the direct symbol consumers compile against the renamed identifiers, the targeted crypto/wallet tests are green, and the closeout summary distinguishes row-owned proof from broader out-of-scope residual discovery.
</verification>

<success_criteria>
`036-22 Canonicalize The Non-Tari Hash-Domain And KDF Salt Surface Without Semantic Drift` is complete only when every row in the embedded ledger has a repository-backed outcome, the row-owned declaration and salt files contain only the canonical dotted-lowercase forms, the three expanded identifiers are reflected in their direct consumers, and the summary records any remaining broad-scan residuals without collapsing the separate `036-20` partial truth.
</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-22-SUMMARY.md`.
</output>