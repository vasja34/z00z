---
phase: 036-rename
plan: 20
type: execute
wave: 10
depends_on: [036-19]
files_modified:
  - crates/z00z_utils/src/time/traits.rs
  - crates/z00z_crypto/src/ecdh_stealth.rs
  - crates/z00z_crypto/src/types.rs
  - crates/z00z_wallets/src/core/stealth/encoding.rs
  - crates/z00z_wallets/src/core/stealth/tag.rs
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/core/address/address_manager/address_manager_trait.rs
  - crates/z00z_wallets/src/services/wallet_service_actions_receive.rs
  - crates/z00z_storage/src/assets/types_record.rs
  - crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Explicit compatibility-only time wrappers are no longer used ambiguously by production callers.
    - Wallet, simulator, and crypto callers route through the canonical shim replacements named in the embedded table.
    - Storage and simulator compatibility shims are either deleted with proof-backed replacements or carried forward explicitly under the open-gap rules.
  artifacts:
    - path: .planning/phases/036-rename/036-20-PLAN.md
      provides: Self-contained shim-removal execution authority
    - path: crates/z00z_utils/src/time/traits.rs
      provides: Explicit production versus compatibility time contract
    - path: crates/z00z_wallets/src/core/stealth/tag.rs
      provides: Canonical stealth binding ownership after duplicate-formula removal
    - path: crates/z00z_storage/src/assets/types_record.rs
      provides: Storage root compatibility boundary with explicit survivor or deletion proof
  key_links:
    - from: crates/z00z_utils/src/time/traits.rs
      to: simulator, storage, logger, and wallet callers
      via: try_* and compat_* migrations
      pattern: compat_unix_timestamp|try_unix_timestamp
    - from: crates/z00z_wallets/src/core/stealth/output.rs
      to: simulator stage 4 and stage 6 helper callers
      via: canonical output_build surface
      pattern: build_stealth_bundle|build_stealth_sender_leaf
    - from: crates/z00z_storage/src/assets/types_record.rs
      to: storage and simulator root consumers
      via: AssetStateRoot/CheckRoot/RootApi migration
      pattern: CompatRoot
---

# 036-20 PLAN

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Execute `036-20 Remove The A4 Compatibility Shims Through Their Canonical Owners`.

Purpose: use `.planning/phases/036-rename/036-a4-shims-spec.md` as the live authority for the shim-removal wave, while embedding the full shim target table here so the plan is self-sufficient even if the sidecar spec is not reopened during execution.
Output: deleted or explicitly reclassified compatibility shims, caller migrations onto their canonical owners, a residual scan proving what remains, and a truthful `036-20-SUMMARY.md`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
@.github/skills/smart-tests-bootstrap/SKILL.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-a4-shims-spec.md
@.planning/phases/036-rename/036-19-PLAN.md

## Embedded Repository-Backed Baseline

- `crates/z00z_utils/src/time/traits.rs` says `try_unix_timestamp*()` is the canonical production contract and `compat_unix_timestamp*()` is the explicit compatibility surface.
- `crates/z00z_crypto/src/ecdh.rs` is the canonical owner for point-based stealth ECDH and the `derive_dh_key(&Z00ZRistrettoPoint)` formula chain.
- `crates/z00z_wallets/src/core/stealth/mod.rs` says public callers should go through `core::ecdh`, `core::kdf`, and `core::scan`.
- `crates/z00z_wallets/src/core/tx/mod.rs` says `core::tx::builder::*` and `core::tx::output_flow::*` are no longer part of the public caller surface.
- `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs` says `recv_range(...)` is the preferred request-aware receive lane.
- `crates/z00z_storage/src/assets/README.MD` and `crates/z00z_storage/src/assets/types_record.rs` say `AssetStateRoot` is the canonical storage state commitment, `CheckRoot` is the checkpoint-facing root, and `CompatRoot` is the compatibility root for older migration paths.
- `crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs` says `SeqSecureRngProvider` is a simulator-only injection shim and must not be treated as production entropy.

## Embedded Canonical Target Map

| Compatibility surface | Current owner | Canonical path | Deletion action | Comments |
| --- | --- | --- | --- | --- |
| Deprecated unix timestamp wrappers | `crates/z00z_utils/src/time/traits.rs` | `try_unix_timestamp*()` and `compat_unix_timestamp*()` on `TimeProvider` | Delete `unix_timestamp*()` wrappers and update callers to the explicit canonical or compatibility entrypoint they actually need. | ✅ Removed in the current 036-20 execution slice. |
| Bytes-oriented stealth ECDH compatibility layer | `crates/z00z_crypto/src/ecdh_stealth.rs` | `z00z_crypto::ecdh::{generate_ephemeral_keypair, compute_stealth_dh_sender, recover_stealth_dh_receiver, derive_dh_key, validate_stealth_point}` | Delete the compatibility-only byte wrapper and route new callers to the point-based crypto API. | ✅ Deleted in the current 036-20 execution slice. |
| Backward-compatible point constructor | `crates/z00z_crypto/src/types.rs` | `Z00ZRistrettoPoint::try_from_bytes([u8; 32])` | Delete `from_canonical_bytes(&[u8])` call sites and switch them to the fixed-size canonical constructor. | ✅ Focused owned production point decode lanes migrated in this cycle; broad repo-wide residual scan is still open. |
| Backward-compatible commitment constructor | `crates/z00z_crypto/src/types.rs` | `PedersenCommitment::from_canonical_bytes(bytes)` followed by `Z00ZCommitment::from_commitment(...)` | Delete `Z00ZCommitment::from_canonical_bytes()` and decode commitments at the call site before wrapping them. | ✅ Focused owned production commitment decode lanes migrated in this cycle; broad test-heavy residual inventory is still open. |
| Backward-compatible stealth key decoder alias | `crates/z00z_wallets/src/core/stealth/encoding.rs` | `decode_public_key()` | Delete `decode_ristretto_pk()` and keep `decode_public_key()` as the single decoder name. | ✅ Removed from the live caller surface and normalized onto canonical decoder names. |
| Wallet-facing tag and leaf-ad duplicates | `crates/z00z_wallets/src/core/stealth/tag.rs` | `z00z_crypto::stealth_bind::{compute_tag16, compute_leaf_ad}` | Delete the wallet duplicate formulas and keep only the request-bound helpers that have distinct semantics. | Open: not re-proven in this continuation slice. |
| Stateless stealth output compatibility lanes | `crates/z00z_wallets/src/core/stealth/output.rs` | `crates/z00z_wallets/src/core/stealth/output_build.rs::{build_output_ctx, build_output_ctx_with_serial, build_output_ctx_with_r}` and the public `build_tx_stealth_output*` / `build_tx_stealth_output_validated` / `build_card_stealth_output_validated` surfaces | Delete `build_stealth_sender_leaf()`, `build_stealth_bundle()`, and `build_stealth_bundle_with_rng()` as compatibility-only lanes. | ✅ Old compatibility names removed from the live caller inventory in the focused shim slice. |
| Fail-closed tx compatibility path | `crates/z00z_wallets/src/core/tx/output_flow.rs` | `crates/z00z_wallets/src/core/tx/tx_digest.rs::build_tx_package_digest` and `crates/z00z_wallets/src/core/tx/tx_verifier.rs::TxVerifier` | Delete the compatibility path once callers move to the canonical digest and verification modules. | Open: broader tx compatibility sweep still pending. |
| Deprecated tx builder shims | `crates/z00z_wallets/src/core/tx/mod.rs` | `crate::core::stealth::{build_stealth_leaf, build_stealth_leaf_with_blind, build_stealth_leaf_with_rng}` | Delete the test-only forwarding layer that still points at the stealth-owned leaf builders. | Open: not closed in this execution slice. |
| Backward-compatible address derivation helper | `crates/z00z_wallets/src/core/address/address_manager/address_manager_trait.rs` | `derive_wallet_keys()`, `create_dual_address()`, `derive_batch()`, `scan_checkpoint_with_requests()`, `scan_range_with_requests()` | Delete `derive_address()` once callers move to the richer manager API. | ✅ `derive_address()` callers were migrated to canonical manager-owned surfaces. |
| Single-asset receive fallback | `crates/z00z_wallets/src/services/wallet_service_actions_receive.rs` | `recv_range()` | Delete `recv_one()` if the compatibility receive lane is no longer required; otherwise keep it explicitly documented as fallback-only. | ✅ Focused residual scans show no live `recv_one` caller or symbol survivor in this cycle. |
| Legacy single/dual address compatibility types | `crates/z00z_wallets/src/core/address/z00z_address/z00z_single_address.rs` and `crates/z00z_wallets/src/core/address/z00z_address/z00z_dual_address.rs` | `AddressManager::derive_wallet_keys()` and `AddressManager::create_dual_address()` | Delete new callers to the legacy address codec structs; prefer manager-owned derivation and Bech32 transport edges. | Open: transport-visible compatibility boundary still present. |
| Compatibility storage root wrapper | `crates/z00z_storage/src/assets/types_record.rs` | `AssetStateRoot`, `CheckRoot`, and `RootApi` | Delete `CompatRoot` only after the migration proof no longer needs the older flat-row compatibility decode path. | Open survivor: still live in storage model/tests and the simulator Stage-4 digest parity boundary. |
| Simulator-local deterministic RNG adapter | `crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs` | `WalletService::create_service_custom_output_directory(...)` with a caller-owned `SecureRngProvider` boundary, or `WalletService::with_output_dir(...)` for the default production path | Delete `SeqSecureRngProvider` from simulator transport once reproducibility is handled by a named test fixture instead of a simulator-owned entropy shim. | Open survivor: still the simulator-only reproducibility shim in stage-2 transport. |

## Embedded Open Gaps And Watchpoints

- The Phase 1 asset RPC methods in `crates/z00z_wallets/src/core/wallet/wallet_entity_asset_api.rs` are stub reachability methods, not alias wrappers. They still need a separate implementation decision before they can be treated as canonical production paths.
- The old address codec structs in `crates/z00z_wallets/src/core/address/z00z_address/` are still used as transport-visible compatibility types. If this wave wants to delete them entirely, it must land the replacement transport story in the same patch set.
- The `Z00ZCommitment` and `Z00ZRistrettoPoint` backward-compatible constructors are used broadly across crypto, tx, and address code. Their deletion requires a deliberate call-site sweep so the repo does not end up with scattered partial migrations.

## Embedded Validation Scan

```bash
rg -n "compat_unix_timestamp|unix_timestamp\b|decode_ristretto_pk|build_stealth_sender_leaf|build_stealth_bundle|build_stealth_bundle_with_rng|recv_one\b|derive_address\b|CompatRoot\b|SeqSecureRngProvider|from_canonical_bytes\b" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'
```

</context>

<tasks>

<!-- REVIEW 2026-04-20: 036-20.A is partially executed. -->
<!-- ✅ Removed deprecated TimeProvider::unix_timestamp*() wrappers from the trait surface. -->
<!-- ✅ Deleted the bytes-oriented ecdh_stealth compatibility module and its test suite from z00z_crypto. -->
<!-- ✅ Migrated the Phase-036-owned claim/core/address decode seams touched in this cycle onto try_from_bytes()/Commitment::from_bytes() or equivalent explicit canonical decode paths. -->
<!-- NOT YET ✅ 036-20.A as a whole: the broad residual inventory now reports 71 scan hits total (54 `from_canonical_bytes`, 13 `CompatRoot`, 4 `SeqSecureRngProvider`), so the constructor/storage/simulator sweep is still open even though the active non-test source tail is down to a src-local test helper and a rustdoc example. -->
<task type="auto">
  <name>036-20.A Remove explicit time and crypto constructor shims</name>
  <files>crates/z00z_utils/src/time/traits.rs, crates/z00z_utils/src/logger/file_logger.rs, crates/z00z_utils/src/logger/rotating_file_logger.rs, crates/z00z_utils/examples/time_provider_demo.rs, crates/z00z_simulator/src/scenario_1/stage_2_utils/flow.rs, crates/z00z_simulator/src/scenario_1/stage_3_runtime.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/wallet_flow.rs, crates/z00z_storage/src/assets/store_internal/redb_backend_helpers.rs, crates/z00z_wallets/src/services/chain_service.rs, crates/z00z_crypto/src/ecdh_stealth.rs, crates/z00z_crypto/src/types.rs, crates/z00z_simulator/tests/test_claim_emit.rs, crates/z00z_simulator/tests/test_claim_pkg_runtime.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs, crates/z00z_simulator/tests/test_stage4_claim_gate.rs, crates/z00z_simulator/tests/test_claim_integration.rs</files>
  <action>Delete the deprecated `unix_timestamp*()` wrappers in favor of explicit `try_*` or `compat_*` callers, remove the byte-oriented stealth ECDH compatibility layer, and sweep every Phase-036-owned `from_canonical_bytes` caller onto the fixed-size point constructor or the commitment-then-wrap pattern. Do not touch Tari vendor code, and do not leave mixed constructor styles inside one ownership seam.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "compat_unix_timestamp|unix_timestamp\b|from_canonical_bytes\b" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>Explicit time callers use only `try_*` or `compat_*`, the ECDH byte shim is gone, and the owned constructor call sites no longer depend on the deprecated compatibility constructors.</done>
</task>

<!-- REVIEW 2026-04-20: 036-20.B is partially executed. -->
<!-- ✅ Removed `decode_ristretto_pk` from the wallet stealth owner surface and normalized live callers onto `decode_public_key()` or `decode_card_public_key()`. -->
<!-- ✅ Renamed `build_stealth_sender_leaf`, `build_stealth_bundle`, and `build_stealth_bundle_with_rng` to `build_card_stealth_leaf`, `build_output_bundle`, and `build_output_bundle_with_rng`, then migrated the live caller inventory. -->
<!-- ✅ Renamed `derive_address()` to `derive_spend_key()` and `recv_one()` to `scan_asset_report()` or simulator-local `scan_asset()` across the live caller inventory. -->
<!-- ✅ Two consecutive focused old-name residual scans returned zero hits for `recv_one`, `decode_ristretto_pk`, `build_stealth_sender_leaf`, `build_stealth_bundle*`, and `derive_address`. -->
<!-- UNVERIFIED: historical notes claim two consecutive bootstrap reruns stayed green after the focused old-name survivor removal, but this repository evidence set does not currently preserve that older paired-bootstrap proof, so keep the bootstrap claim downgraded until it is re-established in the same execution cycle. -->
<!-- NOT YET ✅ 036-20.B as a whole: broader shim-table deletions and explicit survivor reclassification still remain open outside the focused old-name caller slice. -->
<task type="auto">
  <name>036-20.B Migrate wallet, simulator, and tx callers onto the canonical stealth and address owners</name>
  <files>crates/z00z_wallets/src/core/stealth/encoding.rs, crates/z00z_wallets/src/core/stealth/tag.rs, crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/core/stealth/mod.rs, crates/z00z_wallets/src/core/tx/output_flow.rs, crates/z00z_wallets/src/core/tx/mod.rs, crates/z00z_wallets/src/core/address/address_manager/address_manager_trait.rs, crates/z00z_wallets/src/services/wallet_service_actions_receive.rs, crates/z00z_wallets/src/core/address/z00z_address/z00z_single_address.rs, crates/z00z_wallets/src/core/address/z00z_address/z00z_dual_address.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/mod.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/output_construction_balance.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bridge_output_router.rs, crates/z00z_simulator/examples/simulator_interop.rs, crates/z00z_simulator/examples/simulator_interop/support.rs, crates/z00z_simulator/tests/test_e2e_phase4.rs, crates/z00z_wallets/src/services/test_wallet_service_suite.rs</files>
  <action>Delete wallet-local stealth duplicates, deprecated tx compatibility lanes, and the legacy `derive_address()` helper by routing callers to `decode_public_key()`, `z00z_crypto::stealth_bind::*`, the canonical `output_build` and `tx_digest` or `tx_verifier` surfaces, and the richer address-manager APIs. For `recv_one()` and the legacy address codec structs, delete them only if the live caller inventory proves the compatibility lane is no longer needed; otherwise retain them with an explicit fallback-only or transport-visible note in the summary instead of silently claiming deletion.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "decode_ristretto_pk|build_stealth_sender_leaf|build_stealth_bundle|build_stealth_bundle_with_rng|derive_address\b|recv_one\b" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>Owned wallet and simulator callers use the canonical stealth, tx, and address surfaces, and any compatibility survivor is called out explicitly as fallback-only or transport-visible in the execution summary.</done>
</task>

<!-- REVIEW 2026-04-20: 036-20.C survivor inventory was re-read in this execution cycle, but no storage or simulator survivor deletion proof landed yet. -->
<task type="auto">
  <name>036-20.C Resolve storage and simulator shim survivors, then prove the residual boundary</name>
  <files>crates/z00z_storage/src/assets/types_record.rs, crates/z00z_storage/src/assets/types.rs, crates/z00z_storage/src/assets/model.rs, crates/z00z_storage/src/assets/mod.rs, crates/z00z_storage/src/assets/test_model.rs, crates/z00z_simulator/tests/test_stage4_digest.rs, crates/z00z_simulator/src/scenario_1/stage_2_utils/transport.rs, .planning/phases/036-rename/036-a4-shims-spec.md, .planning/phases/036-rename/036-20-SUMMARY.md</files>
  <action>Delete `CompatRoot` and `SeqSecureRngProvider` only if the migration proof and reproducibility fixture story are fully present in the same patch set. If either survivor still has an open-gap justification, keep it intentionally and record the exact blocker in `036-20-SUMMARY.md` rather than silently narrowing the phase claim. End with the embedded residual scan and reconcile the summary against the live survivor set.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "CompatRoot\b|SeqSecureRngProvider|compat_unix_timestamp|unix_timestamp\b|decode_ristretto_pk|build_stealth_sender_leaf|build_stealth_bundle|build_stealth_bundle_with_rng|recv_one\b|derive_address\b|from_canonical_bytes\b" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>The residual scan cleanly distinguishes deleted shims from explicit survivors, and `036-20-SUMMARY.md` states the exact remaining blockers instead of over-claiming full shim removal.</done>
</task>

</tasks>

<verification>
This plan closes only when every row in the embedded shim target map is either deleted through its canonical owner or carried forward explicitly under the embedded open-gap rules, with a residual scan and summary that prove the exact survivor set.
</verification>

<success_criteria>
`036-20 Remove The A4 Compatibility Shims Through Their Canonical Owners` is complete only when the embedded shim table has been fully reconciled against the workspace, no shim is left in an ambiguous half-migrated state, and the summary records any intentional survivors with code-backed reasons.
</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-20-SUMMARY.md`.
</output>