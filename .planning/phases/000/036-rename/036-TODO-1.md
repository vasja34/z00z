# 036-TODO-1

Canonical design source:

- [036-a1-suffixes-spec](./036-a1-suffixes-spec.md)

Execution rules:

- execute this file strictly in the order given by `Execution Order By Row Range`;
- treat `036-a1-suffixes-spec.md` as normative for row meaning and this file as normative for execution order;
- use the `Raw Inventory Appendix` as the only task-generation surface;
- translate each raw row through `Table-to-Task Translation Protocol` without collapsing identical signatures across different paths;
- preserve stable row IDs, including intentional gaps left by removed fabricated rows;
- keep production rows and test-only rows on separate tracks;
- if execution discovers a new design constraint, update the spec first and then update this backlog;
- do not start any `036-0x` execution step until the task-local `Files`, `Tests`, and `Exit condition` blocks match the raw appendix and any newly discovered live caller or producer surfaces are reconciled back into this file first;
- do not renumber rows or invent replacement rows for removed entries.

Phase 036 is now summary-backed complete. The row ledger keeps the original
suffix-bearing signatures for auditability, so rows closed by Steps `1-4` now
use final-state values in `Current classification` and `Action now`: `removed`
means the original signature no longer exists in the live repository,
`renamed survivor` means the suffixed row converged onto the current unsuffixed
live contract, and `closed` means the planned action has already been executed
and verified.

## Decision Summary

The execution baseline for Phase 036 is:

1. use the raw inventory appendix as the only source of truth for row-to-task translation;
2. execute cleanup and hold decisions in the exact row-range order defined by `Execution Order By Row Range`;
3. preserve one task per raw row, even when the same signature text appears in more than one file;
4. treat `Current classification`, `Action now`, and `Future survivor target` as different fields with different meanings;
5. keep future-reserved rows and hold-only rows in the backlog but do not convert them into cleanup blockers;
6. keep test-only rows in a separate review track and do not mix them into production cleanup;
7. do not infer the inner claim proof version from the outer `CLAIM_PROOF_V2` wire tag.

## Dependency Chain

Execution dependency chain:

1. `036-01` freeze keep-only and future-reserved rows
2. `036-02` legacy wallet-KDF retirement
3. `036-03` legacy backup-KDF retirement
4. `036-04` legacy backup payload import-export retirement
5. `036-05` seed-container contract migration hold
6. `036-06` receiver-card publication migration hold
7. `036-07` claim-v2 protocol surface hold
8. `036-08` address-v2 future activation hold
9. `036-09` test-only row review after production steps

Hard dependencies:

- `036-02` must not start before `036-01` is documented and preserved.
- `036-03` must not start before `036-02` is resolved or explicitly deferred.
- `036-04` must not start before `036-03` is resolved or explicitly deferred.
- `036-09` depends on `036-01` through `036-08` and should only close after the production rows have been reviewed in order.

## File-First Implementation Order

Edit order by file cluster:

1. `crates/z00z_crypto/src/kdf_domains.rs` and `crates/z00z_crypto/src/crypto_constants.rs`
2. wallet KDF and migration files: `crates/z00z_wallets/src/db/redb_wallet_crypto.rs`, `crates/z00z_wallets/src/core/key/key_manager_redb.rs`, `crates/z00z_wallets/src/core/key/key_manager_redb_wallet.rs`, `crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs`, `crates/z00z_wallets/src/core/hashing.rs`
3. backup compatibility files: `crates/z00z_wallets/src/core/backup/wallet_backup_kdf.rs`, `crates/z00z_wallets/src/core/backup/wallet_backup.rs`, `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs`, `crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs`, `crates/z00z_wallets/src/core/backup/backup_exporter_verify.rs`, `crates/z00z_wallets/src/core/backup/backup_wire.rs`
4. current-contract seed, payload, AAD, index, and schema files: `crates/z00z_wallets/src/core/key/seed_cipher_container.rs`, `crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs`, `crates/z00z_crypto/src/aead_aad.rs`, `crates/z00z_wallets/src/db/index_codecs.rs`, `crates/z00z_wallets/src/db/schema_keys.rs`, `crates/z00z_wallets/src/wasm/schema_keys.rs`
5. claim, tx, and RPC surface files: `crates/z00z_crypto/src/claim/v2.rs`, `crates/z00z_wallets/src/core/tx/claim_wire_types.rs`, `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl.rs`, `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/key.rs`, `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs`
6. address and receiver files: `crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs`, `crates/z00z_wallets/src/core/chain/receiver_card_record.rs`
7. supporting current-contract files: `crates/z00z_wallets/src/core/claim/claim_receipt.rs`, `crates/z00z_wallets/src/core/tx/fee_estimator.rs`
8. test files listed in the row ledger below

## Validation Matrix

This table proves that the implementation-driving instructions from `036-a1-suffixes-spec.md` have been migrated into this backlog and remain explicitly traceable.

| 036-a1-suffixes-spec source | Required theme | TODO coverage | Status |
| --- | --- | --- | --- |
| `Execution Order By Row Range` | serial row-range execution and hold/deletion order | `036-01` through `036-09` | Mapped |
| `Raw Inventory Appendix -> Non-Test Signatures` | all production rows as raw task items | `036-01` through `036-08` and the non-test row ledger | Mapped |
| `Raw Inventory Appendix -> Test-Only Signatures` | separate test track | `036-09` and the test-only row ledger | Mapped |
| `Table-to-Task Translation Protocol` | row-level translation rules, stable row IDs, and no collapsing by name | execution rules, task checklists, and the row ledgers | Mapped |
| `Pitfall Guards` | no recreated summary table, no row-number guessing, no cross-track collapse | execution rules and explicit phase boundary | Mapped |
| `Execution Step Completion Rules` | explicit resolved status and no extra signature-level change outside the raw inventory | `Completion Gate` | Mapped |

## Completion Gate

Global execution order is fixed and non-parallel for this backlog:

1. `036-01` must close before `036-02` starts.
2. `036-02` must close before `036-03` starts.
3. `036-03` must close before `036-04` starts.
4. `036-04` must close before `036-05` starts.
5. `036-05` must close before `036-06` starts.
6. `036-06` must close before `036-07` starts.
7. `036-07` must close before `036-08` starts.
8. `036-08` must close before `036-09` starts.

Inside each task, work order is also fixed:

1. `036-01`: keep-only freeze for rows `1-3`, `5`, `7`, `20-23`, `25-26`, `31`, `40-42`, and `57-60`; no rename and no delete in this step.
2. `036-02`: execute rows `16-17` first, then rows `8`, `10`, `12`, `14`, `24`, `28-30`, then rename survivor rows `9`, `11`, `13`, and `15` only after compatibility retirement is proven.
3. `036-03`: delete rows `44-45` first, then rename survivor row `43` only after legacy restore retirement is proven.
4. `036-04`: delete rows `32-33` first, then `36-38`, then `54-56`, then `50-51`, then rename survivor rows `34`, `35`, `52`, and `53` only after old backup-format retirement is proven.
5. `036-05`: hold rows `18-19` with no rename and no delete in this wave.
6. `036-06`: hold row `39` with no rename and no delete in this wave.
7. `036-07`: hold row `27` with no rename and no delete in this wave.
8. `036-08`: hold rows `61-63` as future-reserved only with no cleanup task in this wave.
9. `036-09`: review test-only rows `1-28` only after `036-01` through `036-08` are resolved or explicitly held; do not retire compatibility-proof tests early.

No task is complete unless all of the following are true:

1. The `Execution Order By Row Range` mapping still matches the raw rows implemented by the task.
2. Every affected raw row has an explicit resolved state consistent with the spec.
3. The step-specific keep, hold, delete, or rename condition remains truthful.
4. No signature-level change was introduced outside the raw inventory.

## Explicit Phase Boundary

The following topics are intentionally tracked as explicit Phase 036 non-goals so the backlog stays faithful to the raw inventory:

- do not recreate a summary table outside the raw inventory appendix;
- do not collapse same signatures across distinct files or paths;
- do not use `Future survivor target` as a current-wave rename target unless the execution-step condition is already satisfied;
- do not mix production cleanup rows and test-only review rows into one task;
- do not infer inner claim proof version from the outer claim wire tag;
- do not activate future-reserved address rows `61-63` in this wave;
- do not rename or delete hold-only rows `18-19`, `27`, and `39` in this wave;
- keep raw row gaps such as `4` and `6` intentional; do not renumber the appendix.

## Concrete Execution Tasks

### 036-01 Freeze Keep-Only And Future-Reserved Rows

Spec references:

- `Execution Order By Row Range` step 0
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 0 block in `Execution Order By Row Range`, then the non-test rows `1-3`, `5`, `7`, `20-23`, `25-26`, `31`, `40-42`, and `57-60` in the `Raw Inventory Appendix`.

- [x] Keep the future-reserved rows untouched and do not invent a `*_V1` counterpart for any of them.
- [x] Keep the current production contracts untouched and do not rename or delete them in this wave.
- [x] Preserve row gaps and row IDs exactly as written in the raw inventory.
- [x] If a row needs tracking, create a hold-only note or regression check instead of a cleanup change.

Files:

- `crates/z00z_crypto/src/kdf_domains.rs`
- `crates/z00z_crypto/src/crypto_constants.rs`
- `crates/z00z_wallets/src/db/redb_wallet_crypto.rs`
- `crates/z00z_wallets/src/core/security/password.rs`
- `crates/z00z_wallets/src/core/key/seed_cipher_container.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs`
- `crates/z00z_crypto/src/aead_aad.rs`
- `crates/z00z_wallets/src/db/index_codecs.rs`
- `crates/z00z_wallets/src/db/schema_keys.rs`
- `crates/z00z_wallets/src/wasm/schema_keys.rs`
- `crates/z00z_crypto/src/claim/v2.rs`
- `crates/z00z_wallets/src/core/tx/claim_wire_types.rs`
- `crates/z00z_wallets/src/core/claim/claim_receipt.rs`
- `crates/z00z_wallets/src/core/tx/fee_estimator.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key.rs`
- `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs`

Tests:

- `crates/z00z_crypto/src/types_tests.rs`
- `crates/z00z_wallets/tests/test_kdf.rs`
- `crates/z00z_wallets/tests/test_stealth_kdf_vectors.rs`
- `crates/z00z_wallets/tests/test_claim_state_core.rs`
- `crates/z00z_wallets/tests/test_claim_state_compat.rs`
- `crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs`
- `crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs`
- `crates/z00z_wallets/tests/test_rpc_types_serialization.rs`
- `crates/z00z_wallets/tests/test_seed_salt_policy.rs`

Exit condition:

- every Step 0 row remains intentionally unchanged and its raw row ID still matches the spec appendix.

### 036-02 Legacy Wallet-KDF Retirement

Spec references:

- `Execution Order By Row Range` step 1
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 1 block in `Execution Order By Row Range`, then the non-test rows `8-17`, `24`, and `28-30` in the `Raw Inventory Appendix`.

- [x] Removed migration helpers `16-17`, removed compatibility rows `8`, `10`, `12`, `14`, `24`, `28-30`, and converged survivor rows `9`, `11`, `13`, `15` onto the live unsuffixed contracts.
- [x] Closed the wallet lane as current-only and fail-closed; no wallet-open or post-unlock path defaults to V1 metadata or retries `aad_secret_v1`.
- [x] Preserved the distinct provenance of rows `16` and `17` until both removal points were separately verified.
- [x] Rejected legacy wallet metadata instead of deferring survivor closure once caller-proof surfaces were reverified.

Files:

- `crates/z00z_wallets/src/db/redb_wallet_crypto.rs`
- `crates/z00z_wallets/src/core/key/key_manager_redb.rs`
- `crates/z00z_wallets/src/core/key/key_manager_redb_wallet.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs`
- `crates/z00z_wallets/src/core/hashing.rs`
- `crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs`
- `crates/z00z_wallets/src/db/redb_wallet_crypto_aad.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store_open_session.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store.rs`

Tests:

- `crates/z00z_wallets/tests/test_wallet_kdf_migration.rs`
- `crates/z00z_wallets/tests/test_kdf.rs`
- `crates/z00z_wallets/tests/test_stealth_kdf_vectors.rs`
- `crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs`

Exit condition:

- wallet KDF is current-only and fail-closed, with live unsuffixed markers `HKDF_INFO_VERSION`, `AAD_SECRET_VERSION`, `KdfParams::VERSION`, and `derive_wallet_keys`, and with no V1 compatibility or migration helpers remaining.

### 036-03 Legacy Backup-KDF Retirement

Spec references:

- `Execution Order By Row Range` step 2
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 2 block in `Execution Order By Row Range`, then the non-test rows `43-45` in the `Raw Inventory Appendix`.

- [x] Removed the legacy backup-KDF helpers in raw-row order and converged `default_v2` onto the canonical unsuffixed `default` constructor.
- [x] Verified exporter and importer paths no longer construct, accept, or fall back to `legacy_v1`.
- [x] Kept one canonical backup-KDF contract aligned with wallet `KdfParams` after legacy retirement.

Files:

- `crates/z00z_wallets/src/core/backup/wallet_backup_kdf.rs`
- `crates/z00z_wallets/src/core/backup/wallet_backup.rs`
- `crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs`
- `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs`

Tests:

- `crates/z00z_wallets/tests/test_backup_kdf_contract.rs`
- `crates/z00z_wallets/tests/test_backup_restore_identity.rs`
- `crates/z00z_wallets/tests/test_backup_metadata_policy.rs`
- `crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs`
- `crates/z00z_wallets/src/core/backup/backup_importer_tests.rs`
- `crates/z00z_wallets/src/core/backup/wallet_backup_tests.rs`
- `crates/z00z_wallets/src/services/wallet_service_tests.rs`

Exit condition:

- one live backup KDF constructor remains at `BackupKdf::default`, and no legacy fallback contract survives.

### 036-04 Legacy Backup Payload Import-Export Retirement

Spec references:

- `Execution Order By Row Range` step 3
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 3 block in `Execution Order By Row Range`, then the non-test rows `32-38` and `50-56` in the `Raw Inventory Appendix`.

- [x] Removed the AAD, decode, verify, and legacy payload compatibility rows in the raw-row order from the spec.
- [x] Converged `build_aad_bytes_v3`, `decode_export_pack_v4`, `BackupPayloadV4`, and `verify_export_pack_v4` onto the canonical unsuffixed live lanes.
- [x] Kept one backup payload model and one V4-only metadata contract across export, import, and verify.
- [x] Verified no backup path emits or accepts V1, V2, or V3 payload semantics after the delete wave.

Files:

- `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs`
- `crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs`
- `crates/z00z_wallets/src/core/backup/backup_exporter_verify.rs`
- `crates/z00z_wallets/src/core/backup/backup_wire.rs`

Tests:

- `crates/z00z_wallets/tests/test_backup_restore_identity.rs`
- `crates/z00z_wallets/tests/test_backup_metadata_policy.rs`
- `crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs`
- `crates/z00z_wallets/tests/test_wallet_persistence_no_std.rs`
- `crates/z00z_wallets/src/core/backup/backup_exporter_tests.rs`

Exit condition:

- backup import/export is V4-only and the live canonical lanes are `build_aad_bytes`, `decode_export_pack`, `BackupPayload`, and `verify_export_pack`.

### 036-05 Seed-Container Contract Migration Hold

Spec references:

- `Execution Order By Row Range` step 4
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 4 block in `Execution Order By Row Range`, then the non-test rows `18-19` in the `Raw Inventory Appendix`.

- [x] Keep the seed container version and AAD version untouched in this wave.
- [x] Do not rename the seed-container rows until an explicit persisted-format migration is approved.
- [x] Treat these rows as live contracts, not cleanup blockers.

Files:

- `crates/z00z_wallets/src/core/key/seed_cipher_container.rs`

Tests:

- `crates/z00z_wallets/tests/test_seed_salt_policy.rs`
- `crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs`

Exit condition:

- seed-container persisted format remains unchanged in this wave and the hold is explicit.

### 036-06 Receiver-Card Publication Migration Hold

Spec references:

- `Execution Order By Row Range` step 5
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 5 block in `Execution Order By Row Range`, then the non-test row `39` in the `Raw Inventory Appendix`.

- [x] Keep `ReceiverCardRecordV1` untouched.
- [x] Do not rename or delete the published receiver trust contract in this wave.
- [x] Preserve the distinction between raw routing transport and published record trust.

Files:

- `crates/z00z_wallets/src/core/chain/receiver_card_record.rs`

Tests:

- `crates/z00z_wallets/tests/test_receiver_card_record.rs`
- `crates/z00z_wallets/tests/test_receiver_secret_validation.rs`
- `crates/z00z_wallets/tests/test_e2e_public_path.rs`
- `crates/z00z_wallets/tests/test_live_path_enforcement.rs`

Exit condition:

- published receiver trust stays current and is not collapsed into a new layer.

### 036-07 Claim-V2 Protocol Surface Hold

Spec references:

- `Execution Order By Row Range` step 6
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 6 block in `Execution Order By Row Range`, then the non-test row `27` in the `Raw Inventory Appendix`.

- [x] Keep the outer claim-v2 wire surface live and do not infer a proof-version upgrade from it alone.
- [x] Preserve the current inner non-test proof version boundary until a separate protocol-wide wave exists.
- [x] Keep the outer proof tag and the inner proof semantics classified separately.

Files:

- `crates/z00z_crypto/src/claim/v2.rs`
- `crates/z00z_wallets/src/core/tx/claim_wire_types.rs`
- `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl.rs`
- `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`
- `crates/z00z_storage/src/assets/store_internal/store_query.rs`

Tests:

- `crates/z00z_storage/tests/test_claim_source_proof.rs`
- `crates/z00z_wallets/tests/test_claim_state_core.rs`
- `crates/z00z_wallets/tests/test_claim_state_compat.rs`
- `crates/z00z_wallets/tests/test_claim_core_safety.rs`
- `crates/z00z_wallets/tests/test_claim_import_reason_codes.rs`

Exit condition:

- the outer claim-v2 surface remains live while the inner proof-version boundary stays truthful.

### 036-08 Address-V2 Future Activation Hold

Spec references:

- `Execution Order By Row Range` step 7
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 7 block in `Execution Order By Row Range`, then the non-test rows `61-63` in the `Raw Inventory Appendix`.

- [x] Keep the address-v2 functions future-reserved.
- [x] Do not activate the address migration until the migration is real and explicitly approved.
- [x] Do not rename the rows in this wave.

Files:

- `crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs`

Tests:

- `crates/z00z_wallets/src/core/address/canonical_snapshot_tests.rs`
- `crates/z00z_wallets/src/core/address/stealth_scan_support_tests.rs`
- `crates/z00z_wallets/src/core/address/test_stealth_request.rs`
- `crates/z00z_wallets/src/core/address/test_stealth_card.rs`

Exit condition:

- address-v2 stays dormant and future-reserved in this wave.

### 036-09 Test-Only Row Review After Production Steps

Spec references:

- `Execution Order By Row Range` step 8
- `Raw Inventory Appendix -> Test-Only Signatures`
- `Table-to-Task Translation Protocol`

MANDATORY pre-read before implementation: re-read the step 8 block in `Execution Order By Row Range`, then the full `Raw Inventory Appendix -> Test-Only Signatures` table, then the test-row translation rules in `Table-to-Task Translation Protocol`.

- [x] Keep compatibility-proof tests that still prove live behavior.
- [x] Rename or retire only rows that are pure suffix noise after the linked production rows have stabilized.
- [x] Do not mix production cleanup into the test-only review track.
- [x] Preserve the literal action spelling from the raw appendix when a test row says `keep or rename to ...`.

Files:

- `crates/z00z_core/src/assets/leaf_tests.rs`
- `crates/z00z_core/tests/genesis/test_config.rs`
- `crates/z00z_wallets/tests/test_redb_wlt_open.rs`
- `crates/z00z_crypto/src/types_tests.rs`
- `crates/z00z_wallets/src/core/key/key_manager_redb_tests.rs`
- `crates/z00z_core/tests/assets/test_integration_assets_test24.rs`
- `crates/z00z_wallets/tests/test_wallet_kdf_migration.rs`
- `crates/z00z_wallets/tests/test_phase11_derivation.rs`
- `crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs`
- `crates/z00z_wallets/src/db/tests/redb_wallet_store.rs`
- `crates/z00z_wallets/src/core/stealth/facade_zkpack_tests.rs`
- `crates/z00z_wallets/src/core/stealth/facade_kdf.rs`
- `crates/z00z_crypto/src/claim/prover.rs`
- `crates/z00z_wallets/src/core/tx/fee_estimator_tests.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs`
- `crates/z00z_wallets/src/core/address/z00z_address/tests.rs`

Tests:

- the test-only row ledger below is the test surface for this task; keep each listed suite aligned with its raw row entry.

Exit condition:

- every test-only raw row has a resolved state kept, renamed, deferred, or retired with proof.

## Raw Row Task Ledger

This ledger is the normative mapping from raw row to task ID. One raw row equals one ledger entry. Keep the `Task ID` column aligned with the step task above and do not collapse identical signatures across different paths.

### Non-Test Signatures

| Task ID | Row | Signature | Type | Path | Current classification | Action now | Future survivor target | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `036-01` | 1 | `HKDF_INFO_REDB_DATA_V2` | const | `crates/z00z_crypto/src/kdf_domains.rs` | future-reserved | keep | `HKDF_INFO_REDB_DATA` | exported domain marker; current wallet-open code does not select this directly |
| `036-01` | 2 | `HKDF_INFO_REDB_INDEX_V2` | const | `crates/z00z_crypto/src/kdf_domains.rs` | future-reserved | keep | `HKDF_INFO_REDB_INDEX` | exported domain marker; current wallet-open code does not select this directly |
| `036-01` | 3 | `HKDF_INFO_REDB_INTEGRITY_V2` | const | `crates/z00z_crypto/src/kdf_domains.rs` | future-reserved | keep | `HKDF_INFO_REDB_INTEGRITY` | exported domain marker; current wallet-open code does not select this directly |
| `036-01` | 5 | `RANGE_PROOF_BITS_V2` | const | `crates/z00z_crypto/src/crypto_constants.rs` | future-reserved | keep | `RANGE_PROOF_BITS` | live non-test range-proof backend accepts only `RANGE_PROOF_BITS`; keep only if a future proof-width migration is approved |
| `036-01` | 7 | `MAX_PROOF_SIZE_V2` | const | `crates/z00z_crypto/src/crypto_constants.rs` | future-reserved | keep | `MAX_PROOF_SIZE` | version-2 helper dispatch exists in validation code, but no non-test production proof lane currently selects it as the active contract |
| `036-02` | 8 | `HKDF_INFO_VER_V1` | const | `crates/z00z_wallets/src/db/redb_wallet_crypto.rs` | removed | closed | `HKDF_INFO_VERSION` | retired in `036-02`; wallet open now rejects legacy V1 HKDF-info metadata |
| `036-02` | 9 | `HKDF_INFO_VER_V2` | const | `crates/z00z_wallets/src/db/redb_wallet_crypto.rs` | renamed survivor | closed | `HKDF_INFO_VERSION` | closed in `036-02`; the live current HKDF-info marker is now unsuffixed |
| `036-02` | 10 | `AAD_SECRET_VER_V1` | const | `crates/z00z_wallets/src/db/redb_wallet_crypto.rs` | removed | closed | `AAD_SECRET_VERSION` | retired in `036-02`; the secret-AAD path is now fail-closed on legacy metadata |
| `036-02` | 11 | `AAD_SECRET_VER_V2` | const | `crates/z00z_wallets/src/db/redb_wallet_crypto.rs` | renamed survivor | closed | `AAD_SECRET_VERSION` | closed in `036-02`; the live current secret-AAD marker is now unsuffixed |
| `036-02` | 12 | `VERSION_V1` | const | `crates/z00z_wallets/src/db/redb_wallet_crypto.rs` | removed | closed | `KdfParams::VERSION` | retired in `036-02`; V1 wallet KDF metadata is no longer accepted |
| `036-02` | 13 | `VERSION_V2` | const | `crates/z00z_wallets/src/db/redb_wallet_crypto.rs` | renamed survivor | closed | `KdfParams::VERSION` | closed in `036-02`; the current KDF version gate now lives on `KdfParams::VERSION` |
| `036-02` | 14 | `derive_wallet_keys_v1` | fn | `crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs` | removed | closed | `derive_wallet_keys` | retired in `036-02`; no V1 wallet-key derivation helper remains |
| `036-02` | 15 | `derive_wallet_keys_v2` | fn | `crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs` | renamed survivor | closed | `derive_wallet_keys` | closed in `036-02`; the current wallet-key derivation helper is unsuffixed |
| `036-02` | 16 | `migrate_kdf_v1_to_v2` | fn | `crates/z00z_wallets/src/core/key/key_manager_redb_wallet.rs` | removed | closed | none | retired in `036-02`; wallet open rejects legacy metadata instead of migrating it |
| `036-02` | 17 | `migrate_kdf_v1_to_v2` | fn | `crates/z00z_wallets/src/core/key/key_manager_redb.rs` | removed | closed | none | retired in `036-02`; same helper removal at the crate-local manager boundary |
| `036-05` | 18 | `VERSION_V1` | const | `crates/z00z_wallets/src/core/key/seed_cipher_container.rs` | production current | keep | `CipherSeedContainer` | current seed-container persisted version |
| `036-05` | 19 | `AAD_VERSION_V1` | const | `crates/z00z_wallets/src/core/key/seed_cipher_container.rs` | production current | keep | `AAD_VERSION` | current and only supported seed-container AAD version |
| `036-01` | 20 | `OBJECT_PAYLOAD_HEADER_VERSION_V1` | const | `crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs` | production current | keep | `OBJECT_PAYLOAD_HEADER_VERSION` | current encrypted object payload header version |
| `036-01` | 21 | `build_aad_v1` | fn | `crates/z00z_crypto/src/aead_aad.rs` | production current | keep | `build_aad` | current multipart AAD builder still funnels through this V1 framing helper |
| `036-01` | 22 | `TX_TIME_KEY_VERSION_V1` | const | `crates/z00z_wallets/src/db/index_codecs.rs` | production current | keep | `TX_TIME_KEY_VERSION` | current tx-time index key format |
| `036-01` | 23 | `SEMANTIC_KEY_VERSION_V1` | const | `crates/z00z_wallets/src/db/index_codecs.rs` | production current | keep | `SEMANTIC_KEY_VERSION` | current semantic index key format |
| `036-02` | 24 | `aad_secret_v1` | fn | `crates/z00z_wallets/src/db/redb_wallet_crypto_aad.rs` | removed | closed | `aad_secret` | retired in `036-02`; the current secret AAD helper is the unsuffixed `aad_secret` |
| `036-01` | 25 | `META_WALLET_INTEGRITY_V1` | const | `crates/z00z_wallets/src/db/schema_keys.rs` | production current | keep | `META_WALLET_INTEGRITY` | current wallet integrity meta key |
| `036-01` | 26 | `META_WALLET_INTEGRITY_V1` | const | `crates/z00z_wallets/src/wasm/schema_keys.rs` | production current | keep | `META_WALLET_INTEGRITY` | same contract exposed through wasm |
| `036-07` | 27 | `claim_stmt_hash_v2` | fn | `crates/z00z_crypto/src/claim/v2.rs` | production current | keep | `claim_stmt_hash` | current hash helper for live claim-v2 signing surface; outer claim-v2 wire is live even though current non-test inner claim source proof version remains `ClaimProofVer::V1` |
| `036-02` | 28 | `redb_hkdf_info_data_v1` | fn | `crates/z00z_wallets/src/core/hashing.rs` | removed | closed | `redb_wallet_hkdf_info_data` | retired in `036-02`; the live wallet data HKDF helper is unsuffixed |
| `036-02` | 29 | `redb_hkdf_info_index_v1` | fn | `crates/z00z_wallets/src/core/hashing.rs` | removed | closed | `redb_wallet_hkdf_info_index` | retired in `036-02`; the live wallet index HKDF helper is unsuffixed |
| `036-02` | 30 | `redb_hkdf_info_integrity_v1` | fn | `crates/z00z_wallets/src/core/hashing.rs` | removed | closed | `redb_wallet_hkdf_info_integrity` | retired in `036-02`; the live wallet integrity HKDF helper is unsuffixed |
| `036-01` | 31 | `DENYLIST_BLOOM_V1` | const | `crates/z00z_wallets/src/core/security/password.rs` | production current | keep | `DENYLIST_BLOOM` | current password denylist bloom payload |
| `036-04` | 32 | `build_aad_bytes_v1` | fn | `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs` | removed | closed | `build_aad_bytes` | retired in `036-04`; importer and exporter no longer carry legacy AAD framing |
| `036-04` | 33 | `build_aad_bytes_v2` | fn | `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs` | removed | closed | `build_aad_bytes` | retired in `036-04`; the intermediate backup AAD lane is gone |
| `036-04` | 34 | `build_aad_bytes_v3` | fn | `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs` | renamed survivor | closed | `build_aad_bytes` | closed in `036-04`; the canonical nonce-stripped backup AAD builder is now unsuffixed |
| `036-04` | 35 | `decode_export_pack_v4` | fn | `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs` | renamed survivor | closed | `decode_export_pack` | closed in `036-04`; the canonical V4-only export-pack decoder is now unsuffixed |
| `036-04` | 36 | `decode_export_pack_v3` | fn | `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs` | removed | closed | `decode_export_pack` | retired in `036-04`; older export-pack decode compatibility was removed |
| `036-04` | 37 | `decode_export_pack_v2` | fn | `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs` | removed | closed | `decode_export_pack` | retired in `036-04`; older export-pack decode compatibility was removed |
| `036-04` | 38 | `decode_legacy_payload_v1` | fn | `crates/z00z_wallets/src/core/backup/backup_importer_impl.rs` | removed | closed | none | retired in `036-04`; legacy payload import was removed |
| `036-06` | 39 | `ReceiverCardRecordV1` | struct | `crates/z00z_wallets/src/core/chain/receiver_card_record.rs` | production current | keep | `ReceiverCardRecord` | current receiver-card publication contract |
| `036-01` | 40 | `CLAIM_PROOF_V2` | const | `crates/z00z_wallets/src/core/tx/claim_wire_types.rs` | production current | keep | `CLAIM_PROOF` | current canonical outer proof-type tag for claim tx; do not read this alone as proof that the inner `ClaimSourceProof` version lane is already V2 |
| `036-01` | 41 | `CLAIM_SCHEMA_V1` | const | `crates/z00z_wallets/src/core/claim/claim_receipt.rs` | production current | keep | `CLAIM_SCHEMA` | current claim receipt schema |
| `036-01` | 42 | `FEE_WGT_VER_V1` | const | `crates/z00z_wallets/src/core/tx/fee_estimator.rs` | production current | keep | `FEE_WGT_VER` | current fee weight model tag |
| `036-03` | 43 | `default_v2` | fn | `crates/z00z_wallets/src/core/backup/wallet_backup_kdf.rs` | renamed survivor | closed | `default` | closed in `036-03`; the canonical backup KDF constructor is now unsuffixed |
| `036-03` | 44 | `legacy_v1` | fn | `crates/z00z_wallets/src/core/backup/wallet_backup_kdf.rs` | removed | closed | none | retired in `036-03`; the explicit legacy backup KDF contract was removed |
| `036-03` | 45 | `derive_key_legacy_v1` | fn | `crates/z00z_wallets/src/core/backup/wallet_backup.rs` | removed | closed | `derive_key_with_kdf` | retired in `036-03`; backup key derivation now flows through `WalletBackupCrypto::derive_key_with_kdf` |
| `036-04` | 50 | `BackupPayloadV1` | struct | `crates/z00z_wallets/src/core/backup/backup_wire.rs` | removed | closed | `BackupPayload` | retired in `036-04`; legacy backup payload struct removed |
| `036-04` | 51 | `BackupPayloadV3` | struct | `crates/z00z_wallets/src/core/backup/backup_wire.rs` | removed | closed | `BackupPayload` | retired in `036-04`; intermediate backup payload struct removed |
| `036-04` | 52 | `BackupPayloadV4` | struct | `crates/z00z_wallets/src/core/backup/backup_wire.rs` | renamed survivor | closed | `BackupPayload` | closed in `036-04`; the canonical backup payload struct is now unsuffixed |
| `036-04` | 53 | `verify_export_pack_v4` | fn | `crates/z00z_wallets/src/core/backup/backup_exporter_verify.rs` | renamed survivor | closed | `verify_export_pack` | closed in `036-04`; the canonical export-pack verify lane is now unsuffixed |
| `036-04` | 54 | `verify_export_pack_v3` | fn | `crates/z00z_wallets/src/core/backup/backup_exporter_verify.rs` | removed | closed | `verify_export_pack` | retired in `036-04`; older export-pack verify compatibility was removed |
| `036-04` | 55 | `verify_export_pack_v2` | fn | `crates/z00z_wallets/src/core/backup/backup_exporter_verify.rs` | removed | closed | `verify_export_pack` | retired in `036-04`; older export-pack verify compatibility was removed |
| `036-04` | 56 | `verify_legacy_payload_v1` | fn | `crates/z00z_wallets/src/core/backup/backup_exporter_verify.rs` | removed | closed | none | retired in `036-04`; legacy payload verify was removed |
| `036-01` | 57 | `TxStoreMetaV1` | struct | `crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs` | production current | keep | `TxStoreMeta` | current RPC storage metadata |
| `036-01` | 58 | `export_public_material_v2` | async fn | `crates/z00z_wallets/src/adapters/rpc/methods/key.rs` | production current | keep | `export_public_material` | current RPC surface |
| `036-01` | 59 | `wallet.key.export_public_material_v2` | RPC string | `crates/z00z_wallets/src/adapters/rpc/methods/key.rs` | production current | keep | `wallet.key.export_public_material` | current RPC method string |
| `036-01` | 60 | `export_public_material_v2` | async fn | `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs` | production current | keep | `export_public_material` | server-side implementation of the current RPC surface |
| `036-08` | 61 | `encode_single_v2` | fn | `crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs` | future-reserved | keep | `encode_single` | not yet active, reserved for a later migration |
| `036-08` | 62 | `encode_dual_v2` | fn | `crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs` | future-reserved | keep | `encode_dual` | not yet active, reserved for a later migration |
| `036-08` | 63 | `decode_v2` | fn | `crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs` | future-reserved | keep | `decode` | not yet active, reserved for a later migration |

### Test-Only Signatures

| Task ID | Row | Signature | Type | Path | Current classification | Action now | Future survivor target | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `036-09` | 1 | `test_value_endian` | fn | `crates/z00z_core/src/assets/leaf_tests.rs` | test residue | keep | same | renamed from `test_value_endian_v1` during `036-09` suffix-noise cleanup |
| `036-09` | 2 | `test_offsets` | fn | `crates/z00z_core/src/assets/leaf_tests.rs` | test residue | keep | same | renamed from `test_offsets_v1` during `036-09` suffix-noise cleanup |
| `036-09` | 3 | `test_rejects_seed_genesis_v3` | fn | `crates/z00z_core/tests/genesis/test_config.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 4 | `test_rejects_seed_genesis_v2` | fn | `crates/z00z_core/tests/genesis/test_config.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 5 | `test_open_rejects_kdf_v1` | fn | `crates/z00z_wallets/tests/test_redb_wlt_open.rs` | test residue | keep | same | fail-closed legacy wallet-open test after migration retirement |
| `036-09` | 6 | `test_proof_size_validation_v1` | fn | `crates/z00z_crypto/src/types_tests.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 7 | `test_proof_size_validation_v2` | fn | `crates/z00z_crypto/src/types_tests.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 8 | `unwrap_rejects_non_current_record_params` | fn | `crates/z00z_wallets/src/core/key/key_manager_redb_tests.rs` | test residue | keep | same | fail-closed persisted-params rejection test after migration retirement |
| `036-09` | 9 | `test_asset_cross_mixed_v2` | fn | `crates/z00z_core/tests/assets/test_integration_assets_test24.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 10 | `rewrite_v1` | fn | `crates/z00z_wallets/tests/test_wallet_kdf_migration.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 11 | `test_chain_vector_v1` | fn | `crates/z00z_wallets/tests/test_phase11_derivation.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 12 | `test_sender_fix_v1` | fn | `crates/z00z_wallets/tests/test_phase11_derivation.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 13 | `test_receiver_fix_v1` | fn | `crates/z00z_wallets/tests/test_phase11_derivation.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 14 | `test_api_parity_v1` | fn | `crates/z00z_wallets/tests/test_phase11_derivation.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 15 | `test_domain_mismatch_v1` | fn | `crates/z00z_wallets/tests/test_phase11_derivation.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 16 | `test_migration_vec_v1` | fn | `crates/z00z_wallets/tests/test_phase11_derivation.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 17 | `test_rejects_params_untrusted_v2` | fn | `crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 18 | `test_rejects_params_untrusted_v3` | fn | `crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 19 | `test_rejects_params_untrusted_v4` | fn | `crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 20 | `test_rejects_wlt_open_v2` | fn | `crates/z00z_wallets/src/db/tests/redb_wallet_store.rs` | test residue with misleading V2 noise | rename | `test_rejects_wlt_open_invalid_save_seq` | the test mutates `META_WALLET_SAVE_SEQ`, so the current name overstates version semantics |
| `036-09` | 21 | `test_golden_yaml_v1` | fn | `crates/z00z_wallets/src/core/stealth/facade_zkpack_tests.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 22 | `test_key_golden_v1` | fn | `crates/z00z_wallets/src/core/stealth/facade_kdf.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 23 | `test_nonce_golden_v1` | fn | `crates/z00z_wallets/src/core/stealth/facade_kdf.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 24 | `test_prove_claim_v1` | fn | `crates/z00z_crypto/src/claim/prover.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 25 | `test_weight_fixture_v1` | fn | `crates/z00z_wallets/src/core/tx/fee_estimator_tests.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 26 | `test_export_public_material_v2` | async fn | `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 27 | `test_encode_single_v2` | fn | `crates/z00z_wallets/src/core/address/z00z_address/tests.rs` | test residue | keep | same | test-only versioned case |
| `036-09` | 28 | `test_encode_dual_v2` | fn | `crates/z00z_wallets/src/core/address/z00z_address/tests.rs` | test residue | keep | same | test-only versioned case |

Row ledger ends here.
