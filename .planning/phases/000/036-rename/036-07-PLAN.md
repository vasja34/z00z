---
phase: 036-rename
plan: 7
type: execute
wave: 4
depends_on: [036-06]
files_modified:
  - crates/z00z_crypto/src/aead_aad.rs
  - crates/z00z_wallets/src/db/index_codecs.rs
  - crates/z00z_wallets/src/db/index_codecs_tx_time.rs
  - crates/z00z_wallets/src/db/index_codecs_body.rs
  - crates/z00z_wallets/src/db/schema_keys.rs
  - crates/z00z_wallets/src/wasm/schema_keys.rs
  - crates/z00z_wallets/src/db/wallet_validate.rs
  - crates/z00z_wallets/src/db/redb_wallet_store.rs
  - crates/z00z_wallets/src/core/key/seed_cipher_container.rs
  - crates/z00z_wallets/src/core/key/seed_cipher_container_crypto.rs
  - crates/z00z_wallets/src/core/key/seed_cipher_persistence.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs
  - crates/z00z_wallets/src/core/claim/claim_receipt.rs
  - crates/z00z_wallets/src/core/tx/fee_estimator.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
  - crates/z00z_wallets/src/core/security/password.rs
  - crates/z00z_wallets/src/core/security/password_checks.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Step 3 renames change Rust identifiers only, not encoded bytes, strings, or persisted numeric values.
    - Storage, schema-key, receipt, fee, and password surfaces still decode and emit the same wire or persisted contracts after the rename pass.
    - Compiler and call-site reconciliation happens per rename cluster instead of speculative batch edits.
  artifacts:
    - path: crates/z00z_crypto/src/aead_aad.rs
      provides: unsuffixed AAD helper name with unchanged `z00z.aead.v1\0` framing.
    - path: crates/z00z_wallets/src/core/key/seed_cipher_container.rs
      provides: unsuffixed version constants with unchanged payload bytes.
    - path: crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
      provides: unsuffixed internal metadata type with unchanged decode shape.
  key_links:
    - from: crates/z00z_wallets/src/db/schema_keys.rs
      to: crates/z00z_wallets/src/wasm/schema_keys.rs
      via: shared `wallet.integrity.v1` literal contract
      pattern: constant identifier renames must not change the literal value
    - from: crates/z00z_wallets/src/core/claim/claim_receipt.rs
      to: crates/z00z_wallets/src/core/tx/fee_estimator.rs
      via: Step 3 single-version identifier cleanup
      pattern: Rust symbol simplification only; schema byte and `fee-weight-v1` payload stay unchanged
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-07 PLAN

<objective>
Execute the Step 3 single-version identifier rename slice from the canonical Phase 036 backlog.

Purpose: simplify current-only internal and persisted identifier names while keeping every encoded byte, literal string, schema content, and stored numeric value unchanged.
Output: `036-04 Rename Current Single-Version Internal And Persisted Identifiers Without Changing Encoded Values` is closed on the exact Step 3 raw-row set, with compiler and call-site reconciliation complete after each rename cluster.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-2.md
@.planning/phases/036-rename/036-a1-versioning-spec.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Preserve the exact task wording from `036-TODO-2.md`; do not rename the task or widen its ownership beyond Step 3 rows `36-43`, `45-47`, and `52`.
- Honor D-01 through D-20 from `036-CONTEXT.md`, especially D-03, D-08, D-11, D-12, D-15, D-16, and D-18.
- The raw inventory is the only task-generation surface. If execution finds a missed caller or persisted seam for one of the Step 3 rows, update `036-a1-versioning-spec.md` first, then `036-TODO-2.md`, and only then resume code edits.
- Treat the `Compact Patch-Plan By Disputed Row Group` as a grouping aid only; if it ever disagrees with a raw row, the raw row wins.
- Before any edit, complete `read_first` and revalidate that the task-local `files`, `required_test_surfaces`, `acceptance_criteria`, `verify`, and `done` blocks still match the canonical raw-row ownership in `036-a1-versioning-spec.md` and `036-TODO-2.md`.
- If execution edits `036-a1-versioning-spec.md` or `036-TODO-2.md`, stop the current wave immediately and rerun review plus planning before any further code edits, closure checks, or summary claims.
- In every auto-task verify block, run validation in this order: bootstrap gate first, then `cargo test --release --features test-fast --features wallet_debug_dump`, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun the deterministic gates after each fix cycle, and do not stop the review loop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-04 Rename Current Single-Version Internal And Persisted Identifiers Without Changing Encoded Values</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-01 through D-20 and the `Task Transfer Matrix` row for `036-04`.
    - `.planning/phases/036-rename/036-TODO-2.md` — the full `036-04` section, `Row-Step Coverage Ledger`, and `Completion Gate`.
    - `.planning/phases/036-rename/036-a1-versioning-spec.md` — `Execution Order By Row Range`, `Implementation Examples`, `Raw Inventory Appendix -> Non-Test Signatures`, and `Execution Step Completion Rules`.
  </read_first>
  <files>
    crates/z00z_crypto/src/aead_aad.rs
    crates/z00z_wallets/src/db/index_codecs.rs
    crates/z00z_wallets/src/db/index_codecs_tx_time.rs
    crates/z00z_wallets/src/db/index_codecs_body.rs
    crates/z00z_wallets/src/db/schema_keys.rs
    crates/z00z_wallets/src/wasm/schema_keys.rs
    crates/z00z_wallets/src/db/wallet_validate.rs
    crates/z00z_wallets/src/db/redb_wallet_store.rs
    crates/z00z_wallets/src/core/key/seed_cipher_container.rs
    crates/z00z_wallets/src/core/key/seed_cipher_container_crypto.rs
    crates/z00z_wallets/src/core/key/seed_cipher_persistence.rs
    crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs
    crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs
    crates/z00z_wallets/src/core/claim/claim_receipt.rs
    crates/z00z_wallets/src/core/tx/fee_estimator.rs
    crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
    crates/z00z_wallets/src/core/security/password.rs
    crates/z00z_wallets/src/core/security/password_checks.rs
  </files>
  <action>Execute `036-04 Rename Current Single-Version Internal And Persisted Identifiers Without Changing Encoded Values` exactly as written in `036-TODO-2.md`. Rename only Step 3 rows `36-43`, `45-47`, and `52` at the Rust identifier layer: `build_aad_v1` to `build_aad`; `TX_TIME_KEY_VERSION_V1` to `TX_TIME_KEY_VERSION`; `SEMANTIC_KEY_VERSION_V1` to `SEMANTIC_KEY_VERSION`; both `META_WALLET_INTEGRITY_V1` constants to `META_WALLET_INTEGRITY`; `VERSION_V1` to `VERSION`; `AAD_VERSION_V1` to `AAD_VERSION`; `OBJECT_PAYLOAD_HEADER_VERSION_V1` to `OBJECT_PAYLOAD_HEADER_VERSION`; `CLAIM_SCHEMA_V1` to `CLAIM_SCHEMA`; `FEE_WGT_VER_V1` to `FEE_WGT_VER`; `TxStoreMetaV1` to `TxStoreMeta`; and `DENYLIST_BLOOM_V1` to `DENYLIST_BLOOM`. Keep the underlying value contract byte-for-byte unchanged, including `wallet.integrity.v1`, schema byte `1`, AAD framing bytes, stored numeric versions, receipt schema bytes, and `fee-weight-v1`. After the helper rename, `build_aad` remains the current alias for the existing v1-framed AAD contract only; any future AAD lane must introduce a new helper and a new domain value rather than overloading this helper. Reconcile compiler and call sites after each rename cluster instead of batching speculative follow-up edits, including the direct consumers in `seed_cipher_container_crypto.rs` and `seed_cipher_persistence.rs`, and reject any rename that would alter encoded bytes, schema strings, literal payloads, or persisted numeric values.</action>
  <required_test_surfaces>
    crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
    crates/z00z_wallets/tests/test_redb_wlt_open.rs
    crates/z00z_wallets/tests/test_tx_fee.rs
    crates/z00z_wallets/src/core/tx/fee_estimator_tests.rs
    crates/z00z_wallets/src/core/key/seed_cipher_metadata_tests.rs
    crates/z00z_wallets/src/core/key/seed_cipher_basic_tests.rs
    crates/z00z_wallets/tests/test_key_manager.rs
    crates/z00z_crypto/src/types_tests.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - Every Step 3 row now uses the unsuffixed Rust identifier required by the canonical spec.
    - All underlying encoded, persisted, and literal contracts remain unchanged byte-for-byte.
    - Native and wasm schema-key mirrors stay value-identical, and `build_aad` remains explicitly a v1-only helper alias rather than a version-neutral helper.
    - Storage readers, schema decoders, and exported payloads still observe the same wire or persisted values after the rename pass.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --test test_redb_wlt_open</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --test test_tx_fee</automated>
    <automated>rg -n "build_aad_v1|TX_TIME_KEY_VERSION_V1|SEMANTIC_KEY_VERSION_V1|META_WALLET_INTEGRITY_V1|VERSION_V1|AAD_VERSION_V1|OBJECT_PAYLOAD_HEADER_VERSION_V1|CLAIM_SCHEMA_V1|FEE_WGT_VER_V1|TxStoreMetaV1|DENYLIST_BLOOM_V1|wallet\.integrity\.v1|fee-weight-v1" crates/z00z_crypto/src crates/z00z_wallets/src crates/z00z_wallets/tests</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified by deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic gates; continue additional review or fix cycles until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Step 3 single-version identifiers are simplified at the Rust-symbol layer only, with unchanged encoded bytes, persisted values, and literal payloads across all affected seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust identifier rename -> persisted or encoded contract | Step 3 renames must never mutate the stored or emitted payload. |
| shared schema-key constants -> native and wasm consumers | Constant renames must not desynchronize native and wasm value contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-07-01 | T | Step 3 constants and helpers | mitigate | Rename only the Rust identifier layer and recheck unchanged literal bytes, schema strings, and numeric values after each cluster. |
| T-036-07-02 | I | native or wasm schema key mirrors | mitigate | Keep both `wallet.integrity.v1` mirrors byte-identical while renaming the constant identifiers only. |
| T-036-07-03 | R | fee or receipt value contracts | mitigate | Preserve schema byte `1`, receipt contract bytes, and `fee-weight-v1` while rerunning receipt and fee tests after the rename pass. |
</threat_model>

<verification>
The executor must finish this plan with all Step 3 symbol renames complete, all call sites reconciled, and every underlying literal, byte, and persisted value unchanged.
</verification>

<success_criteria>
`036-04 Rename Current Single-Version Internal And Persisted Identifiers Without Changing Encoded Values` is closed exactly on the Step 3 raw-row set, with unsuffixed Rust identifiers and unchanged encoded or persisted contracts.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-07-SUMMARY.md`.
</output>