---
phase: 036-rename
plan: 8
type: execute
wave: 5
depends_on: [036-07]
files_modified:
  - crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto.rs
  - crates/z00z_wallets/src/core/chain/receiver_card_record.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Step 4 rows remain explicitly blocked by paired legacy-lane or outward-contract proof requirements.
    - No speculative rename is emitted for `derive_key_v2_zero_padding` or `ReceiverCardRecordV1`.
    - Any future simplification is recorded as missing migration proof, not implemented here.
  artifacts:
    - path: crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
      provides: paired legacy-lane helper held under explicit contract block.
    - path: crates/z00z_wallets/src/core/chain/receiver_card_record.rs
      provides: published receiver-card record held under outward-contract block.
  key_links:
    - from: crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
      to: crates/z00z_wallets/tests/test_redb_wlt_open.rs
      via: legacy-lane pairing evidence
      pattern: derive_key_v2_zero_padding remains blocked until paired-lane migration proof exists
    - from: crates/z00z_wallets/src/core/chain/receiver_card_record.rs
      to: crates/z00z_wallets/tests/test_receiver_card_record.rs
      via: published receiver-card contract coverage
      pattern: ReceiverCardRecordV1 remains explicit until outward publication migration is approved
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-08 PLAN

<objective>
Execute the Step 4 outward-contract hold slice from the canonical Phase 036 backlog.

Purpose: keep the paired legacy-lane helper and published receiver-card contract explicit until migration proof exists, rather than flattening them during the rename cleanup.
Output: `036-05 Hold Paired Legacy/Public Outward Contracts` is closed with rows `19` and `44` held in place and no speculative rename work emitted.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-2.md
@.planning/phases/036-rename/036-a1-versioning-spec.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Preserve the exact task wording from `036-TODO-2.md`; do not rename the task or widen its ownership beyond Step 4 rows `19` and `44`.
- Honor D-01 through D-20 from `036-CONTEXT.md`, especially D-03, D-09, D-11, D-12, D-15, D-16, and D-17.
- The raw inventory is the only task-generation surface. If execution finds a new paired-lane or outward-contract dependency, update `036-a1-versioning-spec.md` first, then `036-TODO-2.md`, and only then resume code edits.
- Treat the `Compact Patch-Plan By Disputed Row Group` as a grouping aid only; if it ever disagrees with a raw row, the raw row wins.
- Before any edit, complete `read_first` and revalidate that the task-local `files`, `required_test_surfaces`, `acceptance_criteria`, `verify`, and `done` blocks still match the canonical raw-row ownership in `036-a1-versioning-spec.md` and `036-TODO-2.md`.
- If execution edits `036-a1-versioning-spec.md` or `036-TODO-2.md`, stop the current wave immediately and rerun review plus planning before any further code edits, closure checks, or summary claims.
- In every auto-task verify block, run validation in this order: bootstrap gate first, then `cargo test --release --features test-fast --features wallet_debug_dump`, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun the deterministic gates after each fix cycle, and do not stop the review loop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-05 Hold Paired Legacy/Public Outward Contracts</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-01 through D-20 and the `Task Transfer Matrix` row for `036-05`.
    - `.planning/phases/036-rename/036-TODO-2.md` — the full `036-05` section, `Row-Step Coverage Ledger`, and `Completion Gate`.
    - `.planning/phases/036-rename/036-a1-versioning-spec.md` — `Execution Order By Row Range`, `Raw Inventory Appendix -> Non-Test Signatures`, and `Execution Step Completion Rules`.
  </read_first>
  <files>
    crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
    crates/z00z_wallets/src/db/redb_wallet_crypto.rs
    crates/z00z_wallets/src/core/chain/receiver_card_record.rs
  </files>
  <action>Execute `036-05 Hold Paired Legacy/Public Outward Contracts` exactly as written in `036-TODO-2.md`. Hold non-test rows `19` and `44` as blocked-by-contract rows with no speculative rename work. Preserve the distinction between the paired legacy-lane helper `derive_key_v2_zero_padding` and the published outward receiver-card contract `ReceiverCardRecordV1`. If future simplification is desired, record the missing paired-lane or publication migration proof instead of editing the symbols in this wave. Holding `derive_key_v2_zero_padding` as a symbol does not authorize any legacy backup acceptance path below the current metadata-version gate; legacy acceptance remains blocked unless a future migration proof explicitly changes that contract. Do not rename either symbol, do not delete either symbol, and do not collapse receiver-card publication semantics into internal transport or routing abstractions.</action>
  <required_test_surfaces>
    crates/z00z_wallets/tests/test_receiver_card_record.rs
    crates/z00z_wallets/tests/test_redb_wlt_open.rs
    crates/z00z_wallets/tests/test_s5_record_gate.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - Both Step 4 rows remain explicitly held as contract-blocked items.
    - No rename or delete work is emitted for `derive_key_v2_zero_padding` or `ReceiverCardRecordV1`.
    - Symbol retention does not relax current metadata-version rejection or otherwise reopen legacy acceptance semantics.
    - Any future simplification need is recorded as missing migration proof, not as hidden execution inside this plan.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --test test_receiver_card_record</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --test test_redb_wlt_open</automated>
    <automated>rg -n "derive_key_v2_zero_padding|ReceiverCardRecordV1" crates/z00z_wallets/src crates/z00z_wallets/tests crates/z00z_simulator/src</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified by deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic gates; continue additional review or fix cycles until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Rows `19` and `44` remain explicit hold-only contracts with no speculative rename work and with migration-proof requirements recorded instead of executed prematurely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paired legacy helper -> cleanup pass | Premature simplification can erase active migration semantics. |
| published receiver-card contract -> internal refactor | Renaming the outward contract would break published trust expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-08-01 | T | derive_key_v2_zero_padding pairing semantics | mitigate | Keep the helper explicit and record missing paired-lane migration proof instead of renaming it in this wave. |
| T-036-08-02 | I | ReceiverCardRecordV1 publication contract | mitigate | Hold the published receiver-card type unchanged and rerun outward-contract tests before closing the plan. |
| T-036-08-03 | E | receiver-card trust boundary | mitigate | Forbid collapsing published receiver-card semantics into internal routing or transport abstractions during cleanup. |
</threat_model>

<verification>
The executor must finish this plan with both Step 4 rows held unchanged and with no hidden rename or delete work emitted for contract-blocked items.
</verification>

<success_criteria>
`036-05 Hold Paired Legacy/Public Outward Contracts` is closed exactly on rows `19` and `44`, with both contracts still explicit and blocked on future migration proof instead of cleanup speculation.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-08-SUMMARY.md`.
</output>