---
phase: 036-rename
plan: 21
type: execute
wave: 10
depends_on: [036-20]
files_modified:
  - crates/z00z_utils/examples/file_io.rs
  - crates/z00z_storage/src/assets/store_internal/tx_plan.rs
  - crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs
  - crates/z00z_wallets/src/db/redb_wallet_store.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_open.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier.rs
  - crates/z00z_wallets/src/core/security/encryption.rs
  - crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
  - crates/z00z_storage/benches/common/fixture.rs
  - crates/z00z_storage/benches/common/output.rs
  - crates/z00z_wallets/src/core/wallet/chain_id.rs
  - crates/z00z_wallets/src/services/wallet_service_session_derivation.rs
  - crates/z00z_wallets/src/services/wallet_service_session_snapshot.rs
  - crates/z00z_wallets/src/services/wallet_service_store_persistence_pack.rs
  - crates/z00z_wallets/src/services/wallet_service_store_load_restore.rs
  - crates/z00z_core/src/genesis/genesis_output_support.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs
  - crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx.rs
  - crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs
  - crates/z00z_wallets/src/core/wallet/snapshot_impl.rs
  - .planning/phases/036-rename/036-21-SUMMARY.md
autonomous: true
requirements: [None]
must_haves:
  truths:
    - "Every row in the embedded Phase 036 attribute matrix is reconciled explicitly: REMOVE rows are gone, NARROW rows are reduced to the smallest justified scope, REFACTOR rows no longer need the attribute, and blank rows remain intentionally unchanged."
    - "Wallet persistence, snapshot creation, RPC registration, simulator stage flow, and genesis reporting still preserve their current runtime behavior after the attribute-driven refactors."
    - "The closeout summary proves the full attribute inventory, including KEEP AS-IS rows, without collapsing the still-partial shim-removal truth already recorded in 036-20-SUMMARY.md."
  artifacts:
    - path: .planning/phases/036-rename/036-21-PLAN.md
      provides: Self-contained execution contract for the attribute-remediation continuation rooted in 036-attrib-spec.md.
    - path: .planning/phases/036-rename/036-21-SUMMARY.md
      provides: Truth-based execution summary covering every attribute table row and the final residual scan.
    - path: .planning/phases/036-rename/036-attrib-spec.md
      provides: Canonical source table for all REMOVE, NARROW, REFACTOR, and KEEP AS-IS dispositions.
  key_links:
    - from: .planning/phases/036-rename/036-attrib-spec.md
      to: crates/z00z_wallets/src/services/wallet_service_session_derivation.rs
      via: unreachable_code refactor requirement for cfg-separated wasm32 and non-wasm persistence flow
      pattern: eliminate the trailing unreachable Ok(()) without changing the fail-closed wasm32 contract
    - from: .planning/phases/036-rename/036-attrib-spec.md
      to: crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs
      via: too_many_arguments refactor requirement for stage-4 runtime packaging and persistence helpers
      pattern: split argument bundles or context structs instead of keeping a broad allow attribute
    - from: .planning/phases/036-rename/036-attrib-spec.md
      to: crates/z00z_wallets/src/core/wallet/snapshot_impl.rs
      via: too_many_arguments refactor requirement for snapshot constructors
      pattern: preserve snapshot semantics while collapsing constructor arity behind typed inputs or builder-style assembly
---

# 036-21 PLAN

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Execute `036-21 Reconcile The Phase 036 Attribute Audit Recommendations Without Semantic Drift`.

Purpose: use `.planning/phases/036-rename/036-attrib-spec.md` as the authoritative, self-contained source for the next Phase 036 cleanup wave so the remaining `#[allow(...)]` surface is either removed, narrowed, or refactored with repository-backed proof instead of silent tolerance.
Output: attribute-backed cleanup and behavior-preserving refactors across utils, storage, simulator, core, and wallet files plus a truthful `036-21-SUMMARY.md` that accounts for every row in the embedded tables below.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
@.github/skills/smart-tests-bootstrap/SKILL.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-20-SUMMARY.md
@.planning/phases/036-rename/036-attrib-spec.md

## Embedded Attribute Reference Tables

The inventory below is embedded from `.planning/phases/036-rename/036-attrib-spec.md` so this plan stays self-sufficient.

The `Comments` column records the verified 036-21 execution closeout state; `✅` means the row was reconciled during this plan's execution.

### Table Legend

| Value in `Final recommendation` | Meaning |
| --- | --- |
| `REMOVE` | Safe to remove the attribute now. |
| `REPLACE` | Replace with narrower code or test or config change. |
| `REFACTOR` | Keep behavior, but restructure code so the attribute can be removed. |
| `NARROW` | Keep the exception, but reduce its scope. |
| *(blank)* | `KEEP AS-IS` |

### `#[allow(unused_imports)]`

Default evaluation rule: blank `Final recommendation` means `KEEP AS-IS`.

| File | Line | Scoped item | Final recommendation | Comments |
| --- | --- | --- | --- | --- |
| `crates/z00z_utils/examples/file_io.rs` | 11 | `use serde::{Deserialize, Serialize};` | `REMOVE` | `✅` |
| `crates/z00z_storage/src/assets/store_internal/tx_plan.rs` | 1 | `use std::{ collections::{BTreeMap, BTreeSet, HashMap}, env };` | `REMOVE` | `✅` |
| `crates/z00z_storage/src/assets/store_internal/tx_plan.rs` | 21 | `use self::tx_plan_types::{ NextState, PlanMode, PlanScope, SeenOps, SerialItem, ShardItem, ShardKey, ShardPlan, StoreSnap, Touch };` | `REMOVE` | `✅` |
| `crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs` | 59 | `pub(crate) use self::exec_input_builder::{ build_cp_proof, build_exec_input, checkpoint_from_draft, draft_link, exec_out_from_wire, in_ref_from_input };` | `NARROW` | `✅` |
| `crates/z00z_wallets/src/db/redb_wallet_store.rs` | 108 | `use self::open::validate_objects_on_open;` | `REMOVE` | `✅` |
| `crates/z00z_wallets/src/db/redb_wallet_store_open.rs` | 11 | `pub(crate) use self::open_session::validate_objects_on_open;` | `REMOVE` | `✅` |
| `crates/z00z_wallets/src/core/tx/tx_verifier.rs` | 6 | `pub(crate) use super::tx_wire_types::{ default_regular_package_type, default_regular_tx_type, REGULAR_TX_PACKAGE_TYPE, REGULAR_TX_TYPE, TX_PACKAGE_KIND };` | `NARROW` | `✅` |

### `#[allow(deprecated)]`

| File | Line | Scoped item | Final recommendation | Comments |
| --- | --- | --- | --- | --- |
| `crates/z00z_wallets/src/core/security/encryption.rs` | 175 | `let params = Argon2Params::test_fast();` | `REMOVE` | `✅` |

### `#[allow(dead_code)]`

| File | Line | Scoped item | Final recommendation | Comments |
| --- | --- | --- | --- | --- |
| `crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs` | 23 | `pub(crate) const ZERO_ROOT: [u8; 32] = [0u8; 32];` | `REMOVE` | `✅` |
| `crates/z00z_storage/benches/common/fixture.rs` | 228 | `impl BenchFx { pub fn read_paths_case(...); pub fn proof_paths_case(...); }` | `REMOVE` | `✅` |
| `crates/z00z_storage/benches/common/output.rs` | 41 | `pub(crate) fn write_note(name: &str, body: &str)` | `REMOVE` | `✅` |

### `#[allow(unreachable_code)]`

These four cases are the same structural pattern: a wasm32 `return Err(...)` branch followed by a trailing `Ok(())`. The behavior must stay fail-closed, but the attribute should disappear.

| File | Line | Scoped item | Final recommendation | Comments |
| --- | --- | --- | --- | --- |
| `crates/z00z_wallets/src/services/wallet_service_session_derivation.rs` | 357 | trailing `Ok(())` after wasm32 guard | `REFACTOR` | `✅` |
| `crates/z00z_wallets/src/services/wallet_service_session_snapshot.rs` | 112 | trailing `Ok(())` after wasm32 guard | `REFACTOR` | `✅` |
| `crates/z00z_wallets/src/services/wallet_service_store_persistence_pack.rs` | 190 | trailing `Ok(())` after wasm32 guard | `REFACTOR` | `✅` |
| `crates/z00z_wallets/src/services/wallet_service_store_load_restore.rs` | 63 | trailing `Ok(())` after wasm32 guard | `REFACTOR` | `✅` |

### `#[allow(clippy::too_many_arguments)]`

Default evaluation rule: blank `Final recommendation` means `KEEP AS-IS`.

| File | Line | Scoped item | Final recommendation | Comments |
| --- | --- | --- | --- | --- |
| `crates/z00z_core/src/assets/definition.rs` | 238 | `pub fn new(` |  | `✅` |
| `crates/z00z_core/src/genesis/genesis_output_support.rs` | 158 | `pub(crate) fn write_genesis_report(` | `REFACTOR` | `✅` |
| `crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs` | 3 | `pub(super) fn finish_claim_after_event(` | `REFACTOR` | `✅` |
| `crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs` | 37 | `pub(super) fn prepare_tx_package_artifacts(` | `REFACTOR` | `✅` |
| `crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs` | 306 | `pub(super) async fn persist_and_confirm_runtime(` | `REFACTOR` | `✅` |
| `crates/z00z_wallets/src/adapters/rpc/methods/tx.rs` | 5 | `#![allow(clippy::too_many_arguments)]` | `NARROW` | `✅` |
| `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs` | 323 | `pub fn register_all_wallet_rpc_methods(` | `REFACTOR` | `✅` |
| `crates/z00z_wallets/src/core/wallet/snapshot_impl.rs` | 40 | `pub fn new(` | `REFACTOR` | `✅` |
| `crates/z00z_wallets/src/core/wallet/snapshot_impl.rs` | 156 | `pub fn new_with_checksum(` | `REFACTOR` | `✅` |

### Other `#[allow(clippy::...)]`

Default evaluation rule: blank `Final recommendation` means `KEEP AS-IS`.

| File | Line | Lint | Scoped item | Final recommendation | Comments |
| --- | --- | --- | --- | --- | --- |
| `crates/z00z_wallets/src/core/wallet/chain_id.rs` | 75 | `clippy::items_after_test_module` | `mod chain_id_tests {` | `REMOVE` | `✅` |

## Keep-As-Is Guardrails

- `crates/z00z_core/src/assets/definition.rs::new()` is the only blank `too_many_arguments` row and must remain unchanged in this plan unless a separate source artifact explicitly reclassifies it.
- No new attribute family or row may be added opportunistically. If execution discovers a missed attribute outside the embedded tables, stop, update the source spec first, and then re-plan.
- `036-20-SUMMARY.md` remains authoritative for the partial shim-removal truth. `036-21` must not claim that shim-removal finished unless the same execution patch set proves it independently.
</context>

<global_execution_gates>

- Treat `.planning/phases/036-rename/036-attrib-spec.md` as the sole authority for this wave. Every embedded row must be reconciled explicitly.
- Preserve runtime behavior, serialized values, fail-closed boundaries, and public API contracts while removing the attribute-backed escape hatches.
- Do not touch `crates/z00z_crypto/tari/**` or any unrelated attribute outside the embedded tables.
- Blank recommendation rows mean `KEEP AS-IS`; they must be mentioned in `036-21-SUMMARY.md` rather than silently dropped from the closeout.
- If execution needs a git commit, use `.github/skills/z00z-git-versioning/scripts/version-manager.sh` through the `/z00z-git-versioning` workflow instead of raw git commands.
</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-21.A Remove safe attribute residue and narrow the remaining import or RPC escape hatches</name>
  <files>crates/z00z_utils/examples/file_io.rs, crates/z00z_storage/src/assets/store_internal/tx_plan.rs, crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs, crates/z00z_wallets/src/db/redb_wallet_store.rs, crates/z00z_wallets/src/db/redb_wallet_store_open.rs, crates/z00z_wallets/src/core/tx/tx_verifier.rs, crates/z00z_wallets/src/core/security/encryption.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs, crates/z00z_storage/benches/common/fixture.rs, crates/z00z_storage/benches/common/output.rs, crates/z00z_wallets/src/core/wallet/chain_id.rs, crates/z00z_wallets/src/adapters/rpc/methods/tx.rs</files>
  <action>Remove every explicit `REMOVE` row from the unused_imports, deprecated, dead_code, and items_after_test_module tables by deleting the unused imports or helpers, relocating the `chain_id_tests` module so no post-test item remains, and switching the test-fast Argon2 path onto a non-deprecated constructor. For the two `NARROW` cases, reduce scope instead of deleting semantics: in `bundle_lane_impl.rs`, narrow the `exec_input_builder` re-export surface to only the identifiers actually needed by non-test callers and push purely test-only items behind `#[cfg(test)]` or local imports; in `tx_verifier.rs` and `methods/tx.rs`, remove module-wide escape hatches and keep the smallest item-level allowance only where the exact RPC or helper signature still requires it. Do not widen public exports or add replacement shims.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "#\[allow\((unused_imports|deprecated|dead_code)\)\]|#\[allow\(clippy::items_after_test_module\)\]|#!\[allow\(clippy::too_many_arguments\)\]" crates/z00z_utils/examples/file_io.rs crates/z00z_storage/src/assets/store_internal/tx_plan.rs crates/z00z_simulator/src/scenario_1/stage_6_utils/bundle_lane_impl.rs crates/z00z_wallets/src/db/redb_wallet_store.rs crates/z00z_wallets/src/db/redb_wallet_store_open.rs crates/z00z_wallets/src/core/tx/tx_verifier.rs crates/z00z_wallets/src/core/security/encryption.rs crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs crates/z00z_storage/benches/common/fixture.rs crates/z00z_storage/benches/common/output.rs crates/z00z_wallets/src/core/wallet/chain_id.rs crates/z00z_wallets/src/adapters/rpc/methods/tx.rs</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>The safe removal rows no longer need their attributes, the module-wide `too_many_arguments` escape hatch in `methods/tx.rs` is narrowed to the smallest justified surface, and the two import-related `NARROW` cases keep only the exact non-test exports or imports still required.</done>
</task>

<task type="auto">
  <name>036-21.B Refactor cfg-separated wallet persistence flows and simulator or genesis helpers to eliminate structural allow attributes</name>
  <files>crates/z00z_wallets/src/services/wallet_service_session_derivation.rs, crates/z00z_wallets/src/services/wallet_service_session_snapshot.rs, crates/z00z_wallets/src/services/wallet_service_store_persistence_pack.rs, crates/z00z_wallets/src/services/wallet_service_store_load_restore.rs, crates/z00z_core/src/genesis/genesis_output_support.rs, crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs, crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs</files>
  <action>Remove the four `#[allow(unreachable_code)]` markers by restructuring each wallet service function into cfg-selected helper branches or cfg-selected expression returns so the wasm32 error path and the non-wasm success path stay explicit without a dead trailing `Ok(())`. In the same patch set, eliminate the `REFACTOR` rows in `genesis_output_support.rs`, `stage_3_finalize.rs`, and both `tx_lane_runtime_flow.rs` helpers by introducing typed parameter bundles or local context structs that collapse argument count without changing the external behavior, persisted artifacts, or step logging contracts. Preserve every current caller-visible contract and keep function ownership in the same modules; this is a shape refactor, not a semantic redesign.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "#\[allow\(unreachable_code\)\]|#\[allow\(clippy::too_many_arguments\)\]" crates/z00z_wallets/src/services/wallet_service_session_derivation.rs crates/z00z_wallets/src/services/wallet_service_session_snapshot.rs crates/z00z_wallets/src/services/wallet_service_store_persistence_pack.rs crates/z00z_wallets/src/services/wallet_service_store_load_restore.rs crates/z00z_core/src/genesis/genesis_output_support.rs crates/z00z_simulator/src/scenario_1/stage_3_finalize.rs crates/z00z_simulator/src/scenario_1/stage_4_utils/tx_lane_runtime_flow.rs</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>The wallet persistence or load functions no longer need unreachable-code allowances, and the genesis or simulator helpers express their inputs through narrower typed bundles instead of wide argument lists while preserving the same observable outputs and fail-closed behavior.</done>
</task>

<task type="auto">
  <name>036-21.C Refactor wallet snapshot and dispatcher constructors, then close the full attribute matrix truthfully</name>
  <files>crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs, crates/z00z_wallets/src/core/wallet/snapshot_impl.rs, .planning/phases/036-rename/036-21-SUMMARY.md</files>
  <action>Remove the remaining `REFACTOR` rows by collapsing `register_all_wallet_rpc_methods()` and the two snapshot constructors behind typed registration or snapshot input bundles, builder-style helpers, or private assembly structs so the broad argument lists disappear without altering RPC registration coverage or snapshot checksum semantics. Then write `036-21-SUMMARY.md` as a truth-based closeout that enumerates every row from the embedded attribute tables, explicitly records the unchanged KEEP AS-IS row for `definition.rs::new()`, and states whether Phase 036 still remains open because `036-20` is partial or because any new attribute blocker was discovered in execution.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "#\[allow\(clippy::too_many_arguments\)\]" crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs crates/z00z_wallets/src/core/wallet/snapshot_impl.rs</automated>
    <automated>rg -n "KEEP AS-IS|definition.rs::new|036-20-SUMMARY|unused_imports|deprecated|dead_code|unreachable_code|too_many_arguments|items_after_test_module" .planning/phases/036-rename/036-21-SUMMARY.md</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue after each pass, and stop only after at least 2 consecutive runs report no significant issues.</automated>
  </verify>
  <done>The wallet dispatcher and snapshot constructors no longer rely on broad `too_many_arguments` allowances, and `036-21-SUMMARY.md` truthfully closes the embedded attribute matrix without overwriting the separate partial truth already recorded in `036-20-SUMMARY.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attribute cleanup -> runtime behavior | Removing or narrowing allow attributes can accidentally change exported symbols, cfg behavior, or logging and snapshot semantics. |
| simulator or genesis helpers -> persisted artifacts | Refactoring argument-heavy helpers can silently reorder inputs or alter persisted report and tx-package artifacts. |
| wallet RPC or snapshot constructors -> public contract | Constructor or dispatcher refactors can break caller wiring, checksum calculation, or RPC registration completeness if the new bundles omit fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-21-01 | T | `wallet_service_*` cfg-separated persistence helpers | mitigate | Keep wasm32 and non-wasm branches explicit in separate helpers or cfg-selected expressions and prove behavior with bootstrap plus release-style test gates. |
| T-036-21-02 | T | `tx_lane_runtime_flow.rs`, `stage_3_finalize.rs`, `genesis_output_support.rs` | mitigate | Replace wide signatures with typed parameter bundles that preserve field order, existing call-site values, and current log or artifact writes, then rerun deterministic gates and review passes. |
| T-036-21-03 | E | `wallet_dispatcher_wiring.rs`, `snapshot_impl.rs`, `methods/tx.rs` | mitigate | Narrow attributes without expanding public surface; preserve RPC registration completeness and snapshot checksum inputs by keeping old semantics behind private assembly structs or builders and verifying exact remaining allow markers by rg scans. |
| T-036-21-04 | R | `036-21-SUMMARY.md` closeout narrative | mitigate | Force the summary to enumerate every embedded table row, explicitly mark the KEEP AS-IS row, and state that `036-20-SUMMARY.md` remains the authoritative partial shim-removal truth unless this wave independently proves otherwise. |
</threat_model>

<verification>
This plan closes only when the full embedded attribute matrix is reconciled row-by-row, the deterministic validation order is respected in every auto task, and the summary distinguishes removed, narrowed, refactored, and intentionally retained rows without silently shrinking scope.
</verification>

<success_criteria>
`036-21 Reconcile The Phase 036 Attribute Audit Recommendations Without Semantic Drift` is complete only when every embedded row has a repository-backed outcome, all non-KEEP attribute exceptions covered by this plan are removed or narrowed to the minimum justified scope, and the summary proves the final inventory while preserving the separate partial truth already recorded for `036-20`.
</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-21-SUMMARY.md`.
</output>