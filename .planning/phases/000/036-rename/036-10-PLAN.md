---
phase: 036-rename
plan: 10
type: execute
wave: 7
depends_on: [036-09]
files_modified:
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Steps 0 through 5 are all closed against the exact raw-row sets from the canonical spec.
    - Residual scanning proves no uncovered phase-owned version-bearing signature remains in scope.
    - The raw inventory remains the only task-generation surface and no second planning layer was reintroduced.
  artifacts:
    - path: .planning/phases/036-rename/036-a1-versioning-spec.md
      provides: Canonical row ownership, pitfall guards, and completion rules used for closure validation.
    - path: .planning/phases/036-rename/036-TODO-2.md
      provides: Canonical task ledger and completion gate used for closure validation.
  key_links:
    - from: .planning/phases/036-rename/036-a1-versioning-spec.md
      to: .planning/phases/036-rename/036-TODO-2.md
      via: row ownership and completion gate reconciliation
      pattern: executed code, source spec, and backlog agree on the same remaining holds
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-10 PLAN

<objective>
Execute the final Phase 036 closure-validation slice from the canonical embedded-versioning backlog.

Purpose: prove that Steps 0 through 5 were executed against the exact raw-row sets from the canonical source, rerun the residual scan, and confirm that no second task-generation layer or speculative contract rename was introduced during execution.
Output: `036-07 Row-Coverage Validation And Regression Closure` is closed with row-exact reconciliation between executed code, `036-a1-versioning-spec.md`, and `036-TODO-2.md`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-2.md
@.planning/phases/036-rename/036-a1-versioning-spec.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Preserve the exact task wording from `036-TODO-2.md`; do not rename the task or substitute a different closure surface.
- Honor D-01 through D-20 from `036-CONTEXT.md`, especially D-03, D-11, D-12, D-14, D-15, D-16, D-19, and D-20.
- The raw inventory is the only task-generation surface. Any uncovered phase-owned version-bearing signature found by the residual scan must be added back to `036-a1-versioning-spec.md` first, then `036-TODO-2.md`, before closure can be claimed.
- Treat the `Compact Patch-Plan By Disputed Row Group` as a grouping aid only; if it ever disagrees with a raw row, the raw row wins.
- Before any edit, complete `read_first` and revalidate that the task-local `files`, `required_test_surfaces`, `acceptance_criteria`, `verify`, and `done` blocks still match the canonical raw-row ownership in `036-a1-versioning-spec.md` and `036-TODO-2.md`.
- If execution edits `036-a1-versioning-spec.md` or `036-TODO-2.md`, stop the current wave immediately and rerun review plus planning before any further code edits, closure checks, or summary claims.
- In every auto-task verify block, run validation in this order: bootstrap gate first, then `cargo test --release --features test-fast --features wallet_debug_dump`, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun the deterministic gates after each fix cycle, and do not stop the review loop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-07 Row-Coverage Validation And Regression Closure</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-01 through D-20 and the `Task Transfer Matrix` row for `036-07`.
    - `.planning/phases/036-rename/036-TODO-2.md` — the full `036-07` section, `Row-Step Coverage Ledger`, and `Completion Gate`.
    - `.planning/phases/036-rename/036-a1-versioning-spec.md` — `Execution Step Completion Rules`, `Pitfall Guards`, and `Non-Negotiable Translation Rules`.
  </read_first>
  <action>Execute `036-07 Row-Coverage Validation And Regression Closure` exactly as written in `036-TODO-2.md`. Verify that Steps `0-5` are all closed against the exact raw-row sets defined in the canonical source spec, rerun targeted tests for every touched production or test seam, record the hold-only rows that were intentionally not edited, and rerun the Phase 036 residual review as declaration-backed reconciliation against the `Raw Inventory Appendix` and `Row-Step Coverage Ledger`. Broad grep or semantic scans may be used only as discovery hints; no scan hit may authorize closure state until it is re-mapped to a concrete raw row in `036-a1-versioning-spec.md`. Any uncovered phase-owned version-bearing signature must be pushed back into `036-a1-versioning-spec.md` first and then into `036-TODO-2.md` before closure is claimed. Treat `036-a1-versioning-spec.md` and `036-TODO-2.md` as the only authoritative closure surfaces. Do not reintroduce a second task-generation layer, do not merge multiple path-specific rows into synthetic owners, and do not treat historical TODO1 or suffix-spec artifacts as live closure authority.</action>
  <required_test_surfaces>
    All task-local test surfaces from `036-04-PLAN.md` through `036-09-PLAN.md`
  </required_test_surfaces>
  <acceptance_criteria>
    - The executed code, `036-a1-versioning-spec.md`, and `036-TODO-2.md` agree on row ownership, step completion, and remaining hold-only contracts.
    - The residual scan finds no uncovered phase-owned version-bearing signature in scope.
    - Historical artifact strings are allowed only inside explicit non-authority warnings; closure does not treat mere text mention as authority import.
    - No second task-generation layer, speculative row merge, or protocol-literal rename was introduced during execution.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Reconcile compiler and direct call-site proof for every renamed row by confirming the task-local summaries and verification artifacts from `036-04-PLAN.md` through `036-09-PLAN.md` remain satisfied before closure is accepted.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified by deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic gates; continue additional review or fix cycles until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Phase 036 closure is proven against the canonical versioning-spec/TODO-2 authority chain, with no uncovered phase-owned version-bearing residue and no reintroduced second planning surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executed code -> closure claim | Closure must reflect the exact raw-row ownership in the canonical spec and backlog. |
| residual scan -> scope boundary | The scan must surface any missed phase-owned version-bearing signature before closeout. |
| historical artifacts -> current authority | Old TODO1 or suffix-spec artifacts must not silently retake authority during closeout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-10-01 | T | closure evidence chain | mitigate | Reconcile executed code, source spec, and TODO-2 row-by-row before claiming closure. |
| T-036-10-02 | R | residual scan coverage | mitigate | Rerun the full phase residual scan excluding the protected Tari vendor subtree and feed uncovered phase-owned rows back into the canonical spec before closeout. |
| T-036-10-03 | E | historical planning artifacts | mitigate | Treat `036-a1-versioning-spec.md` and `036-TODO-2.md` as the only live closure authority and reject TODO1 or suffix-spec reimport. |
</threat_model>

<verification>
The executor must finish this plan with row-exact closure evidence tying the executed code to the canonical spec and backlog, plus a residual scan showing no uncovered phase-owned version-bearing residue.
</verification>

<success_criteria>
`036-07 Row-Coverage Validation And Regression Closure` is closed on the exact canonical authority chain, with no uncovered phase-owned version-bearing signatures and no reintroduced second task-generation surface.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-10-SUMMARY.md`.
</output>