---
phase: 036-rename
plan: 14
type: execute
wave: 4
depends_on: [036-11, 036-12, 036-13]
files_modified:
  - .planning/phases/036-rename/036-a2-legacy-removing-spec.md
  - .planning/phases/036-rename/036-TODO-3.md
  - crates/z00z_storage/tests/test_checkpoint_store_api.rs
  - crates/z00z_storage/tests/test_redb_rehydrate.rs
  - crates/z00z_storage/tests/test_checkpoint_codec.rs
  - crates/z00z_storage/tests/checkpoint/test_fixtures.rs
  - crates/z00z_storage/tests/test_checkpoint_link_injective.rs
  - crates/z00z_storage/tests/test_checkpoint_finalization.rs
  - crates/z00z_wallets/tests/test_backup_kdf_contract.rs
  - crates/z00z_wallets/tests/test_tx_digest_framing.rs
  - crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  - crates/z00z_wallets/tests/test_claim_state_compat.rs
  - crates/z00z_wallets/tests/test_phase30_split.rs
  - crates/z00z_wallets/src/services/wallet_service_tests.rs
  - crates/z00z_wallets/src/adapters/rpc/dispatcher_handlers.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_tests.rs
  - crates/z00z_wallets/src/core/chain/receiver_card_record.rs
  - crates/z00z_wallets/src/core/key/key_manager_redb_tests.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs
  - crates/z00z_wallets/src/core/tx/test_claim_tx.rs
  - crates/z00z_wallets/src/core/tx/tx_verifier_tests.rs
  - crates/z00z_wallets/src/core/backup/backup_importer_tests.rs
  - crates/z00z_wallets/src/core/wallet/snapshot_tests.rs
  - crates/z00z_wallets/src/core/address/z00z_address/tests.rs
  - crates/z00z_crypto/tests/test_fail_closed.rs
  - crates/z00z_crypto/tests/test_public_surface.rs
  - crates/z00z_crypto/tests/test_hash_policy.rs
  - crates/z00z_simulator/tests/test_stage4_source_shape.rs
  - crates/z00z_simulator/tests/test_claim_emit.rs
  - crates/z00z_utils/src/compression/test_compression.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Fixture and reject-test deletions never lead production semantics; they follow the outcome of 036-13 and preserve blocked compatibility evidence.
    - Historical names that still prove a live migration/rejection boundary stay explicit and justified.
    - The canonical removal docs record which residues were deleted and which remain blocked.
  artifacts:
    - path: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      provides: Row-specific residue disposition after Wave 3 cleanup.
    - path: .planning/phases/036-rename/036-TODO-3.md
      provides: Wave 3 backlog truth aligned to production outcomes and preserved historical evidence.
  key_links:
    - from: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      to: crates/z00z_storage/tests/test_redb_rehydrate.rs
      via: production-owner fate deciding whether historical checkpoint fixtures may be deleted
      pattern: fixture deletion occurs only when the owning compatibility surface is retired or reclassified safely
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-14 PLAN

<objective>
Execute `036-14 Reconcile Fixtures, Reject Tests, And Delete-Only Residue` after Wave 2 production outcomes are fixed.

Purpose: delete the historical/test residue that is downstream-safe after Wave 2, while keeping blocked rows only as explicit deletion blockers until their owning production delete lands.
Output: test, fixture, and residue deletion is aligned to the actual production outcome instead of leading it.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-a2-legacy-removing-spec.md
@.planning/phases/036-rename/036-TODO-3.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Wave 3 cannot outrun Wave 2. If `036-13` left a production owner blocked, keep the corresponding historical test/fixture row as an explicit deletion blocker and document why.
- Do not touch `examples/**/*.rs` or `benches/**/*.rs` in this plan unless `036-a2-legacy-removing-spec.md` was explicitly updated first to show new declaration-level hits.
- Treat reject tests and compatibility fixtures as delete targets once their owning production delete is proven.
- If a test name or fixture still communicates a live compatibility or rejection boundary, keep it only as an explicit deletion-blocker note in `036-a2` / `036-TODO-3`, not as an intentional non-deletion state.
- In every auto-task verify block, run the bootstrap gate first, then `cargo test --release --features test-fast --features wallet_debug_dump` when Rust/test-affecting changes land, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun deterministic checks after each fix cycle, and do not stop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-14.A Reconcile storage checkpoint fixtures and reject tests</name>
  <files>crates/z00z_storage/tests/test_checkpoint_store_api.rs, crates/z00z_storage/tests/test_redb_rehydrate.rs, crates/z00z_storage/tests/test_checkpoint_codec.rs, crates/z00z_storage/tests/checkpoint/test_fixtures.rs, crates/z00z_storage/tests/test_checkpoint_link_injective.rs, crates/z00z_storage/tests/test_checkpoint_finalization.rs, .planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `crates/z00z_storage/tests/test_checkpoint_store_api.rs`
    - `crates/z00z_storage/tests/test_redb_rehydrate.rs`
    - `crates/z00z_storage/tests/test_checkpoint_codec.rs`
    - `crates/z00z_storage/tests/checkpoint/test_fixtures.rs`
    - `crates/z00z_storage/tests/test_checkpoint_link_injective.rs`
    - `crates/z00z_storage/tests/test_checkpoint_finalization.rs`
    - Wave 1/Wave 2 decisions for the corresponding storage owners in `036-a2-legacy-removing-spec.md`
  </read_first>
  <action>Delete only the storage fixture/test residue whose owning production legacy surface was deleted safely in `036-13`. If any storage compatibility lane remains live because production deletion is still blocked (for example legacy artifact decode, checkpoint-link upgrade, or legacy opaque handling), keep the corresponding test/fixture row blocked and add an explicit blocker note to `036-a2-legacy-removing-spec.md` instead of preserving it as an intentional non-deletion state. Keep the residue decision row-specific.</action>
  <required_test_surfaces>
    `z00z_storage` checkpoint and rehydrate regression coverage for every touched residue row
  </required_test_surfaces>
  <acceptance_criteria>
    - Storage tests only delete residue that is downstream-safe after Wave 2.
    - Blocked rows remain explicit only until owning production deletion lands.
    - Every deleted or blocked row is documented back into the canonical removal docs.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "\\blegacy\\b|Legacy" crates/z00z_storage/tests/test_checkpoint_store_api.rs crates/z00z_storage/tests/test_redb_rehydrate.rs crates/z00z_storage/tests/test_checkpoint_codec.rs crates/z00z_storage/tests/checkpoint/test_fixtures.rs crates/z00z_storage/tests/test_checkpoint_link_injective.rs crates/z00z_storage/tests/test_checkpoint_finalization.rs</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Storage checkpoint fixture/test residue matches the actual production deletion outcome instead of leading it.</done>
</task>

<task type="auto">
  <name>036-14.B Reconcile wallet fixture and reject-test residue</name>
  <files>crates/z00z_wallets/tests/test_backup_kdf_contract.rs, crates/z00z_wallets/tests/test_tx_digest_framing.rs, crates/z00z_wallets/tests/test_wallet_kdf_migration.rs, crates/z00z_wallets/tests/test_redb_wlt_open.rs, crates/z00z_wallets/tests/test_claim_state_compat.rs, crates/z00z_wallets/tests/test_phase30_split.rs, crates/z00z_wallets/src/services/wallet_service_tests.rs, crates/z00z_wallets/src/adapters/rpc/dispatcher_handlers.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_tests.rs, crates/z00z_wallets/src/core/chain/receiver_card_record.rs, crates/z00z_wallets/src/core/key/key_manager_redb_tests.rs, crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs, crates/z00z_wallets/src/core/tx/test_claim_tx.rs, crates/z00z_wallets/src/core/tx/tx_verifier_tests.rs, crates/z00z_wallets/src/core/backup/backup_importer_tests.rs, crates/z00z_wallets/src/core/wallet/snapshot_tests.rs, crates/z00z_wallets/src/core/address/z00z_address/tests.rs, .planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - All wallet test/source files listed in `files_modified`
    - Wave 1/Wave 2 decisions for the corresponding wallet owners in `036-a2-legacy-removing-spec.md`
  </read_first>
  <action>Process the wallet residue files in two buckets: delete residue whose owning production lane is already deleted, and block residue whose owning production delete is not yet proven. That includes backup import/KDF tests, tx digest framing, RPC legacy-id acceptance tests, claim-state compatibility fixtures, receiver-card compact rejects, and any source-file test modules under `src/`. For each blocked historical name, write the blocker back into `036-a2-legacy-removing-spec.md` and keep `036-TODO-3.md` aligned. Do not convert blocked rows into non-deletion cleanup.</action>
  <required_test_surfaces>
    `z00z_wallets` targeted backup, KDF, tx framing, RPC, address, and fixture regressions tied to touched residue rows
  </required_test_surfaces>
  <acceptance_criteria>
    - Wallet residue deletions follow production outcomes rather than guessing them.
    - Blocked reject/compatibility rows stay explicit only until deletion prerequisites clear.
    - Canonical docs reflect every wallet residue delete/block choice.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "\\blegacy\\b|Legacy" crates/z00z_wallets/tests/test_backup_kdf_contract.rs crates/z00z_wallets/tests/test_tx_digest_framing.rs crates/z00z_wallets/tests/test_wallet_kdf_migration.rs crates/z00z_wallets/tests/test_redb_wlt_open.rs crates/z00z_wallets/tests/test_claim_state_compat.rs crates/z00z_wallets/tests/test_phase30_split.rs crates/z00z_wallets/src/services/wallet_service_tests.rs crates/z00z_wallets/src/adapters/rpc/dispatcher_handlers.rs crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_tests.rs crates/z00z_wallets/src/core/chain/receiver_card_record.rs crates/z00z_wallets/src/core/key/key_manager_redb_tests.rs crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs crates/z00z_wallets/src/core/tx/test_claim_tx.rs crates/z00z_wallets/src/core/tx/tx_verifier_tests.rs crates/z00z_wallets/src/core/backup/backup_importer_tests.rs crates/z00z_wallets/src/core/wallet/snapshot_tests.rs crates/z00z_wallets/src/core/address/z00z_address/tests.rs</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Wallet fixture/test residue is reconciled to real production outcomes, with blocked rows left explicit only as temporary blockers.</done>
</task>

<task type="auto">
  <name>036-14.C Reconcile crypto, simulator, and utility historical residue</name>
  <files>crates/z00z_crypto/tests/test_fail_closed.rs, crates/z00z_crypto/tests/test_public_surface.rs, crates/z00z_crypto/tests/test_hash_policy.rs, crates/z00z_simulator/tests/test_stage4_source_shape.rs, crates/z00z_simulator/tests/test_claim_emit.rs, crates/z00z_utils/src/compression/test_compression.rs, .planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `crates/z00z_crypto/tests/test_fail_closed.rs`
    - `crates/z00z_crypto/tests/test_public_surface.rs`
    - `crates/z00z_crypto/tests/test_hash_policy.rs`
    - `crates/z00z_simulator/tests/test_stage4_source_shape.rs`
    - `crates/z00z_simulator/tests/test_claim_emit.rs`
    - `crates/z00z_utils/src/compression/test_compression.rs`
    - Wave 1/Wave 2 decisions for the corresponding owners in `036-a2-legacy-removing-spec.md`
  </read_first>
  <action>Delete or block the remaining crypto/simulator/utils historical residue using the same rule: if the owning production legacy contract was deleted or converted into a delete-now row, delete the residue; otherwise keep the row blocked and write the blocker explicitly into `036-a2` / `036-TODO-3`. Do not preserve any feature-gate, fail-closed, or source-shape wording as an intentional non-deletion state if it still contains `legacy`.</action>
  <required_test_surfaces>
    `z00z_crypto`, `z00z_simulator`, and `z00z_utils` targeted regression coverage for the touched residue rows
  </required_test_surfaces>
  <acceptance_criteria>
    - Historical residue deletions in these crates follow the actual owner fate.
    - Blocked boundary-proof rows are temporary blockers, not approved survivors.
    - Canonical docs record the delete/block disposition for each residue row.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "\\blegacy\\b|Legacy" crates/z00z_crypto/tests/test_fail_closed.rs crates/z00z_crypto/tests/test_public_surface.rs crates/z00z_crypto/tests/test_hash_policy.rs crates/z00z_simulator/tests/test_stage4_source_shape.rs crates/z00z_simulator/tests/test_claim_emit.rs crates/z00z_utils/src/compression/test_compression.rs</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Crypto/simulator/utils historical residue is reconciled to the real production fate without drifting into non-deletion cleanup.</done>
</task>

<task type="auto">
  <name>036-14.D Sync Wave 3 residue disposition into the canonical removal docs</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md`
    - `.planning/phases/036-rename/036-TODO-3.md`
    - The diff from tasks `036-14.A` through `036-14.C`
  </read_first>
  <action>Update the canonical removal spec and backlog so every Wave 3 residue row is marked as either deleted or still blocked by a live production deletion prerequisite. Keep the notes row-specific. Reconfirm that `examples/**/*.rs` and `benches/**/*.rs` remain unchanged and empty unless a fresh scan proved new hits first.</action>
  <required_test_surfaces>
    Canonical residue disposition truth and empty-bucket preservation
  </required_test_surfaces>
  <acceptance_criteria>
    - `036-a2` and `036-TODO-3` match the actual Wave 3 delete/block outcome.
    - No blocked row is misrepresented as an intentional non-deletion state.
    - Example/bench emptiness remains truthful.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "deleted|blocked|examples/\*\*/\.rs|benches/\*\*/\.rs" .planning/phases/036-rename/036-a2-legacy-removing-spec.md .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Wave 3 delete/block disposition is fully synchronized back into the canonical removal docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production outcome -> residue cleanup | Wave 3 must consume Wave 2 results instead of inventing its own semantics. |
| historical name -> live compatibility evidence | Premature deletion here can destroy explicit proof of a still-live rejection or migration boundary. |
| residue diff -> canonical docs | Every delete/block choice must be reflected back into the authority docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-14-01 | T | residue cleanup ordering | mitigate | Require every Wave 3 deletion to prove its owning production surface was deleted or blocked explicitly in Waves 1-2. |
| T-036-14-02 | I | historical proof loss | mitigate | Preserve explicit reject/compatibility names when they still communicate a live boundary and justify them in the canonical docs. |
| T-036-14-03 | R | residue disposition drift | mitigate | Sync every Wave 3 delete/block outcome back into `036-a2` and `036-TODO-3` immediately after edits. |
</threat_model>

<verification>
This plan closes only when fixture and test deletion no longer leads production semantics and every surviving historical name is explicitly blocked as temporary.
</verification>

<success_criteria>
`036-14 Reconcile Fixtures, Reject Tests, And Delete-Only Residue` is complete only when residue deletion follows real Wave 2 production outcomes and every blocked row remains explicitly temporary.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-14-SUMMARY.md`.
</output>