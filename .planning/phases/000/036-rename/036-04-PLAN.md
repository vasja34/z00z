---
phase: 036-rename
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_storage/src/serialization/artifact.rs
  - crates/z00z_storage/src/snapshot/types.rs
  - crates/z00z_storage/src/checkpoint/exec_input.rs
  - crates/z00z_storage/src/checkpoint/link.rs
  - crates/z00z_storage/src/checkpoint/audit.rs
  - crates/z00z_storage/src/checkpoint/ids.rs
  - crates/z00z_storage/src/checkpoint/artifact_types.rs
  - crates/z00z_storage/src/checkpoint/artifact_stmt.rs
  - crates/z00z_crypto/src/claim/mod.rs
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_crypto/src/claim/proof.rs
  - crates/z00z_crypto/src/zkpack.rs
  - crates/z00z_crypto/src/kdf_domains.rs
  - crates/z00z_crypto/src/crypto_constants.rs
  - crates/z00z_core/src/assets/version.rs
  - crates/z00z_core/src/assets/leaf.rs
  - crates/z00z_wallets/src/core/tx/prover.rs
  - crates/z00z_wallets/src/core/tx/claim_wire_types.rs
  - crates/z00z_wallets/src/core/tx/claim_tx.rs
  - crates/z00z_wallets/src/core/claim/claim_receipt.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key.rs
  - crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs
  - crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Step 0 rows remain explicitly frozen with row-backed no-rename justification.
    - Literal-backed contracts stay byte-for-byte unchanged across claim, RPC, checkpoint, and domain-separation surfaces.
    - No new phase-owned row is executed unless it first lands in the raw inventory and TODO-2 backlog.
  artifacts:
    - path: .planning/phases/036-rename/036-a1-versioning-spec.md
      provides: Canonical Step 0 row ownership and freeze rules.
    - path: .planning/phases/036-rename/036-TODO-2.md
      provides: Canonical 036-01 execution contract and completion gate.
    - path: crates/z00z_crypto/src/claim/v2.rs
      provides: Live claim-v2 lane helpers and error-string contracts that must remain explicit.
    - path: crates/z00z_wallets/src/core/tx/claim_wire_types.rs
      provides: Live claim transport literals that must stay frozen.
  key_links:
    - from: .planning/phases/036-rename/036-a1-versioning-spec.md
      to: .planning/phases/036-rename/036-TODO-2.md
      via: Raw Inventory Appendix to 036-01 mapping
      pattern: Step 0 row ownership stays path-specific and spec-backed
    - from: crates/z00z_crypto/src/claim/v2.rs
      to: crates/z00z_wallets/src/core/tx/claim_wire_types.rs
      via: outer claim-v2 helper and transport tag surfaces
      pattern: claim_stmt_hash_v2 and CLAIM_PROOF_V2 remain explicit and unchanged
    - from: crates/z00z_wallets/src/adapters/rpc/methods/key.rs
      to: crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
      via: live RPC registration string
      pattern: wallet.key.export_public_material_v2 remains byte-for-byte unchanged
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-04 PLAN

<objective>
Execute the first canonical `036-TODO-2.md` task with zero scope drift.

Purpose: close the Step 0 freeze slice from the versioning-spec-backed backlog while proving that live wire discriminants, coexistence lanes, future-reserved helpers, and literal or error contracts are not misclassified as cleanup noise.
Output: `036-01 Freeze Explicit Wire Discriminants, Live Lanes, And Literal Contracts` is closed on the exact raw-row set owned by Step 0, with any missed live surface pushed back through the canonical spec and backlog before further code edits continue.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-2.md
@.planning/phases/036-rename/036-a1-versioning-spec.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Preserve the exact task wording from `036-TODO-2.md`; do not rename the task, split its raw-row ownership, or import meaning from `036-TODO-1.md` or `036-a1-suffixes-spec.md`.
- Honor D-01 through D-20 from `036-CONTEXT.md`, especially D-03, D-05, D-11, D-12, D-15, D-16, and D-19.
- The raw inventory is the only task-generation surface. If execution finds a missed caller, producer, or path-specific version-bearing contract, update `036-a1-versioning-spec.md` first, then `036-TODO-2.md`, and only then resume code edits.
- Treat the `Compact Patch-Plan By Disputed Row Group` as a grouping aid only; if it ever disagrees with a raw row, the raw row wins.
- Before any edit, complete `read_first` and revalidate that the task-local `files`, `required_test_surfaces`, `acceptance_criteria`, `verify`, and `done` blocks still match the canonical raw-row ownership in `036-a1-versioning-spec.md` and `036-TODO-2.md`.
- If execution edits `036-a1-versioning-spec.md` or `036-TODO-2.md`, stop the current wave immediately and rerun review plus planning before any further code edits, closure checks, or summary claims.
- In every auto-task verify block, run validation in this order: bootstrap gate first, then `cargo test --release --features test-fast --features wallet_debug_dump`, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun the deterministic gates after each fix cycle, and do not stop the review loop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-01 Freeze Explicit Wire Discriminants, Live Lanes, And Literal Contracts</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-01 through D-20 and the `Task Transfer Matrix` row for `036-01`.
    - `.planning/phases/036-rename/036-TODO-2.md` — the full `036-01` section, `Row-Step Coverage Ledger`, and `Completion Gate`.
    - `.planning/phases/036-rename/036-a1-versioning-spec.md` — `Manual Broad-Scan Residual Disposition`, `Execution Order By Row Range`, `Raw Inventory Appendix -> Non-Test Signatures`, and `Raw Inventory Appendix -> Literal-Backed Contracts`.
  </read_first>
  <files>
    crates/z00z_storage/src/serialization/artifact.rs
    crates/z00z_storage/src/snapshot/types.rs
    crates/z00z_storage/src/checkpoint/exec_input.rs
    crates/z00z_storage/src/checkpoint/link.rs
    crates/z00z_storage/src/checkpoint/audit.rs
    crates/z00z_storage/src/checkpoint/ids.rs
    crates/z00z_storage/src/checkpoint/artifact_types.rs
    crates/z00z_storage/src/checkpoint/artifact_stmt.rs
    crates/z00z_crypto/src/claim/mod.rs
    crates/z00z_crypto/src/claim/v2.rs
    crates/z00z_crypto/src/claim/proof.rs
    crates/z00z_crypto/src/zkpack.rs
    crates/z00z_crypto/src/kdf_domains.rs
    crates/z00z_crypto/src/crypto_constants.rs
    crates/z00z_core/src/assets/version.rs
    crates/z00z_core/src/assets/leaf.rs
    crates/z00z_wallets/src/core/tx/prover.rs
    crates/z00z_wallets/src/core/tx/claim_wire_types.rs
    crates/z00z_wallets/src/core/tx/claim_tx.rs
    crates/z00z_wallets/src/core/claim/claim_receipt.rs
    crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl.rs
    crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key.rs
    crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs
    crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs
  </files>
  <action>Execute `036-01 Freeze Explicit Wire Discriminants, Live Lanes, And Literal Contracts` exactly as written in `036-TODO-2.md`. Freeze non-test rows `1-18`, `26`, `29-35`, and `48-55` plus literal rows `1-25` with no speculative rename or delete work. Keep explicit discriminants and lane markers such as `JmtSerVersion::V1`, `PrepSnapshotVersion::V1`, `CheckpointVersion::V1`, `ClaimRootVer::V1/V2`, `ClaimProofVer::V1/V2`, `AssetPackVersion::V1Basic`, `AssetPackVersion::V2Memo`, `DecodedAssetPack::V1Basic`, `DecodedAssetPack::V2Memo`, `claim_stmt_hash_v2`, `CLAIM_PROOF_V2`, `export_public_material_v2`, `export_public_material_v2` registrations, `encode_single_v2`, `encode_dual_v2`, and `decode_v2` at their current Rust and literal spellings. Treat direct consumers in `claim_tx.rs` and `claim_tx_verifier_impl_proof.rs` as supporting call sites for the same held row-owned literals rather than as new row owners. Treat all claim-v2, checkpoint, RPC, zkpack, KDF-domain, and address-v2 rows as hold or keep items unless the source spec is updated first. Do not invent unsuffixed aliases, do not flatten transport or error strings, do not collapse future-reserved helpers into live cleanup, and do not widen execution into comments, benches, examples, or stale suffix-spec notes.</action>
  <required_test_surfaces>
    crates/z00z_crypto/tests/test_claim_v2_contract.rs
    crates/z00z_core/src/assets/leaf_tests.rs
    crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs
    crates/z00z_wallets/tests/test_asset_pack_v2_memo.rs
    crates/z00z_wallets/src/core/address/z00z_address/tests.rs
    crates/z00z_storage/src/serialization/artifact.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - Every Step 0 production row and literal row remains explicitly frozen with a row-backed no-rename justification that still matches the live code path.
    - No claim, checkpoint, storage, RPC, or address-v2 literal or discriminant changed under cleanup pretext.
    - Any missed phase-owned live surface is first added to `036-a1-versioning-spec.md` and `036-TODO-2.md` before execution proceeds.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_crypto --release --features test-fast --test test_claim_v2_contract</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --test test_rpc_wiring_spec_a</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --test test_asset_pack_v2_memo</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified by deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic gates; continue additional review or fix cycles until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Step 0 freeze rows remain intact, literal-backed contracts are unchanged, and the canonical spec plus backlog still own the entire executed Step 0 surface truthfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw inventory -> implementation edits | Step 0 rows must remain frozen exactly as classified in the canonical spec. |
| claim and RPC literals -> runtime behavior | Accidental rename of live claim or RPC strings would silently break transport contracts. |
| future-reserved helpers -> cleanup pass | Flattening dormant versioned helpers would destroy explicit upgrade boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-04-01 | T | Step 0 discriminants and literals | mitigate | Freeze only; reject any rename/delete unless the source spec is updated first and rerun the row audit before continuing. |
| T-036-04-02 | I | claim-v2 and RPC string surfaces | mitigate | Keep `CLAIM_PROOF_V2`, claim-v2 errors, and `wallet.key.export_public_material_v2` byte-for-byte unchanged and revalidate the adjacent contract tests. |
| T-036-04-03 | E | future-reserved address-v2 helpers | mitigate | Keep `encode_single_v2`, `encode_dual_v2`, and `decode_v2` explicit and out of cleanup scope in this wave. |
</threat_model>

<verification>
The executor must finish this plan with Step 0 row coverage still matching the canonical raw inventory, no widened task surface, and recorded evidence that every preserved contract remained unchanged.
</verification>

<success_criteria>
`036-01 Freeze Explicit Wire Discriminants, Live Lanes, And Literal Contracts` is closed exactly on the Step 0 raw-row set, with literal contracts and live version lanes preserved unchanged and no extra phase-owned row left undocumented.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-04-SUMMARY.md`.
</output>