---
phase: 036-rename
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/036-rename/036-a2-legacy-removing-spec.md
  - .planning/phases/036-rename/036-TODO-3.md
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Every production `legacy` owner in the embedded removal snapshot is classified explicitly as delete-blocked, delete-only, or historical-test-only.
    - Every compatibility-owned production owner records the exact persisted, public, or transport surface it still protects.
    - The empty `examples/**/*.rs` and `benches/**/*.rs` buckets remain re-proven rather than assumed.
  artifacts:
    - path: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      provides: Wave 0 owner-class freeze and deletion-blocker notes for every production legacy owner.
    - path: .planning/phases/036-rename/036-TODO-3.md
      provides: Backlog wording aligned to the frozen owner classes and forbidden-scope boundaries.
  key_links:
    - from: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      to: .planning/phases/036-rename/036-TODO-3.md
      via: Wave 0 owner-class freeze feeding 036-12 and 036-13 authorization boundaries
      pattern: owner classes, protected surfaces, and empty-bucket proofs agree across both files
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-11 PLAN

<objective>
Execute `036-11 Freeze Legacy Owners Into Explicit Delete Dispositions` from the self-contained legacy-removal backlog.

Purpose: freeze every production `legacy` owner into one explicit delete disposition before any code work is allowed.
Output: `036-a2-legacy-removing-spec.md` and `036-TODO-3.md` record a row-exact delete inventory for every production owner plus the exact blocker for each row that cannot yet be deleted.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-a2-legacy-removing-spec.md
@.planning/phases/036-rename/036-TODO-3.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Treat `036-a2-legacy-removing-spec.md` and `036-TODO-3.md` as the only live authority for this continuation slice; `036-TODO-2.md` remains completed history only.
- Do not touch Rust source or test files in this plan. This plan is documentation-backed Wave 0 classification only.
- Preserve the existing grouped removal table as the stable owner table. Add Wave 0 classification meaning inside the canonical spec file, not in a new manifest or side register.
- If any production owner cannot be scheduled for deletion without changing code or inventing contract meaning, mark it unresolved in `036-a2-legacy-removing-spec.md`, stop the wave, and re-plan instead of guessing.
- Preserve the verified empty `examples/**/*.rs` and `benches/**/*.rs` buckets unless a fresh declaration-level Rust scan proves new hits appeared.
- In every auto-task verify block, run the bootstrap gate first, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun deterministic checks after each fix cycle, and do not stop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-11.A Freeze explicit delete dispositions in the canonical removal spec</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — `Detailed Legacy Removal Table`, `Deletion Blockers`, and `Delete-Only Candidate Class`.
    - `.planning/phases/036-rename/036-TODO-3.md` — `Detailed Legacy Removal Table`, `Decision Summary`, and `036-11` section.
  </read_first>
  <action>Add Wave 0 delete results directly into `036-a2-legacy-removing-spec.md` for every production owner in the embedded `Src Scope Removal Targets` table. For each row, record exactly one delete disposition: `delete in Wave 2`, `delete in Wave 3`, or `blocked until prerequisite proof`. Do not create a parallel manifest, spreadsheet, or new planning file. Keep the grouped table as the stable owner table, and add only the minimum new spec structure needed to freeze delete truth unambiguously.</action>
  <required_test_surfaces>
    Canonical removal table consistency and row-exact owner coverage
  </required_test_surfaces>
  <acceptance_criteria>
    - No production row in the `Src Scope Removal Targets` table remains without a delete disposition.
    - Every row has exactly one explicit delete disposition.
    - No Rust or test code was modified during this task.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "\\blegacy\\b" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The canonical removal spec freezes one explicit delete disposition for every production `legacy` row in scope.</done>
</task>

<task type="auto">
  <name>036-11.B Record the exact deletion blocker for blocked rows</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — `Deletion Blockers`, `Proof Requirements Before Any Delete`, and any Wave 0 classification added by task `036-11.A`.
    - Representative production owners listed in `036-TODO-3.md` under `036-11`.
  </read_first>
  <action>For every production row marked `blocked until prerequisite proof`, extend `036-a2-legacy-removing-spec.md` with the exact blocker that still prevents deletion: persisted value, decode lane, public feature gate, transport literal, schema marker, or public identifier. Use repository-backed wording tied to the listed owner file and symbol; do not write generic statements like `compatibility helper`. If a row has more than one blocker, list them explicitly so later waves can delete the blocker rather than preserve it.</action>
  <required_test_surfaces>
    Contract-protection notes remain repository-backed and row-specific
  </required_test_surfaces>
  <acceptance_criteria>
    - Every blocked production row names the exact blocker that still prevents deletion.
    - No row is described as an intentionally retained non-deletion state.
    - No owner-class note relies on speculation or grep-only inference.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "LEGACY|legacy-claim-v1|decode_legacy|Legacy" crates/z00z_storage crates/z00z_wallets crates/z00z_crypto crates/z00z_core --glob '*.rs'</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Every blocked production row now records the exact blocker that still prevents deletion.</done>
</task>

<task type="auto">
  <name>036-11.C Sync the backlog to the frozen Wave 0 delete inventory</name>
  <files>.planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-TODO-3.md` — `036-11`, `036-12`, and `036-13` sections.
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — Wave 0 classification content created by tasks `036-11.A` and `036-11.B`.
  </read_first>
  <action>Update `036-TODO-3.md` so the backlog text for `036-11`, `036-12`, and `036-13` references the frozen delete inventory and exact blocker notes from `036-a2-legacy-removing-spec.md`. Keep the backlog self-contained enough to drive `036-12-PLAN.md`, but do not duplicate the entire spec again. Preserve the `examples/**/*.rs` and `benches/**/*.rs` empty-bucket rule explicitly unless a fresh scan proved otherwise.</action>
  <required_test_surfaces>
    Backlog/spec alignment and empty-bucket proof wording
  </required_test_surfaces>
  <acceptance_criteria>
    - `036-TODO-3.md` reflects the same delete dispositions and blocker language as `036-a2-legacy-removing-spec.md`.
    - `036-12` and `036-13` no longer rely on implicit preserve-or-non-deletion semantics.
    - The empty example/bench buckets remain explicit and truthful.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "examples/\*\*/\.rs|benches/\*\*/\.rs|delete in Wave 2|delete in Wave 3|blocked until prerequisite proof|historical-test-only" .planning/phases/036-rename/036-a2-legacy-removing-spec.md .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The backlog is aligned to the frozen Wave 0 delete truth and remains self-contained for the next plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removal table -> backlog | Classification truth must flow from the canonical spec into the backlog without drift. |
| repository evidence -> owner class | Every class assignment must be backed by actual code ownership, not grep-only guesswork. |
| empty bucket scan -> future work | Example/bench emptiness must be re-proven before those buckets remain excluded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-11-01 | T | delete inventory freeze | mitigate | Record one explicit delete disposition per production row inside `036-a2-legacy-removing-spec.md` and reject ambiguous or duplicate assignments. |
| T-036-11-02 | R | blocker evidence | mitigate | Tie every blocked row to an exact persisted/public/transport blocker named in repository-backed wording. |
| T-036-11-03 | I | backlog self-containment | mitigate | Sync `036-TODO-3.md` to the frozen Wave 0 delete truth so later plans do not infer missing delete semantics. |
</threat_model>

<verification>
This plan closes only when the Wave 0 delete inventory is explicit, row-exact, and synchronized between the canonical removal spec and the self-contained backlog.
</verification>

<success_criteria>
`036-11 Freeze Legacy Owners Into Explicit Delete Dispositions` is complete only when every production `legacy` owner has one explicit delete disposition and every blocked row names the blocker that must be removed.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-11-SUMMARY.md`.
</output>