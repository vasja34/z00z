---
phase: 036-rename
plan: 6
type: execute
wave: 3
depends_on: [036-05]
files_modified:
  - crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs
  - crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
  - crates/z00z_wallets/src/core/wallet/errors_types.rs
  - crates/z00z_wallets/src/db/wallet_validate.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Step 2 renames are limited to the three raw rows authorized by the canonical spec.
    - Internal rename work lands without changing outward RPC strings or transport contracts.
    - All direct call sites move to the unsuffixed internal names.
  artifacts:
    - path: crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs
      provides: internal params type renamed to `KeyExportPublicParams`.
    - path: crates/z00z_wallets/src/core/wallet/errors_types.rs
      provides: diagnostic enum variant renamed to `IntegrityMismatch`.
    - path: crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs
      provides: internal helper renamed to `export_public_material_impl`.
  key_links:
    - from: crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs
      to: crates/z00z_wallets/src/adapters/rpc/methods/key.rs
      via: internal RPC wiring and request dispatch
      pattern: internal rename does not alter wallet.key.export_public_material_v2 outward surface
    - from: crates/z00z_wallets/src/core/wallet/errors_types.rs
      to: crates/z00z_wallets/src/db/wallet_validate.rs
      via: diagnostic code mapping
      pattern: renamed enum variant still maps to the same persisted diagnostic semantics
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-06 PLAN

<objective>
Execute the Step 2 internal-rename slice from the canonical Phase 036 backlog.

Purpose: remove embedded version noise only where the enclosing scope already carries the live version boundary, without disturbing outward RPC or transport surfaces.
Output: `036-03 Rename Internal Wiring And Diagnostic Noise` is closed on rows `22-23` and `28`, with all internal call sites updated and all public-facing strings unchanged.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-2.md
@.planning/phases/036-rename/036-a1-versioning-spec.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Preserve the exact task wording from `036-TODO-2.md`; do not rename the task or widen its ownership beyond rows `22-23` and `28`.
- Honor D-01 through D-20 from `036-CONTEXT.md`, especially D-03, D-07, D-11, D-12, D-15, D-16, and D-19.
- The raw inventory is the only task-generation surface. If execution finds a missed caller or producer for one of the three Step 2 rows, update `036-a1-versioning-spec.md` first, then `036-TODO-2.md`, and only then resume code edits.
- Treat the `Compact Patch-Plan By Disputed Row Group` as a grouping aid only; if it ever disagrees with a raw row, the raw row wins.
- Before any edit, complete `read_first` and revalidate that the task-local `files`, `required_test_surfaces`, `acceptance_criteria`, `verify`, and `done` blocks still match the canonical raw-row ownership in `036-a1-versioning-spec.md` and `036-TODO-2.md`.
- If execution edits `036-a1-versioning-spec.md` or `036-TODO-2.md`, stop the current wave immediately and rerun review plus planning before any further code edits, closure checks, or summary claims.
- In every auto-task verify block, run validation in this order: bootstrap gate first, then `cargo test --release --features test-fast --features wallet_debug_dump`, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun the deterministic gates after each fix cycle, and do not stop the review loop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-03 Rename Internal Wiring And Diagnostic Noise</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-01 through D-20 and the `Task Transfer Matrix` row for `036-03`.
    - `.planning/phases/036-rename/036-TODO-2.md` — the full `036-03` section, `Row-Step Coverage Ledger`, and `Completion Gate`.
    - `.planning/phases/036-rename/036-a1-versioning-spec.md` — `Implementation Examples`, `Execution Order By Row Range`, and `Raw Inventory Appendix -> Non-Test Signatures`.
  </read_first>
  <files>
    crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs
    crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
    crates/z00z_wallets/src/core/wallet/errors_types.rs
    crates/z00z_wallets/src/db/wallet_validate.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs
  </files>
  <action>Execute `036-03 Rename Internal Wiring And Diagnostic Noise` exactly as written in `036-TODO-2.md`. Rename only non-test rows `22-23` and `28` at the Rust symbol layer: `KeyExportPublicV2Params` becomes `KeyExportPublicParams`, `IntegrityMismatchV1` becomes `IntegrityMismatch`, and `export_public_material_v2_impl` becomes `export_public_material_impl`. Update every direct internal caller and mapping site in `wallet_dispatcher_wiring.rs`, `wallet_dispatcher_wiring_register.rs`, `wallet_validate.rs`, `key.rs`, `server_requests.rs`, and `server.rs` so the unsuffixed names are the only internal spelling. Keep the public RPC method name `wallet.key.export_public_material_v2`, its transport strings, and the outward claim or RPC literal contracts byte-for-byte unchanged. Reject any rename outside these three raw rows unless the canonical source spec is updated first.</action>
  <required_test_surfaces>
    crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs
    crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - The only symbol renames in this plan are the three Step 2 rows owned by the canonical spec.
    - All direct call sites use `KeyExportPublicParams`, `IntegrityMismatch`, and `export_public_material_impl`.
    - The outward RPC method string and any transport literal remain byte-for-byte unchanged.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --test test_rpc_wiring_spec_a</automated>
    <automated>rg -n "KeyExportPublicV2Params|IntegrityMismatchV1|export_public_material_v2_impl|wallet\.key\.export_public_material_v2" crates/z00z_wallets/src crates/z00z_wallets/tests</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified by deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic gates.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic gates; continue additional review or fix cycles until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Rows `22-23` and `28` are renamed at the internal Rust-symbol layer only, all call sites are updated, and all outward RPC and transport contracts remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal wiring rename -> outward RPC surface | Internal simplification must not leak into public method strings or transport contracts. |
| diagnostic enum rename -> persisted or mapped codes | Internal rename must not alter diagnostic semantics observed by validation code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-06-01 | T | internal Step 2 symbols | mitigate | Limit edits strictly to rows `22-23` and `28`; reject any rename outside that set unless the source spec changes first. |
| T-036-06-02 | I | outward RPC method surface | mitigate | Keep `wallet.key.export_public_material_v2` and all transport literals byte-for-byte unchanged while renaming only internal helpers. |
| T-036-06-03 | R | wallet diagnostic code mapping | mitigate | Revalidate `wallet_validate.rs` and related tests so `IntegrityMismatch` preserves the existing diagnostic meaning and code path. |
</threat_model>

<verification>
The executor must finish this plan with only the three Step 2 rows renamed, every direct call site updated, and all outward RPC or transport surfaces unchanged.
</verification>

<success_criteria>
`036-03 Rename Internal Wiring And Diagnostic Noise` is closed exactly on rows `22-23` and `28`, with internal symbol cleanup complete and public-facing RPC or transport behavior unchanged.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-06-SUMMARY.md`.
</output>