---
status: testing
phase: 036-rename
source:
  - 036-20-SUMMARY.md
  - 036-21-SUMMARY.md
  - 036-22-SUMMARY.md
  - 036-23-SUMMARY.md
  - 036-24-PLAN.md
started: 2026-04-21T18:03:27Z
updated: 2026-04-21T18:03:27Z
---

# Phase 036 UAT

## Current Test

number: 1
name: Public crypto facade still exposes the canonical module roots after the path-group rehome
expected: |
  Run `cargo test -p z00z_crypto --release --features test-fast --lib`.

  The crate should compile and the public-surface guard should still see the expected stable roots:
  `pub mod protocol`, `pub mod vendor`, `pub mod aead`, `pub mod hash`, `pub mod kdf`, and `pub mod backend`.
  The result should not reintroduce a second public dialect for the crypto facade.
awaiting: next test run

## Tests

### 1. Public crypto facade still exposes the canonical module roots after the path-group rehome

expected: Run `cargo test -p z00z_crypto --release --features test-fast --lib`. The crate should compile and the public-surface guard should still see the expected stable roots: `pub mod protocol`, `pub mod vendor`, `pub mod aead`, `pub mod hash`, `pub mod kdf`, and `pub mod backend`. The result should not reintroduce a second public dialect for the crypto facade.
result: passed

### 2. Claim contract stays on the new root-version contract

expected: Run `cargo test -p z00z_crypto --release --features test-fast --test test_claim_contract`. The claim regression suite should pass, `claim_stmt_hash()` should keep using the `claim_contract` label, `CLAIM_ROOT_VERSION` should remain exported, and `ClaimSourceProof`/`ClaimStmt` mismatch checks should still fail closed on the raw root-version contract.
result: [pending]

### 3. Wallet, storage, and simulator claim consumers still interoperate

expected: Run `cargo test -p z00z_wallets --release --features test-fast --test test_claim_tx -- --nocapture`, `cargo test -p z00z_storage --release --test test_claim_source_proof -- --nocapture`, and `cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_pkg_crypto_support -- --nocapture`. The claim consumers should keep accepting the raw-byte root-version contract without reintroducing the retired shim names.
result: [pending]

### 4. The partial 036-20 boundary remains separate from the path-group rehome

expected: Re-read `036-20-SUMMARY.md` alongside the recorded residue scans for `CompatRoot` and `SeqSecureRngProvider`. The result should remain explicitly partial, with the surviving constructor/storage tail kept separate from the path-group rehome and not collapsed into a false closeout.
result: [pending]

## Summary

total: 4
passed: 1
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps

[none yet]
