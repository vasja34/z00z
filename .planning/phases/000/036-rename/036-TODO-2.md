# 036-TODO-2

Canonical design source:

- [036-a1-versioning-spec](./036-a1-versioning-spec.md)

Execution rules:

- execute this file strictly in the order defined by `Execution Order By Row Range` in `036-a1-versioning-spec.md`;
- treat `036-a1-versioning-spec.md` as normative for requirement meaning and this file as normative for execution order;
- use the `Raw Inventory Appendix` in `036-a1-versioning-spec.md` as the only task-generation surface for this backlog;
- do not import execution meaning from `036-a1-suffixes-spec.md`, `036-TODO-1.md`, or older planning notes; those may inform format only, not task truth;
- preserve one task owner per step and one path-specific interpretation per raw row; do not collapse identical signatures across different files;
- keep literal-backed contracts frozen in this wave unless the source spec explicitly upgrades them into a protocol-migration task;
- treat the `Compact Patch-Plan By Disputed Row Group` as a grouping aid only; if it ever disagrees with a raw row, the raw row wins;
- if execution discovers a new design constraint, update `036-a1-versioning-spec.md` first and only then update this backlog;
- if execution edits `036-a1-versioning-spec.md` or this backlog, stop the current wave immediately and rerun review plus planning before any further code edits, closure checks, or closeout claims;
- before starting any numbered task, complete its `MANDATORY pre-read` block and verify that its `Files`, `Tests`, and `Exit condition` still match the source spec.
- in every task verify pass, run deterministic gates first and use AI review as one evidence pass only; do not rerun AI review until it becomes green.

## Decision Summary

The execution baseline for this backlog is:

1. use `036-a1-versioning-spec.md` as the only source of truth for embedded-versioning cleanup meaning;
2. preserve explicit wire discriminants, coexisting live lanes, and literal-backed contracts as hold or keep items in this wave;
3. preserve compatibility shims and compatibility read-import lanes until code-backed retirement proof exists;
4. perform immediate rename work only for rows whose surrounding scope already carries the live version boundary or whose stored value remains unchanged;
5. keep paired legacy/public outward contracts frozen until a later publication or paired-lane migration explicitly authorizes simplification;
6. execute local and test-only cleanup only after production naming decisions are stable and never let test cleanup redefine protocol semantics;
7. close the phase only when every step still maps cleanly back to the raw inventory and no second task-generation layer has been introduced.

## Dependency Chain

Execution dependency chain:

1. `036-01` freeze explicit wire discriminants, live lanes, and literal-backed contracts
2. `036-02` preserve compatibility shims and compatibility read-import lanes
3. `036-03` rename current internal wiring and diagnostic noise
4. `036-04` rename current single-version internal or persisted identifiers without changing encoded values
5. `036-05` hold paired legacy/public outward contracts
6. `036-06` clean local and test-only residue after production naming stabilizes
7. `036-07` run row-coverage validation and regression closure

Hard dependencies:

- `036-02` depends on `036-01`
- `036-03` depends on `036-01` and `036-02`
- `036-04` depends on `036-01` and `036-02`
- `036-05` depends on `036-01` through `036-04`
- `036-06` depends on `036-01` through `036-05`
- `036-07` depends on `036-01` through `036-06`

## File-First Implementation Order

Edit order by file cluster:

1. storage and checkpoint contract files: `crates/z00z_storage/src/serialization/artifact.rs`, `crates/z00z_storage/src/snapshot/types.rs`, `crates/z00z_storage/src/checkpoint/exec_input.rs`, `crates/z00z_storage/src/checkpoint/link.rs`, `crates/z00z_storage/src/checkpoint/audit.rs`, `crates/z00z_storage/src/checkpoint/ids.rs`, `crates/z00z_storage/src/checkpoint/artifact_types.rs`, `crates/z00z_storage/src/checkpoint/artifact_stmt.rs`
2. live claim and crypto contract files: `crates/z00z_crypto/src/claim/mod.rs`, `crates/z00z_crypto/src/claim/v2.rs`, `crates/z00z_crypto/src/claim/proof.rs`, `crates/z00z_crypto/src/zkpack.rs`, `crates/z00z_crypto/src/kdf_domains.rs`, `crates/z00z_crypto/src/crypto_constants.rs`, `crates/z00z_crypto/src/aead_aad.rs`
3. live asset-lane files: `crates/z00z_core/src/assets/version.rs`, `crates/z00z_core/src/assets/leaf.rs`
4. compatibility-hold and internal-rename wallet files: `crates/z00z_wallets/src/core/tx/spending.rs`, `crates/z00z_wallets/src/core/tx/asset_selector.rs`, `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs`, `crates/z00z_wallets/src/core/wallet/errors_types.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs`
5. single-version identifier files: `crates/z00z_wallets/src/db/index_codecs.rs`, `crates/z00z_wallets/src/db/schema_keys.rs`, `crates/z00z_wallets/src/wasm/schema_keys.rs`, `crates/z00z_wallets/src/core/key/seed_cipher_container.rs`, `crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs`, `crates/z00z_wallets/src/core/claim/claim_receipt.rs`, `crates/z00z_wallets/src/core/tx/fee_estimator.rs`, `crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs`, `crates/z00z_wallets/src/core/security/password.rs`
6. outward-contract hold files: `crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs`, `crates/z00z_wallets/src/core/chain/receiver_card_record.rs`
7. local and test cleanup files named in `Raw Inventory Appendix -> Local And Test-Only Residue`
8. validation rerun and scan artifacts only after all production and test edits are complete

## Validation Matrix

| 036-a1-versioning-spec source | Required theme | TODO coverage | Status |
| --- | --- | --- | --- |
| `Raw-First Execution Model` | raw inventory is the only task-generation surface | execution rules; `036-01` through `036-07`; row-step coverage ledger | Mapped as execution guardrail |
| `Manual Broad-Scan Residual Disposition` | only path-specific version-bearing contracts enter the task surface | execution rules; `036-01`; `036-02`; `036-07` | Mapped as scope filter |
| `Execution Order By Row Range` | six-step execution order with hold/rename sequencing | dependency chain; `036-01` through `036-06`; `Completion Gate` | Validated mapped |
| `Implementation Examples` | internal rename vs literal hold vs coexistence-lane keep behavior | `036-01`; `036-03`; `036-04`; task exit conditions | Mapped as translation oracle |
| `Raw Inventory Appendix -> Non-Test Signatures` | production row ownership and per-path semantics | `036-01` through `036-05`; file-first order; row-step coverage ledger | Validated mapped |
| `Raw Inventory Appendix -> Literal-Backed Contracts` | literal and error strings are hold-only in this wave | `036-01`; `036-07`; explicit phase boundary | Mapped as preservation constraint |
| `Raw Inventory Appendix -> Local And Test-Only Residue` | cleanup-only residue must follow production naming | `036-06`; `036-07` | Validated mapped |
| `Compact Patch-Plan By Disputed Row Group` | disputed row groups resolve through raw rows only | `036-01` through `036-06`; row-step coverage ledger | Mapped with explicit no-duplication rule |
| `Table-to-Task Translation Protocol` | one path-specific interpretation per row, no extra owner layer | execution rules; all task checklists; row-step coverage ledger | Mapped as translation discipline |
| `Execution Step Completion Rules` | phase closure needs row-backed step completion and no second task surface | `036-07`; `Completion Gate` | Validated mapped |
| `Pitfall Guards` and `Non-Negotiable Translation Rules` | no speculative collapse, no protocol-literal rename, no cross-file row merge | execution rules; explicit phase boundary; all task exit conditions | Mapped as guardrail |

## Explicit Phase Boundary

The following topics are intentionally out of scope for this backlog:

- do not pull task meaning from `036-a1-suffixes-spec.md`; this backlog is versioning-spec-backed only;
- do not invent a parallel register, facade, migration tracker, or second ownership table outside `036-a1-versioning-spec.md`;
- do not rename wire/schema discriminants, literal-backed contracts, RPC strings, or error strings in this wave unless the source spec explicitly says otherwise;
- do not convert hold-only rows into rename-now work because the future survivor name looks cleaner;
- do not collapse coexisting V1/V2 production lanes, future-reserved address helpers, or live claim-v2 outer-lane markers into unsuffixed names in this backlog;
- do not let local/test cleanup lead production naming or erase explicit legacy/V1/V2 scenario coverage;
- do not treat bench/example/bin output, generic logging text, or comments as part of the execution surface for this TODO.

## Concrete Execution Tasks

### 036-01 Freeze Explicit Wire Discriminants, Live Lanes, And Literal Contracts

Spec references:

- `Execution Order By Row Range` step 0
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Raw Inventory Appendix -> Literal-Backed Contracts`
- `Compact Patch-Plan By Disputed Row Group`

MANDATORY pre-read in `036-a1-versioning-spec.md`:

- section `Manual Broad-Scan Residual Disposition`
- section `Execution Order By Row Range`
- section `Raw Inventory Appendix -> Non-Test Signatures`
- section `Raw Inventory Appendix -> Literal-Backed Contracts`

- [ ] Freeze non-test rows `1-18`, `26`, `29-35`, and `48-55` exactly as keep/hold items with no speculative rename work.
- [ ] Freeze literal rows `1-25` exactly as hold items and confirm that no transport, domain-separation, or error-string contract is being flattened into naming cleanup.
- [ ] Verify that every Step 0 row still has a row-backed no-rename justification that matches the current code path.
- [ ] Supporting callers that consume Step 0 held literals may be reconciled as dependent call sites only; they do not create new raw-row ownership.
- [ ] If any Step 0 caller or producer surface was missed by the spec, update the source spec first instead of extending this TODO with ad hoc rows.

Files:

- `crates/z00z_storage/src/serialization/artifact.rs`
- `crates/z00z_storage/src/snapshot/types.rs`
- `crates/z00z_storage/src/checkpoint/exec_input.rs`
- `crates/z00z_storage/src/checkpoint/link.rs`
- `crates/z00z_storage/src/checkpoint/audit.rs`
- `crates/z00z_storage/src/checkpoint/ids.rs`
- `crates/z00z_storage/src/checkpoint/artifact_types.rs`
- `crates/z00z_storage/src/checkpoint/artifact_stmt.rs`
- `crates/z00z_crypto/src/claim/mod.rs`
- `crates/z00z_crypto/src/claim/v2.rs`
- `crates/z00z_crypto/src/claim/proof.rs`
- `crates/z00z_crypto/src/zkpack.rs`
- `crates/z00z_crypto/src/kdf_domains.rs`
- `crates/z00z_crypto/src/crypto_constants.rs`
- `crates/z00z_core/src/assets/version.rs`
- `crates/z00z_core/src/assets/leaf.rs`
- `crates/z00z_wallets/src/core/tx/prover.rs`
- `crates/z00z_wallets/src/core/tx/claim_wire_types.rs`
- `crates/z00z_wallets/src/core/tx/claim_tx.rs`
- `crates/z00z_wallets/src/core/claim/claim_receipt.rs`
- `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl.rs`
- `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key.rs`
- `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs`
- `crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs`

Tests:

- [ ] preserve and rerun the adjacent contract and lane tests in:
  - `crates/z00z_crypto/tests/test_claim_v2_contract.rs`
  - `crates/z00z_core/src/assets/leaf_tests.rs`
  - `crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs`
  - `crates/z00z_wallets/tests/test_asset_pack_v2_memo.rs`
  - `crates/z00z_wallets/src/core/address/z00z_address/tests.rs`
  - `crates/z00z_storage/src/serialization/artifact.rs`

Exit condition:

- every Step 0 production and literal row is still explicitly frozen by row-backed justification, and no protocol, RPC, storage, or cryptographic contract was renamed under the pretext of cleanup.

### 036-02 Preserve Compatibility Shims And Compatibility Read-Import Lanes

Spec references:

- `Execution Order By Row Range` step 1
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Compact Patch-Plan By Disputed Row Group`

MANDATORY pre-read in `036-a1-versioning-spec.md`:

- section `Execution Order By Row Range`
- section `Raw Inventory Appendix -> Non-Test Signatures`
- section `Execution Step Completion Rules`

- [ ] Keep non-test rows `20-21`, `24-25`, and `27` as compatibility-owned rows with no rename-now emission.
- [ ] Link each row to its concrete compatibility lane, decode/import window, or public shim justification before any later retirement work is proposed.
- [ ] Record any needed retirement proof as future follow-up instead of smuggling it into this cleanup backlog.
- [ ] Confirm that Step 1 still has zero rename tasks in this wave.

Files:

- `crates/z00z_wallets/src/core/tx/spending.rs`
- `crates/z00z_wallets/src/core/tx/mod.rs`
- `crates/z00z_storage/src/assets/proof.rs`
- `crates/z00z_storage/src/assets/store_internal/redb_backend_state.rs`
- `crates/z00z_storage/src/assets/store_internal/redb_backend_helpers.rs`
- `crates/z00z_wallets/src/core/tx/asset_selector.rs`

Tests:

- [ ] preserve or extend compatibility coverage in:
  - `crates/z00z_wallets/src/core/tx/spending.rs`
  - `crates/z00z_wallets/src/core/tx/asset_selector_tests.rs`
  - `crates/z00z_storage/src/assets/store_internal/test_whitebox_proofs.rs`
  - `crates/z00z_storage/tests/test_redb_rehydrate.rs`

Exit condition:

- every Step 1 row is explicitly classified as compatibility-owned with a concrete retirement precondition, and this backlog emits no premature rename or delete work for those rows.

### 036-03 Rename Internal Wiring And Diagnostic Noise

Spec references:

- `Execution Order By Row Range` step 2
- `Implementation Examples`
- `Raw Inventory Appendix -> Non-Test Signatures`

MANDATORY pre-read in `036-a1-versioning-spec.md`:

- section `Implementation Examples`
- section `Execution Order By Row Range`
- section `Raw Inventory Appendix -> Non-Test Signatures`

- [x] Rename non-test rows `22-23` and `28` only at the Rust symbol layer.
- [x] Update all direct call sites and internal references to `KeyExportPublicParams`, `IntegrityMismatch`, and `export_public_material_impl`.
- [x] Verify that the public RPC method name, transport strings, and outward claim/RPC contracts remain byte-for-byte unchanged.
- [x] Reject any proposed rename outside these three raw rows unless the source spec is first updated.

Files:

- `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs`
- `crates/z00z_wallets/src/core/wallet/errors_types.rs`
- `crates/z00z_wallets/src/db/wallet_validate.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server_requests.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs`

Tests:

- [x] extend or rerun internal-facing coverage in:
  - `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs`
  - `crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs`

Exit condition:

- Step 2 rows are renamed in code and at all call sites, while all outward RPC and transport contracts remain unchanged.

### 036-04 Rename Current Single-Version Internal And Persisted Identifiers Without Changing Encoded Values

Spec references:

- `Execution Order By Row Range` step 3
- `Implementation Examples`
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Execution Step Completion Rules`

MANDATORY pre-read in `036-a1-versioning-spec.md`:

- section `Execution Order By Row Range`
- section `Implementation Examples`
- section `Raw Inventory Appendix -> Non-Test Signatures`
- section `Execution Step Completion Rules`

- [x] Rename non-test rows `36-43`, `45-47`, and `52` only at the Rust identifier layer.
- [x] Keep encoded bytes, schema-string contents, numeric persisted values, and literal payloads byte-for-byte unchanged.
- [x] Reconcile compiler, serialization, and RPC call sites after each rename cluster rather than batching speculative follow-up changes.
- [x] Direct consumers touched only because they depend on Step 3 rows remain supporting call sites inside the same owned row set; they do not create a new execution surface.
- [x] Verify that schema readers, storage decoders, and exported payloads still observe the same wire values after the rename pass.

Files:

- `crates/z00z_crypto/src/aead_aad.rs`
- `crates/z00z_wallets/src/db/index_codecs.rs`
- `crates/z00z_wallets/src/db/index_codecs_tx_time.rs`
- `crates/z00z_wallets/src/db/index_codecs_body.rs`
- `crates/z00z_wallets/src/db/schema_keys.rs`
- `crates/z00z_wallets/src/wasm/schema_keys.rs`
- `crates/z00z_wallets/src/db/wallet_validate.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store.rs`
- `crates/z00z_wallets/src/core/key/seed_cipher_container.rs`
- `crates/z00z_wallets/src/core/key/seed_cipher_container_crypto.rs`
- `crates/z00z_wallets/src/core/key/seed_cipher_persistence.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs`
- `crates/z00z_wallets/src/core/claim/claim_receipt.rs`
- `crates/z00z_wallets/src/core/tx/fee_estimator.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs`
- `crates/z00z_wallets/src/core/security/password.rs`
- `crates/z00z_wallets/src/core/security/password_checks.rs`

Tests:

- [x] preserve or extend storage, codec, and receipt coverage in:
  - `crates/z00z_wallets/tests/test_wallet_kdf_migration.rs`
  - `crates/z00z_wallets/tests/test_redb_wlt_open.rs`
  - `crates/z00z_wallets/tests/test_tx_fee.rs`
  - `crates/z00z_wallets/src/core/tx/fee_estimator_tests.rs`
  - `crates/z00z_wallets/src/core/key/seed_cipher_metadata_tests.rs`
  - `crates/z00z_wallets/src/core/key/seed_cipher_basic_tests.rs`
  - `crates/z00z_wallets/tests/test_key_manager.rs`
  - `crates/z00z_crypto/src/types_tests.rs`

Exit condition:

- every Step 3 row has an unsuffixed Rust identifier where required, and all underlying encoded, persisted, and literal contracts remain unchanged.

### 036-05 Hold Paired Legacy/Public Outward Contracts

Spec references:

- `Execution Order By Row Range` step 4
- `Raw Inventory Appendix -> Non-Test Signatures`
- `Compact Patch-Plan By Disputed Row Group`

MANDATORY pre-read in `036-a1-versioning-spec.md`:

- section `Execution Order By Row Range`
- section `Raw Inventory Appendix -> Non-Test Signatures`
- section `Execution Step Completion Rules`

- [x] Hold non-test rows `19` and `44` as blocked-by-contract rows with no speculative rename work.
- [x] Preserve the distinction between paired legacy-lane helpers and published outward receiver-card contracts.
- [x] If future simplification is desired, record the missing paired-lane or publication migration proof instead of editing the symbols in this wave.

Files:

- `crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs`
- `crates/z00z_wallets/src/db/redb_wallet_crypto.rs`
- `crates/z00z_wallets/src/core/chain/receiver_card_record.rs`

Tests:

- [x] preserve or extend outward-contract and pairing coverage in:
  - `crates/z00z_wallets/tests/test_receiver_card_record.rs`
  - `crates/z00z_wallets/tests/test_redb_wlt_open.rs`
  - `crates/z00z_wallets/tests/test_s5_record_gate.rs`

Exit condition:

- Step 4 rows are explicitly held as contract-blocked items, and this backlog emits no rename for them without new source-backed authorization.

### 036-06 Clean Local And Test-Only Residue After Production Naming Stabilizes

Spec references:

- `Execution Order By Row Range` step 5
- `Raw Inventory Appendix -> Local And Test-Only Residue`
- `Compact Patch-Plan By Disputed Row Group`

MANDATORY pre-read in `036-a1-versioning-spec.md`:

- section `Execution Order By Row Range`
- section `Raw Inventory Appendix -> Local And Test-Only Residue`
- section `Table-to-Task Translation Protocol`

- [x] Recheck local/test rows `1-92` only after `036-01` through `036-05` are stable.
- [x] Rename only cleanup-safe locals and fixture strings whose rows explicitly say `rename now`.
- [x] Preserve every explicit legacy, V1, V2, V3, or V4 scenario helper/test that the source spec classifies as `keep`.
- [x] Ensure local cleanup names reflect the final production terminology rather than inventing new semantics.

Files:

- `crates/z00z_core/src/assets/registry_tests.rs`
- `crates/z00z_core/tests/assets/test_integration_assets_test12.rs`
- `crates/z00z_core/src/assets/leaf_tests.rs`
- `crates/z00z_core/src/assets/version.rs`
- `crates/z00z_core/tests/assets/test_serial_id_encoding.rs`
- `crates/z00z_core/tests/assets/test_wire_format_snapshots.rs`
- `crates/z00z_core/tests/genesis/test_crypto_security.rs`
- `crates/z00z_crypto/src/commitments.rs`
- `crates/z00z_crypto/src/kdf_tests.rs`
- `crates/z00z_crypto/src/types_tests.rs`
- `crates/z00z_crypto/tests/test_claim_v2_contract.rs`
- `crates/z00z_crypto/tests/test_pedersen.rs`
- `crates/z00z_storage/src/assets/store_internal/test_whitebox_proofs.rs`
- `crates/z00z_storage/tests/test_redb_rehydrate.rs`
- `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs`
- `crates/z00z_wallets/src/core/address/address_manager/tests.rs`
- `crates/z00z_wallets/src/core/address/z00z_address/tests.rs`
- `crates/z00z_wallets/src/core/backup/backup_importer_tests.rs`
- `crates/z00z_wallets/src/core/key/seed_cipher_metadata_tests.rs`
- `crates/z00z_wallets/src/core/key/stealth_keys_tests.rs`
- `crates/z00z_wallets/src/core/wallet/snapshot_tests.rs`
- `crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs`
- `crates/z00z_wallets/src/services/wallet_service_tests.rs`
- `crates/z00z_wallets/src/services/wallet_service_store_transfer_import.rs`
- `crates/z00z_wallets/tests/test_adversarial.rs`
- `crates/z00z_wallets/tests/test_asset_pack_v2_memo.rs`
- `crates/z00z_wallets/tests/test_receiver_card_record.rs`
- `crates/z00z_wallets/tests/test_redb_wlt_open.rs`
- `crates/z00z_wallets/tests/test_rpc_wiring_spec_a.rs`
- `crates/z00z_wallets/tests/test_stealth_request.rs`
- `crates/z00z_wallets/tests/test_stealth_scanner_flow.rs`
- `crates/z00z_wallets/tests/test_tx_poison.rs`
- `crates/z00z_wallets/tests/test_wallet_kdf_migration.rs`

Tests:

- [x] rerun only the affected suites whose local/test residue changed;
- [x] keep explicit scenario tests green for all rows classified as `keep`;
- [x] verify that cleanup-only rename rows now use the simplified names from the source spec.

Exit condition:

- every local/test row has a resolved state consistent with the source spec, and no cleanup step erased intentional legacy or version-scenario coverage.

## Concrete Test Execution Tasks

### 036-07 Row-Coverage Validation And Regression Closure

Spec references:

- `Execution Step Completion Rules`
- `Pitfall Guards`
- `Non-Negotiable Translation Rules`

MANDATORY pre-read in `036-a1-versioning-spec.md`:

- section `Execution Step Completion Rules`
- section `Pitfall Guards`
- section `Non-Negotiable Translation Rules`

- [x] Verify that Steps `0-5` are all closed against the exact raw-row sets defined in the source spec.
- [x] Rerun targeted tests for every touched production or test seam and record any hold-only rows that were intentionally not edited.
- [x] Rerun the residual row scan used by the phase so any uncovered version-bearing signature is pushed back into the source spec before this backlog is considered complete.
- [x] Confirm that no second task-generation layer, no speculative row merge, and no protocol-literal rename was introduced during execution.

Files:

- `./036-a1-versioning-spec.md`
- `./036-TODO-2.md`

Tests:

- [x] targeted crate-level and file-level tests for every changed seam
- [x] residual scan proving no uncovered phase-owned version-bearing signatures remain
- [x] compiler and call-site validation for every renamed row

Exit condition:

- the executed code, the source spec, and this TODO agree on row ownership, step completion, and remaining hold-only contracts, with no uncovered phase-owned version-bearing residue.

## Row-Step Coverage Ledger

This ledger is the normative mapping from execution task to the raw-row sets in `036-a1-versioning-spec.md`. The raw inventory remains the detailed truth; this ledger only pins step ownership inside the backlog.

| Task ID | Source step | Raw rows owned by the task | Execution meaning |
| --- | --- | --- | --- |
| `036-01` | Step 0 | non-test `1-18`, `26`, `29-35`, `48-55`; literal `1-25` | freeze explicit wire discriminants, live lanes, and literal/error contracts |
| `036-02` | Step 1 | non-test `20-21`, `24-25`, `27` | preserve compatibility shims and compatibility read-import lanes |
| `036-03` | Step 2 | non-test `22-23`, `28` | rename internal wiring and diagnostic noise |
| `036-04` | Step 3 | non-test `36-43`, `45-47`, `52` | rename current single-version internal/persisted identifiers without changing encoded values |
| `036-05` | Step 4 | non-test `19`, `44` | hold paired legacy/public outward contracts |
| `036-06` | Step 5 | local/test `1-92` | clean rename-safe local/test residue after production naming stabilizes |
| `036-07` | Closure | Steps `0-5` as executed | validate row coverage, regressions, and no-extra-surface closure |

## Completion Gate

Global execution order is fixed and non-parallel for this backlog:

1. `036-01` must close before `036-02` starts.
2. `036-02` must close before `036-03` starts.
3. `036-03` must close before `036-04` starts.
4. `036-04` must close before `036-05` starts.
5. `036-05` must close before `036-06` starts.
6. `036-06` must close before `036-07` starts.

No task is complete unless all of the following are true:

1. its owned raw-row set still matches the source spec exactly;
2. its `Files`, `Tests`, and `Exit condition` blocks still describe the real touched seam;
3. any hold-only or keep-only rows under the task remain explicitly justified rather than silently untouched;
4. any rename-now rows under the task are closed without changing frozen literal, schema, RPC, or persisted-value contracts;
5. no new execution meaning was imported from `036-a1-suffixes-spec.md`, `036-TODO-1.md`, or stale notes;
6. `036-07` proves that the raw inventory is still the only task-generation surface and that no uncovered phase-owned version-bearing signature remains.
7. if the source spec or this backlog changed during execution, the active wave was stopped and re-planned before any further implementation or closure work resumed.
