---
phase: 036-rename
plan: 16
type: execute
wave: 6
depends_on: [036-11, 036-12, 036-13, 036-14, 036-15]
files_modified:
  - .planning/phases/036-rename/036-TODO-3.md
  - crates/z00z_simulator/src/scenario_1/stage_3.rs
  - crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
  - crates/z00z_core/src/assets/nonce_type.rs
  - crates/z00z_core/src/assets/nonce_derivation.rs
  - crates/z00z_core/tests/genesis/test_helpers.rs
  - crates/z00z_storage/benches/assets/nested.rs
  - crates/z00z_storage/benches/common/output.rs
  - crates/z00z_storage/tests/snapshot/test_fix.rs
  - crates/z00z_crypto/src/backend/backend_info.rs
  - crates/z00z_crypto/src/backend/backend_trait.rs
  - crates/z00z_crypto/src/aead_primitives.rs
  - crates/z00z_wallets/examples/example_0_wallet_creation/impl_derivation.rs
  - crates/z00z_wallets/examples/example_0_wallet_creation/impl.rs
  - crates/z00z_wallets/tests/support/test_s5_sender_examples_support.rs
  - crates/z00z_wallets/src/wasm/storage_backend.rs
  - crates/z00z_wallets/src/db/storage_backend.rs
  - crates/z00z_wallets/src/db/index_codecs.rs
  - crates/z00z_wallets/src/db/index_codecs_body.rs
  - crates/z00z_wallets/src/db/index_codecs_tx_time.rs
  - crates/z00z_wallets/src/core/tx/builder.rs
  - crates/z00z_wallets/src/core/tx/output_flow.rs
  - crates/z00z_wallets/src/core/wallet/errors_types.rs
  - crates/z00z_wallets/src/core/address/canonical_snapshot.rs
  - crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - All non-Tari `#[allow(dead_code)]` occurrences are removed from Rust sources in scope.
    - Every former attribute site is resolved by deleting dead code, making the symbol live, or narrowing it to an existing cfg boundary without adding a replacement blanket allow attribute.
    - The canonical Phase 036 backlog records the dead_code attribute sweep and its grep-zero closure proof.
  artifacts:
    - path: .planning/phases/036-rename/036-TODO-3.md
      provides: Canonical inventory and closure note for the dead_code attribute sweep.
    - path: .planning/phases/036-rename/036-16-SUMMARY.md
      provides: Execution summary with validation and grep-zero proof.
  key_links:
    - from: crates
      to: .planning/phases/036-rename/036-TODO-3.md
      via: frozen non-Tari `#[allow(dead_code)]` inventory and closure reconciliation
      pattern: `rg -n "#\\[allow\\(dead_code\\)\\]" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'`
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-16 PLAN

<objective>
Execute `036-16 Remove Non-Tari #[allow(dead_code)] Escape Hatches` after the Phase 036 legacy-removal closure slice.

Purpose: remove every non-Tari `#[allow(dead_code)]` attribute from the workspace without replacing it with a new blanket suppression, while keeping the protected Tari vendor subtree untouched.
Output: a grep-zero non-Tari `#[allow(dead_code)]` result, updated canonical backlog notes, and a summary-backed closure artifact for this dead-code escape-hatch sweep.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-TODO-3.md
@.planning/phases/036-rename/036-15-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
</context>

<global_execution_gates>

- Exclude `crates/z00z_crypto/tari/**` completely; this plan must not edit protected vendor code even if the grep inventory reports `#[allow(dead_code)]` there.
- Freeze the exact non-Tari inventory with the required grep before code edits. If any new file appears beyond the frozen list in `036-TODO-3.md`, add it to the backlog note before touching code.
- Remove the attribute by removing dead code, making the symbol live through an existing real caller, or narrowing it to an already-existing cfg boundary. Do not replace `#[allow(dead_code)]` with another blanket `allow` suppression.
- Do not widen this cleanup into unrelated rename, formatting, or API redesign work. Every code touch must directly eliminate a `#[allow(dead_code)]` site or the now-unneeded dead code behind it.
- In every auto-task verify block, run the bootstrap gate first, then deterministic Rust validation relevant to the touched crates, then the final grep-zero proof, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun deterministic checks after each fix cycle, and do not stop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-16.A Freeze the exact non-Tari `#[allow(dead_code)]` inventory</name>
  <files>.planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-TODO-3.md` — `036-16 Remove Non-Tari #[allow(dead_code)] Escape Hatches`
    - `rg -n "#\[allow\(dead_code\)\]" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'`
  </read_first>
  <action>Rerun the non-Tari Rust inventory for `#[allow(dead_code)]`, compare it to the frozen file list in `036-TODO-3.md`, and update that backlog note only if the live inventory drifted. Record the exact file set that execution will touch. Do not begin code edits until the frozen inventory matches the live grep output.</action>
  <required_test_surfaces>
    Inventory freeze for every non-Tari `#[allow(dead_code)]` site in scope
  </required_test_surfaces>
  <acceptance_criteria>
    - The frozen file list matches the live grep inventory.
    - Tari vendor hits remain excluded.
    - No code file is edited before inventory freeze completes.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "#\[allow\(dead_code\)\]" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The exact non-Tari `#[allow(dead_code)]` inventory is frozen in the canonical backlog before code edits begin.</done>
</task>

<task type="auto">
  <name>036-16.B Remove every frozen non-Tari `#[allow(dead_code)]` attribute and the dead code behind it</name>
  <files>crates/z00z_simulator/src/scenario_1/stage_3.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs, crates/z00z_core/src/assets/nonce_type.rs, crates/z00z_core/src/assets/nonce_derivation.rs, crates/z00z_core/tests/genesis/test_helpers.rs, crates/z00z_storage/benches/assets/nested.rs, crates/z00z_storage/benches/common/output.rs, crates/z00z_storage/tests/snapshot/test_fix.rs, crates/z00z_crypto/src/backend/backend_info.rs, crates/z00z_crypto/src/backend/backend_trait.rs, crates/z00z_crypto/src/aead_primitives.rs, crates/z00z_wallets/examples/example_0_wallet_creation/impl_derivation.rs, crates/z00z_wallets/examples/example_0_wallet_creation/impl.rs, crates/z00z_wallets/tests/support/test_s5_sender_examples_support.rs, crates/z00z_wallets/src/wasm/storage_backend.rs, crates/z00z_wallets/src/db/storage_backend.rs, crates/z00z_wallets/src/db/index_codecs.rs, crates/z00z_wallets/src/db/index_codecs_body.rs, crates/z00z_wallets/src/db/index_codecs_tx_time.rs, crates/z00z_wallets/src/core/tx/builder.rs, crates/z00z_wallets/src/core/tx/output_flow.rs, crates/z00z_wallets/src/core/wallet/errors_types.rs, crates/z00z_wallets/src/core/address/canonical_snapshot.rs, crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs</files>
  <read_first>
    - All files listed in the frozen inventory from task `036-16.A`
    - `.github/requirements/Z00Z_DESIGN_FOUNDATION.md` — error handling, naming, and vendor-boundary rules
  </read_first>
  <action>For every frozen non-Tari inventory file, remove `#[allow(dead_code)]` and resolve the underlying unused code honestly. If the symbol is truly unnecessary, delete it. If it is still needed, make it live through an existing real caller or narrow it behind an already-existing cfg boundary such as an established test-only module boundary. Do not add a replacement `#[allow(unused)]`, `#[allow(unused_variables)]`, or any new blanket suppression. Do not touch Tari vendor files. Keep public contracts, encoded values, and existing behavior stable unless the dead code itself is removed.</action>
  <required_test_surfaces>
    Workspace-wide non-Tari `#[allow(dead_code)]` elimination without replacement suppressions
  </required_test_surfaces>
  <acceptance_criteria>
    - Every frozen non-Tari `#[allow(dead_code)]` site is gone.
    - No new blanket `allow` suppression replaced the removed attribute.
    - Protected Tari vendor code remains untouched.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo clippy --workspace --all-targets --all-features -- -D warnings</automated>
    <automated>rg -n "#\[allow\(dead_code\)\]" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>rg -n "#\[allow\((unused|dead_code)" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>All frozen non-Tari `#[allow(dead_code)]` sites are removed with honest code cleanup and no replacement blanket suppressions.</done>
</task>

<task type="auto">
  <name>036-16.C Record the grep-zero closure for the dead_code sweep</name>
  <files>.planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-TODO-3.md`
    - Validation outputs from tasks `036-16.A` and `036-16.B`
  </read_first>
  <action>Update the `036-16` section in `036-TODO-3.md` with the final closure truth for this sweep: the frozen inventory used, confirmation that Tari remained excluded, and the grep-zero proof command/result for non-Tari `#[allow(dead_code)]`. Keep this scoped to the attribute sweep; do not rewrite the completed `036-04` through `036-15` authority chain.</action>
  <required_test_surfaces>
    Canonical backlog closure wording and grep-zero proof preservation
  </required_test_surfaces>
  <acceptance_criteria>
    - `036-TODO-3.md` records the final grep-zero proof for the dead_code sweep.
    - The closure note stays scoped to `036-16`.
    - Tari exclusion remains explicit and truthful.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "036-16|allow\(dead_code\)|grep-zero|Tari remained excluded" .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The canonical backlog records a truthful grep-zero closure for the non-Tari `#[allow(dead_code)]` sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grep inventory -> code edits | The frozen inventory must match the live non-Tari workspace before any attribute removal begins. |
| attribute removal -> behavior stability | Removing a suppression must not silently widen into unrelated API changes or vendor edits. |
| code cleanup -> closure claim | Closure is invalid unless the final non-Tari grep is zero and validation gates rerun. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-16-01 | T | inventory freeze | mitigate | Rerun and freeze the exact non-Tari grep inventory before code edits; update backlog notes if drift appears. |
| T-036-16-02 | E | vendor boundary | mitigate | Exclude `crates/z00z_crypto/tari/**` explicitly from inventory, edits, and closure claims. |
| T-036-16-03 | T | suppression replacement | mitigate | Ban replacement blanket `allow` attributes and require honest deletion, real use, or existing cfg-boundary narrowing. |
| T-036-16-04 | R | closure wording | mitigate | Require grep-zero proof plus deterministic validation before recording `036-16` closure in the canonical backlog. |
</threat_model>

<verification>
This plan closes only when the frozen non-Tari inventory is exhausted, workspace validation reruns, and the final grep-zero proof for `#[allow(dead_code)]` is recorded without touching Tari vendor code.
</verification>

<success_criteria>
`036-16 Remove Non-Tari #[allow(dead_code)] Escape Hatches` is complete only when `rg -n "#\[allow\(dead_code\)\]" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'` returns no matches and no replacement blanket suppression was introduced.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-16-SUMMARY.md`.
</output>