---
phase: 036-rename
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/036-rename/036-TODO-1.md
  - .planning/phases/036-rename/036-a1-suffixes-spec.md
  - crates/z00z_crypto/src/kdf_domains.rs
  - crates/z00z_crypto/src/crypto_constants.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto.rs
  - crates/z00z_wallets/src/core/security/password.rs
  - crates/z00z_wallets/src/core/key/seed_cipher_container.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs
  - crates/z00z_crypto/src/aead_aad.rs
  - crates/z00z_wallets/src/db/index_codecs.rs
  - crates/z00z_wallets/src/db/schema_keys.rs
  - crates/z00z_wallets/src/wasm/schema_keys.rs
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_wallets/src/core/tx/claim_wire_types.rs
  - crates/z00z_wallets/src/core/claim/claim_receipt.rs
  - crates/z00z_wallets/src/core/tx/fee_estimator.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key.rs
  - crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs
  - crates/z00z_wallets/src/core/key/key_manager_redb.rs
  - crates/z00z_wallets/src/core/key/key_manager_redb_wallet.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs
  - crates/z00z_wallets/src/core/hashing.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto_aad.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_open_session.rs
  - crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs
  - crates/z00z_wallets/src/db/redb_wallet_store.rs
  - crates/z00z_wallets/src/core/backup/wallet_backup_kdf.rs
  - crates/z00z_wallets/src/core/backup/wallet_backup.rs
  - crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
  - crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Step 0 keep-only and future-reserved rows remain intentionally unchanged.
    - Legacy wallet-KDF compatibility and migration lanes are retired before survivor renames land.
    - Legacy backup-KDF fallback is removed only after the current constructor is proven as the sole live lane.
  artifacts:
    - path: .planning/phases/036-rename/036-TODO-1.md
      provides: Preflight-corrected raw row/file truth if D-27 or D-31 mismatch persists.
    - path: crates/z00z_wallets/src/db/redb_wallet_crypto.rs
      provides: Canonical wallet-KDF survivor and retired compatibility constants.
    - path: crates/z00z_wallets/src/core/backup/wallet_backup_kdf.rs
      provides: Canonical backup-KDF survivor after legacy helper retirement.
  key_links:
    - from: .planning/phases/036-rename/036-TODO-1.md
      to: .planning/phases/036-rename/036-a1-suffixes-spec.md
      via: D-27 and D-31 preflight reconciliation
      pattern: row or file mismatch must be fixed in phase-local sources before code edits
    - from: crates/z00z_wallets/src/db/redb_wallet_crypto.rs
      to: crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
      via: current wallet KDF selection
      pattern: unsuffixed survivor names only after V1 retirement proof
    - from: crates/z00z_wallets/src/core/backup/wallet_backup.rs
      to: crates/z00z_wallets/src/core/backup/wallet_backup_kdf.rs
      via: live backup constructor selection
      pattern: one live constructor remains after legacy removal
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-01 PLAN

<objective>
Execute the first fixed serial slice of Phase 036 without scope drift.

Purpose: Preserve every Step 0 keep-only or future-reserved contract, then retire the wallet-KDF and backup-KDF compatibility lanes in the exact raw-row order required by `036-TODO-1.md` and `036-a1-suffixes-spec.md`.
Output: `036-01`, `036-02`, and `036-03` are implemented with preflight reconciliation, ordered retirements, survivor renames only after proof, and verification artifacts ready for summary closure.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-1.md
@.planning/phases/036-rename/036-a1-suffixes-spec.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
</context>

<global_execution_gates>

- Inherit `Decision Summary`, `Dependency Chain`, `File-First Implementation Order`, `Validation Matrix`, `Completion Gate`, and `Explicit Phase Boundary` from `036-TODO-1.md` as mandatory execution controls, not as advisory background.
- Use the raw inventory appendix as the only task-generation surface, and if execution reveals a new design constraint or a new live caller or producer surface, update `036-a1-suffixes-spec.md` first, then `036-TODO-1.md`, before code edits continue.
- Fail closed if live row re-resolution reveals any missing file owner, named test surface, exit-condition gap, caller surface, or compatibility producer not already captured in `036-TODO-1.md`; update the phase-local sources before code edits continue.
- Do not duplicate code paths or leave a hidden compatibility layer alive. Any survivor rename requires explicit zero-caller proof across the live open, import, export, verify, and public-surface seams for the retired symbol set.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-01 Freeze Keep-Only And Future-Reserved Rows</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-01 through D-31, with special attention to D-24, D-27, D-28, and D-31.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-01` section, Completion Gate step 1, and the non-test row ledger entries for rows `1-3`, `5`, `7`, `20-23`, `25-26`, `31`, `40-42`, and `57-60`.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 0 plus the raw appendix rows mapped to `036-01`.
  </read_first>
  <files>
    .planning/phases/036-rename/036-TODO-1.md
    .planning/phases/036-rename/036-a1-suffixes-spec.md
    crates/z00z_crypto/src/kdf_domains.rs
    crates/z00z_crypto/src/crypto_constants.rs
    crates/z00z_wallets/src/db/redb_wallet_crypto.rs
    crates/z00z_wallets/src/core/security/password.rs
    crates/z00z_wallets/src/core/key/seed_cipher_container.rs
    crates/z00z_wallets/src/db/redb_wallet_store_codecs.rs
    crates/z00z_crypto/src/aead_aad.rs
    crates/z00z_wallets/src/db/index_codecs.rs
    crates/z00z_wallets/src/db/schema_keys.rs
    crates/z00z_wallets/src/wasm/schema_keys.rs
    crates/z00z_crypto/src/claim/v2.rs
    crates/z00z_wallets/src/core/tx/claim_wire_types.rs
    crates/z00z_wallets/src/core/claim/claim_receipt.rs
    crates/z00z_wallets/src/core/tx/fee_estimator.rs
    crates/z00z_wallets/src/adapters/rpc/methods/tx_rpc_storage.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key.rs
    crates/z00z_wallets/src/adapters/rpc/wallet_dispatcher_wiring_register.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key_impl/server.rs
  </files>
  <action>Implement the exact `036-01` backlog item without renaming it. First execute the D-27 or D-31 preflight reconciliation against the live declaration paths for the known mismatch-prone rows called out in `036-CONTEXT.md`; if any task-local `Files`, `Tests`, or `Exit condition` block still diverges from the raw appendix, update only `.planning/phases/036-rename/036-TODO-1.md` and `.planning/phases/036-rename/036-a1-suffixes-spec.md` until the row-to-file mapping and test surface are truthful, then begin code work. After the preflight is clean, freeze every Step 0 row in place: keep `HKDF_INFO_REDB_DATA_V2`, `HKDF_INFO_REDB_INDEX_V2`, `HKDF_INFO_REDB_INTEGRITY_V2`, `RANGE_PROOF_BITS_V2`, `MAX_PROOF_SIZE_V2`, `OBJECT_PAYLOAD_HEADER_VERSION_V1`, `build_aad_v1`, `TX_TIME_KEY_VERSION_V1`, `SEMANTIC_KEY_VERSION_V1`, both `META_WALLET_INTEGRITY_V1` constants, `DENYLIST_BLOOM_V1`, `CLAIM_PROOF_V2`, `CLAIM_SCHEMA_V1`, `FEE_WGT_VER_V1`, `TxStoreMetaV1`, and the current `export_public_material_v2` RPC surfaces untouched. Do not invent a fabricated `*_V1` counterpart, do not introduce unsuffixed aliases, do not delete anything in this task, do not infer a hidden proof-version upgrade from the outer claim-v2 tag, and do not activate any future-reserved address work here. If tracking is needed, add only explicit hold or regression coverage per D-24.</action>
  <required_test_surfaces>
    crates/z00z_crypto/src/types_tests.rs
    crates/z00z_wallets/tests/test_kdf.rs
    crates/z00z_wallets/tests/test_stealth_kdf_vectors.rs
    crates/z00z_wallets/tests/test_claim_state_core.rs
    crates/z00z_wallets/tests/test_claim_state_compat.rs
    crates/z00z_wallets/tests/test_rpc_dispatcher_roundtrip.rs
    crates/z00z_wallets/tests/test_rpc_key_derive_e2e.rs
    crates/z00z_wallets/tests/test_rpc_types_serialization.rs
    crates/z00z_wallets/tests/test_seed_salt_policy.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - The phase-local backlog and spec are truthful for every known Step 0 row or file-path mismatch before code edits proceed.
    - Every `036-01` raw row still exists under its current versioned spelling and no new unsuffixed replacement or compatibility-cleanup alias was introduced.
    - Step 0 coverage remains explicit in code or regression tests without altering the current runtime contracts.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>Step 0 keep-only and future-reserved rows are frozen truthfully, and the phase-local source-of-truth files match the live row-to-file mapping for this task.</done>
</task>

<task type="auto">
  <name>036-02 Legacy Wallet-KDF Retirement</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-19, D-20, D-27, D-29, and D-30.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-02` section, Completion Gate step 2, and non-test rows `8-17`, `24`, and `28-30`.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 1 plus the raw appendix entries tied to wallet KDF retirement.
  </read_first>
  <files>
    crates/z00z_wallets/src/db/redb_wallet_crypto.rs
    crates/z00z_wallets/src/core/key/key_manager_redb.rs
    crates/z00z_wallets/src/core/key/key_manager_redb_wallet.rs
    crates/z00z_wallets/src/db/redb_wallet_store_migrations.rs
    crates/z00z_wallets/src/core/hashing.rs
    crates/z00z_wallets/src/db/redb_wallet_crypto_kdf_helpers.rs
    crates/z00z_wallets/src/db/redb_wallet_crypto_aad.rs
    crates/z00z_wallets/src/db/redb_wallet_store_open_session.rs
    crates/z00z_wallets/src/db/redb_wallet_store_crypto_ops.rs
    crates/z00z_wallets/src/db/redb_wallet_store.rs
  </files>
  <action>Implement the exact `036-02` backlog item without renaming it. Follow the mandated order: retire the two distinct `migrate_kdf_v1_to_v2` helpers first as separate raw rows, then remove the blocked compatibility rows `HKDF_INFO_VER_V1`, `AAD_SECRET_VER_V1`, `VERSION_V1`, `derive_wallet_keys_v1`, `aad_secret_v1`, and `redb_hkdf_info_*_v1` only after proving no live caller still needs the V1 metadata-open path. Caller proof is not complete until `redb_wallet_store_open_session.rs`, `redb_wallet_store_crypto_ops.rs`, and `redb_wallet_store.rs` no longer default missing metadata to V1 or retry `aad_secret_v1`. After the legacy open lane is provably gone, rename only the survivor rows `HKDF_INFO_VER_V2`, `AAD_SECRET_VER_V2`, `VERSION_V2`, and `derive_wallet_keys_v2` to their unsuffixed canonical names. Do not collapse the two migration helper rows into one conceptual cleanup item, do not rename the survivors before compatibility retirement is closed, and do not widen the change into unrelated wallet DB or hashing refactors.</action>
  <required_test_surfaces>
    crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
    crates/z00z_wallets/tests/test_kdf.rs
    crates/z00z_wallets/tests/test_stealth_kdf_vectors.rs
    crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - No live non-test wallet-open path depends on V1 KDF metadata, helper, or hashing symbols outside explicitly deferred rows.
    - The V2 survivor constants and helper are the only live wallet-KDF lane and they now own the unsuffixed canonical names.
    - The removal and rename order matches raw rows `16-17`, then `8`, `10`, `12`, `14`, `24`, `28-30`, then survivor rows `9`, `11`, `13`, and `15`.
    - Unsuffixed survivor names are blocked unless zero-caller proof covers wallet-open, post-unlock, and public store-entry seams.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>rg -n "HKDF_INFO_VER_V1|AAD_SECRET_VER_V1|VERSION_V1|derive_wallet_keys_v1|aad_secret_v1|redb_hkdf_info_(data|index|integrity)_v1" crates/z00z_wallets/src crates/z00z_wallets/tests</automated>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast test_wallet_kdf_migration -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>Wallet-KDF cleanup no longer depends on retired V1 compatibility or migration helpers, and the surviving wallet-KDF lane owns the canonical unsuffixed names.</done>
</task>

<task type="auto">
  <name>036-03 Legacy Backup-KDF Retirement</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-20, D-27, D-29, and the Step 2 execution notes.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-03` section, Completion Gate step 3, and non-test rows `43-45`.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 2 plus the raw appendix entries for backup-KDF retirement.
  </read_first>
  <files>
    crates/z00z_wallets/src/core/backup/wallet_backup_kdf.rs
    crates/z00z_wallets/src/core/backup/wallet_backup.rs
    crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
    crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
  </files>
    <action>Implement the exact `036-03` backlog item without renaming it. Keep `default_v2` untouched until the Step 2 compatibility-retirement condition is proven closed. Remove `legacy_v1` and `derive_key_legacy_v1` in the exact order required by the raw appendix, then rename the survivor `default_v2` to the unsuffixed canonical constructor only after the legacy restore lane is fully retired. Retirement proof is incomplete if any exporter, importer, verifier, or restore-adjacent path still constructs, accepts, falls back to, or keeps tests against `legacy_v1` after the delete wave. Do not keep a shadow fallback path, do not introduce a second constructor model, and do not let backup restore keep silently depending on the removed legacy helpers.</action>
  <required_test_surfaces>
    crates/z00z_wallets/tests/test_backup_kdf_contract.rs
    crates/z00z_wallets/tests/test_backup_restore_identity.rs
    crates/z00z_wallets/tests/test_backup_metadata_policy.rs
    crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs
    crates/z00z_wallets/src/core/backup/backup_importer_tests.rs
    crates/z00z_wallets/src/core/backup/wallet_backup_tests.rs
    crates/z00z_wallets/src/services/wallet_service_tests.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - Only one live backup-KDF constructor remains after execution.
    - Legacy backup-KDF helpers are removed in the exact row order from the raw appendix.
    - The surviving constructor uses the unsuffixed canonical name only after the legacy lane is fully retired.
    - No exporter, importer, verifier, or restore-adjacent test surface still constructs, accepts, or falls back to `legacy_v1` when the rename closes.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>rg -n "legacy_v1|derive_key_legacy_v1|default_v2" crates/z00z_wallets/src crates/z00z_wallets/tests</automated>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast test_backup_kdf_contract -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>Backup-KDF compatibility helpers are retired in-order and one canonical constructor remains as the only live lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase-local spec -> execution edits | Raw-row mapping must stay truthful before code changes begin. |
| wallet-open compatibility -> live KDF lane | Retiring V1 too early can strand persisted wallets or reopen silent fallback behavior. |
| backup-restore compatibility -> live backup constructor | Removing or renaming constructors in the wrong order can make restore semantics untruthful. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-01 | T | `.planning/phases/036-rename/036-TODO-1.md` and `036-a1-suffixes-spec.md` | mitigate | Run D-27 or D-31 preflight reconciliation before any code edit and fix phase-local source drift first. |
| T-036-02 | E | wallet KDF selection path | mitigate | Remove V1 compatibility only after caller-proof review and keep survivor renames until retirement proof is complete. |
| T-036-03 | D | backup restore path | mitigate | Keep `default_v2` live until legacy helper retirement is complete and prove one canonical constructor remains. |
| T-036-04 | R | raw-row execution ordering | mitigate | Preserve exact task names and row ordering from `036-TODO-1.md`; no row collapsing or parallel execution. |
</threat_model>

<verification>
Run the per-task ordered verification exactly as written. The plan only passes when Step 0 rows remain unchanged, the wallet-KDF retirement order is proven, the backup-KDF retirement order is proven, and the review prompt reports two consecutive clean passes after the minimum three YOLO review runs.</verification>

<success_criteria>
`036-01`, `036-02`, and `036-03` are closed with truthful source-of-truth files, exact raw-row ordering, no survivor rename before compatibility retirement, and no new parallel compatibility layer introduced.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-01-SUMMARY.md`.
</output>