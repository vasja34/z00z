---
phase: 036-rename
plan: 12
type: execute
wave: 2
depends_on: [036-11]
files_modified:
  - .planning/phases/036-rename/036-a2-legacy-removing-spec.md
  - .planning/phases/036-rename/036-TODO-3.md
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Every production target has one explicit deletion outcome: delete in Wave 2, delete in Wave 3, or blocked until prerequisite proof.
    - Every deletion decision carries a proof bundle naming the exact tests and data paths required before code changes land.
    - `036-13` can only act on the subset explicitly authorized by these deletion decisions.
  artifacts:
    - path: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      provides: Wave 1 deletion decisions and proof-bundle requirements for production targets.
    - path: .planning/phases/036-rename/036-TODO-3.md
      provides: Backlog wording aligned to allowed/forbidden production edits for Wave 2.
  key_links:
    - from: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      to: .planning/phases/036-rename/036-TODO-3.md
      via: deletion decisions gating all later delete and cleanup work
      pattern: only explicitly authorized rows can move into 036-13 execution
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-12 PLAN

<objective>
Execute `036-12 Decide Contract Fate For Compatibility-Owned Groups` from the self-contained legacy-removal backlog.

Purpose: convert frozen Wave 0 delete rows into explicit delete order and blocker-removal decisions with proof obligations.
Output: `036-a2-legacy-removing-spec.md` and `036-TODO-3.md` carry row-backed deletion decisions that narrowly authorize `036-13` and block every other production edit.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-a2-legacy-removing-spec.md
@.planning/phases/036-rename/036-TODO-3.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Treat the Wave 0 classification freeze from `036-11` as immutable input. Do not reclassify rows in this plan unless `036-11` was factually wrong and both canonical files are corrected together.
- Do not modify Rust source or tests in this plan. This is Wave 1 decision writing only.
- Every production target must resolve to exactly one outcome: `delete in Wave 2`, `delete in Wave 3`, or `blocked until prerequisite proof`.
- Every deletion decision must name the exact proof bundle needed before code changes: tests, data paths, persisted values, transport literals, or public feature gates.
- If a production target lacks enough repository evidence to choose a delete outcome safely, leave it as an explicit deletion blocker and re-plan. Do not smuggle a code edit into `036-13` by vague wording.
- In every auto-task verify block, run the bootstrap gate first, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun deterministic checks after each fix cycle, and do not stop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-12.A Decide one explicit deletion outcome for every production target</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — Wave 0 classification content and `Deletion Blockers`.
    - `.planning/phases/036-rename/036-TODO-3.md` — `036-12` section.
  </read_first>
  <action>Extend `036-a2-legacy-removing-spec.md` so every production target resolves to exactly one explicit deletion outcome: `delete in Wave 2`, `delete in Wave 3`, or `blocked until prerequisite proof`. Keep the decision at the group or row level needed to authorize code safely. Do not leave generic placeholders like `future decision`; if evidence is missing, mark the row blocked and explain what evidence is missing. Do not create any intentional-survivor lane.</action>
  <required_test_surfaces>
    Wave 1 fate table completeness and row-specific decision coverage
  </required_test_surfaces>
  <acceptance_criteria>
    - Every production target has exactly one deletion outcome.
    - Any blocked row explains why the decision cannot yet be made.
    - No row is escalated into a non-deletion hold state.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "delete in Wave 2|delete in Wave 3|blocked until prerequisite proof|blocked" .planning/phases/036-rename/036-a2-legacy-removing-spec.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Every production target now has one explicit deletion outcome or an explicit blocked reason.</done>
</task>

<task type="auto">
  <name>036-12.B Attach proof bundles to every deletion decision</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — `Proof Requirements Before Any Delete`, `Validation Requirements`, and the fate outcomes from task `036-12.A`.
    - `.planning/phases/036-rename/036-TODO-3.md` — `Validation Requirements` and `036-12`/`036-13` sections.
  </read_first>
  <action>For each deletion outcome, record the exact proof bundle that must pass before any code edit lands: the tests, data paths, persisted or transport surfaces, and documentation updates required for that row or group. Keep the proof bundle specific enough that `036-13` can tell which subset is authorized and which subset remains blocked. Do not use broad phrases like `run relevant tests`.</action>
  <required_test_surfaces>
    Proof-bundle completeness and named validation surfaces
  </required_test_surfaces>
  <acceptance_criteria>
    - Every non-blocked deletion decision names exact tests and data-path proof obligations.
    - Every blocked row names the reason deletion cannot yet land.
    - No authorized production deletion is left without a corresponding proof bundle.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "storage checkpoint|wallet backup|seed/KDF|wallet DB|feature gate|transport|persisted|proof bundle" .planning/phases/036-rename/036-a2-legacy-removing-spec.md .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Every deletion outcome carries a row-specific proof bundle that later execution can follow without interpretation.</done>
</task>

<task type="auto">
  <name>036-12.C Narrow Wave 2 and Wave 3 to the explicitly authorized delete subset</name>
  <files>.planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-TODO-3.md` — `036-13` and `036-14` sections.
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — Wave 1 fate outcomes and proof bundles.
  </read_first>
  <action>Update `036-TODO-3.md` so `036-13` and `036-14` state explicitly which production/test subsets are authorized to delete, which remain blocked, and which must wait for blocker removal instead of drifting into non-deletion cleanup. Preserve the backlog as self-contained. If Wave 1 authorizes more than one broad subsystem or more than a manageable subset for a single executor pass, state that the additional files require follow-on planning rather than silently widening `036-13` or `036-14`.</action>
  <required_test_surfaces>
    Backlog authorization boundaries and explicit stop conditions
  </required_test_surfaces>
  <acceptance_criteria>
    - `036-13` is narrow enough to execute against an explicit authorized delete subset.
    - `036-14` knows which residue rows must be deleted after production blockers clear.
    - Blocked subsets remain explicit deletion blockers rather than disappearing from the backlog.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "authorized subset|blocked|justify|follow-on planning|proof bundle" .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>Wave 2 and Wave 3 are explicitly bounded by the Wave 1 deletion decisions and proof bundles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 freeze -> Wave 1 decisions | Fate decisions must consume the frozen owner classes without reinterpretation. |
| decision text -> later code edits | Any vague authorization here could leak unsafe code changes into Wave 2. |
| proof bundle -> actual validation | Later plans depend on these proof bundles being exact and complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-12-01 | T | deletion decisions | mitigate | Force one explicit deletion outcome per production target and block vague or mixed outcomes. |
| T-036-12-02 | E | Wave 2 authorization | mitigate | Narrow `036-13` and `036-14` to the exact authorized delete subset and preserve explicit stop conditions. |
| T-036-12-03 | R | proof bundles | mitigate | Name exact tests, data paths, and docs per target so later validation is auditable. |
</threat_model>

<verification>
This plan closes only when every production target has one explicit deletion outcome and a concrete proof bundle that later execution can follow without guessing.
</verification>

<success_criteria>
`036-12 Decide Contract Fate For Compatibility-Owned Groups` is complete only when Wave 2 and Wave 3 are bounded by explicit, proof-backed deletion decisions.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-12-SUMMARY.md`.
</output>