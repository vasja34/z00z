---
phase: 036-rename
plan: 3
type: execute
wave: 3
depends_on: [036-02]
files_modified:
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_wallets/src/core/tx/claim_wire_types.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs
  - crates/z00z_core/src/assets/leaf_tests.rs
  - crates/z00z_core/tests/genesis/test_config.rs
  - crates/z00z_wallets/tests/test_redb_wlt_open.rs
  - crates/z00z_crypto/src/types_tests.rs
  - crates/z00z_wallets/src/core/key/key_manager_redb_tests.rs
  - crates/z00z_core/tests/assets/test_integration_assets_test24.rs
  - crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
  - crates/z00z_wallets/tests/test_phase11_derivation.rs
  - crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs
  - crates/z00z_wallets/src/db/tests/redb_wallet_store.rs
  - crates/z00z_wallets/src/core/stealth/facade_zkpack_tests.rs
  - crates/z00z_wallets/src/core/stealth/facade_kdf.rs
  - crates/z00z_crypto/src/claim/prover.rs
  - crates/z00z_wallets/src/core/tx/fee_estimator_tests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs
  - crates/z00z_wallets/src/core/address/z00z_address/tests.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - The outer claim-v2 wire surface remains live while the inner proof-version boundary stays truthful and unchanged.
    - Address-v2 helpers remain future-reserved and dormant in this wave.
    - Every test-only raw row is reviewed only after production rows are resolved or explicitly held.
  artifacts:
    - path: crates/z00z_crypto/src/claim/v2.rs
      provides: Held outer claim-v2 surface.
    - path: crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs
      provides: Held future-reserved address-v2 helpers.
    - path: crates/z00z_wallets/src/core/address/z00z_address/tests.rs
      provides: Final test-only row review surface for address track.
  key_links:
    - from: crates/z00z_crypto/src/claim/v2.rs
      to: crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
      via: outer wire tag vs inner proof semantics
      pattern: keep classifications separate and truthful
    - from: crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs
      to: address tests
      via: future-reserved helper coverage
      pattern: no activation or rename in this wave
    - from: production rows 01-08
      to: test-only rows 1-28
      via: Completion Gate step 9
      pattern: test-only review starts only after production track resolution or explicit hold
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-03 PLAN

<objective>
Execute the final fixed serial slice of Phase 036.

Purpose: Preserve the claim-v2 and address-v2 hold lanes exactly as the backlog requires, then review the entire test-only raw-row track only after production rows are resolved or explicitly held.
Output: `036-07`, `036-08`, and `036-09` are implemented with truthful hold semantics, no accidental future activation, and a complete resolved-state review for every test-only raw row.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-1.md
@.planning/phases/036-rename/036-a1-suffixes-spec.md
@.planning/phases/036-rename/036-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
</context>

<global_execution_gates>

- Inherit `Decision Summary`, `Dependency Chain`, `File-First Implementation Order`, `Validation Matrix`, `Completion Gate`, and `Explicit Phase Boundary` from `036-TODO-1.md` as mandatory execution controls, not as advisory background.
- Use the raw inventory appendix as the only task-generation surface, and if execution reveals a new design constraint or a new live caller or producer surface, update `036-a1-suffixes-spec.md` first, then `036-TODO-1.md`, before code edits continue.
- Fail closed if live row re-resolution reveals any missing file owner, named test surface, exit-condition gap, caller surface, or compatibility producer not already captured in `036-TODO-1.md`; update the phase-local sources before code edits continue.
- Do not duplicate code paths or leave a hidden compatibility layer alive. Any survivor rename requires explicit zero-caller proof across the live open, import, export, verify, and public-surface seams for the retired symbol set.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-07 Claim-V2 Protocol Surface Hold</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-21, D-27, and the claim-version truth guards.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-07` section, Completion Gate step 7, and non-test row `27`.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 6 plus the raw appendix row for claim-v2 hold.
  </read_first>
  <files>
    crates/z00z_crypto/src/claim/v2.rs
    crates/z00z_wallets/src/core/tx/claim_wire_types.rs
    crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl.rs
    crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
    crates/z00z_storage/src/assets/store_internal/store_query.rs
  </files>
  <action>Implement the exact `036-07` backlog item without renaming it. Keep the outer claim-v2 wire surface live, including `claim_stmt_hash_v2` and the `CLAIM_PROOF_V2` outer contract, while preserving the current inner non-test proof-version boundary exactly as-is. `crates/z00z_storage/src/assets/store_internal/store_query.rs` remains the authoritative issuer of storage-backed `ClaimSourceProof` with inner `ClaimProofVer::V1` semantics, and `claim_tx_verifier_impl_proof.rs` continues to enforce that boundary. Do not infer a protocol-wide upgrade from the outer wire tag alone, do not rename the inner proof semantics, and do not collapse outer wire tagging and inner proof versioning into one classification. If truth-preserving comments or regression coverage are needed, keep them narrowly scoped to that classification boundary.</action>
  <required_test_surfaces>
    crates/z00z_storage/tests/test_claim_source_proof.rs
    crates/z00z_wallets/tests/test_claim_state_core.rs
    crates/z00z_wallets/tests/test_claim_state_compat.rs
    crates/z00z_wallets/tests/test_claim_core_safety.rs
    crates/z00z_wallets/tests/test_claim_import_reason_codes.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - The outer claim-v2 wire surface remains live and unchanged in this wave.
    - The inner proof-version boundary remains truthful and separate from the outer wire tag classification.
    - No rename, delete, or implicit upgrade lands on the hold-only claim-v2 surface.
    - Storage-backed `ClaimSourceProof` issuance remains explicitly pinned to `ClaimProofVer::V1` until a separate protocol migration closes.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>cargo test -p z00z_storage --release --features test-fast --test test_claim_source_proof -- --nocapture</automated>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>The outer claim-v2 surface stays live while the inner proof-version boundary stays truthful and unchanged.</done>
</task>

<task type="auto">
  <name>036-08 Address-V2 Future Activation Hold</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-24, D-27, and future-reserved address guidance.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-08` section, Completion Gate step 8, and non-test rows `61-63`.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 7 plus the raw appendix rows for address-v2 hold.
  </read_first>
  <files>
    crates/z00z_wallets/src/core/address/z00z_address/z00z_address_parts.rs
  </files>
  <action>Implement the exact `036-08` backlog item without renaming it. Keep `encode_single_v2`, `encode_dual_v2`, and `decode_v2` future-reserved and dormant in this wave. Do not activate them, do not rename them, do not add unsuffixed replacements, and do not treat their presence as a blocker for current address behavior. If additional truth-preserving comments or coverage are needed, keep them limited to documenting the future-reserved state.</action>
  <required_test_surfaces>
    crates/z00z_wallets/src/core/address/canonical_snapshot_tests.rs
    crates/z00z_wallets/src/core/address/stealth_scan_support_tests.rs
    crates/z00z_wallets/src/core/address/test_stealth_request.rs
    crates/z00z_wallets/src/core/address/test_stealth_card.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - Address-v2 helpers remain future-reserved and dormant.
    - No activation, rename, or unsuffixed alias lands on rows `61-63`.
    - Current address behavior stays separated from the dormant address-v2 surface.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>Address-v2 stays dormant and future-reserved in this wave.</done>
</task>

<task type="auto">
  <name>036-09 Test-Only Row Review After Production Steps</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-26 through D-31, especially the explicit separation of production rows and test-only rows.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-09` section, Completion Gate step 9, and the full test-only raw-row ledger.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 8, the full `Raw Inventory Appendix -> Test-Only Signatures`, and the test-row rules in `Table-to-Task Translation Protocol`.
  </read_first>
  <files>
    crates/z00z_core/src/assets/leaf_tests.rs
    crates/z00z_core/tests/genesis/test_config.rs
    crates/z00z_wallets/tests/test_redb_wlt_open.rs
    crates/z00z_crypto/src/types_tests.rs
    crates/z00z_wallets/src/core/key/key_manager_redb_tests.rs
    crates/z00z_core/tests/assets/test_integration_assets_test24.rs
    crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
    crates/z00z_wallets/tests/test_phase11_derivation.rs
    crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs
    crates/z00z_wallets/src/db/tests/redb_wallet_store.rs
    crates/z00z_wallets/src/core/stealth/facade_zkpack_tests.rs
    crates/z00z_wallets/src/core/stealth/facade_kdf.rs
    crates/z00z_crypto/src/claim/prover.rs
    crates/z00z_wallets/src/core/tx/fee_estimator_tests.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs
    crates/z00z_wallets/src/core/address/z00z_address/tests.rs
  </files>
  <action>Implement the exact `036-09` backlog item without renaming it. Start only after plans 01 and 02 are complete and the production rows in `036-01` through `036-08` are either resolved or explicitly held. Review every test-only raw row one by one, preserve compatibility-proof tests that still validate live behavior, and rename or retire only the rows that are pure suffix noise after their linked production rows have stabilized. Preserve literal action spellings from the raw appendix when a row says `keep or rename to ...`, do not mix production cleanup into this review lane, and make sure every test-only row ends the task with an explicit resolved state: kept, renamed, deferred, or retired, backed by proof in the summary and changed test surface.</action>
  <required_test_surfaces>
    crates/z00z_core/src/assets/leaf_tests.rs
    crates/z00z_core/tests/genesis/test_config.rs
    crates/z00z_wallets/tests/test_redb_wlt_open.rs
    crates/z00z_crypto/src/types_tests.rs
    crates/z00z_wallets/src/core/key/key_manager_redb_tests.rs
    crates/z00z_core/tests/assets/test_integration_assets_test24.rs
    crates/z00z_wallets/tests/test_wallet_kdf_migration.rs
    crates/z00z_wallets/tests/test_phase11_derivation.rs
    crates/z00z_wallets/src/db/redb_wallet_crypto_tests.rs
    crates/z00z_wallets/src/db/tests/redb_wallet_store.rs
    crates/z00z_wallets/src/core/stealth/facade_zkpack_tests.rs
    crates/z00z_wallets/src/core/stealth/facade_kdf.rs
    crates/z00z_crypto/src/claim/prover.rs
    crates/z00z_wallets/src/core/tx/fee_estimator_tests.rs
    crates/z00z_wallets/src/adapters/rpc/methods/key_impl/test_key_impl.rs
    crates/z00z_wallets/src/core/address/z00z_address/tests.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - Every test-only raw row ends in an explicit resolved state consistent with the appendix.
    - Compatibility-proof tests that still validate live behavior remain intact.
    - Only pure suffix-noise rows are renamed or retired, and only after their linked production rows are stable.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>Every test-only raw row has a resolved state kept, renamed, deferred, or retired with proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outer claim tag -> inner proof semantics | A misleading rename here would falsify security semantics. |
| dormant address-v2 helpers -> live address surface | Activating future-reserved helpers early would create undocumented protocol drift. |
| production rows -> test-only review | Renaming tests before production stabilization can erase required compatibility proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-09 | T | claim-v2 protocol surface | mitigate | Keep outer claim-v2 wire rows unchanged and preserve the inner proof-version boundary as a separate classification. |
| T-036-10 | E | address-v2 helpers | mitigate | Hold rows `61-63` unchanged and forbid activation or rename in this wave. |
| T-036-11 | R | test-only review track | mitigate | Start `036-09` only after production rows are resolved or explicitly held and keep explicit row-by-row outcomes. |
| T-036-12 | D | compatibility-proof tests | mitigate | Preserve proof-bearing tests unless their linked production rows are already stable and the raw appendix explicitly allows rename or retirement. |
</threat_model>

<verification>
Run the per-task ordered verification exactly as written. This plan passes only when the claim-v2 and address-v2 hold surfaces remain truthful and unchanged, and the test-only raw-row review records an explicit resolved state for every test-only row without mixing in new production cleanup.</verification>

<success_criteria>
`036-07`, `036-08`, and `036-09` are closed with explicit hold semantics for claim-v2 and address-v2, plus a complete proof-backed resolution of the test-only raw-row track after production stabilization.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-03-SUMMARY.md`.
</output>