---
phase: 036-rename
plan: 18
type: execute
wave: 8
depends_on: [036-17]
files_modified:
  - crates/z00z_storage/src/assets/test_model.rs
  - crates/z00z_storage/src/assets/store_internal/tx_plan_engine.rs
  - crates/z00z_storage/src/assets/store_internal/store_rows.rs
  - crates/z00z_core/src/genesis/genesis_verification.rs
  - crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs
  - crates/z00z_simulator/examples/simulator_interop/support.rs
  - crates/z00z_simulator/tests/test_e2e_phase4.rs
  - crates/z00z_wallets/src/core/stealth/output_build.rs
  - crates/z00z_wallets/src/core/claim/nullifier_store_global.rs
  - crates/z00z_wallets/src/core/tx/builder.rs
  - crates/z00z_wallets/src/core/stealth/output.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_tests.rs
  - crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs
  - crates/z00z_wallets/tests/test_stealth_scan_support.rs
  - crates/z00z_storage/benches/common/output.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - "Every declaration listed in the a3 reference table now uses the suggested semantic symbol name, and no owned row still exposes the old name in scope."
    - "The renamed declarations still satisfy the existing cross-crate, simulator, wallet, and storage regression coverage that exercises those helpers."
    - "The plan closes with a residual scan proving the scoped declaration set matches the a3 reference table and contains no missed row."
  artifacts:
    - path: .planning/phases/036-rename/036-18-PLAN.md
      provides: Exact rename table and executable task breakdown for the a3-based follow-on wave.
    - path: .planning/phases/036-rename/036-18-SUMMARY.md
      provides: Execution summary, rename validation, and residual-scan closure for the a3 wave.
  key_links:
    - from: .planning/phases/036-rename/036-a3_for_remane-spec.md
      to: crates/z00z_storage/src/assets/store_internal/tx_plan_engine.rs
      via: Old-Name to Suggested-Name mapping for batch selection helpers
      pattern: the exact table drives rename scope and naming choice
    - from: .planning/phases/036-rename/036-a3_for_remane-spec.md
      to: crates/z00z_wallets/src/core/stealth/output_build.rs
      via: Old-Name to Suggested-Name mapping for stealth output helpers
      pattern: the exact table drives rename scope and naming choice
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-18 PLAN

<objective>
Execute `036-18 Rename The A3 Old-Name Symbols To Their Suggested-Name Targets`.

Purpose: use `.planning/phases/036-rename/036-a3_for_remane-spec.md` as the live source of truth for the follow-on cleanup and rename every declaration listed in that table to its suggested semantic symbol without changing runtime semantics, serialized values, or regression evidence.
Output: semantic identifiers across the scoped core, storage, simulator, wallet, and bench files plus a residual scan and summary artifact.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-a3_for_remane-spec.md

@.planning/phases/036-rename/036-a3_for_remane-spec.md is the live reference table for this wave. Use the exact Old-Name -> Suggested-Name mapping below as the executor-facing row set.

## Exact Reference Table

| Path | Old-Name | Suggested-Name | Comments |
| --- | --- | --- | --- |
| `output_build.rs` | `build_output_ctx_for` | `build_output_ctx_with_serial` | The helper becomes explicit about serial input instead of relying on a generic `for` suffix. |
| `test_model.rs` | `root_for` | `build_root_from_path` | The helper builds a root from a path and leaf, so the semantic constructor name is clearer. |
| `tx_plan_engine.rs` | `asset_batches_for` | `select_asset_batches` | This helper selects a batching strategy rather than merely building a value. |
| `tx_plan_engine.rs` | `serial_batches_for` | `select_serial_batches` | This helper dispatches the serial lane based on the current plan mode. |
| `post_claim.rs` | `wlt_path_for` | `require_wallet_file_path` | The helper validates existence, so `require` expresses the contract better than `for`. |
| `store_rows.rs` | `claim_rows_for` | `build_claim_rows` | The helper constructs persisted rows from claims. |
| `genesis_verification.rs` | `expected_hash_for` | `expected_genesis_hash` | The domain noun is more specific than a generic hash helper. |
| `support.rs` | `claim_leaf_for` | `decrypt_claim_leaf` | The operation is a leaf decryption, not a generic lookup. |
| `test_e2e_phase4.rs` | `claim_leaf_for` | `decrypt_claim_leaf` | Keep the test helper aligned with the production semantic name. |
| `nullifier_store_global.rs` | `lease_for` | `create_nullifier_lease` | The helper returns a lease handle, so `create` is the right verb. |
| `test_stealth_scan_support.rs` | `make_leaf_for` | `build_leaf_with_keys` | The helper builds a leaf from keys, which should be explicit in the name. |
| `builder.rs` | `sender_create_output_for` | `build_sender_output_leaf` | The helper constructs a sender output leaf and should read like a builder. |
| `output.rs` | `build_tx_stealth_output_for` | `build_stealth_output_with_serial` | The helper is serial-lane specific, so `with_serial` should be explicit. |
| `support.rs` | `label_for` | `lookup_address_label` | This is a lookup helper, not a generic label helper. |
| `asset_impl_tests.rs` | `seed_for` | `derive_wallet_mark_seed` | The helper derives a deterministic seed tied to wallet marking. |

</context>

<global_execution_gates>

- Treat `.planning/phases/036-rename/036-a3_for_remane-spec.md` as the live row set for this follow-on cleanup; the table above is the exact executor-facing reference.
- Do not touch `crates/z00z_crypto/tari/**` or any other vendor subtree.
- Remove versioned or legacy-style suffix noise from Rust identifiers only. If a live distinction still matters, replace the marker with a semantic noun already appropriate to the surrounding module, not with another `vN`-style name.
- Preserve encoded bytes, persisted values, and literal-backed contracts unless the marker is part of the symbol name only.
- If execution discovers a missed row or a new live declaration not represented in the a3 inventory, stop that wave, update the inventory first, and re-plan before continuing.
</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-18.A Rename storage, core, and simulator helpers from the a3 table</name>
  <files>crates/z00z_storage/src/assets/test_model.rs, crates/z00z_storage/src/assets/store_internal/tx_plan_engine.rs, crates/z00z_storage/src/assets/store_internal/store_rows.rs, crates/z00z_core/src/genesis/genesis_verification.rs, crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs</files>
  <read_first>
    - `.planning/phases/036-rename/036-a3_for_remane-spec.md` — exact rows for storage, core, and simulator helper renames
    - `crates/z00z_storage/src/assets/test_model.rs`
    - `crates/z00z_storage/src/assets/store_internal/tx_plan_engine.rs`
    - `crates/z00z_storage/src/assets/store_internal/store_rows.rs`
    - `crates/z00z_core/src/genesis/genesis_verification.rs`
    - `crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs`
  </read_first>
  <action>Rename every declaration listed in the a3 table for these files to the suggested semantic name, keep the batching, root-building, claim-row, genesis-hash, and post-claim semantics intact, and do not introduce a new compatibility layer or any alternate `for`-style name. If an existing test name is evidence, rename the declaration instead of deleting it.</action>
  <required_test_surfaces>
    Storage row-building and plan-engine coverage, core genesis verification, and simulator post-claim regression coverage for the touched declarations
  </required_test_surfaces>
  <acceptance_criteria>
    - Every old name in the a3 rows owned by these files is replaced by its suggested semantic name.
    - The renamed helpers still satisfy the current storage, core, and simulator behavior.
    - No Tari vendor file is modified.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_storage --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_core --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "(build_output_ctx_for|root_for|asset_batches_for|serial_batches_for|wlt_path_for|claim_rows_for|expected_hash_for|claim_leaf_for)" crates/z00z_storage/src/assets/test_model.rs crates/z00z_storage/src/assets/store_internal/tx_plan_engine.rs crates/z00z_storage/src/assets/store_internal/store_rows.rs crates/z00z_core/src/genesis/genesis_verification.rs crates/z00z_simulator/src/scenario_1/stage_3_utils/post_claim.rs</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
  </verify>
  <done>The storage, core, and simulator helper symbols from the a3 table now use the suggested semantic names and keep their existing behavior intact.</done>
</task>

<task type="auto">
  <name>036-18.B Rename wallet helper declarations from the a3 table</name>
  <files>crates/z00z_wallets/src/core/stealth/output_build.rs, crates/z00z_wallets/src/core/claim/nullifier_store_global.rs, crates/z00z_wallets/src/core/tx/builder.rs, crates/z00z_wallets/src/core/stealth/output.rs, crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_tests.rs</files>
  <read_first>
    - `.planning/phases/036-rename/036-a3_for_remane-spec.md` — exact rows for wallet helper renames
    - `crates/z00z_wallets/src/core/stealth/output_build.rs`
    - `crates/z00z_wallets/src/core/claim/nullifier_store_global.rs`
    - `crates/z00z_wallets/src/core/tx/builder.rs`
    - `crates/z00z_wallets/src/core/stealth/output.rs`
    - `crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_tests.rs`
  </read_first>
  <action>Rename every declaration listed in the a3 table for these wallet files to the suggested semantic name, keep wallet output construction, nullifier lease creation, sender output building, stealth output building, and asset RPC test coverage intact, and do not introduce any compatibility shim or alternate `for` suffix. Preserve behavior and evidence-bearing assertions while changing only the declaration names and their direct call sites.</action>
  <required_test_surfaces>
    Wallet stealth-output, claim-nullifier, tx-builder, and asset RPC regression coverage for the touched declarations
  </required_test_surfaces>
  <acceptance_criteria>
    - Every old name in the a3 rows owned by these wallet files is replaced by its suggested semantic name.
    - Existing wallet behavior and evidence-bearing tests remain intact.
    - No Tari vendor file is modified.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "(build_output_ctx_for|lease_for|sender_create_output_for|build_tx_stealth_output_for|asset_send_rejects_legacy|seed_for)" crates/z00z_wallets/src/core/stealth/output_build.rs crates/z00z_wallets/src/core/claim/nullifier_store_global.rs crates/z00z_wallets/src/core/tx/builder.rs crates/z00z_wallets/src/core/stealth/output.rs crates/z00z_wallets/src/adapters/rpc/methods/asset_impl_tests.rs</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
  </verify>
  <done>The wallet helper symbols from the a3 table now use the suggested semantic names and continue to satisfy the existing wallet regression coverage.</done>
</task>

<task type="auto">
  <name>036-18.C Rename remaining cross-crate support, test, and bench symbols from the a3 table</name>
  <files>crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs, crates/z00z_wallets/tests/test_stealth_scan_support.rs, crates/z00z_simulator/examples/simulator_interop/support.rs, crates/z00z_simulator/tests/test_e2e_phase4.rs, crates/z00z_storage/benches/common/output.rs</files>
  <read_first>
    - `.planning/phases/036-rename/036-a3_for_remane-spec.md` — exact rows for the remaining support, test, and bench renames
    - `crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs`
    - `crates/z00z_wallets/tests/test_stealth_scan_support.rs`
    - `crates/z00z_simulator/examples/simulator_interop/support.rs`
    - `crates/z00z_simulator/tests/test_e2e_phase4.rs`
    - `crates/z00z_storage/benches/common/output.rs`
  </read_first>
  <action>Rename every declaration listed in the a3 table for these remaining support, test, and bench files to the suggested semantic name, keep the address-label lookup, stealth-scan test support, simulator interop support, E2E coverage, and storage bench output semantics intact, and do not leave an old `for`-style declaration name behind. Finish by running a workspace residual scan so the a3 table and the codebase agree on the final renamed set.</action>
  <required_test_surfaces>
    Wallet RPC support, stealth scan tests, simulator E2E coverage, and storage bench compilation coverage for the touched declarations
  </required_test_surfaces>
  <acceptance_criteria>
    - Every old name in the a3 rows owned by these remaining files is replaced by its suggested semantic name.
    - The workspace residual scan shows no missed row from the a3 reference table.
    - No Tari vendor file is modified.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --workspace --release --features test-fast --features wallet_debug_dump</automated>
    <automated>rg -n "(make_leaf_for|label_for|test_stealth_scan_support|test_e2e_phase4|output\b)" crates/z00z_wallets/src/adapters/rpc/methods/key_impl/support.rs crates/z00z_wallets/tests/test_stealth_scan_support.rs crates/z00z_simulator/examples/simulator_interop/support.rs crates/z00z_simulator/tests/test_e2e_phase4.rs crates/z00z_storage/benches/common/output.rs</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
  </verify>
  <done>The remaining cross-crate support, test, and bench symbols from the a3 table now use the suggested semantic names and the final residual scan is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a3 inventory -> code edits | The a3 table must remain the row source for every rename decision. |
| symbol rename -> contract stability | Removing old-name noise must not alter serialized bytes, persisted values, or vendor code. |
| residual scan -> closure claim | Closure is invalid unless the final workspace scan proves the scoped declaration set matches the a3 table. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-18-01 | T | a3-driven rename scope | mitigate | Require every rename to map back to a row in `.planning/phases/036-rename/036-a3_for_remane-spec.md` and stop the wave if a declaration is missing from the table. |
| T-036-18-02 | E | Tari vendor boundary | mitigate | Exclude `crates/z00z_crypto/tari/**` from all searches and edits and keep the vendor subtree out of the closure proof. |
| T-036-18-03 | I | contract drift | mitigate | Preserve runtime behavior, serialized bytes, persisted values, and literal-backed contracts while renaming only the symbol layer. |
| T-036-18-04 | R | closure wording | mitigate | Require a fresh residual scan, a summary artifact, and a table-backed plan before claiming the wave complete. |
</threat_model>

<verification>
This plan closes only when the a3 inventory rows in scope have been renamed to the suggested semantic names, the cross-crate residual scan is zero, and the summary artifact records the exact mapping that was executed.
</verification>

<success_criteria>
`036-18 Rename The A3 Old-Name Symbols To Their Suggested-Name Targets` is complete only when no declaration in the scoped files still uses the old names from the a3 reference table.
</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-18-SUMMARY.md`.
</output>