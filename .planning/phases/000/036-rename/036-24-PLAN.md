<!-- markdownlint-disable-file MD041 MD007 MD032 MD033 MD022 MD055 MD056 MD060 -->
---
phase: 036-rename
plan: 24
type: execute
wave: 13
depends_on: [036-20, 036-23]
files_modified:
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/src/aead.rs
  - crates/z00z_crypto/src/aead/mod.rs
  - crates/z00z_crypto/src/hash.rs
  - crates/z00z_crypto/src/hash/mod.rs
  - crates/z00z_crypto/src/kdf.rs
  - crates/z00z_crypto/src/backend.rs
  - crates/z00z_crypto/src/kdf/mod.rs
  - crates/z00z_crypto/src/backend/mod.rs
  - crates/z00z_crypto/src/protocol/mod.rs
  - crates/z00z_crypto/src/vendor.rs
  - crates/z00z_crypto/src/vendor/mod.rs
    autonomous: true
    requirements: [None]
    must_haves:
    truths:
    - "The a7 path-group recommendations are represented as one executable continuation: /src and /src/claim stay baseline-aligned, while protocol, vendor, aead, hash, kdf, and backend are rehomed into canonical directory-backed module roots."
    - "The public z00z_crypto facade still compiles and keeps the same observable behavior after the module rehome, because any required compatibility wrappers remain thin and root-level names stay stable."
    - "The plan and closeout remain honest about the separate 036-20 partial shim-removal boundary and do not reuse this continuation to claim broader phase closure."
    artifacts:
    - path: crates/z00z_crypto/src/protocol/mod.rs
      provides: Canonical ownership root for the protocol family.
    - path: crates/z00z_crypto/src/vendor/mod.rs
      provides: Canonical ownership root for the vendor and expert lanes.
    - path: crates/z00z_crypto/src/aead/mod.rs
      provides: Canonical ownership root for the AEAD family.
    - path: crates/z00z_crypto/src/hash/mod.rs
      provides: Canonical ownership root for the hash family.
    - path: crates/z00z_crypto/src/kdf/mod.rs
      provides: Canonical ownership root for the KDF family.
    - path: crates/z00z_crypto/src/backend/mod.rs
      provides: Canonical ownership root for the backend family.
    - path: .planning/phases/036-rename/036-24-SUMMARY.md
      provides: Truthful closeout for the a7 continuation and the residual boundary state.
    key_links:
    - from: .planning/phases/036-rename/036-a7_crypto-spec.md
      to: crates/z00z_crypto/src/commitments.rs
      via: the protocol family is one of the remaining grouped rehome targets.
      pattern: move the protocol implementation under one canonical module root and keep the public facade stable.
    - from: .planning/phases/036-rename/036-a7_crypto-spec.md
      to: crates/z00z_crypto/src/aead/mod.rs
      via: the AEAD family is one of the remaining grouped rehome targets.
      pattern: move AEAD implementation files under one canonical module root and keep the public facade stable.
    - from: .planning/phases/036-rename/036-a7_crypto-spec.md
      to: crates/z00z_crypto/src/kdf/mod.rs
      via: the KDF family is one of the remaining grouped rehome targets.
      pattern: move KDF implementation files under one canonical module root and keep the public facade stable.
    - from: .planning/phases/036-rename/036-a7_crypto-spec.md
      to: crates/z00z_crypto/src/backend/mod.rs
      via: the backend family is a private implementation root that must be regrouped without changing backend selection semantics.
      pattern: keep backend behavior stable while rehoming helper files.

<objective>
Execute `036-24 Rehome The Remaining Crypto Module Families Into Canonical Path Groups Without Changing Public Behavior`.

Purpose: use `.planning/phases/036-rename/036-a7_crypto-spec.md` as the self-contained continuation for the remaining Z00Z crypto source-tree organization work so the crate surface matches the recommended path groups, the public facade stays stable through thin compatibility wrappers, and the still-partial `036-20` boundary remains separate.
Output: canonical directory-backed module roots for protocol, vendor, aead, hash, kdf, and backend; preserved public module names where required by the current facade; and a truthful `036-24-SUMMARY.md` that records the continuation without over-claiming phase closure.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
@.github/skills/smart-tests-bootstrap/SKILL.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-20-SUMMARY.md
@.planning/phases/036-rename/036-23-SUMMARY.md
@.planning/phases/036-rename/036-a7_crypto-spec.md
@crates/z00z_crypto/src/commitments.rs
@crates/z00z_crypto/src/ecdh.rs
@crates/z00z_crypto/src/range_proofs.rs
@crates/z00z_crypto/src/stealth_bind.rs
@crates/z00z_crypto/src/zkpack.rs
@crates/z00z_crypto/src/vendor.rs
@crates/z00z_crypto/src/expert.rs
@crates/z00z_crypto/src/aead.rs
@crates/z00z_crypto/src/hash.rs
@crates/z00z_crypto/src/kdf.rs
@crates/z00z_crypto/src/backend.rs

## Source Coverage Audit

| Source | Group | Coverage | Task | Notes |
| --- | --- | --- | --- | --- |
| GOAL | a7 continuation | COVERED | 1-3 | Keep the phase self-contained under the existing `036-rename` directory. |
| TABLE | /src | BASELINE | 1-3 | Root files stay as the crate's top-level anchor; no move is required to satisfy the recommendation. |
| TABLE | /src/claim | BASELINE | 1-3 | Claim is already directory-backed from `036-23` and only needs regression-safe verification. |
| TABLE | /src/aead | COVERED | 2 | Rehome the AEAD implementation and its sidecars under `aead/`. |
| TABLE | /src/hash | COVERED | 2 | Rehome the hash implementation and its sidecars under `hash/`. |
| TABLE | /src/kdf | COVERED | 3 | Rehome the KDF implementation and its sidecars under `kdf/`. |
| TABLE | /src/backend | COVERED | 3 | Rehome the backend implementation and its sidecars under `backend/`. |
| TABLE | /src/protocol | COVERED | 1 | Rehome the protocol helpers under `protocol/`. |
| TABLE | /src/vendor | COVERED | 1 | Rehome the vendor and expert lanes under `vendor/`. |

## Embedded Path-Group Recommendation Table

The table below is a translated planning copy of `.planning/phases/036-rename/036-a7_crypto-spec.md`. The `Comments` column is a planning coverage marker here and will be replaced by execution truth in `036-24-SUMMARY.md`.

| New path | What goes there | Comments |
| --- | --- | --- |
| /src | src/lib.rs, src/error.rs, src/domains.rs, src/types.rs, src/validation.rs, src/crypto_constants.rs, src/protocol_constants.rs, src/lib_api.rs, src/secret.rs, src/hidden.rs | ✅ baseline-aligned root facade; unchanged in this wave |
| /src/claim | src/claim/mod.rs, src/claim/claim_contract.rs | ✅ already directory-backed from 036-23 |
| /src/aead | src/aead.rs, src/aead_aad.rs, src/aead_envelope.rs, src/aead_error.rs, src/aead_primitives.rs, src/aead_transport.rs, src/aead_test_only.rs, src/aead_zkpack.rs | ✅ aead/mod.rs owns the family; root wrapper retained |
| /src/hash | src/hash.rs, src/blake2_hash.rs, src/hmac_sha256.rs, src/sha256_hash.rs, src/hash_convenience.rs, src/hash_domains.rs, src/hash_policy.rs, src/hash_typed.rs, src/hash_zk.rs | ✅ hash/mod.rs owns the family; root wrapper retained |
| /src/kdf | src/kdf.rs, src/argon2_kdf.rs, src/argon2_params.rs, src/hkdf_kdf.rs, src/kdf_domains.rs, src/secret_bytes.rs | ✅ kdf/mod.rs owns the family; root wrapper retained |
| /src/backend | src/backend.rs, src/backend/backend_info.rs, src/backend/backend_trait.rs, src/backend_batch.rs, src/backend_commitment.rs, src/backend_handles.rs, src/backend_init.rs, src/backend_range_proofs.rs, src/backend_tari.rs | ✅ backend/mod.rs owns the family; root wrapper retained |
| /src/protocol | src/commitments.rs, src/ecdh.rs, src/range_proofs.rs, src/stealth_bind.rs, src/zkpack.rs | ✅ protocol/mod.rs groups the protocol lane; root leaf modules remain compatibility surfaces |
| /src/vendor | src/vendor.rs, src/expert.rs | ✅ vendor/mod.rs groups the vendor/expert lane; vendor.rs remains compatibility wrapper |

## Tasks

<task type="auto">
  <name>Task 1: Rehome the protocol and vendor families while preserving public entrypoints</name>
  <files>crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/src/protocol/mod.rs, crates/z00z_crypto/src/vendor.rs, crates/z00z_crypto/src/vendor/mod.rs</files>
  <action>Add canonical grouping roots under `src/protocol/` and `src/vendor/`, and keep the existing root leaf modules in place as compatibility surfaces. The directory modules provide the grouped namespaces while the root public entrypoints stay stable; do not reopen the already-aligned `036-23` claim-contract work.</action>
  <verify>
    <automated>cargo test -p z00z_crypto --release --features test-fast --lib</automated>
    <automated>rg -n 'protocol/mod|vendor/mod|pub mod commitments|pub mod ecdh|pub mod range_proofs|pub mod stealth_bind|pub mod zkpack|pub mod vendor|pub mod expert' crates/z00z_crypto/src</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>The protocol and vendor families are canonical directory-backed module roots, and any root-level compatibility wrappers still preserve the public surface instead of creating a second dialect.</done>
</task>

<task type="auto">
  <name>Task 2: Rehome the AEAD and hash families into directory-backed modules</name>
  <files>crates/z00z_crypto/src/aead.rs, crates/z00z_crypto/src/hash.rs, crates/z00z_crypto/src/aead/mod.rs, crates/z00z_crypto/src/hash/mod.rs</files>
  <action>Wrap the AEAD and hash families through directory-backed ownership roots under `src/aead/` and `src/hash/`. Keep the existing sidecar implementation files at the root and load them through the new module trees so public behavior, envelope format, hash-domain semantics, and public reexports remain unchanged. Do not widen into KDF or backend work in this task.</action>
  <verify>
    <automated>cargo test -p z00z_crypto --release --features test-fast --lib</automated>
    <automated>rg -n 'aead/mod|hash/mod|#[.]path = "aead_|#[.]path = "hash_' crates/z00z_crypto/src/aead/mod.rs crates/z00z_crypto/src/hash/mod.rs crates/z00z_crypto/src/aead.rs crates/z00z_crypto/src/hash.rs</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>The AEAD and hash families are canonical directory-backed module roots, and the root wrappers still preserve the public surface without changing cryptographic semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Rehome the KDF and backend families and close out the continuation</name>
  <files>crates/z00z_crypto/src/kdf.rs, crates/z00z_crypto/src/backend.rs, crates/z00z_crypto/src/kdf/mod.rs, crates/z00z_crypto/src/backend/mod.rs, .planning/phases/036-rename/036-24-SUMMARY.md</files>
  <action>Wrap the KDF and backend families through directory-backed ownership roots under `src/kdf/` and `src/backend/`, then write a truthful closeout summary that records the wrapper-plus-module-root layout and explicitly keeps `036-20` separate. Keep the existing root sidecar files as implementation inputs behind the new module trees, do not touch Tari vendor code, and do not reopen the already-aligned claim directory.</action>
  <verify>
    <automated>cargo test -p z00z_crypto --release --features test-fast --lib</automated>
    <automated>rg -n 'kdf/mod|backend/mod|#[.]path = "kdf_|#[.]path = "backend_|036-20-SUMMARY|path-group|residual' crates/z00z_crypto/src/kdf/mod.rs crates/z00z_crypto/src/backend/mod.rs crates/z00z_crypto/src/kdf.rs crates/z00z_crypto/src/backend.rs .planning/phases/036-rename/036-24-SUMMARY.md</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix every issue and warning after each pass, and stop only after at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>The KDF and backend families are canonical directory-backed module roots, and the summary closes the a7 continuation without over-claiming phase closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root wrapper files -> canonical directory modules | A misplaced wrapper path can silently alter public imports or break crate build semantics. |
| public module names -> downstream consumers | The public module names must remain stable unless the plan explicitly changes them. |
| continuation summary -> phase truth | The summary must not merge this path-group rehome with the separate partial `036-20` shim-removal boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-24-01 | T | `crates/z00z_crypto/src/{protocol,vendor,aead,hash,kdf,backend}` | mitigate | Move one family at a time, keep the root wrappers thin, and run the crate test suite after each wave so wrapper path drift cannot linger. |
| T-036-24-02 | D | `crates/z00z_crypto/src/lib.rs` and the root compatibility wrappers | mitigate | Preserve the current public facade, validate the exports with targeted grep checks, and do not collapse the module names into a second dialect. |
| T-036-24-03 | R | `036-24-SUMMARY.md` and the phase roadmap/state updates | mitigate | Require the summary to distinguish the a7 continuation from `036-20`, and record any residuals explicitly instead of treating a layout plan as a phase closeout. |
</threat_model>

<verification>
This continuation closes only when the eight path groups are represented exactly, the canonical directory modules compile, the root compatibility wrappers preserve the current public facade where needed, and `036-24-SUMMARY.md` records the result without converting this layout refactor into a false phase closeout.

After the deterministic gates pass, run `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode three times, fixing every issue and warning after each pass, and stop only after at least two consecutive runs report no significant issues.
</verification>

<success_criteria>
`036-24 Rehome The Remaining Crypto Module Families Into Canonical Path Groups Without Changing Public Behavior` is complete only when the path-group table from `036-a7_crypto-spec.md` has a truthful outcome for every row, the protocol/vendor/aead/hash/kdf/backend families have canonical directory-backed ownership roots, the public facade still compiles through any required compatibility wrappers, and the plan records the separate `036-20` partial truth without over-claiming phase closure.
</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-24-SUMMARY.md`.
</output>
