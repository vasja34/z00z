---
phase: 036-rename
plan: 2
type: execute
wave: 2
depends_on: [036-01]
files_modified:
  - crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
  - crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
  - crates/z00z_wallets/src/core/backup/backup_exporter_verify.rs
  - crates/z00z_wallets/src/core/backup/backup_wire.rs
  - crates/z00z_wallets/src/core/key/seed_cipher_container.rs
  - crates/z00z_wallets/src/core/chain/receiver_card_record.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Legacy backup payload compatibility lanes disappear in the raw-row order while one truthful V4 import/export surface remains.
    - Seed-container version and AAD version stay explicitly held with no rename or delete in this wave.
    - Receiver-card publication trust remains a held contract and is not collapsed into a new abstraction.
  artifacts:
    - path: crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
      provides: Canonical import surface after legacy payload retirement.
    - path: crates/z00z_wallets/src/core/backup/backup_wire.rs
      provides: Canonical backup payload type after V1 or V3 retirement.
    - path: crates/z00z_wallets/src/core/key/seed_cipher_container.rs
      provides: Explicit hold-only persisted seed-container contract.
    - path: crates/z00z_wallets/src/core/chain/receiver_card_record.rs
      provides: Explicit hold-only published receiver trust contract.
  key_links:
    - from: crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
      to: crates/z00z_wallets/src/core/backup/backup_wire.rs
      via: decode path and payload type selection
      pattern: V4-only live decode lane after legacy retirement
    - from: crates/z00z_wallets/src/core/key/seed_cipher_container.rs
      to: persisted seed-container format
      via: VERSION_V1 and AAD_VERSION_V1
      pattern: explicit hold with no rename or delete
    - from: crates/z00z_wallets/src/core/chain/receiver_card_record.rs
      to: published receiver trust path
      via: ReceiverCardRecordV1
      pattern: preserve published record trust vs raw transport distinction
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-02 PLAN

<objective>
Execute the middle fixed serial slice of Phase 036.

Purpose: Retire legacy backup import/export compatibility lanes in strict raw-row order, then make the two migration holds explicit for the persisted seed-container contract and the published receiver-card trust contract.
Output: `036-04`, `036-05`, and `036-06` are implemented without introducing a parallel payload model, without renaming held contracts, and with the current V4 backup path preserved as the only live import/export lane.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-TODO-1.md
@.planning/phases/036-rename/036-a1-suffixes-spec.md
@.planning/phases/036-rename/036-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
</context>

<global_execution_gates>

- Inherit `Decision Summary`, `Dependency Chain`, `File-First Implementation Order`, `Validation Matrix`, `Completion Gate`, and `Explicit Phase Boundary` from `036-TODO-1.md` as mandatory execution controls, not as advisory background.
- Use the raw inventory appendix as the only task-generation surface, and if execution reveals a new design constraint or a new live caller or producer surface, update `036-a1-suffixes-spec.md` first, then `036-TODO-1.md`, before code edits continue.
- Fail closed if live row re-resolution reveals any missing file owner, named test surface, exit-condition gap, caller surface, or compatibility producer not already captured in `036-TODO-1.md`; update the phase-local sources before code edits continue.
- Do not duplicate code paths or leave a hidden compatibility layer alive. Any survivor rename requires explicit zero-caller proof across the live open, import, export, verify, and public-surface seams for the retired symbol set.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-04 Legacy Backup Payload Import-Export Retirement</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-20, D-27, D-29, and the guardrails forbidding a parallel payload model.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-04` section, Completion Gate step 4, and non-test rows `32-38` and `50-56`.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 3 plus the raw appendix rows for backup payload retirement.
  </read_first>
  <files>
    crates/z00z_wallets/src/core/backup/backup_importer_impl.rs
    crates/z00z_wallets/src/core/backup/backup_exporter_impl.rs
    crates/z00z_wallets/src/core/backup/backup_exporter_verify.rs
    crates/z00z_wallets/src/core/backup/backup_wire.rs
  </files>
  <action>Implement the exact `036-04` backlog item without renaming it. Follow the spec order exactly: delete `build_aad_bytes_v1` and `build_aad_bytes_v2` first, then retire the compatibility decode lanes `decode_export_pack_v3`, `decode_export_pack_v2`, and `decode_legacy_payload_v1`, then remove the verify compatibility lanes `verify_export_pack_v3`, `verify_export_pack_v2`, and `verify_legacy_payload_v1`, then retire the legacy payload rows `BackupPayloadV1` and `BackupPayloadV3`. Treat `backup_exporter_impl.rs` as part of the same live contract so producer-side `build_aad_bytes_v1/v2/v3` and payload emission cannot drift away from the importer or verifier story. Only after those deletions are complete may the surviving rows `build_aad_bytes_v3`, `decode_export_pack_v4`, `BackupPayloadV4`, and `verify_export_pack_v4` move to unsuffixed canonical names. Do not introduce or keep a second payload model, do not preserve compatibility shims after the retirement condition closes, and do not reorder the raw rows for convenience.</action>
  <required_test_surfaces>
    crates/z00z_wallets/tests/test_backup_restore_identity.rs
    crates/z00z_wallets/tests/test_backup_metadata_policy.rs
    crates/z00z_wallets/tests/test_wallet_persistence_backup_service.rs
    crates/z00z_wallets/tests/test_wallet_persistence_no_std.rs
    crates/z00z_wallets/src/core/backup/backup_exporter_tests.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - Legacy backup AAD, decode, verify, and payload rows are retired in the exact raw-row order.
    - The surviving V4 helpers become the only live backup import/export lane after the legacy rows are removed.
    - No second backup payload model or compatibility shim remains in the active path.
    - No live `BackupPayloadV1/V3`, `build_aad_bytes_v1/v2`, `decode_export_pack_v2/v3`, `decode_legacy_payload_v1`, `verify_export_pack_v2/v3`, or `verify_legacy_payload_v1` caller, producer, parser, emitter, or acceptance path remains once the delete wave closes.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>rg -n "BackupPayloadV1|BackupPayloadV3|build_aad_bytes_v1|build_aad_bytes_v2|decode_export_pack_v2|decode_export_pack_v3|decode_legacy_payload_v1|verify_export_pack_v2|verify_export_pack_v3|verify_legacy_payload_v1" crates/z00z_wallets/src crates/z00z_wallets/tests</automated>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast backup_exporter_tests -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>Backup import/export compatibility lanes are removed in-order and the V4 backup path is the only live import/export surface.</done>
</task>

<task type="auto">
  <name>036-05 Seed-Container Contract Migration Hold</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-22, D-27, and hold-only guidance for persisted formats.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-05` section, Completion Gate step 5, and non-test rows `18-19`.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 4 plus the raw appendix rows for seed-container hold.
  </read_first>
  <files>
    crates/z00z_wallets/src/core/key/seed_cipher_container.rs
  </files>
  <action>Implement the exact `036-05` backlog item without renaming it. Keep `VERSION_V1` and `AAD_VERSION_V1` untouched in this wave and make the hold explicit in code comments or regression coverage only if that is required to preserve truthfulness. Do not rename these rows, do not delete them, do not silently prepare an implicit persisted-format migration, and do not reclassify them as cleanup blockers.</action>
  <required_test_surfaces>
    crates/z00z_wallets/tests/test_seed_salt_policy.rs
    crates/z00z_wallets/tests/test_show_seed_phrase_plaintext.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - `VERSION_V1` and `AAD_VERSION_V1` remain unchanged and live in the persisted seed-container contract.
    - The hold is explicit and truthful without expanding scope into a new migration wave.
    - No unsuffixed alias or cleanup rename was introduced for the seed-container contract.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>Seed-container persisted format stays unchanged in this wave and the hold is explicit.</done>
</task>

<task type="auto">
  <name>036-06 Receiver-Card Publication Migration Hold</name>
  <read_first>
    - `.planning/phases/036-rename/036-CONTEXT.md` — D-23, D-27, and the trust-boundary notes for published receiver data.
    - `.planning/phases/036-rename/036-TODO-1.md` — `036-06` section, Completion Gate step 6, and non-test row `39`.
    - `.planning/phases/036-rename/036-a1-suffixes-spec.md` — `Execution Order By Row Range` step 5 plus the raw appendix row for receiver-card hold.
  </read_first>
  <files>
    crates/z00z_wallets/src/core/chain/receiver_card_record.rs
  </files>
  <action>Implement the exact `036-06` backlog item without renaming it. Keep `ReceiverCardRecordV1` untouched and explicit as the published receiver trust contract in this wave. Do not rename it, do not delete it, and do not collapse published trust semantics into raw routing transport or a newly invented abstraction layer. If the hold needs extra truth-preserving coverage, add only bounded regression or clarifying comment support.</action>
  <required_test_surfaces>
    crates/z00z_wallets/tests/test_receiver_card_record.rs
    crates/z00z_wallets/tests/test_receiver_secret_validation.rs
    crates/z00z_wallets/tests/test_e2e_public_path.rs
    crates/z00z_wallets/tests/test_live_path_enforcement.rs
  </required_test_surfaces>
  <acceptance_criteria>
    - `ReceiverCardRecordV1` remains the published trust contract with no rename or delete.
    - The code still distinguishes raw routing transport from published receiver trust.
    - No new abstraction or alias obscures the held contract boundary.
  </acceptance_criteria>
  <verify>
    <manual>Execute every path listed in `required_test_surfaces` or record a source-backed reason why it is not runnable before closing this task.</manual>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode at least 3 times against the current task changes; continue until 2 consecutive runs report no significant issues or warnings.</automated>
  </verify>
  <done>Published receiver trust stays current and is not collapsed into a new layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup compatibility -> live import/export lane | Retiring decode or verify helpers out of order can break truthful restore semantics. |
| persisted seed container -> migration hold | Renaming a held persisted contract can create silent format drift. |
| routing transport -> published receiver trust | Collapsing these semantics can undermine receiver publication guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-05 | T | backup importer or verifier compatibility lanes | mitigate | Remove legacy AAD, decode, verify, and payload rows only in the exact raw-row order and keep one V4 live lane. |
| T-036-06 | I | seed cipher container format | mitigate | Hold `VERSION_V1` and `AAD_VERSION_V1` unchanged; forbid rename or delete in this wave. |
| T-036-07 | S | receiver card trust record | mitigate | Preserve `ReceiverCardRecordV1` and keep published trust separate from raw routing transport. |
| T-036-08 | R | plan ordering | mitigate | Depend on `036-01-PLAN.md` completion and keep no parallel execution inside this plan. |
</threat_model>

<verification>
Run the per-task ordered verification exactly as written. This plan passes only when the backup payload retirement order is preserved, the V4 lane is the sole live import/export surface, and the seed-container plus receiver-card contracts remain explicit holds with no rename or delete.</verification>

<success_criteria>
`036-04`, `036-05`, and `036-06` are closed with exact raw-row ordering, no parallel payload model, and explicit hold-only treatment for the persisted seed-container and published receiver-card contracts.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-02-SUMMARY.md`.
</output>