---
phase: 036-rename
plan: 13
type: execute
wave: 3
depends_on: [036-11, 036-12]
files_modified:
  - .planning/phases/036-rename/036-a2-legacy-removing-spec.md
  - .planning/phases/036-rename/036-TODO-3.md
  - crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
  - crates/z00z_storage/src/checkpoint/codec.rs
  - crates/z00z_storage/src/checkpoint/link.rs
autonomous: true
requirements: [None]
must_haves:
  truths:
    - Only the Wave 1 authorized storage checkpoint delete subset is changed in this plan.
    - No encoded value, persisted marker, transport literal, or compatibility-owned behavior changes in this plan.
    - The canonical spec/backlog are updated to mark exactly which authorized subset landed and which rows remain explicit deletion blockers.
  artifacts:
    - path: crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs
      provides: Deletion of the authorized checkpoint-era legacy naming without changing persisted hash-era acceptance.
    - path: crates/z00z_storage/src/checkpoint/codec.rs
      provides: Deletion of the authorized private artifact-wire legacy naming while preserving binary/JSON fallback behavior.
    - path: crates/z00z_storage/src/checkpoint/link.rs
      provides: Deletion of the authorized private link-wire legacy naming while preserving upgrade-to-bound-link behavior.
  key_links:
    - from: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      to: crates/z00z_storage/src/checkpoint/codec.rs
      via: Wave 1 authorization proving storage compatibility delete safety
      pattern: authorized subset preserves the checkpoint decode/value contract while deleting only the authorized Rust symbol names
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-13 PLAN

<objective>
Execute `036-13 Delete Authorized Production Legacy Code` for the first storage-owned Wave 2 production subset.

Purpose: delete only the storage checkpoint legacy code that Wave 1 explicitly authorized, while blocking broader wallet/core deletion work.
Output: the authorized safe storage checkpoint subset is deleted; any over-broad or unproven row is carried forward only as an explicit deletion blocker, with no non-deletion substitute work.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-a2-legacy-removing-spec.md
@.planning/phases/036-rename/036-TODO-3.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Before touching Rust code, read the Wave 1 authorization result from `036-a2-legacy-removing-spec.md`. This plan may edit only the files listed in `files_modified` and only if those rows were authorized for deletion in `036-12`.
- Before touching Rust code, read the Wave 1 authorization result from `036-a2-legacy-removing-spec.md`. This plan may edit only the storage checkpoint rows authorized for the current `036-13` subset.
- If Wave 1 authorizes zero production deletions, update the canonical docs to record a zero-authorized subset and stop without speculative code edits.
- If execution reveals that an authorized storage row still depends on broader proof-system or test surfaces outside the safe file-local subset, stop that row and carry it forward as an explicit deletion blocker instead of widening this plan.
- Do not do non-deletion cleanup in this plan. Every code touch must remove a `legacy` owner or fail closed as an explicit deletion blocker.
- Keep doc alignment (`036-a2` and `036-TODO-3`) in sync with the landed subset or zero-authorized outcome.
- In every auto-task verify block, run the bootstrap gate first, then `cargo test --release --features test-fast --features wallet_debug_dump` when Rust/test-affecting changes land, then task-specific deterministic checks, then at least three `/GSD-Review-Tasks-Execution` runs in YOLO mode. Fix every material issue or warning, rerun deterministic checks after each fix cycle, and do not stop until at least two consecutive review runs report no significant issues.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-13.A Gate-check and freeze the exact Wave 2 authorized storage delete subset</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — Wave 1 fate decisions and proof bundles.
    - `.planning/phases/036-rename/036-TODO-3.md` — `036-13` section and any `authorized subset` wording added by `036-12`.
  </read_first>
  <action>Before any code edit, verify that the Wave 1 authorization for production deletion is limited to the storage checkpoint subset. Delete only the safe file-local rows represented by `redb_backend_validate.rs`, `codec.rs`, and `link.rs`, plus the two canonical planning files. If Wave 1 authorizes zero production files, record that zero-subset outcome in `036-a2` and `036-TODO-3` and stop this plan. If any authorized storage row proves to require wider proof-system, enum, constructor, or test-surface edits, mark that row as an explicit deletion blocker for follow-on planning instead of widening the execution set.</action>
  <required_test_surfaces>
    Wave 1 storage-subset authorization boundary and zero-subset stop condition
  </required_test_surfaces>
  <acceptance_criteria>
    - The exact authorized storage subset is explicit before code edits begin.
    - Zero-authorized or over-broad outcomes are handled fail-closed.
    - No code file changes land before the subset gate passes.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "authorized subset|delete-only|blocked|zero-authorized" .planning/phases/036-rename/036-a2-legacy-removing-spec.md .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The storage production delete subset is either explicitly authorized and narrow enough for this plan or fail-closed with no speculative code edit.</done>
</task>

<task type="auto">
  <name>036-13.B Delete the authorized safe storage checkpoint legacy code</name>
  <files>crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs, crates/z00z_storage/src/checkpoint/codec.rs, crates/z00z_storage/src/checkpoint/link.rs</files>
  <read_first>
    - `crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs`
    - `crates/z00z_storage/src/checkpoint/codec.rs`
    - `crates/z00z_storage/src/checkpoint/link.rs`
    - Wave 1 authorization notes for these rows in `036-a2-legacy-removing-spec.md`
  </read_first>
  <action>Delete only the Wave 1 authorized safe storage checkpoint legacy code inside the three listed files. Remove `legacy`-named helpers, branches, constructors, and decode paths only when Wave 1 marked them deletable in this subset. Do not widen edits into proof-system constants, public enums, constructor names, wallet/core compatibility lanes, literal values, or transport contracts. If any file reveals that its `legacy` symbol still owns more than the documented safe file-local storage boundary, revert that file, mark the row blocked back in `036-a2`, and stop deletion for that row.</action>
  <required_test_surfaces>
    `z00z_storage` targeted checkpoint regression tests tied to the deleted subset
  </required_test_surfaces>
  <acceptance_criteria>
    - Only authorized safe storage legacy code was deleted.
    - No non-deletion substitute work was used to fake deletion.
    - Any misclassified row was fail-closed and documented instead of partial cleanup.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_storage --release --features test-fast --test test_checkpoint_store_api --test test_checkpoint_finalization --test test_checkpoint_link_injective --test test_redb_rehydrate</automated>
    <automated>rg -n "LegacyArtWire|LegacyCheckpointLink|legacy_hash|legacy_exec_id|legacy_draft_id|legacy_check_id|is_legacy_era" crates/z00z_storage/src/assets/store_internal/redb_backend_validate.rs crates/z00z_storage/src/checkpoint/codec.rs crates/z00z_storage/src/checkpoint/link.rs</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The authorized safe storage checkpoint subset is deleted with no contract drift, and any broader storage rows are carried forward explicitly as deletion blockers.</done>
</task>

<task type="auto">
  <name>036-13.C Sync the canonical removal docs to the landed Wave 2 storage subset</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md`
    - `.planning/phases/036-rename/036-TODO-3.md`
    - The code diff produced by task `036-13.B`
  </read_first>
  <action>Update `036-a2-legacy-removing-spec.md` and `036-TODO-3.md` to mark exactly which safe storage compatibility rows landed in this plan, which storage rows remained blocked, and whether the plan closed on a zero-authorized outcome. Keep the notes narrow and row-specific; do not broaden future authorization or silently mark untouched wallet/core compatibility rows as deleted.</action>
  <required_test_surfaces>
    Canonical doc alignment with the landed Wave 2 storage subset
  </required_test_surfaces>
  <acceptance_criteria>
    - Docs match the exact landed storage subset.
    - Blocked or untouched wallet/core compatibility rows remain explicit deletion blockers.
    - No unauthorized row is described as completed.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "landed|blocked|zero-authorized|storage subset|wallet/core compatibility" .planning/phases/036-rename/036-a2-legacy-removing-spec.md .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The canonical removal docs truthfully record the exact Wave 2 storage subset landed by this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 1 decisions -> Wave 2 edits | Only explicitly authorized storage checkpoint rows may cross into production code edits. |
| local deletion -> persisted/public surface | A misclassified local helper could accidentally remove a compatibility-owned contract. |
| code diff -> canonical docs | Landed subset truth must flow back into `036-a2` and `036-TODO-3` without exaggeration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-13-01 | T | authorized subset gate | mitigate | Fail closed if Wave 1 authorizes zero files or any broader subset than this plan can safely handle. |
| T-036-13-02 | E | storage compatibility delete scope | mitigate | Restrict code edits to the listed storage files and stop if any row proves broader contract-bearing behavior in code. |
| T-036-13-03 | R | landed subset reporting | mitigate | Sync `036-a2` and `036-TODO-3` to the exact landed subset and blocked remainder after code edits. |
</threat_model>

<verification>
This plan closes only when a narrow, Wave 1 authorized storage checkpoint production subset lands with no contract drift and the canonical removal docs reflect the exact result.
</verification>

<success_criteria>
`036-13 Delete Authorized Production Legacy Code` is complete for this first subset only when the landed production edits remain strictly within the Wave 1 authorized storage checkpoint boundary.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-13-SUMMARY.md`.
</output>