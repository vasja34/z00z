---
phase: 036-rename
plan: 23
type: execute
wave: 12
depends_on: [036-20, 036-22]
files_modified:
  - crates/z00z_crypto/src/claim/v2.rs
  - crates/z00z_crypto/src/claim/claim_contract.rs
  - crates/z00z_crypto/src/claim/mod.rs
  - crates/z00z_crypto/src/lib.rs
  - crates/z00z_crypto/tests/test_claim_v2_contract.rs
  - crates/z00z_crypto/tests/test_claim_contract.rs
  - crates/z00z_wallets/src/core/tx/claim_auth.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs
  - crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
  - crates/z00z_wallets/src/core/tx/claim_errors.rs
  - crates/z00z_wallets/src/core/tx/claim_tx.rs
  - crates/z00z_storage/src/assets/store_internal/store_query.rs
  - crates/z00z_storage/src/checkpoint/build.rs
  - crates/z00z_storage/src/assets/types_identity.rs
  - crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs
  - crates/z00z_wallets/src/core/tx/test_claim_tx.rs
  - crates/z00z_storage/tests/test_claim_source_proof.rs
  - .planning/phases/036-rename/036-23-SUMMARY.md
autonomous: true
requirements: [None]
must_haves:
  truths:
    - "The crypto-owned claim implementation no longer exports or depends on the `ClaimRootVer` wrapper type: the owner surface is renamed from `v2` to `claim_contract`, `CLAIM_ROOT_VERSION` is the only live root-version constant, and the public facade exports that constant instead of the deleted wrapper type."
    - "All live wallet, storage, and simulator claim consumers now use `root_version` naming, `CLAIM_ROOT_VERSION`, and the renamed `SourceRootVersion` error spelling without silently preserving `ClaimRootVer`, `SourceRootVer`, `root_ver()`, or `claim_v2` residue in `crates/**/*.rs`."
    - "The closeout summary proves the full 21-row claim rename table plus the embedded coverage-proof table, records the deliberate `claim_v2` -> `claim_contract` domain-label migration as a self-contained compatibility-breaking contract change, and does not over-claim closure for the still-partial `036-20` shim-removal boundary."
  artifacts:
    - path: .planning/phases/036-rename/036-23-PLAN.md
      provides: Self-contained execution contract for the claim-lane continuation rooted in 036-a6_claim-spec.md.
    - path: .planning/phases/036-rename/036-a6_claim-spec.md
      provides: Canonical source table for the 21 claim rename rows plus the live coverage-proof mapping.
    - path: .planning/phases/036-rename/036-23-SUMMARY.md
      provides: Truth-based execution summary covering the claim contract rename, root-version simplification, call-site migration, validation, and residual scans.
  key_links:
    - from: .planning/phases/036-rename/036-a6_claim-spec.md
      to: crates/z00z_crypto/src/claim/claim_contract.rs
      via: rows 1, 5, 6, 7, 8, and 9 rename the owner module, remove ClaimRootVer, rename root_version fields, and migrate the claim statement hash label.
      pattern: preserve claim proof encoding order and CLAIM_TAG while changing only the owned module/file names, root-version surface, and row-owned literals.
    - from: .planning/phases/036-rename/036-a6_claim-spec.md
      to: crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
      via: rows 12, 13, and 14 rename SourceRootVer, migrate ClaimRootVer consumers to CLAIM_ROOT_VERSION, and keep proof-version checks untouched.
      pattern: wallet verifier error classes and root-version guards must follow the new spelling without rewriting unrelated proof validation.
    - from: .planning/phases/036-rename/036-a6_claim-spec.md
      to: crates/z00z_storage/src/assets/types_identity.rs
      via: rows 15, 16, and 17 replace ClaimRootVer-typed storage identity helpers with raw-byte CLAIM_ROOT_VERSION/root_version ownership.
      pattern: storage-owned claim source roots stay typed, but the deleted wrapper type does not survive behind storage facades.
---

# 036-23 PLAN

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

<objective>
Execute `036-23 Rename The Claim Contract Surface And Retire ClaimRootVer Without Semantic Drift`.

Purpose: use `.planning/phases/036-rename/036-a6_claim-spec.md` as the authoritative, self-contained source for the next Phase 036 continuation so the claim lane stops presenting the old `v2`/`ClaimRootVer` wording, switches to the `claim_contract` module and `CLAIM_ROOT_VERSION` constant, and updates every live wallet/storage/simulator consumer that still anchors to the deleted type or old human-facing strings.
Output: renamed crypto claim owner files, root-version simplification across direct consumers and tests, and a truthful `036-23-SUMMARY.md` that closes the full 21-row claim table plus the embedded coverage-proof table without overstating the separate partial `036-20` boundary.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
@.github/skills/smart-tests-bootstrap/SKILL.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-CONTEXT.md
@.planning/phases/036-rename/036-20-SUMMARY.md
@.planning/phases/036-rename/036-22-SUMMARY.md
@.planning/phases/036-rename/036-a6_claim-spec.md
@crates/z00z_crypto/src/claim/v2.rs
@crates/z00z_crypto/src/claim/mod.rs
@crates/z00z_crypto/src/lib.rs
@crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs
@crates/z00z_storage/src/assets/types_identity.rs

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Keep the next Phase 036 continuation self-contained under `.planning/phases/036-rename/` instead of creating a duplicate phase folder. | 23 | COVERED | `036-23-PLAN.md` lives in the existing Phase 036 directory and is registered in `STATE.md` and `ROADMAP.md`. |
| SPEC | ROWS-001-005 | Rename the owner files/modules and remove `ClaimRootVer` in favor of `CLAIM_ROOT_VERSION`. | 23 | COVERED | Task A owns both file renames, the module/export updates, and the dead wrapper-type retirement. |
| SPEC | ROWS-006-009 | Rename row-owned error strings, field/accessor names, root-version checks, and the `claim_v2` domain label. | 23 | COVERED | Task A updates the owner claim module and crypto test expectations while preserving `ClaimProofVer` and `CLAIM_TAG`. |
| SPEC | ROWS-010-017 | Migrate live wallet and storage callers to `claim contract`, `CLAIM_ROOT_VERSION`, `root_version`, and `SourceRootVersion`. | 23 | COVERED | Task B updates the live consumer seams without widening into unrelated claim-proof contracts. |
| SPEC | ROWS-018-021 | Update crypto, simulator, wallet, and storage tests to the new contract name and raw root-version mismatch fixtures. | 23 | COVERED | Task B updates every row-owned test surface and then runs targeted plus broad validation. |
| SPEC | COVERAGE-PROOF | Preserve the live `crates/**/*.rs` hit mapping from the source spec and close it on repository-backed evidence. | 23 | COVERED | Tasks A/B clear the owned hits; Task C records the exact post-change scan outcome against the embedded coverage-proof table. |
| CONTEXT | D-11 | Do not rename literal-backed contracts unless the source spec explicitly upgrades them. | 23 | COVERED | Row 9 is the only allowed claim-domain literal migration; all other literal/proof/version contracts stay unchanged unless named by the table. |
| CONTEXT | D-12 | One raw row, one owner; do not collapse multiple files into synthetic ownership. | 23 | COVERED | Task A owns crypto claim producers, Task B owns the direct consumer/test fallout, and Task C records truth without widening scope. |
| CONTEXT | D-15 | If a missed caller or new design constraint appears, update the source spec first and then resume. | 23 | COVERED | Global gates require fail-closed behavior if new `ClaimRootVer`/`claim_v2` residue appears outside the embedded tables. |

<interfaces>
From `crates/z00z_crypto/src/claim/v2.rs`:

```rust
pub struct ClaimStmt {
    pub chain_id: u32,
    pub root_ver: ClaimRootVer,
    pub proof_ver: ClaimProofVer,
    pub tx_ver: u32,
    pub range_ctx_hash: [u8; 32],
    pub claim_id: [u8; 32],
    pub claim_source_asset_id: [u8; 32],
    pub claim_source_commitment: [u8; 32],
    pub source_root: [u8; 32],
    pub claim_scope_hash: [u8; 32],
    pub recipient_binding: [u8; 32],
    pub nullifier: [u8; 32],
    pub owner_bind_digest: [u8; 32],
    pub output_leaf_hashes: Vec<[u8; 32]>,
}

pub struct ClaimSourceProof {
    root_ver: ClaimRootVer,
    source_root: [u8; 32],
    proof_ver: ClaimProofVer,
    proof_blob: Vec<u8>,
}

pub fn claim_stmt_hash(stmt: &ClaimStmt) -> Result<[u8; 32], ClaimError>;
```

From `crates/z00z_crypto/src/claim/mod.rs`:

```rust
mod v2;
pub use v2::{
    claim_stmt_hash, ClaimAuthoritySig, ClaimError, ClaimProofVer, ClaimRootVer, ClaimSourceProof,
    ClaimStmt,
};
```

From `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs`:

```rust
Ok(ClaimStmt {
    chain_id: pkg.chain_id,
    root_ver: ClaimRootVer::V1,
    proof_ver: ClaimProofVer::V1,
    tx_ver: pkg.version,
    ...
})

if proof.root_ver() != ClaimRootVer::V1 {
    return Err(ClaimTxError::SourceRootVer(
        proof.root_ver().as_u8().to_string(),
    ));
}
```

From `crates/z00z_storage/src/assets/types_identity.rs`:

```rust
pub struct ClaimSourceRoot {
    root_ver: ClaimRootVer,
    root: AssetStateRoot,
}

impl ClaimSourceRoot {
    pub const fn new(root_ver: ClaimRootVer, root: AssetStateRoot) -> Self;
    pub const fn root_ver(self) -> ClaimRootVer;
}
```

</interfaces>

## Embedded Claim Rename Tables

The tables below are embedded from `.planning/phases/036-rename/036-a6_claim-spec.md` so this plan remains self-sufficient.

The `Comments` column records verified `036-23` execution state and must remain blank until the implementation and validation are actually complete.

### Rename Table

| # | File | Line(s) | Old | New / Action | Notes | Comments |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `crates/z00z_crypto/src/claim/v2.rs` | file | `v2.rs` | `claim_contract.rs` | Rename the claim implementation module file. | ✅ |
| 2 | `crates/z00z_crypto/tests/test_claim_v2_contract.rs` | file | `test_claim_v2_contract.rs` | `test_claim_contract.rs` | Rename the test-only file to match the new contract name. | ✅ |
| 3 | `crates/z00z_crypto/src/claim/mod.rs` | 1-4 | `mod v2;` / `v2::{...}` | `mod claim_contract;` / `claim_contract::{...}` | Update the module declaration and re-export path after the file rename. | ✅ |
| 4 | `crates/z00z_crypto/src/lib.rs` | 114 | `ClaimRootVer` | remove from export list; export `CLAIM_ROOT_VERSION` instead | Public facade must stop exporting the deleted type. | ✅ |
| 5 | `crates/z00z_crypto/src/claim/v2.rs` | 17-29 | `ClaimRootVer` type, `V1`, `V2`, `new`, `as_u8` | delete; replace with `pub const CLAIM_ROOT_VERSION: u8 = 2` | Remove the dead wrapper type entirely. | ✅ |
| 6 | `crates/z00z_crypto/src/claim/v2.rs` | 60-80 | `claim-v2 ...` error strings and `BadRootVer` / `RootVerMix` | `claim contract ...`; `BadRootVersion` / `RootVersionMismatch` | Keep the claim-proof / tx-version errors as they are unless their own names are touched separately. | ✅ |
| 7 | `crates/z00z_crypto/src/claim/v2.rs` | 91-321 | `root_ver` field / getter / setter sites | `root_version` | Rename the root-version field and its accessor on `ClaimStmt`, `ClaimSourceRoot`, and `ClaimSourceProof`. | ✅ |
| 8 | `crates/z00z_crypto/src/claim/v2.rs` | 146-201 | `ClaimRootVer::V1/::V2` checks | `CLAIM_ROOT_VERSION` for valid cases; `3u8` (or another non-2 nonzero byte) for mismatch fixtures | Replace the enum-based root-version handling with a raw-byte constant and explicit mismatch fixtures. | ✅ |
| 9 | `crates/z00z_crypto/src/claim/v2.rs` | 355 | `"claim_v2"` | `"claim_contract"` | Deliberate domain-separator migration; this is compatibility-breaking and must be treated as part of the claim contract rename. | ✅ |
| 10 | `crates/z00z_wallets/src/core/tx/claim_auth.rs` | 39, 69 | `claim_v2` in comments | `claim contract` | Human-facing comment cleanup only. | ✅ |
| 11 | `crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs` | 15, 33 | `ClaimRootVer::V1.as_u8()` / `claim_v2` in comment | `CLAIM_ROOT_VERSION` / `claim contract` | Helper and comment must follow the new claim root version surface. | ✅ |
| 12 | `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs` | 93, 105, 163-165 | `claim_v2`, `ClaimRootVer::V1`, `SourceRootVer` | `claim contract`, `CLAIM_ROOT_VERSION`, `SourceRootVersion` | Update the proof verifier and its error path. | ✅ |
| 13 | `crates/z00z_wallets/src/core/tx/claim_errors.rs` | 26 | `SourceRootVer` | `SourceRootVersion` | Keep wallet error naming consistent with the new root-version spelling. | ✅ |
| 14 | `crates/z00z_wallets/src/core/tx/claim_tx.rs` | 192 | `SourceRootVer(_)` | `SourceRootVersion(_)` | Update the verifier error-class match arm. | ✅ |
| 15 | `crates/z00z_storage/src/assets/store_internal/store_query.rs` | 1, 194, 206 | `ClaimRootVer` import/usages | `CLAIM_ROOT_VERSION` / `root_version` | Update storage-side claim-source root construction and checks. | ✅ |
| 16 | `crates/z00z_storage/src/checkpoint/build.rs` | 22, 344 | `ClaimRootVer::V1` | `CLAIM_ROOT_VERSION` | Storage checkpoint builder should use the single live claim root version constant. | ✅ |
| 17 | `crates/z00z_storage/src/assets/types_identity.rs` | 1, 121, 127, 132 | `ClaimRootVer` import/field/ctor/getter | `CLAIM_ROOT_VERSION` / `root_version` | Storage-side identity wrapper should stop carrying the dead type. | ✅ |
| 18 | `crates/z00z_crypto/tests/test_claim_v2_contract.rs` | 1, 16, 51, 67, 70, 80, 82, 88, 170, 177, 184-192 | file name, `claim-v2` expectations, `ClaimRootVer::V1/::V2` | `test_claim_contract.rs`, `claim contract`, `CLAIM_ROOT_VERSION` / `3u8` in mismatch fixtures | Update the test to the new contract name and root-version constant. | ✅ |
| 19 | `crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs` | 4, 229-230, 301, 326, 343, 349, 371 | `ClaimRootVer::V1/::V2` and `root_ver()` expectations | `CLAIM_ROOT_VERSION` / `root_version` | Simulator tests must follow the same root-version simplification. | ✅ |
| 20 | `crates/z00z_wallets/src/core/tx/test_claim_tx.rs` | 6, 333, 467, 592, 639 | `ClaimRootVer::V1/::V2` and `ClaimSourceProof::new(...)` | `CLAIM_ROOT_VERSION` / `root_version` | Wallet claim tests need the same root-version migration. | ✅ |
| 21 | `crates/z00z_storage/tests/test_claim_source_proof.rs` | 1, 34, 35, 48, 50, 55, 63, 80, 88, 96, 99, 113 | `ClaimRootVer::V1` and `root_ver()` checks | `CLAIM_ROOT_VERSION` / `root_version` | Storage proof tests must be updated to the new root-version naming. | ✅ |

### Summary

- File renames: 2
- Module/export updates: 2
- Core claim API replacements: 6
- Wallet/storage call-site updates: 6
- Test updates: 5

### Coverage Proof

Live `crates/**/*.rs` hits for `claim-v2` / `claim_v2` map to the rows above as follows:

| Live hit group | Covered row(s) | Notes |
| --- | --- | --- |
| `claim-v2` error literals in `crates/z00z_crypto/src/claim/v2.rs` | 6 | Covers all human-facing error strings in the claim module. |
| `claim_v2` domain label in `crates/z00z_crypto/src/claim/v2.rs` | 9 | Covers the hash domain separator migration. |
| `claim_v2` comments in `crates/z00z_wallets/src/core/tx/claim_auth.rs` | 10 | Covers both auth comments. |
| `claim_v2` comment and root-version helper in `crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs` | 11 | Covers the helper and its comment. |
| `claim_v2` string and version checks in `crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs` | 12-14 | Covers the live proof-verifier string and its root-version error path. |
| `claim-v2` expectations in `crates/z00z_crypto/tests/test_claim_contract.rs` | 18 | Covers the test file rename plus all quoted `claim-v2` expectations. |
| `ClaimRootVer::V1/::V2` call sites in storage, simulator, wallets, and tests | 15-21 | Covers every live root-version caller that still anchors the claim contract to the deleted type. |

Archive and planning artifacts under `.planning/**` and `logs/**` still contain historical `claim_v2` mentions. They are intentionally not part of this rename wave unless a separate documentation cleanup pass is planned.
</context>

<global_execution_gates>

- Treat `.planning/phases/036-rename/036-a6_claim-spec.md` as the sole owner of this continuation. Every embedded row must map to an explicit code or summary outcome.
- The two file renames are mandatory and must land in the same execution wave as the corresponding `mod.rs` and public re-export updates. Do not leave a mixed `v2`/`claim_contract` module state behind.
- Remove the dead `ClaimRootVer` wrapper type completely. The only live root-version surface owned by this continuation is `pub const CLAIM_ROOT_VERSION: u8 = 2` plus raw-byte mismatch fixtures.
- Preserve `CLAIM_TAG`, `ClaimProofVer`, binary encode/decode ordering, and claim-proof / tx-version contracts unless a row in the embedded table explicitly owns the rename. This continuation changes claim root-version naming, not proof-version semantics.
- Row 9 is a deliberate compatibility-breaking domain-label migration from `claim_v2` to `claim_contract`. Keep that change tightly scoped to the claim statement hash owner surface and the tests that intentionally mirror it.
- `036-20-SUMMARY.md` remains the authoritative partial shim-removal truth. `036-23` must not claim broader Phase 036 closure.
- Do not touch `crates/z00z_crypto/tari/**`.
- If execution needs a git commit, use `.github/skills/z00z-git-versioning/scripts/version-manager.sh` through the `/z00z-git-versioning` workflow instead of raw git commands.
</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-23.A Rename the crypto-owned claim module, retire ClaimRootVer, and move the owner surface onto CLAIM_ROOT_VERSION</name>
  <files>crates/z00z_crypto/src/claim/v2.rs, crates/z00z_crypto/src/claim/claim_contract.rs, crates/z00z_crypto/src/claim/mod.rs, crates/z00z_crypto/src/lib.rs, crates/z00z_crypto/tests/test_claim_v2_contract.rs, crates/z00z_crypto/tests/test_claim_contract.rs</files>
  <action>Execute rows 1 through 9 and 18 where the crypto claim module is the owner. Rename `crates/z00z_crypto/src/claim/v2.rs` to `claim_contract.rs` and `crates/z00z_crypto/tests/test_claim_v2_contract.rs` to `test_claim_contract.rs`, then update `crates/z00z_crypto/src/claim/mod.rs` and `crates/z00z_crypto/src/lib.rs` so the public facade stops exporting `ClaimRootVer` and exports `CLAIM_ROOT_VERSION` instead. Inside the renamed claim module, delete the `ClaimRootVer` wrapper type entirely, replace it with `pub const CLAIM_ROOT_VERSION: u8 = 2`, rename every row-owned `root_ver` field/getter/site to `root_version`, rename `BadRootVer` to `BadRootVersion` and `RootVerMix` to `RootVersionMismatch`, migrate the human-facing `claim-v2` error strings to `claim contract`, replace enum-based root-version valid/mismatch logic with `CLAIM_ROOT_VERSION` and explicit non-2 fixtures such as `3u8`, and change the deliberate statement-hash label from `claim_v2` to `claim_contract`. Preserve `ClaimProofVer`, `CLAIM_TAG`, proof blob validation, and binary framing order; do not widen this task into unrelated proof-version or tx-version renames.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_crypto --release --features test-fast --test test_claim_contract -- --nocapture</automated>
      <automated>! rg -n '\bClaimRootVer\b' crates/z00z_crypto/src/claim crates/z00z_crypto/src/lib.rs crates/z00z_crypto/tests/test_claim_contract.rs && ! rg -n -F 'claim-v2' crates/z00z_crypto/src/claim crates/z00z_crypto/src/lib.rs crates/z00z_crypto/tests/test_claim_contract.rs && ! rg -n -F 'claim_v2' crates/z00z_crypto/src/claim crates/z00z_crypto/src/lib.rs && rg -n -F 'claim_v2' crates/z00z_crypto/tests/test_claim_contract.rs && ! rg -n '\bBadRootVer\b' crates/z00z_crypto/src/claim crates/z00z_crypto/src/lib.rs crates/z00z_crypto/tests/test_claim_contract.rs && ! rg -n '\bRootVerMix\b' crates/z00z_crypto/src/claim crates/z00z_crypto/src/lib.rs crates/z00z_crypto/tests/test_claim_contract.rs && ! rg -n '\broot_ver\b|\broot_ver\(' crates/z00z_crypto/src/claim crates/z00z_crypto/src/lib.rs crates/z00z_crypto/tests/test_claim_contract.rs</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix all issues and warnings after each pass, and stop only after at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>The crypto-owned claim module is renamed to `claim_contract`, `ClaimRootVer` no longer exists anywhere in the crypto owner surface, `CLAIM_ROOT_VERSION` is the only exported root-version owner constant, and the crypto integration test expectations match the new claim contract wording plus raw-byte mismatch fixtures.</done>
</task>

<task type="auto">
  <name>036-23.B Migrate wallet, storage, and simulator claim consumers to root_version and CLAIM_ROOT_VERSION without widening proof semantics</name>
  <files>crates/z00z_wallets/src/core/tx/claim_auth.rs, crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs, crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs, crates/z00z_wallets/src/core/tx/claim_errors.rs, crates/z00z_wallets/src/core/tx/claim_tx.rs, crates/z00z_storage/src/assets/store_internal/store_query.rs, crates/z00z_storage/src/checkpoint/build.rs, crates/z00z_storage/src/assets/types_identity.rs, crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs, crates/z00z_wallets/src/core/tx/test_claim_tx.rs, crates/z00z_storage/tests/test_claim_source_proof.rs</files>
  <action>Execute rows 10 through 21 exactly in the live consumer seams. Update wallet comments from `claim_v2` to `claim contract` only where the table owns those comments. Replace `ClaimRootVer::V1` and `ClaimRootVer::V2` usages with `CLAIM_ROOT_VERSION` or explicit mismatch bytes, rename `SourceRootVer` to `SourceRootVersion`, rename `root_ver()`/`root_ver` usages to `root_version()`/`root_version`, and keep `ClaimProofVer::V1` plus the proof-version contract unchanged unless the embedded table explicitly touches it. In storage-owned wrappers such as `ClaimSourceRoot`, stop carrying the deleted type and keep the typed wrapper centered on the raw root bytes plus `CLAIM_ROOT_VERSION`. In simulator, wallet, and storage tests, replace the old enum-based fixtures with raw-byte/root_version expectations so the tests prove the new surface instead of preserving the deleted type through compatibility drift.</action>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test -p z00z_storage --release --features test-fast --test test_claim_source_proof -- --nocapture</automated>
    <automated>cargo test -p z00z_wallets --release --features test-fast core::tx::claim_tx::claim_tx_tests -- --nocapture</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump --test test_claim_pkg_crypto_support -- --nocapture</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>! rg -n '\bClaimRootVer\b' crates/z00z_wallets/src/core/tx/claim_auth.rs crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs crates/z00z_wallets/src/core/tx/claim_errors.rs crates/z00z_wallets/src/core/tx/claim_tx.rs crates/z00z_storage/src/assets/store_internal/store_query.rs crates/z00z_storage/src/checkpoint/build.rs crates/z00z_storage/src/assets/types_identity.rs crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs crates/z00z_wallets/src/core/tx/test_claim_tx.rs crates/z00z_storage/tests/test_claim_source_proof.rs && ! rg -n '\bSourceRootVer\b' crates/z00z_wallets/src/core/tx/claim_auth.rs crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs crates/z00z_wallets/src/core/tx/claim_errors.rs crates/z00z_wallets/src/core/tx/claim_tx.rs crates/z00z_storage/src/assets/store_internal/store_query.rs crates/z00z_storage/src/checkpoint/build.rs crates/z00z_storage/src/assets/types_identity.rs crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs crates/z00z_wallets/src/core/tx/test_claim_tx.rs crates/z00z_storage/tests/test_claim_source_proof.rs && ! rg -n -F 'claim_v2' crates/z00z_wallets/src/core/tx/claim_auth.rs crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs crates/z00z_wallets/src/core/tx/claim_errors.rs crates/z00z_wallets/src/core/tx/claim_tx.rs crates/z00z_storage/src/assets/store_internal/store_query.rs crates/z00z_storage/src/checkpoint/build.rs crates/z00z_storage/src/assets/types_identity.rs crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs crates/z00z_wallets/src/core/tx/test_claim_tx.rs crates/z00z_storage/tests/test_claim_source_proof.rs && ! rg -n -F 'claim-v2' crates/z00z_wallets/src/core/tx/claim_auth.rs crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs crates/z00z_wallets/src/core/tx/claim_errors.rs crates/z00z_wallets/src/core/tx/claim_tx.rs crates/z00z_storage/src/assets/store_internal/store_query.rs crates/z00z_storage/src/checkpoint/build.rs crates/z00z_storage/src/assets/types_identity.rs crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs crates/z00z_wallets/src/core/tx/test_claim_tx.rs crates/z00z_storage/tests/test_claim_source_proof.rs && ! rg -n '\broot_ver\(' crates/z00z_wallets/src/core/tx/claim_auth.rs crates/z00z_wallets/src/core/tx/claim_tx_helpers.rs crates/z00z_wallets/src/core/tx/claim_tx_verifier_impl_proof.rs crates/z00z_wallets/src/core/tx/claim_errors.rs crates/z00z_wallets/src/core/tx/claim_tx.rs crates/z00z_storage/src/assets/store_internal/store_query.rs crates/z00z_storage/src/checkpoint/build.rs crates/z00z_storage/src/assets/types_identity.rs crates/z00z_simulator/tests/test_claim_pkg_crypto_support.rs crates/z00z_wallets/src/core/tx/test_claim_tx.rs crates/z00z_storage/tests/test_claim_source_proof.rs</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix all issues and warnings after each pass, and stop only after at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>Every live wallet/storage/simulator claim consumer owned by the embedded rows now compiles and validates against `CLAIM_ROOT_VERSION`, `root_version`, and `SourceRootVersion`, and the deleted `ClaimRootVer` or `claim_v2` residue no longer survives in the row-owned claim consumers/tests under `crates/**/*.rs`.</done>
</task>

<task type="auto">
  <name>036-23.C Write a truthful closeout summary for the full claim rename table and coverage-proof table</name>
  <files>.planning/phases/036-rename/036-23-SUMMARY.md</files>
  <action>Write `036-23-SUMMARY.md` as a row-exact closeout for the embedded claim rename tables. Record both file renames, the `ClaimRootVer` retirement, the `CLAIM_ROOT_VERSION` export, the root_version spelling migration, the `SourceRootVersion` rename, the row-9 `claim_contract` domain-label migration, the exact targeted validations run, and the post-change residual scans across `crates/**/*.rs`. Re-run the code discovery scan for `ClaimRootVer`, `SourceRootVer`, `claim_v2`, `claim-v2`, and `root_ver(` across the live Rust tree, then classify the result truthfully against the embedded coverage-proof table instead of collapsing everything into a false zero-hit claim: the final closeout must distinguish zero live code-path residue from any intentional test-only legacy mirrors. State explicitly that archive/planning artifacts may still contain historical `claim_v2` mentions under `.planning/**` or `logs/**`, and state plainly that `036-20-SUMMARY.md` remains the authoritative partial shim-removal boundary for the overall phase.</action>
  <verify>
    <automated>rg -n 'CLAIM_ROOT_VERSION|claim_contract|SourceRootVersion|036-20-SUMMARY|coverage-proof|ClaimRootVer|claim_v2|claim-v2|root_version' .planning/phases/036-rename/036-23-SUMMARY.md</automated>
    <automated>! rg -n '\bClaimRootVer\b' crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**' && ! rg -n '\bSourceRootVer\b' crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**' && ! rg -n -F 'claim-v2' crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**' && ! rg -n '\broot_ver\(' crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**' && rg -n -F 'claim_v2' crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>Use `/.github/prompts/gsd-review-tasks-execution.prompt.md` in YOLO mode at least 3 times after the deterministic gates above; fix all issues and warnings after each pass, and stop only after at least 2 consecutive runs show no significant issues.</automated>
  </verify>
  <done>`036-23-SUMMARY.md` proves the full 21-row claim rename sweep with repository-backed evidence, reconciles the embedded coverage-proof table against the post-change code scan, and preserves the separate partial truth already recorded in `036-20-SUMMARY.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim owner module -> encoded claim statement contract | Renaming the owner file, deleting `ClaimRootVer`, or changing `root_version` handling in the wrong way could silently change on-wire claim statement bytes or mismatch behavior. |
| claim owner module -> domain-separated hash contract | Row 9 intentionally changes the statement-hash label from `claim_v2` to `claim_contract`; widening that migration beyond the owned claim statement seam would create unintended compatibility breakage. |
| crypto owner rename -> wallet/storage/simulator consumers | Leaving live consumers on `ClaimRootVer`, `SourceRootVer`, or `root_ver()` after the owner rename would create compile drift, runtime mismatch drift, or false-positive test coverage. |
| summary closeout -> phase truth | A false zero-hit claim across `ClaimRootVer`/`claim_v2` scans could overstate the continuation and hide remaining residue or the still-open `036-20` phase boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-23-01 | T | `crates/z00z_crypto/src/claim/claim_contract.rs` | mitigate | Restrict owner edits to the embedded rows, preserve `CLAIM_TAG` and proof-version framing, and prove the new owner surface with the crypto integration test plus residue scans. |
| T-036-23-02 | T | `claim_stmt_hash()` domain label | mitigate | Treat `claim_v2` -> `claim_contract` as a deliberate row-owned compatibility break only at the claim statement hash seam and update only the tests that intentionally mirror that label. |
| T-036-23-03 | D | wallet/storage/simulator claim consumers | mitigate | Update direct consumers in the same execution wave and run targeted storage/wallet/simulator tests plus the broader release-style cargo gate before claiming closure. |
| T-036-23-04 | R | `036-23-SUMMARY.md` closeout narrative | mitigate | Require the summary to restate the embedded rename table, the coverage-proof table, the exact scan command, and the still-partial `036-20` boundary instead of relying on broad claims. |
</threat_model>

<verification>
This continuation closes only when the embedded 21-row claim table and coverage-proof table are reconciled exactly, the owner module and test files are renamed, the deleted `ClaimRootVer` type no longer survives on the live code path, the direct consumers validate against `CLAIM_ROOT_VERSION` and `root_version`, and the summary records the claim-contract domain-label migration plus the still-open `036-20` phase boundary truthfully.
</verification>

<success_criteria>
`036-23 Rename The Claim Contract Surface And Retire ClaimRootVer Without Semantic Drift` is complete only when every embedded row from `.planning/phases/036-rename/036-a6_claim-spec.md` has a repository-backed outcome, the live claim owner surface is `claim_contract` plus `CLAIM_ROOT_VERSION`, all direct code hits for the owned `ClaimRootVer`/`claim_v2`/`root_ver` surfaces inside `crates/**/*.rs` are cleared or truthfully classified, and `036-23-SUMMARY.md` proves that closure without collapsing the separate partial truth recorded in `036-20-SUMMARY.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-23-SUMMARY.md`.
</output>