---
phase: 036-rename
plan: 15
type: execute
wave: 5
depends_on: [036-11, 036-12, 036-13, 036-14]
files_modified:
  - .planning/phases/036-rename/036-a2-legacy-removing-spec.md
  - .planning/phases/036-rename/036-TODO-3.md
autonomous: true
requirements: [None]
must_haves:
  truths:
    - All deterministic gates for the touched crates rerun in the required order and pass or fail closed with explicit documentation.
  - A fresh residual Rust scan shows no unintended production `legacy` owner outside explicit blocker tracking or deletion.
  - Every surviving production `legacy` owner is documented explicitly as a blocking defect until deleted.
  artifacts:
    - path: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      provides: Final residual disposition after Wave 4 validation closure.
    - path: .planning/phases/036-rename/036-TODO-3.md
      provides: Final backlog status and residual-scan closure truth for the removal slice.
  key_links:
    - from: .planning/phases/036-rename/036-a2-legacy-removing-spec.md
      to: .planning/phases/036-rename/036-TODO-3.md
      via: final residual-scan reconciliation and zero-legacy closure truth
      pattern: no unintended production `legacy` owner remains undocumented
---

<!-- markdownlint-disable MD022 MD033 MD041 MD047 MD055 MD056 MD060 -->

# 036-15 PLAN

<objective>
Execute `036-15 Run Validation Closure And Residual Scan Proof` for the full planned legacy-removal continuation.

Purpose: rerun the deterministic gates in the required order, prove that no production `legacy` surface survives undocumented, and close the continuation slice truthfully.
Output: canonical validation closure in `036-a2-legacy-removing-spec.md` and `036-TODO-3.md` backed by a fresh residual Rust scan and the required review loop.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/copilot-instructions.md
@.github/skills/z00z-git-versioning/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/036-rename/036-a2-legacy-removing-spec.md
@.planning/phases/036-rename/036-TODO-3.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.github/requirements/Z00Z_DESIGN_FOUNDATION.md
</context>

<global_execution_gates>

- Validation order is mandatory: bootstrap gate first, then broader Rust/test validation when relevant, then task-specific residual scans, then repeated `/GSD-Review-Tasks-Execution` YOLO passes until at least two consecutive runs report no significant issues.
- Do not claim closure from grep-only residue counts. Every surviving production `legacy` owner must be categorized explicitly as a blocking deletion defect until deleted.
- Exclude the protected `crates/z00z_crypto/tari/` vendor subtree from residue ownership claims.
- Do not reopen the completed `036-04` through `036-10` embedded-versioning closure under the label of legacy removal. This plan closes only the `036-a2` / `036-TODO-3` continuation slice.
- If the executor must create a git commit during execution, use the repository-owned `/z00z-git-versioning` workflow instead of ad hoc git commands.

</global_execution_gates>

<tasks>

<task type="auto">
  <name>036-15.A Rerun deterministic validation gates for the touched crates</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — `Validation Requirements`
    - `.planning/phases/036-rename/036-TODO-3.md` — `036-15` section
    - The diffs and summaries produced by `036-13` and `036-14`
  </read_first>
  <action>Rerun the deterministic validation suite for the crates touched by the continuation: bootstrap gate first, then the broader release-style Rust/test gate, then any crate-specific reruns needed to prove storage, wallet, simulator, crypto, and utility residue changes. Fail closed on the first regression; do not proceed to residual-scan closure with a red deterministic gate.</action>
  <required_test_surfaces>
    Bootstrap gate, broader release-style Rust/test gate, and crate-specific reruns for touched residue classes
  </required_test_surfaces>
  <acceptance_criteria>
    - Deterministic validation ran in the required order.
    - Any regression blocks closure until fixed.
    - Validation scope covers every crate touched by Waves 2-3.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>cargo test --release --features test-fast --features wallet_debug_dump</automated>
    <automated>cargo test -p z00z_simulator --release --features test-fast --features wallet_debug_dump</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>All required deterministic validation gates are green, or the plan is fail-closed with regressions fixed before continuing.</done>
</task>

<task type="auto">
  <name>036-15.B Run the residual Rust scan and fail on every surviving production owner</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md` — current survivor lists and blocker notes
    - `.planning/phases/036-rename/036-TODO-3.md` — `Detailed Legacy Removal Table` and `036-15` section
  </read_first>
  <action>Run a fresh substring-based residual Rust scan for `legacy|Legacy` outside Tari, compare the hits against the canonical removal table, and treat every surviving production owner as a blocker until deleted. Any new uncovered production owner is a blocker: add it back into the canonical removal docs before closure. Preserve the verified empty `examples/**/*.rs` and `benches/**/*.rs` buckets unless the fresh scan disproves them. Do not use a word-boundary-only pattern here; it misses underscore-linked identifiers such as `legacy_wallet` and can produce a false zero-result.</action>
  <required_test_surfaces>
    Residual Rust scan and survivor-to-authority reconciliation
  </required_test_surfaces>
  <acceptance_criteria>
    - No unintended production `legacy` owner remains undocumented.
    - Any newly found owner is pushed back into the canonical docs before closure.
    - Empty example/bench buckets remain truthful.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "legacy|Legacy" crates --glob '*.rs' --glob '!crates/z00z_crypto/tari/**'</automated>
    <automated>rg -n "examples/\*\*/\.rs|benches/\*\*/\.rs" .planning/phases/036-rename/036-a2-legacy-removing-spec.md .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The fresh residual scan is reconciled completely against the canonical removal docs, with no surviving production owner treated as acceptable residue.</done>
</task>

<task type="auto">
  <name>036-15.C Write the final zero-legacy closure into the canonical removal docs</name>
  <files>.planning/phases/036-rename/036-a2-legacy-removing-spec.md, .planning/phases/036-rename/036-TODO-3.md</files>
  <read_first>
    - `.planning/phases/036-rename/036-a2-legacy-removing-spec.md`
    - `.planning/phases/036-rename/036-TODO-3.md`
    - Validation outputs from tasks `036-15.A` and `036-15.B`
  </read_first>
  <action>Update `036-a2-legacy-removing-spec.md` and `036-TODO-3.md` with the final Wave 4 closure truth: which production owners were deleted, which blocked rows remain open deletion defects, and what residual scan evidence backs that state. Keep the closure scoped to the `036-a2`/`036-TODO-3` continuation and do not restate or alter the completed `036-04` through `036-10` truth.</action>
  <required_test_surfaces>
    Canonical closure wording and survivor-list consistency
  </required_test_surfaces>
  <acceptance_criteria>
    - Final closure wording matches deterministic validation and the fresh residual scan.
    - No surviving production owner is described as approved residue.
    - The docs do not over-claim completion beyond what the scan and tests proved.
  </acceptance_criteria>
  <verify>
    <automated>./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh</automated>
    <automated>rg -n "deleted|blocked defect|residual scan|closure" .planning/phases/036-rename/036-a2-legacy-removing-spec.md .planning/phases/036-rename/036-TODO-3.md</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode after deterministic checks for review pass 1; any material issue or warning blocks closure until fixed and reverified.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 2 after fixing any material findings and rerunning deterministic checks.</automated>
    <automated>Run `/GSD-Review-Tasks-Execution` in YOLO mode for review pass 3 after fixing any material findings and rerunning deterministic checks; continue until at least two consecutive runs report no significant issues.</automated>
  </verify>
  <done>The canonical removal docs truthfully close the continuation slice with a fresh residual-scan-backed zero-legacy target state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deterministic gates -> closure claim | Closure is invalid if the required gate order was not executed or if any regression remains. |
| residual scan -> closure claim | Surviving `legacy` owners must be treated as blocking defects before closure is claimed. |
| continuation docs -> previous Phase 036 closure | This validation slice must not rewrite the already completed embedded-versioning closure truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-036-15-01 | T | validation ordering | mitigate | Enforce bootstrap first, then broader Rust/test validation, then residual scans and review passes before closure wording. |
| T-036-15-02 | R | residual inventory | mitigate | Reconcile every fresh residual-scan hit against the canonical removal docs before closure and treat survivors as defects. |
| T-036-15-03 | E | cross-slice closure drift | mitigate | Keep final wording scoped to `036-a2` / `036-TODO-3` and explicitly preserve the completed `036-04` through `036-10` truth. |
</threat_model>

<verification>
This plan closes only when deterministic gates are green, the fresh residual scan is fully reconciled, and every surviving production `legacy` owner is treated explicitly as a blocker or deleted.
</verification>

<success_criteria>
`036-15 Run Validation Closure And Residual Scan Proof` is complete only when no unintended production `legacy` owner remains outside explicit blocker tracking or deletion.</success_criteria>

<output>
After completion, create `.planning/phases/036-rename/036-15-SUMMARY.md`.
</output>